/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};

/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {

/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId])
/******/ 			return installedModules[moduleId].exports;

/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			exports: {},
/******/ 			id: moduleId,
/******/ 			loaded: false
/******/ 		};

/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);

/******/ 		// Flag the module as loaded
/******/ 		module.loaded = true;

/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}


/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;

/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;

/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "/semanticTtajdemo/";

/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(0);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports, __webpack_require__) {

	__webpack_require__(1);
	module.exports = __webpack_require__(326);


/***/ }),
/* 1 */
/***/ (function(module, exports, __webpack_require__) {

	/* WEBPACK VAR INJECTION */(function(global) {"use strict";

	__webpack_require__(2);

	__webpack_require__(322);

	__webpack_require__(323);

	if (global._babelPolyfill) {
	  throw new Error("only one instance of babel-polyfill is allowed");
	}
	global._babelPolyfill = true;

	var DEFINE_PROPERTY = "defineProperty";
	function define(O, key, value) {
	  O[key] || Object[DEFINE_PROPERTY](O, key, {
	    writable: true,
	    configurable: true,
	    value: value
	  });
	}

	define(String.prototype, "padLeft", "".padStart);
	define(String.prototype, "padRight", "".padEnd);

	"pop,reverse,shift,keys,values,entries,indexOf,every,some,forEach,map,filter,find,findIndex,includes,join,slice,concat,push,splice,unshift,sort,lastIndexOf,reduce,reduceRight,copyWithin,fill".split(",").forEach(function (key) {
	  [][key] && define(Array, key, Function.call.bind([][key]));
	});
	/* WEBPACK VAR INJECTION */}.call(exports, (function() { return this; }())))

/***/ }),
/* 2 */
/***/ (function(module, exports, __webpack_require__) {

	__webpack_require__(3);
	__webpack_require__(51);
	__webpack_require__(52);
	__webpack_require__(53);
	__webpack_require__(54);
	__webpack_require__(56);
	__webpack_require__(59);
	__webpack_require__(60);
	__webpack_require__(61);
	__webpack_require__(62);
	__webpack_require__(63);
	__webpack_require__(64);
	__webpack_require__(65);
	__webpack_require__(66);
	__webpack_require__(67);
	__webpack_require__(69);
	__webpack_require__(71);
	__webpack_require__(73);
	__webpack_require__(75);
	__webpack_require__(78);
	__webpack_require__(79);
	__webpack_require__(80);
	__webpack_require__(84);
	__webpack_require__(86);
	__webpack_require__(88);
	__webpack_require__(91);
	__webpack_require__(92);
	__webpack_require__(93);
	__webpack_require__(94);
	__webpack_require__(96);
	__webpack_require__(97);
	__webpack_require__(98);
	__webpack_require__(99);
	__webpack_require__(100);
	__webpack_require__(101);
	__webpack_require__(102);
	__webpack_require__(104);
	__webpack_require__(105);
	__webpack_require__(106);
	__webpack_require__(108);
	__webpack_require__(109);
	__webpack_require__(110);
	__webpack_require__(112);
	__webpack_require__(114);
	__webpack_require__(115);
	__webpack_require__(116);
	__webpack_require__(117);
	__webpack_require__(118);
	__webpack_require__(119);
	__webpack_require__(120);
	__webpack_require__(121);
	__webpack_require__(122);
	__webpack_require__(123);
	__webpack_require__(124);
	__webpack_require__(125);
	__webpack_require__(126);
	__webpack_require__(131);
	__webpack_require__(132);
	__webpack_require__(136);
	__webpack_require__(137);
	__webpack_require__(138);
	__webpack_require__(139);
	__webpack_require__(141);
	__webpack_require__(142);
	__webpack_require__(143);
	__webpack_require__(144);
	__webpack_require__(145);
	__webpack_require__(146);
	__webpack_require__(147);
	__webpack_require__(148);
	__webpack_require__(149);
	__webpack_require__(150);
	__webpack_require__(151);
	__webpack_require__(152);
	__webpack_require__(153);
	__webpack_require__(154);
	__webpack_require__(155);
	__webpack_require__(157);
	__webpack_require__(158);
	__webpack_require__(160);
	__webpack_require__(161);
	__webpack_require__(167);
	__webpack_require__(168);
	__webpack_require__(170);
	__webpack_require__(171);
	__webpack_require__(172);
	__webpack_require__(176);
	__webpack_require__(177);
	__webpack_require__(178);
	__webpack_require__(179);
	__webpack_require__(180);
	__webpack_require__(182);
	__webpack_require__(183);
	__webpack_require__(184);
	__webpack_require__(185);
	__webpack_require__(188);
	__webpack_require__(190);
	__webpack_require__(191);
	__webpack_require__(192);
	__webpack_require__(194);
	__webpack_require__(196);
	__webpack_require__(198);
	__webpack_require__(199);
	__webpack_require__(200);
	__webpack_require__(202);
	__webpack_require__(203);
	__webpack_require__(204);
	__webpack_require__(205);
	__webpack_require__(215);
	__webpack_require__(219);
	__webpack_require__(220);
	__webpack_require__(222);
	__webpack_require__(223);
	__webpack_require__(227);
	__webpack_require__(228);
	__webpack_require__(230);
	__webpack_require__(231);
	__webpack_require__(232);
	__webpack_require__(233);
	__webpack_require__(234);
	__webpack_require__(235);
	__webpack_require__(236);
	__webpack_require__(237);
	__webpack_require__(238);
	__webpack_require__(239);
	__webpack_require__(240);
	__webpack_require__(241);
	__webpack_require__(242);
	__webpack_require__(243);
	__webpack_require__(244);
	__webpack_require__(245);
	__webpack_require__(246);
	__webpack_require__(247);
	__webpack_require__(248);
	__webpack_require__(250);
	__webpack_require__(251);
	__webpack_require__(252);
	__webpack_require__(253);
	__webpack_require__(254);
	__webpack_require__(256);
	__webpack_require__(257);
	__webpack_require__(258);
	__webpack_require__(260);
	__webpack_require__(261);
	__webpack_require__(262);
	__webpack_require__(263);
	__webpack_require__(264);
	__webpack_require__(265);
	__webpack_require__(266);
	__webpack_require__(267);
	__webpack_require__(269);
	__webpack_require__(270);
	__webpack_require__(272);
	__webpack_require__(273);
	__webpack_require__(274);
	__webpack_require__(275);
	__webpack_require__(278);
	__webpack_require__(279);
	__webpack_require__(281);
	__webpack_require__(282);
	__webpack_require__(283);
	__webpack_require__(284);
	__webpack_require__(286);
	__webpack_require__(287);
	__webpack_require__(288);
	__webpack_require__(289);
	__webpack_require__(290);
	__webpack_require__(291);
	__webpack_require__(292);
	__webpack_require__(293);
	__webpack_require__(294);
	__webpack_require__(295);
	__webpack_require__(297);
	__webpack_require__(298);
	__webpack_require__(299);
	__webpack_require__(300);
	__webpack_require__(301);
	__webpack_require__(302);
	__webpack_require__(303);
	__webpack_require__(304);
	__webpack_require__(305);
	__webpack_require__(306);
	__webpack_require__(307);
	__webpack_require__(309);
	__webpack_require__(310);
	__webpack_require__(311);
	__webpack_require__(312);
	__webpack_require__(313);
	__webpack_require__(314);
	__webpack_require__(315);
	__webpack_require__(316);
	__webpack_require__(317);
	__webpack_require__(318);
	__webpack_require__(319);
	__webpack_require__(320);
	__webpack_require__(321);
	module.exports = __webpack_require__(9);


/***/ }),
/* 3 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// ECMAScript 6 symbols shim
	var global = __webpack_require__(4);
	var has = __webpack_require__(5);
	var DESCRIPTORS = __webpack_require__(6);
	var $export = __webpack_require__(8);
	var redefine = __webpack_require__(18);
	var META = __webpack_require__(22).KEY;
	var $fails = __webpack_require__(7);
	var shared = __webpack_require__(23);
	var setToStringTag = __webpack_require__(24);
	var uid = __webpack_require__(19);
	var wks = __webpack_require__(25);
	var wksExt = __webpack_require__(26);
	var wksDefine = __webpack_require__(27);
	var enumKeys = __webpack_require__(29);
	var isArray = __webpack_require__(44);
	var anObject = __webpack_require__(12);
	var toIObject = __webpack_require__(32);
	var toPrimitive = __webpack_require__(16);
	var createDesc = __webpack_require__(17);
	var _create = __webpack_require__(45);
	var gOPNExt = __webpack_require__(48);
	var $GOPD = __webpack_require__(50);
	var $DP = __webpack_require__(11);
	var $keys = __webpack_require__(30);
	var gOPD = $GOPD.f;
	var dP = $DP.f;
	var gOPN = gOPNExt.f;
	var $Symbol = global.Symbol;
	var $JSON = global.JSON;
	var _stringify = $JSON && $JSON.stringify;
	var PROTOTYPE = 'prototype';
	var HIDDEN = wks('_hidden');
	var TO_PRIMITIVE = wks('toPrimitive');
	var isEnum = {}.propertyIsEnumerable;
	var SymbolRegistry = shared('symbol-registry');
	var AllSymbols = shared('symbols');
	var OPSymbols = shared('op-symbols');
	var ObjectProto = Object[PROTOTYPE];
	var USE_NATIVE = typeof $Symbol == 'function';
	var QObject = global.QObject;
	// Don't use setters in Qt Script, https://github.com/zloirock/core-js/issues/173
	var setter = !QObject || !QObject[PROTOTYPE] || !QObject[PROTOTYPE].findChild;

	// fallback for old Android, https://code.google.com/p/v8/issues/detail?id=687
	var setSymbolDesc = DESCRIPTORS && $fails(function () {
	  return _create(dP({}, 'a', {
	    get: function () { return dP(this, 'a', { value: 7 }).a; }
	  })).a != 7;
	}) ? function (it, key, D) {
	  var protoDesc = gOPD(ObjectProto, key);
	  if (protoDesc) delete ObjectProto[key];
	  dP(it, key, D);
	  if (protoDesc && it !== ObjectProto) dP(ObjectProto, key, protoDesc);
	} : dP;

	var wrap = function (tag) {
	  var sym = AllSymbols[tag] = _create($Symbol[PROTOTYPE]);
	  sym._k = tag;
	  return sym;
	};

	var isSymbol = USE_NATIVE && typeof $Symbol.iterator == 'symbol' ? function (it) {
	  return typeof it == 'symbol';
	} : function (it) {
	  return it instanceof $Symbol;
	};

	var $defineProperty = function defineProperty(it, key, D) {
	  if (it === ObjectProto) $defineProperty(OPSymbols, key, D);
	  anObject(it);
	  key = toPrimitive(key, true);
	  anObject(D);
	  if (has(AllSymbols, key)) {
	    if (!D.enumerable) {
	      if (!has(it, HIDDEN)) dP(it, HIDDEN, createDesc(1, {}));
	      it[HIDDEN][key] = true;
	    } else {
	      if (has(it, HIDDEN) && it[HIDDEN][key]) it[HIDDEN][key] = false;
	      D = _create(D, { enumerable: createDesc(0, false) });
	    } return setSymbolDesc(it, key, D);
	  } return dP(it, key, D);
	};
	var $defineProperties = function defineProperties(it, P) {
	  anObject(it);
	  var keys = enumKeys(P = toIObject(P));
	  var i = 0;
	  var l = keys.length;
	  var key;
	  while (l > i) $defineProperty(it, key = keys[i++], P[key]);
	  return it;
	};
	var $create = function create(it, P) {
	  return P === undefined ? _create(it) : $defineProperties(_create(it), P);
	};
	var $propertyIsEnumerable = function propertyIsEnumerable(key) {
	  var E = isEnum.call(this, key = toPrimitive(key, true));
	  if (this === ObjectProto && has(AllSymbols, key) && !has(OPSymbols, key)) return false;
	  return E || !has(this, key) || !has(AllSymbols, key) || has(this, HIDDEN) && this[HIDDEN][key] ? E : true;
	};
	var $getOwnPropertyDescriptor = function getOwnPropertyDescriptor(it, key) {
	  it = toIObject(it);
	  key = toPrimitive(key, true);
	  if (it === ObjectProto && has(AllSymbols, key) && !has(OPSymbols, key)) return;
	  var D = gOPD(it, key);
	  if (D && has(AllSymbols, key) && !(has(it, HIDDEN) && it[HIDDEN][key])) D.enumerable = true;
	  return D;
	};
	var $getOwnPropertyNames = function getOwnPropertyNames(it) {
	  var names = gOPN(toIObject(it));
	  var result = [];
	  var i = 0;
	  var key;
	  while (names.length > i) {
	    if (!has(AllSymbols, key = names[i++]) && key != HIDDEN && key != META) result.push(key);
	  } return result;
	};
	var $getOwnPropertySymbols = function getOwnPropertySymbols(it) {
	  var IS_OP = it === ObjectProto;
	  var names = gOPN(IS_OP ? OPSymbols : toIObject(it));
	  var result = [];
	  var i = 0;
	  var key;
	  while (names.length > i) {
	    if (has(AllSymbols, key = names[i++]) && (IS_OP ? has(ObjectProto, key) : true)) result.push(AllSymbols[key]);
	  } return result;
	};

	// 19.4.1.1 Symbol([description])
	if (!USE_NATIVE) {
	  $Symbol = function Symbol() {
	    if (this instanceof $Symbol) throw TypeError('Symbol is not a constructor!');
	    var tag = uid(arguments.length > 0 ? arguments[0] : undefined);
	    var $set = function (value) {
	      if (this === ObjectProto) $set.call(OPSymbols, value);
	      if (has(this, HIDDEN) && has(this[HIDDEN], tag)) this[HIDDEN][tag] = false;
	      setSymbolDesc(this, tag, createDesc(1, value));
	    };
	    if (DESCRIPTORS && setter) setSymbolDesc(ObjectProto, tag, { configurable: true, set: $set });
	    return wrap(tag);
	  };
	  redefine($Symbol[PROTOTYPE], 'toString', function toString() {
	    return this._k;
	  });

	  $GOPD.f = $getOwnPropertyDescriptor;
	  $DP.f = $defineProperty;
	  __webpack_require__(49).f = gOPNExt.f = $getOwnPropertyNames;
	  __webpack_require__(43).f = $propertyIsEnumerable;
	  __webpack_require__(42).f = $getOwnPropertySymbols;

	  if (DESCRIPTORS && !__webpack_require__(28)) {
	    redefine(ObjectProto, 'propertyIsEnumerable', $propertyIsEnumerable, true);
	  }

	  wksExt.f = function (name) {
	    return wrap(wks(name));
	  };
	}

	$export($export.G + $export.W + $export.F * !USE_NATIVE, { Symbol: $Symbol });

	for (var es6Symbols = (
	  // 19.4.2.2, 19.4.2.3, 19.4.2.4, 19.4.2.6, 19.4.2.8, 19.4.2.9, 19.4.2.10, 19.4.2.11, 19.4.2.12, 19.4.2.13, 19.4.2.14
	  'hasInstance,isConcatSpreadable,iterator,match,replace,search,species,split,toPrimitive,toStringTag,unscopables'
	).split(','), j = 0; es6Symbols.length > j;)wks(es6Symbols[j++]);

	for (var wellKnownSymbols = $keys(wks.store), k = 0; wellKnownSymbols.length > k;) wksDefine(wellKnownSymbols[k++]);

	$export($export.S + $export.F * !USE_NATIVE, 'Symbol', {
	  // 19.4.2.1 Symbol.for(key)
	  'for': function (key) {
	    return has(SymbolRegistry, key += '')
	      ? SymbolRegistry[key]
	      : SymbolRegistry[key] = $Symbol(key);
	  },
	  // 19.4.2.5 Symbol.keyFor(sym)
	  keyFor: function keyFor(sym) {
	    if (!isSymbol(sym)) throw TypeError(sym + ' is not a symbol!');
	    for (var key in SymbolRegistry) if (SymbolRegistry[key] === sym) return key;
	  },
	  useSetter: function () { setter = true; },
	  useSimple: function () { setter = false; }
	});

	$export($export.S + $export.F * !USE_NATIVE, 'Object', {
	  // 19.1.2.2 Object.create(O [, Properties])
	  create: $create,
	  // 19.1.2.4 Object.defineProperty(O, P, Attributes)
	  defineProperty: $defineProperty,
	  // 19.1.2.3 Object.defineProperties(O, Properties)
	  defineProperties: $defineProperties,
	  // 19.1.2.6 Object.getOwnPropertyDescriptor(O, P)
	  getOwnPropertyDescriptor: $getOwnPropertyDescriptor,
	  // 19.1.2.7 Object.getOwnPropertyNames(O)
	  getOwnPropertyNames: $getOwnPropertyNames,
	  // 19.1.2.8 Object.getOwnPropertySymbols(O)
	  getOwnPropertySymbols: $getOwnPropertySymbols
	});

	// 24.3.2 JSON.stringify(value [, replacer [, space]])
	$JSON && $export($export.S + $export.F * (!USE_NATIVE || $fails(function () {
	  var S = $Symbol();
	  // MS Edge converts symbol values to JSON as {}
	  // WebKit converts symbol values to JSON as null
	  // V8 throws on boxed symbols
	  return _stringify([S]) != '[null]' || _stringify({ a: S }) != '{}' || _stringify(Object(S)) != '{}';
	})), 'JSON', {
	  stringify: function stringify(it) {
	    if (it === undefined || isSymbol(it)) return; // IE8 returns string on undefined
	    var args = [it];
	    var i = 1;
	    var replacer, $replacer;
	    while (arguments.length > i) args.push(arguments[i++]);
	    replacer = args[1];
	    if (typeof replacer == 'function') $replacer = replacer;
	    if ($replacer || !isArray(replacer)) replacer = function (key, value) {
	      if ($replacer) value = $replacer.call(this, key, value);
	      if (!isSymbol(value)) return value;
	    };
	    args[1] = replacer;
	    return _stringify.apply($JSON, args);
	  }
	});

	// 19.4.3.4 Symbol.prototype[@@toPrimitive](hint)
	$Symbol[PROTOTYPE][TO_PRIMITIVE] || __webpack_require__(10)($Symbol[PROTOTYPE], TO_PRIMITIVE, $Symbol[PROTOTYPE].valueOf);
	// 19.4.3.5 Symbol.prototype[@@toStringTag]
	setToStringTag($Symbol, 'Symbol');
	// 20.2.1.9 Math[@@toStringTag]
	setToStringTag(Math, 'Math', true);
	// 24.3.3 JSON[@@toStringTag]
	setToStringTag(global.JSON, 'JSON', true);


/***/ }),
/* 4 */
/***/ (function(module, exports) {

	// https://github.com/zloirock/core-js/issues/86#issuecomment-115759028
	var global = module.exports = typeof window != 'undefined' && window.Math == Math
	  ? window : typeof self != 'undefined' && self.Math == Math ? self
	  // eslint-disable-next-line no-new-func
	  : Function('return this')();
	if (typeof __g == 'number') __g = global; // eslint-disable-line no-undef


/***/ }),
/* 5 */
/***/ (function(module, exports) {

	var hasOwnProperty = {}.hasOwnProperty;
	module.exports = function (it, key) {
	  return hasOwnProperty.call(it, key);
	};


/***/ }),
/* 6 */
/***/ (function(module, exports, __webpack_require__) {

	// Thank's IE8 for his funny defineProperty
	module.exports = !__webpack_require__(7)(function () {
	  return Object.defineProperty({}, 'a', { get: function () { return 7; } }).a != 7;
	});


/***/ }),
/* 7 */
/***/ (function(module, exports) {

	module.exports = function (exec) {
	  try {
	    return !!exec();
	  } catch (e) {
	    return true;
	  }
	};


/***/ }),
/* 8 */
/***/ (function(module, exports, __webpack_require__) {

	var global = __webpack_require__(4);
	var core = __webpack_require__(9);
	var hide = __webpack_require__(10);
	var redefine = __webpack_require__(18);
	var ctx = __webpack_require__(20);
	var PROTOTYPE = 'prototype';

	var $export = function (type, name, source) {
	  var IS_FORCED = type & $export.F;
	  var IS_GLOBAL = type & $export.G;
	  var IS_STATIC = type & $export.S;
	  var IS_PROTO = type & $export.P;
	  var IS_BIND = type & $export.B;
	  var target = IS_GLOBAL ? global : IS_STATIC ? global[name] || (global[name] = {}) : (global[name] || {})[PROTOTYPE];
	  var exports = IS_GLOBAL ? core : core[name] || (core[name] = {});
	  var expProto = exports[PROTOTYPE] || (exports[PROTOTYPE] = {});
	  var key, own, out, exp;
	  if (IS_GLOBAL) source = name;
	  for (key in source) {
	    // contains in native
	    own = !IS_FORCED && target && target[key] !== undefined;
	    // export native or passed
	    out = (own ? target : source)[key];
	    // bind timers to global for call from export context
	    exp = IS_BIND && own ? ctx(out, global) : IS_PROTO && typeof out == 'function' ? ctx(Function.call, out) : out;
	    // extend global
	    if (target) redefine(target, key, out, type & $export.U);
	    // export
	    if (exports[key] != out) hide(exports, key, exp);
	    if (IS_PROTO && expProto[key] != out) expProto[key] = out;
	  }
	};
	global.core = core;
	// type bitmap
	$export.F = 1;   // forced
	$export.G = 2;   // global
	$export.S = 4;   // static
	$export.P = 8;   // proto
	$export.B = 16;  // bind
	$export.W = 32;  // wrap
	$export.U = 64;  // safe
	$export.R = 128; // real proto method for `library`
	module.exports = $export;


/***/ }),
/* 9 */
/***/ (function(module, exports) {

	var core = module.exports = { version: '2.5.1' };
	if (typeof __e == 'number') __e = core; // eslint-disable-line no-undef


/***/ }),
/* 10 */
/***/ (function(module, exports, __webpack_require__) {

	var dP = __webpack_require__(11);
	var createDesc = __webpack_require__(17);
	module.exports = __webpack_require__(6) ? function (object, key, value) {
	  return dP.f(object, key, createDesc(1, value));
	} : function (object, key, value) {
	  object[key] = value;
	  return object;
	};


/***/ }),
/* 11 */
/***/ (function(module, exports, __webpack_require__) {

	var anObject = __webpack_require__(12);
	var IE8_DOM_DEFINE = __webpack_require__(14);
	var toPrimitive = __webpack_require__(16);
	var dP = Object.defineProperty;

	exports.f = __webpack_require__(6) ? Object.defineProperty : function defineProperty(O, P, Attributes) {
	  anObject(O);
	  P = toPrimitive(P, true);
	  anObject(Attributes);
	  if (IE8_DOM_DEFINE) try {
	    return dP(O, P, Attributes);
	  } catch (e) { /* empty */ }
	  if ('get' in Attributes || 'set' in Attributes) throw TypeError('Accessors not supported!');
	  if ('value' in Attributes) O[P] = Attributes.value;
	  return O;
	};


/***/ }),
/* 12 */
/***/ (function(module, exports, __webpack_require__) {

	var isObject = __webpack_require__(13);
	module.exports = function (it) {
	  if (!isObject(it)) throw TypeError(it + ' is not an object!');
	  return it;
	};


/***/ }),
/* 13 */
/***/ (function(module, exports) {

	module.exports = function (it) {
	  return typeof it === 'object' ? it !== null : typeof it === 'function';
	};


/***/ }),
/* 14 */
/***/ (function(module, exports, __webpack_require__) {

	module.exports = !__webpack_require__(6) && !__webpack_require__(7)(function () {
	  return Object.defineProperty(__webpack_require__(15)('div'), 'a', { get: function () { return 7; } }).a != 7;
	});


/***/ }),
/* 15 */
/***/ (function(module, exports, __webpack_require__) {

	var isObject = __webpack_require__(13);
	var document = __webpack_require__(4).document;
	// typeof document.createElement is 'object' in old IE
	var is = isObject(document) && isObject(document.createElement);
	module.exports = function (it) {
	  return is ? document.createElement(it) : {};
	};


/***/ }),
/* 16 */
/***/ (function(module, exports, __webpack_require__) {

	// 7.1.1 ToPrimitive(input [, PreferredType])
	var isObject = __webpack_require__(13);
	// instead of the ES6 spec version, we didn't implement @@toPrimitive case
	// and the second argument - flag - preferred type is a string
	module.exports = function (it, S) {
	  if (!isObject(it)) return it;
	  var fn, val;
	  if (S && typeof (fn = it.toString) == 'function' && !isObject(val = fn.call(it))) return val;
	  if (typeof (fn = it.valueOf) == 'function' && !isObject(val = fn.call(it))) return val;
	  if (!S && typeof (fn = it.toString) == 'function' && !isObject(val = fn.call(it))) return val;
	  throw TypeError("Can't convert object to primitive value");
	};


/***/ }),
/* 17 */
/***/ (function(module, exports) {

	module.exports = function (bitmap, value) {
	  return {
	    enumerable: !(bitmap & 1),
	    configurable: !(bitmap & 2),
	    writable: !(bitmap & 4),
	    value: value
	  };
	};


/***/ }),
/* 18 */
/***/ (function(module, exports, __webpack_require__) {

	var global = __webpack_require__(4);
	var hide = __webpack_require__(10);
	var has = __webpack_require__(5);
	var SRC = __webpack_require__(19)('src');
	var TO_STRING = 'toString';
	var $toString = Function[TO_STRING];
	var TPL = ('' + $toString).split(TO_STRING);

	__webpack_require__(9).inspectSource = function (it) {
	  return $toString.call(it);
	};

	(module.exports = function (O, key, val, safe) {
	  var isFunction = typeof val == 'function';
	  if (isFunction) has(val, 'name') || hide(val, 'name', key);
	  if (O[key] === val) return;
	  if (isFunction) has(val, SRC) || hide(val, SRC, O[key] ? '' + O[key] : TPL.join(String(key)));
	  if (O === global) {
	    O[key] = val;
	  } else if (!safe) {
	    delete O[key];
	    hide(O, key, val);
	  } else if (O[key]) {
	    O[key] = val;
	  } else {
	    hide(O, key, val);
	  }
	// add fake Function#toString for correct work wrapped methods / constructors with methods like LoDash isNative
	})(Function.prototype, TO_STRING, function toString() {
	  return typeof this == 'function' && this[SRC] || $toString.call(this);
	});


/***/ }),
/* 19 */
/***/ (function(module, exports) {

	var id = 0;
	var px = Math.random();
	module.exports = function (key) {
	  return 'Symbol('.concat(key === undefined ? '' : key, ')_', (++id + px).toString(36));
	};


/***/ }),
/* 20 */
/***/ (function(module, exports, __webpack_require__) {

	// optional / simple context binding
	var aFunction = __webpack_require__(21);
	module.exports = function (fn, that, length) {
	  aFunction(fn);
	  if (that === undefined) return fn;
	  switch (length) {
	    case 1: return function (a) {
	      return fn.call(that, a);
	    };
	    case 2: return function (a, b) {
	      return fn.call(that, a, b);
	    };
	    case 3: return function (a, b, c) {
	      return fn.call(that, a, b, c);
	    };
	  }
	  return function (/* ...args */) {
	    return fn.apply(that, arguments);
	  };
	};


/***/ }),
/* 21 */
/***/ (function(module, exports) {

	module.exports = function (it) {
	  if (typeof it != 'function') throw TypeError(it + ' is not a function!');
	  return it;
	};


/***/ }),
/* 22 */
/***/ (function(module, exports, __webpack_require__) {

	var META = __webpack_require__(19)('meta');
	var isObject = __webpack_require__(13);
	var has = __webpack_require__(5);
	var setDesc = __webpack_require__(11).f;
	var id = 0;
	var isExtensible = Object.isExtensible || function () {
	  return true;
	};
	var FREEZE = !__webpack_require__(7)(function () {
	  return isExtensible(Object.preventExtensions({}));
	});
	var setMeta = function (it) {
	  setDesc(it, META, { value: {
	    i: 'O' + ++id, // object ID
	    w: {}          // weak collections IDs
	  } });
	};
	var fastKey = function (it, create) {
	  // return primitive with prefix
	  if (!isObject(it)) return typeof it == 'symbol' ? it : (typeof it == 'string' ? 'S' : 'P') + it;
	  if (!has(it, META)) {
	    // can't set metadata to uncaught frozen object
	    if (!isExtensible(it)) return 'F';
	    // not necessary to add metadata
	    if (!create) return 'E';
	    // add missing metadata
	    setMeta(it);
	  // return object ID
	  } return it[META].i;
	};
	var getWeak = function (it, create) {
	  if (!has(it, META)) {
	    // can't set metadata to uncaught frozen object
	    if (!isExtensible(it)) return true;
	    // not necessary to add metadata
	    if (!create) return false;
	    // add missing metadata
	    setMeta(it);
	  // return hash weak collections IDs
	  } return it[META].w;
	};
	// add metadata on freeze-family methods calling
	var onFreeze = function (it) {
	  if (FREEZE && meta.NEED && isExtensible(it) && !has(it, META)) setMeta(it);
	  return it;
	};
	var meta = module.exports = {
	  KEY: META,
	  NEED: false,
	  fastKey: fastKey,
	  getWeak: getWeak,
	  onFreeze: onFreeze
	};


/***/ }),
/* 23 */
/***/ (function(module, exports, __webpack_require__) {

	var global = __webpack_require__(4);
	var SHARED = '__core-js_shared__';
	var store = global[SHARED] || (global[SHARED] = {});
	module.exports = function (key) {
	  return store[key] || (store[key] = {});
	};


/***/ }),
/* 24 */
/***/ (function(module, exports, __webpack_require__) {

	var def = __webpack_require__(11).f;
	var has = __webpack_require__(5);
	var TAG = __webpack_require__(25)('toStringTag');

	module.exports = function (it, tag, stat) {
	  if (it && !has(it = stat ? it : it.prototype, TAG)) def(it, TAG, { configurable: true, value: tag });
	};


/***/ }),
/* 25 */
/***/ (function(module, exports, __webpack_require__) {

	var store = __webpack_require__(23)('wks');
	var uid = __webpack_require__(19);
	var Symbol = __webpack_require__(4).Symbol;
	var USE_SYMBOL = typeof Symbol == 'function';

	var $exports = module.exports = function (name) {
	  return store[name] || (store[name] =
	    USE_SYMBOL && Symbol[name] || (USE_SYMBOL ? Symbol : uid)('Symbol.' + name));
	};

	$exports.store = store;


/***/ }),
/* 26 */
/***/ (function(module, exports, __webpack_require__) {

	exports.f = __webpack_require__(25);


/***/ }),
/* 27 */
/***/ (function(module, exports, __webpack_require__) {

	var global = __webpack_require__(4);
	var core = __webpack_require__(9);
	var LIBRARY = __webpack_require__(28);
	var wksExt = __webpack_require__(26);
	var defineProperty = __webpack_require__(11).f;
	module.exports = function (name) {
	  var $Symbol = core.Symbol || (core.Symbol = LIBRARY ? {} : global.Symbol || {});
	  if (name.charAt(0) != '_' && !(name in $Symbol)) defineProperty($Symbol, name, { value: wksExt.f(name) });
	};


/***/ }),
/* 28 */
/***/ (function(module, exports) {

	module.exports = false;


/***/ }),
/* 29 */
/***/ (function(module, exports, __webpack_require__) {

	// all enumerable object keys, includes symbols
	var getKeys = __webpack_require__(30);
	var gOPS = __webpack_require__(42);
	var pIE = __webpack_require__(43);
	module.exports = function (it) {
	  var result = getKeys(it);
	  var getSymbols = gOPS.f;
	  if (getSymbols) {
	    var symbols = getSymbols(it);
	    var isEnum = pIE.f;
	    var i = 0;
	    var key;
	    while (symbols.length > i) if (isEnum.call(it, key = symbols[i++])) result.push(key);
	  } return result;
	};


/***/ }),
/* 30 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.2.14 / 15.2.3.14 Object.keys(O)
	var $keys = __webpack_require__(31);
	var enumBugKeys = __webpack_require__(41);

	module.exports = Object.keys || function keys(O) {
	  return $keys(O, enumBugKeys);
	};


/***/ }),
/* 31 */
/***/ (function(module, exports, __webpack_require__) {

	var has = __webpack_require__(5);
	var toIObject = __webpack_require__(32);
	var arrayIndexOf = __webpack_require__(36)(false);
	var IE_PROTO = __webpack_require__(40)('IE_PROTO');

	module.exports = function (object, names) {
	  var O = toIObject(object);
	  var i = 0;
	  var result = [];
	  var key;
	  for (key in O) if (key != IE_PROTO) has(O, key) && result.push(key);
	  // Don't enum bug & hidden keys
	  while (names.length > i) if (has(O, key = names[i++])) {
	    ~arrayIndexOf(result, key) || result.push(key);
	  }
	  return result;
	};


/***/ }),
/* 32 */
/***/ (function(module, exports, __webpack_require__) {

	// to indexed object, toObject with fallback for non-array-like ES3 strings
	var IObject = __webpack_require__(33);
	var defined = __webpack_require__(35);
	module.exports = function (it) {
	  return IObject(defined(it));
	};


/***/ }),
/* 33 */
/***/ (function(module, exports, __webpack_require__) {

	// fallback for non-array-like ES3 and non-enumerable old V8 strings
	var cof = __webpack_require__(34);
	// eslint-disable-next-line no-prototype-builtins
	module.exports = Object('z').propertyIsEnumerable(0) ? Object : function (it) {
	  return cof(it) == 'String' ? it.split('') : Object(it);
	};


/***/ }),
/* 34 */
/***/ (function(module, exports) {

	var toString = {}.toString;

	module.exports = function (it) {
	  return toString.call(it).slice(8, -1);
	};


/***/ }),
/* 35 */
/***/ (function(module, exports) {

	// 7.2.1 RequireObjectCoercible(argument)
	module.exports = function (it) {
	  if (it == undefined) throw TypeError("Can't call method on  " + it);
	  return it;
	};


/***/ }),
/* 36 */
/***/ (function(module, exports, __webpack_require__) {

	// false -> Array#indexOf
	// true  -> Array#includes
	var toIObject = __webpack_require__(32);
	var toLength = __webpack_require__(37);
	var toAbsoluteIndex = __webpack_require__(39);
	module.exports = function (IS_INCLUDES) {
	  return function ($this, el, fromIndex) {
	    var O = toIObject($this);
	    var length = toLength(O.length);
	    var index = toAbsoluteIndex(fromIndex, length);
	    var value;
	    // Array#includes uses SameValueZero equality algorithm
	    // eslint-disable-next-line no-self-compare
	    if (IS_INCLUDES && el != el) while (length > index) {
	      value = O[index++];
	      // eslint-disable-next-line no-self-compare
	      if (value != value) return true;
	    // Array#indexOf ignores holes, Array#includes - not
	    } else for (;length > index; index++) if (IS_INCLUDES || index in O) {
	      if (O[index] === el) return IS_INCLUDES || index || 0;
	    } return !IS_INCLUDES && -1;
	  };
	};


/***/ }),
/* 37 */
/***/ (function(module, exports, __webpack_require__) {

	// 7.1.15 ToLength
	var toInteger = __webpack_require__(38);
	var min = Math.min;
	module.exports = function (it) {
	  return it > 0 ? min(toInteger(it), 0x1fffffffffffff) : 0; // pow(2, 53) - 1 == 9007199254740991
	};


/***/ }),
/* 38 */
/***/ (function(module, exports) {

	// 7.1.4 ToInteger
	var ceil = Math.ceil;
	var floor = Math.floor;
	module.exports = function (it) {
	  return isNaN(it = +it) ? 0 : (it > 0 ? floor : ceil)(it);
	};


/***/ }),
/* 39 */
/***/ (function(module, exports, __webpack_require__) {

	var toInteger = __webpack_require__(38);
	var max = Math.max;
	var min = Math.min;
	module.exports = function (index, length) {
	  index = toInteger(index);
	  return index < 0 ? max(index + length, 0) : min(index, length);
	};


/***/ }),
/* 40 */
/***/ (function(module, exports, __webpack_require__) {

	var shared = __webpack_require__(23)('keys');
	var uid = __webpack_require__(19);
	module.exports = function (key) {
	  return shared[key] || (shared[key] = uid(key));
	};


/***/ }),
/* 41 */
/***/ (function(module, exports) {

	// IE 8- don't enum bug keys
	module.exports = (
	  'constructor,hasOwnProperty,isPrototypeOf,propertyIsEnumerable,toLocaleString,toString,valueOf'
	).split(',');


/***/ }),
/* 42 */
/***/ (function(module, exports) {

	exports.f = Object.getOwnPropertySymbols;


/***/ }),
/* 43 */
/***/ (function(module, exports) {

	exports.f = {}.propertyIsEnumerable;


/***/ }),
/* 44 */
/***/ (function(module, exports, __webpack_require__) {

	// 7.2.2 IsArray(argument)
	var cof = __webpack_require__(34);
	module.exports = Array.isArray || function isArray(arg) {
	  return cof(arg) == 'Array';
	};


/***/ }),
/* 45 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.2.2 / 15.2.3.5 Object.create(O [, Properties])
	var anObject = __webpack_require__(12);
	var dPs = __webpack_require__(46);
	var enumBugKeys = __webpack_require__(41);
	var IE_PROTO = __webpack_require__(40)('IE_PROTO');
	var Empty = function () { /* empty */ };
	var PROTOTYPE = 'prototype';

	// Create object with fake `null` prototype: use iframe Object with cleared prototype
	var createDict = function () {
	  // Thrash, waste and sodomy: IE GC bug
	  var iframe = __webpack_require__(15)('iframe');
	  var i = enumBugKeys.length;
	  var lt = '<';
	  var gt = '>';
	  var iframeDocument;
	  iframe.style.display = 'none';
	  __webpack_require__(47).appendChild(iframe);
	  iframe.src = 'javascript:'; // eslint-disable-line no-script-url
	  // createDict = iframe.contentWindow.Object;
	  // html.removeChild(iframe);
	  iframeDocument = iframe.contentWindow.document;
	  iframeDocument.open();
	  iframeDocument.write(lt + 'script' + gt + 'document.F=Object' + lt + '/script' + gt);
	  iframeDocument.close();
	  createDict = iframeDocument.F;
	  while (i--) delete createDict[PROTOTYPE][enumBugKeys[i]];
	  return createDict();
	};

	module.exports = Object.create || function create(O, Properties) {
	  var result;
	  if (O !== null) {
	    Empty[PROTOTYPE] = anObject(O);
	    result = new Empty();
	    Empty[PROTOTYPE] = null;
	    // add "__proto__" for Object.getPrototypeOf polyfill
	    result[IE_PROTO] = O;
	  } else result = createDict();
	  return Properties === undefined ? result : dPs(result, Properties);
	};


/***/ }),
/* 46 */
/***/ (function(module, exports, __webpack_require__) {

	var dP = __webpack_require__(11);
	var anObject = __webpack_require__(12);
	var getKeys = __webpack_require__(30);

	module.exports = __webpack_require__(6) ? Object.defineProperties : function defineProperties(O, Properties) {
	  anObject(O);
	  var keys = getKeys(Properties);
	  var length = keys.length;
	  var i = 0;
	  var P;
	  while (length > i) dP.f(O, P = keys[i++], Properties[P]);
	  return O;
	};


/***/ }),
/* 47 */
/***/ (function(module, exports, __webpack_require__) {

	var document = __webpack_require__(4).document;
	module.exports = document && document.documentElement;


/***/ }),
/* 48 */
/***/ (function(module, exports, __webpack_require__) {

	// fallback for IE11 buggy Object.getOwnPropertyNames with iframe and window
	var toIObject = __webpack_require__(32);
	var gOPN = __webpack_require__(49).f;
	var toString = {}.toString;

	var windowNames = typeof window == 'object' && window && Object.getOwnPropertyNames
	  ? Object.getOwnPropertyNames(window) : [];

	var getWindowNames = function (it) {
	  try {
	    return gOPN(it);
	  } catch (e) {
	    return windowNames.slice();
	  }
	};

	module.exports.f = function getOwnPropertyNames(it) {
	  return windowNames && toString.call(it) == '[object Window]' ? getWindowNames(it) : gOPN(toIObject(it));
	};


/***/ }),
/* 49 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.2.7 / 15.2.3.4 Object.getOwnPropertyNames(O)
	var $keys = __webpack_require__(31);
	var hiddenKeys = __webpack_require__(41).concat('length', 'prototype');

	exports.f = Object.getOwnPropertyNames || function getOwnPropertyNames(O) {
	  return $keys(O, hiddenKeys);
	};


/***/ }),
/* 50 */
/***/ (function(module, exports, __webpack_require__) {

	var pIE = __webpack_require__(43);
	var createDesc = __webpack_require__(17);
	var toIObject = __webpack_require__(32);
	var toPrimitive = __webpack_require__(16);
	var has = __webpack_require__(5);
	var IE8_DOM_DEFINE = __webpack_require__(14);
	var gOPD = Object.getOwnPropertyDescriptor;

	exports.f = __webpack_require__(6) ? gOPD : function getOwnPropertyDescriptor(O, P) {
	  O = toIObject(O);
	  P = toPrimitive(P, true);
	  if (IE8_DOM_DEFINE) try {
	    return gOPD(O, P);
	  } catch (e) { /* empty */ }
	  if (has(O, P)) return createDesc(!pIE.f.call(O, P), O[P]);
	};


/***/ }),
/* 51 */
/***/ (function(module, exports, __webpack_require__) {

	var $export = __webpack_require__(8);
	// 19.1.2.2 / 15.2.3.5 Object.create(O [, Properties])
	$export($export.S, 'Object', { create: __webpack_require__(45) });


/***/ }),
/* 52 */
/***/ (function(module, exports, __webpack_require__) {

	var $export = __webpack_require__(8);
	// 19.1.2.4 / 15.2.3.6 Object.defineProperty(O, P, Attributes)
	$export($export.S + $export.F * !__webpack_require__(6), 'Object', { defineProperty: __webpack_require__(11).f });


/***/ }),
/* 53 */
/***/ (function(module, exports, __webpack_require__) {

	var $export = __webpack_require__(8);
	// 19.1.2.3 / 15.2.3.7 Object.defineProperties(O, Properties)
	$export($export.S + $export.F * !__webpack_require__(6), 'Object', { defineProperties: __webpack_require__(46) });


/***/ }),
/* 54 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.2.6 Object.getOwnPropertyDescriptor(O, P)
	var toIObject = __webpack_require__(32);
	var $getOwnPropertyDescriptor = __webpack_require__(50).f;

	__webpack_require__(55)('getOwnPropertyDescriptor', function () {
	  return function getOwnPropertyDescriptor(it, key) {
	    return $getOwnPropertyDescriptor(toIObject(it), key);
	  };
	});


/***/ }),
/* 55 */
/***/ (function(module, exports, __webpack_require__) {

	// most Object methods by ES6 should accept primitives
	var $export = __webpack_require__(8);
	var core = __webpack_require__(9);
	var fails = __webpack_require__(7);
	module.exports = function (KEY, exec) {
	  var fn = (core.Object || {})[KEY] || Object[KEY];
	  var exp = {};
	  exp[KEY] = exec(fn);
	  $export($export.S + $export.F * fails(function () { fn(1); }), 'Object', exp);
	};


/***/ }),
/* 56 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.2.9 Object.getPrototypeOf(O)
	var toObject = __webpack_require__(57);
	var $getPrototypeOf = __webpack_require__(58);

	__webpack_require__(55)('getPrototypeOf', function () {
	  return function getPrototypeOf(it) {
	    return $getPrototypeOf(toObject(it));
	  };
	});


/***/ }),
/* 57 */
/***/ (function(module, exports, __webpack_require__) {

	// 7.1.13 ToObject(argument)
	var defined = __webpack_require__(35);
	module.exports = function (it) {
	  return Object(defined(it));
	};


/***/ }),
/* 58 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.2.9 / 15.2.3.2 Object.getPrototypeOf(O)
	var has = __webpack_require__(5);
	var toObject = __webpack_require__(57);
	var IE_PROTO = __webpack_require__(40)('IE_PROTO');
	var ObjectProto = Object.prototype;

	module.exports = Object.getPrototypeOf || function (O) {
	  O = toObject(O);
	  if (has(O, IE_PROTO)) return O[IE_PROTO];
	  if (typeof O.constructor == 'function' && O instanceof O.constructor) {
	    return O.constructor.prototype;
	  } return O instanceof Object ? ObjectProto : null;
	};


/***/ }),
/* 59 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.2.14 Object.keys(O)
	var toObject = __webpack_require__(57);
	var $keys = __webpack_require__(30);

	__webpack_require__(55)('keys', function () {
	  return function keys(it) {
	    return $keys(toObject(it));
	  };
	});


/***/ }),
/* 60 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.2.7 Object.getOwnPropertyNames(O)
	__webpack_require__(55)('getOwnPropertyNames', function () {
	  return __webpack_require__(48).f;
	});


/***/ }),
/* 61 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.2.5 Object.freeze(O)
	var isObject = __webpack_require__(13);
	var meta = __webpack_require__(22).onFreeze;

	__webpack_require__(55)('freeze', function ($freeze) {
	  return function freeze(it) {
	    return $freeze && isObject(it) ? $freeze(meta(it)) : it;
	  };
	});


/***/ }),
/* 62 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.2.17 Object.seal(O)
	var isObject = __webpack_require__(13);
	var meta = __webpack_require__(22).onFreeze;

	__webpack_require__(55)('seal', function ($seal) {
	  return function seal(it) {
	    return $seal && isObject(it) ? $seal(meta(it)) : it;
	  };
	});


/***/ }),
/* 63 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.2.15 Object.preventExtensions(O)
	var isObject = __webpack_require__(13);
	var meta = __webpack_require__(22).onFreeze;

	__webpack_require__(55)('preventExtensions', function ($preventExtensions) {
	  return function preventExtensions(it) {
	    return $preventExtensions && isObject(it) ? $preventExtensions(meta(it)) : it;
	  };
	});


/***/ }),
/* 64 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.2.12 Object.isFrozen(O)
	var isObject = __webpack_require__(13);

	__webpack_require__(55)('isFrozen', function ($isFrozen) {
	  return function isFrozen(it) {
	    return isObject(it) ? $isFrozen ? $isFrozen(it) : false : true;
	  };
	});


/***/ }),
/* 65 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.2.13 Object.isSealed(O)
	var isObject = __webpack_require__(13);

	__webpack_require__(55)('isSealed', function ($isSealed) {
	  return function isSealed(it) {
	    return isObject(it) ? $isSealed ? $isSealed(it) : false : true;
	  };
	});


/***/ }),
/* 66 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.2.11 Object.isExtensible(O)
	var isObject = __webpack_require__(13);

	__webpack_require__(55)('isExtensible', function ($isExtensible) {
	  return function isExtensible(it) {
	    return isObject(it) ? $isExtensible ? $isExtensible(it) : true : false;
	  };
	});


/***/ }),
/* 67 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.3.1 Object.assign(target, source)
	var $export = __webpack_require__(8);

	$export($export.S + $export.F, 'Object', { assign: __webpack_require__(68) });


/***/ }),
/* 68 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// 19.1.2.1 Object.assign(target, source, ...)
	var getKeys = __webpack_require__(30);
	var gOPS = __webpack_require__(42);
	var pIE = __webpack_require__(43);
	var toObject = __webpack_require__(57);
	var IObject = __webpack_require__(33);
	var $assign = Object.assign;

	// should work with symbols and should have deterministic property order (V8 bug)
	module.exports = !$assign || __webpack_require__(7)(function () {
	  var A = {};
	  var B = {};
	  // eslint-disable-next-line no-undef
	  var S = Symbol();
	  var K = 'abcdefghijklmnopqrst';
	  A[S] = 7;
	  K.split('').forEach(function (k) { B[k] = k; });
	  return $assign({}, A)[S] != 7 || Object.keys($assign({}, B)).join('') != K;
	}) ? function assign(target, source) { // eslint-disable-line no-unused-vars
	  var T = toObject(target);
	  var aLen = arguments.length;
	  var index = 1;
	  var getSymbols = gOPS.f;
	  var isEnum = pIE.f;
	  while (aLen > index) {
	    var S = IObject(arguments[index++]);
	    var keys = getSymbols ? getKeys(S).concat(getSymbols(S)) : getKeys(S);
	    var length = keys.length;
	    var j = 0;
	    var key;
	    while (length > j) if (isEnum.call(S, key = keys[j++])) T[key] = S[key];
	  } return T;
	} : $assign;


/***/ }),
/* 69 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.3.10 Object.is(value1, value2)
	var $export = __webpack_require__(8);
	$export($export.S, 'Object', { is: __webpack_require__(70) });


/***/ }),
/* 70 */
/***/ (function(module, exports) {

	// 7.2.9 SameValue(x, y)
	module.exports = Object.is || function is(x, y) {
	  // eslint-disable-next-line no-self-compare
	  return x === y ? x !== 0 || 1 / x === 1 / y : x != x && y != y;
	};


/***/ }),
/* 71 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.1.3.19 Object.setPrototypeOf(O, proto)
	var $export = __webpack_require__(8);
	$export($export.S, 'Object', { setPrototypeOf: __webpack_require__(72).set });


/***/ }),
/* 72 */
/***/ (function(module, exports, __webpack_require__) {

	// Works with __proto__ only. Old v8 can't work with null proto objects.
	/* eslint-disable no-proto */
	var isObject = __webpack_require__(13);
	var anObject = __webpack_require__(12);
	var check = function (O, proto) {
	  anObject(O);
	  if (!isObject(proto) && proto !== null) throw TypeError(proto + ": can't set as prototype!");
	};
	module.exports = {
	  set: Object.setPrototypeOf || ('__proto__' in {} ? // eslint-disable-line
	    function (test, buggy, set) {
	      try {
	        set = __webpack_require__(20)(Function.call, __webpack_require__(50).f(Object.prototype, '__proto__').set, 2);
	        set(test, []);
	        buggy = !(test instanceof Array);
	      } catch (e) { buggy = true; }
	      return function setPrototypeOf(O, proto) {
	        check(O, proto);
	        if (buggy) O.__proto__ = proto;
	        else set(O, proto);
	        return O;
	      };
	    }({}, false) : undefined),
	  check: check
	};


/***/ }),
/* 73 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// 19.1.3.6 Object.prototype.toString()
	var classof = __webpack_require__(74);
	var test = {};
	test[__webpack_require__(25)('toStringTag')] = 'z';
	if (test + '' != '[object z]') {
	  __webpack_require__(18)(Object.prototype, 'toString', function toString() {
	    return '[object ' + classof(this) + ']';
	  }, true);
	}


/***/ }),
/* 74 */
/***/ (function(module, exports, __webpack_require__) {

	// getting tag from 19.1.3.6 Object.prototype.toString()
	var cof = __webpack_require__(34);
	var TAG = __webpack_require__(25)('toStringTag');
	// ES3 wrong here
	var ARG = cof(function () { return arguments; }()) == 'Arguments';

	// fallback for IE11 Script Access Denied error
	var tryGet = function (it, key) {
	  try {
	    return it[key];
	  } catch (e) { /* empty */ }
	};

	module.exports = function (it) {
	  var O, T, B;
	  return it === undefined ? 'Undefined' : it === null ? 'Null'
	    // @@toStringTag case
	    : typeof (T = tryGet(O = Object(it), TAG)) == 'string' ? T
	    // builtinTag case
	    : ARG ? cof(O)
	    // ES3 arguments fallback
	    : (B = cof(O)) == 'Object' && typeof O.callee == 'function' ? 'Arguments' : B;
	};


/***/ }),
/* 75 */
/***/ (function(module, exports, __webpack_require__) {

	// 19.2.3.2 / 15.3.4.5 Function.prototype.bind(thisArg, args...)
	var $export = __webpack_require__(8);

	$export($export.P, 'Function', { bind: __webpack_require__(76) });


/***/ }),
/* 76 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var aFunction = __webpack_require__(21);
	var isObject = __webpack_require__(13);
	var invoke = __webpack_require__(77);
	var arraySlice = [].slice;
	var factories = {};

	var construct = function (F, len, args) {
	  if (!(len in factories)) {
	    for (var n = [], i = 0; i < len; i++) n[i] = 'a[' + i + ']';
	    // eslint-disable-next-line no-new-func
	    factories[len] = Function('F,a', 'return new F(' + n.join(',') + ')');
	  } return factories[len](F, args);
	};

	module.exports = Function.bind || function bind(that /* , ...args */) {
	  var fn = aFunction(this);
	  var partArgs = arraySlice.call(arguments, 1);
	  var bound = function (/* args... */) {
	    var args = partArgs.concat(arraySlice.call(arguments));
	    return this instanceof bound ? construct(fn, args.length, args) : invoke(fn, args, that);
	  };
	  if (isObject(fn.prototype)) bound.prototype = fn.prototype;
	  return bound;
	};


/***/ }),
/* 77 */
/***/ (function(module, exports) {

	// fast apply, http://jsperf.lnkit.com/fast-apply/5
	module.exports = function (fn, args, that) {
	  var un = that === undefined;
	  switch (args.length) {
	    case 0: return un ? fn()
	                      : fn.call(that);
	    case 1: return un ? fn(args[0])
	                      : fn.call(that, args[0]);
	    case 2: return un ? fn(args[0], args[1])
	                      : fn.call(that, args[0], args[1]);
	    case 3: return un ? fn(args[0], args[1], args[2])
	                      : fn.call(that, args[0], args[1], args[2]);
	    case 4: return un ? fn(args[0], args[1], args[2], args[3])
	                      : fn.call(that, args[0], args[1], args[2], args[3]);
	  } return fn.apply(that, args);
	};


/***/ }),
/* 78 */
/***/ (function(module, exports, __webpack_require__) {

	var dP = __webpack_require__(11).f;
	var FProto = Function.prototype;
	var nameRE = /^\s*function ([^ (]*)/;
	var NAME = 'name';

	// 19.2.4.2 name
	NAME in FProto || __webpack_require__(6) && dP(FProto, NAME, {
	  configurable: true,
	  get: function () {
	    try {
	      return ('' + this).match(nameRE)[1];
	    } catch (e) {
	      return '';
	    }
	  }
	});


/***/ }),
/* 79 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var isObject = __webpack_require__(13);
	var getPrototypeOf = __webpack_require__(58);
	var HAS_INSTANCE = __webpack_require__(25)('hasInstance');
	var FunctionProto = Function.prototype;
	// 19.2.3.6 Function.prototype[@@hasInstance](V)
	if (!(HAS_INSTANCE in FunctionProto)) __webpack_require__(11).f(FunctionProto, HAS_INSTANCE, { value: function (O) {
	  if (typeof this != 'function' || !isObject(O)) return false;
	  if (!isObject(this.prototype)) return O instanceof this;
	  // for environment w/o native `@@hasInstance` logic enough `instanceof`, but add this:
	  while (O = getPrototypeOf(O)) if (this.prototype === O) return true;
	  return false;
	} });


/***/ }),
/* 80 */
/***/ (function(module, exports, __webpack_require__) {

	var $export = __webpack_require__(8);
	var $parseInt = __webpack_require__(81);
	// 18.2.5 parseInt(string, radix)
	$export($export.G + $export.F * (parseInt != $parseInt), { parseInt: $parseInt });


/***/ }),
/* 81 */
/***/ (function(module, exports, __webpack_require__) {

	var $parseInt = __webpack_require__(4).parseInt;
	var $trim = __webpack_require__(82).trim;
	var ws = __webpack_require__(83);
	var hex = /^[-+]?0[xX]/;

	module.exports = $parseInt(ws + '08') !== 8 || $parseInt(ws + '0x16') !== 22 ? function parseInt(str, radix) {
	  var string = $trim(String(str), 3);
	  return $parseInt(string, (radix >>> 0) || (hex.test(string) ? 16 : 10));
	} : $parseInt;


/***/ }),
/* 82 */
/***/ (function(module, exports, __webpack_require__) {

	var $export = __webpack_require__(8);
	var defined = __webpack_require__(35);
	var fails = __webpack_require__(7);
	var spaces = __webpack_require__(83);
	var space = '[' + spaces + ']';
	var non = '\u200b\u0085';
	var ltrim = RegExp('^' + space + space + '*');
	var rtrim = RegExp(space + space + '*$');

	var exporter = function (KEY, exec, ALIAS) {
	  var exp = {};
	  var FORCE = fails(function () {
	    return !!spaces[KEY]() || non[KEY]() != non;
	  });
	  var fn = exp[KEY] = FORCE ? exec(trim) : spaces[KEY];
	  if (ALIAS) exp[ALIAS] = fn;
	  $export($export.P + $export.F * FORCE, 'String', exp);
	};

	// 1 -> String#trimLeft
	// 2 -> String#trimRight
	// 3 -> String#trim
	var trim = exporter.trim = function (string, TYPE) {
	  string = String(defined(string));
	  if (TYPE & 1) string = string.replace(ltrim, '');
	  if (TYPE & 2) string = string.replace(rtrim, '');
	  return string;
	};

	module.exports = exporter;


/***/ }),
/* 83 */
/***/ (function(module, exports) {

	module.exports = '\x09\x0A\x0B\x0C\x0D\x20\xA0\u1680\u180E\u2000\u2001\u2002\u2003' +
	  '\u2004\u2005\u2006\u2007\u2008\u2009\u200A\u202F\u205F\u3000\u2028\u2029\uFEFF';


/***/ }),
/* 84 */
/***/ (function(module, exports, __webpack_require__) {

	var $export = __webpack_require__(8);
	var $parseFloat = __webpack_require__(85);
	// 18.2.4 parseFloat(string)
	$export($export.G + $export.F * (parseFloat != $parseFloat), { parseFloat: $parseFloat });


/***/ }),
/* 85 */
/***/ (function(module, exports, __webpack_require__) {

	var $parseFloat = __webpack_require__(4).parseFloat;
	var $trim = __webpack_require__(82).trim;

	module.exports = 1 / $parseFloat(__webpack_require__(83) + '-0') !== -Infinity ? function parseFloat(str) {
	  var string = $trim(String(str), 3);
	  var result = $parseFloat(string);
	  return result === 0 && string.charAt(0) == '-' ? -0 : result;
	} : $parseFloat;


/***/ }),
/* 86 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var global = __webpack_require__(4);
	var has = __webpack_require__(5);
	var cof = __webpack_require__(34);
	var inheritIfRequired = __webpack_require__(87);
	var toPrimitive = __webpack_require__(16);
	var fails = __webpack_require__(7);
	var gOPN = __webpack_require__(49).f;
	var gOPD = __webpack_require__(50).f;
	var dP = __webpack_require__(11).f;
	var $trim = __webpack_require__(82).trim;
	var NUMBER = 'Number';
	var $Number = global[NUMBER];
	var Base = $Number;
	var proto = $Number.prototype;
	// Opera ~12 has broken Object#toString
	var BROKEN_COF = cof(__webpack_require__(45)(proto)) == NUMBER;
	var TRIM = 'trim' in String.prototype;

	// 7.1.3 ToNumber(argument)
	var toNumber = function (argument) {
	  var it = toPrimitive(argument, false);
	  if (typeof it == 'string' && it.length > 2) {
	    it = TRIM ? it.trim() : $trim(it, 3);
	    var first = it.charCodeAt(0);
	    var third, radix, maxCode;
	    if (first === 43 || first === 45) {
	      third = it.charCodeAt(2);
	      if (third === 88 || third === 120) return NaN; // Number('+0x1') should be NaN, old V8 fix
	    } else if (first === 48) {
	      switch (it.charCodeAt(1)) {
	        case 66: case 98: radix = 2; maxCode = 49; break; // fast equal /^0b[01]+$/i
	        case 79: case 111: radix = 8; maxCode = 55; break; // fast equal /^0o[0-7]+$/i
	        default: return +it;
	      }
	      for (var digits = it.slice(2), i = 0, l = digits.length, code; i < l; i++) {
	        code = digits.charCodeAt(i);
	        // parseInt parses a string to a first unavailable symbol
	        // but ToNumber should return NaN if a string contains unavailable symbols
	        if (code < 48 || code > maxCode) return NaN;
	      } return parseInt(digits, radix);
	    }
	  } return +it;
	};

	if (!$Number(' 0o1') || !$Number('0b1') || $Number('+0x1')) {
	  $Number = function Number(value) {
	    var it = arguments.length < 1 ? 0 : value;
	    var that = this;
	    return that instanceof $Number
	      // check on 1..constructor(foo) case
	      && (BROKEN_COF ? fails(function () { proto.valueOf.call(that); }) : cof(that) != NUMBER)
	        ? inheritIfRequired(new Base(toNumber(it)), that, $Number) : toNumber(it);
	  };
	  for (var keys = __webpack_require__(6) ? gOPN(Base) : (
	    // ES3:
	    'MAX_VALUE,MIN_VALUE,NaN,NEGATIVE_INFINITY,POSITIVE_INFINITY,' +
	    // ES6 (in case, if modules with ES6 Number statics required before):
	    'EPSILON,isFinite,isInteger,isNaN,isSafeInteger,MAX_SAFE_INTEGER,' +
	    'MIN_SAFE_INTEGER,parseFloat,parseInt,isInteger'
	  ).split(','), j = 0, key; keys.length > j; j++) {
	    if (has(Base, key = keys[j]) && !has($Number, key)) {
	      dP($Number, key, gOPD(Base, key));
	    }
	  }
	  $Number.prototype = proto;
	  proto.constructor = $Number;
	  __webpack_require__(18)(global, NUMBER, $Number);
	}


/***/ }),
/* 87 */
/***/ (function(module, exports, __webpack_require__) {

	var isObject = __webpack_require__(13);
	var setPrototypeOf = __webpack_require__(72).set;
	module.exports = function (that, target, C) {
	  var S = target.constructor;
	  var P;
	  if (S !== C && typeof S == 'function' && (P = S.prototype) !== C.prototype && isObject(P) && setPrototypeOf) {
	    setPrototypeOf(that, P);
	  } return that;
	};


/***/ }),
/* 88 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var toInteger = __webpack_require__(38);
	var aNumberValue = __webpack_require__(89);
	var repeat = __webpack_require__(90);
	var $toFixed = 1.0.toFixed;
	var floor = Math.floor;
	var data = [0, 0, 0, 0, 0, 0];
	var ERROR = 'Number.toFixed: incorrect invocation!';
	var ZERO = '0';

	var multiply = function (n, c) {
	  var i = -1;
	  var c2 = c;
	  while (++i < 6) {
	    c2 += n * data[i];
	    data[i] = c2 % 1e7;
	    c2 = floor(c2 / 1e7);
	  }
	};
	var divide = function (n) {
	  var i = 6;
	  var c = 0;
	  while (--i >= 0) {
	    c += data[i];
	    data[i] = floor(c / n);
	    c = (c % n) * 1e7;
	  }
	};
	var numToString = function () {
	  var i = 6;
	  var s = '';
	  while (--i >= 0) {
	    if (s !== '' || i === 0 || data[i] !== 0) {
	      var t = String(data[i]);
	      s = s === '' ? t : s + repeat.call(ZERO, 7 - t.length) + t;
	    }
	  } return s;
	};
	var pow = function (x, n, acc) {
	  return n === 0 ? acc : n % 2 === 1 ? pow(x, n - 1, acc * x) : pow(x * x, n / 2, acc);
	};
	var log = function (x) {
	  var n = 0;
	  var x2 = x;
	  while (x2 >= 4096) {
	    n += 12;
	    x2 /= 4096;
	  }
	  while (x2 >= 2) {
	    n += 1;
	    x2 /= 2;
	  } return n;
	};

	$export($export.P + $export.F * (!!$toFixed && (
	  0.00008.toFixed(3) !== '0.000' ||
	  0.9.toFixed(0) !== '1' ||
	  1.255.toFixed(2) !== '1.25' ||
	  1000000000000000128.0.toFixed(0) !== '1000000000000000128'
	) || !__webpack_require__(7)(function () {
	  // V8 ~ Android 4.3-
	  $toFixed.call({});
	})), 'Number', {
	  toFixed: function toFixed(fractionDigits) {
	    var x = aNumberValue(this, ERROR);
	    var f = toInteger(fractionDigits);
	    var s = '';
	    var m = ZERO;
	    var e, z, j, k;
	    if (f < 0 || f > 20) throw RangeError(ERROR);
	    // eslint-disable-next-line no-self-compare
	    if (x != x) return 'NaN';
	    if (x <= -1e21 || x >= 1e21) return String(x);
	    if (x < 0) {
	      s = '-';
	      x = -x;
	    }
	    if (x > 1e-21) {
	      e = log(x * pow(2, 69, 1)) - 69;
	      z = e < 0 ? x * pow(2, -e, 1) : x / pow(2, e, 1);
	      z *= 0x10000000000000;
	      e = 52 - e;
	      if (e > 0) {
	        multiply(0, z);
	        j = f;
	        while (j >= 7) {
	          multiply(1e7, 0);
	          j -= 7;
	        }
	        multiply(pow(10, j, 1), 0);
	        j = e - 1;
	        while (j >= 23) {
	          divide(1 << 23);
	          j -= 23;
	        }
	        divide(1 << j);
	        multiply(1, 1);
	        divide(2);
	        m = numToString();
	      } else {
	        multiply(0, z);
	        multiply(1 << -e, 0);
	        m = numToString() + repeat.call(ZERO, f);
	      }
	    }
	    if (f > 0) {
	      k = m.length;
	      m = s + (k <= f ? '0.' + repeat.call(ZERO, f - k) + m : m.slice(0, k - f) + '.' + m.slice(k - f));
	    } else {
	      m = s + m;
	    } return m;
	  }
	});


/***/ }),
/* 89 */
/***/ (function(module, exports, __webpack_require__) {

	var cof = __webpack_require__(34);
	module.exports = function (it, msg) {
	  if (typeof it != 'number' && cof(it) != 'Number') throw TypeError(msg);
	  return +it;
	};


/***/ }),
/* 90 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var toInteger = __webpack_require__(38);
	var defined = __webpack_require__(35);

	module.exports = function repeat(count) {
	  var str = String(defined(this));
	  var res = '';
	  var n = toInteger(count);
	  if (n < 0 || n == Infinity) throw RangeError("Count can't be negative");
	  for (;n > 0; (n >>>= 1) && (str += str)) if (n & 1) res += str;
	  return res;
	};


/***/ }),
/* 91 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var $fails = __webpack_require__(7);
	var aNumberValue = __webpack_require__(89);
	var $toPrecision = 1.0.toPrecision;

	$export($export.P + $export.F * ($fails(function () {
	  // IE7-
	  return $toPrecision.call(1, undefined) !== '1';
	}) || !$fails(function () {
	  // V8 ~ Android 4.3-
	  $toPrecision.call({});
	})), 'Number', {
	  toPrecision: function toPrecision(precision) {
	    var that = aNumberValue(this, 'Number#toPrecision: incorrect invocation!');
	    return precision === undefined ? $toPrecision.call(that) : $toPrecision.call(that, precision);
	  }
	});


/***/ }),
/* 92 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.1.2.1 Number.EPSILON
	var $export = __webpack_require__(8);

	$export($export.S, 'Number', { EPSILON: Math.pow(2, -52) });


/***/ }),
/* 93 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.1.2.2 Number.isFinite(number)
	var $export = __webpack_require__(8);
	var _isFinite = __webpack_require__(4).isFinite;

	$export($export.S, 'Number', {
	  isFinite: function isFinite(it) {
	    return typeof it == 'number' && _isFinite(it);
	  }
	});


/***/ }),
/* 94 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.1.2.3 Number.isInteger(number)
	var $export = __webpack_require__(8);

	$export($export.S, 'Number', { isInteger: __webpack_require__(95) });


/***/ }),
/* 95 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.1.2.3 Number.isInteger(number)
	var isObject = __webpack_require__(13);
	var floor = Math.floor;
	module.exports = function isInteger(it) {
	  return !isObject(it) && isFinite(it) && floor(it) === it;
	};


/***/ }),
/* 96 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.1.2.4 Number.isNaN(number)
	var $export = __webpack_require__(8);

	$export($export.S, 'Number', {
	  isNaN: function isNaN(number) {
	    // eslint-disable-next-line no-self-compare
	    return number != number;
	  }
	});


/***/ }),
/* 97 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.1.2.5 Number.isSafeInteger(number)
	var $export = __webpack_require__(8);
	var isInteger = __webpack_require__(95);
	var abs = Math.abs;

	$export($export.S, 'Number', {
	  isSafeInteger: function isSafeInteger(number) {
	    return isInteger(number) && abs(number) <= 0x1fffffffffffff;
	  }
	});


/***/ }),
/* 98 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.1.2.6 Number.MAX_SAFE_INTEGER
	var $export = __webpack_require__(8);

	$export($export.S, 'Number', { MAX_SAFE_INTEGER: 0x1fffffffffffff });


/***/ }),
/* 99 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.1.2.10 Number.MIN_SAFE_INTEGER
	var $export = __webpack_require__(8);

	$export($export.S, 'Number', { MIN_SAFE_INTEGER: -0x1fffffffffffff });


/***/ }),
/* 100 */
/***/ (function(module, exports, __webpack_require__) {

	var $export = __webpack_require__(8);
	var $parseFloat = __webpack_require__(85);
	// 20.1.2.12 Number.parseFloat(string)
	$export($export.S + $export.F * (Number.parseFloat != $parseFloat), 'Number', { parseFloat: $parseFloat });


/***/ }),
/* 101 */
/***/ (function(module, exports, __webpack_require__) {

	var $export = __webpack_require__(8);
	var $parseInt = __webpack_require__(81);
	// 20.1.2.13 Number.parseInt(string, radix)
	$export($export.S + $export.F * (Number.parseInt != $parseInt), 'Number', { parseInt: $parseInt });


/***/ }),
/* 102 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.3 Math.acosh(x)
	var $export = __webpack_require__(8);
	var log1p = __webpack_require__(103);
	var sqrt = Math.sqrt;
	var $acosh = Math.acosh;

	$export($export.S + $export.F * !($acosh
	  // V8 bug: https://code.google.com/p/v8/issues/detail?id=3509
	  && Math.floor($acosh(Number.MAX_VALUE)) == 710
	  // Tor Browser bug: Math.acosh(Infinity) -> NaN
	  && $acosh(Infinity) == Infinity
	), 'Math', {
	  acosh: function acosh(x) {
	    return (x = +x) < 1 ? NaN : x > 94906265.62425156
	      ? Math.log(x) + Math.LN2
	      : log1p(x - 1 + sqrt(x - 1) * sqrt(x + 1));
	  }
	});


/***/ }),
/* 103 */
/***/ (function(module, exports) {

	// 20.2.2.20 Math.log1p(x)
	module.exports = Math.log1p || function log1p(x) {
	  return (x = +x) > -1e-8 && x < 1e-8 ? x - x * x / 2 : Math.log(1 + x);
	};


/***/ }),
/* 104 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.5 Math.asinh(x)
	var $export = __webpack_require__(8);
	var $asinh = Math.asinh;

	function asinh(x) {
	  return !isFinite(x = +x) || x == 0 ? x : x < 0 ? -asinh(-x) : Math.log(x + Math.sqrt(x * x + 1));
	}

	// Tor Browser bug: Math.asinh(0) -> -0
	$export($export.S + $export.F * !($asinh && 1 / $asinh(0) > 0), 'Math', { asinh: asinh });


/***/ }),
/* 105 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.7 Math.atanh(x)
	var $export = __webpack_require__(8);
	var $atanh = Math.atanh;

	// Tor Browser bug: Math.atanh(-0) -> 0
	$export($export.S + $export.F * !($atanh && 1 / $atanh(-0) < 0), 'Math', {
	  atanh: function atanh(x) {
	    return (x = +x) == 0 ? x : Math.log((1 + x) / (1 - x)) / 2;
	  }
	});


/***/ }),
/* 106 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.9 Math.cbrt(x)
	var $export = __webpack_require__(8);
	var sign = __webpack_require__(107);

	$export($export.S, 'Math', {
	  cbrt: function cbrt(x) {
	    return sign(x = +x) * Math.pow(Math.abs(x), 1 / 3);
	  }
	});


/***/ }),
/* 107 */
/***/ (function(module, exports) {

	// 20.2.2.28 Math.sign(x)
	module.exports = Math.sign || function sign(x) {
	  // eslint-disable-next-line no-self-compare
	  return (x = +x) == 0 || x != x ? x : x < 0 ? -1 : 1;
	};


/***/ }),
/* 108 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.11 Math.clz32(x)
	var $export = __webpack_require__(8);

	$export($export.S, 'Math', {
	  clz32: function clz32(x) {
	    return (x >>>= 0) ? 31 - Math.floor(Math.log(x + 0.5) * Math.LOG2E) : 32;
	  }
	});


/***/ }),
/* 109 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.12 Math.cosh(x)
	var $export = __webpack_require__(8);
	var exp = Math.exp;

	$export($export.S, 'Math', {
	  cosh: function cosh(x) {
	    return (exp(x = +x) + exp(-x)) / 2;
	  }
	});


/***/ }),
/* 110 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.14 Math.expm1(x)
	var $export = __webpack_require__(8);
	var $expm1 = __webpack_require__(111);

	$export($export.S + $export.F * ($expm1 != Math.expm1), 'Math', { expm1: $expm1 });


/***/ }),
/* 111 */
/***/ (function(module, exports) {

	// 20.2.2.14 Math.expm1(x)
	var $expm1 = Math.expm1;
	module.exports = (!$expm1
	  // Old FF bug
	  || $expm1(10) > 22025.465794806719 || $expm1(10) < 22025.4657948067165168
	  // Tor Browser bug
	  || $expm1(-2e-17) != -2e-17
	) ? function expm1(x) {
	  return (x = +x) == 0 ? x : x > -1e-6 && x < 1e-6 ? x + x * x / 2 : Math.exp(x) - 1;
	} : $expm1;


/***/ }),
/* 112 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.16 Math.fround(x)
	var $export = __webpack_require__(8);

	$export($export.S, 'Math', { fround: __webpack_require__(113) });


/***/ }),
/* 113 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.16 Math.fround(x)
	var sign = __webpack_require__(107);
	var pow = Math.pow;
	var EPSILON = pow(2, -52);
	var EPSILON32 = pow(2, -23);
	var MAX32 = pow(2, 127) * (2 - EPSILON32);
	var MIN32 = pow(2, -126);

	var roundTiesToEven = function (n) {
	  return n + 1 / EPSILON - 1 / EPSILON;
	};

	module.exports = Math.fround || function fround(x) {
	  var $abs = Math.abs(x);
	  var $sign = sign(x);
	  var a, result;
	  if ($abs < MIN32) return $sign * roundTiesToEven($abs / MIN32 / EPSILON32) * MIN32 * EPSILON32;
	  a = (1 + EPSILON32 / EPSILON) * $abs;
	  result = a - (a - $abs);
	  // eslint-disable-next-line no-self-compare
	  if (result > MAX32 || result != result) return $sign * Infinity;
	  return $sign * result;
	};


/***/ }),
/* 114 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.17 Math.hypot([value1[, value2[, … ]]])
	var $export = __webpack_require__(8);
	var abs = Math.abs;

	$export($export.S, 'Math', {
	  hypot: function hypot(value1, value2) { // eslint-disable-line no-unused-vars
	    var sum = 0;
	    var i = 0;
	    var aLen = arguments.length;
	    var larg = 0;
	    var arg, div;
	    while (i < aLen) {
	      arg = abs(arguments[i++]);
	      if (larg < arg) {
	        div = larg / arg;
	        sum = sum * div * div + 1;
	        larg = arg;
	      } else if (arg > 0) {
	        div = arg / larg;
	        sum += div * div;
	      } else sum += arg;
	    }
	    return larg === Infinity ? Infinity : larg * Math.sqrt(sum);
	  }
	});


/***/ }),
/* 115 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.18 Math.imul(x, y)
	var $export = __webpack_require__(8);
	var $imul = Math.imul;

	// some WebKit versions fails with big numbers, some has wrong arity
	$export($export.S + $export.F * __webpack_require__(7)(function () {
	  return $imul(0xffffffff, 5) != -5 || $imul.length != 2;
	}), 'Math', {
	  imul: function imul(x, y) {
	    var UINT16 = 0xffff;
	    var xn = +x;
	    var yn = +y;
	    var xl = UINT16 & xn;
	    var yl = UINT16 & yn;
	    return 0 | xl * yl + ((UINT16 & xn >>> 16) * yl + xl * (UINT16 & yn >>> 16) << 16 >>> 0);
	  }
	});


/***/ }),
/* 116 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.21 Math.log10(x)
	var $export = __webpack_require__(8);

	$export($export.S, 'Math', {
	  log10: function log10(x) {
	    return Math.log(x) * Math.LOG10E;
	  }
	});


/***/ }),
/* 117 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.20 Math.log1p(x)
	var $export = __webpack_require__(8);

	$export($export.S, 'Math', { log1p: __webpack_require__(103) });


/***/ }),
/* 118 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.22 Math.log2(x)
	var $export = __webpack_require__(8);

	$export($export.S, 'Math', {
	  log2: function log2(x) {
	    return Math.log(x) / Math.LN2;
	  }
	});


/***/ }),
/* 119 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.28 Math.sign(x)
	var $export = __webpack_require__(8);

	$export($export.S, 'Math', { sign: __webpack_require__(107) });


/***/ }),
/* 120 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.30 Math.sinh(x)
	var $export = __webpack_require__(8);
	var expm1 = __webpack_require__(111);
	var exp = Math.exp;

	// V8 near Chromium 38 has a problem with very small numbers
	$export($export.S + $export.F * __webpack_require__(7)(function () {
	  return !Math.sinh(-2e-17) != -2e-17;
	}), 'Math', {
	  sinh: function sinh(x) {
	    return Math.abs(x = +x) < 1
	      ? (expm1(x) - expm1(-x)) / 2
	      : (exp(x - 1) - exp(-x - 1)) * (Math.E / 2);
	  }
	});


/***/ }),
/* 121 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.33 Math.tanh(x)
	var $export = __webpack_require__(8);
	var expm1 = __webpack_require__(111);
	var exp = Math.exp;

	$export($export.S, 'Math', {
	  tanh: function tanh(x) {
	    var a = expm1(x = +x);
	    var b = expm1(-x);
	    return a == Infinity ? 1 : b == Infinity ? -1 : (a - b) / (exp(x) + exp(-x));
	  }
	});


/***/ }),
/* 122 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.2.2.34 Math.trunc(x)
	var $export = __webpack_require__(8);

	$export($export.S, 'Math', {
	  trunc: function trunc(it) {
	    return (it > 0 ? Math.floor : Math.ceil)(it);
	  }
	});


/***/ }),
/* 123 */
/***/ (function(module, exports, __webpack_require__) {

	var $export = __webpack_require__(8);
	var toAbsoluteIndex = __webpack_require__(39);
	var fromCharCode = String.fromCharCode;
	var $fromCodePoint = String.fromCodePoint;

	// length should be 1, old FF problem
	$export($export.S + $export.F * (!!$fromCodePoint && $fromCodePoint.length != 1), 'String', {
	  // 21.1.2.2 String.fromCodePoint(...codePoints)
	  fromCodePoint: function fromCodePoint(x) { // eslint-disable-line no-unused-vars
	    var res = [];
	    var aLen = arguments.length;
	    var i = 0;
	    var code;
	    while (aLen > i) {
	      code = +arguments[i++];
	      if (toAbsoluteIndex(code, 0x10ffff) !== code) throw RangeError(code + ' is not a valid code point');
	      res.push(code < 0x10000
	        ? fromCharCode(code)
	        : fromCharCode(((code -= 0x10000) >> 10) + 0xd800, code % 0x400 + 0xdc00)
	      );
	    } return res.join('');
	  }
	});


/***/ }),
/* 124 */
/***/ (function(module, exports, __webpack_require__) {

	var $export = __webpack_require__(8);
	var toIObject = __webpack_require__(32);
	var toLength = __webpack_require__(37);

	$export($export.S, 'String', {
	  // 21.1.2.4 String.raw(callSite, ...substitutions)
	  raw: function raw(callSite) {
	    var tpl = toIObject(callSite.raw);
	    var len = toLength(tpl.length);
	    var aLen = arguments.length;
	    var res = [];
	    var i = 0;
	    while (len > i) {
	      res.push(String(tpl[i++]));
	      if (i < aLen) res.push(String(arguments[i]));
	    } return res.join('');
	  }
	});


/***/ }),
/* 125 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// 21.1.3.25 String.prototype.trim()
	__webpack_require__(82)('trim', function ($trim) {
	  return function trim() {
	    return $trim(this, 3);
	  };
	});


/***/ }),
/* 126 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $at = __webpack_require__(127)(true);

	// 21.1.3.27 String.prototype[@@iterator]()
	__webpack_require__(128)(String, 'String', function (iterated) {
	  this._t = String(iterated); // target
	  this._i = 0;                // next index
	// 21.1.5.2.1 %StringIteratorPrototype%.next()
	}, function () {
	  var O = this._t;
	  var index = this._i;
	  var point;
	  if (index >= O.length) return { value: undefined, done: true };
	  point = $at(O, index);
	  this._i += point.length;
	  return { value: point, done: false };
	});


/***/ }),
/* 127 */
/***/ (function(module, exports, __webpack_require__) {

	var toInteger = __webpack_require__(38);
	var defined = __webpack_require__(35);
	// true  -> String#at
	// false -> String#codePointAt
	module.exports = function (TO_STRING) {
	  return function (that, pos) {
	    var s = String(defined(that));
	    var i = toInteger(pos);
	    var l = s.length;
	    var a, b;
	    if (i < 0 || i >= l) return TO_STRING ? '' : undefined;
	    a = s.charCodeAt(i);
	    return a < 0xd800 || a > 0xdbff || i + 1 === l || (b = s.charCodeAt(i + 1)) < 0xdc00 || b > 0xdfff
	      ? TO_STRING ? s.charAt(i) : a
	      : TO_STRING ? s.slice(i, i + 2) : (a - 0xd800 << 10) + (b - 0xdc00) + 0x10000;
	  };
	};


/***/ }),
/* 128 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var LIBRARY = __webpack_require__(28);
	var $export = __webpack_require__(8);
	var redefine = __webpack_require__(18);
	var hide = __webpack_require__(10);
	var has = __webpack_require__(5);
	var Iterators = __webpack_require__(129);
	var $iterCreate = __webpack_require__(130);
	var setToStringTag = __webpack_require__(24);
	var getPrototypeOf = __webpack_require__(58);
	var ITERATOR = __webpack_require__(25)('iterator');
	var BUGGY = !([].keys && 'next' in [].keys()); // Safari has buggy iterators w/o `next`
	var FF_ITERATOR = '@@iterator';
	var KEYS = 'keys';
	var VALUES = 'values';

	var returnThis = function () { return this; };

	module.exports = function (Base, NAME, Constructor, next, DEFAULT, IS_SET, FORCED) {
	  $iterCreate(Constructor, NAME, next);
	  var getMethod = function (kind) {
	    if (!BUGGY && kind in proto) return proto[kind];
	    switch (kind) {
	      case KEYS: return function keys() { return new Constructor(this, kind); };
	      case VALUES: return function values() { return new Constructor(this, kind); };
	    } return function entries() { return new Constructor(this, kind); };
	  };
	  var TAG = NAME + ' Iterator';
	  var DEF_VALUES = DEFAULT == VALUES;
	  var VALUES_BUG = false;
	  var proto = Base.prototype;
	  var $native = proto[ITERATOR] || proto[FF_ITERATOR] || DEFAULT && proto[DEFAULT];
	  var $default = $native || getMethod(DEFAULT);
	  var $entries = DEFAULT ? !DEF_VALUES ? $default : getMethod('entries') : undefined;
	  var $anyNative = NAME == 'Array' ? proto.entries || $native : $native;
	  var methods, key, IteratorPrototype;
	  // Fix native
	  if ($anyNative) {
	    IteratorPrototype = getPrototypeOf($anyNative.call(new Base()));
	    if (IteratorPrototype !== Object.prototype && IteratorPrototype.next) {
	      // Set @@toStringTag to native iterators
	      setToStringTag(IteratorPrototype, TAG, true);
	      // fix for some old engines
	      if (!LIBRARY && !has(IteratorPrototype, ITERATOR)) hide(IteratorPrototype, ITERATOR, returnThis);
	    }
	  }
	  // fix Array#{values, @@iterator}.name in V8 / FF
	  if (DEF_VALUES && $native && $native.name !== VALUES) {
	    VALUES_BUG = true;
	    $default = function values() { return $native.call(this); };
	  }
	  // Define iterator
	  if ((!LIBRARY || FORCED) && (BUGGY || VALUES_BUG || !proto[ITERATOR])) {
	    hide(proto, ITERATOR, $default);
	  }
	  // Plug for library
	  Iterators[NAME] = $default;
	  Iterators[TAG] = returnThis;
	  if (DEFAULT) {
	    methods = {
	      values: DEF_VALUES ? $default : getMethod(VALUES),
	      keys: IS_SET ? $default : getMethod(KEYS),
	      entries: $entries
	    };
	    if (FORCED) for (key in methods) {
	      if (!(key in proto)) redefine(proto, key, methods[key]);
	    } else $export($export.P + $export.F * (BUGGY || VALUES_BUG), NAME, methods);
	  }
	  return methods;
	};


/***/ }),
/* 129 */
/***/ (function(module, exports) {

	module.exports = {};


/***/ }),
/* 130 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var create = __webpack_require__(45);
	var descriptor = __webpack_require__(17);
	var setToStringTag = __webpack_require__(24);
	var IteratorPrototype = {};

	// 25.1.2.1.1 %IteratorPrototype%[@@iterator]()
	__webpack_require__(10)(IteratorPrototype, __webpack_require__(25)('iterator'), function () { return this; });

	module.exports = function (Constructor, NAME, next) {
	  Constructor.prototype = create(IteratorPrototype, { next: descriptor(1, next) });
	  setToStringTag(Constructor, NAME + ' Iterator');
	};


/***/ }),
/* 131 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var $at = __webpack_require__(127)(false);
	$export($export.P, 'String', {
	  // 21.1.3.3 String.prototype.codePointAt(pos)
	  codePointAt: function codePointAt(pos) {
	    return $at(this, pos);
	  }
	});


/***/ }),
/* 132 */
/***/ (function(module, exports, __webpack_require__) {

	// 21.1.3.6 String.prototype.endsWith(searchString [, endPosition])
	'use strict';
	var $export = __webpack_require__(8);
	var toLength = __webpack_require__(37);
	var context = __webpack_require__(133);
	var ENDS_WITH = 'endsWith';
	var $endsWith = ''[ENDS_WITH];

	$export($export.P + $export.F * __webpack_require__(135)(ENDS_WITH), 'String', {
	  endsWith: function endsWith(searchString /* , endPosition = @length */) {
	    var that = context(this, searchString, ENDS_WITH);
	    var endPosition = arguments.length > 1 ? arguments[1] : undefined;
	    var len = toLength(that.length);
	    var end = endPosition === undefined ? len : Math.min(toLength(endPosition), len);
	    var search = String(searchString);
	    return $endsWith
	      ? $endsWith.call(that, search, end)
	      : that.slice(end - search.length, end) === search;
	  }
	});


/***/ }),
/* 133 */
/***/ (function(module, exports, __webpack_require__) {

	// helper for String#{startsWith, endsWith, includes}
	var isRegExp = __webpack_require__(134);
	var defined = __webpack_require__(35);

	module.exports = function (that, searchString, NAME) {
	  if (isRegExp(searchString)) throw TypeError('String#' + NAME + " doesn't accept regex!");
	  return String(defined(that));
	};


/***/ }),
/* 134 */
/***/ (function(module, exports, __webpack_require__) {

	// 7.2.8 IsRegExp(argument)
	var isObject = __webpack_require__(13);
	var cof = __webpack_require__(34);
	var MATCH = __webpack_require__(25)('match');
	module.exports = function (it) {
	  var isRegExp;
	  return isObject(it) && ((isRegExp = it[MATCH]) !== undefined ? !!isRegExp : cof(it) == 'RegExp');
	};


/***/ }),
/* 135 */
/***/ (function(module, exports, __webpack_require__) {

	var MATCH = __webpack_require__(25)('match');
	module.exports = function (KEY) {
	  var re = /./;
	  try {
	    '/./'[KEY](re);
	  } catch (e) {
	    try {
	      re[MATCH] = false;
	      return !'/./'[KEY](re);
	    } catch (f) { /* empty */ }
	  } return true;
	};


/***/ }),
/* 136 */
/***/ (function(module, exports, __webpack_require__) {

	// 21.1.3.7 String.prototype.includes(searchString, position = 0)
	'use strict';
	var $export = __webpack_require__(8);
	var context = __webpack_require__(133);
	var INCLUDES = 'includes';

	$export($export.P + $export.F * __webpack_require__(135)(INCLUDES), 'String', {
	  includes: function includes(searchString /* , position = 0 */) {
	    return !!~context(this, searchString, INCLUDES)
	      .indexOf(searchString, arguments.length > 1 ? arguments[1] : undefined);
	  }
	});


/***/ }),
/* 137 */
/***/ (function(module, exports, __webpack_require__) {

	var $export = __webpack_require__(8);

	$export($export.P, 'String', {
	  // 21.1.3.13 String.prototype.repeat(count)
	  repeat: __webpack_require__(90)
	});


/***/ }),
/* 138 */
/***/ (function(module, exports, __webpack_require__) {

	// 21.1.3.18 String.prototype.startsWith(searchString [, position ])
	'use strict';
	var $export = __webpack_require__(8);
	var toLength = __webpack_require__(37);
	var context = __webpack_require__(133);
	var STARTS_WITH = 'startsWith';
	var $startsWith = ''[STARTS_WITH];

	$export($export.P + $export.F * __webpack_require__(135)(STARTS_WITH), 'String', {
	  startsWith: function startsWith(searchString /* , position = 0 */) {
	    var that = context(this, searchString, STARTS_WITH);
	    var index = toLength(Math.min(arguments.length > 1 ? arguments[1] : undefined, that.length));
	    var search = String(searchString);
	    return $startsWith
	      ? $startsWith.call(that, search, index)
	      : that.slice(index, index + search.length) === search;
	  }
	});


/***/ }),
/* 139 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// B.2.3.2 String.prototype.anchor(name)
	__webpack_require__(140)('anchor', function (createHTML) {
	  return function anchor(name) {
	    return createHTML(this, 'a', 'name', name);
	  };
	});


/***/ }),
/* 140 */
/***/ (function(module, exports, __webpack_require__) {

	var $export = __webpack_require__(8);
	var fails = __webpack_require__(7);
	var defined = __webpack_require__(35);
	var quot = /"/g;
	// B.2.3.2.1 CreateHTML(string, tag, attribute, value)
	var createHTML = function (string, tag, attribute, value) {
	  var S = String(defined(string));
	  var p1 = '<' + tag;
	  if (attribute !== '') p1 += ' ' + attribute + '="' + String(value).replace(quot, '&quot;') + '"';
	  return p1 + '>' + S + '</' + tag + '>';
	};
	module.exports = function (NAME, exec) {
	  var O = {};
	  O[NAME] = exec(createHTML);
	  $export($export.P + $export.F * fails(function () {
	    var test = ''[NAME]('"');
	    return test !== test.toLowerCase() || test.split('"').length > 3;
	  }), 'String', O);
	};


/***/ }),
/* 141 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// B.2.3.3 String.prototype.big()
	__webpack_require__(140)('big', function (createHTML) {
	  return function big() {
	    return createHTML(this, 'big', '', '');
	  };
	});


/***/ }),
/* 142 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// B.2.3.4 String.prototype.blink()
	__webpack_require__(140)('blink', function (createHTML) {
	  return function blink() {
	    return createHTML(this, 'blink', '', '');
	  };
	});


/***/ }),
/* 143 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// B.2.3.5 String.prototype.bold()
	__webpack_require__(140)('bold', function (createHTML) {
	  return function bold() {
	    return createHTML(this, 'b', '', '');
	  };
	});


/***/ }),
/* 144 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// B.2.3.6 String.prototype.fixed()
	__webpack_require__(140)('fixed', function (createHTML) {
	  return function fixed() {
	    return createHTML(this, 'tt', '', '');
	  };
	});


/***/ }),
/* 145 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// B.2.3.7 String.prototype.fontcolor(color)
	__webpack_require__(140)('fontcolor', function (createHTML) {
	  return function fontcolor(color) {
	    return createHTML(this, 'font', 'color', color);
	  };
	});


/***/ }),
/* 146 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// B.2.3.8 String.prototype.fontsize(size)
	__webpack_require__(140)('fontsize', function (createHTML) {
	  return function fontsize(size) {
	    return createHTML(this, 'font', 'size', size);
	  };
	});


/***/ }),
/* 147 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// B.2.3.9 String.prototype.italics()
	__webpack_require__(140)('italics', function (createHTML) {
	  return function italics() {
	    return createHTML(this, 'i', '', '');
	  };
	});


/***/ }),
/* 148 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// B.2.3.10 String.prototype.link(url)
	__webpack_require__(140)('link', function (createHTML) {
	  return function link(url) {
	    return createHTML(this, 'a', 'href', url);
	  };
	});


/***/ }),
/* 149 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// B.2.3.11 String.prototype.small()
	__webpack_require__(140)('small', function (createHTML) {
	  return function small() {
	    return createHTML(this, 'small', '', '');
	  };
	});


/***/ }),
/* 150 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// B.2.3.12 String.prototype.strike()
	__webpack_require__(140)('strike', function (createHTML) {
	  return function strike() {
	    return createHTML(this, 'strike', '', '');
	  };
	});


/***/ }),
/* 151 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// B.2.3.13 String.prototype.sub()
	__webpack_require__(140)('sub', function (createHTML) {
	  return function sub() {
	    return createHTML(this, 'sub', '', '');
	  };
	});


/***/ }),
/* 152 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// B.2.3.14 String.prototype.sup()
	__webpack_require__(140)('sup', function (createHTML) {
	  return function sup() {
	    return createHTML(this, 'sup', '', '');
	  };
	});


/***/ }),
/* 153 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.3.3.1 / 15.9.4.4 Date.now()
	var $export = __webpack_require__(8);

	$export($export.S, 'Date', { now: function () { return new Date().getTime(); } });


/***/ }),
/* 154 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var toObject = __webpack_require__(57);
	var toPrimitive = __webpack_require__(16);

	$export($export.P + $export.F * __webpack_require__(7)(function () {
	  return new Date(NaN).toJSON() !== null
	    || Date.prototype.toJSON.call({ toISOString: function () { return 1; } }) !== 1;
	}), 'Date', {
	  // eslint-disable-next-line no-unused-vars
	  toJSON: function toJSON(key) {
	    var O = toObject(this);
	    var pv = toPrimitive(O);
	    return typeof pv == 'number' && !isFinite(pv) ? null : O.toISOString();
	  }
	});


/***/ }),
/* 155 */
/***/ (function(module, exports, __webpack_require__) {

	// 20.3.4.36 / 15.9.5.43 Date.prototype.toISOString()
	var $export = __webpack_require__(8);
	var toISOString = __webpack_require__(156);

	// PhantomJS / old WebKit has a broken implementations
	$export($export.P + $export.F * (Date.prototype.toISOString !== toISOString), 'Date', {
	  toISOString: toISOString
	});


/***/ }),
/* 156 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// 20.3.4.36 / 15.9.5.43 Date.prototype.toISOString()
	var fails = __webpack_require__(7);
	var getTime = Date.prototype.getTime;
	var $toISOString = Date.prototype.toISOString;

	var lz = function (num) {
	  return num > 9 ? num : '0' + num;
	};

	// PhantomJS / old WebKit has a broken implementations
	module.exports = (fails(function () {
	  return $toISOString.call(new Date(-5e13 - 1)) != '0385-07-25T07:06:39.999Z';
	}) || !fails(function () {
	  $toISOString.call(new Date(NaN));
	})) ? function toISOString() {
	  if (!isFinite(getTime.call(this))) throw RangeError('Invalid time value');
	  var d = this;
	  var y = d.getUTCFullYear();
	  var m = d.getUTCMilliseconds();
	  var s = y < 0 ? '-' : y > 9999 ? '+' : '';
	  return s + ('00000' + Math.abs(y)).slice(s ? -6 : -4) +
	    '-' + lz(d.getUTCMonth() + 1) + '-' + lz(d.getUTCDate()) +
	    'T' + lz(d.getUTCHours()) + ':' + lz(d.getUTCMinutes()) +
	    ':' + lz(d.getUTCSeconds()) + '.' + (m > 99 ? m : '0' + lz(m)) + 'Z';
	} : $toISOString;


/***/ }),
/* 157 */
/***/ (function(module, exports, __webpack_require__) {

	var DateProto = Date.prototype;
	var INVALID_DATE = 'Invalid Date';
	var TO_STRING = 'toString';
	var $toString = DateProto[TO_STRING];
	var getTime = DateProto.getTime;
	if (new Date(NaN) + '' != INVALID_DATE) {
	  __webpack_require__(18)(DateProto, TO_STRING, function toString() {
	    var value = getTime.call(this);
	    // eslint-disable-next-line no-self-compare
	    return value === value ? $toString.call(this) : INVALID_DATE;
	  });
	}


/***/ }),
/* 158 */
/***/ (function(module, exports, __webpack_require__) {

	var TO_PRIMITIVE = __webpack_require__(25)('toPrimitive');
	var proto = Date.prototype;

	if (!(TO_PRIMITIVE in proto)) __webpack_require__(10)(proto, TO_PRIMITIVE, __webpack_require__(159));


/***/ }),
/* 159 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var anObject = __webpack_require__(12);
	var toPrimitive = __webpack_require__(16);
	var NUMBER = 'number';

	module.exports = function (hint) {
	  if (hint !== 'string' && hint !== NUMBER && hint !== 'default') throw TypeError('Incorrect hint');
	  return toPrimitive(anObject(this), hint != NUMBER);
	};


/***/ }),
/* 160 */
/***/ (function(module, exports, __webpack_require__) {

	// 22.1.2.2 / 15.4.3.2 Array.isArray(arg)
	var $export = __webpack_require__(8);

	$export($export.S, 'Array', { isArray: __webpack_require__(44) });


/***/ }),
/* 161 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var ctx = __webpack_require__(20);
	var $export = __webpack_require__(8);
	var toObject = __webpack_require__(57);
	var call = __webpack_require__(162);
	var isArrayIter = __webpack_require__(163);
	var toLength = __webpack_require__(37);
	var createProperty = __webpack_require__(164);
	var getIterFn = __webpack_require__(165);

	$export($export.S + $export.F * !__webpack_require__(166)(function (iter) { Array.from(iter); }), 'Array', {
	  // 22.1.2.1 Array.from(arrayLike, mapfn = undefined, thisArg = undefined)
	  from: function from(arrayLike /* , mapfn = undefined, thisArg = undefined */) {
	    var O = toObject(arrayLike);
	    var C = typeof this == 'function' ? this : Array;
	    var aLen = arguments.length;
	    var mapfn = aLen > 1 ? arguments[1] : undefined;
	    var mapping = mapfn !== undefined;
	    var index = 0;
	    var iterFn = getIterFn(O);
	    var length, result, step, iterator;
	    if (mapping) mapfn = ctx(mapfn, aLen > 2 ? arguments[2] : undefined, 2);
	    // if object isn't iterable or it's array with default iterator - use simple case
	    if (iterFn != undefined && !(C == Array && isArrayIter(iterFn))) {
	      for (iterator = iterFn.call(O), result = new C(); !(step = iterator.next()).done; index++) {
	        createProperty(result, index, mapping ? call(iterator, mapfn, [step.value, index], true) : step.value);
	      }
	    } else {
	      length = toLength(O.length);
	      for (result = new C(length); length > index; index++) {
	        createProperty(result, index, mapping ? mapfn(O[index], index) : O[index]);
	      }
	    }
	    result.length = index;
	    return result;
	  }
	});


/***/ }),
/* 162 */
/***/ (function(module, exports, __webpack_require__) {

	// call something on iterator step with safe closing on error
	var anObject = __webpack_require__(12);
	module.exports = function (iterator, fn, value, entries) {
	  try {
	    return entries ? fn(anObject(value)[0], value[1]) : fn(value);
	  // 7.4.6 IteratorClose(iterator, completion)
	  } catch (e) {
	    var ret = iterator['return'];
	    if (ret !== undefined) anObject(ret.call(iterator));
	    throw e;
	  }
	};


/***/ }),
/* 163 */
/***/ (function(module, exports, __webpack_require__) {

	// check on default Array iterator
	var Iterators = __webpack_require__(129);
	var ITERATOR = __webpack_require__(25)('iterator');
	var ArrayProto = Array.prototype;

	module.exports = function (it) {
	  return it !== undefined && (Iterators.Array === it || ArrayProto[ITERATOR] === it);
	};


/***/ }),
/* 164 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $defineProperty = __webpack_require__(11);
	var createDesc = __webpack_require__(17);

	module.exports = function (object, index, value) {
	  if (index in object) $defineProperty.f(object, index, createDesc(0, value));
	  else object[index] = value;
	};


/***/ }),
/* 165 */
/***/ (function(module, exports, __webpack_require__) {

	var classof = __webpack_require__(74);
	var ITERATOR = __webpack_require__(25)('iterator');
	var Iterators = __webpack_require__(129);
	module.exports = __webpack_require__(9).getIteratorMethod = function (it) {
	  if (it != undefined) return it[ITERATOR]
	    || it['@@iterator']
	    || Iterators[classof(it)];
	};


/***/ }),
/* 166 */
/***/ (function(module, exports, __webpack_require__) {

	var ITERATOR = __webpack_require__(25)('iterator');
	var SAFE_CLOSING = false;

	try {
	  var riter = [7][ITERATOR]();
	  riter['return'] = function () { SAFE_CLOSING = true; };
	  // eslint-disable-next-line no-throw-literal
	  Array.from(riter, function () { throw 2; });
	} catch (e) { /* empty */ }

	module.exports = function (exec, skipClosing) {
	  if (!skipClosing && !SAFE_CLOSING) return false;
	  var safe = false;
	  try {
	    var arr = [7];
	    var iter = arr[ITERATOR]();
	    iter.next = function () { return { done: safe = true }; };
	    arr[ITERATOR] = function () { return iter; };
	    exec(arr);
	  } catch (e) { /* empty */ }
	  return safe;
	};


/***/ }),
/* 167 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var createProperty = __webpack_require__(164);

	// WebKit Array.of isn't generic
	$export($export.S + $export.F * __webpack_require__(7)(function () {
	  function F() { /* empty */ }
	  return !(Array.of.call(F) instanceof F);
	}), 'Array', {
	  // 22.1.2.3 Array.of( ...items)
	  of: function of(/* ...args */) {
	    var index = 0;
	    var aLen = arguments.length;
	    var result = new (typeof this == 'function' ? this : Array)(aLen);
	    while (aLen > index) createProperty(result, index, arguments[index++]);
	    result.length = aLen;
	    return result;
	  }
	});


/***/ }),
/* 168 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// 22.1.3.13 Array.prototype.join(separator)
	var $export = __webpack_require__(8);
	var toIObject = __webpack_require__(32);
	var arrayJoin = [].join;

	// fallback for not array-like strings
	$export($export.P + $export.F * (__webpack_require__(33) != Object || !__webpack_require__(169)(arrayJoin)), 'Array', {
	  join: function join(separator) {
	    return arrayJoin.call(toIObject(this), separator === undefined ? ',' : separator);
	  }
	});


/***/ }),
/* 169 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var fails = __webpack_require__(7);

	module.exports = function (method, arg) {
	  return !!method && fails(function () {
	    // eslint-disable-next-line no-useless-call
	    arg ? method.call(null, function () { /* empty */ }, 1) : method.call(null);
	  });
	};


/***/ }),
/* 170 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var html = __webpack_require__(47);
	var cof = __webpack_require__(34);
	var toAbsoluteIndex = __webpack_require__(39);
	var toLength = __webpack_require__(37);
	var arraySlice = [].slice;

	// fallback for not array-like ES3 strings and DOM objects
	$export($export.P + $export.F * __webpack_require__(7)(function () {
	  if (html) arraySlice.call(html);
	}), 'Array', {
	  slice: function slice(begin, end) {
	    var len = toLength(this.length);
	    var klass = cof(this);
	    end = end === undefined ? len : end;
	    if (klass == 'Array') return arraySlice.call(this, begin, end);
	    var start = toAbsoluteIndex(begin, len);
	    var upTo = toAbsoluteIndex(end, len);
	    var size = toLength(upTo - start);
	    var cloned = Array(size);
	    var i = 0;
	    for (; i < size; i++) cloned[i] = klass == 'String'
	      ? this.charAt(start + i)
	      : this[start + i];
	    return cloned;
	  }
	});


/***/ }),
/* 171 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var aFunction = __webpack_require__(21);
	var toObject = __webpack_require__(57);
	var fails = __webpack_require__(7);
	var $sort = [].sort;
	var test = [1, 2, 3];

	$export($export.P + $export.F * (fails(function () {
	  // IE8-
	  test.sort(undefined);
	}) || !fails(function () {
	  // V8 bug
	  test.sort(null);
	  // Old WebKit
	}) || !__webpack_require__(169)($sort)), 'Array', {
	  // 22.1.3.25 Array.prototype.sort(comparefn)
	  sort: function sort(comparefn) {
	    return comparefn === undefined
	      ? $sort.call(toObject(this))
	      : $sort.call(toObject(this), aFunction(comparefn));
	  }
	});


/***/ }),
/* 172 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var $forEach = __webpack_require__(173)(0);
	var STRICT = __webpack_require__(169)([].forEach, true);

	$export($export.P + $export.F * !STRICT, 'Array', {
	  // 22.1.3.10 / 15.4.4.18 Array.prototype.forEach(callbackfn [, thisArg])
	  forEach: function forEach(callbackfn /* , thisArg */) {
	    return $forEach(this, callbackfn, arguments[1]);
	  }
	});


/***/ }),
/* 173 */
/***/ (function(module, exports, __webpack_require__) {

	// 0 -> Array#forEach
	// 1 -> Array#map
	// 2 -> Array#filter
	// 3 -> Array#some
	// 4 -> Array#every
	// 5 -> Array#find
	// 6 -> Array#findIndex
	var ctx = __webpack_require__(20);
	var IObject = __webpack_require__(33);
	var toObject = __webpack_require__(57);
	var toLength = __webpack_require__(37);
	var asc = __webpack_require__(174);
	module.exports = function (TYPE, $create) {
	  var IS_MAP = TYPE == 1;
	  var IS_FILTER = TYPE == 2;
	  var IS_SOME = TYPE == 3;
	  var IS_EVERY = TYPE == 4;
	  var IS_FIND_INDEX = TYPE == 6;
	  var NO_HOLES = TYPE == 5 || IS_FIND_INDEX;
	  var create = $create || asc;
	  return function ($this, callbackfn, that) {
	    var O = toObject($this);
	    var self = IObject(O);
	    var f = ctx(callbackfn, that, 3);
	    var length = toLength(self.length);
	    var index = 0;
	    var result = IS_MAP ? create($this, length) : IS_FILTER ? create($this, 0) : undefined;
	    var val, res;
	    for (;length > index; index++) if (NO_HOLES || index in self) {
	      val = self[index];
	      res = f(val, index, O);
	      if (TYPE) {
	        if (IS_MAP) result[index] = res;   // map
	        else if (res) switch (TYPE) {
	          case 3: return true;             // some
	          case 5: return val;              // find
	          case 6: return index;            // findIndex
	          case 2: result.push(val);        // filter
	        } else if (IS_EVERY) return false; // every
	      }
	    }
	    return IS_FIND_INDEX ? -1 : IS_SOME || IS_EVERY ? IS_EVERY : result;
	  };
	};


/***/ }),
/* 174 */
/***/ (function(module, exports, __webpack_require__) {

	// 9.4.2.3 ArraySpeciesCreate(originalArray, length)
	var speciesConstructor = __webpack_require__(175);

	module.exports = function (original, length) {
	  return new (speciesConstructor(original))(length);
	};


/***/ }),
/* 175 */
/***/ (function(module, exports, __webpack_require__) {

	var isObject = __webpack_require__(13);
	var isArray = __webpack_require__(44);
	var SPECIES = __webpack_require__(25)('species');

	module.exports = function (original) {
	  var C;
	  if (isArray(original)) {
	    C = original.constructor;
	    // cross-realm fallback
	    if (typeof C == 'function' && (C === Array || isArray(C.prototype))) C = undefined;
	    if (isObject(C)) {
	      C = C[SPECIES];
	      if (C === null) C = undefined;
	    }
	  } return C === undefined ? Array : C;
	};


/***/ }),
/* 176 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var $map = __webpack_require__(173)(1);

	$export($export.P + $export.F * !__webpack_require__(169)([].map, true), 'Array', {
	  // 22.1.3.15 / 15.4.4.19 Array.prototype.map(callbackfn [, thisArg])
	  map: function map(callbackfn /* , thisArg */) {
	    return $map(this, callbackfn, arguments[1]);
	  }
	});


/***/ }),
/* 177 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var $filter = __webpack_require__(173)(2);

	$export($export.P + $export.F * !__webpack_require__(169)([].filter, true), 'Array', {
	  // 22.1.3.7 / 15.4.4.20 Array.prototype.filter(callbackfn [, thisArg])
	  filter: function filter(callbackfn /* , thisArg */) {
	    return $filter(this, callbackfn, arguments[1]);
	  }
	});


/***/ }),
/* 178 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var $some = __webpack_require__(173)(3);

	$export($export.P + $export.F * !__webpack_require__(169)([].some, true), 'Array', {
	  // 22.1.3.23 / 15.4.4.17 Array.prototype.some(callbackfn [, thisArg])
	  some: function some(callbackfn /* , thisArg */) {
	    return $some(this, callbackfn, arguments[1]);
	  }
	});


/***/ }),
/* 179 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var $every = __webpack_require__(173)(4);

	$export($export.P + $export.F * !__webpack_require__(169)([].every, true), 'Array', {
	  // 22.1.3.5 / 15.4.4.16 Array.prototype.every(callbackfn [, thisArg])
	  every: function every(callbackfn /* , thisArg */) {
	    return $every(this, callbackfn, arguments[1]);
	  }
	});


/***/ }),
/* 180 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var $reduce = __webpack_require__(181);

	$export($export.P + $export.F * !__webpack_require__(169)([].reduce, true), 'Array', {
	  // 22.1.3.18 / 15.4.4.21 Array.prototype.reduce(callbackfn [, initialValue])
	  reduce: function reduce(callbackfn /* , initialValue */) {
	    return $reduce(this, callbackfn, arguments.length, arguments[1], false);
	  }
	});


/***/ }),
/* 181 */
/***/ (function(module, exports, __webpack_require__) {

	var aFunction = __webpack_require__(21);
	var toObject = __webpack_require__(57);
	var IObject = __webpack_require__(33);
	var toLength = __webpack_require__(37);

	module.exports = function (that, callbackfn, aLen, memo, isRight) {
	  aFunction(callbackfn);
	  var O = toObject(that);
	  var self = IObject(O);
	  var length = toLength(O.length);
	  var index = isRight ? length - 1 : 0;
	  var i = isRight ? -1 : 1;
	  if (aLen < 2) for (;;) {
	    if (index in self) {
	      memo = self[index];
	      index += i;
	      break;
	    }
	    index += i;
	    if (isRight ? index < 0 : length <= index) {
	      throw TypeError('Reduce of empty array with no initial value');
	    }
	  }
	  for (;isRight ? index >= 0 : length > index; index += i) if (index in self) {
	    memo = callbackfn(memo, self[index], index, O);
	  }
	  return memo;
	};


/***/ }),
/* 182 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var $reduce = __webpack_require__(181);

	$export($export.P + $export.F * !__webpack_require__(169)([].reduceRight, true), 'Array', {
	  // 22.1.3.19 / 15.4.4.22 Array.prototype.reduceRight(callbackfn [, initialValue])
	  reduceRight: function reduceRight(callbackfn /* , initialValue */) {
	    return $reduce(this, callbackfn, arguments.length, arguments[1], true);
	  }
	});


/***/ }),
/* 183 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var $indexOf = __webpack_require__(36)(false);
	var $native = [].indexOf;
	var NEGATIVE_ZERO = !!$native && 1 / [1].indexOf(1, -0) < 0;

	$export($export.P + $export.F * (NEGATIVE_ZERO || !__webpack_require__(169)($native)), 'Array', {
	  // 22.1.3.11 / 15.4.4.14 Array.prototype.indexOf(searchElement [, fromIndex])
	  indexOf: function indexOf(searchElement /* , fromIndex = 0 */) {
	    return NEGATIVE_ZERO
	      // convert -0 to +0
	      ? $native.apply(this, arguments) || 0
	      : $indexOf(this, searchElement, arguments[1]);
	  }
	});


/***/ }),
/* 184 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var toIObject = __webpack_require__(32);
	var toInteger = __webpack_require__(38);
	var toLength = __webpack_require__(37);
	var $native = [].lastIndexOf;
	var NEGATIVE_ZERO = !!$native && 1 / [1].lastIndexOf(1, -0) < 0;

	$export($export.P + $export.F * (NEGATIVE_ZERO || !__webpack_require__(169)($native)), 'Array', {
	  // 22.1.3.14 / 15.4.4.15 Array.prototype.lastIndexOf(searchElement [, fromIndex])
	  lastIndexOf: function lastIndexOf(searchElement /* , fromIndex = @[*-1] */) {
	    // convert -0 to +0
	    if (NEGATIVE_ZERO) return $native.apply(this, arguments) || 0;
	    var O = toIObject(this);
	    var length = toLength(O.length);
	    var index = length - 1;
	    if (arguments.length > 1) index = Math.min(index, toInteger(arguments[1]));
	    if (index < 0) index = length + index;
	    for (;index >= 0; index--) if (index in O) if (O[index] === searchElement) return index || 0;
	    return -1;
	  }
	});


/***/ }),
/* 185 */
/***/ (function(module, exports, __webpack_require__) {

	// 22.1.3.3 Array.prototype.copyWithin(target, start, end = this.length)
	var $export = __webpack_require__(8);

	$export($export.P, 'Array', { copyWithin: __webpack_require__(186) });

	__webpack_require__(187)('copyWithin');


/***/ }),
/* 186 */
/***/ (function(module, exports, __webpack_require__) {

	// 22.1.3.3 Array.prototype.copyWithin(target, start, end = this.length)
	'use strict';
	var toObject = __webpack_require__(57);
	var toAbsoluteIndex = __webpack_require__(39);
	var toLength = __webpack_require__(37);

	module.exports = [].copyWithin || function copyWithin(target /* = 0 */, start /* = 0, end = @length */) {
	  var O = toObject(this);
	  var len = toLength(O.length);
	  var to = toAbsoluteIndex(target, len);
	  var from = toAbsoluteIndex(start, len);
	  var end = arguments.length > 2 ? arguments[2] : undefined;
	  var count = Math.min((end === undefined ? len : toAbsoluteIndex(end, len)) - from, len - to);
	  var inc = 1;
	  if (from < to && to < from + count) {
	    inc = -1;
	    from += count - 1;
	    to += count - 1;
	  }
	  while (count-- > 0) {
	    if (from in O) O[to] = O[from];
	    else delete O[to];
	    to += inc;
	    from += inc;
	  } return O;
	};


/***/ }),
/* 187 */
/***/ (function(module, exports, __webpack_require__) {

	// 22.1.3.31 Array.prototype[@@unscopables]
	var UNSCOPABLES = __webpack_require__(25)('unscopables');
	var ArrayProto = Array.prototype;
	if (ArrayProto[UNSCOPABLES] == undefined) __webpack_require__(10)(ArrayProto, UNSCOPABLES, {});
	module.exports = function (key) {
	  ArrayProto[UNSCOPABLES][key] = true;
	};


/***/ }),
/* 188 */
/***/ (function(module, exports, __webpack_require__) {

	// 22.1.3.6 Array.prototype.fill(value, start = 0, end = this.length)
	var $export = __webpack_require__(8);

	$export($export.P, 'Array', { fill: __webpack_require__(189) });

	__webpack_require__(187)('fill');


/***/ }),
/* 189 */
/***/ (function(module, exports, __webpack_require__) {

	// 22.1.3.6 Array.prototype.fill(value, start = 0, end = this.length)
	'use strict';
	var toObject = __webpack_require__(57);
	var toAbsoluteIndex = __webpack_require__(39);
	var toLength = __webpack_require__(37);
	module.exports = function fill(value /* , start = 0, end = @length */) {
	  var O = toObject(this);
	  var length = toLength(O.length);
	  var aLen = arguments.length;
	  var index = toAbsoluteIndex(aLen > 1 ? arguments[1] : undefined, length);
	  var end = aLen > 2 ? arguments[2] : undefined;
	  var endPos = end === undefined ? length : toAbsoluteIndex(end, length);
	  while (endPos > index) O[index++] = value;
	  return O;
	};


/***/ }),
/* 190 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// 22.1.3.8 Array.prototype.find(predicate, thisArg = undefined)
	var $export = __webpack_require__(8);
	var $find = __webpack_require__(173)(5);
	var KEY = 'find';
	var forced = true;
	// Shouldn't skip holes
	if (KEY in []) Array(1)[KEY](function () { forced = false; });
	$export($export.P + $export.F * forced, 'Array', {
	  find: function find(callbackfn /* , that = undefined */) {
	    return $find(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
	  }
	});
	__webpack_require__(187)(KEY);


/***/ }),
/* 191 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// 22.1.3.9 Array.prototype.findIndex(predicate, thisArg = undefined)
	var $export = __webpack_require__(8);
	var $find = __webpack_require__(173)(6);
	var KEY = 'findIndex';
	var forced = true;
	// Shouldn't skip holes
	if (KEY in []) Array(1)[KEY](function () { forced = false; });
	$export($export.P + $export.F * forced, 'Array', {
	  findIndex: function findIndex(callbackfn /* , that = undefined */) {
	    return $find(this, callbackfn, arguments.length > 1 ? arguments[1] : undefined);
	  }
	});
	__webpack_require__(187)(KEY);


/***/ }),
/* 192 */
/***/ (function(module, exports, __webpack_require__) {

	__webpack_require__(193)('Array');


/***/ }),
/* 193 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var global = __webpack_require__(4);
	var dP = __webpack_require__(11);
	var DESCRIPTORS = __webpack_require__(6);
	var SPECIES = __webpack_require__(25)('species');

	module.exports = function (KEY) {
	  var C = global[KEY];
	  if (DESCRIPTORS && C && !C[SPECIES]) dP.f(C, SPECIES, {
	    configurable: true,
	    get: function () { return this; }
	  });
	};


/***/ }),
/* 194 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var addToUnscopables = __webpack_require__(187);
	var step = __webpack_require__(195);
	var Iterators = __webpack_require__(129);
	var toIObject = __webpack_require__(32);

	// 22.1.3.4 Array.prototype.entries()
	// 22.1.3.13 Array.prototype.keys()
	// 22.1.3.29 Array.prototype.values()
	// 22.1.3.30 Array.prototype[@@iterator]()
	module.exports = __webpack_require__(128)(Array, 'Array', function (iterated, kind) {
	  this._t = toIObject(iterated); // target
	  this._i = 0;                   // next index
	  this._k = kind;                // kind
	// 22.1.5.2.1 %ArrayIteratorPrototype%.next()
	}, function () {
	  var O = this._t;
	  var kind = this._k;
	  var index = this._i++;
	  if (!O || index >= O.length) {
	    this._t = undefined;
	    return step(1);
	  }
	  if (kind == 'keys') return step(0, index);
	  if (kind == 'values') return step(0, O[index]);
	  return step(0, [index, O[index]]);
	}, 'values');

	// argumentsList[@@iterator] is %ArrayProto_values% (9.4.4.6, 9.4.4.7)
	Iterators.Arguments = Iterators.Array;

	addToUnscopables('keys');
	addToUnscopables('values');
	addToUnscopables('entries');


/***/ }),
/* 195 */
/***/ (function(module, exports) {

	module.exports = function (done, value) {
	  return { value: value, done: !!done };
	};


/***/ }),
/* 196 */
/***/ (function(module, exports, __webpack_require__) {

	var global = __webpack_require__(4);
	var inheritIfRequired = __webpack_require__(87);
	var dP = __webpack_require__(11).f;
	var gOPN = __webpack_require__(49).f;
	var isRegExp = __webpack_require__(134);
	var $flags = __webpack_require__(197);
	var $RegExp = global.RegExp;
	var Base = $RegExp;
	var proto = $RegExp.prototype;
	var re1 = /a/g;
	var re2 = /a/g;
	// "new" creates a new object, old webkit buggy here
	var CORRECT_NEW = new $RegExp(re1) !== re1;

	if (__webpack_require__(6) && (!CORRECT_NEW || __webpack_require__(7)(function () {
	  re2[__webpack_require__(25)('match')] = false;
	  // RegExp constructor can alter flags and IsRegExp works correct with @@match
	  return $RegExp(re1) != re1 || $RegExp(re2) == re2 || $RegExp(re1, 'i') != '/a/i';
	}))) {
	  $RegExp = function RegExp(p, f) {
	    var tiRE = this instanceof $RegExp;
	    var piRE = isRegExp(p);
	    var fiU = f === undefined;
	    return !tiRE && piRE && p.constructor === $RegExp && fiU ? p
	      : inheritIfRequired(CORRECT_NEW
	        ? new Base(piRE && !fiU ? p.source : p, f)
	        : Base((piRE = p instanceof $RegExp) ? p.source : p, piRE && fiU ? $flags.call(p) : f)
	      , tiRE ? this : proto, $RegExp);
	  };
	  var proxy = function (key) {
	    key in $RegExp || dP($RegExp, key, {
	      configurable: true,
	      get: function () { return Base[key]; },
	      set: function (it) { Base[key] = it; }
	    });
	  };
	  for (var keys = gOPN(Base), i = 0; keys.length > i;) proxy(keys[i++]);
	  proto.constructor = $RegExp;
	  $RegExp.prototype = proto;
	  __webpack_require__(18)(global, 'RegExp', $RegExp);
	}

	__webpack_require__(193)('RegExp');


/***/ }),
/* 197 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// 21.2.5.3 get RegExp.prototype.flags
	var anObject = __webpack_require__(12);
	module.exports = function () {
	  var that = anObject(this);
	  var result = '';
	  if (that.global) result += 'g';
	  if (that.ignoreCase) result += 'i';
	  if (that.multiline) result += 'm';
	  if (that.unicode) result += 'u';
	  if (that.sticky) result += 'y';
	  return result;
	};


/***/ }),
/* 198 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	__webpack_require__(199);
	var anObject = __webpack_require__(12);
	var $flags = __webpack_require__(197);
	var DESCRIPTORS = __webpack_require__(6);
	var TO_STRING = 'toString';
	var $toString = /./[TO_STRING];

	var define = function (fn) {
	  __webpack_require__(18)(RegExp.prototype, TO_STRING, fn, true);
	};

	// 21.2.5.14 RegExp.prototype.toString()
	if (__webpack_require__(7)(function () { return $toString.call({ source: 'a', flags: 'b' }) != '/a/b'; })) {
	  define(function toString() {
	    var R = anObject(this);
	    return '/'.concat(R.source, '/',
	      'flags' in R ? R.flags : !DESCRIPTORS && R instanceof RegExp ? $flags.call(R) : undefined);
	  });
	// FF44- RegExp#toString has a wrong name
	} else if ($toString.name != TO_STRING) {
	  define(function toString() {
	    return $toString.call(this);
	  });
	}


/***/ }),
/* 199 */
/***/ (function(module, exports, __webpack_require__) {

	// 21.2.5.3 get RegExp.prototype.flags()
	if (__webpack_require__(6) && /./g.flags != 'g') __webpack_require__(11).f(RegExp.prototype, 'flags', {
	  configurable: true,
	  get: __webpack_require__(197)
	});


/***/ }),
/* 200 */
/***/ (function(module, exports, __webpack_require__) {

	// @@match logic
	__webpack_require__(201)('match', 1, function (defined, MATCH, $match) {
	  // 21.1.3.11 String.prototype.match(regexp)
	  return [function match(regexp) {
	    'use strict';
	    var O = defined(this);
	    var fn = regexp == undefined ? undefined : regexp[MATCH];
	    return fn !== undefined ? fn.call(regexp, O) : new RegExp(regexp)[MATCH](String(O));
	  }, $match];
	});


/***/ }),
/* 201 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var hide = __webpack_require__(10);
	var redefine = __webpack_require__(18);
	var fails = __webpack_require__(7);
	var defined = __webpack_require__(35);
	var wks = __webpack_require__(25);

	module.exports = function (KEY, length, exec) {
	  var SYMBOL = wks(KEY);
	  var fns = exec(defined, SYMBOL, ''[KEY]);
	  var strfn = fns[0];
	  var rxfn = fns[1];
	  if (fails(function () {
	    var O = {};
	    O[SYMBOL] = function () { return 7; };
	    return ''[KEY](O) != 7;
	  })) {
	    redefine(String.prototype, KEY, strfn);
	    hide(RegExp.prototype, SYMBOL, length == 2
	      // 21.2.5.8 RegExp.prototype[@@replace](string, replaceValue)
	      // 21.2.5.11 RegExp.prototype[@@split](string, limit)
	      ? function (string, arg) { return rxfn.call(string, this, arg); }
	      // 21.2.5.6 RegExp.prototype[@@match](string)
	      // 21.2.5.9 RegExp.prototype[@@search](string)
	      : function (string) { return rxfn.call(string, this); }
	    );
	  }
	};


/***/ }),
/* 202 */
/***/ (function(module, exports, __webpack_require__) {

	// @@replace logic
	__webpack_require__(201)('replace', 2, function (defined, REPLACE, $replace) {
	  // 21.1.3.14 String.prototype.replace(searchValue, replaceValue)
	  return [function replace(searchValue, replaceValue) {
	    'use strict';
	    var O = defined(this);
	    var fn = searchValue == undefined ? undefined : searchValue[REPLACE];
	    return fn !== undefined
	      ? fn.call(searchValue, O, replaceValue)
	      : $replace.call(String(O), searchValue, replaceValue);
	  }, $replace];
	});


/***/ }),
/* 203 */
/***/ (function(module, exports, __webpack_require__) {

	// @@search logic
	__webpack_require__(201)('search', 1, function (defined, SEARCH, $search) {
	  // 21.1.3.15 String.prototype.search(regexp)
	  return [function search(regexp) {
	    'use strict';
	    var O = defined(this);
	    var fn = regexp == undefined ? undefined : regexp[SEARCH];
	    return fn !== undefined ? fn.call(regexp, O) : new RegExp(regexp)[SEARCH](String(O));
	  }, $search];
	});


/***/ }),
/* 204 */
/***/ (function(module, exports, __webpack_require__) {

	// @@split logic
	__webpack_require__(201)('split', 2, function (defined, SPLIT, $split) {
	  'use strict';
	  var isRegExp = __webpack_require__(134);
	  var _split = $split;
	  var $push = [].push;
	  var $SPLIT = 'split';
	  var LENGTH = 'length';
	  var LAST_INDEX = 'lastIndex';
	  if (
	    'abbc'[$SPLIT](/(b)*/)[1] == 'c' ||
	    'test'[$SPLIT](/(?:)/, -1)[LENGTH] != 4 ||
	    'ab'[$SPLIT](/(?:ab)*/)[LENGTH] != 2 ||
	    '.'[$SPLIT](/(.?)(.?)/)[LENGTH] != 4 ||
	    '.'[$SPLIT](/()()/)[LENGTH] > 1 ||
	    ''[$SPLIT](/.?/)[LENGTH]
	  ) {
	    var NPCG = /()??/.exec('')[1] === undefined; // nonparticipating capturing group
	    // based on es5-shim implementation, need to rework it
	    $split = function (separator, limit) {
	      var string = String(this);
	      if (separator === undefined && limit === 0) return [];
	      // If `separator` is not a regex, use native split
	      if (!isRegExp(separator)) return _split.call(string, separator, limit);
	      var output = [];
	      var flags = (separator.ignoreCase ? 'i' : '') +
	                  (separator.multiline ? 'm' : '') +
	                  (separator.unicode ? 'u' : '') +
	                  (separator.sticky ? 'y' : '');
	      var lastLastIndex = 0;
	      var splitLimit = limit === undefined ? 4294967295 : limit >>> 0;
	      // Make `global` and avoid `lastIndex` issues by working with a copy
	      var separatorCopy = new RegExp(separator.source, flags + 'g');
	      var separator2, match, lastIndex, lastLength, i;
	      // Doesn't need flags gy, but they don't hurt
	      if (!NPCG) separator2 = new RegExp('^' + separatorCopy.source + '$(?!\\s)', flags);
	      while (match = separatorCopy.exec(string)) {
	        // `separatorCopy.lastIndex` is not reliable cross-browser
	        lastIndex = match.index + match[0][LENGTH];
	        if (lastIndex > lastLastIndex) {
	          output.push(string.slice(lastLastIndex, match.index));
	          // Fix browsers whose `exec` methods don't consistently return `undefined` for NPCG
	          // eslint-disable-next-line no-loop-func
	          if (!NPCG && match[LENGTH] > 1) match[0].replace(separator2, function () {
	            for (i = 1; i < arguments[LENGTH] - 2; i++) if (arguments[i] === undefined) match[i] = undefined;
	          });
	          if (match[LENGTH] > 1 && match.index < string[LENGTH]) $push.apply(output, match.slice(1));
	          lastLength = match[0][LENGTH];
	          lastLastIndex = lastIndex;
	          if (output[LENGTH] >= splitLimit) break;
	        }
	        if (separatorCopy[LAST_INDEX] === match.index) separatorCopy[LAST_INDEX]++; // Avoid an infinite loop
	      }
	      if (lastLastIndex === string[LENGTH]) {
	        if (lastLength || !separatorCopy.test('')) output.push('');
	      } else output.push(string.slice(lastLastIndex));
	      return output[LENGTH] > splitLimit ? output.slice(0, splitLimit) : output;
	    };
	  // Chakra, V8
	  } else if ('0'[$SPLIT](undefined, 0)[LENGTH]) {
	    $split = function (separator, limit) {
	      return separator === undefined && limit === 0 ? [] : _split.call(this, separator, limit);
	    };
	  }
	  // 21.1.3.17 String.prototype.split(separator, limit)
	  return [function split(separator, limit) {
	    var O = defined(this);
	    var fn = separator == undefined ? undefined : separator[SPLIT];
	    return fn !== undefined ? fn.call(separator, O, limit) : $split.call(String(O), separator, limit);
	  }, $split];
	});


/***/ }),
/* 205 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var LIBRARY = __webpack_require__(28);
	var global = __webpack_require__(4);
	var ctx = __webpack_require__(20);
	var classof = __webpack_require__(74);
	var $export = __webpack_require__(8);
	var isObject = __webpack_require__(13);
	var aFunction = __webpack_require__(21);
	var anInstance = __webpack_require__(206);
	var forOf = __webpack_require__(207);
	var speciesConstructor = __webpack_require__(208);
	var task = __webpack_require__(209).set;
	var microtask = __webpack_require__(210)();
	var newPromiseCapabilityModule = __webpack_require__(211);
	var perform = __webpack_require__(212);
	var promiseResolve = __webpack_require__(213);
	var PROMISE = 'Promise';
	var TypeError = global.TypeError;
	var process = global.process;
	var $Promise = global[PROMISE];
	var isNode = classof(process) == 'process';
	var empty = function () { /* empty */ };
	var Internal, newGenericPromiseCapability, OwnPromiseCapability, Wrapper;
	var newPromiseCapability = newGenericPromiseCapability = newPromiseCapabilityModule.f;

	var USE_NATIVE = !!function () {
	  try {
	    // correct subclassing with @@species support
	    var promise = $Promise.resolve(1);
	    var FakePromise = (promise.constructor = {})[__webpack_require__(25)('species')] = function (exec) {
	      exec(empty, empty);
	    };
	    // unhandled rejections tracking support, NodeJS Promise without it fails @@species test
	    return (isNode || typeof PromiseRejectionEvent == 'function') && promise.then(empty) instanceof FakePromise;
	  } catch (e) { /* empty */ }
	}();

	// helpers
	var isThenable = function (it) {
	  var then;
	  return isObject(it) && typeof (then = it.then) == 'function' ? then : false;
	};
	var notify = function (promise, isReject) {
	  if (promise._n) return;
	  promise._n = true;
	  var chain = promise._c;
	  microtask(function () {
	    var value = promise._v;
	    var ok = promise._s == 1;
	    var i = 0;
	    var run = function (reaction) {
	      var handler = ok ? reaction.ok : reaction.fail;
	      var resolve = reaction.resolve;
	      var reject = reaction.reject;
	      var domain = reaction.domain;
	      var result, then;
	      try {
	        if (handler) {
	          if (!ok) {
	            if (promise._h == 2) onHandleUnhandled(promise);
	            promise._h = 1;
	          }
	          if (handler === true) result = value;
	          else {
	            if (domain) domain.enter();
	            result = handler(value);
	            if (domain) domain.exit();
	          }
	          if (result === reaction.promise) {
	            reject(TypeError('Promise-chain cycle'));
	          } else if (then = isThenable(result)) {
	            then.call(result, resolve, reject);
	          } else resolve(result);
	        } else reject(value);
	      } catch (e) {
	        reject(e);
	      }
	    };
	    while (chain.length > i) run(chain[i++]); // variable length - can't use forEach
	    promise._c = [];
	    promise._n = false;
	    if (isReject && !promise._h) onUnhandled(promise);
	  });
	};
	var onUnhandled = function (promise) {
	  task.call(global, function () {
	    var value = promise._v;
	    var unhandled = isUnhandled(promise);
	    var result, handler, console;
	    if (unhandled) {
	      result = perform(function () {
	        if (isNode) {
	          process.emit('unhandledRejection', value, promise);
	        } else if (handler = global.onunhandledrejection) {
	          handler({ promise: promise, reason: value });
	        } else if ((console = global.console) && console.error) {
	          console.error('Unhandled promise rejection', value);
	        }
	      });
	      // Browsers should not trigger `rejectionHandled` event if it was handled here, NodeJS - should
	      promise._h = isNode || isUnhandled(promise) ? 2 : 1;
	    } promise._a = undefined;
	    if (unhandled && result.e) throw result.v;
	  });
	};
	var isUnhandled = function (promise) {
	  if (promise._h == 1) return false;
	  var chain = promise._a || promise._c;
	  var i = 0;
	  var reaction;
	  while (chain.length > i) {
	    reaction = chain[i++];
	    if (reaction.fail || !isUnhandled(reaction.promise)) return false;
	  } return true;
	};
	var onHandleUnhandled = function (promise) {
	  task.call(global, function () {
	    var handler;
	    if (isNode) {
	      process.emit('rejectionHandled', promise);
	    } else if (handler = global.onrejectionhandled) {
	      handler({ promise: promise, reason: promise._v });
	    }
	  });
	};
	var $reject = function (value) {
	  var promise = this;
	  if (promise._d) return;
	  promise._d = true;
	  promise = promise._w || promise; // unwrap
	  promise._v = value;
	  promise._s = 2;
	  if (!promise._a) promise._a = promise._c.slice();
	  notify(promise, true);
	};
	var $resolve = function (value) {
	  var promise = this;
	  var then;
	  if (promise._d) return;
	  promise._d = true;
	  promise = promise._w || promise; // unwrap
	  try {
	    if (promise === value) throw TypeError("Promise can't be resolved itself");
	    if (then = isThenable(value)) {
	      microtask(function () {
	        var wrapper = { _w: promise, _d: false }; // wrap
	        try {
	          then.call(value, ctx($resolve, wrapper, 1), ctx($reject, wrapper, 1));
	        } catch (e) {
	          $reject.call(wrapper, e);
	        }
	      });
	    } else {
	      promise._v = value;
	      promise._s = 1;
	      notify(promise, false);
	    }
	  } catch (e) {
	    $reject.call({ _w: promise, _d: false }, e); // wrap
	  }
	};

	// constructor polyfill
	if (!USE_NATIVE) {
	  // 25.4.3.1 Promise(executor)
	  $Promise = function Promise(executor) {
	    anInstance(this, $Promise, PROMISE, '_h');
	    aFunction(executor);
	    Internal.call(this);
	    try {
	      executor(ctx($resolve, this, 1), ctx($reject, this, 1));
	    } catch (err) {
	      $reject.call(this, err);
	    }
	  };
	  // eslint-disable-next-line no-unused-vars
	  Internal = function Promise(executor) {
	    this._c = [];             // <- awaiting reactions
	    this._a = undefined;      // <- checked in isUnhandled reactions
	    this._s = 0;              // <- state
	    this._d = false;          // <- done
	    this._v = undefined;      // <- value
	    this._h = 0;              // <- rejection state, 0 - default, 1 - handled, 2 - unhandled
	    this._n = false;          // <- notify
	  };
	  Internal.prototype = __webpack_require__(214)($Promise.prototype, {
	    // 25.4.5.3 Promise.prototype.then(onFulfilled, onRejected)
	    then: function then(onFulfilled, onRejected) {
	      var reaction = newPromiseCapability(speciesConstructor(this, $Promise));
	      reaction.ok = typeof onFulfilled == 'function' ? onFulfilled : true;
	      reaction.fail = typeof onRejected == 'function' && onRejected;
	      reaction.domain = isNode ? process.domain : undefined;
	      this._c.push(reaction);
	      if (this._a) this._a.push(reaction);
	      if (this._s) notify(this, false);
	      return reaction.promise;
	    },
	    // 25.4.5.1 Promise.prototype.catch(onRejected)
	    'catch': function (onRejected) {
	      return this.then(undefined, onRejected);
	    }
	  });
	  OwnPromiseCapability = function () {
	    var promise = new Internal();
	    this.promise = promise;
	    this.resolve = ctx($resolve, promise, 1);
	    this.reject = ctx($reject, promise, 1);
	  };
	  newPromiseCapabilityModule.f = newPromiseCapability = function (C) {
	    return C === $Promise || C === Wrapper
	      ? new OwnPromiseCapability(C)
	      : newGenericPromiseCapability(C);
	  };
	}

	$export($export.G + $export.W + $export.F * !USE_NATIVE, { Promise: $Promise });
	__webpack_require__(24)($Promise, PROMISE);
	__webpack_require__(193)(PROMISE);
	Wrapper = __webpack_require__(9)[PROMISE];

	// statics
	$export($export.S + $export.F * !USE_NATIVE, PROMISE, {
	  // 25.4.4.5 Promise.reject(r)
	  reject: function reject(r) {
	    var capability = newPromiseCapability(this);
	    var $$reject = capability.reject;
	    $$reject(r);
	    return capability.promise;
	  }
	});
	$export($export.S + $export.F * (LIBRARY || !USE_NATIVE), PROMISE, {
	  // 25.4.4.6 Promise.resolve(x)
	  resolve: function resolve(x) {
	    return promiseResolve(LIBRARY && this === Wrapper ? $Promise : this, x);
	  }
	});
	$export($export.S + $export.F * !(USE_NATIVE && __webpack_require__(166)(function (iter) {
	  $Promise.all(iter)['catch'](empty);
	})), PROMISE, {
	  // 25.4.4.1 Promise.all(iterable)
	  all: function all(iterable) {
	    var C = this;
	    var capability = newPromiseCapability(C);
	    var resolve = capability.resolve;
	    var reject = capability.reject;
	    var result = perform(function () {
	      var values = [];
	      var index = 0;
	      var remaining = 1;
	      forOf(iterable, false, function (promise) {
	        var $index = index++;
	        var alreadyCalled = false;
	        values.push(undefined);
	        remaining++;
	        C.resolve(promise).then(function (value) {
	          if (alreadyCalled) return;
	          alreadyCalled = true;
	          values[$index] = value;
	          --remaining || resolve(values);
	        }, reject);
	      });
	      --remaining || resolve(values);
	    });
	    if (result.e) reject(result.v);
	    return capability.promise;
	  },
	  // 25.4.4.4 Promise.race(iterable)
	  race: function race(iterable) {
	    var C = this;
	    var capability = newPromiseCapability(C);
	    var reject = capability.reject;
	    var result = perform(function () {
	      forOf(iterable, false, function (promise) {
	        C.resolve(promise).then(capability.resolve, reject);
	      });
	    });
	    if (result.e) reject(result.v);
	    return capability.promise;
	  }
	});


/***/ }),
/* 206 */
/***/ (function(module, exports) {

	module.exports = function (it, Constructor, name, forbiddenField) {
	  if (!(it instanceof Constructor) || (forbiddenField !== undefined && forbiddenField in it)) {
	    throw TypeError(name + ': incorrect invocation!');
	  } return it;
	};


/***/ }),
/* 207 */
/***/ (function(module, exports, __webpack_require__) {

	var ctx = __webpack_require__(20);
	var call = __webpack_require__(162);
	var isArrayIter = __webpack_require__(163);
	var anObject = __webpack_require__(12);
	var toLength = __webpack_require__(37);
	var getIterFn = __webpack_require__(165);
	var BREAK = {};
	var RETURN = {};
	var exports = module.exports = function (iterable, entries, fn, that, ITERATOR) {
	  var iterFn = ITERATOR ? function () { return iterable; } : getIterFn(iterable);
	  var f = ctx(fn, that, entries ? 2 : 1);
	  var index = 0;
	  var length, step, iterator, result;
	  if (typeof iterFn != 'function') throw TypeError(iterable + ' is not iterable!');
	  // fast case for arrays with default iterator
	  if (isArrayIter(iterFn)) for (length = toLength(iterable.length); length > index; index++) {
	    result = entries ? f(anObject(step = iterable[index])[0], step[1]) : f(iterable[index]);
	    if (result === BREAK || result === RETURN) return result;
	  } else for (iterator = iterFn.call(iterable); !(step = iterator.next()).done;) {
	    result = call(iterator, f, step.value, entries);
	    if (result === BREAK || result === RETURN) return result;
	  }
	};
	exports.BREAK = BREAK;
	exports.RETURN = RETURN;


/***/ }),
/* 208 */
/***/ (function(module, exports, __webpack_require__) {

	// 7.3.20 SpeciesConstructor(O, defaultConstructor)
	var anObject = __webpack_require__(12);
	var aFunction = __webpack_require__(21);
	var SPECIES = __webpack_require__(25)('species');
	module.exports = function (O, D) {
	  var C = anObject(O).constructor;
	  var S;
	  return C === undefined || (S = anObject(C)[SPECIES]) == undefined ? D : aFunction(S);
	};


/***/ }),
/* 209 */
/***/ (function(module, exports, __webpack_require__) {

	var ctx = __webpack_require__(20);
	var invoke = __webpack_require__(77);
	var html = __webpack_require__(47);
	var cel = __webpack_require__(15);
	var global = __webpack_require__(4);
	var process = global.process;
	var setTask = global.setImmediate;
	var clearTask = global.clearImmediate;
	var MessageChannel = global.MessageChannel;
	var Dispatch = global.Dispatch;
	var counter = 0;
	var queue = {};
	var ONREADYSTATECHANGE = 'onreadystatechange';
	var defer, channel, port;
	var run = function () {
	  var id = +this;
	  // eslint-disable-next-line no-prototype-builtins
	  if (queue.hasOwnProperty(id)) {
	    var fn = queue[id];
	    delete queue[id];
	    fn();
	  }
	};
	var listener = function (event) {
	  run.call(event.data);
	};
	// Node.js 0.9+ & IE10+ has setImmediate, otherwise:
	if (!setTask || !clearTask) {
	  setTask = function setImmediate(fn) {
	    var args = [];
	    var i = 1;
	    while (arguments.length > i) args.push(arguments[i++]);
	    queue[++counter] = function () {
	      // eslint-disable-next-line no-new-func
	      invoke(typeof fn == 'function' ? fn : Function(fn), args);
	    };
	    defer(counter);
	    return counter;
	  };
	  clearTask = function clearImmediate(id) {
	    delete queue[id];
	  };
	  // Node.js 0.8-
	  if (__webpack_require__(34)(process) == 'process') {
	    defer = function (id) {
	      process.nextTick(ctx(run, id, 1));
	    };
	  // Sphere (JS game engine) Dispatch API
	  } else if (Dispatch && Dispatch.now) {
	    defer = function (id) {
	      Dispatch.now(ctx(run, id, 1));
	    };
	  // Browsers with MessageChannel, includes WebWorkers
	  } else if (MessageChannel) {
	    channel = new MessageChannel();
	    port = channel.port2;
	    channel.port1.onmessage = listener;
	    defer = ctx(port.postMessage, port, 1);
	  // Browsers with postMessage, skip WebWorkers
	  // IE8 has postMessage, but it's sync & typeof its postMessage is 'object'
	  } else if (global.addEventListener && typeof postMessage == 'function' && !global.importScripts) {
	    defer = function (id) {
	      global.postMessage(id + '', '*');
	    };
	    global.addEventListener('message', listener, false);
	  // IE8-
	  } else if (ONREADYSTATECHANGE in cel('script')) {
	    defer = function (id) {
	      html.appendChild(cel('script'))[ONREADYSTATECHANGE] = function () {
	        html.removeChild(this);
	        run.call(id);
	      };
	    };
	  // Rest old browsers
	  } else {
	    defer = function (id) {
	      setTimeout(ctx(run, id, 1), 0);
	    };
	  }
	}
	module.exports = {
	  set: setTask,
	  clear: clearTask
	};


/***/ }),
/* 210 */
/***/ (function(module, exports, __webpack_require__) {

	var global = __webpack_require__(4);
	var macrotask = __webpack_require__(209).set;
	var Observer = global.MutationObserver || global.WebKitMutationObserver;
	var process = global.process;
	var Promise = global.Promise;
	var isNode = __webpack_require__(34)(process) == 'process';

	module.exports = function () {
	  var head, last, notify;

	  var flush = function () {
	    var parent, fn;
	    if (isNode && (parent = process.domain)) parent.exit();
	    while (head) {
	      fn = head.fn;
	      head = head.next;
	      try {
	        fn();
	      } catch (e) {
	        if (head) notify();
	        else last = undefined;
	        throw e;
	      }
	    } last = undefined;
	    if (parent) parent.enter();
	  };

	  // Node.js
	  if (isNode) {
	    notify = function () {
	      process.nextTick(flush);
	    };
	  // browsers with MutationObserver
	  } else if (Observer) {
	    var toggle = true;
	    var node = document.createTextNode('');
	    new Observer(flush).observe(node, { characterData: true }); // eslint-disable-line no-new
	    notify = function () {
	      node.data = toggle = !toggle;
	    };
	  // environments with maybe non-completely correct, but existent Promise
	  } else if (Promise && Promise.resolve) {
	    var promise = Promise.resolve();
	    notify = function () {
	      promise.then(flush);
	    };
	  // for other environments - macrotask based on:
	  // - setImmediate
	  // - MessageChannel
	  // - window.postMessag
	  // - onreadystatechange
	  // - setTimeout
	  } else {
	    notify = function () {
	      // strange IE + webpack dev server bug - use .call(global)
	      macrotask.call(global, flush);
	    };
	  }

	  return function (fn) {
	    var task = { fn: fn, next: undefined };
	    if (last) last.next = task;
	    if (!head) {
	      head = task;
	      notify();
	    } last = task;
	  };
	};


/***/ }),
/* 211 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// 25.4.1.5 NewPromiseCapability(C)
	var aFunction = __webpack_require__(21);

	function PromiseCapability(C) {
	  var resolve, reject;
	  this.promise = new C(function ($$resolve, $$reject) {
	    if (resolve !== undefined || reject !== undefined) throw TypeError('Bad Promise constructor');
	    resolve = $$resolve;
	    reject = $$reject;
	  });
	  this.resolve = aFunction(resolve);
	  this.reject = aFunction(reject);
	}

	module.exports.f = function (C) {
	  return new PromiseCapability(C);
	};


/***/ }),
/* 212 */
/***/ (function(module, exports) {

	module.exports = function (exec) {
	  try {
	    return { e: false, v: exec() };
	  } catch (e) {
	    return { e: true, v: e };
	  }
	};


/***/ }),
/* 213 */
/***/ (function(module, exports, __webpack_require__) {

	var anObject = __webpack_require__(12);
	var isObject = __webpack_require__(13);
	var newPromiseCapability = __webpack_require__(211);

	module.exports = function (C, x) {
	  anObject(C);
	  if (isObject(x) && x.constructor === C) return x;
	  var promiseCapability = newPromiseCapability.f(C);
	  var resolve = promiseCapability.resolve;
	  resolve(x);
	  return promiseCapability.promise;
	};


/***/ }),
/* 214 */
/***/ (function(module, exports, __webpack_require__) {

	var redefine = __webpack_require__(18);
	module.exports = function (target, src, safe) {
	  for (var key in src) redefine(target, key, src[key], safe);
	  return target;
	};


/***/ }),
/* 215 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var strong = __webpack_require__(216);
	var validate = __webpack_require__(217);
	var MAP = 'Map';

	// 23.1 Map Objects
	module.exports = __webpack_require__(218)(MAP, function (get) {
	  return function Map() { return get(this, arguments.length > 0 ? arguments[0] : undefined); };
	}, {
	  // 23.1.3.6 Map.prototype.get(key)
	  get: function get(key) {
	    var entry = strong.getEntry(validate(this, MAP), key);
	    return entry && entry.v;
	  },
	  // 23.1.3.9 Map.prototype.set(key, value)
	  set: function set(key, value) {
	    return strong.def(validate(this, MAP), key === 0 ? 0 : key, value);
	  }
	}, strong, true);


/***/ }),
/* 216 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var dP = __webpack_require__(11).f;
	var create = __webpack_require__(45);
	var redefineAll = __webpack_require__(214);
	var ctx = __webpack_require__(20);
	var anInstance = __webpack_require__(206);
	var forOf = __webpack_require__(207);
	var $iterDefine = __webpack_require__(128);
	var step = __webpack_require__(195);
	var setSpecies = __webpack_require__(193);
	var DESCRIPTORS = __webpack_require__(6);
	var fastKey = __webpack_require__(22).fastKey;
	var validate = __webpack_require__(217);
	var SIZE = DESCRIPTORS ? '_s' : 'size';

	var getEntry = function (that, key) {
	  // fast case
	  var index = fastKey(key);
	  var entry;
	  if (index !== 'F') return that._i[index];
	  // frozen object case
	  for (entry = that._f; entry; entry = entry.n) {
	    if (entry.k == key) return entry;
	  }
	};

	module.exports = {
	  getConstructor: function (wrapper, NAME, IS_MAP, ADDER) {
	    var C = wrapper(function (that, iterable) {
	      anInstance(that, C, NAME, '_i');
	      that._t = NAME;         // collection type
	      that._i = create(null); // index
	      that._f = undefined;    // first entry
	      that._l = undefined;    // last entry
	      that[SIZE] = 0;         // size
	      if (iterable != undefined) forOf(iterable, IS_MAP, that[ADDER], that);
	    });
	    redefineAll(C.prototype, {
	      // 23.1.3.1 Map.prototype.clear()
	      // 23.2.3.2 Set.prototype.clear()
	      clear: function clear() {
	        for (var that = validate(this, NAME), data = that._i, entry = that._f; entry; entry = entry.n) {
	          entry.r = true;
	          if (entry.p) entry.p = entry.p.n = undefined;
	          delete data[entry.i];
	        }
	        that._f = that._l = undefined;
	        that[SIZE] = 0;
	      },
	      // 23.1.3.3 Map.prototype.delete(key)
	      // 23.2.3.4 Set.prototype.delete(value)
	      'delete': function (key) {
	        var that = validate(this, NAME);
	        var entry = getEntry(that, key);
	        if (entry) {
	          var next = entry.n;
	          var prev = entry.p;
	          delete that._i[entry.i];
	          entry.r = true;
	          if (prev) prev.n = next;
	          if (next) next.p = prev;
	          if (that._f == entry) that._f = next;
	          if (that._l == entry) that._l = prev;
	          that[SIZE]--;
	        } return !!entry;
	      },
	      // 23.2.3.6 Set.prototype.forEach(callbackfn, thisArg = undefined)
	      // 23.1.3.5 Map.prototype.forEach(callbackfn, thisArg = undefined)
	      forEach: function forEach(callbackfn /* , that = undefined */) {
	        validate(this, NAME);
	        var f = ctx(callbackfn, arguments.length > 1 ? arguments[1] : undefined, 3);
	        var entry;
	        while (entry = entry ? entry.n : this._f) {
	          f(entry.v, entry.k, this);
	          // revert to the last existing entry
	          while (entry && entry.r) entry = entry.p;
	        }
	      },
	      // 23.1.3.7 Map.prototype.has(key)
	      // 23.2.3.7 Set.prototype.has(value)
	      has: function has(key) {
	        return !!getEntry(validate(this, NAME), key);
	      }
	    });
	    if (DESCRIPTORS) dP(C.prototype, 'size', {
	      get: function () {
	        return validate(this, NAME)[SIZE];
	      }
	    });
	    return C;
	  },
	  def: function (that, key, value) {
	    var entry = getEntry(that, key);
	    var prev, index;
	    // change existing entry
	    if (entry) {
	      entry.v = value;
	    // create new entry
	    } else {
	      that._l = entry = {
	        i: index = fastKey(key, true), // <- index
	        k: key,                        // <- key
	        v: value,                      // <- value
	        p: prev = that._l,             // <- previous entry
	        n: undefined,                  // <- next entry
	        r: false                       // <- removed
	      };
	      if (!that._f) that._f = entry;
	      if (prev) prev.n = entry;
	      that[SIZE]++;
	      // add to index
	      if (index !== 'F') that._i[index] = entry;
	    } return that;
	  },
	  getEntry: getEntry,
	  setStrong: function (C, NAME, IS_MAP) {
	    // add .keys, .values, .entries, [@@iterator]
	    // 23.1.3.4, 23.1.3.8, 23.1.3.11, 23.1.3.12, 23.2.3.5, 23.2.3.8, 23.2.3.10, 23.2.3.11
	    $iterDefine(C, NAME, function (iterated, kind) {
	      this._t = validate(iterated, NAME); // target
	      this._k = kind;                     // kind
	      this._l = undefined;                // previous
	    }, function () {
	      var that = this;
	      var kind = that._k;
	      var entry = that._l;
	      // revert to the last existing entry
	      while (entry && entry.r) entry = entry.p;
	      // get next entry
	      if (!that._t || !(that._l = entry = entry ? entry.n : that._t._f)) {
	        // or finish the iteration
	        that._t = undefined;
	        return step(1);
	      }
	      // return step by kind
	      if (kind == 'keys') return step(0, entry.k);
	      if (kind == 'values') return step(0, entry.v);
	      return step(0, [entry.k, entry.v]);
	    }, IS_MAP ? 'entries' : 'values', !IS_MAP, true);

	    // add [@@species], 23.1.2.2, 23.2.2.2
	    setSpecies(NAME);
	  }
	};


/***/ }),
/* 217 */
/***/ (function(module, exports, __webpack_require__) {

	var isObject = __webpack_require__(13);
	module.exports = function (it, TYPE) {
	  if (!isObject(it) || it._t !== TYPE) throw TypeError('Incompatible receiver, ' + TYPE + ' required!');
	  return it;
	};


/***/ }),
/* 218 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var global = __webpack_require__(4);
	var $export = __webpack_require__(8);
	var redefine = __webpack_require__(18);
	var redefineAll = __webpack_require__(214);
	var meta = __webpack_require__(22);
	var forOf = __webpack_require__(207);
	var anInstance = __webpack_require__(206);
	var isObject = __webpack_require__(13);
	var fails = __webpack_require__(7);
	var $iterDetect = __webpack_require__(166);
	var setToStringTag = __webpack_require__(24);
	var inheritIfRequired = __webpack_require__(87);

	module.exports = function (NAME, wrapper, methods, common, IS_MAP, IS_WEAK) {
	  var Base = global[NAME];
	  var C = Base;
	  var ADDER = IS_MAP ? 'set' : 'add';
	  var proto = C && C.prototype;
	  var O = {};
	  var fixMethod = function (KEY) {
	    var fn = proto[KEY];
	    redefine(proto, KEY,
	      KEY == 'delete' ? function (a) {
	        return IS_WEAK && !isObject(a) ? false : fn.call(this, a === 0 ? 0 : a);
	      } : KEY == 'has' ? function has(a) {
	        return IS_WEAK && !isObject(a) ? false : fn.call(this, a === 0 ? 0 : a);
	      } : KEY == 'get' ? function get(a) {
	        return IS_WEAK && !isObject(a) ? undefined : fn.call(this, a === 0 ? 0 : a);
	      } : KEY == 'add' ? function add(a) { fn.call(this, a === 0 ? 0 : a); return this; }
	        : function set(a, b) { fn.call(this, a === 0 ? 0 : a, b); return this; }
	    );
	  };
	  if (typeof C != 'function' || !(IS_WEAK || proto.forEach && !fails(function () {
	    new C().entries().next();
	  }))) {
	    // create collection constructor
	    C = common.getConstructor(wrapper, NAME, IS_MAP, ADDER);
	    redefineAll(C.prototype, methods);
	    meta.NEED = true;
	  } else {
	    var instance = new C();
	    // early implementations not supports chaining
	    var HASNT_CHAINING = instance[ADDER](IS_WEAK ? {} : -0, 1) != instance;
	    // V8 ~  Chromium 40- weak-collections throws on primitives, but should return false
	    var THROWS_ON_PRIMITIVES = fails(function () { instance.has(1); });
	    // most early implementations doesn't supports iterables, most modern - not close it correctly
	    var ACCEPT_ITERABLES = $iterDetect(function (iter) { new C(iter); }); // eslint-disable-line no-new
	    // for early implementations -0 and +0 not the same
	    var BUGGY_ZERO = !IS_WEAK && fails(function () {
	      // V8 ~ Chromium 42- fails only with 5+ elements
	      var $instance = new C();
	      var index = 5;
	      while (index--) $instance[ADDER](index, index);
	      return !$instance.has(-0);
	    });
	    if (!ACCEPT_ITERABLES) {
	      C = wrapper(function (target, iterable) {
	        anInstance(target, C, NAME);
	        var that = inheritIfRequired(new Base(), target, C);
	        if (iterable != undefined) forOf(iterable, IS_MAP, that[ADDER], that);
	        return that;
	      });
	      C.prototype = proto;
	      proto.constructor = C;
	    }
	    if (THROWS_ON_PRIMITIVES || BUGGY_ZERO) {
	      fixMethod('delete');
	      fixMethod('has');
	      IS_MAP && fixMethod('get');
	    }
	    if (BUGGY_ZERO || HASNT_CHAINING) fixMethod(ADDER);
	    // weak collections should not contains .clear method
	    if (IS_WEAK && proto.clear) delete proto.clear;
	  }

	  setToStringTag(C, NAME);

	  O[NAME] = C;
	  $export($export.G + $export.W + $export.F * (C != Base), O);

	  if (!IS_WEAK) common.setStrong(C, NAME, IS_MAP);

	  return C;
	};


/***/ }),
/* 219 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var strong = __webpack_require__(216);
	var validate = __webpack_require__(217);
	var SET = 'Set';

	// 23.2 Set Objects
	module.exports = __webpack_require__(218)(SET, function (get) {
	  return function Set() { return get(this, arguments.length > 0 ? arguments[0] : undefined); };
	}, {
	  // 23.2.3.1 Set.prototype.add(value)
	  add: function add(value) {
	    return strong.def(validate(this, SET), value = value === 0 ? 0 : value, value);
	  }
	}, strong);


/***/ }),
/* 220 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var each = __webpack_require__(173)(0);
	var redefine = __webpack_require__(18);
	var meta = __webpack_require__(22);
	var assign = __webpack_require__(68);
	var weak = __webpack_require__(221);
	var isObject = __webpack_require__(13);
	var fails = __webpack_require__(7);
	var validate = __webpack_require__(217);
	var WEAK_MAP = 'WeakMap';
	var getWeak = meta.getWeak;
	var isExtensible = Object.isExtensible;
	var uncaughtFrozenStore = weak.ufstore;
	var tmp = {};
	var InternalMap;

	var wrapper = function (get) {
	  return function WeakMap() {
	    return get(this, arguments.length > 0 ? arguments[0] : undefined);
	  };
	};

	var methods = {
	  // 23.3.3.3 WeakMap.prototype.get(key)
	  get: function get(key) {
	    if (isObject(key)) {
	      var data = getWeak(key);
	      if (data === true) return uncaughtFrozenStore(validate(this, WEAK_MAP)).get(key);
	      return data ? data[this._i] : undefined;
	    }
	  },
	  // 23.3.3.5 WeakMap.prototype.set(key, value)
	  set: function set(key, value) {
	    return weak.def(validate(this, WEAK_MAP), key, value);
	  }
	};

	// 23.3 WeakMap Objects
	var $WeakMap = module.exports = __webpack_require__(218)(WEAK_MAP, wrapper, methods, weak, true, true);

	// IE11 WeakMap frozen keys fix
	if (fails(function () { return new $WeakMap().set((Object.freeze || Object)(tmp), 7).get(tmp) != 7; })) {
	  InternalMap = weak.getConstructor(wrapper, WEAK_MAP);
	  assign(InternalMap.prototype, methods);
	  meta.NEED = true;
	  each(['delete', 'has', 'get', 'set'], function (key) {
	    var proto = $WeakMap.prototype;
	    var method = proto[key];
	    redefine(proto, key, function (a, b) {
	      // store frozen objects on internal weakmap shim
	      if (isObject(a) && !isExtensible(a)) {
	        if (!this._f) this._f = new InternalMap();
	        var result = this._f[key](a, b);
	        return key == 'set' ? this : result;
	      // store all the rest on native weakmap
	      } return method.call(this, a, b);
	    });
	  });
	}


/***/ }),
/* 221 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var redefineAll = __webpack_require__(214);
	var getWeak = __webpack_require__(22).getWeak;
	var anObject = __webpack_require__(12);
	var isObject = __webpack_require__(13);
	var anInstance = __webpack_require__(206);
	var forOf = __webpack_require__(207);
	var createArrayMethod = __webpack_require__(173);
	var $has = __webpack_require__(5);
	var validate = __webpack_require__(217);
	var arrayFind = createArrayMethod(5);
	var arrayFindIndex = createArrayMethod(6);
	var id = 0;

	// fallback for uncaught frozen keys
	var uncaughtFrozenStore = function (that) {
	  return that._l || (that._l = new UncaughtFrozenStore());
	};
	var UncaughtFrozenStore = function () {
	  this.a = [];
	};
	var findUncaughtFrozen = function (store, key) {
	  return arrayFind(store.a, function (it) {
	    return it[0] === key;
	  });
	};
	UncaughtFrozenStore.prototype = {
	  get: function (key) {
	    var entry = findUncaughtFrozen(this, key);
	    if (entry) return entry[1];
	  },
	  has: function (key) {
	    return !!findUncaughtFrozen(this, key);
	  },
	  set: function (key, value) {
	    var entry = findUncaughtFrozen(this, key);
	    if (entry) entry[1] = value;
	    else this.a.push([key, value]);
	  },
	  'delete': function (key) {
	    var index = arrayFindIndex(this.a, function (it) {
	      return it[0] === key;
	    });
	    if (~index) this.a.splice(index, 1);
	    return !!~index;
	  }
	};

	module.exports = {
	  getConstructor: function (wrapper, NAME, IS_MAP, ADDER) {
	    var C = wrapper(function (that, iterable) {
	      anInstance(that, C, NAME, '_i');
	      that._t = NAME;      // collection type
	      that._i = id++;      // collection id
	      that._l = undefined; // leak store for uncaught frozen objects
	      if (iterable != undefined) forOf(iterable, IS_MAP, that[ADDER], that);
	    });
	    redefineAll(C.prototype, {
	      // 23.3.3.2 WeakMap.prototype.delete(key)
	      // 23.4.3.3 WeakSet.prototype.delete(value)
	      'delete': function (key) {
	        if (!isObject(key)) return false;
	        var data = getWeak(key);
	        if (data === true) return uncaughtFrozenStore(validate(this, NAME))['delete'](key);
	        return data && $has(data, this._i) && delete data[this._i];
	      },
	      // 23.3.3.4 WeakMap.prototype.has(key)
	      // 23.4.3.4 WeakSet.prototype.has(value)
	      has: function has(key) {
	        if (!isObject(key)) return false;
	        var data = getWeak(key);
	        if (data === true) return uncaughtFrozenStore(validate(this, NAME)).has(key);
	        return data && $has(data, this._i);
	      }
	    });
	    return C;
	  },
	  def: function (that, key, value) {
	    var data = getWeak(anObject(key), true);
	    if (data === true) uncaughtFrozenStore(that).set(key, value);
	    else data[that._i] = value;
	    return that;
	  },
	  ufstore: uncaughtFrozenStore
	};


/***/ }),
/* 222 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var weak = __webpack_require__(221);
	var validate = __webpack_require__(217);
	var WEAK_SET = 'WeakSet';

	// 23.4 WeakSet Objects
	__webpack_require__(218)(WEAK_SET, function (get) {
	  return function WeakSet() { return get(this, arguments.length > 0 ? arguments[0] : undefined); };
	}, {
	  // 23.4.3.1 WeakSet.prototype.add(value)
	  add: function add(value) {
	    return weak.def(validate(this, WEAK_SET), value, true);
	  }
	}, weak, false, true);


/***/ }),
/* 223 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var $typed = __webpack_require__(224);
	var buffer = __webpack_require__(225);
	var anObject = __webpack_require__(12);
	var toAbsoluteIndex = __webpack_require__(39);
	var toLength = __webpack_require__(37);
	var isObject = __webpack_require__(13);
	var ArrayBuffer = __webpack_require__(4).ArrayBuffer;
	var speciesConstructor = __webpack_require__(208);
	var $ArrayBuffer = buffer.ArrayBuffer;
	var $DataView = buffer.DataView;
	var $isView = $typed.ABV && ArrayBuffer.isView;
	var $slice = $ArrayBuffer.prototype.slice;
	var VIEW = $typed.VIEW;
	var ARRAY_BUFFER = 'ArrayBuffer';

	$export($export.G + $export.W + $export.F * (ArrayBuffer !== $ArrayBuffer), { ArrayBuffer: $ArrayBuffer });

	$export($export.S + $export.F * !$typed.CONSTR, ARRAY_BUFFER, {
	  // 24.1.3.1 ArrayBuffer.isView(arg)
	  isView: function isView(it) {
	    return $isView && $isView(it) || isObject(it) && VIEW in it;
	  }
	});

	$export($export.P + $export.U + $export.F * __webpack_require__(7)(function () {
	  return !new $ArrayBuffer(2).slice(1, undefined).byteLength;
	}), ARRAY_BUFFER, {
	  // 24.1.4.3 ArrayBuffer.prototype.slice(start, end)
	  slice: function slice(start, end) {
	    if ($slice !== undefined && end === undefined) return $slice.call(anObject(this), start); // FF fix
	    var len = anObject(this).byteLength;
	    var first = toAbsoluteIndex(start, len);
	    var final = toAbsoluteIndex(end === undefined ? len : end, len);
	    var result = new (speciesConstructor(this, $ArrayBuffer))(toLength(final - first));
	    var viewS = new $DataView(this);
	    var viewT = new $DataView(result);
	    var index = 0;
	    while (first < final) {
	      viewT.setUint8(index++, viewS.getUint8(first++));
	    } return result;
	  }
	});

	__webpack_require__(193)(ARRAY_BUFFER);


/***/ }),
/* 224 */
/***/ (function(module, exports, __webpack_require__) {

	var global = __webpack_require__(4);
	var hide = __webpack_require__(10);
	var uid = __webpack_require__(19);
	var TYPED = uid('typed_array');
	var VIEW = uid('view');
	var ABV = !!(global.ArrayBuffer && global.DataView);
	var CONSTR = ABV;
	var i = 0;
	var l = 9;
	var Typed;

	var TypedArrayConstructors = (
	  'Int8Array,Uint8Array,Uint8ClampedArray,Int16Array,Uint16Array,Int32Array,Uint32Array,Float32Array,Float64Array'
	).split(',');

	while (i < l) {
	  if (Typed = global[TypedArrayConstructors[i++]]) {
	    hide(Typed.prototype, TYPED, true);
	    hide(Typed.prototype, VIEW, true);
	  } else CONSTR = false;
	}

	module.exports = {
	  ABV: ABV,
	  CONSTR: CONSTR,
	  TYPED: TYPED,
	  VIEW: VIEW
	};


/***/ }),
/* 225 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var global = __webpack_require__(4);
	var DESCRIPTORS = __webpack_require__(6);
	var LIBRARY = __webpack_require__(28);
	var $typed = __webpack_require__(224);
	var hide = __webpack_require__(10);
	var redefineAll = __webpack_require__(214);
	var fails = __webpack_require__(7);
	var anInstance = __webpack_require__(206);
	var toInteger = __webpack_require__(38);
	var toLength = __webpack_require__(37);
	var toIndex = __webpack_require__(226);
	var gOPN = __webpack_require__(49).f;
	var dP = __webpack_require__(11).f;
	var arrayFill = __webpack_require__(189);
	var setToStringTag = __webpack_require__(24);
	var ARRAY_BUFFER = 'ArrayBuffer';
	var DATA_VIEW = 'DataView';
	var PROTOTYPE = 'prototype';
	var WRONG_LENGTH = 'Wrong length!';
	var WRONG_INDEX = 'Wrong index!';
	var $ArrayBuffer = global[ARRAY_BUFFER];
	var $DataView = global[DATA_VIEW];
	var Math = global.Math;
	var RangeError = global.RangeError;
	// eslint-disable-next-line no-shadow-restricted-names
	var Infinity = global.Infinity;
	var BaseBuffer = $ArrayBuffer;
	var abs = Math.abs;
	var pow = Math.pow;
	var floor = Math.floor;
	var log = Math.log;
	var LN2 = Math.LN2;
	var BUFFER = 'buffer';
	var BYTE_LENGTH = 'byteLength';
	var BYTE_OFFSET = 'byteOffset';
	var $BUFFER = DESCRIPTORS ? '_b' : BUFFER;
	var $LENGTH = DESCRIPTORS ? '_l' : BYTE_LENGTH;
	var $OFFSET = DESCRIPTORS ? '_o' : BYTE_OFFSET;

	// IEEE754 conversions based on https://github.com/feross/ieee754
	function packIEEE754(value, mLen, nBytes) {
	  var buffer = Array(nBytes);
	  var eLen = nBytes * 8 - mLen - 1;
	  var eMax = (1 << eLen) - 1;
	  var eBias = eMax >> 1;
	  var rt = mLen === 23 ? pow(2, -24) - pow(2, -77) : 0;
	  var i = 0;
	  var s = value < 0 || value === 0 && 1 / value < 0 ? 1 : 0;
	  var e, m, c;
	  value = abs(value);
	  // eslint-disable-next-line no-self-compare
	  if (value != value || value === Infinity) {
	    // eslint-disable-next-line no-self-compare
	    m = value != value ? 1 : 0;
	    e = eMax;
	  } else {
	    e = floor(log(value) / LN2);
	    if (value * (c = pow(2, -e)) < 1) {
	      e--;
	      c *= 2;
	    }
	    if (e + eBias >= 1) {
	      value += rt / c;
	    } else {
	      value += rt * pow(2, 1 - eBias);
	    }
	    if (value * c >= 2) {
	      e++;
	      c /= 2;
	    }
	    if (e + eBias >= eMax) {
	      m = 0;
	      e = eMax;
	    } else if (e + eBias >= 1) {
	      m = (value * c - 1) * pow(2, mLen);
	      e = e + eBias;
	    } else {
	      m = value * pow(2, eBias - 1) * pow(2, mLen);
	      e = 0;
	    }
	  }
	  for (; mLen >= 8; buffer[i++] = m & 255, m /= 256, mLen -= 8);
	  e = e << mLen | m;
	  eLen += mLen;
	  for (; eLen > 0; buffer[i++] = e & 255, e /= 256, eLen -= 8);
	  buffer[--i] |= s * 128;
	  return buffer;
	}
	function unpackIEEE754(buffer, mLen, nBytes) {
	  var eLen = nBytes * 8 - mLen - 1;
	  var eMax = (1 << eLen) - 1;
	  var eBias = eMax >> 1;
	  var nBits = eLen - 7;
	  var i = nBytes - 1;
	  var s = buffer[i--];
	  var e = s & 127;
	  var m;
	  s >>= 7;
	  for (; nBits > 0; e = e * 256 + buffer[i], i--, nBits -= 8);
	  m = e & (1 << -nBits) - 1;
	  e >>= -nBits;
	  nBits += mLen;
	  for (; nBits > 0; m = m * 256 + buffer[i], i--, nBits -= 8);
	  if (e === 0) {
	    e = 1 - eBias;
	  } else if (e === eMax) {
	    return m ? NaN : s ? -Infinity : Infinity;
	  } else {
	    m = m + pow(2, mLen);
	    e = e - eBias;
	  } return (s ? -1 : 1) * m * pow(2, e - mLen);
	}

	function unpackI32(bytes) {
	  return bytes[3] << 24 | bytes[2] << 16 | bytes[1] << 8 | bytes[0];
	}
	function packI8(it) {
	  return [it & 0xff];
	}
	function packI16(it) {
	  return [it & 0xff, it >> 8 & 0xff];
	}
	function packI32(it) {
	  return [it & 0xff, it >> 8 & 0xff, it >> 16 & 0xff, it >> 24 & 0xff];
	}
	function packF64(it) {
	  return packIEEE754(it, 52, 8);
	}
	function packF32(it) {
	  return packIEEE754(it, 23, 4);
	}

	function addGetter(C, key, internal) {
	  dP(C[PROTOTYPE], key, { get: function () { return this[internal]; } });
	}

	function get(view, bytes, index, isLittleEndian) {
	  var numIndex = +index;
	  var intIndex = toIndex(numIndex);
	  if (intIndex + bytes > view[$LENGTH]) throw RangeError(WRONG_INDEX);
	  var store = view[$BUFFER]._b;
	  var start = intIndex + view[$OFFSET];
	  var pack = store.slice(start, start + bytes);
	  return isLittleEndian ? pack : pack.reverse();
	}
	function set(view, bytes, index, conversion, value, isLittleEndian) {
	  var numIndex = +index;
	  var intIndex = toIndex(numIndex);
	  if (intIndex + bytes > view[$LENGTH]) throw RangeError(WRONG_INDEX);
	  var store = view[$BUFFER]._b;
	  var start = intIndex + view[$OFFSET];
	  var pack = conversion(+value);
	  for (var i = 0; i < bytes; i++) store[start + i] = pack[isLittleEndian ? i : bytes - i - 1];
	}

	if (!$typed.ABV) {
	  $ArrayBuffer = function ArrayBuffer(length) {
	    anInstance(this, $ArrayBuffer, ARRAY_BUFFER);
	    var byteLength = toIndex(length);
	    this._b = arrayFill.call(Array(byteLength), 0);
	    this[$LENGTH] = byteLength;
	  };

	  $DataView = function DataView(buffer, byteOffset, byteLength) {
	    anInstance(this, $DataView, DATA_VIEW);
	    anInstance(buffer, $ArrayBuffer, DATA_VIEW);
	    var bufferLength = buffer[$LENGTH];
	    var offset = toInteger(byteOffset);
	    if (offset < 0 || offset > bufferLength) throw RangeError('Wrong offset!');
	    byteLength = byteLength === undefined ? bufferLength - offset : toLength(byteLength);
	    if (offset + byteLength > bufferLength) throw RangeError(WRONG_LENGTH);
	    this[$BUFFER] = buffer;
	    this[$OFFSET] = offset;
	    this[$LENGTH] = byteLength;
	  };

	  if (DESCRIPTORS) {
	    addGetter($ArrayBuffer, BYTE_LENGTH, '_l');
	    addGetter($DataView, BUFFER, '_b');
	    addGetter($DataView, BYTE_LENGTH, '_l');
	    addGetter($DataView, BYTE_OFFSET, '_o');
	  }

	  redefineAll($DataView[PROTOTYPE], {
	    getInt8: function getInt8(byteOffset) {
	      return get(this, 1, byteOffset)[0] << 24 >> 24;
	    },
	    getUint8: function getUint8(byteOffset) {
	      return get(this, 1, byteOffset)[0];
	    },
	    getInt16: function getInt16(byteOffset /* , littleEndian */) {
	      var bytes = get(this, 2, byteOffset, arguments[1]);
	      return (bytes[1] << 8 | bytes[0]) << 16 >> 16;
	    },
	    getUint16: function getUint16(byteOffset /* , littleEndian */) {
	      var bytes = get(this, 2, byteOffset, arguments[1]);
	      return bytes[1] << 8 | bytes[0];
	    },
	    getInt32: function getInt32(byteOffset /* , littleEndian */) {
	      return unpackI32(get(this, 4, byteOffset, arguments[1]));
	    },
	    getUint32: function getUint32(byteOffset /* , littleEndian */) {
	      return unpackI32(get(this, 4, byteOffset, arguments[1])) >>> 0;
	    },
	    getFloat32: function getFloat32(byteOffset /* , littleEndian */) {
	      return unpackIEEE754(get(this, 4, byteOffset, arguments[1]), 23, 4);
	    },
	    getFloat64: function getFloat64(byteOffset /* , littleEndian */) {
	      return unpackIEEE754(get(this, 8, byteOffset, arguments[1]), 52, 8);
	    },
	    setInt8: function setInt8(byteOffset, value) {
	      set(this, 1, byteOffset, packI8, value);
	    },
	    setUint8: function setUint8(byteOffset, value) {
	      set(this, 1, byteOffset, packI8, value);
	    },
	    setInt16: function setInt16(byteOffset, value /* , littleEndian */) {
	      set(this, 2, byteOffset, packI16, value, arguments[2]);
	    },
	    setUint16: function setUint16(byteOffset, value /* , littleEndian */) {
	      set(this, 2, byteOffset, packI16, value, arguments[2]);
	    },
	    setInt32: function setInt32(byteOffset, value /* , littleEndian */) {
	      set(this, 4, byteOffset, packI32, value, arguments[2]);
	    },
	    setUint32: function setUint32(byteOffset, value /* , littleEndian */) {
	      set(this, 4, byteOffset, packI32, value, arguments[2]);
	    },
	    setFloat32: function setFloat32(byteOffset, value /* , littleEndian */) {
	      set(this, 4, byteOffset, packF32, value, arguments[2]);
	    },
	    setFloat64: function setFloat64(byteOffset, value /* , littleEndian */) {
	      set(this, 8, byteOffset, packF64, value, arguments[2]);
	    }
	  });
	} else {
	  if (!fails(function () {
	    $ArrayBuffer(1);
	  }) || !fails(function () {
	    new $ArrayBuffer(-1); // eslint-disable-line no-new
	  }) || fails(function () {
	    new $ArrayBuffer(); // eslint-disable-line no-new
	    new $ArrayBuffer(1.5); // eslint-disable-line no-new
	    new $ArrayBuffer(NaN); // eslint-disable-line no-new
	    return $ArrayBuffer.name != ARRAY_BUFFER;
	  })) {
	    $ArrayBuffer = function ArrayBuffer(length) {
	      anInstance(this, $ArrayBuffer);
	      return new BaseBuffer(toIndex(length));
	    };
	    var ArrayBufferProto = $ArrayBuffer[PROTOTYPE] = BaseBuffer[PROTOTYPE];
	    for (var keys = gOPN(BaseBuffer), j = 0, key; keys.length > j;) {
	      if (!((key = keys[j++]) in $ArrayBuffer)) hide($ArrayBuffer, key, BaseBuffer[key]);
	    }
	    if (!LIBRARY) ArrayBufferProto.constructor = $ArrayBuffer;
	  }
	  // iOS Safari 7.x bug
	  var view = new $DataView(new $ArrayBuffer(2));
	  var $setInt8 = $DataView[PROTOTYPE].setInt8;
	  view.setInt8(0, 2147483648);
	  view.setInt8(1, 2147483649);
	  if (view.getInt8(0) || !view.getInt8(1)) redefineAll($DataView[PROTOTYPE], {
	    setInt8: function setInt8(byteOffset, value) {
	      $setInt8.call(this, byteOffset, value << 24 >> 24);
	    },
	    setUint8: function setUint8(byteOffset, value) {
	      $setInt8.call(this, byteOffset, value << 24 >> 24);
	    }
	  }, true);
	}
	setToStringTag($ArrayBuffer, ARRAY_BUFFER);
	setToStringTag($DataView, DATA_VIEW);
	hide($DataView[PROTOTYPE], $typed.VIEW, true);
	exports[ARRAY_BUFFER] = $ArrayBuffer;
	exports[DATA_VIEW] = $DataView;


/***/ }),
/* 226 */
/***/ (function(module, exports, __webpack_require__) {

	// https://tc39.github.io/ecma262/#sec-toindex
	var toInteger = __webpack_require__(38);
	var toLength = __webpack_require__(37);
	module.exports = function (it) {
	  if (it === undefined) return 0;
	  var number = toInteger(it);
	  var length = toLength(number);
	  if (number !== length) throw RangeError('Wrong length!');
	  return length;
	};


/***/ }),
/* 227 */
/***/ (function(module, exports, __webpack_require__) {

	var $export = __webpack_require__(8);
	$export($export.G + $export.W + $export.F * !__webpack_require__(224).ABV, {
	  DataView: __webpack_require__(225).DataView
	});


/***/ }),
/* 228 */
/***/ (function(module, exports, __webpack_require__) {

	__webpack_require__(229)('Int8', 1, function (init) {
	  return function Int8Array(data, byteOffset, length) {
	    return init(this, data, byteOffset, length);
	  };
	});


/***/ }),
/* 229 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	if (__webpack_require__(6)) {
	  var LIBRARY = __webpack_require__(28);
	  var global = __webpack_require__(4);
	  var fails = __webpack_require__(7);
	  var $export = __webpack_require__(8);
	  var $typed = __webpack_require__(224);
	  var $buffer = __webpack_require__(225);
	  var ctx = __webpack_require__(20);
	  var anInstance = __webpack_require__(206);
	  var propertyDesc = __webpack_require__(17);
	  var hide = __webpack_require__(10);
	  var redefineAll = __webpack_require__(214);
	  var toInteger = __webpack_require__(38);
	  var toLength = __webpack_require__(37);
	  var toIndex = __webpack_require__(226);
	  var toAbsoluteIndex = __webpack_require__(39);
	  var toPrimitive = __webpack_require__(16);
	  var has = __webpack_require__(5);
	  var classof = __webpack_require__(74);
	  var isObject = __webpack_require__(13);
	  var toObject = __webpack_require__(57);
	  var isArrayIter = __webpack_require__(163);
	  var create = __webpack_require__(45);
	  var getPrototypeOf = __webpack_require__(58);
	  var gOPN = __webpack_require__(49).f;
	  var getIterFn = __webpack_require__(165);
	  var uid = __webpack_require__(19);
	  var wks = __webpack_require__(25);
	  var createArrayMethod = __webpack_require__(173);
	  var createArrayIncludes = __webpack_require__(36);
	  var speciesConstructor = __webpack_require__(208);
	  var ArrayIterators = __webpack_require__(194);
	  var Iterators = __webpack_require__(129);
	  var $iterDetect = __webpack_require__(166);
	  var setSpecies = __webpack_require__(193);
	  var arrayFill = __webpack_require__(189);
	  var arrayCopyWithin = __webpack_require__(186);
	  var $DP = __webpack_require__(11);
	  var $GOPD = __webpack_require__(50);
	  var dP = $DP.f;
	  var gOPD = $GOPD.f;
	  var RangeError = global.RangeError;
	  var TypeError = global.TypeError;
	  var Uint8Array = global.Uint8Array;
	  var ARRAY_BUFFER = 'ArrayBuffer';
	  var SHARED_BUFFER = 'Shared' + ARRAY_BUFFER;
	  var BYTES_PER_ELEMENT = 'BYTES_PER_ELEMENT';
	  var PROTOTYPE = 'prototype';
	  var ArrayProto = Array[PROTOTYPE];
	  var $ArrayBuffer = $buffer.ArrayBuffer;
	  var $DataView = $buffer.DataView;
	  var arrayForEach = createArrayMethod(0);
	  var arrayFilter = createArrayMethod(2);
	  var arraySome = createArrayMethod(3);
	  var arrayEvery = createArrayMethod(4);
	  var arrayFind = createArrayMethod(5);
	  var arrayFindIndex = createArrayMethod(6);
	  var arrayIncludes = createArrayIncludes(true);
	  var arrayIndexOf = createArrayIncludes(false);
	  var arrayValues = ArrayIterators.values;
	  var arrayKeys = ArrayIterators.keys;
	  var arrayEntries = ArrayIterators.entries;
	  var arrayLastIndexOf = ArrayProto.lastIndexOf;
	  var arrayReduce = ArrayProto.reduce;
	  var arrayReduceRight = ArrayProto.reduceRight;
	  var arrayJoin = ArrayProto.join;
	  var arraySort = ArrayProto.sort;
	  var arraySlice = ArrayProto.slice;
	  var arrayToString = ArrayProto.toString;
	  var arrayToLocaleString = ArrayProto.toLocaleString;
	  var ITERATOR = wks('iterator');
	  var TAG = wks('toStringTag');
	  var TYPED_CONSTRUCTOR = uid('typed_constructor');
	  var DEF_CONSTRUCTOR = uid('def_constructor');
	  var ALL_CONSTRUCTORS = $typed.CONSTR;
	  var TYPED_ARRAY = $typed.TYPED;
	  var VIEW = $typed.VIEW;
	  var WRONG_LENGTH = 'Wrong length!';

	  var $map = createArrayMethod(1, function (O, length) {
	    return allocate(speciesConstructor(O, O[DEF_CONSTRUCTOR]), length);
	  });

	  var LITTLE_ENDIAN = fails(function () {
	    // eslint-disable-next-line no-undef
	    return new Uint8Array(new Uint16Array([1]).buffer)[0] === 1;
	  });

	  var FORCED_SET = !!Uint8Array && !!Uint8Array[PROTOTYPE].set && fails(function () {
	    new Uint8Array(1).set({});
	  });

	  var toOffset = function (it, BYTES) {
	    var offset = toInteger(it);
	    if (offset < 0 || offset % BYTES) throw RangeError('Wrong offset!');
	    return offset;
	  };

	  var validate = function (it) {
	    if (isObject(it) && TYPED_ARRAY in it) return it;
	    throw TypeError(it + ' is not a typed array!');
	  };

	  var allocate = function (C, length) {
	    if (!(isObject(C) && TYPED_CONSTRUCTOR in C)) {
	      throw TypeError('It is not a typed array constructor!');
	    } return new C(length);
	  };

	  var speciesFromList = function (O, list) {
	    return fromList(speciesConstructor(O, O[DEF_CONSTRUCTOR]), list);
	  };

	  var fromList = function (C, list) {
	    var index = 0;
	    var length = list.length;
	    var result = allocate(C, length);
	    while (length > index) result[index] = list[index++];
	    return result;
	  };

	  var addGetter = function (it, key, internal) {
	    dP(it, key, { get: function () { return this._d[internal]; } });
	  };

	  var $from = function from(source /* , mapfn, thisArg */) {
	    var O = toObject(source);
	    var aLen = arguments.length;
	    var mapfn = aLen > 1 ? arguments[1] : undefined;
	    var mapping = mapfn !== undefined;
	    var iterFn = getIterFn(O);
	    var i, length, values, result, step, iterator;
	    if (iterFn != undefined && !isArrayIter(iterFn)) {
	      for (iterator = iterFn.call(O), values = [], i = 0; !(step = iterator.next()).done; i++) {
	        values.push(step.value);
	      } O = values;
	    }
	    if (mapping && aLen > 2) mapfn = ctx(mapfn, arguments[2], 2);
	    for (i = 0, length = toLength(O.length), result = allocate(this, length); length > i; i++) {
	      result[i] = mapping ? mapfn(O[i], i) : O[i];
	    }
	    return result;
	  };

	  var $of = function of(/* ...items */) {
	    var index = 0;
	    var length = arguments.length;
	    var result = allocate(this, length);
	    while (length > index) result[index] = arguments[index++];
	    return result;
	  };

	  // iOS Safari 6.x fails here
	  var TO_LOCALE_BUG = !!Uint8Array && fails(function () { arrayToLocaleString.call(new Uint8Array(1)); });

	  var $toLocaleString = function toLocaleString() {
	    return arrayToLocaleString.apply(TO_LOCALE_BUG ? arraySlice.call(validate(this)) : validate(this), arguments);
	  };

	  var proto = {
	    copyWithin: function copyWithin(target, start /* , end */) {
	      return arrayCopyWithin.call(validate(this), target, start, arguments.length > 2 ? arguments[2] : undefined);
	    },
	    every: function every(callbackfn /* , thisArg */) {
	      return arrayEvery(validate(this), callbackfn, arguments.length > 1 ? arguments[1] : undefined);
	    },
	    fill: function fill(value /* , start, end */) { // eslint-disable-line no-unused-vars
	      return arrayFill.apply(validate(this), arguments);
	    },
	    filter: function filter(callbackfn /* , thisArg */) {
	      return speciesFromList(this, arrayFilter(validate(this), callbackfn,
	        arguments.length > 1 ? arguments[1] : undefined));
	    },
	    find: function find(predicate /* , thisArg */) {
	      return arrayFind(validate(this), predicate, arguments.length > 1 ? arguments[1] : undefined);
	    },
	    findIndex: function findIndex(predicate /* , thisArg */) {
	      return arrayFindIndex(validate(this), predicate, arguments.length > 1 ? arguments[1] : undefined);
	    },
	    forEach: function forEach(callbackfn /* , thisArg */) {
	      arrayForEach(validate(this), callbackfn, arguments.length > 1 ? arguments[1] : undefined);
	    },
	    indexOf: function indexOf(searchElement /* , fromIndex */) {
	      return arrayIndexOf(validate(this), searchElement, arguments.length > 1 ? arguments[1] : undefined);
	    },
	    includes: function includes(searchElement /* , fromIndex */) {
	      return arrayIncludes(validate(this), searchElement, arguments.length > 1 ? arguments[1] : undefined);
	    },
	    join: function join(separator) { // eslint-disable-line no-unused-vars
	      return arrayJoin.apply(validate(this), arguments);
	    },
	    lastIndexOf: function lastIndexOf(searchElement /* , fromIndex */) { // eslint-disable-line no-unused-vars
	      return arrayLastIndexOf.apply(validate(this), arguments);
	    },
	    map: function map(mapfn /* , thisArg */) {
	      return $map(validate(this), mapfn, arguments.length > 1 ? arguments[1] : undefined);
	    },
	    reduce: function reduce(callbackfn /* , initialValue */) { // eslint-disable-line no-unused-vars
	      return arrayReduce.apply(validate(this), arguments);
	    },
	    reduceRight: function reduceRight(callbackfn /* , initialValue */) { // eslint-disable-line no-unused-vars
	      return arrayReduceRight.apply(validate(this), arguments);
	    },
	    reverse: function reverse() {
	      var that = this;
	      var length = validate(that).length;
	      var middle = Math.floor(length / 2);
	      var index = 0;
	      var value;
	      while (index < middle) {
	        value = that[index];
	        that[index++] = that[--length];
	        that[length] = value;
	      } return that;
	    },
	    some: function some(callbackfn /* , thisArg */) {
	      return arraySome(validate(this), callbackfn, arguments.length > 1 ? arguments[1] : undefined);
	    },
	    sort: function sort(comparefn) {
	      return arraySort.call(validate(this), comparefn);
	    },
	    subarray: function subarray(begin, end) {
	      var O = validate(this);
	      var length = O.length;
	      var $begin = toAbsoluteIndex(begin, length);
	      return new (speciesConstructor(O, O[DEF_CONSTRUCTOR]))(
	        O.buffer,
	        O.byteOffset + $begin * O.BYTES_PER_ELEMENT,
	        toLength((end === undefined ? length : toAbsoluteIndex(end, length)) - $begin)
	      );
	    }
	  };

	  var $slice = function slice(start, end) {
	    return speciesFromList(this, arraySlice.call(validate(this), start, end));
	  };

	  var $set = function set(arrayLike /* , offset */) {
	    validate(this);
	    var offset = toOffset(arguments[1], 1);
	    var length = this.length;
	    var src = toObject(arrayLike);
	    var len = toLength(src.length);
	    var index = 0;
	    if (len + offset > length) throw RangeError(WRONG_LENGTH);
	    while (index < len) this[offset + index] = src[index++];
	  };

	  var $iterators = {
	    entries: function entries() {
	      return arrayEntries.call(validate(this));
	    },
	    keys: function keys() {
	      return arrayKeys.call(validate(this));
	    },
	    values: function values() {
	      return arrayValues.call(validate(this));
	    }
	  };

	  var isTAIndex = function (target, key) {
	    return isObject(target)
	      && target[TYPED_ARRAY]
	      && typeof key != 'symbol'
	      && key in target
	      && String(+key) == String(key);
	  };
	  var $getDesc = function getOwnPropertyDescriptor(target, key) {
	    return isTAIndex(target, key = toPrimitive(key, true))
	      ? propertyDesc(2, target[key])
	      : gOPD(target, key);
	  };
	  var $setDesc = function defineProperty(target, key, desc) {
	    if (isTAIndex(target, key = toPrimitive(key, true))
	      && isObject(desc)
	      && has(desc, 'value')
	      && !has(desc, 'get')
	      && !has(desc, 'set')
	      // TODO: add validation descriptor w/o calling accessors
	      && !desc.configurable
	      && (!has(desc, 'writable') || desc.writable)
	      && (!has(desc, 'enumerable') || desc.enumerable)
	    ) {
	      target[key] = desc.value;
	      return target;
	    } return dP(target, key, desc);
	  };

	  if (!ALL_CONSTRUCTORS) {
	    $GOPD.f = $getDesc;
	    $DP.f = $setDesc;
	  }

	  $export($export.S + $export.F * !ALL_CONSTRUCTORS, 'Object', {
	    getOwnPropertyDescriptor: $getDesc,
	    defineProperty: $setDesc
	  });

	  if (fails(function () { arrayToString.call({}); })) {
	    arrayToString = arrayToLocaleString = function toString() {
	      return arrayJoin.call(this);
	    };
	  }

	  var $TypedArrayPrototype$ = redefineAll({}, proto);
	  redefineAll($TypedArrayPrototype$, $iterators);
	  hide($TypedArrayPrototype$, ITERATOR, $iterators.values);
	  redefineAll($TypedArrayPrototype$, {
	    slice: $slice,
	    set: $set,
	    constructor: function () { /* noop */ },
	    toString: arrayToString,
	    toLocaleString: $toLocaleString
	  });
	  addGetter($TypedArrayPrototype$, 'buffer', 'b');
	  addGetter($TypedArrayPrototype$, 'byteOffset', 'o');
	  addGetter($TypedArrayPrototype$, 'byteLength', 'l');
	  addGetter($TypedArrayPrototype$, 'length', 'e');
	  dP($TypedArrayPrototype$, TAG, {
	    get: function () { return this[TYPED_ARRAY]; }
	  });

	  // eslint-disable-next-line max-statements
	  module.exports = function (KEY, BYTES, wrapper, CLAMPED) {
	    CLAMPED = !!CLAMPED;
	    var NAME = KEY + (CLAMPED ? 'Clamped' : '') + 'Array';
	    var GETTER = 'get' + KEY;
	    var SETTER = 'set' + KEY;
	    var TypedArray = global[NAME];
	    var Base = TypedArray || {};
	    var TAC = TypedArray && getPrototypeOf(TypedArray);
	    var FORCED = !TypedArray || !$typed.ABV;
	    var O = {};
	    var TypedArrayPrototype = TypedArray && TypedArray[PROTOTYPE];
	    var getter = function (that, index) {
	      var data = that._d;
	      return data.v[GETTER](index * BYTES + data.o, LITTLE_ENDIAN);
	    };
	    var setter = function (that, index, value) {
	      var data = that._d;
	      if (CLAMPED) value = (value = Math.round(value)) < 0 ? 0 : value > 0xff ? 0xff : value & 0xff;
	      data.v[SETTER](index * BYTES + data.o, value, LITTLE_ENDIAN);
	    };
	    var addElement = function (that, index) {
	      dP(that, index, {
	        get: function () {
	          return getter(this, index);
	        },
	        set: function (value) {
	          return setter(this, index, value);
	        },
	        enumerable: true
	      });
	    };
	    if (FORCED) {
	      TypedArray = wrapper(function (that, data, $offset, $length) {
	        anInstance(that, TypedArray, NAME, '_d');
	        var index = 0;
	        var offset = 0;
	        var buffer, byteLength, length, klass;
	        if (!isObject(data)) {
	          length = toIndex(data);
	          byteLength = length * BYTES;
	          buffer = new $ArrayBuffer(byteLength);
	        } else if (data instanceof $ArrayBuffer || (klass = classof(data)) == ARRAY_BUFFER || klass == SHARED_BUFFER) {
	          buffer = data;
	          offset = toOffset($offset, BYTES);
	          var $len = data.byteLength;
	          if ($length === undefined) {
	            if ($len % BYTES) throw RangeError(WRONG_LENGTH);
	            byteLength = $len - offset;
	            if (byteLength < 0) throw RangeError(WRONG_LENGTH);
	          } else {
	            byteLength = toLength($length) * BYTES;
	            if (byteLength + offset > $len) throw RangeError(WRONG_LENGTH);
	          }
	          length = byteLength / BYTES;
	        } else if (TYPED_ARRAY in data) {
	          return fromList(TypedArray, data);
	        } else {
	          return $from.call(TypedArray, data);
	        }
	        hide(that, '_d', {
	          b: buffer,
	          o: offset,
	          l: byteLength,
	          e: length,
	          v: new $DataView(buffer)
	        });
	        while (index < length) addElement(that, index++);
	      });
	      TypedArrayPrototype = TypedArray[PROTOTYPE] = create($TypedArrayPrototype$);
	      hide(TypedArrayPrototype, 'constructor', TypedArray);
	    } else if (!fails(function () {
	      TypedArray(1);
	    }) || !fails(function () {
	      new TypedArray(-1); // eslint-disable-line no-new
	    }) || !$iterDetect(function (iter) {
	      new TypedArray(); // eslint-disable-line no-new
	      new TypedArray(null); // eslint-disable-line no-new
	      new TypedArray(1.5); // eslint-disable-line no-new
	      new TypedArray(iter); // eslint-disable-line no-new
	    }, true)) {
	      TypedArray = wrapper(function (that, data, $offset, $length) {
	        anInstance(that, TypedArray, NAME);
	        var klass;
	        // `ws` module bug, temporarily remove validation length for Uint8Array
	        // https://github.com/websockets/ws/pull/645
	        if (!isObject(data)) return new Base(toIndex(data));
	        if (data instanceof $ArrayBuffer || (klass = classof(data)) == ARRAY_BUFFER || klass == SHARED_BUFFER) {
	          return $length !== undefined
	            ? new Base(data, toOffset($offset, BYTES), $length)
	            : $offset !== undefined
	              ? new Base(data, toOffset($offset, BYTES))
	              : new Base(data);
	        }
	        if (TYPED_ARRAY in data) return fromList(TypedArray, data);
	        return $from.call(TypedArray, data);
	      });
	      arrayForEach(TAC !== Function.prototype ? gOPN(Base).concat(gOPN(TAC)) : gOPN(Base), function (key) {
	        if (!(key in TypedArray)) hide(TypedArray, key, Base[key]);
	      });
	      TypedArray[PROTOTYPE] = TypedArrayPrototype;
	      if (!LIBRARY) TypedArrayPrototype.constructor = TypedArray;
	    }
	    var $nativeIterator = TypedArrayPrototype[ITERATOR];
	    var CORRECT_ITER_NAME = !!$nativeIterator
	      && ($nativeIterator.name == 'values' || $nativeIterator.name == undefined);
	    var $iterator = $iterators.values;
	    hide(TypedArray, TYPED_CONSTRUCTOR, true);
	    hide(TypedArrayPrototype, TYPED_ARRAY, NAME);
	    hide(TypedArrayPrototype, VIEW, true);
	    hide(TypedArrayPrototype, DEF_CONSTRUCTOR, TypedArray);

	    if (CLAMPED ? new TypedArray(1)[TAG] != NAME : !(TAG in TypedArrayPrototype)) {
	      dP(TypedArrayPrototype, TAG, {
	        get: function () { return NAME; }
	      });
	    }

	    O[NAME] = TypedArray;

	    $export($export.G + $export.W + $export.F * (TypedArray != Base), O);

	    $export($export.S, NAME, {
	      BYTES_PER_ELEMENT: BYTES
	    });

	    $export($export.S + $export.F * fails(function () { Base.of.call(TypedArray, 1); }), NAME, {
	      from: $from,
	      of: $of
	    });

	    if (!(BYTES_PER_ELEMENT in TypedArrayPrototype)) hide(TypedArrayPrototype, BYTES_PER_ELEMENT, BYTES);

	    $export($export.P, NAME, proto);

	    setSpecies(NAME);

	    $export($export.P + $export.F * FORCED_SET, NAME, { set: $set });

	    $export($export.P + $export.F * !CORRECT_ITER_NAME, NAME, $iterators);

	    if (!LIBRARY && TypedArrayPrototype.toString != arrayToString) TypedArrayPrototype.toString = arrayToString;

	    $export($export.P + $export.F * fails(function () {
	      new TypedArray(1).slice();
	    }), NAME, { slice: $slice });

	    $export($export.P + $export.F * (fails(function () {
	      return [1, 2].toLocaleString() != new TypedArray([1, 2]).toLocaleString();
	    }) || !fails(function () {
	      TypedArrayPrototype.toLocaleString.call([1, 2]);
	    })), NAME, { toLocaleString: $toLocaleString });

	    Iterators[NAME] = CORRECT_ITER_NAME ? $nativeIterator : $iterator;
	    if (!LIBRARY && !CORRECT_ITER_NAME) hide(TypedArrayPrototype, ITERATOR, $iterator);
	  };
	} else module.exports = function () { /* empty */ };


/***/ }),
/* 230 */
/***/ (function(module, exports, __webpack_require__) {

	__webpack_require__(229)('Uint8', 1, function (init) {
	  return function Uint8Array(data, byteOffset, length) {
	    return init(this, data, byteOffset, length);
	  };
	});


/***/ }),
/* 231 */
/***/ (function(module, exports, __webpack_require__) {

	__webpack_require__(229)('Uint8', 1, function (init) {
	  return function Uint8ClampedArray(data, byteOffset, length) {
	    return init(this, data, byteOffset, length);
	  };
	}, true);


/***/ }),
/* 232 */
/***/ (function(module, exports, __webpack_require__) {

	__webpack_require__(229)('Int16', 2, function (init) {
	  return function Int16Array(data, byteOffset, length) {
	    return init(this, data, byteOffset, length);
	  };
	});


/***/ }),
/* 233 */
/***/ (function(module, exports, __webpack_require__) {

	__webpack_require__(229)('Uint16', 2, function (init) {
	  return function Uint16Array(data, byteOffset, length) {
	    return init(this, data, byteOffset, length);
	  };
	});


/***/ }),
/* 234 */
/***/ (function(module, exports, __webpack_require__) {

	__webpack_require__(229)('Int32', 4, function (init) {
	  return function Int32Array(data, byteOffset, length) {
	    return init(this, data, byteOffset, length);
	  };
	});


/***/ }),
/* 235 */
/***/ (function(module, exports, __webpack_require__) {

	__webpack_require__(229)('Uint32', 4, function (init) {
	  return function Uint32Array(data, byteOffset, length) {
	    return init(this, data, byteOffset, length);
	  };
	});


/***/ }),
/* 236 */
/***/ (function(module, exports, __webpack_require__) {

	__webpack_require__(229)('Float32', 4, function (init) {
	  return function Float32Array(data, byteOffset, length) {
	    return init(this, data, byteOffset, length);
	  };
	});


/***/ }),
/* 237 */
/***/ (function(module, exports, __webpack_require__) {

	__webpack_require__(229)('Float64', 8, function (init) {
	  return function Float64Array(data, byteOffset, length) {
	    return init(this, data, byteOffset, length);
	  };
	});


/***/ }),
/* 238 */
/***/ (function(module, exports, __webpack_require__) {

	// 26.1.1 Reflect.apply(target, thisArgument, argumentsList)
	var $export = __webpack_require__(8);
	var aFunction = __webpack_require__(21);
	var anObject = __webpack_require__(12);
	var rApply = (__webpack_require__(4).Reflect || {}).apply;
	var fApply = Function.apply;
	// MS Edge argumentsList argument is optional
	$export($export.S + $export.F * !__webpack_require__(7)(function () {
	  rApply(function () { /* empty */ });
	}), 'Reflect', {
	  apply: function apply(target, thisArgument, argumentsList) {
	    var T = aFunction(target);
	    var L = anObject(argumentsList);
	    return rApply ? rApply(T, thisArgument, L) : fApply.call(T, thisArgument, L);
	  }
	});


/***/ }),
/* 239 */
/***/ (function(module, exports, __webpack_require__) {

	// 26.1.2 Reflect.construct(target, argumentsList [, newTarget])
	var $export = __webpack_require__(8);
	var create = __webpack_require__(45);
	var aFunction = __webpack_require__(21);
	var anObject = __webpack_require__(12);
	var isObject = __webpack_require__(13);
	var fails = __webpack_require__(7);
	var bind = __webpack_require__(76);
	var rConstruct = (__webpack_require__(4).Reflect || {}).construct;

	// MS Edge supports only 2 arguments and argumentsList argument is optional
	// FF Nightly sets third argument as `new.target`, but does not create `this` from it
	var NEW_TARGET_BUG = fails(function () {
	  function F() { /* empty */ }
	  return !(rConstruct(function () { /* empty */ }, [], F) instanceof F);
	});
	var ARGS_BUG = !fails(function () {
	  rConstruct(function () { /* empty */ });
	});

	$export($export.S + $export.F * (NEW_TARGET_BUG || ARGS_BUG), 'Reflect', {
	  construct: function construct(Target, args /* , newTarget */) {
	    aFunction(Target);
	    anObject(args);
	    var newTarget = arguments.length < 3 ? Target : aFunction(arguments[2]);
	    if (ARGS_BUG && !NEW_TARGET_BUG) return rConstruct(Target, args, newTarget);
	    if (Target == newTarget) {
	      // w/o altered newTarget, optimization for 0-4 arguments
	      switch (args.length) {
	        case 0: return new Target();
	        case 1: return new Target(args[0]);
	        case 2: return new Target(args[0], args[1]);
	        case 3: return new Target(args[0], args[1], args[2]);
	        case 4: return new Target(args[0], args[1], args[2], args[3]);
	      }
	      // w/o altered newTarget, lot of arguments case
	      var $args = [null];
	      $args.push.apply($args, args);
	      return new (bind.apply(Target, $args))();
	    }
	    // with altered newTarget, not support built-in constructors
	    var proto = newTarget.prototype;
	    var instance = create(isObject(proto) ? proto : Object.prototype);
	    var result = Function.apply.call(Target, instance, args);
	    return isObject(result) ? result : instance;
	  }
	});


/***/ }),
/* 240 */
/***/ (function(module, exports, __webpack_require__) {

	// 26.1.3 Reflect.defineProperty(target, propertyKey, attributes)
	var dP = __webpack_require__(11);
	var $export = __webpack_require__(8);
	var anObject = __webpack_require__(12);
	var toPrimitive = __webpack_require__(16);

	// MS Edge has broken Reflect.defineProperty - throwing instead of returning false
	$export($export.S + $export.F * __webpack_require__(7)(function () {
	  // eslint-disable-next-line no-undef
	  Reflect.defineProperty(dP.f({}, 1, { value: 1 }), 1, { value: 2 });
	}), 'Reflect', {
	  defineProperty: function defineProperty(target, propertyKey, attributes) {
	    anObject(target);
	    propertyKey = toPrimitive(propertyKey, true);
	    anObject(attributes);
	    try {
	      dP.f(target, propertyKey, attributes);
	      return true;
	    } catch (e) {
	      return false;
	    }
	  }
	});


/***/ }),
/* 241 */
/***/ (function(module, exports, __webpack_require__) {

	// 26.1.4 Reflect.deleteProperty(target, propertyKey)
	var $export = __webpack_require__(8);
	var gOPD = __webpack_require__(50).f;
	var anObject = __webpack_require__(12);

	$export($export.S, 'Reflect', {
	  deleteProperty: function deleteProperty(target, propertyKey) {
	    var desc = gOPD(anObject(target), propertyKey);
	    return desc && !desc.configurable ? false : delete target[propertyKey];
	  }
	});


/***/ }),
/* 242 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// 26.1.5 Reflect.enumerate(target)
	var $export = __webpack_require__(8);
	var anObject = __webpack_require__(12);
	var Enumerate = function (iterated) {
	  this._t = anObject(iterated); // target
	  this._i = 0;                  // next index
	  var keys = this._k = [];      // keys
	  var key;
	  for (key in iterated) keys.push(key);
	};
	__webpack_require__(130)(Enumerate, 'Object', function () {
	  var that = this;
	  var keys = that._k;
	  var key;
	  do {
	    if (that._i >= keys.length) return { value: undefined, done: true };
	  } while (!((key = keys[that._i++]) in that._t));
	  return { value: key, done: false };
	});

	$export($export.S, 'Reflect', {
	  enumerate: function enumerate(target) {
	    return new Enumerate(target);
	  }
	});


/***/ }),
/* 243 */
/***/ (function(module, exports, __webpack_require__) {

	// 26.1.6 Reflect.get(target, propertyKey [, receiver])
	var gOPD = __webpack_require__(50);
	var getPrototypeOf = __webpack_require__(58);
	var has = __webpack_require__(5);
	var $export = __webpack_require__(8);
	var isObject = __webpack_require__(13);
	var anObject = __webpack_require__(12);

	function get(target, propertyKey /* , receiver */) {
	  var receiver = arguments.length < 3 ? target : arguments[2];
	  var desc, proto;
	  if (anObject(target) === receiver) return target[propertyKey];
	  if (desc = gOPD.f(target, propertyKey)) return has(desc, 'value')
	    ? desc.value
	    : desc.get !== undefined
	      ? desc.get.call(receiver)
	      : undefined;
	  if (isObject(proto = getPrototypeOf(target))) return get(proto, propertyKey, receiver);
	}

	$export($export.S, 'Reflect', { get: get });


/***/ }),
/* 244 */
/***/ (function(module, exports, __webpack_require__) {

	// 26.1.7 Reflect.getOwnPropertyDescriptor(target, propertyKey)
	var gOPD = __webpack_require__(50);
	var $export = __webpack_require__(8);
	var anObject = __webpack_require__(12);

	$export($export.S, 'Reflect', {
	  getOwnPropertyDescriptor: function getOwnPropertyDescriptor(target, propertyKey) {
	    return gOPD.f(anObject(target), propertyKey);
	  }
	});


/***/ }),
/* 245 */
/***/ (function(module, exports, __webpack_require__) {

	// 26.1.8 Reflect.getPrototypeOf(target)
	var $export = __webpack_require__(8);
	var getProto = __webpack_require__(58);
	var anObject = __webpack_require__(12);

	$export($export.S, 'Reflect', {
	  getPrototypeOf: function getPrototypeOf(target) {
	    return getProto(anObject(target));
	  }
	});


/***/ }),
/* 246 */
/***/ (function(module, exports, __webpack_require__) {

	// 26.1.9 Reflect.has(target, propertyKey)
	var $export = __webpack_require__(8);

	$export($export.S, 'Reflect', {
	  has: function has(target, propertyKey) {
	    return propertyKey in target;
	  }
	});


/***/ }),
/* 247 */
/***/ (function(module, exports, __webpack_require__) {

	// 26.1.10 Reflect.isExtensible(target)
	var $export = __webpack_require__(8);
	var anObject = __webpack_require__(12);
	var $isExtensible = Object.isExtensible;

	$export($export.S, 'Reflect', {
	  isExtensible: function isExtensible(target) {
	    anObject(target);
	    return $isExtensible ? $isExtensible(target) : true;
	  }
	});


/***/ }),
/* 248 */
/***/ (function(module, exports, __webpack_require__) {

	// 26.1.11 Reflect.ownKeys(target)
	var $export = __webpack_require__(8);

	$export($export.S, 'Reflect', { ownKeys: __webpack_require__(249) });


/***/ }),
/* 249 */
/***/ (function(module, exports, __webpack_require__) {

	// all object keys, includes non-enumerable and symbols
	var gOPN = __webpack_require__(49);
	var gOPS = __webpack_require__(42);
	var anObject = __webpack_require__(12);
	var Reflect = __webpack_require__(4).Reflect;
	module.exports = Reflect && Reflect.ownKeys || function ownKeys(it) {
	  var keys = gOPN.f(anObject(it));
	  var getSymbols = gOPS.f;
	  return getSymbols ? keys.concat(getSymbols(it)) : keys;
	};


/***/ }),
/* 250 */
/***/ (function(module, exports, __webpack_require__) {

	// 26.1.12 Reflect.preventExtensions(target)
	var $export = __webpack_require__(8);
	var anObject = __webpack_require__(12);
	var $preventExtensions = Object.preventExtensions;

	$export($export.S, 'Reflect', {
	  preventExtensions: function preventExtensions(target) {
	    anObject(target);
	    try {
	      if ($preventExtensions) $preventExtensions(target);
	      return true;
	    } catch (e) {
	      return false;
	    }
	  }
	});


/***/ }),
/* 251 */
/***/ (function(module, exports, __webpack_require__) {

	// 26.1.13 Reflect.set(target, propertyKey, V [, receiver])
	var dP = __webpack_require__(11);
	var gOPD = __webpack_require__(50);
	var getPrototypeOf = __webpack_require__(58);
	var has = __webpack_require__(5);
	var $export = __webpack_require__(8);
	var createDesc = __webpack_require__(17);
	var anObject = __webpack_require__(12);
	var isObject = __webpack_require__(13);

	function set(target, propertyKey, V /* , receiver */) {
	  var receiver = arguments.length < 4 ? target : arguments[3];
	  var ownDesc = gOPD.f(anObject(target), propertyKey);
	  var existingDescriptor, proto;
	  if (!ownDesc) {
	    if (isObject(proto = getPrototypeOf(target))) {
	      return set(proto, propertyKey, V, receiver);
	    }
	    ownDesc = createDesc(0);
	  }
	  if (has(ownDesc, 'value')) {
	    if (ownDesc.writable === false || !isObject(receiver)) return false;
	    existingDescriptor = gOPD.f(receiver, propertyKey) || createDesc(0);
	    existingDescriptor.value = V;
	    dP.f(receiver, propertyKey, existingDescriptor);
	    return true;
	  }
	  return ownDesc.set === undefined ? false : (ownDesc.set.call(receiver, V), true);
	}

	$export($export.S, 'Reflect', { set: set });


/***/ }),
/* 252 */
/***/ (function(module, exports, __webpack_require__) {

	// 26.1.14 Reflect.setPrototypeOf(target, proto)
	var $export = __webpack_require__(8);
	var setProto = __webpack_require__(72);

	if (setProto) $export($export.S, 'Reflect', {
	  setPrototypeOf: function setPrototypeOf(target, proto) {
	    setProto.check(target, proto);
	    try {
	      setProto.set(target, proto);
	      return true;
	    } catch (e) {
	      return false;
	    }
	  }
	});


/***/ }),
/* 253 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// https://github.com/tc39/Array.prototype.includes
	var $export = __webpack_require__(8);
	var $includes = __webpack_require__(36)(true);

	$export($export.P, 'Array', {
	  includes: function includes(el /* , fromIndex = 0 */) {
	    return $includes(this, el, arguments.length > 1 ? arguments[1] : undefined);
	  }
	});

	__webpack_require__(187)('includes');


/***/ }),
/* 254 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// https://tc39.github.io/proposal-flatMap/#sec-Array.prototype.flatMap
	var $export = __webpack_require__(8);
	var flattenIntoArray = __webpack_require__(255);
	var toObject = __webpack_require__(57);
	var toLength = __webpack_require__(37);
	var aFunction = __webpack_require__(21);
	var arraySpeciesCreate = __webpack_require__(174);

	$export($export.P, 'Array', {
	  flatMap: function flatMap(callbackfn /* , thisArg */) {
	    var O = toObject(this);
	    var sourceLen, A;
	    aFunction(callbackfn);
	    sourceLen = toLength(O.length);
	    A = arraySpeciesCreate(O, 0);
	    flattenIntoArray(A, O, O, sourceLen, 0, 1, callbackfn, arguments[1]);
	    return A;
	  }
	});

	__webpack_require__(187)('flatMap');


/***/ }),
/* 255 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// https://tc39.github.io/proposal-flatMap/#sec-FlattenIntoArray
	var isArray = __webpack_require__(44);
	var isObject = __webpack_require__(13);
	var toLength = __webpack_require__(37);
	var ctx = __webpack_require__(20);
	var IS_CONCAT_SPREADABLE = __webpack_require__(25)('isConcatSpreadable');

	function flattenIntoArray(target, original, source, sourceLen, start, depth, mapper, thisArg) {
	  var targetIndex = start;
	  var sourceIndex = 0;
	  var mapFn = mapper ? ctx(mapper, thisArg, 3) : false;
	  var element, spreadable;

	  while (sourceIndex < sourceLen) {
	    if (sourceIndex in source) {
	      element = mapFn ? mapFn(source[sourceIndex], sourceIndex, original) : source[sourceIndex];

	      spreadable = false;
	      if (isObject(element)) {
	        spreadable = element[IS_CONCAT_SPREADABLE];
	        spreadable = spreadable !== undefined ? !!spreadable : isArray(element);
	      }

	      if (spreadable && depth > 0) {
	        targetIndex = flattenIntoArray(target, original, element, toLength(element.length), targetIndex, depth - 1) - 1;
	      } else {
	        if (targetIndex >= 0x1fffffffffffff) throw TypeError();
	        target[targetIndex] = element;
	      }

	      targetIndex++;
	    }
	    sourceIndex++;
	  }
	  return targetIndex;
	}

	module.exports = flattenIntoArray;


/***/ }),
/* 256 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// https://tc39.github.io/proposal-flatMap/#sec-Array.prototype.flatten
	var $export = __webpack_require__(8);
	var flattenIntoArray = __webpack_require__(255);
	var toObject = __webpack_require__(57);
	var toLength = __webpack_require__(37);
	var toInteger = __webpack_require__(38);
	var arraySpeciesCreate = __webpack_require__(174);

	$export($export.P, 'Array', {
	  flatten: function flatten(/* depthArg = 1 */) {
	    var depthArg = arguments[0];
	    var O = toObject(this);
	    var sourceLen = toLength(O.length);
	    var A = arraySpeciesCreate(O, 0);
	    flattenIntoArray(A, O, O, sourceLen, 0, depthArg === undefined ? 1 : toInteger(depthArg));
	    return A;
	  }
	});

	__webpack_require__(187)('flatten');


/***/ }),
/* 257 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// https://github.com/mathiasbynens/String.prototype.at
	var $export = __webpack_require__(8);
	var $at = __webpack_require__(127)(true);

	$export($export.P, 'String', {
	  at: function at(pos) {
	    return $at(this, pos);
	  }
	});


/***/ }),
/* 258 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// https://github.com/tc39/proposal-string-pad-start-end
	var $export = __webpack_require__(8);
	var $pad = __webpack_require__(259);

	$export($export.P, 'String', {
	  padStart: function padStart(maxLength /* , fillString = ' ' */) {
	    return $pad(this, maxLength, arguments.length > 1 ? arguments[1] : undefined, true);
	  }
	});


/***/ }),
/* 259 */
/***/ (function(module, exports, __webpack_require__) {

	// https://github.com/tc39/proposal-string-pad-start-end
	var toLength = __webpack_require__(37);
	var repeat = __webpack_require__(90);
	var defined = __webpack_require__(35);

	module.exports = function (that, maxLength, fillString, left) {
	  var S = String(defined(that));
	  var stringLength = S.length;
	  var fillStr = fillString === undefined ? ' ' : String(fillString);
	  var intMaxLength = toLength(maxLength);
	  if (intMaxLength <= stringLength || fillStr == '') return S;
	  var fillLen = intMaxLength - stringLength;
	  var stringFiller = repeat.call(fillStr, Math.ceil(fillLen / fillStr.length));
	  if (stringFiller.length > fillLen) stringFiller = stringFiller.slice(0, fillLen);
	  return left ? stringFiller + S : S + stringFiller;
	};


/***/ }),
/* 260 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// https://github.com/tc39/proposal-string-pad-start-end
	var $export = __webpack_require__(8);
	var $pad = __webpack_require__(259);

	$export($export.P, 'String', {
	  padEnd: function padEnd(maxLength /* , fillString = ' ' */) {
	    return $pad(this, maxLength, arguments.length > 1 ? arguments[1] : undefined, false);
	  }
	});


/***/ }),
/* 261 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// https://github.com/sebmarkbage/ecmascript-string-left-right-trim
	__webpack_require__(82)('trimLeft', function ($trim) {
	  return function trimLeft() {
	    return $trim(this, 1);
	  };
	}, 'trimStart');


/***/ }),
/* 262 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// https://github.com/sebmarkbage/ecmascript-string-left-right-trim
	__webpack_require__(82)('trimRight', function ($trim) {
	  return function trimRight() {
	    return $trim(this, 2);
	  };
	}, 'trimEnd');


/***/ }),
/* 263 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// https://tc39.github.io/String.prototype.matchAll/
	var $export = __webpack_require__(8);
	var defined = __webpack_require__(35);
	var toLength = __webpack_require__(37);
	var isRegExp = __webpack_require__(134);
	var getFlags = __webpack_require__(197);
	var RegExpProto = RegExp.prototype;

	var $RegExpStringIterator = function (regexp, string) {
	  this._r = regexp;
	  this._s = string;
	};

	__webpack_require__(130)($RegExpStringIterator, 'RegExp String', function next() {
	  var match = this._r.exec(this._s);
	  return { value: match, done: match === null };
	});

	$export($export.P, 'String', {
	  matchAll: function matchAll(regexp) {
	    defined(this);
	    if (!isRegExp(regexp)) throw TypeError(regexp + ' is not a regexp!');
	    var S = String(this);
	    var flags = 'flags' in RegExpProto ? String(regexp.flags) : getFlags.call(regexp);
	    var rx = new RegExp(regexp.source, ~flags.indexOf('g') ? flags : 'g' + flags);
	    rx.lastIndex = toLength(regexp.lastIndex);
	    return new $RegExpStringIterator(rx, S);
	  }
	});


/***/ }),
/* 264 */
/***/ (function(module, exports, __webpack_require__) {

	__webpack_require__(27)('asyncIterator');


/***/ }),
/* 265 */
/***/ (function(module, exports, __webpack_require__) {

	__webpack_require__(27)('observable');


/***/ }),
/* 266 */
/***/ (function(module, exports, __webpack_require__) {

	// https://github.com/tc39/proposal-object-getownpropertydescriptors
	var $export = __webpack_require__(8);
	var ownKeys = __webpack_require__(249);
	var toIObject = __webpack_require__(32);
	var gOPD = __webpack_require__(50);
	var createProperty = __webpack_require__(164);

	$export($export.S, 'Object', {
	  getOwnPropertyDescriptors: function getOwnPropertyDescriptors(object) {
	    var O = toIObject(object);
	    var getDesc = gOPD.f;
	    var keys = ownKeys(O);
	    var result = {};
	    var i = 0;
	    var key, desc;
	    while (keys.length > i) {
	      desc = getDesc(O, key = keys[i++]);
	      if (desc !== undefined) createProperty(result, key, desc);
	    }
	    return result;
	  }
	});


/***/ }),
/* 267 */
/***/ (function(module, exports, __webpack_require__) {

	// https://github.com/tc39/proposal-object-values-entries
	var $export = __webpack_require__(8);
	var $values = __webpack_require__(268)(false);

	$export($export.S, 'Object', {
	  values: function values(it) {
	    return $values(it);
	  }
	});


/***/ }),
/* 268 */
/***/ (function(module, exports, __webpack_require__) {

	var getKeys = __webpack_require__(30);
	var toIObject = __webpack_require__(32);
	var isEnum = __webpack_require__(43).f;
	module.exports = function (isEntries) {
	  return function (it) {
	    var O = toIObject(it);
	    var keys = getKeys(O);
	    var length = keys.length;
	    var i = 0;
	    var result = [];
	    var key;
	    while (length > i) if (isEnum.call(O, key = keys[i++])) {
	      result.push(isEntries ? [key, O[key]] : O[key]);
	    } return result;
	  };
	};


/***/ }),
/* 269 */
/***/ (function(module, exports, __webpack_require__) {

	// https://github.com/tc39/proposal-object-values-entries
	var $export = __webpack_require__(8);
	var $entries = __webpack_require__(268)(true);

	$export($export.S, 'Object', {
	  entries: function entries(it) {
	    return $entries(it);
	  }
	});


/***/ }),
/* 270 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var toObject = __webpack_require__(57);
	var aFunction = __webpack_require__(21);
	var $defineProperty = __webpack_require__(11);

	// B.2.2.2 Object.prototype.__defineGetter__(P, getter)
	__webpack_require__(6) && $export($export.P + __webpack_require__(271), 'Object', {
	  __defineGetter__: function __defineGetter__(P, getter) {
	    $defineProperty.f(toObject(this), P, { get: aFunction(getter), enumerable: true, configurable: true });
	  }
	});


/***/ }),
/* 271 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// Forced replacement prototype accessors methods
	module.exports = __webpack_require__(28) || !__webpack_require__(7)(function () {
	  var K = Math.random();
	  // In FF throws only define methods
	  // eslint-disable-next-line no-undef, no-useless-call
	  __defineSetter__.call(null, K, function () { /* empty */ });
	  delete __webpack_require__(4)[K];
	});


/***/ }),
/* 272 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var toObject = __webpack_require__(57);
	var aFunction = __webpack_require__(21);
	var $defineProperty = __webpack_require__(11);

	// B.2.2.3 Object.prototype.__defineSetter__(P, setter)
	__webpack_require__(6) && $export($export.P + __webpack_require__(271), 'Object', {
	  __defineSetter__: function __defineSetter__(P, setter) {
	    $defineProperty.f(toObject(this), P, { set: aFunction(setter), enumerable: true, configurable: true });
	  }
	});


/***/ }),
/* 273 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var toObject = __webpack_require__(57);
	var toPrimitive = __webpack_require__(16);
	var getPrototypeOf = __webpack_require__(58);
	var getOwnPropertyDescriptor = __webpack_require__(50).f;

	// B.2.2.4 Object.prototype.__lookupGetter__(P)
	__webpack_require__(6) && $export($export.P + __webpack_require__(271), 'Object', {
	  __lookupGetter__: function __lookupGetter__(P) {
	    var O = toObject(this);
	    var K = toPrimitive(P, true);
	    var D;
	    do {
	      if (D = getOwnPropertyDescriptor(O, K)) return D.get;
	    } while (O = getPrototypeOf(O));
	  }
	});


/***/ }),
/* 274 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	var $export = __webpack_require__(8);
	var toObject = __webpack_require__(57);
	var toPrimitive = __webpack_require__(16);
	var getPrototypeOf = __webpack_require__(58);
	var getOwnPropertyDescriptor = __webpack_require__(50).f;

	// B.2.2.5 Object.prototype.__lookupSetter__(P)
	__webpack_require__(6) && $export($export.P + __webpack_require__(271), 'Object', {
	  __lookupSetter__: function __lookupSetter__(P) {
	    var O = toObject(this);
	    var K = toPrimitive(P, true);
	    var D;
	    do {
	      if (D = getOwnPropertyDescriptor(O, K)) return D.set;
	    } while (O = getPrototypeOf(O));
	  }
	});


/***/ }),
/* 275 */
/***/ (function(module, exports, __webpack_require__) {

	// https://github.com/DavidBruant/Map-Set.prototype.toJSON
	var $export = __webpack_require__(8);

	$export($export.P + $export.R, 'Map', { toJSON: __webpack_require__(276)('Map') });


/***/ }),
/* 276 */
/***/ (function(module, exports, __webpack_require__) {

	// https://github.com/DavidBruant/Map-Set.prototype.toJSON
	var classof = __webpack_require__(74);
	var from = __webpack_require__(277);
	module.exports = function (NAME) {
	  return function toJSON() {
	    if (classof(this) != NAME) throw TypeError(NAME + "#toJSON isn't generic");
	    return from(this);
	  };
	};


/***/ }),
/* 277 */
/***/ (function(module, exports, __webpack_require__) {

	var forOf = __webpack_require__(207);

	module.exports = function (iter, ITERATOR) {
	  var result = [];
	  forOf(iter, false, result.push, result, ITERATOR);
	  return result;
	};


/***/ }),
/* 278 */
/***/ (function(module, exports, __webpack_require__) {

	// https://github.com/DavidBruant/Map-Set.prototype.toJSON
	var $export = __webpack_require__(8);

	$export($export.P + $export.R, 'Set', { toJSON: __webpack_require__(276)('Set') });


/***/ }),
/* 279 */
/***/ (function(module, exports, __webpack_require__) {

	// https://tc39.github.io/proposal-setmap-offrom/#sec-map.of
	__webpack_require__(280)('Map');


/***/ }),
/* 280 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// https://tc39.github.io/proposal-setmap-offrom/
	var $export = __webpack_require__(8);

	module.exports = function (COLLECTION) {
	  $export($export.S, COLLECTION, { of: function of() {
	    var length = arguments.length;
	    var A = Array(length);
	    while (length--) A[length] = arguments[length];
	    return new this(A);
	  } });
	};


/***/ }),
/* 281 */
/***/ (function(module, exports, __webpack_require__) {

	// https://tc39.github.io/proposal-setmap-offrom/#sec-set.of
	__webpack_require__(280)('Set');


/***/ }),
/* 282 */
/***/ (function(module, exports, __webpack_require__) {

	// https://tc39.github.io/proposal-setmap-offrom/#sec-weakmap.of
	__webpack_require__(280)('WeakMap');


/***/ }),
/* 283 */
/***/ (function(module, exports, __webpack_require__) {

	// https://tc39.github.io/proposal-setmap-offrom/#sec-weakset.of
	__webpack_require__(280)('WeakSet');


/***/ }),
/* 284 */
/***/ (function(module, exports, __webpack_require__) {

	// https://tc39.github.io/proposal-setmap-offrom/#sec-map.from
	__webpack_require__(285)('Map');


/***/ }),
/* 285 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// https://tc39.github.io/proposal-setmap-offrom/
	var $export = __webpack_require__(8);
	var aFunction = __webpack_require__(21);
	var ctx = __webpack_require__(20);
	var forOf = __webpack_require__(207);

	module.exports = function (COLLECTION) {
	  $export($export.S, COLLECTION, { from: function from(source /* , mapFn, thisArg */) {
	    var mapFn = arguments[1];
	    var mapping, A, n, cb;
	    aFunction(this);
	    mapping = mapFn !== undefined;
	    if (mapping) aFunction(mapFn);
	    if (source == undefined) return new this();
	    A = [];
	    if (mapping) {
	      n = 0;
	      cb = ctx(mapFn, arguments[2], 2);
	      forOf(source, false, function (nextItem) {
	        A.push(cb(nextItem, n++));
	      });
	    } else {
	      forOf(source, false, A.push, A);
	    }
	    return new this(A);
	  } });
	};


/***/ }),
/* 286 */
/***/ (function(module, exports, __webpack_require__) {

	// https://tc39.github.io/proposal-setmap-offrom/#sec-set.from
	__webpack_require__(285)('Set');


/***/ }),
/* 287 */
/***/ (function(module, exports, __webpack_require__) {

	// https://tc39.github.io/proposal-setmap-offrom/#sec-weakmap.from
	__webpack_require__(285)('WeakMap');


/***/ }),
/* 288 */
/***/ (function(module, exports, __webpack_require__) {

	// https://tc39.github.io/proposal-setmap-offrom/#sec-weakset.from
	__webpack_require__(285)('WeakSet');


/***/ }),
/* 289 */
/***/ (function(module, exports, __webpack_require__) {

	// https://github.com/tc39/proposal-global
	var $export = __webpack_require__(8);

	$export($export.G, { global: __webpack_require__(4) });


/***/ }),
/* 290 */
/***/ (function(module, exports, __webpack_require__) {

	// https://github.com/tc39/proposal-global
	var $export = __webpack_require__(8);

	$export($export.S, 'System', { global: __webpack_require__(4) });


/***/ }),
/* 291 */
/***/ (function(module, exports, __webpack_require__) {

	// https://github.com/ljharb/proposal-is-error
	var $export = __webpack_require__(8);
	var cof = __webpack_require__(34);

	$export($export.S, 'Error', {
	  isError: function isError(it) {
	    return cof(it) === 'Error';
	  }
	});


/***/ }),
/* 292 */
/***/ (function(module, exports, __webpack_require__) {

	// https://rwaldron.github.io/proposal-math-extensions/
	var $export = __webpack_require__(8);

	$export($export.S, 'Math', {
	  clamp: function clamp(x, lower, upper) {
	    return Math.min(upper, Math.max(lower, x));
	  }
	});


/***/ }),
/* 293 */
/***/ (function(module, exports, __webpack_require__) {

	// https://rwaldron.github.io/proposal-math-extensions/
	var $export = __webpack_require__(8);

	$export($export.S, 'Math', { DEG_PER_RAD: Math.PI / 180 });


/***/ }),
/* 294 */
/***/ (function(module, exports, __webpack_require__) {

	// https://rwaldron.github.io/proposal-math-extensions/
	var $export = __webpack_require__(8);
	var RAD_PER_DEG = 180 / Math.PI;

	$export($export.S, 'Math', {
	  degrees: function degrees(radians) {
	    return radians * RAD_PER_DEG;
	  }
	});


/***/ }),
/* 295 */
/***/ (function(module, exports, __webpack_require__) {

	// https://rwaldron.github.io/proposal-math-extensions/
	var $export = __webpack_require__(8);
	var scale = __webpack_require__(296);
	var fround = __webpack_require__(113);

	$export($export.S, 'Math', {
	  fscale: function fscale(x, inLow, inHigh, outLow, outHigh) {
	    return fround(scale(x, inLow, inHigh, outLow, outHigh));
	  }
	});


/***/ }),
/* 296 */
/***/ (function(module, exports) {

	// https://rwaldron.github.io/proposal-math-extensions/
	module.exports = Math.scale || function scale(x, inLow, inHigh, outLow, outHigh) {
	  if (
	    arguments.length === 0
	      // eslint-disable-next-line no-self-compare
	      || x != x
	      // eslint-disable-next-line no-self-compare
	      || inLow != inLow
	      // eslint-disable-next-line no-self-compare
	      || inHigh != inHigh
	      // eslint-disable-next-line no-self-compare
	      || outLow != outLow
	      // eslint-disable-next-line no-self-compare
	      || outHigh != outHigh
	  ) return NaN;
	  if (x === Infinity || x === -Infinity) return x;
	  return (x - inLow) * (outHigh - outLow) / (inHigh - inLow) + outLow;
	};


/***/ }),
/* 297 */
/***/ (function(module, exports, __webpack_require__) {

	// https://gist.github.com/BrendanEich/4294d5c212a6d2254703
	var $export = __webpack_require__(8);

	$export($export.S, 'Math', {
	  iaddh: function iaddh(x0, x1, y0, y1) {
	    var $x0 = x0 >>> 0;
	    var $x1 = x1 >>> 0;
	    var $y0 = y0 >>> 0;
	    return $x1 + (y1 >>> 0) + (($x0 & $y0 | ($x0 | $y0) & ~($x0 + $y0 >>> 0)) >>> 31) | 0;
	  }
	});


/***/ }),
/* 298 */
/***/ (function(module, exports, __webpack_require__) {

	// https://gist.github.com/BrendanEich/4294d5c212a6d2254703
	var $export = __webpack_require__(8);

	$export($export.S, 'Math', {
	  isubh: function isubh(x0, x1, y0, y1) {
	    var $x0 = x0 >>> 0;
	    var $x1 = x1 >>> 0;
	    var $y0 = y0 >>> 0;
	    return $x1 - (y1 >>> 0) - ((~$x0 & $y0 | ~($x0 ^ $y0) & $x0 - $y0 >>> 0) >>> 31) | 0;
	  }
	});


/***/ }),
/* 299 */
/***/ (function(module, exports, __webpack_require__) {

	// https://gist.github.com/BrendanEich/4294d5c212a6d2254703
	var $export = __webpack_require__(8);

	$export($export.S, 'Math', {
	  imulh: function imulh(u, v) {
	    var UINT16 = 0xffff;
	    var $u = +u;
	    var $v = +v;
	    var u0 = $u & UINT16;
	    var v0 = $v & UINT16;
	    var u1 = $u >> 16;
	    var v1 = $v >> 16;
	    var t = (u1 * v0 >>> 0) + (u0 * v0 >>> 16);
	    return u1 * v1 + (t >> 16) + ((u0 * v1 >>> 0) + (t & UINT16) >> 16);
	  }
	});


/***/ }),
/* 300 */
/***/ (function(module, exports, __webpack_require__) {

	// https://rwaldron.github.io/proposal-math-extensions/
	var $export = __webpack_require__(8);

	$export($export.S, 'Math', { RAD_PER_DEG: 180 / Math.PI });


/***/ }),
/* 301 */
/***/ (function(module, exports, __webpack_require__) {

	// https://rwaldron.github.io/proposal-math-extensions/
	var $export = __webpack_require__(8);
	var DEG_PER_RAD = Math.PI / 180;

	$export($export.S, 'Math', {
	  radians: function radians(degrees) {
	    return degrees * DEG_PER_RAD;
	  }
	});


/***/ }),
/* 302 */
/***/ (function(module, exports, __webpack_require__) {

	// https://rwaldron.github.io/proposal-math-extensions/
	var $export = __webpack_require__(8);

	$export($export.S, 'Math', { scale: __webpack_require__(296) });


/***/ }),
/* 303 */
/***/ (function(module, exports, __webpack_require__) {

	// https://gist.github.com/BrendanEich/4294d5c212a6d2254703
	var $export = __webpack_require__(8);

	$export($export.S, 'Math', {
	  umulh: function umulh(u, v) {
	    var UINT16 = 0xffff;
	    var $u = +u;
	    var $v = +v;
	    var u0 = $u & UINT16;
	    var v0 = $v & UINT16;
	    var u1 = $u >>> 16;
	    var v1 = $v >>> 16;
	    var t = (u1 * v0 >>> 0) + (u0 * v0 >>> 16);
	    return u1 * v1 + (t >>> 16) + ((u0 * v1 >>> 0) + (t & UINT16) >>> 16);
	  }
	});


/***/ }),
/* 304 */
/***/ (function(module, exports, __webpack_require__) {

	// http://jfbastien.github.io/papers/Math.signbit.html
	var $export = __webpack_require__(8);

	$export($export.S, 'Math', { signbit: function signbit(x) {
	  // eslint-disable-next-line no-self-compare
	  return (x = +x) != x ? x : x == 0 ? 1 / x == Infinity : x > 0;
	} });


/***/ }),
/* 305 */
/***/ (function(module, exports, __webpack_require__) {

	// https://github.com/tc39/proposal-promise-finally
	'use strict';
	var $export = __webpack_require__(8);
	var core = __webpack_require__(9);
	var global = __webpack_require__(4);
	var speciesConstructor = __webpack_require__(208);
	var promiseResolve = __webpack_require__(213);

	$export($export.P + $export.R, 'Promise', { 'finally': function (onFinally) {
	  var C = speciesConstructor(this, core.Promise || global.Promise);
	  var isFunction = typeof onFinally == 'function';
	  return this.then(
	    isFunction ? function (x) {
	      return promiseResolve(C, onFinally()).then(function () { return x; });
	    } : onFinally,
	    isFunction ? function (e) {
	      return promiseResolve(C, onFinally()).then(function () { throw e; });
	    } : onFinally
	  );
	} });


/***/ }),
/* 306 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// https://github.com/tc39/proposal-promise-try
	var $export = __webpack_require__(8);
	var newPromiseCapability = __webpack_require__(211);
	var perform = __webpack_require__(212);

	$export($export.S, 'Promise', { 'try': function (callbackfn) {
	  var promiseCapability = newPromiseCapability.f(this);
	  var result = perform(callbackfn);
	  (result.e ? promiseCapability.reject : promiseCapability.resolve)(result.v);
	  return promiseCapability.promise;
	} });


/***/ }),
/* 307 */
/***/ (function(module, exports, __webpack_require__) {

	var metadata = __webpack_require__(308);
	var anObject = __webpack_require__(12);
	var toMetaKey = metadata.key;
	var ordinaryDefineOwnMetadata = metadata.set;

	metadata.exp({ defineMetadata: function defineMetadata(metadataKey, metadataValue, target, targetKey) {
	  ordinaryDefineOwnMetadata(metadataKey, metadataValue, anObject(target), toMetaKey(targetKey));
	} });


/***/ }),
/* 308 */
/***/ (function(module, exports, __webpack_require__) {

	var Map = __webpack_require__(215);
	var $export = __webpack_require__(8);
	var shared = __webpack_require__(23)('metadata');
	var store = shared.store || (shared.store = new (__webpack_require__(220))());

	var getOrCreateMetadataMap = function (target, targetKey, create) {
	  var targetMetadata = store.get(target);
	  if (!targetMetadata) {
	    if (!create) return undefined;
	    store.set(target, targetMetadata = new Map());
	  }
	  var keyMetadata = targetMetadata.get(targetKey);
	  if (!keyMetadata) {
	    if (!create) return undefined;
	    targetMetadata.set(targetKey, keyMetadata = new Map());
	  } return keyMetadata;
	};
	var ordinaryHasOwnMetadata = function (MetadataKey, O, P) {
	  var metadataMap = getOrCreateMetadataMap(O, P, false);
	  return metadataMap === undefined ? false : metadataMap.has(MetadataKey);
	};
	var ordinaryGetOwnMetadata = function (MetadataKey, O, P) {
	  var metadataMap = getOrCreateMetadataMap(O, P, false);
	  return metadataMap === undefined ? undefined : metadataMap.get(MetadataKey);
	};
	var ordinaryDefineOwnMetadata = function (MetadataKey, MetadataValue, O, P) {
	  getOrCreateMetadataMap(O, P, true).set(MetadataKey, MetadataValue);
	};
	var ordinaryOwnMetadataKeys = function (target, targetKey) {
	  var metadataMap = getOrCreateMetadataMap(target, targetKey, false);
	  var keys = [];
	  if (metadataMap) metadataMap.forEach(function (_, key) { keys.push(key); });
	  return keys;
	};
	var toMetaKey = function (it) {
	  return it === undefined || typeof it == 'symbol' ? it : String(it);
	};
	var exp = function (O) {
	  $export($export.S, 'Reflect', O);
	};

	module.exports = {
	  store: store,
	  map: getOrCreateMetadataMap,
	  has: ordinaryHasOwnMetadata,
	  get: ordinaryGetOwnMetadata,
	  set: ordinaryDefineOwnMetadata,
	  keys: ordinaryOwnMetadataKeys,
	  key: toMetaKey,
	  exp: exp
	};


/***/ }),
/* 309 */
/***/ (function(module, exports, __webpack_require__) {

	var metadata = __webpack_require__(308);
	var anObject = __webpack_require__(12);
	var toMetaKey = metadata.key;
	var getOrCreateMetadataMap = metadata.map;
	var store = metadata.store;

	metadata.exp({ deleteMetadata: function deleteMetadata(metadataKey, target /* , targetKey */) {
	  var targetKey = arguments.length < 3 ? undefined : toMetaKey(arguments[2]);
	  var metadataMap = getOrCreateMetadataMap(anObject(target), targetKey, false);
	  if (metadataMap === undefined || !metadataMap['delete'](metadataKey)) return false;
	  if (metadataMap.size) return true;
	  var targetMetadata = store.get(target);
	  targetMetadata['delete'](targetKey);
	  return !!targetMetadata.size || store['delete'](target);
	} });


/***/ }),
/* 310 */
/***/ (function(module, exports, __webpack_require__) {

	var metadata = __webpack_require__(308);
	var anObject = __webpack_require__(12);
	var getPrototypeOf = __webpack_require__(58);
	var ordinaryHasOwnMetadata = metadata.has;
	var ordinaryGetOwnMetadata = metadata.get;
	var toMetaKey = metadata.key;

	var ordinaryGetMetadata = function (MetadataKey, O, P) {
	  var hasOwn = ordinaryHasOwnMetadata(MetadataKey, O, P);
	  if (hasOwn) return ordinaryGetOwnMetadata(MetadataKey, O, P);
	  var parent = getPrototypeOf(O);
	  return parent !== null ? ordinaryGetMetadata(MetadataKey, parent, P) : undefined;
	};

	metadata.exp({ getMetadata: function getMetadata(metadataKey, target /* , targetKey */) {
	  return ordinaryGetMetadata(metadataKey, anObject(target), arguments.length < 3 ? undefined : toMetaKey(arguments[2]));
	} });


/***/ }),
/* 311 */
/***/ (function(module, exports, __webpack_require__) {

	var Set = __webpack_require__(219);
	var from = __webpack_require__(277);
	var metadata = __webpack_require__(308);
	var anObject = __webpack_require__(12);
	var getPrototypeOf = __webpack_require__(58);
	var ordinaryOwnMetadataKeys = metadata.keys;
	var toMetaKey = metadata.key;

	var ordinaryMetadataKeys = function (O, P) {
	  var oKeys = ordinaryOwnMetadataKeys(O, P);
	  var parent = getPrototypeOf(O);
	  if (parent === null) return oKeys;
	  var pKeys = ordinaryMetadataKeys(parent, P);
	  return pKeys.length ? oKeys.length ? from(new Set(oKeys.concat(pKeys))) : pKeys : oKeys;
	};

	metadata.exp({ getMetadataKeys: function getMetadataKeys(target /* , targetKey */) {
	  return ordinaryMetadataKeys(anObject(target), arguments.length < 2 ? undefined : toMetaKey(arguments[1]));
	} });


/***/ }),
/* 312 */
/***/ (function(module, exports, __webpack_require__) {

	var metadata = __webpack_require__(308);
	var anObject = __webpack_require__(12);
	var ordinaryGetOwnMetadata = metadata.get;
	var toMetaKey = metadata.key;

	metadata.exp({ getOwnMetadata: function getOwnMetadata(metadataKey, target /* , targetKey */) {
	  return ordinaryGetOwnMetadata(metadataKey, anObject(target)
	    , arguments.length < 3 ? undefined : toMetaKey(arguments[2]));
	} });


/***/ }),
/* 313 */
/***/ (function(module, exports, __webpack_require__) {

	var metadata = __webpack_require__(308);
	var anObject = __webpack_require__(12);
	var ordinaryOwnMetadataKeys = metadata.keys;
	var toMetaKey = metadata.key;

	metadata.exp({ getOwnMetadataKeys: function getOwnMetadataKeys(target /* , targetKey */) {
	  return ordinaryOwnMetadataKeys(anObject(target), arguments.length < 2 ? undefined : toMetaKey(arguments[1]));
	} });


/***/ }),
/* 314 */
/***/ (function(module, exports, __webpack_require__) {

	var metadata = __webpack_require__(308);
	var anObject = __webpack_require__(12);
	var getPrototypeOf = __webpack_require__(58);
	var ordinaryHasOwnMetadata = metadata.has;
	var toMetaKey = metadata.key;

	var ordinaryHasMetadata = function (MetadataKey, O, P) {
	  var hasOwn = ordinaryHasOwnMetadata(MetadataKey, O, P);
	  if (hasOwn) return true;
	  var parent = getPrototypeOf(O);
	  return parent !== null ? ordinaryHasMetadata(MetadataKey, parent, P) : false;
	};

	metadata.exp({ hasMetadata: function hasMetadata(metadataKey, target /* , targetKey */) {
	  return ordinaryHasMetadata(metadataKey, anObject(target), arguments.length < 3 ? undefined : toMetaKey(arguments[2]));
	} });


/***/ }),
/* 315 */
/***/ (function(module, exports, __webpack_require__) {

	var metadata = __webpack_require__(308);
	var anObject = __webpack_require__(12);
	var ordinaryHasOwnMetadata = metadata.has;
	var toMetaKey = metadata.key;

	metadata.exp({ hasOwnMetadata: function hasOwnMetadata(metadataKey, target /* , targetKey */) {
	  return ordinaryHasOwnMetadata(metadataKey, anObject(target)
	    , arguments.length < 3 ? undefined : toMetaKey(arguments[2]));
	} });


/***/ }),
/* 316 */
/***/ (function(module, exports, __webpack_require__) {

	var $metadata = __webpack_require__(308);
	var anObject = __webpack_require__(12);
	var aFunction = __webpack_require__(21);
	var toMetaKey = $metadata.key;
	var ordinaryDefineOwnMetadata = $metadata.set;

	$metadata.exp({ metadata: function metadata(metadataKey, metadataValue) {
	  return function decorator(target, targetKey) {
	    ordinaryDefineOwnMetadata(
	      metadataKey, metadataValue,
	      (targetKey !== undefined ? anObject : aFunction)(target),
	      toMetaKey(targetKey)
	    );
	  };
	} });


/***/ }),
/* 317 */
/***/ (function(module, exports, __webpack_require__) {

	// https://github.com/rwaldron/tc39-notes/blob/master/es6/2014-09/sept-25.md#510-globalasap-for-enqueuing-a-microtask
	var $export = __webpack_require__(8);
	var microtask = __webpack_require__(210)();
	var process = __webpack_require__(4).process;
	var isNode = __webpack_require__(34)(process) == 'process';

	$export($export.G, {
	  asap: function asap(fn) {
	    var domain = isNode && process.domain;
	    microtask(domain ? domain.bind(fn) : fn);
	  }
	});


/***/ }),
/* 318 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';
	// https://github.com/zenparsing/es-observable
	var $export = __webpack_require__(8);
	var global = __webpack_require__(4);
	var core = __webpack_require__(9);
	var microtask = __webpack_require__(210)();
	var OBSERVABLE = __webpack_require__(25)('observable');
	var aFunction = __webpack_require__(21);
	var anObject = __webpack_require__(12);
	var anInstance = __webpack_require__(206);
	var redefineAll = __webpack_require__(214);
	var hide = __webpack_require__(10);
	var forOf = __webpack_require__(207);
	var RETURN = forOf.RETURN;

	var getMethod = function (fn) {
	  return fn == null ? undefined : aFunction(fn);
	};

	var cleanupSubscription = function (subscription) {
	  var cleanup = subscription._c;
	  if (cleanup) {
	    subscription._c = undefined;
	    cleanup();
	  }
	};

	var subscriptionClosed = function (subscription) {
	  return subscription._o === undefined;
	};

	var closeSubscription = function (subscription) {
	  if (!subscriptionClosed(subscription)) {
	    subscription._o = undefined;
	    cleanupSubscription(subscription);
	  }
	};

	var Subscription = function (observer, subscriber) {
	  anObject(observer);
	  this._c = undefined;
	  this._o = observer;
	  observer = new SubscriptionObserver(this);
	  try {
	    var cleanup = subscriber(observer);
	    var subscription = cleanup;
	    if (cleanup != null) {
	      if (typeof cleanup.unsubscribe === 'function') cleanup = function () { subscription.unsubscribe(); };
	      else aFunction(cleanup);
	      this._c = cleanup;
	    }
	  } catch (e) {
	    observer.error(e);
	    return;
	  } if (subscriptionClosed(this)) cleanupSubscription(this);
	};

	Subscription.prototype = redefineAll({}, {
	  unsubscribe: function unsubscribe() { closeSubscription(this); }
	});

	var SubscriptionObserver = function (subscription) {
	  this._s = subscription;
	};

	SubscriptionObserver.prototype = redefineAll({}, {
	  next: function next(value) {
	    var subscription = this._s;
	    if (!subscriptionClosed(subscription)) {
	      var observer = subscription._o;
	      try {
	        var m = getMethod(observer.next);
	        if (m) return m.call(observer, value);
	      } catch (e) {
	        try {
	          closeSubscription(subscription);
	        } finally {
	          throw e;
	        }
	      }
	    }
	  },
	  error: function error(value) {
	    var subscription = this._s;
	    if (subscriptionClosed(subscription)) throw value;
	    var observer = subscription._o;
	    subscription._o = undefined;
	    try {
	      var m = getMethod(observer.error);
	      if (!m) throw value;
	      value = m.call(observer, value);
	    } catch (e) {
	      try {
	        cleanupSubscription(subscription);
	      } finally {
	        throw e;
	      }
	    } cleanupSubscription(subscription);
	    return value;
	  },
	  complete: function complete(value) {
	    var subscription = this._s;
	    if (!subscriptionClosed(subscription)) {
	      var observer = subscription._o;
	      subscription._o = undefined;
	      try {
	        var m = getMethod(observer.complete);
	        value = m ? m.call(observer, value) : undefined;
	      } catch (e) {
	        try {
	          cleanupSubscription(subscription);
	        } finally {
	          throw e;
	        }
	      } cleanupSubscription(subscription);
	      return value;
	    }
	  }
	});

	var $Observable = function Observable(subscriber) {
	  anInstance(this, $Observable, 'Observable', '_f')._f = aFunction(subscriber);
	};

	redefineAll($Observable.prototype, {
	  subscribe: function subscribe(observer) {
	    return new Subscription(observer, this._f);
	  },
	  forEach: function forEach(fn) {
	    var that = this;
	    return new (core.Promise || global.Promise)(function (resolve, reject) {
	      aFunction(fn);
	      var subscription = that.subscribe({
	        next: function (value) {
	          try {
	            return fn(value);
	          } catch (e) {
	            reject(e);
	            subscription.unsubscribe();
	          }
	        },
	        error: reject,
	        complete: resolve
	      });
	    });
	  }
	});

	redefineAll($Observable, {
	  from: function from(x) {
	    var C = typeof this === 'function' ? this : $Observable;
	    var method = getMethod(anObject(x)[OBSERVABLE]);
	    if (method) {
	      var observable = anObject(method.call(x));
	      return observable.constructor === C ? observable : new C(function (observer) {
	        return observable.subscribe(observer);
	      });
	    }
	    return new C(function (observer) {
	      var done = false;
	      microtask(function () {
	        if (!done) {
	          try {
	            if (forOf(x, false, function (it) {
	              observer.next(it);
	              if (done) return RETURN;
	            }) === RETURN) return;
	          } catch (e) {
	            if (done) throw e;
	            observer.error(e);
	            return;
	          } observer.complete();
	        }
	      });
	      return function () { done = true; };
	    });
	  },
	  of: function of() {
	    for (var i = 0, l = arguments.length, items = Array(l); i < l;) items[i] = arguments[i++];
	    return new (typeof this === 'function' ? this : $Observable)(function (observer) {
	      var done = false;
	      microtask(function () {
	        if (!done) {
	          for (var j = 0; j < items.length; ++j) {
	            observer.next(items[j]);
	            if (done) return;
	          } observer.complete();
	        }
	      });
	      return function () { done = true; };
	    });
	  }
	});

	hide($Observable.prototype, OBSERVABLE, function () { return this; });

	$export($export.G, { Observable: $Observable });

	__webpack_require__(193)('Observable');


/***/ }),
/* 319 */
/***/ (function(module, exports, __webpack_require__) {

	// ie9- setTimeout & setInterval additional parameters fix
	var global = __webpack_require__(4);
	var $export = __webpack_require__(8);
	var navigator = global.navigator;
	var slice = [].slice;
	var MSIE = !!navigator && /MSIE .\./.test(navigator.userAgent); // <- dirty ie9- check
	var wrap = function (set) {
	  return function (fn, time /* , ...args */) {
	    var boundArgs = arguments.length > 2;
	    var args = boundArgs ? slice.call(arguments, 2) : false;
	    return set(boundArgs ? function () {
	      // eslint-disable-next-line no-new-func
	      (typeof fn == 'function' ? fn : Function(fn)).apply(this, args);
	    } : fn, time);
	  };
	};
	$export($export.G + $export.B + $export.F * MSIE, {
	  setTimeout: wrap(global.setTimeout),
	  setInterval: wrap(global.setInterval)
	});


/***/ }),
/* 320 */
/***/ (function(module, exports, __webpack_require__) {

	var $export = __webpack_require__(8);
	var $task = __webpack_require__(209);
	$export($export.G + $export.B, {
	  setImmediate: $task.set,
	  clearImmediate: $task.clear
	});


/***/ }),
/* 321 */
/***/ (function(module, exports, __webpack_require__) {

	var $iterators = __webpack_require__(194);
	var getKeys = __webpack_require__(30);
	var redefine = __webpack_require__(18);
	var global = __webpack_require__(4);
	var hide = __webpack_require__(10);
	var Iterators = __webpack_require__(129);
	var wks = __webpack_require__(25);
	var ITERATOR = wks('iterator');
	var TO_STRING_TAG = wks('toStringTag');
	var ArrayValues = Iterators.Array;

	var DOMIterables = {
	  CSSRuleList: true, // TODO: Not spec compliant, should be false.
	  CSSStyleDeclaration: false,
	  CSSValueList: false,
	  ClientRectList: false,
	  DOMRectList: false,
	  DOMStringList: false,
	  DOMTokenList: true,
	  DataTransferItemList: false,
	  FileList: false,
	  HTMLAllCollection: false,
	  HTMLCollection: false,
	  HTMLFormElement: false,
	  HTMLSelectElement: false,
	  MediaList: true, // TODO: Not spec compliant, should be false.
	  MimeTypeArray: false,
	  NamedNodeMap: false,
	  NodeList: true,
	  PaintRequestList: false,
	  Plugin: false,
	  PluginArray: false,
	  SVGLengthList: false,
	  SVGNumberList: false,
	  SVGPathSegList: false,
	  SVGPointList: false,
	  SVGStringList: false,
	  SVGTransformList: false,
	  SourceBufferList: false,
	  StyleSheetList: true, // TODO: Not spec compliant, should be false.
	  TextTrackCueList: false,
	  TextTrackList: false,
	  TouchList: false
	};

	for (var collections = getKeys(DOMIterables), i = 0; i < collections.length; i++) {
	  var NAME = collections[i];
	  var explicit = DOMIterables[NAME];
	  var Collection = global[NAME];
	  var proto = Collection && Collection.prototype;
	  var key;
	  if (proto) {
	    if (!proto[ITERATOR]) hide(proto, ITERATOR, ArrayValues);
	    if (!proto[TO_STRING_TAG]) hide(proto, TO_STRING_TAG, NAME);
	    Iterators[NAME] = ArrayValues;
	    if (explicit) for (key in $iterators) if (!proto[key]) redefine(proto, key, $iterators[key], true);
	  }
	}


/***/ }),
/* 322 */
/***/ (function(module, exports) {

	/* WEBPACK VAR INJECTION */(function(global) {/**
	 * Copyright (c) 2014, Facebook, Inc.
	 * All rights reserved.
	 *
	 * This source code is licensed under the BSD-style license found in the
	 * https://raw.github.com/facebook/regenerator/master/LICENSE file. An
	 * additional grant of patent rights can be found in the PATENTS file in
	 * the same directory.
	 */

	!(function(global) {
	  "use strict";

	  var Op = Object.prototype;
	  var hasOwn = Op.hasOwnProperty;
	  var undefined; // More compressible than void 0.
	  var $Symbol = typeof Symbol === "function" ? Symbol : {};
	  var iteratorSymbol = $Symbol.iterator || "@@iterator";
	  var asyncIteratorSymbol = $Symbol.asyncIterator || "@@asyncIterator";
	  var toStringTagSymbol = $Symbol.toStringTag || "@@toStringTag";

	  var inModule = typeof module === "object";
	  var runtime = global.regeneratorRuntime;
	  if (runtime) {
	    if (inModule) {
	      // If regeneratorRuntime is defined globally and we're in a module,
	      // make the exports object identical to regeneratorRuntime.
	      module.exports = runtime;
	    }
	    // Don't bother evaluating the rest of this file if the runtime was
	    // already defined globally.
	    return;
	  }

	  // Define the runtime globally (as expected by generated code) as either
	  // module.exports (if we're in a module) or a new, empty object.
	  runtime = global.regeneratorRuntime = inModule ? module.exports : {};

	  function wrap(innerFn, outerFn, self, tryLocsList) {
	    // If outerFn provided and outerFn.prototype is a Generator, then outerFn.prototype instanceof Generator.
	    var protoGenerator = outerFn && outerFn.prototype instanceof Generator ? outerFn : Generator;
	    var generator = Object.create(protoGenerator.prototype);
	    var context = new Context(tryLocsList || []);

	    // The ._invoke method unifies the implementations of the .next,
	    // .throw, and .return methods.
	    generator._invoke = makeInvokeMethod(innerFn, self, context);

	    return generator;
	  }
	  runtime.wrap = wrap;

	  // Try/catch helper to minimize deoptimizations. Returns a completion
	  // record like context.tryEntries[i].completion. This interface could
	  // have been (and was previously) designed to take a closure to be
	  // invoked without arguments, but in all the cases we care about we
	  // already have an existing method we want to call, so there's no need
	  // to create a new function object. We can even get away with assuming
	  // the method takes exactly one argument, since that happens to be true
	  // in every case, so we don't have to touch the arguments object. The
	  // only additional allocation required is the completion record, which
	  // has a stable shape and so hopefully should be cheap to allocate.
	  function tryCatch(fn, obj, arg) {
	    try {
	      return { type: "normal", arg: fn.call(obj, arg) };
	    } catch (err) {
	      return { type: "throw", arg: err };
	    }
	  }

	  var GenStateSuspendedStart = "suspendedStart";
	  var GenStateSuspendedYield = "suspendedYield";
	  var GenStateExecuting = "executing";
	  var GenStateCompleted = "completed";

	  // Returning this object from the innerFn has the same effect as
	  // breaking out of the dispatch switch statement.
	  var ContinueSentinel = {};

	  // Dummy constructor functions that we use as the .constructor and
	  // .constructor.prototype properties for functions that return Generator
	  // objects. For full spec compliance, you may wish to configure your
	  // minifier not to mangle the names of these two functions.
	  function Generator() {}
	  function GeneratorFunction() {}
	  function GeneratorFunctionPrototype() {}

	  // This is a polyfill for %IteratorPrototype% for environments that
	  // don't natively support it.
	  var IteratorPrototype = {};
	  IteratorPrototype[iteratorSymbol] = function () {
	    return this;
	  };

	  var getProto = Object.getPrototypeOf;
	  var NativeIteratorPrototype = getProto && getProto(getProto(values([])));
	  if (NativeIteratorPrototype &&
	      NativeIteratorPrototype !== Op &&
	      hasOwn.call(NativeIteratorPrototype, iteratorSymbol)) {
	    // This environment has a native %IteratorPrototype%; use it instead
	    // of the polyfill.
	    IteratorPrototype = NativeIteratorPrototype;
	  }

	  var Gp = GeneratorFunctionPrototype.prototype =
	    Generator.prototype = Object.create(IteratorPrototype);
	  GeneratorFunction.prototype = Gp.constructor = GeneratorFunctionPrototype;
	  GeneratorFunctionPrototype.constructor = GeneratorFunction;
	  GeneratorFunctionPrototype[toStringTagSymbol] =
	    GeneratorFunction.displayName = "GeneratorFunction";

	  // Helper for defining the .next, .throw, and .return methods of the
	  // Iterator interface in terms of a single ._invoke method.
	  function defineIteratorMethods(prototype) {
	    ["next", "throw", "return"].forEach(function(method) {
	      prototype[method] = function(arg) {
	        return this._invoke(method, arg);
	      };
	    });
	  }

	  runtime.isGeneratorFunction = function(genFun) {
	    var ctor = typeof genFun === "function" && genFun.constructor;
	    return ctor
	      ? ctor === GeneratorFunction ||
	        // For the native GeneratorFunction constructor, the best we can
	        // do is to check its .name property.
	        (ctor.displayName || ctor.name) === "GeneratorFunction"
	      : false;
	  };

	  runtime.mark = function(genFun) {
	    if (Object.setPrototypeOf) {
	      Object.setPrototypeOf(genFun, GeneratorFunctionPrototype);
	    } else {
	      genFun.__proto__ = GeneratorFunctionPrototype;
	      if (!(toStringTagSymbol in genFun)) {
	        genFun[toStringTagSymbol] = "GeneratorFunction";
	      }
	    }
	    genFun.prototype = Object.create(Gp);
	    return genFun;
	  };

	  // Within the body of any async function, `await x` is transformed to
	  // `yield regeneratorRuntime.awrap(x)`, so that the runtime can test
	  // `hasOwn.call(value, "__await")` to determine if the yielded value is
	  // meant to be awaited.
	  runtime.awrap = function(arg) {
	    return { __await: arg };
	  };

	  function AsyncIterator(generator) {
	    function invoke(method, arg, resolve, reject) {
	      var record = tryCatch(generator[method], generator, arg);
	      if (record.type === "throw") {
	        reject(record.arg);
	      } else {
	        var result = record.arg;
	        var value = result.value;
	        if (value &&
	            typeof value === "object" &&
	            hasOwn.call(value, "__await")) {
	          return Promise.resolve(value.__await).then(function(value) {
	            invoke("next", value, resolve, reject);
	          }, function(err) {
	            invoke("throw", err, resolve, reject);
	          });
	        }

	        return Promise.resolve(value).then(function(unwrapped) {
	          // When a yielded Promise is resolved, its final value becomes
	          // the .value of the Promise<{value,done}> result for the
	          // current iteration. If the Promise is rejected, however, the
	          // result for this iteration will be rejected with the same
	          // reason. Note that rejections of yielded Promises are not
	          // thrown back into the generator function, as is the case
	          // when an awaited Promise is rejected. This difference in
	          // behavior between yield and await is important, because it
	          // allows the consumer to decide what to do with the yielded
	          // rejection (swallow it and continue, manually .throw it back
	          // into the generator, abandon iteration, whatever). With
	          // await, by contrast, there is no opportunity to examine the
	          // rejection reason outside the generator function, so the
	          // only option is to throw it from the await expression, and
	          // let the generator function handle the exception.
	          result.value = unwrapped;
	          resolve(result);
	        }, reject);
	      }
	    }

	    if (typeof global.process === "object" && global.process.domain) {
	      invoke = global.process.domain.bind(invoke);
	    }

	    var previousPromise;

	    function enqueue(method, arg) {
	      function callInvokeWithMethodAndArg() {
	        return new Promise(function(resolve, reject) {
	          invoke(method, arg, resolve, reject);
	        });
	      }

	      return previousPromise =
	        // If enqueue has been called before, then we want to wait until
	        // all previous Promises have been resolved before calling invoke,
	        // so that results are always delivered in the correct order. If
	        // enqueue has not been called before, then it is important to
	        // call invoke immediately, without waiting on a callback to fire,
	        // so that the async generator function has the opportunity to do
	        // any necessary setup in a predictable way. This predictability
	        // is why the Promise constructor synchronously invokes its
	        // executor callback, and why async functions synchronously
	        // execute code before the first await. Since we implement simple
	        // async functions in terms of async generators, it is especially
	        // important to get this right, even though it requires care.
	        previousPromise ? previousPromise.then(
	          callInvokeWithMethodAndArg,
	          // Avoid propagating failures to Promises returned by later
	          // invocations of the iterator.
	          callInvokeWithMethodAndArg
	        ) : callInvokeWithMethodAndArg();
	    }

	    // Define the unified helper method that is used to implement .next,
	    // .throw, and .return (see defineIteratorMethods).
	    this._invoke = enqueue;
	  }

	  defineIteratorMethods(AsyncIterator.prototype);
	  AsyncIterator.prototype[asyncIteratorSymbol] = function () {
	    return this;
	  };
	  runtime.AsyncIterator = AsyncIterator;

	  // Note that simple async functions are implemented on top of
	  // AsyncIterator objects; they just return a Promise for the value of
	  // the final result produced by the iterator.
	  runtime.async = function(innerFn, outerFn, self, tryLocsList) {
	    var iter = new AsyncIterator(
	      wrap(innerFn, outerFn, self, tryLocsList)
	    );

	    return runtime.isGeneratorFunction(outerFn)
	      ? iter // If outerFn is a generator, return the full iterator.
	      : iter.next().then(function(result) {
	          return result.done ? result.value : iter.next();
	        });
	  };

	  function makeInvokeMethod(innerFn, self, context) {
	    var state = GenStateSuspendedStart;

	    return function invoke(method, arg) {
	      if (state === GenStateExecuting) {
	        throw new Error("Generator is already running");
	      }

	      if (state === GenStateCompleted) {
	        if (method === "throw") {
	          throw arg;
	        }

	        // Be forgiving, per 25.3.3.3.3 of the spec:
	        // https://people.mozilla.org/~jorendorff/es6-draft.html#sec-generatorresume
	        return doneResult();
	      }

	      context.method = method;
	      context.arg = arg;

	      while (true) {
	        var delegate = context.delegate;
	        if (delegate) {
	          var delegateResult = maybeInvokeDelegate(delegate, context);
	          if (delegateResult) {
	            if (delegateResult === ContinueSentinel) continue;
	            return delegateResult;
	          }
	        }

	        if (context.method === "next") {
	          // Setting context._sent for legacy support of Babel's
	          // function.sent implementation.
	          context.sent = context._sent = context.arg;

	        } else if (context.method === "throw") {
	          if (state === GenStateSuspendedStart) {
	            state = GenStateCompleted;
	            throw context.arg;
	          }

	          context.dispatchException(context.arg);

	        } else if (context.method === "return") {
	          context.abrupt("return", context.arg);
	        }

	        state = GenStateExecuting;

	        var record = tryCatch(innerFn, self, context);
	        if (record.type === "normal") {
	          // If an exception is thrown from innerFn, we leave state ===
	          // GenStateExecuting and loop back for another invocation.
	          state = context.done
	            ? GenStateCompleted
	            : GenStateSuspendedYield;

	          if (record.arg === ContinueSentinel) {
	            continue;
	          }

	          return {
	            value: record.arg,
	            done: context.done
	          };

	        } else if (record.type === "throw") {
	          state = GenStateCompleted;
	          // Dispatch the exception by looping back around to the
	          // context.dispatchException(context.arg) call above.
	          context.method = "throw";
	          context.arg = record.arg;
	        }
	      }
	    };
	  }

	  // Call delegate.iterator[context.method](context.arg) and handle the
	  // result, either by returning a { value, done } result from the
	  // delegate iterator, or by modifying context.method and context.arg,
	  // setting context.delegate to null, and returning the ContinueSentinel.
	  function maybeInvokeDelegate(delegate, context) {
	    var method = delegate.iterator[context.method];
	    if (method === undefined) {
	      // A .throw or .return when the delegate iterator has no .throw
	      // method always terminates the yield* loop.
	      context.delegate = null;

	      if (context.method === "throw") {
	        if (delegate.iterator.return) {
	          // If the delegate iterator has a return method, give it a
	          // chance to clean up.
	          context.method = "return";
	          context.arg = undefined;
	          maybeInvokeDelegate(delegate, context);

	          if (context.method === "throw") {
	            // If maybeInvokeDelegate(context) changed context.method from
	            // "return" to "throw", let that override the TypeError below.
	            return ContinueSentinel;
	          }
	        }

	        context.method = "throw";
	        context.arg = new TypeError(
	          "The iterator does not provide a 'throw' method");
	      }

	      return ContinueSentinel;
	    }

	    var record = tryCatch(method, delegate.iterator, context.arg);

	    if (record.type === "throw") {
	      context.method = "throw";
	      context.arg = record.arg;
	      context.delegate = null;
	      return ContinueSentinel;
	    }

	    var info = record.arg;

	    if (! info) {
	      context.method = "throw";
	      context.arg = new TypeError("iterator result is not an object");
	      context.delegate = null;
	      return ContinueSentinel;
	    }

	    if (info.done) {
	      // Assign the result of the finished delegate to the temporary
	      // variable specified by delegate.resultName (see delegateYield).
	      context[delegate.resultName] = info.value;

	      // Resume execution at the desired location (see delegateYield).
	      context.next = delegate.nextLoc;

	      // If context.method was "throw" but the delegate handled the
	      // exception, let the outer generator proceed normally. If
	      // context.method was "next", forget context.arg since it has been
	      // "consumed" by the delegate iterator. If context.method was
	      // "return", allow the original .return call to continue in the
	      // outer generator.
	      if (context.method !== "return") {
	        context.method = "next";
	        context.arg = undefined;
	      }

	    } else {
	      // Re-yield the result returned by the delegate method.
	      return info;
	    }

	    // The delegate iterator is finished, so forget it and continue with
	    // the outer generator.
	    context.delegate = null;
	    return ContinueSentinel;
	  }

	  // Define Generator.prototype.{next,throw,return} in terms of the
	  // unified ._invoke helper method.
	  defineIteratorMethods(Gp);

	  Gp[toStringTagSymbol] = "Generator";

	  // A Generator should always return itself as the iterator object when the
	  // @@iterator function is called on it. Some browsers' implementations of the
	  // iterator prototype chain incorrectly implement this, causing the Generator
	  // object to not be returned from this call. This ensures that doesn't happen.
	  // See https://github.com/facebook/regenerator/issues/274 for more details.
	  Gp[iteratorSymbol] = function() {
	    return this;
	  };

	  Gp.toString = function() {
	    return "[object Generator]";
	  };

	  function pushTryEntry(locs) {
	    var entry = { tryLoc: locs[0] };

	    if (1 in locs) {
	      entry.catchLoc = locs[1];
	    }

	    if (2 in locs) {
	      entry.finallyLoc = locs[2];
	      entry.afterLoc = locs[3];
	    }

	    this.tryEntries.push(entry);
	  }

	  function resetTryEntry(entry) {
	    var record = entry.completion || {};
	    record.type = "normal";
	    delete record.arg;
	    entry.completion = record;
	  }

	  function Context(tryLocsList) {
	    // The root entry object (effectively a try statement without a catch
	    // or a finally block) gives us a place to store values thrown from
	    // locations where there is no enclosing try statement.
	    this.tryEntries = [{ tryLoc: "root" }];
	    tryLocsList.forEach(pushTryEntry, this);
	    this.reset(true);
	  }

	  runtime.keys = function(object) {
	    var keys = [];
	    for (var key in object) {
	      keys.push(key);
	    }
	    keys.reverse();

	    // Rather than returning an object with a next method, we keep
	    // things simple and return the next function itself.
	    return function next() {
	      while (keys.length) {
	        var key = keys.pop();
	        if (key in object) {
	          next.value = key;
	          next.done = false;
	          return next;
	        }
	      }

	      // To avoid creating an additional object, we just hang the .value
	      // and .done properties off the next function object itself. This
	      // also ensures that the minifier will not anonymize the function.
	      next.done = true;
	      return next;
	    };
	  };

	  function values(iterable) {
	    if (iterable) {
	      var iteratorMethod = iterable[iteratorSymbol];
	      if (iteratorMethod) {
	        return iteratorMethod.call(iterable);
	      }

	      if (typeof iterable.next === "function") {
	        return iterable;
	      }

	      if (!isNaN(iterable.length)) {
	        var i = -1, next = function next() {
	          while (++i < iterable.length) {
	            if (hasOwn.call(iterable, i)) {
	              next.value = iterable[i];
	              next.done = false;
	              return next;
	            }
	          }

	          next.value = undefined;
	          next.done = true;

	          return next;
	        };

	        return next.next = next;
	      }
	    }

	    // Return an iterator with no values.
	    return { next: doneResult };
	  }
	  runtime.values = values;

	  function doneResult() {
	    return { value: undefined, done: true };
	  }

	  Context.prototype = {
	    constructor: Context,

	    reset: function(skipTempReset) {
	      this.prev = 0;
	      this.next = 0;
	      // Resetting context._sent for legacy support of Babel's
	      // function.sent implementation.
	      this.sent = this._sent = undefined;
	      this.done = false;
	      this.delegate = null;

	      this.method = "next";
	      this.arg = undefined;

	      this.tryEntries.forEach(resetTryEntry);

	      if (!skipTempReset) {
	        for (var name in this) {
	          // Not sure about the optimal order of these conditions:
	          if (name.charAt(0) === "t" &&
	              hasOwn.call(this, name) &&
	              !isNaN(+name.slice(1))) {
	            this[name] = undefined;
	          }
	        }
	      }
	    },

	    stop: function() {
	      this.done = true;

	      var rootEntry = this.tryEntries[0];
	      var rootRecord = rootEntry.completion;
	      if (rootRecord.type === "throw") {
	        throw rootRecord.arg;
	      }

	      return this.rval;
	    },

	    dispatchException: function(exception) {
	      if (this.done) {
	        throw exception;
	      }

	      var context = this;
	      function handle(loc, caught) {
	        record.type = "throw";
	        record.arg = exception;
	        context.next = loc;

	        if (caught) {
	          // If the dispatched exception was caught by a catch block,
	          // then let that catch block handle the exception normally.
	          context.method = "next";
	          context.arg = undefined;
	        }

	        return !! caught;
	      }

	      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
	        var entry = this.tryEntries[i];
	        var record = entry.completion;

	        if (entry.tryLoc === "root") {
	          // Exception thrown outside of any try block that could handle
	          // it, so set the completion value of the entire function to
	          // throw the exception.
	          return handle("end");
	        }

	        if (entry.tryLoc <= this.prev) {
	          var hasCatch = hasOwn.call(entry, "catchLoc");
	          var hasFinally = hasOwn.call(entry, "finallyLoc");

	          if (hasCatch && hasFinally) {
	            if (this.prev < entry.catchLoc) {
	              return handle(entry.catchLoc, true);
	            } else if (this.prev < entry.finallyLoc) {
	              return handle(entry.finallyLoc);
	            }

	          } else if (hasCatch) {
	            if (this.prev < entry.catchLoc) {
	              return handle(entry.catchLoc, true);
	            }

	          } else if (hasFinally) {
	            if (this.prev < entry.finallyLoc) {
	              return handle(entry.finallyLoc);
	            }

	          } else {
	            throw new Error("try statement without catch or finally");
	          }
	        }
	      }
	    },

	    abrupt: function(type, arg) {
	      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
	        var entry = this.tryEntries[i];
	        if (entry.tryLoc <= this.prev &&
	            hasOwn.call(entry, "finallyLoc") &&
	            this.prev < entry.finallyLoc) {
	          var finallyEntry = entry;
	          break;
	        }
	      }

	      if (finallyEntry &&
	          (type === "break" ||
	           type === "continue") &&
	          finallyEntry.tryLoc <= arg &&
	          arg <= finallyEntry.finallyLoc) {
	        // Ignore the finally entry if control is not jumping to a
	        // location outside the try/catch block.
	        finallyEntry = null;
	      }

	      var record = finallyEntry ? finallyEntry.completion : {};
	      record.type = type;
	      record.arg = arg;

	      if (finallyEntry) {
	        this.method = "next";
	        this.next = finallyEntry.finallyLoc;
	        return ContinueSentinel;
	      }

	      return this.complete(record);
	    },

	    complete: function(record, afterLoc) {
	      if (record.type === "throw") {
	        throw record.arg;
	      }

	      if (record.type === "break" ||
	          record.type === "continue") {
	        this.next = record.arg;
	      } else if (record.type === "return") {
	        this.rval = this.arg = record.arg;
	        this.method = "return";
	        this.next = "end";
	      } else if (record.type === "normal" && afterLoc) {
	        this.next = afterLoc;
	      }

	      return ContinueSentinel;
	    },

	    finish: function(finallyLoc) {
	      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
	        var entry = this.tryEntries[i];
	        if (entry.finallyLoc === finallyLoc) {
	          this.complete(entry.completion, entry.afterLoc);
	          resetTryEntry(entry);
	          return ContinueSentinel;
	        }
	      }
	    },

	    "catch": function(tryLoc) {
	      for (var i = this.tryEntries.length - 1; i >= 0; --i) {
	        var entry = this.tryEntries[i];
	        if (entry.tryLoc === tryLoc) {
	          var record = entry.completion;
	          if (record.type === "throw") {
	            var thrown = record.arg;
	            resetTryEntry(entry);
	          }
	          return thrown;
	        }
	      }

	      // The context.catch method must only be called with a location
	      // argument that corresponds to a known catch block.
	      throw new Error("illegal catch attempt");
	    },

	    delegateYield: function(iterable, resultName, nextLoc) {
	      this.delegate = {
	        iterator: values(iterable),
	        resultName: resultName,
	        nextLoc: nextLoc
	      };

	      if (this.method === "next") {
	        // Deliberately forget the last sent value so that we don't
	        // accidentally pass it on to the delegate.
	        this.arg = undefined;
	      }

	      return ContinueSentinel;
	    }
	  };
	})(
	  // Among the various tricks for obtaining a reference to the global
	  // object, this seems to be the most reliable technique that does not
	  // use indirect eval (which violates Content Security Policy).
	  typeof global === "object" ? global :
	  typeof window === "object" ? window :
	  typeof self === "object" ? self : this
	);

	/* WEBPACK VAR INJECTION */}.call(exports, (function() { return this; }())))

/***/ }),
/* 323 */
/***/ (function(module, exports, __webpack_require__) {

	__webpack_require__(324);
	module.exports = __webpack_require__(9).RegExp.escape;


/***/ }),
/* 324 */
/***/ (function(module, exports, __webpack_require__) {

	// https://github.com/benjamingr/RexExp.escape
	var $export = __webpack_require__(8);
	var $re = __webpack_require__(325)(/[\\^$*+?.()|[\]{}]/g, '\\$&');

	$export($export.S, 'RegExp', { escape: function escape(it) { return $re(it); } });


/***/ }),
/* 325 */
/***/ (function(module, exports) {

	module.exports = function (regExp, replace) {
	  var replacer = replace === Object(replace) ? function (part) {
	    return replace[part];
	  } : replace;
	  return function (it) {
	    return String(it).replace(regExp, replacer);
	  };
	};


/***/ }),
/* 326 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.CaseMockFlag = exports.trajId2Points = exports.availableTrajsinLimitTime = exports.topicLists = exports.trajs = undefined;
	exports.setGlobalTrajData = setGlobalTrajData;
	exports.calTrajsOrder = calTrajsOrder;
	exports.filterGlobalData = filterGlobalData;
	exports.filterDataInTime = filterDataInTime;
	exports.topicAdd = topicAdd;
	exports.mockTrans = mockTrans;
	exports.MockSearchSite = MockSearchSite;

	var _jquery = __webpack_require__(327);

	var _jquery2 = _interopRequireDefault(_jquery);

	var _d = __webpack_require__(328);

	var d3 = _interopRequireWildcard(_d);

	var _datamanager = __webpack_require__(329);

	var datamanager = _interopRequireWildcard(_datamanager);

	var _searchbar = __webpack_require__(332);

	var SearchBar = _interopRequireWildcard(_searchbar);

	var _config = __webpack_require__(346);

	var mapConfig = _interopRequireWildcard(_config);

	var _index = __webpack_require__(341);

	var map = _interopRequireWildcard(_index);

	var _pic = __webpack_require__(339);

	var _poi = __webpack_require__(342);

	var _index2 = __webpack_require__(350);

	var _index3 = __webpack_require__(353);

	var _time = __webpack_require__(355);

	var _index4 = __webpack_require__(356);

	var _setData = __webpack_require__(361);

	var _setNode = __webpack_require__(362);

	var _case = __webpack_require__(363);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	var trajs = exports.trajs = void 0; // 全量数据 


	// import { mock_sites , mock_sites_origin ,mock_traj_ids   }  from '../mock/case1'
	/**
	 * @preserve Copyright 2017 VAG (Visual Analytics Group), Zhejiang Univ.
	 */
	var orderedTrajs = [];
	var trajsPis = [];
	var topicLists = exports.topicLists = []; //选中的轨迹id 
	var availableTrajsinLimitTime = exports.availableTrajsinLimitTime = []; //在设定时间段内的轨迹

	var trajId2Points = exports.trajId2Points = new Map(); // id => stoppoints


	var CaseMockFlag = exports.CaseMockFlag = false;
	var CaseMockSiteHintFlag = false;
	var mockTrajIds = [];

	function resizeWindow() {
		var div = document.getElementById("app");
		var note = d3.select('#app').append('div').style('position', 'fixed').style('top', '100px').style('text-align', 'center').style('width', '100%').style('font-family', 'Times New Roman" ,"Microsoft YaHei').style('opacity', 0.2).style('font-size', '30px');

		note.append('div').text('请设置屏幕分辨率为 1920*1080');
		note.append('div').text('缺少查询后台，Condition Specification View 无法演示');
	}
	resizeWindow();
	// 初始化
	exports.CaseMockFlag = CaseMockFlag = true;
	datamanager.init().then(function (o) {
		return SearchBar.init();
	});

	(0, _setData.mock)();
	// mockNode()


	// 在 searchbar 中将 trajs 进行设置
	function setGlobalTrajData(data) {
		exports.trajs = trajs = data;

		// 设置 pid 到 stoppoints 的映射
		trajs.forEach(function (traj) {
			var stopPoints = [];
			traj.traj.forEach(function (p, i) {
				if (p.stoppoint !== undefined) {
					stopPoints.push({
						siteId: p.site,
						index: i // index 为 该 stoppoint 为 原轨迹中的第 i 个轨迹点
					});
				}
			});
			trajId2Points.set(traj.pid, stopPoints);
			trajsPis.push(traj.pid);
		});

		calTrajsOrder();
		// drawPic(data)
		timeLineInit(); //会调用  filterDataInTime => drawPic => drawList
	}

	function calTrajsOrder() {
		//计算轨迹的分数 并进行排序
		if (!trajs) return;

		var scale = d3.scaleLinear().range([0, 100]);
		var maxScore = void 0,
		    minScore = void 0;

		trajs.forEach(function (traj) {
			var score = getTrajScore(traj.pid);
			traj.score = score;

			if (!maxScore) {
				maxScore = score, minScore = score;
			}

			maxScore = score > maxScore ? score : maxScore;
			minScore = score < minScore ? score : minScore;
		});

		scale.domain([minScore, maxScore]);

		if (minScore == maxScore) scale.range([100, 100]);

		orderedTrajs = trajs.sort(function (t1, t2) {
			var s1 = t1.score,
			    s2 = t2.score;
			return s2 - s1; // 逆序 
		});

		var putInListFormor = [];
		orderedTrajs = orderedTrajs.map(function (traj) {

			traj.per = scale(traj.score).toFixed(2);

			if (CaseMockFlag && mockTrajIds.indexOf(traj.pid + '') != -1) {
				putInListFormor.push(traj);
				return null;
			}
			return traj;
		});
		orderedTrajs = orderedTrajs.filter(function (traj) {
			if (traj) return true;else return false;
		});
		orderedTrajs = putInListFormor.concat(orderedTrajs);

		(0, _index2.draw)(orderedTrajs);
	}

	// 获取 pid 轨迹 的 score
	function getTrajScore(pid) {
		var nodelist = __webpack_require__(336);

		var sites = trajId2Points.get(pid);

		// sites 基本上为一个   有些轨迹经过的site 差不多  因此得到的分数也差不多
		// sites 数据有bug  stoppoint 表示经过点的次数 ！！！！！！！！


		if (!sites || sites.length == 0) return 0;
		var max = 0,
		    sitescores = void 0,
		    max_id = void 0,
		    siteId = void 0;
		sites.forEach(function (site) {
			// sitescores 为该site周围的poi 的score ，大多为一个 

			siteId = mockTrans(+site.siteId); //case1 打开此行 
			// siteId = site.siteId

			if (siteId == 0) return 0;

			sitescores = nodelist.siteScore.get(+siteId);
			if (!sitescores) return 0;

			// 取 max 
			var _iteratorNormalCompletion = true;
			var _didIteratorError = false;
			var _iteratorError = undefined;

			try {
				for (var _iterator = sitescores.values()[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
					var s = _step.value;

					if (s > max) {
						max = s;
					}
				}
			} catch (err) {
				_didIteratorError = true;
				_iteratorError = err;
			} finally {
				try {
					if (!_iteratorNormalCompletion && _iterator.return) {
						_iterator.return();
					}
				} finally {
					if (_didIteratorError) {
						throw _iteratorError;
					}
				}
			}
		});

		return max;
	}

	// 初始化 TimeLine 的时间范围
	function timeLineInit() {
		var timeRange = [];
		for (var i = 0; i < trajs.length; i++) {
			var traj = trajs[i].traj,
			    startPoint = traj[0],
			    endPoint = traj[traj.length - 1],
			    startTime = new Date(startPoint.startTime),
			    endTime = new Date(endPoint.startTime);
			if (i == 0) {
				timeRange = [startTime, endTime];
			} else {
				if (startTime.getTime() < timeRange[0]) timeRange[0] = startTime;
				if (endTime.getTime() > timeRange[1]) timeRange[1] = endTime;
			}
		}
		(0, _time.init)(timeRange);
	}

	// 框选操作后的轨迹被筛选了， 因此 列表现实的轨迹是筛选后的
	function filterGlobalData(_filteredPids) {
		var filteredPids = _filteredPids || [];
		(0, _index2.filterListGeo)(filteredPids); //传递被过滤掉的
	}

	//timeLine的选择框过滤轨迹
	function filterDataInTime(_startTime, _endTime) {

		var _filteredPids = [],
		    availableTrajs = [];

		// 先筛选一遍  找出 满足 经过 POI点在对应时间的轨迹 

		console.log(' Start filterDataInTime');
		var t1 = new Date().getTime();

		var trajs = orderedTrajs;
		// 先按照 stoppoint 是否在 时间范围内 筛选一遍

		var _loop = function _loop(i) {
			var traj = trajs[i].traj,
			    pid = trajs[i].pid,
			    startPoint = traj[0],
			    endPoint = traj[traj.length - 1],
			    startTime = new Date(startPoint.startTime),
			    endTime = new Date(endPoint.startTime);

			var pois = trajId2Points.get(pid); // 改轨迹经过POI点	
			var havePOIinLimitTime = false;

			pois.forEach(function (poi) {
				var index = poi.index;
				var poiStartTime = new Date(traj[index].startTime);
				var poiEndTime = new Date(traj[index].endTime);
				if (poiEndTime.getTime() <= _endTime && poiStartTime.getTime() >= _startTime) {
					havePOIinLimitTime = true;
				}
			});

			if (!havePOIinLimitTime) {
				_filteredPids.push(pid);
			} else {
				availableTrajs.push(trajs[i]);
			}
		};

		for (var i = 0; i < trajs.length; i++) {
			_loop(i);
		}

		exports.availableTrajsinLimitTime = availableTrajsinLimitTime = [];
		// 对 availableTrajs 进行裁剪
		availableTrajs.forEach(function (traj) {
			var cutTraj = [];
			var fullTraj = traj.traj;
			for (var i = 0; i < fullTraj.length; i++) {
				var startTime = new Date(fullTraj[i].startTime);
				var endTime = new Date(fullTraj[i].endTime);
				if (startTime.getTime() >= _startTime && endTime.getTime() <= _endTime) {
					cutTraj.push(fullTraj[i]);
				}
				if (startTime.getTime() >= _endTime) {
					break;
				}
			}

			if (cutTraj.length > 0) {
				availableTrajsinLimitTime.push({
					pid: traj.pid,
					score: traj.score,
					per: traj.per,
					matching: traj.matching,
					traj: cutTraj
				});
			} else {
				_filteredPids.push(traj.pid);
			}
		});

		var t2 = new Date().getTime();
		console.log('filterDataInTime: ' + (t2 - t1) + 'ms');

		// list
		(0, _index2.draw)(orderedTrajs);
		(0, _index2.filterListTime)(_filteredPids);

		// console.log(availableTrajs)
		// map view
		mapConfig.PicUpdateFlag = true;
		(0, _pic.draw)(availableTrajsinLimitTime);
	}
	// 绘制 Topic
	function topicAdd(topicPids) {
		exports.topicLists = topicLists = [];
		availableTrajsinLimitTime.forEach(function (traj) {
			if (topicPids.indexOf(traj.pid) != -1) {
				topicLists.push(traj);
			}
		});
		(0, _index3.draw)(topicLists);
		(0, _index4.draw)(topicLists);
	}

	//  case1 mock  将poi site 隐射到指定的 site
	function mockTrans(origin_siteId) {
		if (!CaseMockSiteHintFlag) return origin_siteId;

		for (var i = 0; i < 2; i++) {
			if (_case.mock_sites[i].indexOf(+origin_siteId) != -1) {
				return _case.mock_sites_origin[i];
			}
		}
		return 0;
	}

	function MockSearchSite(searchSitesArr, names) {

		// console.log( names)
		if (!CaseMockFlag) return;

		if (names.indexOf('江心屿') != -1 && names.indexOf('物华天宝') != -1) {

			var jxyOrder = names.indexOf('江心屿');
			var whtbOrder = names.indexOf('物华天宝');

			if (jxyOrder < whtbOrder) {
				searchSitesArr[0] = _case.mock_sites[0];
				searchSitesArr[1] = _case.mock_sites[1];
				console.log('jxy ->  whtb');
				console.log('please waitting for 3 min');
				mockTrajIds = _case.mock_traj_ids[1];
			} else {
				searchSitesArr[0] = _case.mock_sites[1];
				searchSitesArr[1] = _case.mock_sites[0];
				// let temp = mock_sites_origin[1]
				// mock_sites_origin[1] = mock_sites_origin[0]
				// mock_sites_origin[0] = temp
				console.log('whtb  -> jxy');
				console.log('please waitting for 3 min');
				mockTrajIds = _case.mock_traj_ids[2];
			}

			CaseMockSiteHintFlag = true;
			console.log('mock go!');
		}
		if (names.indexOf('景点') != -1) {
			mockTrajIds = _case.mock_traj_ids[0];
		}

		if (names.indexOf('温州_大学') != -1) {
			console.log(names);
			mockTrajIds = _case.mock_traj_ids[0];
		}

		if (names.indexOf('温州_医科_大学') != -1 && names.indexOf('医院') != -1) {
			console.log(names);
			mockTrajIds = _case.mock_traj_ids[1];
		}
	}

/***/ }),
/* 327 */
/***/ (function(module, exports, __webpack_require__) {

	var __WEBPACK_AMD_DEFINE_ARRAY__, __WEBPACK_AMD_DEFINE_RESULT__;/*!
	 * jQuery JavaScript Library v3.2.1
	 * https://jquery.com/
	 *
	 * Includes Sizzle.js
	 * https://sizzlejs.com/
	 *
	 * Copyright JS Foundation and other contributors
	 * Released under the MIT license
	 * https://jquery.org/license
	 *
	 * Date: 2017-03-20T18:59Z
	 */
	( function( global, factory ) {

		"use strict";

		if ( typeof module === "object" && typeof module.exports === "object" ) {

			// For CommonJS and CommonJS-like environments where a proper `window`
			// is present, execute the factory and get jQuery.
			// For environments that do not have a `window` with a `document`
			// (such as Node.js), expose a factory as module.exports.
			// This accentuates the need for the creation of a real `window`.
			// e.g. var jQuery = require("jquery")(window);
			// See ticket #14549 for more info.
			module.exports = global.document ?
				factory( global, true ) :
				function( w ) {
					if ( !w.document ) {
						throw new Error( "jQuery requires a window with a document" );
					}
					return factory( w );
				};
		} else {
			factory( global );
		}

	// Pass this if window is not defined yet
	} )( typeof window !== "undefined" ? window : this, function( window, noGlobal ) {

	// Edge <= 12 - 13+, Firefox <=18 - 45+, IE 10 - 11, Safari 5.1 - 9+, iOS 6 - 9.1
	// throw exceptions when non-strict code (e.g., ASP.NET 4.5) accesses strict mode
	// arguments.callee.caller (trac-13335). But as of jQuery 3.0 (2016), strict mode should be common
	// enough that all such attempts are guarded in a try block.
	"use strict";

	var arr = [];

	var document = window.document;

	var getProto = Object.getPrototypeOf;

	var slice = arr.slice;

	var concat = arr.concat;

	var push = arr.push;

	var indexOf = arr.indexOf;

	var class2type = {};

	var toString = class2type.toString;

	var hasOwn = class2type.hasOwnProperty;

	var fnToString = hasOwn.toString;

	var ObjectFunctionString = fnToString.call( Object );

	var support = {};



		function DOMEval( code, doc ) {
			doc = doc || document;

			var script = doc.createElement( "script" );

			script.text = code;
			doc.head.appendChild( script ).parentNode.removeChild( script );
		}
	/* global Symbol */
	// Defining this global in .eslintrc.json would create a danger of using the global
	// unguarded in another place, it seems safer to define global only for this module



	var
		version = "3.2.1",

		// Define a local copy of jQuery
		jQuery = function( selector, context ) {

			// The jQuery object is actually just the init constructor 'enhanced'
			// Need init if jQuery is called (just allow error to be thrown if not included)
			return new jQuery.fn.init( selector, context );
		},

		// Support: Android <=4.0 only
		// Make sure we trim BOM and NBSP
		rtrim = /^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g,

		// Matches dashed string for camelizing
		rmsPrefix = /^-ms-/,
		rdashAlpha = /-([a-z])/g,

		// Used by jQuery.camelCase as callback to replace()
		fcamelCase = function( all, letter ) {
			return letter.toUpperCase();
		};

	jQuery.fn = jQuery.prototype = {

		// The current version of jQuery being used
		jquery: version,

		constructor: jQuery,

		// The default length of a jQuery object is 0
		length: 0,

		toArray: function() {
			return slice.call( this );
		},

		// Get the Nth element in the matched element set OR
		// Get the whole matched element set as a clean array
		get: function( num ) {

			// Return all the elements in a clean array
			if ( num == null ) {
				return slice.call( this );
			}

			// Return just the one element from the set
			return num < 0 ? this[ num + this.length ] : this[ num ];
		},

		// Take an array of elements and push it onto the stack
		// (returning the new matched element set)
		pushStack: function( elems ) {

			// Build a new jQuery matched element set
			var ret = jQuery.merge( this.constructor(), elems );

			// Add the old object onto the stack (as a reference)
			ret.prevObject = this;

			// Return the newly-formed element set
			return ret;
		},

		// Execute a callback for every element in the matched set.
		each: function( callback ) {
			return jQuery.each( this, callback );
		},

		map: function( callback ) {
			return this.pushStack( jQuery.map( this, function( elem, i ) {
				return callback.call( elem, i, elem );
			} ) );
		},

		slice: function() {
			return this.pushStack( slice.apply( this, arguments ) );
		},

		first: function() {
			return this.eq( 0 );
		},

		last: function() {
			return this.eq( -1 );
		},

		eq: function( i ) {
			var len = this.length,
				j = +i + ( i < 0 ? len : 0 );
			return this.pushStack( j >= 0 && j < len ? [ this[ j ] ] : [] );
		},

		end: function() {
			return this.prevObject || this.constructor();
		},

		// For internal use only.
		// Behaves like an Array's method, not like a jQuery method.
		push: push,
		sort: arr.sort,
		splice: arr.splice
	};

	jQuery.extend = jQuery.fn.extend = function() {
		var options, name, src, copy, copyIsArray, clone,
			target = arguments[ 0 ] || {},
			i = 1,
			length = arguments.length,
			deep = false;

		// Handle a deep copy situation
		if ( typeof target === "boolean" ) {
			deep = target;

			// Skip the boolean and the target
			target = arguments[ i ] || {};
			i++;
		}

		// Handle case when target is a string or something (possible in deep copy)
		if ( typeof target !== "object" && !jQuery.isFunction( target ) ) {
			target = {};
		}

		// Extend jQuery itself if only one argument is passed
		if ( i === length ) {
			target = this;
			i--;
		}

		for ( ; i < length; i++ ) {

			// Only deal with non-null/undefined values
			if ( ( options = arguments[ i ] ) != null ) {

				// Extend the base object
				for ( name in options ) {
					src = target[ name ];
					copy = options[ name ];

					// Prevent never-ending loop
					if ( target === copy ) {
						continue;
					}

					// Recurse if we're merging plain objects or arrays
					if ( deep && copy && ( jQuery.isPlainObject( copy ) ||
						( copyIsArray = Array.isArray( copy ) ) ) ) {

						if ( copyIsArray ) {
							copyIsArray = false;
							clone = src && Array.isArray( src ) ? src : [];

						} else {
							clone = src && jQuery.isPlainObject( src ) ? src : {};
						}

						// Never move original objects, clone them
						target[ name ] = jQuery.extend( deep, clone, copy );

					// Don't bring in undefined values
					} else if ( copy !== undefined ) {
						target[ name ] = copy;
					}
				}
			}
		}

		// Return the modified object
		return target;
	};

	jQuery.extend( {

		// Unique for each copy of jQuery on the page
		expando: "jQuery" + ( version + Math.random() ).replace( /\D/g, "" ),

		// Assume jQuery is ready without the ready module
		isReady: true,

		error: function( msg ) {
			throw new Error( msg );
		},

		noop: function() {},

		isFunction: function( obj ) {
			return jQuery.type( obj ) === "function";
		},

		isWindow: function( obj ) {
			return obj != null && obj === obj.window;
		},

		isNumeric: function( obj ) {

			// As of jQuery 3.0, isNumeric is limited to
			// strings and numbers (primitives or objects)
			// that can be coerced to finite numbers (gh-2662)
			var type = jQuery.type( obj );
			return ( type === "number" || type === "string" ) &&

				// parseFloat NaNs numeric-cast false positives ("")
				// ...but misinterprets leading-number strings, particularly hex literals ("0x...")
				// subtraction forces infinities to NaN
				!isNaN( obj - parseFloat( obj ) );
		},

		isPlainObject: function( obj ) {
			var proto, Ctor;

			// Detect obvious negatives
			// Use toString instead of jQuery.type to catch host objects
			if ( !obj || toString.call( obj ) !== "[object Object]" ) {
				return false;
			}

			proto = getProto( obj );

			// Objects with no prototype (e.g., `Object.create( null )`) are plain
			if ( !proto ) {
				return true;
			}

			// Objects with prototype are plain iff they were constructed by a global Object function
			Ctor = hasOwn.call( proto, "constructor" ) && proto.constructor;
			return typeof Ctor === "function" && fnToString.call( Ctor ) === ObjectFunctionString;
		},

		isEmptyObject: function( obj ) {

			/* eslint-disable no-unused-vars */
			// See https://github.com/eslint/eslint/issues/6125
			var name;

			for ( name in obj ) {
				return false;
			}
			return true;
		},

		type: function( obj ) {
			if ( obj == null ) {
				return obj + "";
			}

			// Support: Android <=2.3 only (functionish RegExp)
			return typeof obj === "object" || typeof obj === "function" ?
				class2type[ toString.call( obj ) ] || "object" :
				typeof obj;
		},

		// Evaluates a script in a global context
		globalEval: function( code ) {
			DOMEval( code );
		},

		// Convert dashed to camelCase; used by the css and data modules
		// Support: IE <=9 - 11, Edge 12 - 13
		// Microsoft forgot to hump their vendor prefix (#9572)
		camelCase: function( string ) {
			return string.replace( rmsPrefix, "ms-" ).replace( rdashAlpha, fcamelCase );
		},

		each: function( obj, callback ) {
			var length, i = 0;

			if ( isArrayLike( obj ) ) {
				length = obj.length;
				for ( ; i < length; i++ ) {
					if ( callback.call( obj[ i ], i, obj[ i ] ) === false ) {
						break;
					}
				}
			} else {
				for ( i in obj ) {
					if ( callback.call( obj[ i ], i, obj[ i ] ) === false ) {
						break;
					}
				}
			}

			return obj;
		},

		// Support: Android <=4.0 only
		trim: function( text ) {
			return text == null ?
				"" :
				( text + "" ).replace( rtrim, "" );
		},

		// results is for internal usage only
		makeArray: function( arr, results ) {
			var ret = results || [];

			if ( arr != null ) {
				if ( isArrayLike( Object( arr ) ) ) {
					jQuery.merge( ret,
						typeof arr === "string" ?
						[ arr ] : arr
					);
				} else {
					push.call( ret, arr );
				}
			}

			return ret;
		},

		inArray: function( elem, arr, i ) {
			return arr == null ? -1 : indexOf.call( arr, elem, i );
		},

		// Support: Android <=4.0 only, PhantomJS 1 only
		// push.apply(_, arraylike) throws on ancient WebKit
		merge: function( first, second ) {
			var len = +second.length,
				j = 0,
				i = first.length;

			for ( ; j < len; j++ ) {
				first[ i++ ] = second[ j ];
			}

			first.length = i;

			return first;
		},

		grep: function( elems, callback, invert ) {
			var callbackInverse,
				matches = [],
				i = 0,
				length = elems.length,
				callbackExpect = !invert;

			// Go through the array, only saving the items
			// that pass the validator function
			for ( ; i < length; i++ ) {
				callbackInverse = !callback( elems[ i ], i );
				if ( callbackInverse !== callbackExpect ) {
					matches.push( elems[ i ] );
				}
			}

			return matches;
		},

		// arg is for internal usage only
		map: function( elems, callback, arg ) {
			var length, value,
				i = 0,
				ret = [];

			// Go through the array, translating each of the items to their new values
			if ( isArrayLike( elems ) ) {
				length = elems.length;
				for ( ; i < length; i++ ) {
					value = callback( elems[ i ], i, arg );

					if ( value != null ) {
						ret.push( value );
					}
				}

			// Go through every key on the object,
			} else {
				for ( i in elems ) {
					value = callback( elems[ i ], i, arg );

					if ( value != null ) {
						ret.push( value );
					}
				}
			}

			// Flatten any nested arrays
			return concat.apply( [], ret );
		},

		// A global GUID counter for objects
		guid: 1,

		// Bind a function to a context, optionally partially applying any
		// arguments.
		proxy: function( fn, context ) {
			var tmp, args, proxy;

			if ( typeof context === "string" ) {
				tmp = fn[ context ];
				context = fn;
				fn = tmp;
			}

			// Quick check to determine if target is callable, in the spec
			// this throws a TypeError, but we will just return undefined.
			if ( !jQuery.isFunction( fn ) ) {
				return undefined;
			}

			// Simulated bind
			args = slice.call( arguments, 2 );
			proxy = function() {
				return fn.apply( context || this, args.concat( slice.call( arguments ) ) );
			};

			// Set the guid of unique handler to the same of original handler, so it can be removed
			proxy.guid = fn.guid = fn.guid || jQuery.guid++;

			return proxy;
		},

		now: Date.now,

		// jQuery.support is not used in Core but other projects attach their
		// properties to it so it needs to exist.
		support: support
	} );

	if ( typeof Symbol === "function" ) {
		jQuery.fn[ Symbol.iterator ] = arr[ Symbol.iterator ];
	}

	// Populate the class2type map
	jQuery.each( "Boolean Number String Function Array Date RegExp Object Error Symbol".split( " " ),
	function( i, name ) {
		class2type[ "[object " + name + "]" ] = name.toLowerCase();
	} );

	function isArrayLike( obj ) {

		// Support: real iOS 8.2 only (not reproducible in simulator)
		// `in` check used to prevent JIT error (gh-2145)
		// hasOwn isn't used here due to false negatives
		// regarding Nodelist length in IE
		var length = !!obj && "length" in obj && obj.length,
			type = jQuery.type( obj );

		if ( type === "function" || jQuery.isWindow( obj ) ) {
			return false;
		}

		return type === "array" || length === 0 ||
			typeof length === "number" && length > 0 && ( length - 1 ) in obj;
	}
	var Sizzle =
	/*!
	 * Sizzle CSS Selector Engine v2.3.3
	 * https://sizzlejs.com/
	 *
	 * Copyright jQuery Foundation and other contributors
	 * Released under the MIT license
	 * http://jquery.org/license
	 *
	 * Date: 2016-08-08
	 */
	(function( window ) {

	var i,
		support,
		Expr,
		getText,
		isXML,
		tokenize,
		compile,
		select,
		outermostContext,
		sortInput,
		hasDuplicate,

		// Local document vars
		setDocument,
		document,
		docElem,
		documentIsHTML,
		rbuggyQSA,
		rbuggyMatches,
		matches,
		contains,

		// Instance-specific data
		expando = "sizzle" + 1 * new Date(),
		preferredDoc = window.document,
		dirruns = 0,
		done = 0,
		classCache = createCache(),
		tokenCache = createCache(),
		compilerCache = createCache(),
		sortOrder = function( a, b ) {
			if ( a === b ) {
				hasDuplicate = true;
			}
			return 0;
		},

		// Instance methods
		hasOwn = ({}).hasOwnProperty,
		arr = [],
		pop = arr.pop,
		push_native = arr.push,
		push = arr.push,
		slice = arr.slice,
		// Use a stripped-down indexOf as it's faster than native
		// https://jsperf.com/thor-indexof-vs-for/5
		indexOf = function( list, elem ) {
			var i = 0,
				len = list.length;
			for ( ; i < len; i++ ) {
				if ( list[i] === elem ) {
					return i;
				}
			}
			return -1;
		},

		booleans = "checked|selected|async|autofocus|autoplay|controls|defer|disabled|hidden|ismap|loop|multiple|open|readonly|required|scoped",

		// Regular expressions

		// http://www.w3.org/TR/css3-selectors/#whitespace
		whitespace = "[\\x20\\t\\r\\n\\f]",

		// http://www.w3.org/TR/CSS21/syndata.html#value-def-identifier
		identifier = "(?:\\\\.|[\\w-]|[^\0-\\xa0])+",

		// Attribute selectors: http://www.w3.org/TR/selectors/#attribute-selectors
		attributes = "\\[" + whitespace + "*(" + identifier + ")(?:" + whitespace +
			// Operator (capture 2)
			"*([*^$|!~]?=)" + whitespace +
			// "Attribute values must be CSS identifiers [capture 5] or strings [capture 3 or capture 4]"
			"*(?:'((?:\\\\.|[^\\\\'])*)'|\"((?:\\\\.|[^\\\\\"])*)\"|(" + identifier + "))|)" + whitespace +
			"*\\]",

		pseudos = ":(" + identifier + ")(?:\\((" +
			// To reduce the number of selectors needing tokenize in the preFilter, prefer arguments:
			// 1. quoted (capture 3; capture 4 or capture 5)
			"('((?:\\\\.|[^\\\\'])*)'|\"((?:\\\\.|[^\\\\\"])*)\")|" +
			// 2. simple (capture 6)
			"((?:\\\\.|[^\\\\()[\\]]|" + attributes + ")*)|" +
			// 3. anything else (capture 2)
			".*" +
			")\\)|)",

		// Leading and non-escaped trailing whitespace, capturing some non-whitespace characters preceding the latter
		rwhitespace = new RegExp( whitespace + "+", "g" ),
		rtrim = new RegExp( "^" + whitespace + "+|((?:^|[^\\\\])(?:\\\\.)*)" + whitespace + "+$", "g" ),

		rcomma = new RegExp( "^" + whitespace + "*," + whitespace + "*" ),
		rcombinators = new RegExp( "^" + whitespace + "*([>+~]|" + whitespace + ")" + whitespace + "*" ),

		rattributeQuotes = new RegExp( "=" + whitespace + "*([^\\]'\"]*?)" + whitespace + "*\\]", "g" ),

		rpseudo = new RegExp( pseudos ),
		ridentifier = new RegExp( "^" + identifier + "$" ),

		matchExpr = {
			"ID": new RegExp( "^#(" + identifier + ")" ),
			"CLASS": new RegExp( "^\\.(" + identifier + ")" ),
			"TAG": new RegExp( "^(" + identifier + "|[*])" ),
			"ATTR": new RegExp( "^" + attributes ),
			"PSEUDO": new RegExp( "^" + pseudos ),
			"CHILD": new RegExp( "^:(only|first|last|nth|nth-last)-(child|of-type)(?:\\(" + whitespace +
				"*(even|odd|(([+-]|)(\\d*)n|)" + whitespace + "*(?:([+-]|)" + whitespace +
				"*(\\d+)|))" + whitespace + "*\\)|)", "i" ),
			"bool": new RegExp( "^(?:" + booleans + ")$", "i" ),
			// For use in libraries implementing .is()
			// We use this for POS matching in `select`
			"needsContext": new RegExp( "^" + whitespace + "*[>+~]|:(even|odd|eq|gt|lt|nth|first|last)(?:\\(" +
				whitespace + "*((?:-\\d)?\\d*)" + whitespace + "*\\)|)(?=[^-]|$)", "i" )
		},

		rinputs = /^(?:input|select|textarea|button)$/i,
		rheader = /^h\d$/i,

		rnative = /^[^{]+\{\s*\[native \w/,

		// Easily-parseable/retrievable ID or TAG or CLASS selectors
		rquickExpr = /^(?:#([\w-]+)|(\w+)|\.([\w-]+))$/,

		rsibling = /[+~]/,

		// CSS escapes
		// http://www.w3.org/TR/CSS21/syndata.html#escaped-characters
		runescape = new RegExp( "\\\\([\\da-f]{1,6}" + whitespace + "?|(" + whitespace + ")|.)", "ig" ),
		funescape = function( _, escaped, escapedWhitespace ) {
			var high = "0x" + escaped - 0x10000;
			// NaN means non-codepoint
			// Support: Firefox<24
			// Workaround erroneous numeric interpretation of +"0x"
			return high !== high || escapedWhitespace ?
				escaped :
				high < 0 ?
					// BMP codepoint
					String.fromCharCode( high + 0x10000 ) :
					// Supplemental Plane codepoint (surrogate pair)
					String.fromCharCode( high >> 10 | 0xD800, high & 0x3FF | 0xDC00 );
		},

		// CSS string/identifier serialization
		// https://drafts.csswg.org/cssom/#common-serializing-idioms
		rcssescape = /([\0-\x1f\x7f]|^-?\d)|^-$|[^\0-\x1f\x7f-\uFFFF\w-]/g,
		fcssescape = function( ch, asCodePoint ) {
			if ( asCodePoint ) {

				// U+0000 NULL becomes U+FFFD REPLACEMENT CHARACTER
				if ( ch === "\0" ) {
					return "\uFFFD";
				}

				// Control characters and (dependent upon position) numbers get escaped as code points
				return ch.slice( 0, -1 ) + "\\" + ch.charCodeAt( ch.length - 1 ).toString( 16 ) + " ";
			}

			// Other potentially-special ASCII characters get backslash-escaped
			return "\\" + ch;
		},

		// Used for iframes
		// See setDocument()
		// Removing the function wrapper causes a "Permission Denied"
		// error in IE
		unloadHandler = function() {
			setDocument();
		},

		disabledAncestor = addCombinator(
			function( elem ) {
				return elem.disabled === true && ("form" in elem || "label" in elem);
			},
			{ dir: "parentNode", next: "legend" }
		);

	// Optimize for push.apply( _, NodeList )
	try {
		push.apply(
			(arr = slice.call( preferredDoc.childNodes )),
			preferredDoc.childNodes
		);
		// Support: Android<4.0
		// Detect silently failing push.apply
		arr[ preferredDoc.childNodes.length ].nodeType;
	} catch ( e ) {
		push = { apply: arr.length ?

			// Leverage slice if possible
			function( target, els ) {
				push_native.apply( target, slice.call(els) );
			} :

			// Support: IE<9
			// Otherwise append directly
			function( target, els ) {
				var j = target.length,
					i = 0;
				// Can't trust NodeList.length
				while ( (target[j++] = els[i++]) ) {}
				target.length = j - 1;
			}
		};
	}

	function Sizzle( selector, context, results, seed ) {
		var m, i, elem, nid, match, groups, newSelector,
			newContext = context && context.ownerDocument,

			// nodeType defaults to 9, since context defaults to document
			nodeType = context ? context.nodeType : 9;

		results = results || [];

		// Return early from calls with invalid selector or context
		if ( typeof selector !== "string" || !selector ||
			nodeType !== 1 && nodeType !== 9 && nodeType !== 11 ) {

			return results;
		}

		// Try to shortcut find operations (as opposed to filters) in HTML documents
		if ( !seed ) {

			if ( ( context ? context.ownerDocument || context : preferredDoc ) !== document ) {
				setDocument( context );
			}
			context = context || document;

			if ( documentIsHTML ) {

				// If the selector is sufficiently simple, try using a "get*By*" DOM method
				// (excepting DocumentFragment context, where the methods don't exist)
				if ( nodeType !== 11 && (match = rquickExpr.exec( selector )) ) {

					// ID selector
					if ( (m = match[1]) ) {

						// Document context
						if ( nodeType === 9 ) {
							if ( (elem = context.getElementById( m )) ) {

								// Support: IE, Opera, Webkit
								// TODO: identify versions
								// getElementById can match elements by name instead of ID
								if ( elem.id === m ) {
									results.push( elem );
									return results;
								}
							} else {
								return results;
							}

						// Element context
						} else {

							// Support: IE, Opera, Webkit
							// TODO: identify versions
							// getElementById can match elements by name instead of ID
							if ( newContext && (elem = newContext.getElementById( m )) &&
								contains( context, elem ) &&
								elem.id === m ) {

								results.push( elem );
								return results;
							}
						}

					// Type selector
					} else if ( match[2] ) {
						push.apply( results, context.getElementsByTagName( selector ) );
						return results;

					// Class selector
					} else if ( (m = match[3]) && support.getElementsByClassName &&
						context.getElementsByClassName ) {

						push.apply( results, context.getElementsByClassName( m ) );
						return results;
					}
				}

				// Take advantage of querySelectorAll
				if ( support.qsa &&
					!compilerCache[ selector + " " ] &&
					(!rbuggyQSA || !rbuggyQSA.test( selector )) ) {

					if ( nodeType !== 1 ) {
						newContext = context;
						newSelector = selector;

					// qSA looks outside Element context, which is not what we want
					// Thanks to Andrew Dupont for this workaround technique
					// Support: IE <=8
					// Exclude object elements
					} else if ( context.nodeName.toLowerCase() !== "object" ) {

						// Capture the context ID, setting it first if necessary
						if ( (nid = context.getAttribute( "id" )) ) {
							nid = nid.replace( rcssescape, fcssescape );
						} else {
							context.setAttribute( "id", (nid = expando) );
						}

						// Prefix every selector in the list
						groups = tokenize( selector );
						i = groups.length;
						while ( i-- ) {
							groups[i] = "#" + nid + " " + toSelector( groups[i] );
						}
						newSelector = groups.join( "," );

						// Expand context for sibling selectors
						newContext = rsibling.test( selector ) && testContext( context.parentNode ) ||
							context;
					}

					if ( newSelector ) {
						try {
							push.apply( results,
								newContext.querySelectorAll( newSelector )
							);
							return results;
						} catch ( qsaError ) {
						} finally {
							if ( nid === expando ) {
								context.removeAttribute( "id" );
							}
						}
					}
				}
			}
		}

		// All others
		return select( selector.replace( rtrim, "$1" ), context, results, seed );
	}

	/**
	 * Create key-value caches of limited size
	 * @returns {function(string, object)} Returns the Object data after storing it on itself with
	 *	property name the (space-suffixed) string and (if the cache is larger than Expr.cacheLength)
	 *	deleting the oldest entry
	 */
	function createCache() {
		var keys = [];

		function cache( key, value ) {
			// Use (key + " ") to avoid collision with native prototype properties (see Issue #157)
			if ( keys.push( key + " " ) > Expr.cacheLength ) {
				// Only keep the most recent entries
				delete cache[ keys.shift() ];
			}
			return (cache[ key + " " ] = value);
		}
		return cache;
	}

	/**
	 * Mark a function for special use by Sizzle
	 * @param {Function} fn The function to mark
	 */
	function markFunction( fn ) {
		fn[ expando ] = true;
		return fn;
	}

	/**
	 * Support testing using an element
	 * @param {Function} fn Passed the created element and returns a boolean result
	 */
	function assert( fn ) {
		var el = document.createElement("fieldset");

		try {
			return !!fn( el );
		} catch (e) {
			return false;
		} finally {
			// Remove from its parent by default
			if ( el.parentNode ) {
				el.parentNode.removeChild( el );
			}
			// release memory in IE
			el = null;
		}
	}

	/**
	 * Adds the same handler for all of the specified attrs
	 * @param {String} attrs Pipe-separated list of attributes
	 * @param {Function} handler The method that will be applied
	 */
	function addHandle( attrs, handler ) {
		var arr = attrs.split("|"),
			i = arr.length;

		while ( i-- ) {
			Expr.attrHandle[ arr[i] ] = handler;
		}
	}

	/**
	 * Checks document order of two siblings
	 * @param {Element} a
	 * @param {Element} b
	 * @returns {Number} Returns less than 0 if a precedes b, greater than 0 if a follows b
	 */
	function siblingCheck( a, b ) {
		var cur = b && a,
			diff = cur && a.nodeType === 1 && b.nodeType === 1 &&
				a.sourceIndex - b.sourceIndex;

		// Use IE sourceIndex if available on both nodes
		if ( diff ) {
			return diff;
		}

		// Check if b follows a
		if ( cur ) {
			while ( (cur = cur.nextSibling) ) {
				if ( cur === b ) {
					return -1;
				}
			}
		}

		return a ? 1 : -1;
	}

	/**
	 * Returns a function to use in pseudos for input types
	 * @param {String} type
	 */
	function createInputPseudo( type ) {
		return function( elem ) {
			var name = elem.nodeName.toLowerCase();
			return name === "input" && elem.type === type;
		};
	}

	/**
	 * Returns a function to use in pseudos for buttons
	 * @param {String} type
	 */
	function createButtonPseudo( type ) {
		return function( elem ) {
			var name = elem.nodeName.toLowerCase();
			return (name === "input" || name === "button") && elem.type === type;
		};
	}

	/**
	 * Returns a function to use in pseudos for :enabled/:disabled
	 * @param {Boolean} disabled true for :disabled; false for :enabled
	 */
	function createDisabledPseudo( disabled ) {

		// Known :disabled false positives: fieldset[disabled] > legend:nth-of-type(n+2) :can-disable
		return function( elem ) {

			// Only certain elements can match :enabled or :disabled
			// https://html.spec.whatwg.org/multipage/scripting.html#selector-enabled
			// https://html.spec.whatwg.org/multipage/scripting.html#selector-disabled
			if ( "form" in elem ) {

				// Check for inherited disabledness on relevant non-disabled elements:
				// * listed form-associated elements in a disabled fieldset
				//   https://html.spec.whatwg.org/multipage/forms.html#category-listed
				//   https://html.spec.whatwg.org/multipage/forms.html#concept-fe-disabled
				// * option elements in a disabled optgroup
				//   https://html.spec.whatwg.org/multipage/forms.html#concept-option-disabled
				// All such elements have a "form" property.
				if ( elem.parentNode && elem.disabled === false ) {

					// Option elements defer to a parent optgroup if present
					if ( "label" in elem ) {
						if ( "label" in elem.parentNode ) {
							return elem.parentNode.disabled === disabled;
						} else {
							return elem.disabled === disabled;
						}
					}

					// Support: IE 6 - 11
					// Use the isDisabled shortcut property to check for disabled fieldset ancestors
					return elem.isDisabled === disabled ||

						// Where there is no isDisabled, check manually
						/* jshint -W018 */
						elem.isDisabled !== !disabled &&
							disabledAncestor( elem ) === disabled;
				}

				return elem.disabled === disabled;

			// Try to winnow out elements that can't be disabled before trusting the disabled property.
			// Some victims get caught in our net (label, legend, menu, track), but it shouldn't
			// even exist on them, let alone have a boolean value.
			} else if ( "label" in elem ) {
				return elem.disabled === disabled;
			}

			// Remaining elements are neither :enabled nor :disabled
			return false;
		};
	}

	/**
	 * Returns a function to use in pseudos for positionals
	 * @param {Function} fn
	 */
	function createPositionalPseudo( fn ) {
		return markFunction(function( argument ) {
			argument = +argument;
			return markFunction(function( seed, matches ) {
				var j,
					matchIndexes = fn( [], seed.length, argument ),
					i = matchIndexes.length;

				// Match elements found at the specified indexes
				while ( i-- ) {
					if ( seed[ (j = matchIndexes[i]) ] ) {
						seed[j] = !(matches[j] = seed[j]);
					}
				}
			});
		});
	}

	/**
	 * Checks a node for validity as a Sizzle context
	 * @param {Element|Object=} context
	 * @returns {Element|Object|Boolean} The input node if acceptable, otherwise a falsy value
	 */
	function testContext( context ) {
		return context && typeof context.getElementsByTagName !== "undefined" && context;
	}

	// Expose support vars for convenience
	support = Sizzle.support = {};

	/**
	 * Detects XML nodes
	 * @param {Element|Object} elem An element or a document
	 * @returns {Boolean} True iff elem is a non-HTML XML node
	 */
	isXML = Sizzle.isXML = function( elem ) {
		// documentElement is verified for cases where it doesn't yet exist
		// (such as loading iframes in IE - #4833)
		var documentElement = elem && (elem.ownerDocument || elem).documentElement;
		return documentElement ? documentElement.nodeName !== "HTML" : false;
	};

	/**
	 * Sets document-related variables once based on the current document
	 * @param {Element|Object} [doc] An element or document object to use to set the document
	 * @returns {Object} Returns the current document
	 */
	setDocument = Sizzle.setDocument = function( node ) {
		var hasCompare, subWindow,
			doc = node ? node.ownerDocument || node : preferredDoc;

		// Return early if doc is invalid or already selected
		if ( doc === document || doc.nodeType !== 9 || !doc.documentElement ) {
			return document;
		}

		// Update global variables
		document = doc;
		docElem = document.documentElement;
		documentIsHTML = !isXML( document );

		// Support: IE 9-11, Edge
		// Accessing iframe documents after unload throws "permission denied" errors (jQuery #13936)
		if ( preferredDoc !== document &&
			(subWindow = document.defaultView) && subWindow.top !== subWindow ) {

			// Support: IE 11, Edge
			if ( subWindow.addEventListener ) {
				subWindow.addEventListener( "unload", unloadHandler, false );

			// Support: IE 9 - 10 only
			} else if ( subWindow.attachEvent ) {
				subWindow.attachEvent( "onunload", unloadHandler );
			}
		}

		/* Attributes
		---------------------------------------------------------------------- */

		// Support: IE<8
		// Verify that getAttribute really returns attributes and not properties
		// (excepting IE8 booleans)
		support.attributes = assert(function( el ) {
			el.className = "i";
			return !el.getAttribute("className");
		});

		/* getElement(s)By*
		---------------------------------------------------------------------- */

		// Check if getElementsByTagName("*") returns only elements
		support.getElementsByTagName = assert(function( el ) {
			el.appendChild( document.createComment("") );
			return !el.getElementsByTagName("*").length;
		});

		// Support: IE<9
		support.getElementsByClassName = rnative.test( document.getElementsByClassName );

		// Support: IE<10
		// Check if getElementById returns elements by name
		// The broken getElementById methods don't pick up programmatically-set names,
		// so use a roundabout getElementsByName test
		support.getById = assert(function( el ) {
			docElem.appendChild( el ).id = expando;
			return !document.getElementsByName || !document.getElementsByName( expando ).length;
		});

		// ID filter and find
		if ( support.getById ) {
			Expr.filter["ID"] = function( id ) {
				var attrId = id.replace( runescape, funescape );
				return function( elem ) {
					return elem.getAttribute("id") === attrId;
				};
			};
			Expr.find["ID"] = function( id, context ) {
				if ( typeof context.getElementById !== "undefined" && documentIsHTML ) {
					var elem = context.getElementById( id );
					return elem ? [ elem ] : [];
				}
			};
		} else {
			Expr.filter["ID"] =  function( id ) {
				var attrId = id.replace( runescape, funescape );
				return function( elem ) {
					var node = typeof elem.getAttributeNode !== "undefined" &&
						elem.getAttributeNode("id");
					return node && node.value === attrId;
				};
			};

			// Support: IE 6 - 7 only
			// getElementById is not reliable as a find shortcut
			Expr.find["ID"] = function( id, context ) {
				if ( typeof context.getElementById !== "undefined" && documentIsHTML ) {
					var node, i, elems,
						elem = context.getElementById( id );

					if ( elem ) {

						// Verify the id attribute
						node = elem.getAttributeNode("id");
						if ( node && node.value === id ) {
							return [ elem ];
						}

						// Fall back on getElementsByName
						elems = context.getElementsByName( id );
						i = 0;
						while ( (elem = elems[i++]) ) {
							node = elem.getAttributeNode("id");
							if ( node && node.value === id ) {
								return [ elem ];
							}
						}
					}

					return [];
				}
			};
		}

		// Tag
		Expr.find["TAG"] = support.getElementsByTagName ?
			function( tag, context ) {
				if ( typeof context.getElementsByTagName !== "undefined" ) {
					return context.getElementsByTagName( tag );

				// DocumentFragment nodes don't have gEBTN
				} else if ( support.qsa ) {
					return context.querySelectorAll( tag );
				}
			} :

			function( tag, context ) {
				var elem,
					tmp = [],
					i = 0,
					// By happy coincidence, a (broken) gEBTN appears on DocumentFragment nodes too
					results = context.getElementsByTagName( tag );

				// Filter out possible comments
				if ( tag === "*" ) {
					while ( (elem = results[i++]) ) {
						if ( elem.nodeType === 1 ) {
							tmp.push( elem );
						}
					}

					return tmp;
				}
				return results;
			};

		// Class
		Expr.find["CLASS"] = support.getElementsByClassName && function( className, context ) {
			if ( typeof context.getElementsByClassName !== "undefined" && documentIsHTML ) {
				return context.getElementsByClassName( className );
			}
		};

		/* QSA/matchesSelector
		---------------------------------------------------------------------- */

		// QSA and matchesSelector support

		// matchesSelector(:active) reports false when true (IE9/Opera 11.5)
		rbuggyMatches = [];

		// qSa(:focus) reports false when true (Chrome 21)
		// We allow this because of a bug in IE8/9 that throws an error
		// whenever `document.activeElement` is accessed on an iframe
		// So, we allow :focus to pass through QSA all the time to avoid the IE error
		// See https://bugs.jquery.com/ticket/13378
		rbuggyQSA = [];

		if ( (support.qsa = rnative.test( document.querySelectorAll )) ) {
			// Build QSA regex
			// Regex strategy adopted from Diego Perini
			assert(function( el ) {
				// Select is set to empty string on purpose
				// This is to test IE's treatment of not explicitly
				// setting a boolean content attribute,
				// since its presence should be enough
				// https://bugs.jquery.com/ticket/12359
				docElem.appendChild( el ).innerHTML = "<a id='" + expando + "'></a>" +
					"<select id='" + expando + "-\r\\' msallowcapture=''>" +
					"<option selected=''></option></select>";

				// Support: IE8, Opera 11-12.16
				// Nothing should be selected when empty strings follow ^= or $= or *=
				// The test attribute must be unknown in Opera but "safe" for WinRT
				// https://msdn.microsoft.com/en-us/library/ie/hh465388.aspx#attribute_section
				if ( el.querySelectorAll("[msallowcapture^='']").length ) {
					rbuggyQSA.push( "[*^$]=" + whitespace + "*(?:''|\"\")" );
				}

				// Support: IE8
				// Boolean attributes and "value" are not treated correctly
				if ( !el.querySelectorAll("[selected]").length ) {
					rbuggyQSA.push( "\\[" + whitespace + "*(?:value|" + booleans + ")" );
				}

				// Support: Chrome<29, Android<4.4, Safari<7.0+, iOS<7.0+, PhantomJS<1.9.8+
				if ( !el.querySelectorAll( "[id~=" + expando + "-]" ).length ) {
					rbuggyQSA.push("~=");
				}

				// Webkit/Opera - :checked should return selected option elements
				// http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked
				// IE8 throws error here and will not see later tests
				if ( !el.querySelectorAll(":checked").length ) {
					rbuggyQSA.push(":checked");
				}

				// Support: Safari 8+, iOS 8+
				// https://bugs.webkit.org/show_bug.cgi?id=136851
				// In-page `selector#id sibling-combinator selector` fails
				if ( !el.querySelectorAll( "a#" + expando + "+*" ).length ) {
					rbuggyQSA.push(".#.+[+~]");
				}
			});

			assert(function( el ) {
				el.innerHTML = "<a href='' disabled='disabled'></a>" +
					"<select disabled='disabled'><option/></select>";

				// Support: Windows 8 Native Apps
				// The type and name attributes are restricted during .innerHTML assignment
				var input = document.createElement("input");
				input.setAttribute( "type", "hidden" );
				el.appendChild( input ).setAttribute( "name", "D" );

				// Support: IE8
				// Enforce case-sensitivity of name attribute
				if ( el.querySelectorAll("[name=d]").length ) {
					rbuggyQSA.push( "name" + whitespace + "*[*^$|!~]?=" );
				}

				// FF 3.5 - :enabled/:disabled and hidden elements (hidden elements are still enabled)
				// IE8 throws error here and will not see later tests
				if ( el.querySelectorAll(":enabled").length !== 2 ) {
					rbuggyQSA.push( ":enabled", ":disabled" );
				}

				// Support: IE9-11+
				// IE's :disabled selector does not pick up the children of disabled fieldsets
				docElem.appendChild( el ).disabled = true;
				if ( el.querySelectorAll(":disabled").length !== 2 ) {
					rbuggyQSA.push( ":enabled", ":disabled" );
				}

				// Opera 10-11 does not throw on post-comma invalid pseudos
				el.querySelectorAll("*,:x");
				rbuggyQSA.push(",.*:");
			});
		}

		if ( (support.matchesSelector = rnative.test( (matches = docElem.matches ||
			docElem.webkitMatchesSelector ||
			docElem.mozMatchesSelector ||
			docElem.oMatchesSelector ||
			docElem.msMatchesSelector) )) ) {

			assert(function( el ) {
				// Check to see if it's possible to do matchesSelector
				// on a disconnected node (IE 9)
				support.disconnectedMatch = matches.call( el, "*" );

				// This should fail with an exception
				// Gecko does not error, returns false instead
				matches.call( el, "[s!='']:x" );
				rbuggyMatches.push( "!=", pseudos );
			});
		}

		rbuggyQSA = rbuggyQSA.length && new RegExp( rbuggyQSA.join("|") );
		rbuggyMatches = rbuggyMatches.length && new RegExp( rbuggyMatches.join("|") );

		/* Contains
		---------------------------------------------------------------------- */
		hasCompare = rnative.test( docElem.compareDocumentPosition );

		// Element contains another
		// Purposefully self-exclusive
		// As in, an element does not contain itself
		contains = hasCompare || rnative.test( docElem.contains ) ?
			function( a, b ) {
				var adown = a.nodeType === 9 ? a.documentElement : a,
					bup = b && b.parentNode;
				return a === bup || !!( bup && bup.nodeType === 1 && (
					adown.contains ?
						adown.contains( bup ) :
						a.compareDocumentPosition && a.compareDocumentPosition( bup ) & 16
				));
			} :
			function( a, b ) {
				if ( b ) {
					while ( (b = b.parentNode) ) {
						if ( b === a ) {
							return true;
						}
					}
				}
				return false;
			};

		/* Sorting
		---------------------------------------------------------------------- */

		// Document order sorting
		sortOrder = hasCompare ?
		function( a, b ) {

			// Flag for duplicate removal
			if ( a === b ) {
				hasDuplicate = true;
				return 0;
			}

			// Sort on method existence if only one input has compareDocumentPosition
			var compare = !a.compareDocumentPosition - !b.compareDocumentPosition;
			if ( compare ) {
				return compare;
			}

			// Calculate position if both inputs belong to the same document
			compare = ( a.ownerDocument || a ) === ( b.ownerDocument || b ) ?
				a.compareDocumentPosition( b ) :

				// Otherwise we know they are disconnected
				1;

			// Disconnected nodes
			if ( compare & 1 ||
				(!support.sortDetached && b.compareDocumentPosition( a ) === compare) ) {

				// Choose the first element that is related to our preferred document
				if ( a === document || a.ownerDocument === preferredDoc && contains(preferredDoc, a) ) {
					return -1;
				}
				if ( b === document || b.ownerDocument === preferredDoc && contains(preferredDoc, b) ) {
					return 1;
				}

				// Maintain original order
				return sortInput ?
					( indexOf( sortInput, a ) - indexOf( sortInput, b ) ) :
					0;
			}

			return compare & 4 ? -1 : 1;
		} :
		function( a, b ) {
			// Exit early if the nodes are identical
			if ( a === b ) {
				hasDuplicate = true;
				return 0;
			}

			var cur,
				i = 0,
				aup = a.parentNode,
				bup = b.parentNode,
				ap = [ a ],
				bp = [ b ];

			// Parentless nodes are either documents or disconnected
			if ( !aup || !bup ) {
				return a === document ? -1 :
					b === document ? 1 :
					aup ? -1 :
					bup ? 1 :
					sortInput ?
					( indexOf( sortInput, a ) - indexOf( sortInput, b ) ) :
					0;

			// If the nodes are siblings, we can do a quick check
			} else if ( aup === bup ) {
				return siblingCheck( a, b );
			}

			// Otherwise we need full lists of their ancestors for comparison
			cur = a;
			while ( (cur = cur.parentNode) ) {
				ap.unshift( cur );
			}
			cur = b;
			while ( (cur = cur.parentNode) ) {
				bp.unshift( cur );
			}

			// Walk down the tree looking for a discrepancy
			while ( ap[i] === bp[i] ) {
				i++;
			}

			return i ?
				// Do a sibling check if the nodes have a common ancestor
				siblingCheck( ap[i], bp[i] ) :

				// Otherwise nodes in our document sort first
				ap[i] === preferredDoc ? -1 :
				bp[i] === preferredDoc ? 1 :
				0;
		};

		return document;
	};

	Sizzle.matches = function( expr, elements ) {
		return Sizzle( expr, null, null, elements );
	};

	Sizzle.matchesSelector = function( elem, expr ) {
		// Set document vars if needed
		if ( ( elem.ownerDocument || elem ) !== document ) {
			setDocument( elem );
		}

		// Make sure that attribute selectors are quoted
		expr = expr.replace( rattributeQuotes, "='$1']" );

		if ( support.matchesSelector && documentIsHTML &&
			!compilerCache[ expr + " " ] &&
			( !rbuggyMatches || !rbuggyMatches.test( expr ) ) &&
			( !rbuggyQSA     || !rbuggyQSA.test( expr ) ) ) {

			try {
				var ret = matches.call( elem, expr );

				// IE 9's matchesSelector returns false on disconnected nodes
				if ( ret || support.disconnectedMatch ||
						// As well, disconnected nodes are said to be in a document
						// fragment in IE 9
						elem.document && elem.document.nodeType !== 11 ) {
					return ret;
				}
			} catch (e) {}
		}

		return Sizzle( expr, document, null, [ elem ] ).length > 0;
	};

	Sizzle.contains = function( context, elem ) {
		// Set document vars if needed
		if ( ( context.ownerDocument || context ) !== document ) {
			setDocument( context );
		}
		return contains( context, elem );
	};

	Sizzle.attr = function( elem, name ) {
		// Set document vars if needed
		if ( ( elem.ownerDocument || elem ) !== document ) {
			setDocument( elem );
		}

		var fn = Expr.attrHandle[ name.toLowerCase() ],
			// Don't get fooled by Object.prototype properties (jQuery #13807)
			val = fn && hasOwn.call( Expr.attrHandle, name.toLowerCase() ) ?
				fn( elem, name, !documentIsHTML ) :
				undefined;

		return val !== undefined ?
			val :
			support.attributes || !documentIsHTML ?
				elem.getAttribute( name ) :
				(val = elem.getAttributeNode(name)) && val.specified ?
					val.value :
					null;
	};

	Sizzle.escape = function( sel ) {
		return (sel + "").replace( rcssescape, fcssescape );
	};

	Sizzle.error = function( msg ) {
		throw new Error( "Syntax error, unrecognized expression: " + msg );
	};

	/**
	 * Document sorting and removing duplicates
	 * @param {ArrayLike} results
	 */
	Sizzle.uniqueSort = function( results ) {
		var elem,
			duplicates = [],
			j = 0,
			i = 0;

		// Unless we *know* we can detect duplicates, assume their presence
		hasDuplicate = !support.detectDuplicates;
		sortInput = !support.sortStable && results.slice( 0 );
		results.sort( sortOrder );

		if ( hasDuplicate ) {
			while ( (elem = results[i++]) ) {
				if ( elem === results[ i ] ) {
					j = duplicates.push( i );
				}
			}
			while ( j-- ) {
				results.splice( duplicates[ j ], 1 );
			}
		}

		// Clear input after sorting to release objects
		// See https://github.com/jquery/sizzle/pull/225
		sortInput = null;

		return results;
	};

	/**
	 * Utility function for retrieving the text value of an array of DOM nodes
	 * @param {Array|Element} elem
	 */
	getText = Sizzle.getText = function( elem ) {
		var node,
			ret = "",
			i = 0,
			nodeType = elem.nodeType;

		if ( !nodeType ) {
			// If no nodeType, this is expected to be an array
			while ( (node = elem[i++]) ) {
				// Do not traverse comment nodes
				ret += getText( node );
			}
		} else if ( nodeType === 1 || nodeType === 9 || nodeType === 11 ) {
			// Use textContent for elements
			// innerText usage removed for consistency of new lines (jQuery #11153)
			if ( typeof elem.textContent === "string" ) {
				return elem.textContent;
			} else {
				// Traverse its children
				for ( elem = elem.firstChild; elem; elem = elem.nextSibling ) {
					ret += getText( elem );
				}
			}
		} else if ( nodeType === 3 || nodeType === 4 ) {
			return elem.nodeValue;
		}
		// Do not include comment or processing instruction nodes

		return ret;
	};

	Expr = Sizzle.selectors = {

		// Can be adjusted by the user
		cacheLength: 50,

		createPseudo: markFunction,

		match: matchExpr,

		attrHandle: {},

		find: {},

		relative: {
			">": { dir: "parentNode", first: true },
			" ": { dir: "parentNode" },
			"+": { dir: "previousSibling", first: true },
			"~": { dir: "previousSibling" }
		},

		preFilter: {
			"ATTR": function( match ) {
				match[1] = match[1].replace( runescape, funescape );

				// Move the given value to match[3] whether quoted or unquoted
				match[3] = ( match[3] || match[4] || match[5] || "" ).replace( runescape, funescape );

				if ( match[2] === "~=" ) {
					match[3] = " " + match[3] + " ";
				}

				return match.slice( 0, 4 );
			},

			"CHILD": function( match ) {
				/* matches from matchExpr["CHILD"]
					1 type (only|nth|...)
					2 what (child|of-type)
					3 argument (even|odd|\d*|\d*n([+-]\d+)?|...)
					4 xn-component of xn+y argument ([+-]?\d*n|)
					5 sign of xn-component
					6 x of xn-component
					7 sign of y-component
					8 y of y-component
				*/
				match[1] = match[1].toLowerCase();

				if ( match[1].slice( 0, 3 ) === "nth" ) {
					// nth-* requires argument
					if ( !match[3] ) {
						Sizzle.error( match[0] );
					}

					// numeric x and y parameters for Expr.filter.CHILD
					// remember that false/true cast respectively to 0/1
					match[4] = +( match[4] ? match[5] + (match[6] || 1) : 2 * ( match[3] === "even" || match[3] === "odd" ) );
					match[5] = +( ( match[7] + match[8] ) || match[3] === "odd" );

				// other types prohibit arguments
				} else if ( match[3] ) {
					Sizzle.error( match[0] );
				}

				return match;
			},

			"PSEUDO": function( match ) {
				var excess,
					unquoted = !match[6] && match[2];

				if ( matchExpr["CHILD"].test( match[0] ) ) {
					return null;
				}

				// Accept quoted arguments as-is
				if ( match[3] ) {
					match[2] = match[4] || match[5] || "";

				// Strip excess characters from unquoted arguments
				} else if ( unquoted && rpseudo.test( unquoted ) &&
					// Get excess from tokenize (recursively)
					(excess = tokenize( unquoted, true )) &&
					// advance to the next closing parenthesis
					(excess = unquoted.indexOf( ")", unquoted.length - excess ) - unquoted.length) ) {

					// excess is a negative index
					match[0] = match[0].slice( 0, excess );
					match[2] = unquoted.slice( 0, excess );
				}

				// Return only captures needed by the pseudo filter method (type and argument)
				return match.slice( 0, 3 );
			}
		},

		filter: {

			"TAG": function( nodeNameSelector ) {
				var nodeName = nodeNameSelector.replace( runescape, funescape ).toLowerCase();
				return nodeNameSelector === "*" ?
					function() { return true; } :
					function( elem ) {
						return elem.nodeName && elem.nodeName.toLowerCase() === nodeName;
					};
			},

			"CLASS": function( className ) {
				var pattern = classCache[ className + " " ];

				return pattern ||
					(pattern = new RegExp( "(^|" + whitespace + ")" + className + "(" + whitespace + "|$)" )) &&
					classCache( className, function( elem ) {
						return pattern.test( typeof elem.className === "string" && elem.className || typeof elem.getAttribute !== "undefined" && elem.getAttribute("class") || "" );
					});
			},

			"ATTR": function( name, operator, check ) {
				return function( elem ) {
					var result = Sizzle.attr( elem, name );

					if ( result == null ) {
						return operator === "!=";
					}
					if ( !operator ) {
						return true;
					}

					result += "";

					return operator === "=" ? result === check :
						operator === "!=" ? result !== check :
						operator === "^=" ? check && result.indexOf( check ) === 0 :
						operator === "*=" ? check && result.indexOf( check ) > -1 :
						operator === "$=" ? check && result.slice( -check.length ) === check :
						operator === "~=" ? ( " " + result.replace( rwhitespace, " " ) + " " ).indexOf( check ) > -1 :
						operator === "|=" ? result === check || result.slice( 0, check.length + 1 ) === check + "-" :
						false;
				};
			},

			"CHILD": function( type, what, argument, first, last ) {
				var simple = type.slice( 0, 3 ) !== "nth",
					forward = type.slice( -4 ) !== "last",
					ofType = what === "of-type";

				return first === 1 && last === 0 ?

					// Shortcut for :nth-*(n)
					function( elem ) {
						return !!elem.parentNode;
					} :

					function( elem, context, xml ) {
						var cache, uniqueCache, outerCache, node, nodeIndex, start,
							dir = simple !== forward ? "nextSibling" : "previousSibling",
							parent = elem.parentNode,
							name = ofType && elem.nodeName.toLowerCase(),
							useCache = !xml && !ofType,
							diff = false;

						if ( parent ) {

							// :(first|last|only)-(child|of-type)
							if ( simple ) {
								while ( dir ) {
									node = elem;
									while ( (node = node[ dir ]) ) {
										if ( ofType ?
											node.nodeName.toLowerCase() === name :
											node.nodeType === 1 ) {

											return false;
										}
									}
									// Reverse direction for :only-* (if we haven't yet done so)
									start = dir = type === "only" && !start && "nextSibling";
								}
								return true;
							}

							start = [ forward ? parent.firstChild : parent.lastChild ];

							// non-xml :nth-child(...) stores cache data on `parent`
							if ( forward && useCache ) {

								// Seek `elem` from a previously-cached index

								// ...in a gzip-friendly way
								node = parent;
								outerCache = node[ expando ] || (node[ expando ] = {});

								// Support: IE <9 only
								// Defend against cloned attroperties (jQuery gh-1709)
								uniqueCache = outerCache[ node.uniqueID ] ||
									(outerCache[ node.uniqueID ] = {});

								cache = uniqueCache[ type ] || [];
								nodeIndex = cache[ 0 ] === dirruns && cache[ 1 ];
								diff = nodeIndex && cache[ 2 ];
								node = nodeIndex && parent.childNodes[ nodeIndex ];

								while ( (node = ++nodeIndex && node && node[ dir ] ||

									// Fallback to seeking `elem` from the start
									(diff = nodeIndex = 0) || start.pop()) ) {

									// When found, cache indexes on `parent` and break
									if ( node.nodeType === 1 && ++diff && node === elem ) {
										uniqueCache[ type ] = [ dirruns, nodeIndex, diff ];
										break;
									}
								}

							} else {
								// Use previously-cached element index if available
								if ( useCache ) {
									// ...in a gzip-friendly way
									node = elem;
									outerCache = node[ expando ] || (node[ expando ] = {});

									// Support: IE <9 only
									// Defend against cloned attroperties (jQuery gh-1709)
									uniqueCache = outerCache[ node.uniqueID ] ||
										(outerCache[ node.uniqueID ] = {});

									cache = uniqueCache[ type ] || [];
									nodeIndex = cache[ 0 ] === dirruns && cache[ 1 ];
									diff = nodeIndex;
								}

								// xml :nth-child(...)
								// or :nth-last-child(...) or :nth(-last)?-of-type(...)
								if ( diff === false ) {
									// Use the same loop as above to seek `elem` from the start
									while ( (node = ++nodeIndex && node && node[ dir ] ||
										(diff = nodeIndex = 0) || start.pop()) ) {

										if ( ( ofType ?
											node.nodeName.toLowerCase() === name :
											node.nodeType === 1 ) &&
											++diff ) {

											// Cache the index of each encountered element
											if ( useCache ) {
												outerCache = node[ expando ] || (node[ expando ] = {});

												// Support: IE <9 only
												// Defend against cloned attroperties (jQuery gh-1709)
												uniqueCache = outerCache[ node.uniqueID ] ||
													(outerCache[ node.uniqueID ] = {});

												uniqueCache[ type ] = [ dirruns, diff ];
											}

											if ( node === elem ) {
												break;
											}
										}
									}
								}
							}

							// Incorporate the offset, then check against cycle size
							diff -= last;
							return diff === first || ( diff % first === 0 && diff / first >= 0 );
						}
					};
			},

			"PSEUDO": function( pseudo, argument ) {
				// pseudo-class names are case-insensitive
				// http://www.w3.org/TR/selectors/#pseudo-classes
				// Prioritize by case sensitivity in case custom pseudos are added with uppercase letters
				// Remember that setFilters inherits from pseudos
				var args,
					fn = Expr.pseudos[ pseudo ] || Expr.setFilters[ pseudo.toLowerCase() ] ||
						Sizzle.error( "unsupported pseudo: " + pseudo );

				// The user may use createPseudo to indicate that
				// arguments are needed to create the filter function
				// just as Sizzle does
				if ( fn[ expando ] ) {
					return fn( argument );
				}

				// But maintain support for old signatures
				if ( fn.length > 1 ) {
					args = [ pseudo, pseudo, "", argument ];
					return Expr.setFilters.hasOwnProperty( pseudo.toLowerCase() ) ?
						markFunction(function( seed, matches ) {
							var idx,
								matched = fn( seed, argument ),
								i = matched.length;
							while ( i-- ) {
								idx = indexOf( seed, matched[i] );
								seed[ idx ] = !( matches[ idx ] = matched[i] );
							}
						}) :
						function( elem ) {
							return fn( elem, 0, args );
						};
				}

				return fn;
			}
		},

		pseudos: {
			// Potentially complex pseudos
			"not": markFunction(function( selector ) {
				// Trim the selector passed to compile
				// to avoid treating leading and trailing
				// spaces as combinators
				var input = [],
					results = [],
					matcher = compile( selector.replace( rtrim, "$1" ) );

				return matcher[ expando ] ?
					markFunction(function( seed, matches, context, xml ) {
						var elem,
							unmatched = matcher( seed, null, xml, [] ),
							i = seed.length;

						// Match elements unmatched by `matcher`
						while ( i-- ) {
							if ( (elem = unmatched[i]) ) {
								seed[i] = !(matches[i] = elem);
							}
						}
					}) :
					function( elem, context, xml ) {
						input[0] = elem;
						matcher( input, null, xml, results );
						// Don't keep the element (issue #299)
						input[0] = null;
						return !results.pop();
					};
			}),

			"has": markFunction(function( selector ) {
				return function( elem ) {
					return Sizzle( selector, elem ).length > 0;
				};
			}),

			"contains": markFunction(function( text ) {
				text = text.replace( runescape, funescape );
				return function( elem ) {
					return ( elem.textContent || elem.innerText || getText( elem ) ).indexOf( text ) > -1;
				};
			}),

			// "Whether an element is represented by a :lang() selector
			// is based solely on the element's language value
			// being equal to the identifier C,
			// or beginning with the identifier C immediately followed by "-".
			// The matching of C against the element's language value is performed case-insensitively.
			// The identifier C does not have to be a valid language name."
			// http://www.w3.org/TR/selectors/#lang-pseudo
			"lang": markFunction( function( lang ) {
				// lang value must be a valid identifier
				if ( !ridentifier.test(lang || "") ) {
					Sizzle.error( "unsupported lang: " + lang );
				}
				lang = lang.replace( runescape, funescape ).toLowerCase();
				return function( elem ) {
					var elemLang;
					do {
						if ( (elemLang = documentIsHTML ?
							elem.lang :
							elem.getAttribute("xml:lang") || elem.getAttribute("lang")) ) {

							elemLang = elemLang.toLowerCase();
							return elemLang === lang || elemLang.indexOf( lang + "-" ) === 0;
						}
					} while ( (elem = elem.parentNode) && elem.nodeType === 1 );
					return false;
				};
			}),

			// Miscellaneous
			"target": function( elem ) {
				var hash = window.location && window.location.hash;
				return hash && hash.slice( 1 ) === elem.id;
			},

			"root": function( elem ) {
				return elem === docElem;
			},

			"focus": function( elem ) {
				return elem === document.activeElement && (!document.hasFocus || document.hasFocus()) && !!(elem.type || elem.href || ~elem.tabIndex);
			},

			// Boolean properties
			"enabled": createDisabledPseudo( false ),
			"disabled": createDisabledPseudo( true ),

			"checked": function( elem ) {
				// In CSS3, :checked should return both checked and selected elements
				// http://www.w3.org/TR/2011/REC-css3-selectors-20110929/#checked
				var nodeName = elem.nodeName.toLowerCase();
				return (nodeName === "input" && !!elem.checked) || (nodeName === "option" && !!elem.selected);
			},

			"selected": function( elem ) {
				// Accessing this property makes selected-by-default
				// options in Safari work properly
				if ( elem.parentNode ) {
					elem.parentNode.selectedIndex;
				}

				return elem.selected === true;
			},

			// Contents
			"empty": function( elem ) {
				// http://www.w3.org/TR/selectors/#empty-pseudo
				// :empty is negated by element (1) or content nodes (text: 3; cdata: 4; entity ref: 5),
				//   but not by others (comment: 8; processing instruction: 7; etc.)
				// nodeType < 6 works because attributes (2) do not appear as children
				for ( elem = elem.firstChild; elem; elem = elem.nextSibling ) {
					if ( elem.nodeType < 6 ) {
						return false;
					}
				}
				return true;
			},

			"parent": function( elem ) {
				return !Expr.pseudos["empty"]( elem );
			},

			// Element/input types
			"header": function( elem ) {
				return rheader.test( elem.nodeName );
			},

			"input": function( elem ) {
				return rinputs.test( elem.nodeName );
			},

			"button": function( elem ) {
				var name = elem.nodeName.toLowerCase();
				return name === "input" && elem.type === "button" || name === "button";
			},

			"text": function( elem ) {
				var attr;
				return elem.nodeName.toLowerCase() === "input" &&
					elem.type === "text" &&

					// Support: IE<8
					// New HTML5 attribute values (e.g., "search") appear with elem.type === "text"
					( (attr = elem.getAttribute("type")) == null || attr.toLowerCase() === "text" );
			},

			// Position-in-collection
			"first": createPositionalPseudo(function() {
				return [ 0 ];
			}),

			"last": createPositionalPseudo(function( matchIndexes, length ) {
				return [ length - 1 ];
			}),

			"eq": createPositionalPseudo(function( matchIndexes, length, argument ) {
				return [ argument < 0 ? argument + length : argument ];
			}),

			"even": createPositionalPseudo(function( matchIndexes, length ) {
				var i = 0;
				for ( ; i < length; i += 2 ) {
					matchIndexes.push( i );
				}
				return matchIndexes;
			}),

			"odd": createPositionalPseudo(function( matchIndexes, length ) {
				var i = 1;
				for ( ; i < length; i += 2 ) {
					matchIndexes.push( i );
				}
				return matchIndexes;
			}),

			"lt": createPositionalPseudo(function( matchIndexes, length, argument ) {
				var i = argument < 0 ? argument + length : argument;
				for ( ; --i >= 0; ) {
					matchIndexes.push( i );
				}
				return matchIndexes;
			}),

			"gt": createPositionalPseudo(function( matchIndexes, length, argument ) {
				var i = argument < 0 ? argument + length : argument;
				for ( ; ++i < length; ) {
					matchIndexes.push( i );
				}
				return matchIndexes;
			})
		}
	};

	Expr.pseudos["nth"] = Expr.pseudos["eq"];

	// Add button/input type pseudos
	for ( i in { radio: true, checkbox: true, file: true, password: true, image: true } ) {
		Expr.pseudos[ i ] = createInputPseudo( i );
	}
	for ( i in { submit: true, reset: true } ) {
		Expr.pseudos[ i ] = createButtonPseudo( i );
	}

	// Easy API for creating new setFilters
	function setFilters() {}
	setFilters.prototype = Expr.filters = Expr.pseudos;
	Expr.setFilters = new setFilters();

	tokenize = Sizzle.tokenize = function( selector, parseOnly ) {
		var matched, match, tokens, type,
			soFar, groups, preFilters,
			cached = tokenCache[ selector + " " ];

		if ( cached ) {
			return parseOnly ? 0 : cached.slice( 0 );
		}

		soFar = selector;
		groups = [];
		preFilters = Expr.preFilter;

		while ( soFar ) {

			// Comma and first run
			if ( !matched || (match = rcomma.exec( soFar )) ) {
				if ( match ) {
					// Don't consume trailing commas as valid
					soFar = soFar.slice( match[0].length ) || soFar;
				}
				groups.push( (tokens = []) );
			}

			matched = false;

			// Combinators
			if ( (match = rcombinators.exec( soFar )) ) {
				matched = match.shift();
				tokens.push({
					value: matched,
					// Cast descendant combinators to space
					type: match[0].replace( rtrim, " " )
				});
				soFar = soFar.slice( matched.length );
			}

			// Filters
			for ( type in Expr.filter ) {
				if ( (match = matchExpr[ type ].exec( soFar )) && (!preFilters[ type ] ||
					(match = preFilters[ type ]( match ))) ) {
					matched = match.shift();
					tokens.push({
						value: matched,
						type: type,
						matches: match
					});
					soFar = soFar.slice( matched.length );
				}
			}

			if ( !matched ) {
				break;
			}
		}

		// Return the length of the invalid excess
		// if we're just parsing
		// Otherwise, throw an error or return tokens
		return parseOnly ?
			soFar.length :
			soFar ?
				Sizzle.error( selector ) :
				// Cache the tokens
				tokenCache( selector, groups ).slice( 0 );
	};

	function toSelector( tokens ) {
		var i = 0,
			len = tokens.length,
			selector = "";
		for ( ; i < len; i++ ) {
			selector += tokens[i].value;
		}
		return selector;
	}

	function addCombinator( matcher, combinator, base ) {
		var dir = combinator.dir,
			skip = combinator.next,
			key = skip || dir,
			checkNonElements = base && key === "parentNode",
			doneName = done++;

		return combinator.first ?
			// Check against closest ancestor/preceding element
			function( elem, context, xml ) {
				while ( (elem = elem[ dir ]) ) {
					if ( elem.nodeType === 1 || checkNonElements ) {
						return matcher( elem, context, xml );
					}
				}
				return false;
			} :

			// Check against all ancestor/preceding elements
			function( elem, context, xml ) {
				var oldCache, uniqueCache, outerCache,
					newCache = [ dirruns, doneName ];

				// We can't set arbitrary data on XML nodes, so they don't benefit from combinator caching
				if ( xml ) {
					while ( (elem = elem[ dir ]) ) {
						if ( elem.nodeType === 1 || checkNonElements ) {
							if ( matcher( elem, context, xml ) ) {
								return true;
							}
						}
					}
				} else {
					while ( (elem = elem[ dir ]) ) {
						if ( elem.nodeType === 1 || checkNonElements ) {
							outerCache = elem[ expando ] || (elem[ expando ] = {});

							// Support: IE <9 only
							// Defend against cloned attroperties (jQuery gh-1709)
							uniqueCache = outerCache[ elem.uniqueID ] || (outerCache[ elem.uniqueID ] = {});

							if ( skip && skip === elem.nodeName.toLowerCase() ) {
								elem = elem[ dir ] || elem;
							} else if ( (oldCache = uniqueCache[ key ]) &&
								oldCache[ 0 ] === dirruns && oldCache[ 1 ] === doneName ) {

								// Assign to newCache so results back-propagate to previous elements
								return (newCache[ 2 ] = oldCache[ 2 ]);
							} else {
								// Reuse newcache so results back-propagate to previous elements
								uniqueCache[ key ] = newCache;

								// A match means we're done; a fail means we have to keep checking
								if ( (newCache[ 2 ] = matcher( elem, context, xml )) ) {
									return true;
								}
							}
						}
					}
				}
				return false;
			};
	}

	function elementMatcher( matchers ) {
		return matchers.length > 1 ?
			function( elem, context, xml ) {
				var i = matchers.length;
				while ( i-- ) {
					if ( !matchers[i]( elem, context, xml ) ) {
						return false;
					}
				}
				return true;
			} :
			matchers[0];
	}

	function multipleContexts( selector, contexts, results ) {
		var i = 0,
			len = contexts.length;
		for ( ; i < len; i++ ) {
			Sizzle( selector, contexts[i], results );
		}
		return results;
	}

	function condense( unmatched, map, filter, context, xml ) {
		var elem,
			newUnmatched = [],
			i = 0,
			len = unmatched.length,
			mapped = map != null;

		for ( ; i < len; i++ ) {
			if ( (elem = unmatched[i]) ) {
				if ( !filter || filter( elem, context, xml ) ) {
					newUnmatched.push( elem );
					if ( mapped ) {
						map.push( i );
					}
				}
			}
		}

		return newUnmatched;
	}

	function setMatcher( preFilter, selector, matcher, postFilter, postFinder, postSelector ) {
		if ( postFilter && !postFilter[ expando ] ) {
			postFilter = setMatcher( postFilter );
		}
		if ( postFinder && !postFinder[ expando ] ) {
			postFinder = setMatcher( postFinder, postSelector );
		}
		return markFunction(function( seed, results, context, xml ) {
			var temp, i, elem,
				preMap = [],
				postMap = [],
				preexisting = results.length,

				// Get initial elements from seed or context
				elems = seed || multipleContexts( selector || "*", context.nodeType ? [ context ] : context, [] ),

				// Prefilter to get matcher input, preserving a map for seed-results synchronization
				matcherIn = preFilter && ( seed || !selector ) ?
					condense( elems, preMap, preFilter, context, xml ) :
					elems,

				matcherOut = matcher ?
					// If we have a postFinder, or filtered seed, or non-seed postFilter or preexisting results,
					postFinder || ( seed ? preFilter : preexisting || postFilter ) ?

						// ...intermediate processing is necessary
						[] :

						// ...otherwise use results directly
						results :
					matcherIn;

			// Find primary matches
			if ( matcher ) {
				matcher( matcherIn, matcherOut, context, xml );
			}

			// Apply postFilter
			if ( postFilter ) {
				temp = condense( matcherOut, postMap );
				postFilter( temp, [], context, xml );

				// Un-match failing elements by moving them back to matcherIn
				i = temp.length;
				while ( i-- ) {
					if ( (elem = temp[i]) ) {
						matcherOut[ postMap[i] ] = !(matcherIn[ postMap[i] ] = elem);
					}
				}
			}

			if ( seed ) {
				if ( postFinder || preFilter ) {
					if ( postFinder ) {
						// Get the final matcherOut by condensing this intermediate into postFinder contexts
						temp = [];
						i = matcherOut.length;
						while ( i-- ) {
							if ( (elem = matcherOut[i]) ) {
								// Restore matcherIn since elem is not yet a final match
								temp.push( (matcherIn[i] = elem) );
							}
						}
						postFinder( null, (matcherOut = []), temp, xml );
					}

					// Move matched elements from seed to results to keep them synchronized
					i = matcherOut.length;
					while ( i-- ) {
						if ( (elem = matcherOut[i]) &&
							(temp = postFinder ? indexOf( seed, elem ) : preMap[i]) > -1 ) {

							seed[temp] = !(results[temp] = elem);
						}
					}
				}

			// Add elements to results, through postFinder if defined
			} else {
				matcherOut = condense(
					matcherOut === results ?
						matcherOut.splice( preexisting, matcherOut.length ) :
						matcherOut
				);
				if ( postFinder ) {
					postFinder( null, results, matcherOut, xml );
				} else {
					push.apply( results, matcherOut );
				}
			}
		});
	}

	function matcherFromTokens( tokens ) {
		var checkContext, matcher, j,
			len = tokens.length,
			leadingRelative = Expr.relative[ tokens[0].type ],
			implicitRelative = leadingRelative || Expr.relative[" "],
			i = leadingRelative ? 1 : 0,

			// The foundational matcher ensures that elements are reachable from top-level context(s)
			matchContext = addCombinator( function( elem ) {
				return elem === checkContext;
			}, implicitRelative, true ),
			matchAnyContext = addCombinator( function( elem ) {
				return indexOf( checkContext, elem ) > -1;
			}, implicitRelative, true ),
			matchers = [ function( elem, context, xml ) {
				var ret = ( !leadingRelative && ( xml || context !== outermostContext ) ) || (
					(checkContext = context).nodeType ?
						matchContext( elem, context, xml ) :
						matchAnyContext( elem, context, xml ) );
				// Avoid hanging onto element (issue #299)
				checkContext = null;
				return ret;
			} ];

		for ( ; i < len; i++ ) {
			if ( (matcher = Expr.relative[ tokens[i].type ]) ) {
				matchers = [ addCombinator(elementMatcher( matchers ), matcher) ];
			} else {
				matcher = Expr.filter[ tokens[i].type ].apply( null, tokens[i].matches );

				// Return special upon seeing a positional matcher
				if ( matcher[ expando ] ) {
					// Find the next relative operator (if any) for proper handling
					j = ++i;
					for ( ; j < len; j++ ) {
						if ( Expr.relative[ tokens[j].type ] ) {
							break;
						}
					}
					return setMatcher(
						i > 1 && elementMatcher( matchers ),
						i > 1 && toSelector(
							// If the preceding token was a descendant combinator, insert an implicit any-element `*`
							tokens.slice( 0, i - 1 ).concat({ value: tokens[ i - 2 ].type === " " ? "*" : "" })
						).replace( rtrim, "$1" ),
						matcher,
						i < j && matcherFromTokens( tokens.slice( i, j ) ),
						j < len && matcherFromTokens( (tokens = tokens.slice( j )) ),
						j < len && toSelector( tokens )
					);
				}
				matchers.push( matcher );
			}
		}

		return elementMatcher( matchers );
	}

	function matcherFromGroupMatchers( elementMatchers, setMatchers ) {
		var bySet = setMatchers.length > 0,
			byElement = elementMatchers.length > 0,
			superMatcher = function( seed, context, xml, results, outermost ) {
				var elem, j, matcher,
					matchedCount = 0,
					i = "0",
					unmatched = seed && [],
					setMatched = [],
					contextBackup = outermostContext,
					// We must always have either seed elements or outermost context
					elems = seed || byElement && Expr.find["TAG"]( "*", outermost ),
					// Use integer dirruns iff this is the outermost matcher
					dirrunsUnique = (dirruns += contextBackup == null ? 1 : Math.random() || 0.1),
					len = elems.length;

				if ( outermost ) {
					outermostContext = context === document || context || outermost;
				}

				// Add elements passing elementMatchers directly to results
				// Support: IE<9, Safari
				// Tolerate NodeList properties (IE: "length"; Safari: <number>) matching elements by id
				for ( ; i !== len && (elem = elems[i]) != null; i++ ) {
					if ( byElement && elem ) {
						j = 0;
						if ( !context && elem.ownerDocument !== document ) {
							setDocument( elem );
							xml = !documentIsHTML;
						}
						while ( (matcher = elementMatchers[j++]) ) {
							if ( matcher( elem, context || document, xml) ) {
								results.push( elem );
								break;
							}
						}
						if ( outermost ) {
							dirruns = dirrunsUnique;
						}
					}

					// Track unmatched elements for set filters
					if ( bySet ) {
						// They will have gone through all possible matchers
						if ( (elem = !matcher && elem) ) {
							matchedCount--;
						}

						// Lengthen the array for every element, matched or not
						if ( seed ) {
							unmatched.push( elem );
						}
					}
				}

				// `i` is now the count of elements visited above, and adding it to `matchedCount`
				// makes the latter nonnegative.
				matchedCount += i;

				// Apply set filters to unmatched elements
				// NOTE: This can be skipped if there are no unmatched elements (i.e., `matchedCount`
				// equals `i`), unless we didn't visit _any_ elements in the above loop because we have
				// no element matchers and no seed.
				// Incrementing an initially-string "0" `i` allows `i` to remain a string only in that
				// case, which will result in a "00" `matchedCount` that differs from `i` but is also
				// numerically zero.
				if ( bySet && i !== matchedCount ) {
					j = 0;
					while ( (matcher = setMatchers[j++]) ) {
						matcher( unmatched, setMatched, context, xml );
					}

					if ( seed ) {
						// Reintegrate element matches to eliminate the need for sorting
						if ( matchedCount > 0 ) {
							while ( i-- ) {
								if ( !(unmatched[i] || setMatched[i]) ) {
									setMatched[i] = pop.call( results );
								}
							}
						}

						// Discard index placeholder values to get only actual matches
						setMatched = condense( setMatched );
					}

					// Add matches to results
					push.apply( results, setMatched );

					// Seedless set matches succeeding multiple successful matchers stipulate sorting
					if ( outermost && !seed && setMatched.length > 0 &&
						( matchedCount + setMatchers.length ) > 1 ) {

						Sizzle.uniqueSort( results );
					}
				}

				// Override manipulation of globals by nested matchers
				if ( outermost ) {
					dirruns = dirrunsUnique;
					outermostContext = contextBackup;
				}

				return unmatched;
			};

		return bySet ?
			markFunction( superMatcher ) :
			superMatcher;
	}

	compile = Sizzle.compile = function( selector, match /* Internal Use Only */ ) {
		var i,
			setMatchers = [],
			elementMatchers = [],
			cached = compilerCache[ selector + " " ];

		if ( !cached ) {
			// Generate a function of recursive functions that can be used to check each element
			if ( !match ) {
				match = tokenize( selector );
			}
			i = match.length;
			while ( i-- ) {
				cached = matcherFromTokens( match[i] );
				if ( cached[ expando ] ) {
					setMatchers.push( cached );
				} else {
					elementMatchers.push( cached );
				}
			}

			// Cache the compiled function
			cached = compilerCache( selector, matcherFromGroupMatchers( elementMatchers, setMatchers ) );

			// Save selector and tokenization
			cached.selector = selector;
		}
		return cached;
	};

	/**
	 * A low-level selection function that works with Sizzle's compiled
	 *  selector functions
	 * @param {String|Function} selector A selector or a pre-compiled
	 *  selector function built with Sizzle.compile
	 * @param {Element} context
	 * @param {Array} [results]
	 * @param {Array} [seed] A set of elements to match against
	 */
	select = Sizzle.select = function( selector, context, results, seed ) {
		var i, tokens, token, type, find,
			compiled = typeof selector === "function" && selector,
			match = !seed && tokenize( (selector = compiled.selector || selector) );

		results = results || [];

		// Try to minimize operations if there is only one selector in the list and no seed
		// (the latter of which guarantees us context)
		if ( match.length === 1 ) {

			// Reduce context if the leading compound selector is an ID
			tokens = match[0] = match[0].slice( 0 );
			if ( tokens.length > 2 && (token = tokens[0]).type === "ID" &&
					context.nodeType === 9 && documentIsHTML && Expr.relative[ tokens[1].type ] ) {

				context = ( Expr.find["ID"]( token.matches[0].replace(runescape, funescape), context ) || [] )[0];
				if ( !context ) {
					return results;

				// Precompiled matchers will still verify ancestry, so step up a level
				} else if ( compiled ) {
					context = context.parentNode;
				}

				selector = selector.slice( tokens.shift().value.length );
			}

			// Fetch a seed set for right-to-left matching
			i = matchExpr["needsContext"].test( selector ) ? 0 : tokens.length;
			while ( i-- ) {
				token = tokens[i];

				// Abort if we hit a combinator
				if ( Expr.relative[ (type = token.type) ] ) {
					break;
				}
				if ( (find = Expr.find[ type ]) ) {
					// Search, expanding context for leading sibling combinators
					if ( (seed = find(
						token.matches[0].replace( runescape, funescape ),
						rsibling.test( tokens[0].type ) && testContext( context.parentNode ) || context
					)) ) {

						// If seed is empty or no tokens remain, we can return early
						tokens.splice( i, 1 );
						selector = seed.length && toSelector( tokens );
						if ( !selector ) {
							push.apply( results, seed );
							return results;
						}

						break;
					}
				}
			}
		}

		// Compile and execute a filtering function if one is not provided
		// Provide `match` to avoid retokenization if we modified the selector above
		( compiled || compile( selector, match ) )(
			seed,
			context,
			!documentIsHTML,
			results,
			!context || rsibling.test( selector ) && testContext( context.parentNode ) || context
		);
		return results;
	};

	// One-time assignments

	// Sort stability
	support.sortStable = expando.split("").sort( sortOrder ).join("") === expando;

	// Support: Chrome 14-35+
	// Always assume duplicates if they aren't passed to the comparison function
	support.detectDuplicates = !!hasDuplicate;

	// Initialize against the default document
	setDocument();

	// Support: Webkit<537.32 - Safari 6.0.3/Chrome 25 (fixed in Chrome 27)
	// Detached nodes confoundingly follow *each other*
	support.sortDetached = assert(function( el ) {
		// Should return 1, but returns 4 (following)
		return el.compareDocumentPosition( document.createElement("fieldset") ) & 1;
	});

	// Support: IE<8
	// Prevent attribute/property "interpolation"
	// https://msdn.microsoft.com/en-us/library/ms536429%28VS.85%29.aspx
	if ( !assert(function( el ) {
		el.innerHTML = "<a href='#'></a>";
		return el.firstChild.getAttribute("href") === "#" ;
	}) ) {
		addHandle( "type|href|height|width", function( elem, name, isXML ) {
			if ( !isXML ) {
				return elem.getAttribute( name, name.toLowerCase() === "type" ? 1 : 2 );
			}
		});
	}

	// Support: IE<9
	// Use defaultValue in place of getAttribute("value")
	if ( !support.attributes || !assert(function( el ) {
		el.innerHTML = "<input/>";
		el.firstChild.setAttribute( "value", "" );
		return el.firstChild.getAttribute( "value" ) === "";
	}) ) {
		addHandle( "value", function( elem, name, isXML ) {
			if ( !isXML && elem.nodeName.toLowerCase() === "input" ) {
				return elem.defaultValue;
			}
		});
	}

	// Support: IE<9
	// Use getAttributeNode to fetch booleans when getAttribute lies
	if ( !assert(function( el ) {
		return el.getAttribute("disabled") == null;
	}) ) {
		addHandle( booleans, function( elem, name, isXML ) {
			var val;
			if ( !isXML ) {
				return elem[ name ] === true ? name.toLowerCase() :
						(val = elem.getAttributeNode( name )) && val.specified ?
						val.value :
					null;
			}
		});
	}

	return Sizzle;

	})( window );



	jQuery.find = Sizzle;
	jQuery.expr = Sizzle.selectors;

	// Deprecated
	jQuery.expr[ ":" ] = jQuery.expr.pseudos;
	jQuery.uniqueSort = jQuery.unique = Sizzle.uniqueSort;
	jQuery.text = Sizzle.getText;
	jQuery.isXMLDoc = Sizzle.isXML;
	jQuery.contains = Sizzle.contains;
	jQuery.escapeSelector = Sizzle.escape;




	var dir = function( elem, dir, until ) {
		var matched = [],
			truncate = until !== undefined;

		while ( ( elem = elem[ dir ] ) && elem.nodeType !== 9 ) {
			if ( elem.nodeType === 1 ) {
				if ( truncate && jQuery( elem ).is( until ) ) {
					break;
				}
				matched.push( elem );
			}
		}
		return matched;
	};


	var siblings = function( n, elem ) {
		var matched = [];

		for ( ; n; n = n.nextSibling ) {
			if ( n.nodeType === 1 && n !== elem ) {
				matched.push( n );
			}
		}

		return matched;
	};


	var rneedsContext = jQuery.expr.match.needsContext;



	function nodeName( elem, name ) {

	  return elem.nodeName && elem.nodeName.toLowerCase() === name.toLowerCase();

	};
	var rsingleTag = ( /^<([a-z][^\/\0>:\x20\t\r\n\f]*)[\x20\t\r\n\f]*\/?>(?:<\/\1>|)$/i );



	var risSimple = /^.[^:#\[\.,]*$/;

	// Implement the identical functionality for filter and not
	function winnow( elements, qualifier, not ) {
		if ( jQuery.isFunction( qualifier ) ) {
			return jQuery.grep( elements, function( elem, i ) {
				return !!qualifier.call( elem, i, elem ) !== not;
			} );
		}

		// Single element
		if ( qualifier.nodeType ) {
			return jQuery.grep( elements, function( elem ) {
				return ( elem === qualifier ) !== not;
			} );
		}

		// Arraylike of elements (jQuery, arguments, Array)
		if ( typeof qualifier !== "string" ) {
			return jQuery.grep( elements, function( elem ) {
				return ( indexOf.call( qualifier, elem ) > -1 ) !== not;
			} );
		}

		// Simple selector that can be filtered directly, removing non-Elements
		if ( risSimple.test( qualifier ) ) {
			return jQuery.filter( qualifier, elements, not );
		}

		// Complex selector, compare the two sets, removing non-Elements
		qualifier = jQuery.filter( qualifier, elements );
		return jQuery.grep( elements, function( elem ) {
			return ( indexOf.call( qualifier, elem ) > -1 ) !== not && elem.nodeType === 1;
		} );
	}

	jQuery.filter = function( expr, elems, not ) {
		var elem = elems[ 0 ];

		if ( not ) {
			expr = ":not(" + expr + ")";
		}

		if ( elems.length === 1 && elem.nodeType === 1 ) {
			return jQuery.find.matchesSelector( elem, expr ) ? [ elem ] : [];
		}

		return jQuery.find.matches( expr, jQuery.grep( elems, function( elem ) {
			return elem.nodeType === 1;
		} ) );
	};

	jQuery.fn.extend( {
		find: function( selector ) {
			var i, ret,
				len = this.length,
				self = this;

			if ( typeof selector !== "string" ) {
				return this.pushStack( jQuery( selector ).filter( function() {
					for ( i = 0; i < len; i++ ) {
						if ( jQuery.contains( self[ i ], this ) ) {
							return true;
						}
					}
				} ) );
			}

			ret = this.pushStack( [] );

			for ( i = 0; i < len; i++ ) {
				jQuery.find( selector, self[ i ], ret );
			}

			return len > 1 ? jQuery.uniqueSort( ret ) : ret;
		},
		filter: function( selector ) {
			return this.pushStack( winnow( this, selector || [], false ) );
		},
		not: function( selector ) {
			return this.pushStack( winnow( this, selector || [], true ) );
		},
		is: function( selector ) {
			return !!winnow(
				this,

				// If this is a positional/relative selector, check membership in the returned set
				// so $("p:first").is("p:last") won't return true for a doc with two "p".
				typeof selector === "string" && rneedsContext.test( selector ) ?
					jQuery( selector ) :
					selector || [],
				false
			).length;
		}
	} );


	// Initialize a jQuery object


	// A central reference to the root jQuery(document)
	var rootjQuery,

		// A simple way to check for HTML strings
		// Prioritize #id over <tag> to avoid XSS via location.hash (#9521)
		// Strict HTML recognition (#11290: must start with <)
		// Shortcut simple #id case for speed
		rquickExpr = /^(?:\s*(<[\w\W]+>)[^>]*|#([\w-]+))$/,

		init = jQuery.fn.init = function( selector, context, root ) {
			var match, elem;

			// HANDLE: $(""), $(null), $(undefined), $(false)
			if ( !selector ) {
				return this;
			}

			// Method init() accepts an alternate rootjQuery
			// so migrate can support jQuery.sub (gh-2101)
			root = root || rootjQuery;

			// Handle HTML strings
			if ( typeof selector === "string" ) {
				if ( selector[ 0 ] === "<" &&
					selector[ selector.length - 1 ] === ">" &&
					selector.length >= 3 ) {

					// Assume that strings that start and end with <> are HTML and skip the regex check
					match = [ null, selector, null ];

				} else {
					match = rquickExpr.exec( selector );
				}

				// Match html or make sure no context is specified for #id
				if ( match && ( match[ 1 ] || !context ) ) {

					// HANDLE: $(html) -> $(array)
					if ( match[ 1 ] ) {
						context = context instanceof jQuery ? context[ 0 ] : context;

						// Option to run scripts is true for back-compat
						// Intentionally let the error be thrown if parseHTML is not present
						jQuery.merge( this, jQuery.parseHTML(
							match[ 1 ],
							context && context.nodeType ? context.ownerDocument || context : document,
							true
						) );

						// HANDLE: $(html, props)
						if ( rsingleTag.test( match[ 1 ] ) && jQuery.isPlainObject( context ) ) {
							for ( match in context ) {

								// Properties of context are called as methods if possible
								if ( jQuery.isFunction( this[ match ] ) ) {
									this[ match ]( context[ match ] );

								// ...and otherwise set as attributes
								} else {
									this.attr( match, context[ match ] );
								}
							}
						}

						return this;

					// HANDLE: $(#id)
					} else {
						elem = document.getElementById( match[ 2 ] );

						if ( elem ) {

							// Inject the element directly into the jQuery object
							this[ 0 ] = elem;
							this.length = 1;
						}
						return this;
					}

				// HANDLE: $(expr, $(...))
				} else if ( !context || context.jquery ) {
					return ( context || root ).find( selector );

				// HANDLE: $(expr, context)
				// (which is just equivalent to: $(context).find(expr)
				} else {
					return this.constructor( context ).find( selector );
				}

			// HANDLE: $(DOMElement)
			} else if ( selector.nodeType ) {
				this[ 0 ] = selector;
				this.length = 1;
				return this;

			// HANDLE: $(function)
			// Shortcut for document ready
			} else if ( jQuery.isFunction( selector ) ) {
				return root.ready !== undefined ?
					root.ready( selector ) :

					// Execute immediately if ready is not present
					selector( jQuery );
			}

			return jQuery.makeArray( selector, this );
		};

	// Give the init function the jQuery prototype for later instantiation
	init.prototype = jQuery.fn;

	// Initialize central reference
	rootjQuery = jQuery( document );


	var rparentsprev = /^(?:parents|prev(?:Until|All))/,

		// Methods guaranteed to produce a unique set when starting from a unique set
		guaranteedUnique = {
			children: true,
			contents: true,
			next: true,
			prev: true
		};

	jQuery.fn.extend( {
		has: function( target ) {
			var targets = jQuery( target, this ),
				l = targets.length;

			return this.filter( function() {
				var i = 0;
				for ( ; i < l; i++ ) {
					if ( jQuery.contains( this, targets[ i ] ) ) {
						return true;
					}
				}
			} );
		},

		closest: function( selectors, context ) {
			var cur,
				i = 0,
				l = this.length,
				matched = [],
				targets = typeof selectors !== "string" && jQuery( selectors );

			// Positional selectors never match, since there's no _selection_ context
			if ( !rneedsContext.test( selectors ) ) {
				for ( ; i < l; i++ ) {
					for ( cur = this[ i ]; cur && cur !== context; cur = cur.parentNode ) {

						// Always skip document fragments
						if ( cur.nodeType < 11 && ( targets ?
							targets.index( cur ) > -1 :

							// Don't pass non-elements to Sizzle
							cur.nodeType === 1 &&
								jQuery.find.matchesSelector( cur, selectors ) ) ) {

							matched.push( cur );
							break;
						}
					}
				}
			}

			return this.pushStack( matched.length > 1 ? jQuery.uniqueSort( matched ) : matched );
		},

		// Determine the position of an element within the set
		index: function( elem ) {

			// No argument, return index in parent
			if ( !elem ) {
				return ( this[ 0 ] && this[ 0 ].parentNode ) ? this.first().prevAll().length : -1;
			}

			// Index in selector
			if ( typeof elem === "string" ) {
				return indexOf.call( jQuery( elem ), this[ 0 ] );
			}

			// Locate the position of the desired element
			return indexOf.call( this,

				// If it receives a jQuery object, the first element is used
				elem.jquery ? elem[ 0 ] : elem
			);
		},

		add: function( selector, context ) {
			return this.pushStack(
				jQuery.uniqueSort(
					jQuery.merge( this.get(), jQuery( selector, context ) )
				)
			);
		},

		addBack: function( selector ) {
			return this.add( selector == null ?
				this.prevObject : this.prevObject.filter( selector )
			);
		}
	} );

	function sibling( cur, dir ) {
		while ( ( cur = cur[ dir ] ) && cur.nodeType !== 1 ) {}
		return cur;
	}

	jQuery.each( {
		parent: function( elem ) {
			var parent = elem.parentNode;
			return parent && parent.nodeType !== 11 ? parent : null;
		},
		parents: function( elem ) {
			return dir( elem, "parentNode" );
		},
		parentsUntil: function( elem, i, until ) {
			return dir( elem, "parentNode", until );
		},
		next: function( elem ) {
			return sibling( elem, "nextSibling" );
		},
		prev: function( elem ) {
			return sibling( elem, "previousSibling" );
		},
		nextAll: function( elem ) {
			return dir( elem, "nextSibling" );
		},
		prevAll: function( elem ) {
			return dir( elem, "previousSibling" );
		},
		nextUntil: function( elem, i, until ) {
			return dir( elem, "nextSibling", until );
		},
		prevUntil: function( elem, i, until ) {
			return dir( elem, "previousSibling", until );
		},
		siblings: function( elem ) {
			return siblings( ( elem.parentNode || {} ).firstChild, elem );
		},
		children: function( elem ) {
			return siblings( elem.firstChild );
		},
		contents: function( elem ) {
	        if ( nodeName( elem, "iframe" ) ) {
	            return elem.contentDocument;
	        }

	        // Support: IE 9 - 11 only, iOS 7 only, Android Browser <=4.3 only
	        // Treat the template element as a regular one in browsers that
	        // don't support it.
	        if ( nodeName( elem, "template" ) ) {
	            elem = elem.content || elem;
	        }

	        return jQuery.merge( [], elem.childNodes );
		}
	}, function( name, fn ) {
		jQuery.fn[ name ] = function( until, selector ) {
			var matched = jQuery.map( this, fn, until );

			if ( name.slice( -5 ) !== "Until" ) {
				selector = until;
			}

			if ( selector && typeof selector === "string" ) {
				matched = jQuery.filter( selector, matched );
			}

			if ( this.length > 1 ) {

				// Remove duplicates
				if ( !guaranteedUnique[ name ] ) {
					jQuery.uniqueSort( matched );
				}

				// Reverse order for parents* and prev-derivatives
				if ( rparentsprev.test( name ) ) {
					matched.reverse();
				}
			}

			return this.pushStack( matched );
		};
	} );
	var rnothtmlwhite = ( /[^\x20\t\r\n\f]+/g );



	// Convert String-formatted options into Object-formatted ones
	function createOptions( options ) {
		var object = {};
		jQuery.each( options.match( rnothtmlwhite ) || [], function( _, flag ) {
			object[ flag ] = true;
		} );
		return object;
	}

	/*
	 * Create a callback list using the following parameters:
	 *
	 *	options: an optional list of space-separated options that will change how
	 *			the callback list behaves or a more traditional option object
	 *
	 * By default a callback list will act like an event callback list and can be
	 * "fired" multiple times.
	 *
	 * Possible options:
	 *
	 *	once:			will ensure the callback list can only be fired once (like a Deferred)
	 *
	 *	memory:			will keep track of previous values and will call any callback added
	 *					after the list has been fired right away with the latest "memorized"
	 *					values (like a Deferred)
	 *
	 *	unique:			will ensure a callback can only be added once (no duplicate in the list)
	 *
	 *	stopOnFalse:	interrupt callings when a callback returns false
	 *
	 */
	jQuery.Callbacks = function( options ) {

		// Convert options from String-formatted to Object-formatted if needed
		// (we check in cache first)
		options = typeof options === "string" ?
			createOptions( options ) :
			jQuery.extend( {}, options );

		var // Flag to know if list is currently firing
			firing,

			// Last fire value for non-forgettable lists
			memory,

			// Flag to know if list was already fired
			fired,

			// Flag to prevent firing
			locked,

			// Actual callback list
			list = [],

			// Queue of execution data for repeatable lists
			queue = [],

			// Index of currently firing callback (modified by add/remove as needed)
			firingIndex = -1,

			// Fire callbacks
			fire = function() {

				// Enforce single-firing
				locked = locked || options.once;

				// Execute callbacks for all pending executions,
				// respecting firingIndex overrides and runtime changes
				fired = firing = true;
				for ( ; queue.length; firingIndex = -1 ) {
					memory = queue.shift();
					while ( ++firingIndex < list.length ) {

						// Run callback and check for early termination
						if ( list[ firingIndex ].apply( memory[ 0 ], memory[ 1 ] ) === false &&
							options.stopOnFalse ) {

							// Jump to end and forget the data so .add doesn't re-fire
							firingIndex = list.length;
							memory = false;
						}
					}
				}

				// Forget the data if we're done with it
				if ( !options.memory ) {
					memory = false;
				}

				firing = false;

				// Clean up if we're done firing for good
				if ( locked ) {

					// Keep an empty list if we have data for future add calls
					if ( memory ) {
						list = [];

					// Otherwise, this object is spent
					} else {
						list = "";
					}
				}
			},

			// Actual Callbacks object
			self = {

				// Add a callback or a collection of callbacks to the list
				add: function() {
					if ( list ) {

						// If we have memory from a past run, we should fire after adding
						if ( memory && !firing ) {
							firingIndex = list.length - 1;
							queue.push( memory );
						}

						( function add( args ) {
							jQuery.each( args, function( _, arg ) {
								if ( jQuery.isFunction( arg ) ) {
									if ( !options.unique || !self.has( arg ) ) {
										list.push( arg );
									}
								} else if ( arg && arg.length && jQuery.type( arg ) !== "string" ) {

									// Inspect recursively
									add( arg );
								}
							} );
						} )( arguments );

						if ( memory && !firing ) {
							fire();
						}
					}
					return this;
				},

				// Remove a callback from the list
				remove: function() {
					jQuery.each( arguments, function( _, arg ) {
						var index;
						while ( ( index = jQuery.inArray( arg, list, index ) ) > -1 ) {
							list.splice( index, 1 );

							// Handle firing indexes
							if ( index <= firingIndex ) {
								firingIndex--;
							}
						}
					} );
					return this;
				},

				// Check if a given callback is in the list.
				// If no argument is given, return whether or not list has callbacks attached.
				has: function( fn ) {
					return fn ?
						jQuery.inArray( fn, list ) > -1 :
						list.length > 0;
				},

				// Remove all callbacks from the list
				empty: function() {
					if ( list ) {
						list = [];
					}
					return this;
				},

				// Disable .fire and .add
				// Abort any current/pending executions
				// Clear all callbacks and values
				disable: function() {
					locked = queue = [];
					list = memory = "";
					return this;
				},
				disabled: function() {
					return !list;
				},

				// Disable .fire
				// Also disable .add unless we have memory (since it would have no effect)
				// Abort any pending executions
				lock: function() {
					locked = queue = [];
					if ( !memory && !firing ) {
						list = memory = "";
					}
					return this;
				},
				locked: function() {
					return !!locked;
				},

				// Call all callbacks with the given context and arguments
				fireWith: function( context, args ) {
					if ( !locked ) {
						args = args || [];
						args = [ context, args.slice ? args.slice() : args ];
						queue.push( args );
						if ( !firing ) {
							fire();
						}
					}
					return this;
				},

				// Call all the callbacks with the given arguments
				fire: function() {
					self.fireWith( this, arguments );
					return this;
				},

				// To know if the callbacks have already been called at least once
				fired: function() {
					return !!fired;
				}
			};

		return self;
	};


	function Identity( v ) {
		return v;
	}
	function Thrower( ex ) {
		throw ex;
	}

	function adoptValue( value, resolve, reject, noValue ) {
		var method;

		try {

			// Check for promise aspect first to privilege synchronous behavior
			if ( value && jQuery.isFunction( ( method = value.promise ) ) ) {
				method.call( value ).done( resolve ).fail( reject );

			// Other thenables
			} else if ( value && jQuery.isFunction( ( method = value.then ) ) ) {
				method.call( value, resolve, reject );

			// Other non-thenables
			} else {

				// Control `resolve` arguments by letting Array#slice cast boolean `noValue` to integer:
				// * false: [ value ].slice( 0 ) => resolve( value )
				// * true: [ value ].slice( 1 ) => resolve()
				resolve.apply( undefined, [ value ].slice( noValue ) );
			}

		// For Promises/A+, convert exceptions into rejections
		// Since jQuery.when doesn't unwrap thenables, we can skip the extra checks appearing in
		// Deferred#then to conditionally suppress rejection.
		} catch ( value ) {

			// Support: Android 4.0 only
			// Strict mode functions invoked without .call/.apply get global-object context
			reject.apply( undefined, [ value ] );
		}
	}

	jQuery.extend( {

		Deferred: function( func ) {
			var tuples = [

					// action, add listener, callbacks,
					// ... .then handlers, argument index, [final state]
					[ "notify", "progress", jQuery.Callbacks( "memory" ),
						jQuery.Callbacks( "memory" ), 2 ],
					[ "resolve", "done", jQuery.Callbacks( "once memory" ),
						jQuery.Callbacks( "once memory" ), 0, "resolved" ],
					[ "reject", "fail", jQuery.Callbacks( "once memory" ),
						jQuery.Callbacks( "once memory" ), 1, "rejected" ]
				],
				state = "pending",
				promise = {
					state: function() {
						return state;
					},
					always: function() {
						deferred.done( arguments ).fail( arguments );
						return this;
					},
					"catch": function( fn ) {
						return promise.then( null, fn );
					},

					// Keep pipe for back-compat
					pipe: function( /* fnDone, fnFail, fnProgress */ ) {
						var fns = arguments;

						return jQuery.Deferred( function( newDefer ) {
							jQuery.each( tuples, function( i, tuple ) {

								// Map tuples (progress, done, fail) to arguments (done, fail, progress)
								var fn = jQuery.isFunction( fns[ tuple[ 4 ] ] ) && fns[ tuple[ 4 ] ];

								// deferred.progress(function() { bind to newDefer or newDefer.notify })
								// deferred.done(function() { bind to newDefer or newDefer.resolve })
								// deferred.fail(function() { bind to newDefer or newDefer.reject })
								deferred[ tuple[ 1 ] ]( function() {
									var returned = fn && fn.apply( this, arguments );
									if ( returned && jQuery.isFunction( returned.promise ) ) {
										returned.promise()
											.progress( newDefer.notify )
											.done( newDefer.resolve )
											.fail( newDefer.reject );
									} else {
										newDefer[ tuple[ 0 ] + "With" ](
											this,
											fn ? [ returned ] : arguments
										);
									}
								} );
							} );
							fns = null;
						} ).promise();
					},
					then: function( onFulfilled, onRejected, onProgress ) {
						var maxDepth = 0;
						function resolve( depth, deferred, handler, special ) {
							return function() {
								var that = this,
									args = arguments,
									mightThrow = function() {
										var returned, then;

										// Support: Promises/A+ section 2.3.3.3.3
										// https://promisesaplus.com/#point-59
										// Ignore double-resolution attempts
										if ( depth < maxDepth ) {
											return;
										}

										returned = handler.apply( that, args );

										// Support: Promises/A+ section 2.3.1
										// https://promisesaplus.com/#point-48
										if ( returned === deferred.promise() ) {
											throw new TypeError( "Thenable self-resolution" );
										}

										// Support: Promises/A+ sections 2.3.3.1, 3.5
										// https://promisesaplus.com/#point-54
										// https://promisesaplus.com/#point-75
										// Retrieve `then` only once
										then = returned &&

											// Support: Promises/A+ section 2.3.4
											// https://promisesaplus.com/#point-64
											// Only check objects and functions for thenability
											( typeof returned === "object" ||
												typeof returned === "function" ) &&
											returned.then;

										// Handle a returned thenable
										if ( jQuery.isFunction( then ) ) {

											// Special processors (notify) just wait for resolution
											if ( special ) {
												then.call(
													returned,
													resolve( maxDepth, deferred, Identity, special ),
													resolve( maxDepth, deferred, Thrower, special )
												);

											// Normal processors (resolve) also hook into progress
											} else {

												// ...and disregard older resolution values
												maxDepth++;

												then.call(
													returned,
													resolve( maxDepth, deferred, Identity, special ),
													resolve( maxDepth, deferred, Thrower, special ),
													resolve( maxDepth, deferred, Identity,
														deferred.notifyWith )
												);
											}

										// Handle all other returned values
										} else {

											// Only substitute handlers pass on context
											// and multiple values (non-spec behavior)
											if ( handler !== Identity ) {
												that = undefined;
												args = [ returned ];
											}

											// Process the value(s)
											// Default process is resolve
											( special || deferred.resolveWith )( that, args );
										}
									},

									// Only normal processors (resolve) catch and reject exceptions
									process = special ?
										mightThrow :
										function() {
											try {
												mightThrow();
											} catch ( e ) {

												if ( jQuery.Deferred.exceptionHook ) {
													jQuery.Deferred.exceptionHook( e,
														process.stackTrace );
												}

												// Support: Promises/A+ section 2.3.3.3.4.1
												// https://promisesaplus.com/#point-61
												// Ignore post-resolution exceptions
												if ( depth + 1 >= maxDepth ) {

													// Only substitute handlers pass on context
													// and multiple values (non-spec behavior)
													if ( handler !== Thrower ) {
														that = undefined;
														args = [ e ];
													}

													deferred.rejectWith( that, args );
												}
											}
										};

								// Support: Promises/A+ section 2.3.3.3.1
								// https://promisesaplus.com/#point-57
								// Re-resolve promises immediately to dodge false rejection from
								// subsequent errors
								if ( depth ) {
									process();
								} else {

									// Call an optional hook to record the stack, in case of exception
									// since it's otherwise lost when execution goes async
									if ( jQuery.Deferred.getStackHook ) {
										process.stackTrace = jQuery.Deferred.getStackHook();
									}
									window.setTimeout( process );
								}
							};
						}

						return jQuery.Deferred( function( newDefer ) {

							// progress_handlers.add( ... )
							tuples[ 0 ][ 3 ].add(
								resolve(
									0,
									newDefer,
									jQuery.isFunction( onProgress ) ?
										onProgress :
										Identity,
									newDefer.notifyWith
								)
							);

							// fulfilled_handlers.add( ... )
							tuples[ 1 ][ 3 ].add(
								resolve(
									0,
									newDefer,
									jQuery.isFunction( onFulfilled ) ?
										onFulfilled :
										Identity
								)
							);

							// rejected_handlers.add( ... )
							tuples[ 2 ][ 3 ].add(
								resolve(
									0,
									newDefer,
									jQuery.isFunction( onRejected ) ?
										onRejected :
										Thrower
								)
							);
						} ).promise();
					},

					// Get a promise for this deferred
					// If obj is provided, the promise aspect is added to the object
					promise: function( obj ) {
						return obj != null ? jQuery.extend( obj, promise ) : promise;
					}
				},
				deferred = {};

			// Add list-specific methods
			jQuery.each( tuples, function( i, tuple ) {
				var list = tuple[ 2 ],
					stateString = tuple[ 5 ];

				// promise.progress = list.add
				// promise.done = list.add
				// promise.fail = list.add
				promise[ tuple[ 1 ] ] = list.add;

				// Handle state
				if ( stateString ) {
					list.add(
						function() {

							// state = "resolved" (i.e., fulfilled)
							// state = "rejected"
							state = stateString;
						},

						// rejected_callbacks.disable
						// fulfilled_callbacks.disable
						tuples[ 3 - i ][ 2 ].disable,

						// progress_callbacks.lock
						tuples[ 0 ][ 2 ].lock
					);
				}

				// progress_handlers.fire
				// fulfilled_handlers.fire
				// rejected_handlers.fire
				list.add( tuple[ 3 ].fire );

				// deferred.notify = function() { deferred.notifyWith(...) }
				// deferred.resolve = function() { deferred.resolveWith(...) }
				// deferred.reject = function() { deferred.rejectWith(...) }
				deferred[ tuple[ 0 ] ] = function() {
					deferred[ tuple[ 0 ] + "With" ]( this === deferred ? undefined : this, arguments );
					return this;
				};

				// deferred.notifyWith = list.fireWith
				// deferred.resolveWith = list.fireWith
				// deferred.rejectWith = list.fireWith
				deferred[ tuple[ 0 ] + "With" ] = list.fireWith;
			} );

			// Make the deferred a promise
			promise.promise( deferred );

			// Call given func if any
			if ( func ) {
				func.call( deferred, deferred );
			}

			// All done!
			return deferred;
		},

		// Deferred helper
		when: function( singleValue ) {
			var

				// count of uncompleted subordinates
				remaining = arguments.length,

				// count of unprocessed arguments
				i = remaining,

				// subordinate fulfillment data
				resolveContexts = Array( i ),
				resolveValues = slice.call( arguments ),

				// the master Deferred
				master = jQuery.Deferred(),

				// subordinate callback factory
				updateFunc = function( i ) {
					return function( value ) {
						resolveContexts[ i ] = this;
						resolveValues[ i ] = arguments.length > 1 ? slice.call( arguments ) : value;
						if ( !( --remaining ) ) {
							master.resolveWith( resolveContexts, resolveValues );
						}
					};
				};

			// Single- and empty arguments are adopted like Promise.resolve
			if ( remaining <= 1 ) {
				adoptValue( singleValue, master.done( updateFunc( i ) ).resolve, master.reject,
					!remaining );

				// Use .then() to unwrap secondary thenables (cf. gh-3000)
				if ( master.state() === "pending" ||
					jQuery.isFunction( resolveValues[ i ] && resolveValues[ i ].then ) ) {

					return master.then();
				}
			}

			// Multiple arguments are aggregated like Promise.all array elements
			while ( i-- ) {
				adoptValue( resolveValues[ i ], updateFunc( i ), master.reject );
			}

			return master.promise();
		}
	} );


	// These usually indicate a programmer mistake during development,
	// warn about them ASAP rather than swallowing them by default.
	var rerrorNames = /^(Eval|Internal|Range|Reference|Syntax|Type|URI)Error$/;

	jQuery.Deferred.exceptionHook = function( error, stack ) {

		// Support: IE 8 - 9 only
		// Console exists when dev tools are open, which can happen at any time
		if ( window.console && window.console.warn && error && rerrorNames.test( error.name ) ) {
			window.console.warn( "jQuery.Deferred exception: " + error.message, error.stack, stack );
		}
	};




	jQuery.readyException = function( error ) {
		window.setTimeout( function() {
			throw error;
		} );
	};




	// The deferred used on DOM ready
	var readyList = jQuery.Deferred();

	jQuery.fn.ready = function( fn ) {

		readyList
			.then( fn )

			// Wrap jQuery.readyException in a function so that the lookup
			// happens at the time of error handling instead of callback
			// registration.
			.catch( function( error ) {
				jQuery.readyException( error );
			} );

		return this;
	};

	jQuery.extend( {

		// Is the DOM ready to be used? Set to true once it occurs.
		isReady: false,

		// A counter to track how many items to wait for before
		// the ready event fires. See #6781
		readyWait: 1,

		// Handle when the DOM is ready
		ready: function( wait ) {

			// Abort if there are pending holds or we're already ready
			if ( wait === true ? --jQuery.readyWait : jQuery.isReady ) {
				return;
			}

			// Remember that the DOM is ready
			jQuery.isReady = true;

			// If a normal DOM Ready event fired, decrement, and wait if need be
			if ( wait !== true && --jQuery.readyWait > 0 ) {
				return;
			}

			// If there are functions bound, to execute
			readyList.resolveWith( document, [ jQuery ] );
		}
	} );

	jQuery.ready.then = readyList.then;

	// The ready event handler and self cleanup method
	function completed() {
		document.removeEventListener( "DOMContentLoaded", completed );
		window.removeEventListener( "load", completed );
		jQuery.ready();
	}

	// Catch cases where $(document).ready() is called
	// after the browser event has already occurred.
	// Support: IE <=9 - 10 only
	// Older IE sometimes signals "interactive" too soon
	if ( document.readyState === "complete" ||
		( document.readyState !== "loading" && !document.documentElement.doScroll ) ) {

		// Handle it asynchronously to allow scripts the opportunity to delay ready
		window.setTimeout( jQuery.ready );

	} else {

		// Use the handy event callback
		document.addEventListener( "DOMContentLoaded", completed );

		// A fallback to window.onload, that will always work
		window.addEventListener( "load", completed );
	}




	// Multifunctional method to get and set values of a collection
	// The value/s can optionally be executed if it's a function
	var access = function( elems, fn, key, value, chainable, emptyGet, raw ) {
		var i = 0,
			len = elems.length,
			bulk = key == null;

		// Sets many values
		if ( jQuery.type( key ) === "object" ) {
			chainable = true;
			for ( i in key ) {
				access( elems, fn, i, key[ i ], true, emptyGet, raw );
			}

		// Sets one value
		} else if ( value !== undefined ) {
			chainable = true;

			if ( !jQuery.isFunction( value ) ) {
				raw = true;
			}

			if ( bulk ) {

				// Bulk operations run against the entire set
				if ( raw ) {
					fn.call( elems, value );
					fn = null;

				// ...except when executing function values
				} else {
					bulk = fn;
					fn = function( elem, key, value ) {
						return bulk.call( jQuery( elem ), value );
					};
				}
			}

			if ( fn ) {
				for ( ; i < len; i++ ) {
					fn(
						elems[ i ], key, raw ?
						value :
						value.call( elems[ i ], i, fn( elems[ i ], key ) )
					);
				}
			}
		}

		if ( chainable ) {
			return elems;
		}

		// Gets
		if ( bulk ) {
			return fn.call( elems );
		}

		return len ? fn( elems[ 0 ], key ) : emptyGet;
	};
	var acceptData = function( owner ) {

		// Accepts only:
		//  - Node
		//    - Node.ELEMENT_NODE
		//    - Node.DOCUMENT_NODE
		//  - Object
		//    - Any
		return owner.nodeType === 1 || owner.nodeType === 9 || !( +owner.nodeType );
	};




	function Data() {
		this.expando = jQuery.expando + Data.uid++;
	}

	Data.uid = 1;

	Data.prototype = {

		cache: function( owner ) {

			// Check if the owner object already has a cache
			var value = owner[ this.expando ];

			// If not, create one
			if ( !value ) {
				value = {};

				// We can accept data for non-element nodes in modern browsers,
				// but we should not, see #8335.
				// Always return an empty object.
				if ( acceptData( owner ) ) {

					// If it is a node unlikely to be stringify-ed or looped over
					// use plain assignment
					if ( owner.nodeType ) {
						owner[ this.expando ] = value;

					// Otherwise secure it in a non-enumerable property
					// configurable must be true to allow the property to be
					// deleted when data is removed
					} else {
						Object.defineProperty( owner, this.expando, {
							value: value,
							configurable: true
						} );
					}
				}
			}

			return value;
		},
		set: function( owner, data, value ) {
			var prop,
				cache = this.cache( owner );

			// Handle: [ owner, key, value ] args
			// Always use camelCase key (gh-2257)
			if ( typeof data === "string" ) {
				cache[ jQuery.camelCase( data ) ] = value;

			// Handle: [ owner, { properties } ] args
			} else {

				// Copy the properties one-by-one to the cache object
				for ( prop in data ) {
					cache[ jQuery.camelCase( prop ) ] = data[ prop ];
				}
			}
			return cache;
		},
		get: function( owner, key ) {
			return key === undefined ?
				this.cache( owner ) :

				// Always use camelCase key (gh-2257)
				owner[ this.expando ] && owner[ this.expando ][ jQuery.camelCase( key ) ];
		},
		access: function( owner, key, value ) {

			// In cases where either:
			//
			//   1. No key was specified
			//   2. A string key was specified, but no value provided
			//
			// Take the "read" path and allow the get method to determine
			// which value to return, respectively either:
			//
			//   1. The entire cache object
			//   2. The data stored at the key
			//
			if ( key === undefined ||
					( ( key && typeof key === "string" ) && value === undefined ) ) {

				return this.get( owner, key );
			}

			// When the key is not a string, or both a key and value
			// are specified, set or extend (existing objects) with either:
			//
			//   1. An object of properties
			//   2. A key and value
			//
			this.set( owner, key, value );

			// Since the "set" path can have two possible entry points
			// return the expected data based on which path was taken[*]
			return value !== undefined ? value : key;
		},
		remove: function( owner, key ) {
			var i,
				cache = owner[ this.expando ];

			if ( cache === undefined ) {
				return;
			}

			if ( key !== undefined ) {

				// Support array or space separated string of keys
				if ( Array.isArray( key ) ) {

					// If key is an array of keys...
					// We always set camelCase keys, so remove that.
					key = key.map( jQuery.camelCase );
				} else {
					key = jQuery.camelCase( key );

					// If a key with the spaces exists, use it.
					// Otherwise, create an array by matching non-whitespace
					key = key in cache ?
						[ key ] :
						( key.match( rnothtmlwhite ) || [] );
				}

				i = key.length;

				while ( i-- ) {
					delete cache[ key[ i ] ];
				}
			}

			// Remove the expando if there's no more data
			if ( key === undefined || jQuery.isEmptyObject( cache ) ) {

				// Support: Chrome <=35 - 45
				// Webkit & Blink performance suffers when deleting properties
				// from DOM nodes, so set to undefined instead
				// https://bugs.chromium.org/p/chromium/issues/detail?id=378607 (bug restricted)
				if ( owner.nodeType ) {
					owner[ this.expando ] = undefined;
				} else {
					delete owner[ this.expando ];
				}
			}
		},
		hasData: function( owner ) {
			var cache = owner[ this.expando ];
			return cache !== undefined && !jQuery.isEmptyObject( cache );
		}
	};
	var dataPriv = new Data();

	var dataUser = new Data();



	//	Implementation Summary
	//
	//	1. Enforce API surface and semantic compatibility with 1.9.x branch
	//	2. Improve the module's maintainability by reducing the storage
	//		paths to a single mechanism.
	//	3. Use the same single mechanism to support "private" and "user" data.
	//	4. _Never_ expose "private" data to user code (TODO: Drop _data, _removeData)
	//	5. Avoid exposing implementation details on user objects (eg. expando properties)
	//	6. Provide a clear path for implementation upgrade to WeakMap in 2014

	var rbrace = /^(?:\{[\w\W]*\}|\[[\w\W]*\])$/,
		rmultiDash = /[A-Z]/g;

	function getData( data ) {
		if ( data === "true" ) {
			return true;
		}

		if ( data === "false" ) {
			return false;
		}

		if ( data === "null" ) {
			return null;
		}

		// Only convert to a number if it doesn't change the string
		if ( data === +data + "" ) {
			return +data;
		}

		if ( rbrace.test( data ) ) {
			return JSON.parse( data );
		}

		return data;
	}

	function dataAttr( elem, key, data ) {
		var name;

		// If nothing was found internally, try to fetch any
		// data from the HTML5 data-* attribute
		if ( data === undefined && elem.nodeType === 1 ) {
			name = "data-" + key.replace( rmultiDash, "-$&" ).toLowerCase();
			data = elem.getAttribute( name );

			if ( typeof data === "string" ) {
				try {
					data = getData( data );
				} catch ( e ) {}

				// Make sure we set the data so it isn't changed later
				dataUser.set( elem, key, data );
			} else {
				data = undefined;
			}
		}
		return data;
	}

	jQuery.extend( {
		hasData: function( elem ) {
			return dataUser.hasData( elem ) || dataPriv.hasData( elem );
		},

		data: function( elem, name, data ) {
			return dataUser.access( elem, name, data );
		},

		removeData: function( elem, name ) {
			dataUser.remove( elem, name );
		},

		// TODO: Now that all calls to _data and _removeData have been replaced
		// with direct calls to dataPriv methods, these can be deprecated.
		_data: function( elem, name, data ) {
			return dataPriv.access( elem, name, data );
		},

		_removeData: function( elem, name ) {
			dataPriv.remove( elem, name );
		}
	} );

	jQuery.fn.extend( {
		data: function( key, value ) {
			var i, name, data,
				elem = this[ 0 ],
				attrs = elem && elem.attributes;

			// Gets all values
			if ( key === undefined ) {
				if ( this.length ) {
					data = dataUser.get( elem );

					if ( elem.nodeType === 1 && !dataPriv.get( elem, "hasDataAttrs" ) ) {
						i = attrs.length;
						while ( i-- ) {

							// Support: IE 11 only
							// The attrs elements can be null (#14894)
							if ( attrs[ i ] ) {
								name = attrs[ i ].name;
								if ( name.indexOf( "data-" ) === 0 ) {
									name = jQuery.camelCase( name.slice( 5 ) );
									dataAttr( elem, name, data[ name ] );
								}
							}
						}
						dataPriv.set( elem, "hasDataAttrs", true );
					}
				}

				return data;
			}

			// Sets multiple values
			if ( typeof key === "object" ) {
				return this.each( function() {
					dataUser.set( this, key );
				} );
			}

			return access( this, function( value ) {
				var data;

				// The calling jQuery object (element matches) is not empty
				// (and therefore has an element appears at this[ 0 ]) and the
				// `value` parameter was not undefined. An empty jQuery object
				// will result in `undefined` for elem = this[ 0 ] which will
				// throw an exception if an attempt to read a data cache is made.
				if ( elem && value === undefined ) {

					// Attempt to get data from the cache
					// The key will always be camelCased in Data
					data = dataUser.get( elem, key );
					if ( data !== undefined ) {
						return data;
					}

					// Attempt to "discover" the data in
					// HTML5 custom data-* attrs
					data = dataAttr( elem, key );
					if ( data !== undefined ) {
						return data;
					}

					// We tried really hard, but the data doesn't exist.
					return;
				}

				// Set the data...
				this.each( function() {

					// We always store the camelCased key
					dataUser.set( this, key, value );
				} );
			}, null, value, arguments.length > 1, null, true );
		},

		removeData: function( key ) {
			return this.each( function() {
				dataUser.remove( this, key );
			} );
		}
	} );


	jQuery.extend( {
		queue: function( elem, type, data ) {
			var queue;

			if ( elem ) {
				type = ( type || "fx" ) + "queue";
				queue = dataPriv.get( elem, type );

				// Speed up dequeue by getting out quickly if this is just a lookup
				if ( data ) {
					if ( !queue || Array.isArray( data ) ) {
						queue = dataPriv.access( elem, type, jQuery.makeArray( data ) );
					} else {
						queue.push( data );
					}
				}
				return queue || [];
			}
		},

		dequeue: function( elem, type ) {
			type = type || "fx";

			var queue = jQuery.queue( elem, type ),
				startLength = queue.length,
				fn = queue.shift(),
				hooks = jQuery._queueHooks( elem, type ),
				next = function() {
					jQuery.dequeue( elem, type );
				};

			// If the fx queue is dequeued, always remove the progress sentinel
			if ( fn === "inprogress" ) {
				fn = queue.shift();
				startLength--;
			}

			if ( fn ) {

				// Add a progress sentinel to prevent the fx queue from being
				// automatically dequeued
				if ( type === "fx" ) {
					queue.unshift( "inprogress" );
				}

				// Clear up the last queue stop function
				delete hooks.stop;
				fn.call( elem, next, hooks );
			}

			if ( !startLength && hooks ) {
				hooks.empty.fire();
			}
		},

		// Not public - generate a queueHooks object, or return the current one
		_queueHooks: function( elem, type ) {
			var key = type + "queueHooks";
			return dataPriv.get( elem, key ) || dataPriv.access( elem, key, {
				empty: jQuery.Callbacks( "once memory" ).add( function() {
					dataPriv.remove( elem, [ type + "queue", key ] );
				} )
			} );
		}
	} );

	jQuery.fn.extend( {
		queue: function( type, data ) {
			var setter = 2;

			if ( typeof type !== "string" ) {
				data = type;
				type = "fx";
				setter--;
			}

			if ( arguments.length < setter ) {
				return jQuery.queue( this[ 0 ], type );
			}

			return data === undefined ?
				this :
				this.each( function() {
					var queue = jQuery.queue( this, type, data );

					// Ensure a hooks for this queue
					jQuery._queueHooks( this, type );

					if ( type === "fx" && queue[ 0 ] !== "inprogress" ) {
						jQuery.dequeue( this, type );
					}
				} );
		},
		dequeue: function( type ) {
			return this.each( function() {
				jQuery.dequeue( this, type );
			} );
		},
		clearQueue: function( type ) {
			return this.queue( type || "fx", [] );
		},

		// Get a promise resolved when queues of a certain type
		// are emptied (fx is the type by default)
		promise: function( type, obj ) {
			var tmp,
				count = 1,
				defer = jQuery.Deferred(),
				elements = this,
				i = this.length,
				resolve = function() {
					if ( !( --count ) ) {
						defer.resolveWith( elements, [ elements ] );
					}
				};

			if ( typeof type !== "string" ) {
				obj = type;
				type = undefined;
			}
			type = type || "fx";

			while ( i-- ) {
				tmp = dataPriv.get( elements[ i ], type + "queueHooks" );
				if ( tmp && tmp.empty ) {
					count++;
					tmp.empty.add( resolve );
				}
			}
			resolve();
			return defer.promise( obj );
		}
	} );
	var pnum = ( /[+-]?(?:\d*\.|)\d+(?:[eE][+-]?\d+|)/ ).source;

	var rcssNum = new RegExp( "^(?:([+-])=|)(" + pnum + ")([a-z%]*)$", "i" );


	var cssExpand = [ "Top", "Right", "Bottom", "Left" ];

	var isHiddenWithinTree = function( elem, el ) {

			// isHiddenWithinTree might be called from jQuery#filter function;
			// in that case, element will be second argument
			elem = el || elem;

			// Inline style trumps all
			return elem.style.display === "none" ||
				elem.style.display === "" &&

				// Otherwise, check computed style
				// Support: Firefox <=43 - 45
				// Disconnected elements can have computed display: none, so first confirm that elem is
				// in the document.
				jQuery.contains( elem.ownerDocument, elem ) &&

				jQuery.css( elem, "display" ) === "none";
		};

	var swap = function( elem, options, callback, args ) {
		var ret, name,
			old = {};

		// Remember the old values, and insert the new ones
		for ( name in options ) {
			old[ name ] = elem.style[ name ];
			elem.style[ name ] = options[ name ];
		}

		ret = callback.apply( elem, args || [] );

		// Revert the old values
		for ( name in options ) {
			elem.style[ name ] = old[ name ];
		}

		return ret;
	};




	function adjustCSS( elem, prop, valueParts, tween ) {
		var adjusted,
			scale = 1,
			maxIterations = 20,
			currentValue = tween ?
				function() {
					return tween.cur();
				} :
				function() {
					return jQuery.css( elem, prop, "" );
				},
			initial = currentValue(),
			unit = valueParts && valueParts[ 3 ] || ( jQuery.cssNumber[ prop ] ? "" : "px" ),

			// Starting value computation is required for potential unit mismatches
			initialInUnit = ( jQuery.cssNumber[ prop ] || unit !== "px" && +initial ) &&
				rcssNum.exec( jQuery.css( elem, prop ) );

		if ( initialInUnit && initialInUnit[ 3 ] !== unit ) {

			// Trust units reported by jQuery.css
			unit = unit || initialInUnit[ 3 ];

			// Make sure we update the tween properties later on
			valueParts = valueParts || [];

			// Iteratively approximate from a nonzero starting point
			initialInUnit = +initial || 1;

			do {

				// If previous iteration zeroed out, double until we get *something*.
				// Use string for doubling so we don't accidentally see scale as unchanged below
				scale = scale || ".5";

				// Adjust and apply
				initialInUnit = initialInUnit / scale;
				jQuery.style( elem, prop, initialInUnit + unit );

			// Update scale, tolerating zero or NaN from tween.cur()
			// Break the loop if scale is unchanged or perfect, or if we've just had enough.
			} while (
				scale !== ( scale = currentValue() / initial ) && scale !== 1 && --maxIterations
			);
		}

		if ( valueParts ) {
			initialInUnit = +initialInUnit || +initial || 0;

			// Apply relative offset (+=/-=) if specified
			adjusted = valueParts[ 1 ] ?
				initialInUnit + ( valueParts[ 1 ] + 1 ) * valueParts[ 2 ] :
				+valueParts[ 2 ];
			if ( tween ) {
				tween.unit = unit;
				tween.start = initialInUnit;
				tween.end = adjusted;
			}
		}
		return adjusted;
	}


	var defaultDisplayMap = {};

	function getDefaultDisplay( elem ) {
		var temp,
			doc = elem.ownerDocument,
			nodeName = elem.nodeName,
			display = defaultDisplayMap[ nodeName ];

		if ( display ) {
			return display;
		}

		temp = doc.body.appendChild( doc.createElement( nodeName ) );
		display = jQuery.css( temp, "display" );

		temp.parentNode.removeChild( temp );

		if ( display === "none" ) {
			display = "block";
		}
		defaultDisplayMap[ nodeName ] = display;

		return display;
	}

	function showHide( elements, show ) {
		var display, elem,
			values = [],
			index = 0,
			length = elements.length;

		// Determine new display value for elements that need to change
		for ( ; index < length; index++ ) {
			elem = elements[ index ];
			if ( !elem.style ) {
				continue;
			}

			display = elem.style.display;
			if ( show ) {

				// Since we force visibility upon cascade-hidden elements, an immediate (and slow)
				// check is required in this first loop unless we have a nonempty display value (either
				// inline or about-to-be-restored)
				if ( display === "none" ) {
					values[ index ] = dataPriv.get( elem, "display" ) || null;
					if ( !values[ index ] ) {
						elem.style.display = "";
					}
				}
				if ( elem.style.display === "" && isHiddenWithinTree( elem ) ) {
					values[ index ] = getDefaultDisplay( elem );
				}
			} else {
				if ( display !== "none" ) {
					values[ index ] = "none";

					// Remember what we're overwriting
					dataPriv.set( elem, "display", display );
				}
			}
		}

		// Set the display of the elements in a second loop to avoid constant reflow
		for ( index = 0; index < length; index++ ) {
			if ( values[ index ] != null ) {
				elements[ index ].style.display = values[ index ];
			}
		}

		return elements;
	}

	jQuery.fn.extend( {
		show: function() {
			return showHide( this, true );
		},
		hide: function() {
			return showHide( this );
		},
		toggle: function( state ) {
			if ( typeof state === "boolean" ) {
				return state ? this.show() : this.hide();
			}

			return this.each( function() {
				if ( isHiddenWithinTree( this ) ) {
					jQuery( this ).show();
				} else {
					jQuery( this ).hide();
				}
			} );
		}
	} );
	var rcheckableType = ( /^(?:checkbox|radio)$/i );

	var rtagName = ( /<([a-z][^\/\0>\x20\t\r\n\f]+)/i );

	var rscriptType = ( /^$|\/(?:java|ecma)script/i );



	// We have to close these tags to support XHTML (#13200)
	var wrapMap = {

		// Support: IE <=9 only
		option: [ 1, "<select multiple='multiple'>", "</select>" ],

		// XHTML parsers do not magically insert elements in the
		// same way that tag soup parsers do. So we cannot shorten
		// this by omitting <tbody> or other required elements.
		thead: [ 1, "<table>", "</table>" ],
		col: [ 2, "<table><colgroup>", "</colgroup></table>" ],
		tr: [ 2, "<table><tbody>", "</tbody></table>" ],
		td: [ 3, "<table><tbody><tr>", "</tr></tbody></table>" ],

		_default: [ 0, "", "" ]
	};

	// Support: IE <=9 only
	wrapMap.optgroup = wrapMap.option;

	wrapMap.tbody = wrapMap.tfoot = wrapMap.colgroup = wrapMap.caption = wrapMap.thead;
	wrapMap.th = wrapMap.td;


	function getAll( context, tag ) {

		// Support: IE <=9 - 11 only
		// Use typeof to avoid zero-argument method invocation on host objects (#15151)
		var ret;

		if ( typeof context.getElementsByTagName !== "undefined" ) {
			ret = context.getElementsByTagName( tag || "*" );

		} else if ( typeof context.querySelectorAll !== "undefined" ) {
			ret = context.querySelectorAll( tag || "*" );

		} else {
			ret = [];
		}

		if ( tag === undefined || tag && nodeName( context, tag ) ) {
			return jQuery.merge( [ context ], ret );
		}

		return ret;
	}


	// Mark scripts as having already been evaluated
	function setGlobalEval( elems, refElements ) {
		var i = 0,
			l = elems.length;

		for ( ; i < l; i++ ) {
			dataPriv.set(
				elems[ i ],
				"globalEval",
				!refElements || dataPriv.get( refElements[ i ], "globalEval" )
			);
		}
	}


	var rhtml = /<|&#?\w+;/;

	function buildFragment( elems, context, scripts, selection, ignored ) {
		var elem, tmp, tag, wrap, contains, j,
			fragment = context.createDocumentFragment(),
			nodes = [],
			i = 0,
			l = elems.length;

		for ( ; i < l; i++ ) {
			elem = elems[ i ];

			if ( elem || elem === 0 ) {

				// Add nodes directly
				if ( jQuery.type( elem ) === "object" ) {

					// Support: Android <=4.0 only, PhantomJS 1 only
					// push.apply(_, arraylike) throws on ancient WebKit
					jQuery.merge( nodes, elem.nodeType ? [ elem ] : elem );

				// Convert non-html into a text node
				} else if ( !rhtml.test( elem ) ) {
					nodes.push( context.createTextNode( elem ) );

				// Convert html into DOM nodes
				} else {
					tmp = tmp || fragment.appendChild( context.createElement( "div" ) );

					// Deserialize a standard representation
					tag = ( rtagName.exec( elem ) || [ "", "" ] )[ 1 ].toLowerCase();
					wrap = wrapMap[ tag ] || wrapMap._default;
					tmp.innerHTML = wrap[ 1 ] + jQuery.htmlPrefilter( elem ) + wrap[ 2 ];

					// Descend through wrappers to the right content
					j = wrap[ 0 ];
					while ( j-- ) {
						tmp = tmp.lastChild;
					}

					// Support: Android <=4.0 only, PhantomJS 1 only
					// push.apply(_, arraylike) throws on ancient WebKit
					jQuery.merge( nodes, tmp.childNodes );

					// Remember the top-level container
					tmp = fragment.firstChild;

					// Ensure the created nodes are orphaned (#12392)
					tmp.textContent = "";
				}
			}
		}

		// Remove wrapper from fragment
		fragment.textContent = "";

		i = 0;
		while ( ( elem = nodes[ i++ ] ) ) {

			// Skip elements already in the context collection (trac-4087)
			if ( selection && jQuery.inArray( elem, selection ) > -1 ) {
				if ( ignored ) {
					ignored.push( elem );
				}
				continue;
			}

			contains = jQuery.contains( elem.ownerDocument, elem );

			// Append to fragment
			tmp = getAll( fragment.appendChild( elem ), "script" );

			// Preserve script evaluation history
			if ( contains ) {
				setGlobalEval( tmp );
			}

			// Capture executables
			if ( scripts ) {
				j = 0;
				while ( ( elem = tmp[ j++ ] ) ) {
					if ( rscriptType.test( elem.type || "" ) ) {
						scripts.push( elem );
					}
				}
			}
		}

		return fragment;
	}


	( function() {
		var fragment = document.createDocumentFragment(),
			div = fragment.appendChild( document.createElement( "div" ) ),
			input = document.createElement( "input" );

		// Support: Android 4.0 - 4.3 only
		// Check state lost if the name is set (#11217)
		// Support: Windows Web Apps (WWA)
		// `name` and `type` must use .setAttribute for WWA (#14901)
		input.setAttribute( "type", "radio" );
		input.setAttribute( "checked", "checked" );
		input.setAttribute( "name", "t" );

		div.appendChild( input );

		// Support: Android <=4.1 only
		// Older WebKit doesn't clone checked state correctly in fragments
		support.checkClone = div.cloneNode( true ).cloneNode( true ).lastChild.checked;

		// Support: IE <=11 only
		// Make sure textarea (and checkbox) defaultValue is properly cloned
		div.innerHTML = "<textarea>x</textarea>";
		support.noCloneChecked = !!div.cloneNode( true ).lastChild.defaultValue;
	} )();
	var documentElement = document.documentElement;



	var
		rkeyEvent = /^key/,
		rmouseEvent = /^(?:mouse|pointer|contextmenu|drag|drop)|click/,
		rtypenamespace = /^([^.]*)(?:\.(.+)|)/;

	function returnTrue() {
		return true;
	}

	function returnFalse() {
		return false;
	}

	// Support: IE <=9 only
	// See #13393 for more info
	function safeActiveElement() {
		try {
			return document.activeElement;
		} catch ( err ) { }
	}

	function on( elem, types, selector, data, fn, one ) {
		var origFn, type;

		// Types can be a map of types/handlers
		if ( typeof types === "object" ) {

			// ( types-Object, selector, data )
			if ( typeof selector !== "string" ) {

				// ( types-Object, data )
				data = data || selector;
				selector = undefined;
			}
			for ( type in types ) {
				on( elem, type, selector, data, types[ type ], one );
			}
			return elem;
		}

		if ( data == null && fn == null ) {

			// ( types, fn )
			fn = selector;
			data = selector = undefined;
		} else if ( fn == null ) {
			if ( typeof selector === "string" ) {

				// ( types, selector, fn )
				fn = data;
				data = undefined;
			} else {

				// ( types, data, fn )
				fn = data;
				data = selector;
				selector = undefined;
			}
		}
		if ( fn === false ) {
			fn = returnFalse;
		} else if ( !fn ) {
			return elem;
		}

		if ( one === 1 ) {
			origFn = fn;
			fn = function( event ) {

				// Can use an empty set, since event contains the info
				jQuery().off( event );
				return origFn.apply( this, arguments );
			};

			// Use same guid so caller can remove using origFn
			fn.guid = origFn.guid || ( origFn.guid = jQuery.guid++ );
		}
		return elem.each( function() {
			jQuery.event.add( this, types, fn, data, selector );
		} );
	}

	/*
	 * Helper functions for managing events -- not part of the public interface.
	 * Props to Dean Edwards' addEvent library for many of the ideas.
	 */
	jQuery.event = {

		global: {},

		add: function( elem, types, handler, data, selector ) {

			var handleObjIn, eventHandle, tmp,
				events, t, handleObj,
				special, handlers, type, namespaces, origType,
				elemData = dataPriv.get( elem );

			// Don't attach events to noData or text/comment nodes (but allow plain objects)
			if ( !elemData ) {
				return;
			}

			// Caller can pass in an object of custom data in lieu of the handler
			if ( handler.handler ) {
				handleObjIn = handler;
				handler = handleObjIn.handler;
				selector = handleObjIn.selector;
			}

			// Ensure that invalid selectors throw exceptions at attach time
			// Evaluate against documentElement in case elem is a non-element node (e.g., document)
			if ( selector ) {
				jQuery.find.matchesSelector( documentElement, selector );
			}

			// Make sure that the handler has a unique ID, used to find/remove it later
			if ( !handler.guid ) {
				handler.guid = jQuery.guid++;
			}

			// Init the element's event structure and main handler, if this is the first
			if ( !( events = elemData.events ) ) {
				events = elemData.events = {};
			}
			if ( !( eventHandle = elemData.handle ) ) {
				eventHandle = elemData.handle = function( e ) {

					// Discard the second event of a jQuery.event.trigger() and
					// when an event is called after a page has unloaded
					return typeof jQuery !== "undefined" && jQuery.event.triggered !== e.type ?
						jQuery.event.dispatch.apply( elem, arguments ) : undefined;
				};
			}

			// Handle multiple events separated by a space
			types = ( types || "" ).match( rnothtmlwhite ) || [ "" ];
			t = types.length;
			while ( t-- ) {
				tmp = rtypenamespace.exec( types[ t ] ) || [];
				type = origType = tmp[ 1 ];
				namespaces = ( tmp[ 2 ] || "" ).split( "." ).sort();

				// There *must* be a type, no attaching namespace-only handlers
				if ( !type ) {
					continue;
				}

				// If event changes its type, use the special event handlers for the changed type
				special = jQuery.event.special[ type ] || {};

				// If selector defined, determine special event api type, otherwise given type
				type = ( selector ? special.delegateType : special.bindType ) || type;

				// Update special based on newly reset type
				special = jQuery.event.special[ type ] || {};

				// handleObj is passed to all event handlers
				handleObj = jQuery.extend( {
					type: type,
					origType: origType,
					data: data,
					handler: handler,
					guid: handler.guid,
					selector: selector,
					needsContext: selector && jQuery.expr.match.needsContext.test( selector ),
					namespace: namespaces.join( "." )
				}, handleObjIn );

				// Init the event handler queue if we're the first
				if ( !( handlers = events[ type ] ) ) {
					handlers = events[ type ] = [];
					handlers.delegateCount = 0;

					// Only use addEventListener if the special events handler returns false
					if ( !special.setup ||
						special.setup.call( elem, data, namespaces, eventHandle ) === false ) {

						if ( elem.addEventListener ) {
							elem.addEventListener( type, eventHandle );
						}
					}
				}

				if ( special.add ) {
					special.add.call( elem, handleObj );

					if ( !handleObj.handler.guid ) {
						handleObj.handler.guid = handler.guid;
					}
				}

				// Add to the element's handler list, delegates in front
				if ( selector ) {
					handlers.splice( handlers.delegateCount++, 0, handleObj );
				} else {
					handlers.push( handleObj );
				}

				// Keep track of which events have ever been used, for event optimization
				jQuery.event.global[ type ] = true;
			}

		},

		// Detach an event or set of events from an element
		remove: function( elem, types, handler, selector, mappedTypes ) {

			var j, origCount, tmp,
				events, t, handleObj,
				special, handlers, type, namespaces, origType,
				elemData = dataPriv.hasData( elem ) && dataPriv.get( elem );

			if ( !elemData || !( events = elemData.events ) ) {
				return;
			}

			// Once for each type.namespace in types; type may be omitted
			types = ( types || "" ).match( rnothtmlwhite ) || [ "" ];
			t = types.length;
			while ( t-- ) {
				tmp = rtypenamespace.exec( types[ t ] ) || [];
				type = origType = tmp[ 1 ];
				namespaces = ( tmp[ 2 ] || "" ).split( "." ).sort();

				// Unbind all events (on this namespace, if provided) for the element
				if ( !type ) {
					for ( type in events ) {
						jQuery.event.remove( elem, type + types[ t ], handler, selector, true );
					}
					continue;
				}

				special = jQuery.event.special[ type ] || {};
				type = ( selector ? special.delegateType : special.bindType ) || type;
				handlers = events[ type ] || [];
				tmp = tmp[ 2 ] &&
					new RegExp( "(^|\\.)" + namespaces.join( "\\.(?:.*\\.|)" ) + "(\\.|$)" );

				// Remove matching events
				origCount = j = handlers.length;
				while ( j-- ) {
					handleObj = handlers[ j ];

					if ( ( mappedTypes || origType === handleObj.origType ) &&
						( !handler || handler.guid === handleObj.guid ) &&
						( !tmp || tmp.test( handleObj.namespace ) ) &&
						( !selector || selector === handleObj.selector ||
							selector === "**" && handleObj.selector ) ) {
						handlers.splice( j, 1 );

						if ( handleObj.selector ) {
							handlers.delegateCount--;
						}
						if ( special.remove ) {
							special.remove.call( elem, handleObj );
						}
					}
				}

				// Remove generic event handler if we removed something and no more handlers exist
				// (avoids potential for endless recursion during removal of special event handlers)
				if ( origCount && !handlers.length ) {
					if ( !special.teardown ||
						special.teardown.call( elem, namespaces, elemData.handle ) === false ) {

						jQuery.removeEvent( elem, type, elemData.handle );
					}

					delete events[ type ];
				}
			}

			// Remove data and the expando if it's no longer used
			if ( jQuery.isEmptyObject( events ) ) {
				dataPriv.remove( elem, "handle events" );
			}
		},

		dispatch: function( nativeEvent ) {

			// Make a writable jQuery.Event from the native event object
			var event = jQuery.event.fix( nativeEvent );

			var i, j, ret, matched, handleObj, handlerQueue,
				args = new Array( arguments.length ),
				handlers = ( dataPriv.get( this, "events" ) || {} )[ event.type ] || [],
				special = jQuery.event.special[ event.type ] || {};

			// Use the fix-ed jQuery.Event rather than the (read-only) native event
			args[ 0 ] = event;

			for ( i = 1; i < arguments.length; i++ ) {
				args[ i ] = arguments[ i ];
			}

			event.delegateTarget = this;

			// Call the preDispatch hook for the mapped type, and let it bail if desired
			if ( special.preDispatch && special.preDispatch.call( this, event ) === false ) {
				return;
			}

			// Determine handlers
			handlerQueue = jQuery.event.handlers.call( this, event, handlers );

			// Run delegates first; they may want to stop propagation beneath us
			i = 0;
			while ( ( matched = handlerQueue[ i++ ] ) && !event.isPropagationStopped() ) {
				event.currentTarget = matched.elem;

				j = 0;
				while ( ( handleObj = matched.handlers[ j++ ] ) &&
					!event.isImmediatePropagationStopped() ) {

					// Triggered event must either 1) have no namespace, or 2) have namespace(s)
					// a subset or equal to those in the bound event (both can have no namespace).
					if ( !event.rnamespace || event.rnamespace.test( handleObj.namespace ) ) {

						event.handleObj = handleObj;
						event.data = handleObj.data;

						ret = ( ( jQuery.event.special[ handleObj.origType ] || {} ).handle ||
							handleObj.handler ).apply( matched.elem, args );

						if ( ret !== undefined ) {
							if ( ( event.result = ret ) === false ) {
								event.preventDefault();
								event.stopPropagation();
							}
						}
					}
				}
			}

			// Call the postDispatch hook for the mapped type
			if ( special.postDispatch ) {
				special.postDispatch.call( this, event );
			}

			return event.result;
		},

		handlers: function( event, handlers ) {
			var i, handleObj, sel, matchedHandlers, matchedSelectors,
				handlerQueue = [],
				delegateCount = handlers.delegateCount,
				cur = event.target;

			// Find delegate handlers
			if ( delegateCount &&

				// Support: IE <=9
				// Black-hole SVG <use> instance trees (trac-13180)
				cur.nodeType &&

				// Support: Firefox <=42
				// Suppress spec-violating clicks indicating a non-primary pointer button (trac-3861)
				// https://www.w3.org/TR/DOM-Level-3-Events/#event-type-click
				// Support: IE 11 only
				// ...but not arrow key "clicks" of radio inputs, which can have `button` -1 (gh-2343)
				!( event.type === "click" && event.button >= 1 ) ) {

				for ( ; cur !== this; cur = cur.parentNode || this ) {

					// Don't check non-elements (#13208)
					// Don't process clicks on disabled elements (#6911, #8165, #11382, #11764)
					if ( cur.nodeType === 1 && !( event.type === "click" && cur.disabled === true ) ) {
						matchedHandlers = [];
						matchedSelectors = {};
						for ( i = 0; i < delegateCount; i++ ) {
							handleObj = handlers[ i ];

							// Don't conflict with Object.prototype properties (#13203)
							sel = handleObj.selector + " ";

							if ( matchedSelectors[ sel ] === undefined ) {
								matchedSelectors[ sel ] = handleObj.needsContext ?
									jQuery( sel, this ).index( cur ) > -1 :
									jQuery.find( sel, this, null, [ cur ] ).length;
							}
							if ( matchedSelectors[ sel ] ) {
								matchedHandlers.push( handleObj );
							}
						}
						if ( matchedHandlers.length ) {
							handlerQueue.push( { elem: cur, handlers: matchedHandlers } );
						}
					}
				}
			}

			// Add the remaining (directly-bound) handlers
			cur = this;
			if ( delegateCount < handlers.length ) {
				handlerQueue.push( { elem: cur, handlers: handlers.slice( delegateCount ) } );
			}

			return handlerQueue;
		},

		addProp: function( name, hook ) {
			Object.defineProperty( jQuery.Event.prototype, name, {
				enumerable: true,
				configurable: true,

				get: jQuery.isFunction( hook ) ?
					function() {
						if ( this.originalEvent ) {
								return hook( this.originalEvent );
						}
					} :
					function() {
						if ( this.originalEvent ) {
								return this.originalEvent[ name ];
						}
					},

				set: function( value ) {
					Object.defineProperty( this, name, {
						enumerable: true,
						configurable: true,
						writable: true,
						value: value
					} );
				}
			} );
		},

		fix: function( originalEvent ) {
			return originalEvent[ jQuery.expando ] ?
				originalEvent :
				new jQuery.Event( originalEvent );
		},

		special: {
			load: {

				// Prevent triggered image.load events from bubbling to window.load
				noBubble: true
			},
			focus: {

				// Fire native event if possible so blur/focus sequence is correct
				trigger: function() {
					if ( this !== safeActiveElement() && this.focus ) {
						this.focus();
						return false;
					}
				},
				delegateType: "focusin"
			},
			blur: {
				trigger: function() {
					if ( this === safeActiveElement() && this.blur ) {
						this.blur();
						return false;
					}
				},
				delegateType: "focusout"
			},
			click: {

				// For checkbox, fire native event so checked state will be right
				trigger: function() {
					if ( this.type === "checkbox" && this.click && nodeName( this, "input" ) ) {
						this.click();
						return false;
					}
				},

				// For cross-browser consistency, don't fire native .click() on links
				_default: function( event ) {
					return nodeName( event.target, "a" );
				}
			},

			beforeunload: {
				postDispatch: function( event ) {

					// Support: Firefox 20+
					// Firefox doesn't alert if the returnValue field is not set.
					if ( event.result !== undefined && event.originalEvent ) {
						event.originalEvent.returnValue = event.result;
					}
				}
			}
		}
	};

	jQuery.removeEvent = function( elem, type, handle ) {

		// This "if" is needed for plain objects
		if ( elem.removeEventListener ) {
			elem.removeEventListener( type, handle );
		}
	};

	jQuery.Event = function( src, props ) {

		// Allow instantiation without the 'new' keyword
		if ( !( this instanceof jQuery.Event ) ) {
			return new jQuery.Event( src, props );
		}

		// Event object
		if ( src && src.type ) {
			this.originalEvent = src;
			this.type = src.type;

			// Events bubbling up the document may have been marked as prevented
			// by a handler lower down the tree; reflect the correct value.
			this.isDefaultPrevented = src.defaultPrevented ||
					src.defaultPrevented === undefined &&

					// Support: Android <=2.3 only
					src.returnValue === false ?
				returnTrue :
				returnFalse;

			// Create target properties
			// Support: Safari <=6 - 7 only
			// Target should not be a text node (#504, #13143)
			this.target = ( src.target && src.target.nodeType === 3 ) ?
				src.target.parentNode :
				src.target;

			this.currentTarget = src.currentTarget;
			this.relatedTarget = src.relatedTarget;

		// Event type
		} else {
			this.type = src;
		}

		// Put explicitly provided properties onto the event object
		if ( props ) {
			jQuery.extend( this, props );
		}

		// Create a timestamp if incoming event doesn't have one
		this.timeStamp = src && src.timeStamp || jQuery.now();

		// Mark it as fixed
		this[ jQuery.expando ] = true;
	};

	// jQuery.Event is based on DOM3 Events as specified by the ECMAScript Language Binding
	// https://www.w3.org/TR/2003/WD-DOM-Level-3-Events-20030331/ecma-script-binding.html
	jQuery.Event.prototype = {
		constructor: jQuery.Event,
		isDefaultPrevented: returnFalse,
		isPropagationStopped: returnFalse,
		isImmediatePropagationStopped: returnFalse,
		isSimulated: false,

		preventDefault: function() {
			var e = this.originalEvent;

			this.isDefaultPrevented = returnTrue;

			if ( e && !this.isSimulated ) {
				e.preventDefault();
			}
		},
		stopPropagation: function() {
			var e = this.originalEvent;

			this.isPropagationStopped = returnTrue;

			if ( e && !this.isSimulated ) {
				e.stopPropagation();
			}
		},
		stopImmediatePropagation: function() {
			var e = this.originalEvent;

			this.isImmediatePropagationStopped = returnTrue;

			if ( e && !this.isSimulated ) {
				e.stopImmediatePropagation();
			}

			this.stopPropagation();
		}
	};

	// Includes all common event props including KeyEvent and MouseEvent specific props
	jQuery.each( {
		altKey: true,
		bubbles: true,
		cancelable: true,
		changedTouches: true,
		ctrlKey: true,
		detail: true,
		eventPhase: true,
		metaKey: true,
		pageX: true,
		pageY: true,
		shiftKey: true,
		view: true,
		"char": true,
		charCode: true,
		key: true,
		keyCode: true,
		button: true,
		buttons: true,
		clientX: true,
		clientY: true,
		offsetX: true,
		offsetY: true,
		pointerId: true,
		pointerType: true,
		screenX: true,
		screenY: true,
		targetTouches: true,
		toElement: true,
		touches: true,

		which: function( event ) {
			var button = event.button;

			// Add which for key events
			if ( event.which == null && rkeyEvent.test( event.type ) ) {
				return event.charCode != null ? event.charCode : event.keyCode;
			}

			// Add which for click: 1 === left; 2 === middle; 3 === right
			if ( !event.which && button !== undefined && rmouseEvent.test( event.type ) ) {
				if ( button & 1 ) {
					return 1;
				}

				if ( button & 2 ) {
					return 3;
				}

				if ( button & 4 ) {
					return 2;
				}

				return 0;
			}

			return event.which;
		}
	}, jQuery.event.addProp );

	// Create mouseenter/leave events using mouseover/out and event-time checks
	// so that event delegation works in jQuery.
	// Do the same for pointerenter/pointerleave and pointerover/pointerout
	//
	// Support: Safari 7 only
	// Safari sends mouseenter too often; see:
	// https://bugs.chromium.org/p/chromium/issues/detail?id=470258
	// for the description of the bug (it existed in older Chrome versions as well).
	jQuery.each( {
		mouseenter: "mouseover",
		mouseleave: "mouseout",
		pointerenter: "pointerover",
		pointerleave: "pointerout"
	}, function( orig, fix ) {
		jQuery.event.special[ orig ] = {
			delegateType: fix,
			bindType: fix,

			handle: function( event ) {
				var ret,
					target = this,
					related = event.relatedTarget,
					handleObj = event.handleObj;

				// For mouseenter/leave call the handler if related is outside the target.
				// NB: No relatedTarget if the mouse left/entered the browser window
				if ( !related || ( related !== target && !jQuery.contains( target, related ) ) ) {
					event.type = handleObj.origType;
					ret = handleObj.handler.apply( this, arguments );
					event.type = fix;
				}
				return ret;
			}
		};
	} );

	jQuery.fn.extend( {

		on: function( types, selector, data, fn ) {
			return on( this, types, selector, data, fn );
		},
		one: function( types, selector, data, fn ) {
			return on( this, types, selector, data, fn, 1 );
		},
		off: function( types, selector, fn ) {
			var handleObj, type;
			if ( types && types.preventDefault && types.handleObj ) {

				// ( event )  dispatched jQuery.Event
				handleObj = types.handleObj;
				jQuery( types.delegateTarget ).off(
					handleObj.namespace ?
						handleObj.origType + "." + handleObj.namespace :
						handleObj.origType,
					handleObj.selector,
					handleObj.handler
				);
				return this;
			}
			if ( typeof types === "object" ) {

				// ( types-object [, selector] )
				for ( type in types ) {
					this.off( type, selector, types[ type ] );
				}
				return this;
			}
			if ( selector === false || typeof selector === "function" ) {

				// ( types [, fn] )
				fn = selector;
				selector = undefined;
			}
			if ( fn === false ) {
				fn = returnFalse;
			}
			return this.each( function() {
				jQuery.event.remove( this, types, fn, selector );
			} );
		}
	} );


	var

		/* eslint-disable max-len */

		// See https://github.com/eslint/eslint/issues/3229
		rxhtmlTag = /<(?!area|br|col|embed|hr|img|input|link|meta|param)(([a-z][^\/\0>\x20\t\r\n\f]*)[^>]*)\/>/gi,

		/* eslint-enable */

		// Support: IE <=10 - 11, Edge 12 - 13
		// In IE/Edge using regex groups here causes severe slowdowns.
		// See https://connect.microsoft.com/IE/feedback/details/1736512/
		rnoInnerhtml = /<script|<style|<link/i,

		// checked="checked" or checked
		rchecked = /checked\s*(?:[^=]|=\s*.checked.)/i,
		rscriptTypeMasked = /^true\/(.*)/,
		rcleanScript = /^\s*<!(?:\[CDATA\[|--)|(?:\]\]|--)>\s*$/g;

	// Prefer a tbody over its parent table for containing new rows
	function manipulationTarget( elem, content ) {
		if ( nodeName( elem, "table" ) &&
			nodeName( content.nodeType !== 11 ? content : content.firstChild, "tr" ) ) {

			return jQuery( ">tbody", elem )[ 0 ] || elem;
		}

		return elem;
	}

	// Replace/restore the type attribute of script elements for safe DOM manipulation
	function disableScript( elem ) {
		elem.type = ( elem.getAttribute( "type" ) !== null ) + "/" + elem.type;
		return elem;
	}
	function restoreScript( elem ) {
		var match = rscriptTypeMasked.exec( elem.type );

		if ( match ) {
			elem.type = match[ 1 ];
		} else {
			elem.removeAttribute( "type" );
		}

		return elem;
	}

	function cloneCopyEvent( src, dest ) {
		var i, l, type, pdataOld, pdataCur, udataOld, udataCur, events;

		if ( dest.nodeType !== 1 ) {
			return;
		}

		// 1. Copy private data: events, handlers, etc.
		if ( dataPriv.hasData( src ) ) {
			pdataOld = dataPriv.access( src );
			pdataCur = dataPriv.set( dest, pdataOld );
			events = pdataOld.events;

			if ( events ) {
				delete pdataCur.handle;
				pdataCur.events = {};

				for ( type in events ) {
					for ( i = 0, l = events[ type ].length; i < l; i++ ) {
						jQuery.event.add( dest, type, events[ type ][ i ] );
					}
				}
			}
		}

		// 2. Copy user data
		if ( dataUser.hasData( src ) ) {
			udataOld = dataUser.access( src );
			udataCur = jQuery.extend( {}, udataOld );

			dataUser.set( dest, udataCur );
		}
	}

	// Fix IE bugs, see support tests
	function fixInput( src, dest ) {
		var nodeName = dest.nodeName.toLowerCase();

		// Fails to persist the checked state of a cloned checkbox or radio button.
		if ( nodeName === "input" && rcheckableType.test( src.type ) ) {
			dest.checked = src.checked;

		// Fails to return the selected option to the default selected state when cloning options
		} else if ( nodeName === "input" || nodeName === "textarea" ) {
			dest.defaultValue = src.defaultValue;
		}
	}

	function domManip( collection, args, callback, ignored ) {

		// Flatten any nested arrays
		args = concat.apply( [], args );

		var fragment, first, scripts, hasScripts, node, doc,
			i = 0,
			l = collection.length,
			iNoClone = l - 1,
			value = args[ 0 ],
			isFunction = jQuery.isFunction( value );

		// We can't cloneNode fragments that contain checked, in WebKit
		if ( isFunction ||
				( l > 1 && typeof value === "string" &&
					!support.checkClone && rchecked.test( value ) ) ) {
			return collection.each( function( index ) {
				var self = collection.eq( index );
				if ( isFunction ) {
					args[ 0 ] = value.call( this, index, self.html() );
				}
				domManip( self, args, callback, ignored );
			} );
		}

		if ( l ) {
			fragment = buildFragment( args, collection[ 0 ].ownerDocument, false, collection, ignored );
			first = fragment.firstChild;

			if ( fragment.childNodes.length === 1 ) {
				fragment = first;
			}

			// Require either new content or an interest in ignored elements to invoke the callback
			if ( first || ignored ) {
				scripts = jQuery.map( getAll( fragment, "script" ), disableScript );
				hasScripts = scripts.length;

				// Use the original fragment for the last item
				// instead of the first because it can end up
				// being emptied incorrectly in certain situations (#8070).
				for ( ; i < l; i++ ) {
					node = fragment;

					if ( i !== iNoClone ) {
						node = jQuery.clone( node, true, true );

						// Keep references to cloned scripts for later restoration
						if ( hasScripts ) {

							// Support: Android <=4.0 only, PhantomJS 1 only
							// push.apply(_, arraylike) throws on ancient WebKit
							jQuery.merge( scripts, getAll( node, "script" ) );
						}
					}

					callback.call( collection[ i ], node, i );
				}

				if ( hasScripts ) {
					doc = scripts[ scripts.length - 1 ].ownerDocument;

					// Reenable scripts
					jQuery.map( scripts, restoreScript );

					// Evaluate executable scripts on first document insertion
					for ( i = 0; i < hasScripts; i++ ) {
						node = scripts[ i ];
						if ( rscriptType.test( node.type || "" ) &&
							!dataPriv.access( node, "globalEval" ) &&
							jQuery.contains( doc, node ) ) {

							if ( node.src ) {

								// Optional AJAX dependency, but won't run scripts if not present
								if ( jQuery._evalUrl ) {
									jQuery._evalUrl( node.src );
								}
							} else {
								DOMEval( node.textContent.replace( rcleanScript, "" ), doc );
							}
						}
					}
				}
			}
		}

		return collection;
	}

	function remove( elem, selector, keepData ) {
		var node,
			nodes = selector ? jQuery.filter( selector, elem ) : elem,
			i = 0;

		for ( ; ( node = nodes[ i ] ) != null; i++ ) {
			if ( !keepData && node.nodeType === 1 ) {
				jQuery.cleanData( getAll( node ) );
			}

			if ( node.parentNode ) {
				if ( keepData && jQuery.contains( node.ownerDocument, node ) ) {
					setGlobalEval( getAll( node, "script" ) );
				}
				node.parentNode.removeChild( node );
			}
		}

		return elem;
	}

	jQuery.extend( {
		htmlPrefilter: function( html ) {
			return html.replace( rxhtmlTag, "<$1></$2>" );
		},

		clone: function( elem, dataAndEvents, deepDataAndEvents ) {
			var i, l, srcElements, destElements,
				clone = elem.cloneNode( true ),
				inPage = jQuery.contains( elem.ownerDocument, elem );

			// Fix IE cloning issues
			if ( !support.noCloneChecked && ( elem.nodeType === 1 || elem.nodeType === 11 ) &&
					!jQuery.isXMLDoc( elem ) ) {

				// We eschew Sizzle here for performance reasons: https://jsperf.com/getall-vs-sizzle/2
				destElements = getAll( clone );
				srcElements = getAll( elem );

				for ( i = 0, l = srcElements.length; i < l; i++ ) {
					fixInput( srcElements[ i ], destElements[ i ] );
				}
			}

			// Copy the events from the original to the clone
			if ( dataAndEvents ) {
				if ( deepDataAndEvents ) {
					srcElements = srcElements || getAll( elem );
					destElements = destElements || getAll( clone );

					for ( i = 0, l = srcElements.length; i < l; i++ ) {
						cloneCopyEvent( srcElements[ i ], destElements[ i ] );
					}
				} else {
					cloneCopyEvent( elem, clone );
				}
			}

			// Preserve script evaluation history
			destElements = getAll( clone, "script" );
			if ( destElements.length > 0 ) {
				setGlobalEval( destElements, !inPage && getAll( elem, "script" ) );
			}

			// Return the cloned set
			return clone;
		},

		cleanData: function( elems ) {
			var data, elem, type,
				special = jQuery.event.special,
				i = 0;

			for ( ; ( elem = elems[ i ] ) !== undefined; i++ ) {
				if ( acceptData( elem ) ) {
					if ( ( data = elem[ dataPriv.expando ] ) ) {
						if ( data.events ) {
							for ( type in data.events ) {
								if ( special[ type ] ) {
									jQuery.event.remove( elem, type );

								// This is a shortcut to avoid jQuery.event.remove's overhead
								} else {
									jQuery.removeEvent( elem, type, data.handle );
								}
							}
						}

						// Support: Chrome <=35 - 45+
						// Assign undefined instead of using delete, see Data#remove
						elem[ dataPriv.expando ] = undefined;
					}
					if ( elem[ dataUser.expando ] ) {

						// Support: Chrome <=35 - 45+
						// Assign undefined instead of using delete, see Data#remove
						elem[ dataUser.expando ] = undefined;
					}
				}
			}
		}
	} );

	jQuery.fn.extend( {
		detach: function( selector ) {
			return remove( this, selector, true );
		},

		remove: function( selector ) {
			return remove( this, selector );
		},

		text: function( value ) {
			return access( this, function( value ) {
				return value === undefined ?
					jQuery.text( this ) :
					this.empty().each( function() {
						if ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {
							this.textContent = value;
						}
					} );
			}, null, value, arguments.length );
		},

		append: function() {
			return domManip( this, arguments, function( elem ) {
				if ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {
					var target = manipulationTarget( this, elem );
					target.appendChild( elem );
				}
			} );
		},

		prepend: function() {
			return domManip( this, arguments, function( elem ) {
				if ( this.nodeType === 1 || this.nodeType === 11 || this.nodeType === 9 ) {
					var target = manipulationTarget( this, elem );
					target.insertBefore( elem, target.firstChild );
				}
			} );
		},

		before: function() {
			return domManip( this, arguments, function( elem ) {
				if ( this.parentNode ) {
					this.parentNode.insertBefore( elem, this );
				}
			} );
		},

		after: function() {
			return domManip( this, arguments, function( elem ) {
				if ( this.parentNode ) {
					this.parentNode.insertBefore( elem, this.nextSibling );
				}
			} );
		},

		empty: function() {
			var elem,
				i = 0;

			for ( ; ( elem = this[ i ] ) != null; i++ ) {
				if ( elem.nodeType === 1 ) {

					// Prevent memory leaks
					jQuery.cleanData( getAll( elem, false ) );

					// Remove any remaining nodes
					elem.textContent = "";
				}
			}

			return this;
		},

		clone: function( dataAndEvents, deepDataAndEvents ) {
			dataAndEvents = dataAndEvents == null ? false : dataAndEvents;
			deepDataAndEvents = deepDataAndEvents == null ? dataAndEvents : deepDataAndEvents;

			return this.map( function() {
				return jQuery.clone( this, dataAndEvents, deepDataAndEvents );
			} );
		},

		html: function( value ) {
			return access( this, function( value ) {
				var elem = this[ 0 ] || {},
					i = 0,
					l = this.length;

				if ( value === undefined && elem.nodeType === 1 ) {
					return elem.innerHTML;
				}

				// See if we can take a shortcut and just use innerHTML
				if ( typeof value === "string" && !rnoInnerhtml.test( value ) &&
					!wrapMap[ ( rtagName.exec( value ) || [ "", "" ] )[ 1 ].toLowerCase() ] ) {

					value = jQuery.htmlPrefilter( value );

					try {
						for ( ; i < l; i++ ) {
							elem = this[ i ] || {};

							// Remove element nodes and prevent memory leaks
							if ( elem.nodeType === 1 ) {
								jQuery.cleanData( getAll( elem, false ) );
								elem.innerHTML = value;
							}
						}

						elem = 0;

					// If using innerHTML throws an exception, use the fallback method
					} catch ( e ) {}
				}

				if ( elem ) {
					this.empty().append( value );
				}
			}, null, value, arguments.length );
		},

		replaceWith: function() {
			var ignored = [];

			// Make the changes, replacing each non-ignored context element with the new content
			return domManip( this, arguments, function( elem ) {
				var parent = this.parentNode;

				if ( jQuery.inArray( this, ignored ) < 0 ) {
					jQuery.cleanData( getAll( this ) );
					if ( parent ) {
						parent.replaceChild( elem, this );
					}
				}

			// Force callback invocation
			}, ignored );
		}
	} );

	jQuery.each( {
		appendTo: "append",
		prependTo: "prepend",
		insertBefore: "before",
		insertAfter: "after",
		replaceAll: "replaceWith"
	}, function( name, original ) {
		jQuery.fn[ name ] = function( selector ) {
			var elems,
				ret = [],
				insert = jQuery( selector ),
				last = insert.length - 1,
				i = 0;

			for ( ; i <= last; i++ ) {
				elems = i === last ? this : this.clone( true );
				jQuery( insert[ i ] )[ original ]( elems );

				// Support: Android <=4.0 only, PhantomJS 1 only
				// .get() because push.apply(_, arraylike) throws on ancient WebKit
				push.apply( ret, elems.get() );
			}

			return this.pushStack( ret );
		};
	} );
	var rmargin = ( /^margin/ );

	var rnumnonpx = new RegExp( "^(" + pnum + ")(?!px)[a-z%]+$", "i" );

	var getStyles = function( elem ) {

			// Support: IE <=11 only, Firefox <=30 (#15098, #14150)
			// IE throws on elements created in popups
			// FF meanwhile throws on frame elements through "defaultView.getComputedStyle"
			var view = elem.ownerDocument.defaultView;

			if ( !view || !view.opener ) {
				view = window;
			}

			return view.getComputedStyle( elem );
		};



	( function() {

		// Executing both pixelPosition & boxSizingReliable tests require only one layout
		// so they're executed at the same time to save the second computation.
		function computeStyleTests() {

			// This is a singleton, we need to execute it only once
			if ( !div ) {
				return;
			}

			div.style.cssText =
				"box-sizing:border-box;" +
				"position:relative;display:block;" +
				"margin:auto;border:1px;padding:1px;" +
				"top:1%;width:50%";
			div.innerHTML = "";
			documentElement.appendChild( container );

			var divStyle = window.getComputedStyle( div );
			pixelPositionVal = divStyle.top !== "1%";

			// Support: Android 4.0 - 4.3 only, Firefox <=3 - 44
			reliableMarginLeftVal = divStyle.marginLeft === "2px";
			boxSizingReliableVal = divStyle.width === "4px";

			// Support: Android 4.0 - 4.3 only
			// Some styles come back with percentage values, even though they shouldn't
			div.style.marginRight = "50%";
			pixelMarginRightVal = divStyle.marginRight === "4px";

			documentElement.removeChild( container );

			// Nullify the div so it wouldn't be stored in the memory and
			// it will also be a sign that checks already performed
			div = null;
		}

		var pixelPositionVal, boxSizingReliableVal, pixelMarginRightVal, reliableMarginLeftVal,
			container = document.createElement( "div" ),
			div = document.createElement( "div" );

		// Finish early in limited (non-browser) environments
		if ( !div.style ) {
			return;
		}

		// Support: IE <=9 - 11 only
		// Style of cloned element affects source element cloned (#8908)
		div.style.backgroundClip = "content-box";
		div.cloneNode( true ).style.backgroundClip = "";
		support.clearCloneStyle = div.style.backgroundClip === "content-box";

		container.style.cssText = "border:0;width:8px;height:0;top:0;left:-9999px;" +
			"padding:0;margin-top:1px;position:absolute";
		container.appendChild( div );

		jQuery.extend( support, {
			pixelPosition: function() {
				computeStyleTests();
				return pixelPositionVal;
			},
			boxSizingReliable: function() {
				computeStyleTests();
				return boxSizingReliableVal;
			},
			pixelMarginRight: function() {
				computeStyleTests();
				return pixelMarginRightVal;
			},
			reliableMarginLeft: function() {
				computeStyleTests();
				return reliableMarginLeftVal;
			}
		} );
	} )();


	function curCSS( elem, name, computed ) {
		var width, minWidth, maxWidth, ret,

			// Support: Firefox 51+
			// Retrieving style before computed somehow
			// fixes an issue with getting wrong values
			// on detached elements
			style = elem.style;

		computed = computed || getStyles( elem );

		// getPropertyValue is needed for:
		//   .css('filter') (IE 9 only, #12537)
		//   .css('--customProperty) (#3144)
		if ( computed ) {
			ret = computed.getPropertyValue( name ) || computed[ name ];

			if ( ret === "" && !jQuery.contains( elem.ownerDocument, elem ) ) {
				ret = jQuery.style( elem, name );
			}

			// A tribute to the "awesome hack by Dean Edwards"
			// Android Browser returns percentage for some values,
			// but width seems to be reliably pixels.
			// This is against the CSSOM draft spec:
			// https://drafts.csswg.org/cssom/#resolved-values
			if ( !support.pixelMarginRight() && rnumnonpx.test( ret ) && rmargin.test( name ) ) {

				// Remember the original values
				width = style.width;
				minWidth = style.minWidth;
				maxWidth = style.maxWidth;

				// Put in the new values to get a computed value out
				style.minWidth = style.maxWidth = style.width = ret;
				ret = computed.width;

				// Revert the changed values
				style.width = width;
				style.minWidth = minWidth;
				style.maxWidth = maxWidth;
			}
		}

		return ret !== undefined ?

			// Support: IE <=9 - 11 only
			// IE returns zIndex value as an integer.
			ret + "" :
			ret;
	}


	function addGetHookIf( conditionFn, hookFn ) {

		// Define the hook, we'll check on the first run if it's really needed.
		return {
			get: function() {
				if ( conditionFn() ) {

					// Hook not needed (or it's not possible to use it due
					// to missing dependency), remove it.
					delete this.get;
					return;
				}

				// Hook needed; redefine it so that the support test is not executed again.
				return ( this.get = hookFn ).apply( this, arguments );
			}
		};
	}


	var

		// Swappable if display is none or starts with table
		// except "table", "table-cell", or "table-caption"
		// See here for display values: https://developer.mozilla.org/en-US/docs/CSS/display
		rdisplayswap = /^(none|table(?!-c[ea]).+)/,
		rcustomProp = /^--/,
		cssShow = { position: "absolute", visibility: "hidden", display: "block" },
		cssNormalTransform = {
			letterSpacing: "0",
			fontWeight: "400"
		},

		cssPrefixes = [ "Webkit", "Moz", "ms" ],
		emptyStyle = document.createElement( "div" ).style;

	// Return a css property mapped to a potentially vendor prefixed property
	function vendorPropName( name ) {

		// Shortcut for names that are not vendor prefixed
		if ( name in emptyStyle ) {
			return name;
		}

		// Check for vendor prefixed names
		var capName = name[ 0 ].toUpperCase() + name.slice( 1 ),
			i = cssPrefixes.length;

		while ( i-- ) {
			name = cssPrefixes[ i ] + capName;
			if ( name in emptyStyle ) {
				return name;
			}
		}
	}

	// Return a property mapped along what jQuery.cssProps suggests or to
	// a vendor prefixed property.
	function finalPropName( name ) {
		var ret = jQuery.cssProps[ name ];
		if ( !ret ) {
			ret = jQuery.cssProps[ name ] = vendorPropName( name ) || name;
		}
		return ret;
	}

	function setPositiveNumber( elem, value, subtract ) {

		// Any relative (+/-) values have already been
		// normalized at this point
		var matches = rcssNum.exec( value );
		return matches ?

			// Guard against undefined "subtract", e.g., when used as in cssHooks
			Math.max( 0, matches[ 2 ] - ( subtract || 0 ) ) + ( matches[ 3 ] || "px" ) :
			value;
	}

	function augmentWidthOrHeight( elem, name, extra, isBorderBox, styles ) {
		var i,
			val = 0;

		// If we already have the right measurement, avoid augmentation
		if ( extra === ( isBorderBox ? "border" : "content" ) ) {
			i = 4;

		// Otherwise initialize for horizontal or vertical properties
		} else {
			i = name === "width" ? 1 : 0;
		}

		for ( ; i < 4; i += 2 ) {

			// Both box models exclude margin, so add it if we want it
			if ( extra === "margin" ) {
				val += jQuery.css( elem, extra + cssExpand[ i ], true, styles );
			}

			if ( isBorderBox ) {

				// border-box includes padding, so remove it if we want content
				if ( extra === "content" ) {
					val -= jQuery.css( elem, "padding" + cssExpand[ i ], true, styles );
				}

				// At this point, extra isn't border nor margin, so remove border
				if ( extra !== "margin" ) {
					val -= jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );
				}
			} else {

				// At this point, extra isn't content, so add padding
				val += jQuery.css( elem, "padding" + cssExpand[ i ], true, styles );

				// At this point, extra isn't content nor padding, so add border
				if ( extra !== "padding" ) {
					val += jQuery.css( elem, "border" + cssExpand[ i ] + "Width", true, styles );
				}
			}
		}

		return val;
	}

	function getWidthOrHeight( elem, name, extra ) {

		// Start with computed style
		var valueIsBorderBox,
			styles = getStyles( elem ),
			val = curCSS( elem, name, styles ),
			isBorderBox = jQuery.css( elem, "boxSizing", false, styles ) === "border-box";

		// Computed unit is not pixels. Stop here and return.
		if ( rnumnonpx.test( val ) ) {
			return val;
		}

		// Check for style in case a browser which returns unreliable values
		// for getComputedStyle silently falls back to the reliable elem.style
		valueIsBorderBox = isBorderBox &&
			( support.boxSizingReliable() || val === elem.style[ name ] );

		// Fall back to offsetWidth/Height when value is "auto"
		// This happens for inline elements with no explicit setting (gh-3571)
		if ( val === "auto" ) {
			val = elem[ "offset" + name[ 0 ].toUpperCase() + name.slice( 1 ) ];
		}

		// Normalize "", auto, and prepare for extra
		val = parseFloat( val ) || 0;

		// Use the active box-sizing model to add/subtract irrelevant styles
		return ( val +
			augmentWidthOrHeight(
				elem,
				name,
				extra || ( isBorderBox ? "border" : "content" ),
				valueIsBorderBox,
				styles
			)
		) + "px";
	}

	jQuery.extend( {

		// Add in style property hooks for overriding the default
		// behavior of getting and setting a style property
		cssHooks: {
			opacity: {
				get: function( elem, computed ) {
					if ( computed ) {

						// We should always get a number back from opacity
						var ret = curCSS( elem, "opacity" );
						return ret === "" ? "1" : ret;
					}
				}
			}
		},

		// Don't automatically add "px" to these possibly-unitless properties
		cssNumber: {
			"animationIterationCount": true,
			"columnCount": true,
			"fillOpacity": true,
			"flexGrow": true,
			"flexShrink": true,
			"fontWeight": true,
			"lineHeight": true,
			"opacity": true,
			"order": true,
			"orphans": true,
			"widows": true,
			"zIndex": true,
			"zoom": true
		},

		// Add in properties whose names you wish to fix before
		// setting or getting the value
		cssProps: {
			"float": "cssFloat"
		},

		// Get and set the style property on a DOM Node
		style: function( elem, name, value, extra ) {

			// Don't set styles on text and comment nodes
			if ( !elem || elem.nodeType === 3 || elem.nodeType === 8 || !elem.style ) {
				return;
			}

			// Make sure that we're working with the right name
			var ret, type, hooks,
				origName = jQuery.camelCase( name ),
				isCustomProp = rcustomProp.test( name ),
				style = elem.style;

			// Make sure that we're working with the right name. We don't
			// want to query the value if it is a CSS custom property
			// since they are user-defined.
			if ( !isCustomProp ) {
				name = finalPropName( origName );
			}

			// Gets hook for the prefixed version, then unprefixed version
			hooks = jQuery.cssHooks[ name ] || jQuery.cssHooks[ origName ];

			// Check if we're setting a value
			if ( value !== undefined ) {
				type = typeof value;

				// Convert "+=" or "-=" to relative numbers (#7345)
				if ( type === "string" && ( ret = rcssNum.exec( value ) ) && ret[ 1 ] ) {
					value = adjustCSS( elem, name, ret );

					// Fixes bug #9237
					type = "number";
				}

				// Make sure that null and NaN values aren't set (#7116)
				if ( value == null || value !== value ) {
					return;
				}

				// If a number was passed in, add the unit (except for certain CSS properties)
				if ( type === "number" ) {
					value += ret && ret[ 3 ] || ( jQuery.cssNumber[ origName ] ? "" : "px" );
				}

				// background-* props affect original clone's values
				if ( !support.clearCloneStyle && value === "" && name.indexOf( "background" ) === 0 ) {
					style[ name ] = "inherit";
				}

				// If a hook was provided, use that value, otherwise just set the specified value
				if ( !hooks || !( "set" in hooks ) ||
					( value = hooks.set( elem, value, extra ) ) !== undefined ) {

					if ( isCustomProp ) {
						style.setProperty( name, value );
					} else {
						style[ name ] = value;
					}
				}

			} else {

				// If a hook was provided get the non-computed value from there
				if ( hooks && "get" in hooks &&
					( ret = hooks.get( elem, false, extra ) ) !== undefined ) {

					return ret;
				}

				// Otherwise just get the value from the style object
				return style[ name ];
			}
		},

		css: function( elem, name, extra, styles ) {
			var val, num, hooks,
				origName = jQuery.camelCase( name ),
				isCustomProp = rcustomProp.test( name );

			// Make sure that we're working with the right name. We don't
			// want to modify the value if it is a CSS custom property
			// since they are user-defined.
			if ( !isCustomProp ) {
				name = finalPropName( origName );
			}

			// Try prefixed name followed by the unprefixed name
			hooks = jQuery.cssHooks[ name ] || jQuery.cssHooks[ origName ];

			// If a hook was provided get the computed value from there
			if ( hooks && "get" in hooks ) {
				val = hooks.get( elem, true, extra );
			}

			// Otherwise, if a way to get the computed value exists, use that
			if ( val === undefined ) {
				val = curCSS( elem, name, styles );
			}

			// Convert "normal" to computed value
			if ( val === "normal" && name in cssNormalTransform ) {
				val = cssNormalTransform[ name ];
			}

			// Make numeric if forced or a qualifier was provided and val looks numeric
			if ( extra === "" || extra ) {
				num = parseFloat( val );
				return extra === true || isFinite( num ) ? num || 0 : val;
			}

			return val;
		}
	} );

	jQuery.each( [ "height", "width" ], function( i, name ) {
		jQuery.cssHooks[ name ] = {
			get: function( elem, computed, extra ) {
				if ( computed ) {

					// Certain elements can have dimension info if we invisibly show them
					// but it must have a current display style that would benefit
					return rdisplayswap.test( jQuery.css( elem, "display" ) ) &&

						// Support: Safari 8+
						// Table columns in Safari have non-zero offsetWidth & zero
						// getBoundingClientRect().width unless display is changed.
						// Support: IE <=11 only
						// Running getBoundingClientRect on a disconnected node
						// in IE throws an error.
						( !elem.getClientRects().length || !elem.getBoundingClientRect().width ) ?
							swap( elem, cssShow, function() {
								return getWidthOrHeight( elem, name, extra );
							} ) :
							getWidthOrHeight( elem, name, extra );
				}
			},

			set: function( elem, value, extra ) {
				var matches,
					styles = extra && getStyles( elem ),
					subtract = extra && augmentWidthOrHeight(
						elem,
						name,
						extra,
						jQuery.css( elem, "boxSizing", false, styles ) === "border-box",
						styles
					);

				// Convert to pixels if value adjustment is needed
				if ( subtract && ( matches = rcssNum.exec( value ) ) &&
					( matches[ 3 ] || "px" ) !== "px" ) {

					elem.style[ name ] = value;
					value = jQuery.css( elem, name );
				}

				return setPositiveNumber( elem, value, subtract );
			}
		};
	} );

	jQuery.cssHooks.marginLeft = addGetHookIf( support.reliableMarginLeft,
		function( elem, computed ) {
			if ( computed ) {
				return ( parseFloat( curCSS( elem, "marginLeft" ) ) ||
					elem.getBoundingClientRect().left -
						swap( elem, { marginLeft: 0 }, function() {
							return elem.getBoundingClientRect().left;
						} )
					) + "px";
			}
		}
	);

	// These hooks are used by animate to expand properties
	jQuery.each( {
		margin: "",
		padding: "",
		border: "Width"
	}, function( prefix, suffix ) {
		jQuery.cssHooks[ prefix + suffix ] = {
			expand: function( value ) {
				var i = 0,
					expanded = {},

					// Assumes a single number if not a string
					parts = typeof value === "string" ? value.split( " " ) : [ value ];

				for ( ; i < 4; i++ ) {
					expanded[ prefix + cssExpand[ i ] + suffix ] =
						parts[ i ] || parts[ i - 2 ] || parts[ 0 ];
				}

				return expanded;
			}
		};

		if ( !rmargin.test( prefix ) ) {
			jQuery.cssHooks[ prefix + suffix ].set = setPositiveNumber;
		}
	} );

	jQuery.fn.extend( {
		css: function( name, value ) {
			return access( this, function( elem, name, value ) {
				var styles, len,
					map = {},
					i = 0;

				if ( Array.isArray( name ) ) {
					styles = getStyles( elem );
					len = name.length;

					for ( ; i < len; i++ ) {
						map[ name[ i ] ] = jQuery.css( elem, name[ i ], false, styles );
					}

					return map;
				}

				return value !== undefined ?
					jQuery.style( elem, name, value ) :
					jQuery.css( elem, name );
			}, name, value, arguments.length > 1 );
		}
	} );


	function Tween( elem, options, prop, end, easing ) {
		return new Tween.prototype.init( elem, options, prop, end, easing );
	}
	jQuery.Tween = Tween;

	Tween.prototype = {
		constructor: Tween,
		init: function( elem, options, prop, end, easing, unit ) {
			this.elem = elem;
			this.prop = prop;
			this.easing = easing || jQuery.easing._default;
			this.options = options;
			this.start = this.now = this.cur();
			this.end = end;
			this.unit = unit || ( jQuery.cssNumber[ prop ] ? "" : "px" );
		},
		cur: function() {
			var hooks = Tween.propHooks[ this.prop ];

			return hooks && hooks.get ?
				hooks.get( this ) :
				Tween.propHooks._default.get( this );
		},
		run: function( percent ) {
			var eased,
				hooks = Tween.propHooks[ this.prop ];

			if ( this.options.duration ) {
				this.pos = eased = jQuery.easing[ this.easing ](
					percent, this.options.duration * percent, 0, 1, this.options.duration
				);
			} else {
				this.pos = eased = percent;
			}
			this.now = ( this.end - this.start ) * eased + this.start;

			if ( this.options.step ) {
				this.options.step.call( this.elem, this.now, this );
			}

			if ( hooks && hooks.set ) {
				hooks.set( this );
			} else {
				Tween.propHooks._default.set( this );
			}
			return this;
		}
	};

	Tween.prototype.init.prototype = Tween.prototype;

	Tween.propHooks = {
		_default: {
			get: function( tween ) {
				var result;

				// Use a property on the element directly when it is not a DOM element,
				// or when there is no matching style property that exists.
				if ( tween.elem.nodeType !== 1 ||
					tween.elem[ tween.prop ] != null && tween.elem.style[ tween.prop ] == null ) {
					return tween.elem[ tween.prop ];
				}

				// Passing an empty string as a 3rd parameter to .css will automatically
				// attempt a parseFloat and fallback to a string if the parse fails.
				// Simple values such as "10px" are parsed to Float;
				// complex values such as "rotate(1rad)" are returned as-is.
				result = jQuery.css( tween.elem, tween.prop, "" );

				// Empty strings, null, undefined and "auto" are converted to 0.
				return !result || result === "auto" ? 0 : result;
			},
			set: function( tween ) {

				// Use step hook for back compat.
				// Use cssHook if its there.
				// Use .style if available and use plain properties where available.
				if ( jQuery.fx.step[ tween.prop ] ) {
					jQuery.fx.step[ tween.prop ]( tween );
				} else if ( tween.elem.nodeType === 1 &&
					( tween.elem.style[ jQuery.cssProps[ tween.prop ] ] != null ||
						jQuery.cssHooks[ tween.prop ] ) ) {
					jQuery.style( tween.elem, tween.prop, tween.now + tween.unit );
				} else {
					tween.elem[ tween.prop ] = tween.now;
				}
			}
		}
	};

	// Support: IE <=9 only
	// Panic based approach to setting things on disconnected nodes
	Tween.propHooks.scrollTop = Tween.propHooks.scrollLeft = {
		set: function( tween ) {
			if ( tween.elem.nodeType && tween.elem.parentNode ) {
				tween.elem[ tween.prop ] = tween.now;
			}
		}
	};

	jQuery.easing = {
		linear: function( p ) {
			return p;
		},
		swing: function( p ) {
			return 0.5 - Math.cos( p * Math.PI ) / 2;
		},
		_default: "swing"
	};

	jQuery.fx = Tween.prototype.init;

	// Back compat <1.8 extension point
	jQuery.fx.step = {};




	var
		fxNow, inProgress,
		rfxtypes = /^(?:toggle|show|hide)$/,
		rrun = /queueHooks$/;

	function schedule() {
		if ( inProgress ) {
			if ( document.hidden === false && window.requestAnimationFrame ) {
				window.requestAnimationFrame( schedule );
			} else {
				window.setTimeout( schedule, jQuery.fx.interval );
			}

			jQuery.fx.tick();
		}
	}

	// Animations created synchronously will run synchronously
	function createFxNow() {
		window.setTimeout( function() {
			fxNow = undefined;
		} );
		return ( fxNow = jQuery.now() );
	}

	// Generate parameters to create a standard animation
	function genFx( type, includeWidth ) {
		var which,
			i = 0,
			attrs = { height: type };

		// If we include width, step value is 1 to do all cssExpand values,
		// otherwise step value is 2 to skip over Left and Right
		includeWidth = includeWidth ? 1 : 0;
		for ( ; i < 4; i += 2 - includeWidth ) {
			which = cssExpand[ i ];
			attrs[ "margin" + which ] = attrs[ "padding" + which ] = type;
		}

		if ( includeWidth ) {
			attrs.opacity = attrs.width = type;
		}

		return attrs;
	}

	function createTween( value, prop, animation ) {
		var tween,
			collection = ( Animation.tweeners[ prop ] || [] ).concat( Animation.tweeners[ "*" ] ),
			index = 0,
			length = collection.length;
		for ( ; index < length; index++ ) {
			if ( ( tween = collection[ index ].call( animation, prop, value ) ) ) {

				// We're done with this property
				return tween;
			}
		}
	}

	function defaultPrefilter( elem, props, opts ) {
		var prop, value, toggle, hooks, oldfire, propTween, restoreDisplay, display,
			isBox = "width" in props || "height" in props,
			anim = this,
			orig = {},
			style = elem.style,
			hidden = elem.nodeType && isHiddenWithinTree( elem ),
			dataShow = dataPriv.get( elem, "fxshow" );

		// Queue-skipping animations hijack the fx hooks
		if ( !opts.queue ) {
			hooks = jQuery._queueHooks( elem, "fx" );
			if ( hooks.unqueued == null ) {
				hooks.unqueued = 0;
				oldfire = hooks.empty.fire;
				hooks.empty.fire = function() {
					if ( !hooks.unqueued ) {
						oldfire();
					}
				};
			}
			hooks.unqueued++;

			anim.always( function() {

				// Ensure the complete handler is called before this completes
				anim.always( function() {
					hooks.unqueued--;
					if ( !jQuery.queue( elem, "fx" ).length ) {
						hooks.empty.fire();
					}
				} );
			} );
		}

		// Detect show/hide animations
		for ( prop in props ) {
			value = props[ prop ];
			if ( rfxtypes.test( value ) ) {
				delete props[ prop ];
				toggle = toggle || value === "toggle";
				if ( value === ( hidden ? "hide" : "show" ) ) {

					// Pretend to be hidden if this is a "show" and
					// there is still data from a stopped show/hide
					if ( value === "show" && dataShow && dataShow[ prop ] !== undefined ) {
						hidden = true;

					// Ignore all other no-op show/hide data
					} else {
						continue;
					}
				}
				orig[ prop ] = dataShow && dataShow[ prop ] || jQuery.style( elem, prop );
			}
		}

		// Bail out if this is a no-op like .hide().hide()
		propTween = !jQuery.isEmptyObject( props );
		if ( !propTween && jQuery.isEmptyObject( orig ) ) {
			return;
		}

		// Restrict "overflow" and "display" styles during box animations
		if ( isBox && elem.nodeType === 1 ) {

			// Support: IE <=9 - 11, Edge 12 - 13
			// Record all 3 overflow attributes because IE does not infer the shorthand
			// from identically-valued overflowX and overflowY
			opts.overflow = [ style.overflow, style.overflowX, style.overflowY ];

			// Identify a display type, preferring old show/hide data over the CSS cascade
			restoreDisplay = dataShow && dataShow.display;
			if ( restoreDisplay == null ) {
				restoreDisplay = dataPriv.get( elem, "display" );
			}
			display = jQuery.css( elem, "display" );
			if ( display === "none" ) {
				if ( restoreDisplay ) {
					display = restoreDisplay;
				} else {

					// Get nonempty value(s) by temporarily forcing visibility
					showHide( [ elem ], true );
					restoreDisplay = elem.style.display || restoreDisplay;
					display = jQuery.css( elem, "display" );
					showHide( [ elem ] );
				}
			}

			// Animate inline elements as inline-block
			if ( display === "inline" || display === "inline-block" && restoreDisplay != null ) {
				if ( jQuery.css( elem, "float" ) === "none" ) {

					// Restore the original display value at the end of pure show/hide animations
					if ( !propTween ) {
						anim.done( function() {
							style.display = restoreDisplay;
						} );
						if ( restoreDisplay == null ) {
							display = style.display;
							restoreDisplay = display === "none" ? "" : display;
						}
					}
					style.display = "inline-block";
				}
			}
		}

		if ( opts.overflow ) {
			style.overflow = "hidden";
			anim.always( function() {
				style.overflow = opts.overflow[ 0 ];
				style.overflowX = opts.overflow[ 1 ];
				style.overflowY = opts.overflow[ 2 ];
			} );
		}

		// Implement show/hide animations
		propTween = false;
		for ( prop in orig ) {

			// General show/hide setup for this element animation
			if ( !propTween ) {
				if ( dataShow ) {
					if ( "hidden" in dataShow ) {
						hidden = dataShow.hidden;
					}
				} else {
					dataShow = dataPriv.access( elem, "fxshow", { display: restoreDisplay } );
				}

				// Store hidden/visible for toggle so `.stop().toggle()` "reverses"
				if ( toggle ) {
					dataShow.hidden = !hidden;
				}

				// Show elements before animating them
				if ( hidden ) {
					showHide( [ elem ], true );
				}

				/* eslint-disable no-loop-func */

				anim.done( function() {

				/* eslint-enable no-loop-func */

					// The final step of a "hide" animation is actually hiding the element
					if ( !hidden ) {
						showHide( [ elem ] );
					}
					dataPriv.remove( elem, "fxshow" );
					for ( prop in orig ) {
						jQuery.style( elem, prop, orig[ prop ] );
					}
				} );
			}

			// Per-property setup
			propTween = createTween( hidden ? dataShow[ prop ] : 0, prop, anim );
			if ( !( prop in dataShow ) ) {
				dataShow[ prop ] = propTween.start;
				if ( hidden ) {
					propTween.end = propTween.start;
					propTween.start = 0;
				}
			}
		}
	}

	function propFilter( props, specialEasing ) {
		var index, name, easing, value, hooks;

		// camelCase, specialEasing and expand cssHook pass
		for ( index in props ) {
			name = jQuery.camelCase( index );
			easing = specialEasing[ name ];
			value = props[ index ];
			if ( Array.isArray( value ) ) {
				easing = value[ 1 ];
				value = props[ index ] = value[ 0 ];
			}

			if ( index !== name ) {
				props[ name ] = value;
				delete props[ index ];
			}

			hooks = jQuery.cssHooks[ name ];
			if ( hooks && "expand" in hooks ) {
				value = hooks.expand( value );
				delete props[ name ];

				// Not quite $.extend, this won't overwrite existing keys.
				// Reusing 'index' because we have the correct "name"
				for ( index in value ) {
					if ( !( index in props ) ) {
						props[ index ] = value[ index ];
						specialEasing[ index ] = easing;
					}
				}
			} else {
				specialEasing[ name ] = easing;
			}
		}
	}

	function Animation( elem, properties, options ) {
		var result,
			stopped,
			index = 0,
			length = Animation.prefilters.length,
			deferred = jQuery.Deferred().always( function() {

				// Don't match elem in the :animated selector
				delete tick.elem;
			} ),
			tick = function() {
				if ( stopped ) {
					return false;
				}
				var currentTime = fxNow || createFxNow(),
					remaining = Math.max( 0, animation.startTime + animation.duration - currentTime ),

					// Support: Android 2.3 only
					// Archaic crash bug won't allow us to use `1 - ( 0.5 || 0 )` (#12497)
					temp = remaining / animation.duration || 0,
					percent = 1 - temp,
					index = 0,
					length = animation.tweens.length;

				for ( ; index < length; index++ ) {
					animation.tweens[ index ].run( percent );
				}

				deferred.notifyWith( elem, [ animation, percent, remaining ] );

				// If there's more to do, yield
				if ( percent < 1 && length ) {
					return remaining;
				}

				// If this was an empty animation, synthesize a final progress notification
				if ( !length ) {
					deferred.notifyWith( elem, [ animation, 1, 0 ] );
				}

				// Resolve the animation and report its conclusion
				deferred.resolveWith( elem, [ animation ] );
				return false;
			},
			animation = deferred.promise( {
				elem: elem,
				props: jQuery.extend( {}, properties ),
				opts: jQuery.extend( true, {
					specialEasing: {},
					easing: jQuery.easing._default
				}, options ),
				originalProperties: properties,
				originalOptions: options,
				startTime: fxNow || createFxNow(),
				duration: options.duration,
				tweens: [],
				createTween: function( prop, end ) {
					var tween = jQuery.Tween( elem, animation.opts, prop, end,
							animation.opts.specialEasing[ prop ] || animation.opts.easing );
					animation.tweens.push( tween );
					return tween;
				},
				stop: function( gotoEnd ) {
					var index = 0,

						// If we are going to the end, we want to run all the tweens
						// otherwise we skip this part
						length = gotoEnd ? animation.tweens.length : 0;
					if ( stopped ) {
						return this;
					}
					stopped = true;
					for ( ; index < length; index++ ) {
						animation.tweens[ index ].run( 1 );
					}

					// Resolve when we played the last frame; otherwise, reject
					if ( gotoEnd ) {
						deferred.notifyWith( elem, [ animation, 1, 0 ] );
						deferred.resolveWith( elem, [ animation, gotoEnd ] );
					} else {
						deferred.rejectWith( elem, [ animation, gotoEnd ] );
					}
					return this;
				}
			} ),
			props = animation.props;

		propFilter( props, animation.opts.specialEasing );

		for ( ; index < length; index++ ) {
			result = Animation.prefilters[ index ].call( animation, elem, props, animation.opts );
			if ( result ) {
				if ( jQuery.isFunction( result.stop ) ) {
					jQuery._queueHooks( animation.elem, animation.opts.queue ).stop =
						jQuery.proxy( result.stop, result );
				}
				return result;
			}
		}

		jQuery.map( props, createTween, animation );

		if ( jQuery.isFunction( animation.opts.start ) ) {
			animation.opts.start.call( elem, animation );
		}

		// Attach callbacks from options
		animation
			.progress( animation.opts.progress )
			.done( animation.opts.done, animation.opts.complete )
			.fail( animation.opts.fail )
			.always( animation.opts.always );

		jQuery.fx.timer(
			jQuery.extend( tick, {
				elem: elem,
				anim: animation,
				queue: animation.opts.queue
			} )
		);

		return animation;
	}

	jQuery.Animation = jQuery.extend( Animation, {

		tweeners: {
			"*": [ function( prop, value ) {
				var tween = this.createTween( prop, value );
				adjustCSS( tween.elem, prop, rcssNum.exec( value ), tween );
				return tween;
			} ]
		},

		tweener: function( props, callback ) {
			if ( jQuery.isFunction( props ) ) {
				callback = props;
				props = [ "*" ];
			} else {
				props = props.match( rnothtmlwhite );
			}

			var prop,
				index = 0,
				length = props.length;

			for ( ; index < length; index++ ) {
				prop = props[ index ];
				Animation.tweeners[ prop ] = Animation.tweeners[ prop ] || [];
				Animation.tweeners[ prop ].unshift( callback );
			}
		},

		prefilters: [ defaultPrefilter ],

		prefilter: function( callback, prepend ) {
			if ( prepend ) {
				Animation.prefilters.unshift( callback );
			} else {
				Animation.prefilters.push( callback );
			}
		}
	} );

	jQuery.speed = function( speed, easing, fn ) {
		var opt = speed && typeof speed === "object" ? jQuery.extend( {}, speed ) : {
			complete: fn || !fn && easing ||
				jQuery.isFunction( speed ) && speed,
			duration: speed,
			easing: fn && easing || easing && !jQuery.isFunction( easing ) && easing
		};

		// Go to the end state if fx are off
		if ( jQuery.fx.off ) {
			opt.duration = 0;

		} else {
			if ( typeof opt.duration !== "number" ) {
				if ( opt.duration in jQuery.fx.speeds ) {
					opt.duration = jQuery.fx.speeds[ opt.duration ];

				} else {
					opt.duration = jQuery.fx.speeds._default;
				}
			}
		}

		// Normalize opt.queue - true/undefined/null -> "fx"
		if ( opt.queue == null || opt.queue === true ) {
			opt.queue = "fx";
		}

		// Queueing
		opt.old = opt.complete;

		opt.complete = function() {
			if ( jQuery.isFunction( opt.old ) ) {
				opt.old.call( this );
			}

			if ( opt.queue ) {
				jQuery.dequeue( this, opt.queue );
			}
		};

		return opt;
	};

	jQuery.fn.extend( {
		fadeTo: function( speed, to, easing, callback ) {

			// Show any hidden elements after setting opacity to 0
			return this.filter( isHiddenWithinTree ).css( "opacity", 0 ).show()

				// Animate to the value specified
				.end().animate( { opacity: to }, speed, easing, callback );
		},
		animate: function( prop, speed, easing, callback ) {
			var empty = jQuery.isEmptyObject( prop ),
				optall = jQuery.speed( speed, easing, callback ),
				doAnimation = function() {

					// Operate on a copy of prop so per-property easing won't be lost
					var anim = Animation( this, jQuery.extend( {}, prop ), optall );

					// Empty animations, or finishing resolves immediately
					if ( empty || dataPriv.get( this, "finish" ) ) {
						anim.stop( true );
					}
				};
				doAnimation.finish = doAnimation;

			return empty || optall.queue === false ?
				this.each( doAnimation ) :
				this.queue( optall.queue, doAnimation );
		},
		stop: function( type, clearQueue, gotoEnd ) {
			var stopQueue = function( hooks ) {
				var stop = hooks.stop;
				delete hooks.stop;
				stop( gotoEnd );
			};

			if ( typeof type !== "string" ) {
				gotoEnd = clearQueue;
				clearQueue = type;
				type = undefined;
			}
			if ( clearQueue && type !== false ) {
				this.queue( type || "fx", [] );
			}

			return this.each( function() {
				var dequeue = true,
					index = type != null && type + "queueHooks",
					timers = jQuery.timers,
					data = dataPriv.get( this );

				if ( index ) {
					if ( data[ index ] && data[ index ].stop ) {
						stopQueue( data[ index ] );
					}
				} else {
					for ( index in data ) {
						if ( data[ index ] && data[ index ].stop && rrun.test( index ) ) {
							stopQueue( data[ index ] );
						}
					}
				}

				for ( index = timers.length; index--; ) {
					if ( timers[ index ].elem === this &&
						( type == null || timers[ index ].queue === type ) ) {

						timers[ index ].anim.stop( gotoEnd );
						dequeue = false;
						timers.splice( index, 1 );
					}
				}

				// Start the next in the queue if the last step wasn't forced.
				// Timers currently will call their complete callbacks, which
				// will dequeue but only if they were gotoEnd.
				if ( dequeue || !gotoEnd ) {
					jQuery.dequeue( this, type );
				}
			} );
		},
		finish: function( type ) {
			if ( type !== false ) {
				type = type || "fx";
			}
			return this.each( function() {
				var index,
					data = dataPriv.get( this ),
					queue = data[ type + "queue" ],
					hooks = data[ type + "queueHooks" ],
					timers = jQuery.timers,
					length = queue ? queue.length : 0;

				// Enable finishing flag on private data
				data.finish = true;

				// Empty the queue first
				jQuery.queue( this, type, [] );

				if ( hooks && hooks.stop ) {
					hooks.stop.call( this, true );
				}

				// Look for any active animations, and finish them
				for ( index = timers.length; index--; ) {
					if ( timers[ index ].elem === this && timers[ index ].queue === type ) {
						timers[ index ].anim.stop( true );
						timers.splice( index, 1 );
					}
				}

				// Look for any animations in the old queue and finish them
				for ( index = 0; index < length; index++ ) {
					if ( queue[ index ] && queue[ index ].finish ) {
						queue[ index ].finish.call( this );
					}
				}

				// Turn off finishing flag
				delete data.finish;
			} );
		}
	} );

	jQuery.each( [ "toggle", "show", "hide" ], function( i, name ) {
		var cssFn = jQuery.fn[ name ];
		jQuery.fn[ name ] = function( speed, easing, callback ) {
			return speed == null || typeof speed === "boolean" ?
				cssFn.apply( this, arguments ) :
				this.animate( genFx( name, true ), speed, easing, callback );
		};
	} );

	// Generate shortcuts for custom animations
	jQuery.each( {
		slideDown: genFx( "show" ),
		slideUp: genFx( "hide" ),
		slideToggle: genFx( "toggle" ),
		fadeIn: { opacity: "show" },
		fadeOut: { opacity: "hide" },
		fadeToggle: { opacity: "toggle" }
	}, function( name, props ) {
		jQuery.fn[ name ] = function( speed, easing, callback ) {
			return this.animate( props, speed, easing, callback );
		};
	} );

	jQuery.timers = [];
	jQuery.fx.tick = function() {
		var timer,
			i = 0,
			timers = jQuery.timers;

		fxNow = jQuery.now();

		for ( ; i < timers.length; i++ ) {
			timer = timers[ i ];

			// Run the timer and safely remove it when done (allowing for external removal)
			if ( !timer() && timers[ i ] === timer ) {
				timers.splice( i--, 1 );
			}
		}

		if ( !timers.length ) {
			jQuery.fx.stop();
		}
		fxNow = undefined;
	};

	jQuery.fx.timer = function( timer ) {
		jQuery.timers.push( timer );
		jQuery.fx.start();
	};

	jQuery.fx.interval = 13;
	jQuery.fx.start = function() {
		if ( inProgress ) {
			return;
		}

		inProgress = true;
		schedule();
	};

	jQuery.fx.stop = function() {
		inProgress = null;
	};

	jQuery.fx.speeds = {
		slow: 600,
		fast: 200,

		// Default speed
		_default: 400
	};


	// Based off of the plugin by Clint Helfers, with permission.
	// https://web.archive.org/web/20100324014747/http://blindsignals.com/index.php/2009/07/jquery-delay/
	jQuery.fn.delay = function( time, type ) {
		time = jQuery.fx ? jQuery.fx.speeds[ time ] || time : time;
		type = type || "fx";

		return this.queue( type, function( next, hooks ) {
			var timeout = window.setTimeout( next, time );
			hooks.stop = function() {
				window.clearTimeout( timeout );
			};
		} );
	};


	( function() {
		var input = document.createElement( "input" ),
			select = document.createElement( "select" ),
			opt = select.appendChild( document.createElement( "option" ) );

		input.type = "checkbox";

		// Support: Android <=4.3 only
		// Default value for a checkbox should be "on"
		support.checkOn = input.value !== "";

		// Support: IE <=11 only
		// Must access selectedIndex to make default options select
		support.optSelected = opt.selected;

		// Support: IE <=11 only
		// An input loses its value after becoming a radio
		input = document.createElement( "input" );
		input.value = "t";
		input.type = "radio";
		support.radioValue = input.value === "t";
	} )();


	var boolHook,
		attrHandle = jQuery.expr.attrHandle;

	jQuery.fn.extend( {
		attr: function( name, value ) {
			return access( this, jQuery.attr, name, value, arguments.length > 1 );
		},

		removeAttr: function( name ) {
			return this.each( function() {
				jQuery.removeAttr( this, name );
			} );
		}
	} );

	jQuery.extend( {
		attr: function( elem, name, value ) {
			var ret, hooks,
				nType = elem.nodeType;

			// Don't get/set attributes on text, comment and attribute nodes
			if ( nType === 3 || nType === 8 || nType === 2 ) {
				return;
			}

			// Fallback to prop when attributes are not supported
			if ( typeof elem.getAttribute === "undefined" ) {
				return jQuery.prop( elem, name, value );
			}

			// Attribute hooks are determined by the lowercase version
			// Grab necessary hook if one is defined
			if ( nType !== 1 || !jQuery.isXMLDoc( elem ) ) {
				hooks = jQuery.attrHooks[ name.toLowerCase() ] ||
					( jQuery.expr.match.bool.test( name ) ? boolHook : undefined );
			}

			if ( value !== undefined ) {
				if ( value === null ) {
					jQuery.removeAttr( elem, name );
					return;
				}

				if ( hooks && "set" in hooks &&
					( ret = hooks.set( elem, value, name ) ) !== undefined ) {
					return ret;
				}

				elem.setAttribute( name, value + "" );
				return value;
			}

			if ( hooks && "get" in hooks && ( ret = hooks.get( elem, name ) ) !== null ) {
				return ret;
			}

			ret = jQuery.find.attr( elem, name );

			// Non-existent attributes return null, we normalize to undefined
			return ret == null ? undefined : ret;
		},

		attrHooks: {
			type: {
				set: function( elem, value ) {
					if ( !support.radioValue && value === "radio" &&
						nodeName( elem, "input" ) ) {
						var val = elem.value;
						elem.setAttribute( "type", value );
						if ( val ) {
							elem.value = val;
						}
						return value;
					}
				}
			}
		},

		removeAttr: function( elem, value ) {
			var name,
				i = 0,

				// Attribute names can contain non-HTML whitespace characters
				// https://html.spec.whatwg.org/multipage/syntax.html#attributes-2
				attrNames = value && value.match( rnothtmlwhite );

			if ( attrNames && elem.nodeType === 1 ) {
				while ( ( name = attrNames[ i++ ] ) ) {
					elem.removeAttribute( name );
				}
			}
		}
	} );

	// Hooks for boolean attributes
	boolHook = {
		set: function( elem, value, name ) {
			if ( value === false ) {

				// Remove boolean attributes when set to false
				jQuery.removeAttr( elem, name );
			} else {
				elem.setAttribute( name, name );
			}
			return name;
		}
	};

	jQuery.each( jQuery.expr.match.bool.source.match( /\w+/g ), function( i, name ) {
		var getter = attrHandle[ name ] || jQuery.find.attr;

		attrHandle[ name ] = function( elem, name, isXML ) {
			var ret, handle,
				lowercaseName = name.toLowerCase();

			if ( !isXML ) {

				// Avoid an infinite loop by temporarily removing this function from the getter
				handle = attrHandle[ lowercaseName ];
				attrHandle[ lowercaseName ] = ret;
				ret = getter( elem, name, isXML ) != null ?
					lowercaseName :
					null;
				attrHandle[ lowercaseName ] = handle;
			}
			return ret;
		};
	} );




	var rfocusable = /^(?:input|select|textarea|button)$/i,
		rclickable = /^(?:a|area)$/i;

	jQuery.fn.extend( {
		prop: function( name, value ) {
			return access( this, jQuery.prop, name, value, arguments.length > 1 );
		},

		removeProp: function( name ) {
			return this.each( function() {
				delete this[ jQuery.propFix[ name ] || name ];
			} );
		}
	} );

	jQuery.extend( {
		prop: function( elem, name, value ) {
			var ret, hooks,
				nType = elem.nodeType;

			// Don't get/set properties on text, comment and attribute nodes
			if ( nType === 3 || nType === 8 || nType === 2 ) {
				return;
			}

			if ( nType !== 1 || !jQuery.isXMLDoc( elem ) ) {

				// Fix name and attach hooks
				name = jQuery.propFix[ name ] || name;
				hooks = jQuery.propHooks[ name ];
			}

			if ( value !== undefined ) {
				if ( hooks && "set" in hooks &&
					( ret = hooks.set( elem, value, name ) ) !== undefined ) {
					return ret;
				}

				return ( elem[ name ] = value );
			}

			if ( hooks && "get" in hooks && ( ret = hooks.get( elem, name ) ) !== null ) {
				return ret;
			}

			return elem[ name ];
		},

		propHooks: {
			tabIndex: {
				get: function( elem ) {

					// Support: IE <=9 - 11 only
					// elem.tabIndex doesn't always return the
					// correct value when it hasn't been explicitly set
					// https://web.archive.org/web/20141116233347/http://fluidproject.org/blog/2008/01/09/getting-setting-and-removing-tabindex-values-with-javascript/
					// Use proper attribute retrieval(#12072)
					var tabindex = jQuery.find.attr( elem, "tabindex" );

					if ( tabindex ) {
						return parseInt( tabindex, 10 );
					}

					if (
						rfocusable.test( elem.nodeName ) ||
						rclickable.test( elem.nodeName ) &&
						elem.href
					) {
						return 0;
					}

					return -1;
				}
			}
		},

		propFix: {
			"for": "htmlFor",
			"class": "className"
		}
	} );

	// Support: IE <=11 only
	// Accessing the selectedIndex property
	// forces the browser to respect setting selected
	// on the option
	// The getter ensures a default option is selected
	// when in an optgroup
	// eslint rule "no-unused-expressions" is disabled for this code
	// since it considers such accessions noop
	if ( !support.optSelected ) {
		jQuery.propHooks.selected = {
			get: function( elem ) {

				/* eslint no-unused-expressions: "off" */

				var parent = elem.parentNode;
				if ( parent && parent.parentNode ) {
					parent.parentNode.selectedIndex;
				}
				return null;
			},
			set: function( elem ) {

				/* eslint no-unused-expressions: "off" */

				var parent = elem.parentNode;
				if ( parent ) {
					parent.selectedIndex;

					if ( parent.parentNode ) {
						parent.parentNode.selectedIndex;
					}
				}
			}
		};
	}

	jQuery.each( [
		"tabIndex",
		"readOnly",
		"maxLength",
		"cellSpacing",
		"cellPadding",
		"rowSpan",
		"colSpan",
		"useMap",
		"frameBorder",
		"contentEditable"
	], function() {
		jQuery.propFix[ this.toLowerCase() ] = this;
	} );




		// Strip and collapse whitespace according to HTML spec
		// https://html.spec.whatwg.org/multipage/infrastructure.html#strip-and-collapse-whitespace
		function stripAndCollapse( value ) {
			var tokens = value.match( rnothtmlwhite ) || [];
			return tokens.join( " " );
		}


	function getClass( elem ) {
		return elem.getAttribute && elem.getAttribute( "class" ) || "";
	}

	jQuery.fn.extend( {
		addClass: function( value ) {
			var classes, elem, cur, curValue, clazz, j, finalValue,
				i = 0;

			if ( jQuery.isFunction( value ) ) {
				return this.each( function( j ) {
					jQuery( this ).addClass( value.call( this, j, getClass( this ) ) );
				} );
			}

			if ( typeof value === "string" && value ) {
				classes = value.match( rnothtmlwhite ) || [];

				while ( ( elem = this[ i++ ] ) ) {
					curValue = getClass( elem );
					cur = elem.nodeType === 1 && ( " " + stripAndCollapse( curValue ) + " " );

					if ( cur ) {
						j = 0;
						while ( ( clazz = classes[ j++ ] ) ) {
							if ( cur.indexOf( " " + clazz + " " ) < 0 ) {
								cur += clazz + " ";
							}
						}

						// Only assign if different to avoid unneeded rendering.
						finalValue = stripAndCollapse( cur );
						if ( curValue !== finalValue ) {
							elem.setAttribute( "class", finalValue );
						}
					}
				}
			}

			return this;
		},

		removeClass: function( value ) {
			var classes, elem, cur, curValue, clazz, j, finalValue,
				i = 0;

			if ( jQuery.isFunction( value ) ) {
				return this.each( function( j ) {
					jQuery( this ).removeClass( value.call( this, j, getClass( this ) ) );
				} );
			}

			if ( !arguments.length ) {
				return this.attr( "class", "" );
			}

			if ( typeof value === "string" && value ) {
				classes = value.match( rnothtmlwhite ) || [];

				while ( ( elem = this[ i++ ] ) ) {
					curValue = getClass( elem );

					// This expression is here for better compressibility (see addClass)
					cur = elem.nodeType === 1 && ( " " + stripAndCollapse( curValue ) + " " );

					if ( cur ) {
						j = 0;
						while ( ( clazz = classes[ j++ ] ) ) {

							// Remove *all* instances
							while ( cur.indexOf( " " + clazz + " " ) > -1 ) {
								cur = cur.replace( " " + clazz + " ", " " );
							}
						}

						// Only assign if different to avoid unneeded rendering.
						finalValue = stripAndCollapse( cur );
						if ( curValue !== finalValue ) {
							elem.setAttribute( "class", finalValue );
						}
					}
				}
			}

			return this;
		},

		toggleClass: function( value, stateVal ) {
			var type = typeof value;

			if ( typeof stateVal === "boolean" && type === "string" ) {
				return stateVal ? this.addClass( value ) : this.removeClass( value );
			}

			if ( jQuery.isFunction( value ) ) {
				return this.each( function( i ) {
					jQuery( this ).toggleClass(
						value.call( this, i, getClass( this ), stateVal ),
						stateVal
					);
				} );
			}

			return this.each( function() {
				var className, i, self, classNames;

				if ( type === "string" ) {

					// Toggle individual class names
					i = 0;
					self = jQuery( this );
					classNames = value.match( rnothtmlwhite ) || [];

					while ( ( className = classNames[ i++ ] ) ) {

						// Check each className given, space separated list
						if ( self.hasClass( className ) ) {
							self.removeClass( className );
						} else {
							self.addClass( className );
						}
					}

				// Toggle whole class name
				} else if ( value === undefined || type === "boolean" ) {
					className = getClass( this );
					if ( className ) {

						// Store className if set
						dataPriv.set( this, "__className__", className );
					}

					// If the element has a class name or if we're passed `false`,
					// then remove the whole classname (if there was one, the above saved it).
					// Otherwise bring back whatever was previously saved (if anything),
					// falling back to the empty string if nothing was stored.
					if ( this.setAttribute ) {
						this.setAttribute( "class",
							className || value === false ?
							"" :
							dataPriv.get( this, "__className__" ) || ""
						);
					}
				}
			} );
		},

		hasClass: function( selector ) {
			var className, elem,
				i = 0;

			className = " " + selector + " ";
			while ( ( elem = this[ i++ ] ) ) {
				if ( elem.nodeType === 1 &&
					( " " + stripAndCollapse( getClass( elem ) ) + " " ).indexOf( className ) > -1 ) {
						return true;
				}
			}

			return false;
		}
	} );




	var rreturn = /\r/g;

	jQuery.fn.extend( {
		val: function( value ) {
			var hooks, ret, isFunction,
				elem = this[ 0 ];

			if ( !arguments.length ) {
				if ( elem ) {
					hooks = jQuery.valHooks[ elem.type ] ||
						jQuery.valHooks[ elem.nodeName.toLowerCase() ];

					if ( hooks &&
						"get" in hooks &&
						( ret = hooks.get( elem, "value" ) ) !== undefined
					) {
						return ret;
					}

					ret = elem.value;

					// Handle most common string cases
					if ( typeof ret === "string" ) {
						return ret.replace( rreturn, "" );
					}

					// Handle cases where value is null/undef or number
					return ret == null ? "" : ret;
				}

				return;
			}

			isFunction = jQuery.isFunction( value );

			return this.each( function( i ) {
				var val;

				if ( this.nodeType !== 1 ) {
					return;
				}

				if ( isFunction ) {
					val = value.call( this, i, jQuery( this ).val() );
				} else {
					val = value;
				}

				// Treat null/undefined as ""; convert numbers to string
				if ( val == null ) {
					val = "";

				} else if ( typeof val === "number" ) {
					val += "";

				} else if ( Array.isArray( val ) ) {
					val = jQuery.map( val, function( value ) {
						return value == null ? "" : value + "";
					} );
				}

				hooks = jQuery.valHooks[ this.type ] || jQuery.valHooks[ this.nodeName.toLowerCase() ];

				// If set returns undefined, fall back to normal setting
				if ( !hooks || !( "set" in hooks ) || hooks.set( this, val, "value" ) === undefined ) {
					this.value = val;
				}
			} );
		}
	} );

	jQuery.extend( {
		valHooks: {
			option: {
				get: function( elem ) {

					var val = jQuery.find.attr( elem, "value" );
					return val != null ?
						val :

						// Support: IE <=10 - 11 only
						// option.text throws exceptions (#14686, #14858)
						// Strip and collapse whitespace
						// https://html.spec.whatwg.org/#strip-and-collapse-whitespace
						stripAndCollapse( jQuery.text( elem ) );
				}
			},
			select: {
				get: function( elem ) {
					var value, option, i,
						options = elem.options,
						index = elem.selectedIndex,
						one = elem.type === "select-one",
						values = one ? null : [],
						max = one ? index + 1 : options.length;

					if ( index < 0 ) {
						i = max;

					} else {
						i = one ? index : 0;
					}

					// Loop through all the selected options
					for ( ; i < max; i++ ) {
						option = options[ i ];

						// Support: IE <=9 only
						// IE8-9 doesn't update selected after form reset (#2551)
						if ( ( option.selected || i === index ) &&

								// Don't return options that are disabled or in a disabled optgroup
								!option.disabled &&
								( !option.parentNode.disabled ||
									!nodeName( option.parentNode, "optgroup" ) ) ) {

							// Get the specific value for the option
							value = jQuery( option ).val();

							// We don't need an array for one selects
							if ( one ) {
								return value;
							}

							// Multi-Selects return an array
							values.push( value );
						}
					}

					return values;
				},

				set: function( elem, value ) {
					var optionSet, option,
						options = elem.options,
						values = jQuery.makeArray( value ),
						i = options.length;

					while ( i-- ) {
						option = options[ i ];

						/* eslint-disable no-cond-assign */

						if ( option.selected =
							jQuery.inArray( jQuery.valHooks.option.get( option ), values ) > -1
						) {
							optionSet = true;
						}

						/* eslint-enable no-cond-assign */
					}

					// Force browsers to behave consistently when non-matching value is set
					if ( !optionSet ) {
						elem.selectedIndex = -1;
					}
					return values;
				}
			}
		}
	} );

	// Radios and checkboxes getter/setter
	jQuery.each( [ "radio", "checkbox" ], function() {
		jQuery.valHooks[ this ] = {
			set: function( elem, value ) {
				if ( Array.isArray( value ) ) {
					return ( elem.checked = jQuery.inArray( jQuery( elem ).val(), value ) > -1 );
				}
			}
		};
		if ( !support.checkOn ) {
			jQuery.valHooks[ this ].get = function( elem ) {
				return elem.getAttribute( "value" ) === null ? "on" : elem.value;
			};
		}
	} );




	// Return jQuery for attributes-only inclusion


	var rfocusMorph = /^(?:focusinfocus|focusoutblur)$/;

	jQuery.extend( jQuery.event, {

		trigger: function( event, data, elem, onlyHandlers ) {

			var i, cur, tmp, bubbleType, ontype, handle, special,
				eventPath = [ elem || document ],
				type = hasOwn.call( event, "type" ) ? event.type : event,
				namespaces = hasOwn.call( event, "namespace" ) ? event.namespace.split( "." ) : [];

			cur = tmp = elem = elem || document;

			// Don't do events on text and comment nodes
			if ( elem.nodeType === 3 || elem.nodeType === 8 ) {
				return;
			}

			// focus/blur morphs to focusin/out; ensure we're not firing them right now
			if ( rfocusMorph.test( type + jQuery.event.triggered ) ) {
				return;
			}

			if ( type.indexOf( "." ) > -1 ) {

				// Namespaced trigger; create a regexp to match event type in handle()
				namespaces = type.split( "." );
				type = namespaces.shift();
				namespaces.sort();
			}
			ontype = type.indexOf( ":" ) < 0 && "on" + type;

			// Caller can pass in a jQuery.Event object, Object, or just an event type string
			event = event[ jQuery.expando ] ?
				event :
				new jQuery.Event( type, typeof event === "object" && event );

			// Trigger bitmask: & 1 for native handlers; & 2 for jQuery (always true)
			event.isTrigger = onlyHandlers ? 2 : 3;
			event.namespace = namespaces.join( "." );
			event.rnamespace = event.namespace ?
				new RegExp( "(^|\\.)" + namespaces.join( "\\.(?:.*\\.|)" ) + "(\\.|$)" ) :
				null;

			// Clean up the event in case it is being reused
			event.result = undefined;
			if ( !event.target ) {
				event.target = elem;
			}

			// Clone any incoming data and prepend the event, creating the handler arg list
			data = data == null ?
				[ event ] :
				jQuery.makeArray( data, [ event ] );

			// Allow special events to draw outside the lines
			special = jQuery.event.special[ type ] || {};
			if ( !onlyHandlers && special.trigger && special.trigger.apply( elem, data ) === false ) {
				return;
			}

			// Determine event propagation path in advance, per W3C events spec (#9951)
			// Bubble up to document, then to window; watch for a global ownerDocument var (#9724)
			if ( !onlyHandlers && !special.noBubble && !jQuery.isWindow( elem ) ) {

				bubbleType = special.delegateType || type;
				if ( !rfocusMorph.test( bubbleType + type ) ) {
					cur = cur.parentNode;
				}
				for ( ; cur; cur = cur.parentNode ) {
					eventPath.push( cur );
					tmp = cur;
				}

				// Only add window if we got to document (e.g., not plain obj or detached DOM)
				if ( tmp === ( elem.ownerDocument || document ) ) {
					eventPath.push( tmp.defaultView || tmp.parentWindow || window );
				}
			}

			// Fire handlers on the event path
			i = 0;
			while ( ( cur = eventPath[ i++ ] ) && !event.isPropagationStopped() ) {

				event.type = i > 1 ?
					bubbleType :
					special.bindType || type;

				// jQuery handler
				handle = ( dataPriv.get( cur, "events" ) || {} )[ event.type ] &&
					dataPriv.get( cur, "handle" );
				if ( handle ) {
					handle.apply( cur, data );
				}

				// Native handler
				handle = ontype && cur[ ontype ];
				if ( handle && handle.apply && acceptData( cur ) ) {
					event.result = handle.apply( cur, data );
					if ( event.result === false ) {
						event.preventDefault();
					}
				}
			}
			event.type = type;

			// If nobody prevented the default action, do it now
			if ( !onlyHandlers && !event.isDefaultPrevented() ) {

				if ( ( !special._default ||
					special._default.apply( eventPath.pop(), data ) === false ) &&
					acceptData( elem ) ) {

					// Call a native DOM method on the target with the same name as the event.
					// Don't do default actions on window, that's where global variables be (#6170)
					if ( ontype && jQuery.isFunction( elem[ type ] ) && !jQuery.isWindow( elem ) ) {

						// Don't re-trigger an onFOO event when we call its FOO() method
						tmp = elem[ ontype ];

						if ( tmp ) {
							elem[ ontype ] = null;
						}

						// Prevent re-triggering of the same event, since we already bubbled it above
						jQuery.event.triggered = type;
						elem[ type ]();
						jQuery.event.triggered = undefined;

						if ( tmp ) {
							elem[ ontype ] = tmp;
						}
					}
				}
			}

			return event.result;
		},

		// Piggyback on a donor event to simulate a different one
		// Used only for `focus(in | out)` events
		simulate: function( type, elem, event ) {
			var e = jQuery.extend(
				new jQuery.Event(),
				event,
				{
					type: type,
					isSimulated: true
				}
			);

			jQuery.event.trigger( e, null, elem );
		}

	} );

	jQuery.fn.extend( {

		trigger: function( type, data ) {
			return this.each( function() {
				jQuery.event.trigger( type, data, this );
			} );
		},
		triggerHandler: function( type, data ) {
			var elem = this[ 0 ];
			if ( elem ) {
				return jQuery.event.trigger( type, data, elem, true );
			}
		}
	} );


	jQuery.each( ( "blur focus focusin focusout resize scroll click dblclick " +
		"mousedown mouseup mousemove mouseover mouseout mouseenter mouseleave " +
		"change select submit keydown keypress keyup contextmenu" ).split( " " ),
		function( i, name ) {

		// Handle event binding
		jQuery.fn[ name ] = function( data, fn ) {
			return arguments.length > 0 ?
				this.on( name, null, data, fn ) :
				this.trigger( name );
		};
	} );

	jQuery.fn.extend( {
		hover: function( fnOver, fnOut ) {
			return this.mouseenter( fnOver ).mouseleave( fnOut || fnOver );
		}
	} );




	support.focusin = "onfocusin" in window;


	// Support: Firefox <=44
	// Firefox doesn't have focus(in | out) events
	// Related ticket - https://bugzilla.mozilla.org/show_bug.cgi?id=687787
	//
	// Support: Chrome <=48 - 49, Safari <=9.0 - 9.1
	// focus(in | out) events fire after focus & blur events,
	// which is spec violation - http://www.w3.org/TR/DOM-Level-3-Events/#events-focusevent-event-order
	// Related ticket - https://bugs.chromium.org/p/chromium/issues/detail?id=449857
	if ( !support.focusin ) {
		jQuery.each( { focus: "focusin", blur: "focusout" }, function( orig, fix ) {

			// Attach a single capturing handler on the document while someone wants focusin/focusout
			var handler = function( event ) {
				jQuery.event.simulate( fix, event.target, jQuery.event.fix( event ) );
			};

			jQuery.event.special[ fix ] = {
				setup: function() {
					var doc = this.ownerDocument || this,
						attaches = dataPriv.access( doc, fix );

					if ( !attaches ) {
						doc.addEventListener( orig, handler, true );
					}
					dataPriv.access( doc, fix, ( attaches || 0 ) + 1 );
				},
				teardown: function() {
					var doc = this.ownerDocument || this,
						attaches = dataPriv.access( doc, fix ) - 1;

					if ( !attaches ) {
						doc.removeEventListener( orig, handler, true );
						dataPriv.remove( doc, fix );

					} else {
						dataPriv.access( doc, fix, attaches );
					}
				}
			};
		} );
	}
	var location = window.location;

	var nonce = jQuery.now();

	var rquery = ( /\?/ );



	// Cross-browser xml parsing
	jQuery.parseXML = function( data ) {
		var xml;
		if ( !data || typeof data !== "string" ) {
			return null;
		}

		// Support: IE 9 - 11 only
		// IE throws on parseFromString with invalid input.
		try {
			xml = ( new window.DOMParser() ).parseFromString( data, "text/xml" );
		} catch ( e ) {
			xml = undefined;
		}

		if ( !xml || xml.getElementsByTagName( "parsererror" ).length ) {
			jQuery.error( "Invalid XML: " + data );
		}
		return xml;
	};


	var
		rbracket = /\[\]$/,
		rCRLF = /\r?\n/g,
		rsubmitterTypes = /^(?:submit|button|image|reset|file)$/i,
		rsubmittable = /^(?:input|select|textarea|keygen)/i;

	function buildParams( prefix, obj, traditional, add ) {
		var name;

		if ( Array.isArray( obj ) ) {

			// Serialize array item.
			jQuery.each( obj, function( i, v ) {
				if ( traditional || rbracket.test( prefix ) ) {

					// Treat each array item as a scalar.
					add( prefix, v );

				} else {

					// Item is non-scalar (array or object), encode its numeric index.
					buildParams(
						prefix + "[" + ( typeof v === "object" && v != null ? i : "" ) + "]",
						v,
						traditional,
						add
					);
				}
			} );

		} else if ( !traditional && jQuery.type( obj ) === "object" ) {

			// Serialize object item.
			for ( name in obj ) {
				buildParams( prefix + "[" + name + "]", obj[ name ], traditional, add );
			}

		} else {

			// Serialize scalar item.
			add( prefix, obj );
		}
	}

	// Serialize an array of form elements or a set of
	// key/values into a query string
	jQuery.param = function( a, traditional ) {
		var prefix,
			s = [],
			add = function( key, valueOrFunction ) {

				// If value is a function, invoke it and use its return value
				var value = jQuery.isFunction( valueOrFunction ) ?
					valueOrFunction() :
					valueOrFunction;

				s[ s.length ] = encodeURIComponent( key ) + "=" +
					encodeURIComponent( value == null ? "" : value );
			};

		// If an array was passed in, assume that it is an array of form elements.
		if ( Array.isArray( a ) || ( a.jquery && !jQuery.isPlainObject( a ) ) ) {

			// Serialize the form elements
			jQuery.each( a, function() {
				add( this.name, this.value );
			} );

		} else {

			// If traditional, encode the "old" way (the way 1.3.2 or older
			// did it), otherwise encode params recursively.
			for ( prefix in a ) {
				buildParams( prefix, a[ prefix ], traditional, add );
			}
		}

		// Return the resulting serialization
		return s.join( "&" );
	};

	jQuery.fn.extend( {
		serialize: function() {
			return jQuery.param( this.serializeArray() );
		},
		serializeArray: function() {
			return this.map( function() {

				// Can add propHook for "elements" to filter or add form elements
				var elements = jQuery.prop( this, "elements" );
				return elements ? jQuery.makeArray( elements ) : this;
			} )
			.filter( function() {
				var type = this.type;

				// Use .is( ":disabled" ) so that fieldset[disabled] works
				return this.name && !jQuery( this ).is( ":disabled" ) &&
					rsubmittable.test( this.nodeName ) && !rsubmitterTypes.test( type ) &&
					( this.checked || !rcheckableType.test( type ) );
			} )
			.map( function( i, elem ) {
				var val = jQuery( this ).val();

				if ( val == null ) {
					return null;
				}

				if ( Array.isArray( val ) ) {
					return jQuery.map( val, function( val ) {
						return { name: elem.name, value: val.replace( rCRLF, "\r\n" ) };
					} );
				}

				return { name: elem.name, value: val.replace( rCRLF, "\r\n" ) };
			} ).get();
		}
	} );


	var
		r20 = /%20/g,
		rhash = /#.*$/,
		rantiCache = /([?&])_=[^&]*/,
		rheaders = /^(.*?):[ \t]*([^\r\n]*)$/mg,

		// #7653, #8125, #8152: local protocol detection
		rlocalProtocol = /^(?:about|app|app-storage|.+-extension|file|res|widget):$/,
		rnoContent = /^(?:GET|HEAD)$/,
		rprotocol = /^\/\//,

		/* Prefilters
		 * 1) They are useful to introduce custom dataTypes (see ajax/jsonp.js for an example)
		 * 2) These are called:
		 *    - BEFORE asking for a transport
		 *    - AFTER param serialization (s.data is a string if s.processData is true)
		 * 3) key is the dataType
		 * 4) the catchall symbol "*" can be used
		 * 5) execution will start with transport dataType and THEN continue down to "*" if needed
		 */
		prefilters = {},

		/* Transports bindings
		 * 1) key is the dataType
		 * 2) the catchall symbol "*" can be used
		 * 3) selection will start with transport dataType and THEN go to "*" if needed
		 */
		transports = {},

		// Avoid comment-prolog char sequence (#10098); must appease lint and evade compression
		allTypes = "*/".concat( "*" ),

		// Anchor tag for parsing the document origin
		originAnchor = document.createElement( "a" );
		originAnchor.href = location.href;

	// Base "constructor" for jQuery.ajaxPrefilter and jQuery.ajaxTransport
	function addToPrefiltersOrTransports( structure ) {

		// dataTypeExpression is optional and defaults to "*"
		return function( dataTypeExpression, func ) {

			if ( typeof dataTypeExpression !== "string" ) {
				func = dataTypeExpression;
				dataTypeExpression = "*";
			}

			var dataType,
				i = 0,
				dataTypes = dataTypeExpression.toLowerCase().match( rnothtmlwhite ) || [];

			if ( jQuery.isFunction( func ) ) {

				// For each dataType in the dataTypeExpression
				while ( ( dataType = dataTypes[ i++ ] ) ) {

					// Prepend if requested
					if ( dataType[ 0 ] === "+" ) {
						dataType = dataType.slice( 1 ) || "*";
						( structure[ dataType ] = structure[ dataType ] || [] ).unshift( func );

					// Otherwise append
					} else {
						( structure[ dataType ] = structure[ dataType ] || [] ).push( func );
					}
				}
			}
		};
	}

	// Base inspection function for prefilters and transports
	function inspectPrefiltersOrTransports( structure, options, originalOptions, jqXHR ) {

		var inspected = {},
			seekingTransport = ( structure === transports );

		function inspect( dataType ) {
			var selected;
			inspected[ dataType ] = true;
			jQuery.each( structure[ dataType ] || [], function( _, prefilterOrFactory ) {
				var dataTypeOrTransport = prefilterOrFactory( options, originalOptions, jqXHR );
				if ( typeof dataTypeOrTransport === "string" &&
					!seekingTransport && !inspected[ dataTypeOrTransport ] ) {

					options.dataTypes.unshift( dataTypeOrTransport );
					inspect( dataTypeOrTransport );
					return false;
				} else if ( seekingTransport ) {
					return !( selected = dataTypeOrTransport );
				}
			} );
			return selected;
		}

		return inspect( options.dataTypes[ 0 ] ) || !inspected[ "*" ] && inspect( "*" );
	}

	// A special extend for ajax options
	// that takes "flat" options (not to be deep extended)
	// Fixes #9887
	function ajaxExtend( target, src ) {
		var key, deep,
			flatOptions = jQuery.ajaxSettings.flatOptions || {};

		for ( key in src ) {
			if ( src[ key ] !== undefined ) {
				( flatOptions[ key ] ? target : ( deep || ( deep = {} ) ) )[ key ] = src[ key ];
			}
		}
		if ( deep ) {
			jQuery.extend( true, target, deep );
		}

		return target;
	}

	/* Handles responses to an ajax request:
	 * - finds the right dataType (mediates between content-type and expected dataType)
	 * - returns the corresponding response
	 */
	function ajaxHandleResponses( s, jqXHR, responses ) {

		var ct, type, finalDataType, firstDataType,
			contents = s.contents,
			dataTypes = s.dataTypes;

		// Remove auto dataType and get content-type in the process
		while ( dataTypes[ 0 ] === "*" ) {
			dataTypes.shift();
			if ( ct === undefined ) {
				ct = s.mimeType || jqXHR.getResponseHeader( "Content-Type" );
			}
		}

		// Check if we're dealing with a known content-type
		if ( ct ) {
			for ( type in contents ) {
				if ( contents[ type ] && contents[ type ].test( ct ) ) {
					dataTypes.unshift( type );
					break;
				}
			}
		}

		// Check to see if we have a response for the expected dataType
		if ( dataTypes[ 0 ] in responses ) {
			finalDataType = dataTypes[ 0 ];
		} else {

			// Try convertible dataTypes
			for ( type in responses ) {
				if ( !dataTypes[ 0 ] || s.converters[ type + " " + dataTypes[ 0 ] ] ) {
					finalDataType = type;
					break;
				}
				if ( !firstDataType ) {
					firstDataType = type;
				}
			}

			// Or just use first one
			finalDataType = finalDataType || firstDataType;
		}

		// If we found a dataType
		// We add the dataType to the list if needed
		// and return the corresponding response
		if ( finalDataType ) {
			if ( finalDataType !== dataTypes[ 0 ] ) {
				dataTypes.unshift( finalDataType );
			}
			return responses[ finalDataType ];
		}
	}

	/* Chain conversions given the request and the original response
	 * Also sets the responseXXX fields on the jqXHR instance
	 */
	function ajaxConvert( s, response, jqXHR, isSuccess ) {
		var conv2, current, conv, tmp, prev,
			converters = {},

			// Work with a copy of dataTypes in case we need to modify it for conversion
			dataTypes = s.dataTypes.slice();

		// Create converters map with lowercased keys
		if ( dataTypes[ 1 ] ) {
			for ( conv in s.converters ) {
				converters[ conv.toLowerCase() ] = s.converters[ conv ];
			}
		}

		current = dataTypes.shift();

		// Convert to each sequential dataType
		while ( current ) {

			if ( s.responseFields[ current ] ) {
				jqXHR[ s.responseFields[ current ] ] = response;
			}

			// Apply the dataFilter if provided
			if ( !prev && isSuccess && s.dataFilter ) {
				response = s.dataFilter( response, s.dataType );
			}

			prev = current;
			current = dataTypes.shift();

			if ( current ) {

				// There's only work to do if current dataType is non-auto
				if ( current === "*" ) {

					current = prev;

				// Convert response if prev dataType is non-auto and differs from current
				} else if ( prev !== "*" && prev !== current ) {

					// Seek a direct converter
					conv = converters[ prev + " " + current ] || converters[ "* " + current ];

					// If none found, seek a pair
					if ( !conv ) {
						for ( conv2 in converters ) {

							// If conv2 outputs current
							tmp = conv2.split( " " );
							if ( tmp[ 1 ] === current ) {

								// If prev can be converted to accepted input
								conv = converters[ prev + " " + tmp[ 0 ] ] ||
									converters[ "* " + tmp[ 0 ] ];
								if ( conv ) {

									// Condense equivalence converters
									if ( conv === true ) {
										conv = converters[ conv2 ];

									// Otherwise, insert the intermediate dataType
									} else if ( converters[ conv2 ] !== true ) {
										current = tmp[ 0 ];
										dataTypes.unshift( tmp[ 1 ] );
									}
									break;
								}
							}
						}
					}

					// Apply converter (if not an equivalence)
					if ( conv !== true ) {

						// Unless errors are allowed to bubble, catch and return them
						if ( conv && s.throws ) {
							response = conv( response );
						} else {
							try {
								response = conv( response );
							} catch ( e ) {
								return {
									state: "parsererror",
									error: conv ? e : "No conversion from " + prev + " to " + current
								};
							}
						}
					}
				}
			}
		}

		return { state: "success", data: response };
	}

	jQuery.extend( {

		// Counter for holding the number of active queries
		active: 0,

		// Last-Modified header cache for next request
		lastModified: {},
		etag: {},

		ajaxSettings: {
			url: location.href,
			type: "GET",
			isLocal: rlocalProtocol.test( location.protocol ),
			global: true,
			processData: true,
			async: true,
			contentType: "application/x-www-form-urlencoded; charset=UTF-8",

			/*
			timeout: 0,
			data: null,
			dataType: null,
			username: null,
			password: null,
			cache: null,
			throws: false,
			traditional: false,
			headers: {},
			*/

			accepts: {
				"*": allTypes,
				text: "text/plain",
				html: "text/html",
				xml: "application/xml, text/xml",
				json: "application/json, text/javascript"
			},

			contents: {
				xml: /\bxml\b/,
				html: /\bhtml/,
				json: /\bjson\b/
			},

			responseFields: {
				xml: "responseXML",
				text: "responseText",
				json: "responseJSON"
			},

			// Data converters
			// Keys separate source (or catchall "*") and destination types with a single space
			converters: {

				// Convert anything to text
				"* text": String,

				// Text to html (true = no transformation)
				"text html": true,

				// Evaluate text as a json expression
				"text json": JSON.parse,

				// Parse text as xml
				"text xml": jQuery.parseXML
			},

			// For options that shouldn't be deep extended:
			// you can add your own custom options here if
			// and when you create one that shouldn't be
			// deep extended (see ajaxExtend)
			flatOptions: {
				url: true,
				context: true
			}
		},

		// Creates a full fledged settings object into target
		// with both ajaxSettings and settings fields.
		// If target is omitted, writes into ajaxSettings.
		ajaxSetup: function( target, settings ) {
			return settings ?

				// Building a settings object
				ajaxExtend( ajaxExtend( target, jQuery.ajaxSettings ), settings ) :

				// Extending ajaxSettings
				ajaxExtend( jQuery.ajaxSettings, target );
		},

		ajaxPrefilter: addToPrefiltersOrTransports( prefilters ),
		ajaxTransport: addToPrefiltersOrTransports( transports ),

		// Main method
		ajax: function( url, options ) {

			// If url is an object, simulate pre-1.5 signature
			if ( typeof url === "object" ) {
				options = url;
				url = undefined;
			}

			// Force options to be an object
			options = options || {};

			var transport,

				// URL without anti-cache param
				cacheURL,

				// Response headers
				responseHeadersString,
				responseHeaders,

				// timeout handle
				timeoutTimer,

				// Url cleanup var
				urlAnchor,

				// Request state (becomes false upon send and true upon completion)
				completed,

				// To know if global events are to be dispatched
				fireGlobals,

				// Loop variable
				i,

				// uncached part of the url
				uncached,

				// Create the final options object
				s = jQuery.ajaxSetup( {}, options ),

				// Callbacks context
				callbackContext = s.context || s,

				// Context for global events is callbackContext if it is a DOM node or jQuery collection
				globalEventContext = s.context &&
					( callbackContext.nodeType || callbackContext.jquery ) ?
						jQuery( callbackContext ) :
						jQuery.event,

				// Deferreds
				deferred = jQuery.Deferred(),
				completeDeferred = jQuery.Callbacks( "once memory" ),

				// Status-dependent callbacks
				statusCode = s.statusCode || {},

				// Headers (they are sent all at once)
				requestHeaders = {},
				requestHeadersNames = {},

				// Default abort message
				strAbort = "canceled",

				// Fake xhr
				jqXHR = {
					readyState: 0,

					// Builds headers hashtable if needed
					getResponseHeader: function( key ) {
						var match;
						if ( completed ) {
							if ( !responseHeaders ) {
								responseHeaders = {};
								while ( ( match = rheaders.exec( responseHeadersString ) ) ) {
									responseHeaders[ match[ 1 ].toLowerCase() ] = match[ 2 ];
								}
							}
							match = responseHeaders[ key.toLowerCase() ];
						}
						return match == null ? null : match;
					},

					// Raw string
					getAllResponseHeaders: function() {
						return completed ? responseHeadersString : null;
					},

					// Caches the header
					setRequestHeader: function( name, value ) {
						if ( completed == null ) {
							name = requestHeadersNames[ name.toLowerCase() ] =
								requestHeadersNames[ name.toLowerCase() ] || name;
							requestHeaders[ name ] = value;
						}
						return this;
					},

					// Overrides response content-type header
					overrideMimeType: function( type ) {
						if ( completed == null ) {
							s.mimeType = type;
						}
						return this;
					},

					// Status-dependent callbacks
					statusCode: function( map ) {
						var code;
						if ( map ) {
							if ( completed ) {

								// Execute the appropriate callbacks
								jqXHR.always( map[ jqXHR.status ] );
							} else {

								// Lazy-add the new callbacks in a way that preserves old ones
								for ( code in map ) {
									statusCode[ code ] = [ statusCode[ code ], map[ code ] ];
								}
							}
						}
						return this;
					},

					// Cancel the request
					abort: function( statusText ) {
						var finalText = statusText || strAbort;
						if ( transport ) {
							transport.abort( finalText );
						}
						done( 0, finalText );
						return this;
					}
				};

			// Attach deferreds
			deferred.promise( jqXHR );

			// Add protocol if not provided (prefilters might expect it)
			// Handle falsy url in the settings object (#10093: consistency with old signature)
			// We also use the url parameter if available
			s.url = ( ( url || s.url || location.href ) + "" )
				.replace( rprotocol, location.protocol + "//" );

			// Alias method option to type as per ticket #12004
			s.type = options.method || options.type || s.method || s.type;

			// Extract dataTypes list
			s.dataTypes = ( s.dataType || "*" ).toLowerCase().match( rnothtmlwhite ) || [ "" ];

			// A cross-domain request is in order when the origin doesn't match the current origin.
			if ( s.crossDomain == null ) {
				urlAnchor = document.createElement( "a" );

				// Support: IE <=8 - 11, Edge 12 - 13
				// IE throws exception on accessing the href property if url is malformed,
				// e.g. http://example.com:80x/
				try {
					urlAnchor.href = s.url;

					// Support: IE <=8 - 11 only
					// Anchor's host property isn't correctly set when s.url is relative
					urlAnchor.href = urlAnchor.href;
					s.crossDomain = originAnchor.protocol + "//" + originAnchor.host !==
						urlAnchor.protocol + "//" + urlAnchor.host;
				} catch ( e ) {

					// If there is an error parsing the URL, assume it is crossDomain,
					// it can be rejected by the transport if it is invalid
					s.crossDomain = true;
				}
			}

			// Convert data if not already a string
			if ( s.data && s.processData && typeof s.data !== "string" ) {
				s.data = jQuery.param( s.data, s.traditional );
			}

			// Apply prefilters
			inspectPrefiltersOrTransports( prefilters, s, options, jqXHR );

			// If request was aborted inside a prefilter, stop there
			if ( completed ) {
				return jqXHR;
			}

			// We can fire global events as of now if asked to
			// Don't fire events if jQuery.event is undefined in an AMD-usage scenario (#15118)
			fireGlobals = jQuery.event && s.global;

			// Watch for a new set of requests
			if ( fireGlobals && jQuery.active++ === 0 ) {
				jQuery.event.trigger( "ajaxStart" );
			}

			// Uppercase the type
			s.type = s.type.toUpperCase();

			// Determine if request has content
			s.hasContent = !rnoContent.test( s.type );

			// Save the URL in case we're toying with the If-Modified-Since
			// and/or If-None-Match header later on
			// Remove hash to simplify url manipulation
			cacheURL = s.url.replace( rhash, "" );

			// More options handling for requests with no content
			if ( !s.hasContent ) {

				// Remember the hash so we can put it back
				uncached = s.url.slice( cacheURL.length );

				// If data is available, append data to url
				if ( s.data ) {
					cacheURL += ( rquery.test( cacheURL ) ? "&" : "?" ) + s.data;

					// #9682: remove data so that it's not used in an eventual retry
					delete s.data;
				}

				// Add or update anti-cache param if needed
				if ( s.cache === false ) {
					cacheURL = cacheURL.replace( rantiCache, "$1" );
					uncached = ( rquery.test( cacheURL ) ? "&" : "?" ) + "_=" + ( nonce++ ) + uncached;
				}

				// Put hash and anti-cache on the URL that will be requested (gh-1732)
				s.url = cacheURL + uncached;

			// Change '%20' to '+' if this is encoded form body content (gh-2658)
			} else if ( s.data && s.processData &&
				( s.contentType || "" ).indexOf( "application/x-www-form-urlencoded" ) === 0 ) {
				s.data = s.data.replace( r20, "+" );
			}

			// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
			if ( s.ifModified ) {
				if ( jQuery.lastModified[ cacheURL ] ) {
					jqXHR.setRequestHeader( "If-Modified-Since", jQuery.lastModified[ cacheURL ] );
				}
				if ( jQuery.etag[ cacheURL ] ) {
					jqXHR.setRequestHeader( "If-None-Match", jQuery.etag[ cacheURL ] );
				}
			}

			// Set the correct header, if data is being sent
			if ( s.data && s.hasContent && s.contentType !== false || options.contentType ) {
				jqXHR.setRequestHeader( "Content-Type", s.contentType );
			}

			// Set the Accepts header for the server, depending on the dataType
			jqXHR.setRequestHeader(
				"Accept",
				s.dataTypes[ 0 ] && s.accepts[ s.dataTypes[ 0 ] ] ?
					s.accepts[ s.dataTypes[ 0 ] ] +
						( s.dataTypes[ 0 ] !== "*" ? ", " + allTypes + "; q=0.01" : "" ) :
					s.accepts[ "*" ]
			);

			// Check for headers option
			for ( i in s.headers ) {
				jqXHR.setRequestHeader( i, s.headers[ i ] );
			}

			// Allow custom headers/mimetypes and early abort
			if ( s.beforeSend &&
				( s.beforeSend.call( callbackContext, jqXHR, s ) === false || completed ) ) {

				// Abort if not done already and return
				return jqXHR.abort();
			}

			// Aborting is no longer a cancellation
			strAbort = "abort";

			// Install callbacks on deferreds
			completeDeferred.add( s.complete );
			jqXHR.done( s.success );
			jqXHR.fail( s.error );

			// Get transport
			transport = inspectPrefiltersOrTransports( transports, s, options, jqXHR );

			// If no transport, we auto-abort
			if ( !transport ) {
				done( -1, "No Transport" );
			} else {
				jqXHR.readyState = 1;

				// Send global event
				if ( fireGlobals ) {
					globalEventContext.trigger( "ajaxSend", [ jqXHR, s ] );
				}

				// If request was aborted inside ajaxSend, stop there
				if ( completed ) {
					return jqXHR;
				}

				// Timeout
				if ( s.async && s.timeout > 0 ) {
					timeoutTimer = window.setTimeout( function() {
						jqXHR.abort( "timeout" );
					}, s.timeout );
				}

				try {
					completed = false;
					transport.send( requestHeaders, done );
				} catch ( e ) {

					// Rethrow post-completion exceptions
					if ( completed ) {
						throw e;
					}

					// Propagate others as results
					done( -1, e );
				}
			}

			// Callback for when everything is done
			function done( status, nativeStatusText, responses, headers ) {
				var isSuccess, success, error, response, modified,
					statusText = nativeStatusText;

				// Ignore repeat invocations
				if ( completed ) {
					return;
				}

				completed = true;

				// Clear timeout if it exists
				if ( timeoutTimer ) {
					window.clearTimeout( timeoutTimer );
				}

				// Dereference transport for early garbage collection
				// (no matter how long the jqXHR object will be used)
				transport = undefined;

				// Cache response headers
				responseHeadersString = headers || "";

				// Set readyState
				jqXHR.readyState = status > 0 ? 4 : 0;

				// Determine if successful
				isSuccess = status >= 200 && status < 300 || status === 304;

				// Get response data
				if ( responses ) {
					response = ajaxHandleResponses( s, jqXHR, responses );
				}

				// Convert no matter what (that way responseXXX fields are always set)
				response = ajaxConvert( s, response, jqXHR, isSuccess );

				// If successful, handle type chaining
				if ( isSuccess ) {

					// Set the If-Modified-Since and/or If-None-Match header, if in ifModified mode.
					if ( s.ifModified ) {
						modified = jqXHR.getResponseHeader( "Last-Modified" );
						if ( modified ) {
							jQuery.lastModified[ cacheURL ] = modified;
						}
						modified = jqXHR.getResponseHeader( "etag" );
						if ( modified ) {
							jQuery.etag[ cacheURL ] = modified;
						}
					}

					// if no content
					if ( status === 204 || s.type === "HEAD" ) {
						statusText = "nocontent";

					// if not modified
					} else if ( status === 304 ) {
						statusText = "notmodified";

					// If we have data, let's convert it
					} else {
						statusText = response.state;
						success = response.data;
						error = response.error;
						isSuccess = !error;
					}
				} else {

					// Extract error from statusText and normalize for non-aborts
					error = statusText;
					if ( status || !statusText ) {
						statusText = "error";
						if ( status < 0 ) {
							status = 0;
						}
					}
				}

				// Set data for the fake xhr object
				jqXHR.status = status;
				jqXHR.statusText = ( nativeStatusText || statusText ) + "";

				// Success/Error
				if ( isSuccess ) {
					deferred.resolveWith( callbackContext, [ success, statusText, jqXHR ] );
				} else {
					deferred.rejectWith( callbackContext, [ jqXHR, statusText, error ] );
				}

				// Status-dependent callbacks
				jqXHR.statusCode( statusCode );
				statusCode = undefined;

				if ( fireGlobals ) {
					globalEventContext.trigger( isSuccess ? "ajaxSuccess" : "ajaxError",
						[ jqXHR, s, isSuccess ? success : error ] );
				}

				// Complete
				completeDeferred.fireWith( callbackContext, [ jqXHR, statusText ] );

				if ( fireGlobals ) {
					globalEventContext.trigger( "ajaxComplete", [ jqXHR, s ] );

					// Handle the global AJAX counter
					if ( !( --jQuery.active ) ) {
						jQuery.event.trigger( "ajaxStop" );
					}
				}
			}

			return jqXHR;
		},

		getJSON: function( url, data, callback ) {
			return jQuery.get( url, data, callback, "json" );
		},

		getScript: function( url, callback ) {
			return jQuery.get( url, undefined, callback, "script" );
		}
	} );

	jQuery.each( [ "get", "post" ], function( i, method ) {
		jQuery[ method ] = function( url, data, callback, type ) {

			// Shift arguments if data argument was omitted
			if ( jQuery.isFunction( data ) ) {
				type = type || callback;
				callback = data;
				data = undefined;
			}

			// The url can be an options object (which then must have .url)
			return jQuery.ajax( jQuery.extend( {
				url: url,
				type: method,
				dataType: type,
				data: data,
				success: callback
			}, jQuery.isPlainObject( url ) && url ) );
		};
	} );


	jQuery._evalUrl = function( url ) {
		return jQuery.ajax( {
			url: url,

			// Make this explicit, since user can override this through ajaxSetup (#11264)
			type: "GET",
			dataType: "script",
			cache: true,
			async: false,
			global: false,
			"throws": true
		} );
	};


	jQuery.fn.extend( {
		wrapAll: function( html ) {
			var wrap;

			if ( this[ 0 ] ) {
				if ( jQuery.isFunction( html ) ) {
					html = html.call( this[ 0 ] );
				}

				// The elements to wrap the target around
				wrap = jQuery( html, this[ 0 ].ownerDocument ).eq( 0 ).clone( true );

				if ( this[ 0 ].parentNode ) {
					wrap.insertBefore( this[ 0 ] );
				}

				wrap.map( function() {
					var elem = this;

					while ( elem.firstElementChild ) {
						elem = elem.firstElementChild;
					}

					return elem;
				} ).append( this );
			}

			return this;
		},

		wrapInner: function( html ) {
			if ( jQuery.isFunction( html ) ) {
				return this.each( function( i ) {
					jQuery( this ).wrapInner( html.call( this, i ) );
				} );
			}

			return this.each( function() {
				var self = jQuery( this ),
					contents = self.contents();

				if ( contents.length ) {
					contents.wrapAll( html );

				} else {
					self.append( html );
				}
			} );
		},

		wrap: function( html ) {
			var isFunction = jQuery.isFunction( html );

			return this.each( function( i ) {
				jQuery( this ).wrapAll( isFunction ? html.call( this, i ) : html );
			} );
		},

		unwrap: function( selector ) {
			this.parent( selector ).not( "body" ).each( function() {
				jQuery( this ).replaceWith( this.childNodes );
			} );
			return this;
		}
	} );


	jQuery.expr.pseudos.hidden = function( elem ) {
		return !jQuery.expr.pseudos.visible( elem );
	};
	jQuery.expr.pseudos.visible = function( elem ) {
		return !!( elem.offsetWidth || elem.offsetHeight || elem.getClientRects().length );
	};




	jQuery.ajaxSettings.xhr = function() {
		try {
			return new window.XMLHttpRequest();
		} catch ( e ) {}
	};

	var xhrSuccessStatus = {

			// File protocol always yields status code 0, assume 200
			0: 200,

			// Support: IE <=9 only
			// #1450: sometimes IE returns 1223 when it should be 204
			1223: 204
		},
		xhrSupported = jQuery.ajaxSettings.xhr();

	support.cors = !!xhrSupported && ( "withCredentials" in xhrSupported );
	support.ajax = xhrSupported = !!xhrSupported;

	jQuery.ajaxTransport( function( options ) {
		var callback, errorCallback;

		// Cross domain only allowed if supported through XMLHttpRequest
		if ( support.cors || xhrSupported && !options.crossDomain ) {
			return {
				send: function( headers, complete ) {
					var i,
						xhr = options.xhr();

					xhr.open(
						options.type,
						options.url,
						options.async,
						options.username,
						options.password
					);

					// Apply custom fields if provided
					if ( options.xhrFields ) {
						for ( i in options.xhrFields ) {
							xhr[ i ] = options.xhrFields[ i ];
						}
					}

					// Override mime type if needed
					if ( options.mimeType && xhr.overrideMimeType ) {
						xhr.overrideMimeType( options.mimeType );
					}

					// X-Requested-With header
					// For cross-domain requests, seeing as conditions for a preflight are
					// akin to a jigsaw puzzle, we simply never set it to be sure.
					// (it can always be set on a per-request basis or even using ajaxSetup)
					// For same-domain requests, won't change header if already provided.
					if ( !options.crossDomain && !headers[ "X-Requested-With" ] ) {
						headers[ "X-Requested-With" ] = "XMLHttpRequest";
					}

					// Set headers
					for ( i in headers ) {
						xhr.setRequestHeader( i, headers[ i ] );
					}

					// Callback
					callback = function( type ) {
						return function() {
							if ( callback ) {
								callback = errorCallback = xhr.onload =
									xhr.onerror = xhr.onabort = xhr.onreadystatechange = null;

								if ( type === "abort" ) {
									xhr.abort();
								} else if ( type === "error" ) {

									// Support: IE <=9 only
									// On a manual native abort, IE9 throws
									// errors on any property access that is not readyState
									if ( typeof xhr.status !== "number" ) {
										complete( 0, "error" );
									} else {
										complete(

											// File: protocol always yields status 0; see #8605, #14207
											xhr.status,
											xhr.statusText
										);
									}
								} else {
									complete(
										xhrSuccessStatus[ xhr.status ] || xhr.status,
										xhr.statusText,

										// Support: IE <=9 only
										// IE9 has no XHR2 but throws on binary (trac-11426)
										// For XHR2 non-text, let the caller handle it (gh-2498)
										( xhr.responseType || "text" ) !== "text"  ||
										typeof xhr.responseText !== "string" ?
											{ binary: xhr.response } :
											{ text: xhr.responseText },
										xhr.getAllResponseHeaders()
									);
								}
							}
						};
					};

					// Listen to events
					xhr.onload = callback();
					errorCallback = xhr.onerror = callback( "error" );

					// Support: IE 9 only
					// Use onreadystatechange to replace onabort
					// to handle uncaught aborts
					if ( xhr.onabort !== undefined ) {
						xhr.onabort = errorCallback;
					} else {
						xhr.onreadystatechange = function() {

							// Check readyState before timeout as it changes
							if ( xhr.readyState === 4 ) {

								// Allow onerror to be called first,
								// but that will not handle a native abort
								// Also, save errorCallback to a variable
								// as xhr.onerror cannot be accessed
								window.setTimeout( function() {
									if ( callback ) {
										errorCallback();
									}
								} );
							}
						};
					}

					// Create the abort callback
					callback = callback( "abort" );

					try {

						// Do send the request (this may raise an exception)
						xhr.send( options.hasContent && options.data || null );
					} catch ( e ) {

						// #14683: Only rethrow if this hasn't been notified as an error yet
						if ( callback ) {
							throw e;
						}
					}
				},

				abort: function() {
					if ( callback ) {
						callback();
					}
				}
			};
		}
	} );




	// Prevent auto-execution of scripts when no explicit dataType was provided (See gh-2432)
	jQuery.ajaxPrefilter( function( s ) {
		if ( s.crossDomain ) {
			s.contents.script = false;
		}
	} );

	// Install script dataType
	jQuery.ajaxSetup( {
		accepts: {
			script: "text/javascript, application/javascript, " +
				"application/ecmascript, application/x-ecmascript"
		},
		contents: {
			script: /\b(?:java|ecma)script\b/
		},
		converters: {
			"text script": function( text ) {
				jQuery.globalEval( text );
				return text;
			}
		}
	} );

	// Handle cache's special case and crossDomain
	jQuery.ajaxPrefilter( "script", function( s ) {
		if ( s.cache === undefined ) {
			s.cache = false;
		}
		if ( s.crossDomain ) {
			s.type = "GET";
		}
	} );

	// Bind script tag hack transport
	jQuery.ajaxTransport( "script", function( s ) {

		// This transport only deals with cross domain requests
		if ( s.crossDomain ) {
			var script, callback;
			return {
				send: function( _, complete ) {
					script = jQuery( "<script>" ).prop( {
						charset: s.scriptCharset,
						src: s.url
					} ).on(
						"load error",
						callback = function( evt ) {
							script.remove();
							callback = null;
							if ( evt ) {
								complete( evt.type === "error" ? 404 : 200, evt.type );
							}
						}
					);

					// Use native DOM manipulation to avoid our domManip AJAX trickery
					document.head.appendChild( script[ 0 ] );
				},
				abort: function() {
					if ( callback ) {
						callback();
					}
				}
			};
		}
	} );




	var oldCallbacks = [],
		rjsonp = /(=)\?(?=&|$)|\?\?/;

	// Default jsonp settings
	jQuery.ajaxSetup( {
		jsonp: "callback",
		jsonpCallback: function() {
			var callback = oldCallbacks.pop() || ( jQuery.expando + "_" + ( nonce++ ) );
			this[ callback ] = true;
			return callback;
		}
	} );

	// Detect, normalize options and install callbacks for jsonp requests
	jQuery.ajaxPrefilter( "json jsonp", function( s, originalSettings, jqXHR ) {

		var callbackName, overwritten, responseContainer,
			jsonProp = s.jsonp !== false && ( rjsonp.test( s.url ) ?
				"url" :
				typeof s.data === "string" &&
					( s.contentType || "" )
						.indexOf( "application/x-www-form-urlencoded" ) === 0 &&
					rjsonp.test( s.data ) && "data"
			);

		// Handle iff the expected data type is "jsonp" or we have a parameter to set
		if ( jsonProp || s.dataTypes[ 0 ] === "jsonp" ) {

			// Get callback name, remembering preexisting value associated with it
			callbackName = s.jsonpCallback = jQuery.isFunction( s.jsonpCallback ) ?
				s.jsonpCallback() :
				s.jsonpCallback;

			// Insert callback into url or form data
			if ( jsonProp ) {
				s[ jsonProp ] = s[ jsonProp ].replace( rjsonp, "$1" + callbackName );
			} else if ( s.jsonp !== false ) {
				s.url += ( rquery.test( s.url ) ? "&" : "?" ) + s.jsonp + "=" + callbackName;
			}

			// Use data converter to retrieve json after script execution
			s.converters[ "script json" ] = function() {
				if ( !responseContainer ) {
					jQuery.error( callbackName + " was not called" );
				}
				return responseContainer[ 0 ];
			};

			// Force json dataType
			s.dataTypes[ 0 ] = "json";

			// Install callback
			overwritten = window[ callbackName ];
			window[ callbackName ] = function() {
				responseContainer = arguments;
			};

			// Clean-up function (fires after converters)
			jqXHR.always( function() {

				// If previous value didn't exist - remove it
				if ( overwritten === undefined ) {
					jQuery( window ).removeProp( callbackName );

				// Otherwise restore preexisting value
				} else {
					window[ callbackName ] = overwritten;
				}

				// Save back as free
				if ( s[ callbackName ] ) {

					// Make sure that re-using the options doesn't screw things around
					s.jsonpCallback = originalSettings.jsonpCallback;

					// Save the callback name for future use
					oldCallbacks.push( callbackName );
				}

				// Call if it was a function and we have a response
				if ( responseContainer && jQuery.isFunction( overwritten ) ) {
					overwritten( responseContainer[ 0 ] );
				}

				responseContainer = overwritten = undefined;
			} );

			// Delegate to script
			return "script";
		}
	} );




	// Support: Safari 8 only
	// In Safari 8 documents created via document.implementation.createHTMLDocument
	// collapse sibling forms: the second one becomes a child of the first one.
	// Because of that, this security measure has to be disabled in Safari 8.
	// https://bugs.webkit.org/show_bug.cgi?id=137337
	support.createHTMLDocument = ( function() {
		var body = document.implementation.createHTMLDocument( "" ).body;
		body.innerHTML = "<form></form><form></form>";
		return body.childNodes.length === 2;
	} )();


	// Argument "data" should be string of html
	// context (optional): If specified, the fragment will be created in this context,
	// defaults to document
	// keepScripts (optional): If true, will include scripts passed in the html string
	jQuery.parseHTML = function( data, context, keepScripts ) {
		if ( typeof data !== "string" ) {
			return [];
		}
		if ( typeof context === "boolean" ) {
			keepScripts = context;
			context = false;
		}

		var base, parsed, scripts;

		if ( !context ) {

			// Stop scripts or inline event handlers from being executed immediately
			// by using document.implementation
			if ( support.createHTMLDocument ) {
				context = document.implementation.createHTMLDocument( "" );

				// Set the base href for the created document
				// so any parsed elements with URLs
				// are based on the document's URL (gh-2965)
				base = context.createElement( "base" );
				base.href = document.location.href;
				context.head.appendChild( base );
			} else {
				context = document;
			}
		}

		parsed = rsingleTag.exec( data );
		scripts = !keepScripts && [];

		// Single tag
		if ( parsed ) {
			return [ context.createElement( parsed[ 1 ] ) ];
		}

		parsed = buildFragment( [ data ], context, scripts );

		if ( scripts && scripts.length ) {
			jQuery( scripts ).remove();
		}

		return jQuery.merge( [], parsed.childNodes );
	};


	/**
	 * Load a url into a page
	 */
	jQuery.fn.load = function( url, params, callback ) {
		var selector, type, response,
			self = this,
			off = url.indexOf( " " );

		if ( off > -1 ) {
			selector = stripAndCollapse( url.slice( off ) );
			url = url.slice( 0, off );
		}

		// If it's a function
		if ( jQuery.isFunction( params ) ) {

			// We assume that it's the callback
			callback = params;
			params = undefined;

		// Otherwise, build a param string
		} else if ( params && typeof params === "object" ) {
			type = "POST";
		}

		// If we have elements to modify, make the request
		if ( self.length > 0 ) {
			jQuery.ajax( {
				url: url,

				// If "type" variable is undefined, then "GET" method will be used.
				// Make value of this field explicit since
				// user can override it through ajaxSetup method
				type: type || "GET",
				dataType: "html",
				data: params
			} ).done( function( responseText ) {

				// Save response for use in complete callback
				response = arguments;

				self.html( selector ?

					// If a selector was specified, locate the right elements in a dummy div
					// Exclude scripts to avoid IE 'Permission Denied' errors
					jQuery( "<div>" ).append( jQuery.parseHTML( responseText ) ).find( selector ) :

					// Otherwise use the full result
					responseText );

			// If the request succeeds, this function gets "data", "status", "jqXHR"
			// but they are ignored because response was set above.
			// If it fails, this function gets "jqXHR", "status", "error"
			} ).always( callback && function( jqXHR, status ) {
				self.each( function() {
					callback.apply( this, response || [ jqXHR.responseText, status, jqXHR ] );
				} );
			} );
		}

		return this;
	};




	// Attach a bunch of functions for handling common AJAX events
	jQuery.each( [
		"ajaxStart",
		"ajaxStop",
		"ajaxComplete",
		"ajaxError",
		"ajaxSuccess",
		"ajaxSend"
	], function( i, type ) {
		jQuery.fn[ type ] = function( fn ) {
			return this.on( type, fn );
		};
	} );




	jQuery.expr.pseudos.animated = function( elem ) {
		return jQuery.grep( jQuery.timers, function( fn ) {
			return elem === fn.elem;
		} ).length;
	};




	jQuery.offset = {
		setOffset: function( elem, options, i ) {
			var curPosition, curLeft, curCSSTop, curTop, curOffset, curCSSLeft, calculatePosition,
				position = jQuery.css( elem, "position" ),
				curElem = jQuery( elem ),
				props = {};

			// Set position first, in-case top/left are set even on static elem
			if ( position === "static" ) {
				elem.style.position = "relative";
			}

			curOffset = curElem.offset();
			curCSSTop = jQuery.css( elem, "top" );
			curCSSLeft = jQuery.css( elem, "left" );
			calculatePosition = ( position === "absolute" || position === "fixed" ) &&
				( curCSSTop + curCSSLeft ).indexOf( "auto" ) > -1;

			// Need to be able to calculate position if either
			// top or left is auto and position is either absolute or fixed
			if ( calculatePosition ) {
				curPosition = curElem.position();
				curTop = curPosition.top;
				curLeft = curPosition.left;

			} else {
				curTop = parseFloat( curCSSTop ) || 0;
				curLeft = parseFloat( curCSSLeft ) || 0;
			}

			if ( jQuery.isFunction( options ) ) {

				// Use jQuery.extend here to allow modification of coordinates argument (gh-1848)
				options = options.call( elem, i, jQuery.extend( {}, curOffset ) );
			}

			if ( options.top != null ) {
				props.top = ( options.top - curOffset.top ) + curTop;
			}
			if ( options.left != null ) {
				props.left = ( options.left - curOffset.left ) + curLeft;
			}

			if ( "using" in options ) {
				options.using.call( elem, props );

			} else {
				curElem.css( props );
			}
		}
	};

	jQuery.fn.extend( {
		offset: function( options ) {

			// Preserve chaining for setter
			if ( arguments.length ) {
				return options === undefined ?
					this :
					this.each( function( i ) {
						jQuery.offset.setOffset( this, options, i );
					} );
			}

			var doc, docElem, rect, win,
				elem = this[ 0 ];

			if ( !elem ) {
				return;
			}

			// Return zeros for disconnected and hidden (display: none) elements (gh-2310)
			// Support: IE <=11 only
			// Running getBoundingClientRect on a
			// disconnected node in IE throws an error
			if ( !elem.getClientRects().length ) {
				return { top: 0, left: 0 };
			}

			rect = elem.getBoundingClientRect();

			doc = elem.ownerDocument;
			docElem = doc.documentElement;
			win = doc.defaultView;

			return {
				top: rect.top + win.pageYOffset - docElem.clientTop,
				left: rect.left + win.pageXOffset - docElem.clientLeft
			};
		},

		position: function() {
			if ( !this[ 0 ] ) {
				return;
			}

			var offsetParent, offset,
				elem = this[ 0 ],
				parentOffset = { top: 0, left: 0 };

			// Fixed elements are offset from window (parentOffset = {top:0, left: 0},
			// because it is its only offset parent
			if ( jQuery.css( elem, "position" ) === "fixed" ) {

				// Assume getBoundingClientRect is there when computed position is fixed
				offset = elem.getBoundingClientRect();

			} else {

				// Get *real* offsetParent
				offsetParent = this.offsetParent();

				// Get correct offsets
				offset = this.offset();
				if ( !nodeName( offsetParent[ 0 ], "html" ) ) {
					parentOffset = offsetParent.offset();
				}

				// Add offsetParent borders
				parentOffset = {
					top: parentOffset.top + jQuery.css( offsetParent[ 0 ], "borderTopWidth", true ),
					left: parentOffset.left + jQuery.css( offsetParent[ 0 ], "borderLeftWidth", true )
				};
			}

			// Subtract parent offsets and element margins
			return {
				top: offset.top - parentOffset.top - jQuery.css( elem, "marginTop", true ),
				left: offset.left - parentOffset.left - jQuery.css( elem, "marginLeft", true )
			};
		},

		// This method will return documentElement in the following cases:
		// 1) For the element inside the iframe without offsetParent, this method will return
		//    documentElement of the parent window
		// 2) For the hidden or detached element
		// 3) For body or html element, i.e. in case of the html node - it will return itself
		//
		// but those exceptions were never presented as a real life use-cases
		// and might be considered as more preferable results.
		//
		// This logic, however, is not guaranteed and can change at any point in the future
		offsetParent: function() {
			return this.map( function() {
				var offsetParent = this.offsetParent;

				while ( offsetParent && jQuery.css( offsetParent, "position" ) === "static" ) {
					offsetParent = offsetParent.offsetParent;
				}

				return offsetParent || documentElement;
			} );
		}
	} );

	// Create scrollLeft and scrollTop methods
	jQuery.each( { scrollLeft: "pageXOffset", scrollTop: "pageYOffset" }, function( method, prop ) {
		var top = "pageYOffset" === prop;

		jQuery.fn[ method ] = function( val ) {
			return access( this, function( elem, method, val ) {

				// Coalesce documents and windows
				var win;
				if ( jQuery.isWindow( elem ) ) {
					win = elem;
				} else if ( elem.nodeType === 9 ) {
					win = elem.defaultView;
				}

				if ( val === undefined ) {
					return win ? win[ prop ] : elem[ method ];
				}

				if ( win ) {
					win.scrollTo(
						!top ? val : win.pageXOffset,
						top ? val : win.pageYOffset
					);

				} else {
					elem[ method ] = val;
				}
			}, method, val, arguments.length );
		};
	} );

	// Support: Safari <=7 - 9.1, Chrome <=37 - 49
	// Add the top/left cssHooks using jQuery.fn.position
	// Webkit bug: https://bugs.webkit.org/show_bug.cgi?id=29084
	// Blink bug: https://bugs.chromium.org/p/chromium/issues/detail?id=589347
	// getComputedStyle returns percent when specified for top/left/bottom/right;
	// rather than make the css module depend on the offset module, just check for it here
	jQuery.each( [ "top", "left" ], function( i, prop ) {
		jQuery.cssHooks[ prop ] = addGetHookIf( support.pixelPosition,
			function( elem, computed ) {
				if ( computed ) {
					computed = curCSS( elem, prop );

					// If curCSS returns percentage, fallback to offset
					return rnumnonpx.test( computed ) ?
						jQuery( elem ).position()[ prop ] + "px" :
						computed;
				}
			}
		);
	} );


	// Create innerHeight, innerWidth, height, width, outerHeight and outerWidth methods
	jQuery.each( { Height: "height", Width: "width" }, function( name, type ) {
		jQuery.each( { padding: "inner" + name, content: type, "": "outer" + name },
			function( defaultExtra, funcName ) {

			// Margin is only for outerHeight, outerWidth
			jQuery.fn[ funcName ] = function( margin, value ) {
				var chainable = arguments.length && ( defaultExtra || typeof margin !== "boolean" ),
					extra = defaultExtra || ( margin === true || value === true ? "margin" : "border" );

				return access( this, function( elem, type, value ) {
					var doc;

					if ( jQuery.isWindow( elem ) ) {

						// $( window ).outerWidth/Height return w/h including scrollbars (gh-1729)
						return funcName.indexOf( "outer" ) === 0 ?
							elem[ "inner" + name ] :
							elem.document.documentElement[ "client" + name ];
					}

					// Get document width or height
					if ( elem.nodeType === 9 ) {
						doc = elem.documentElement;

						// Either scroll[Width/Height] or offset[Width/Height] or client[Width/Height],
						// whichever is greatest
						return Math.max(
							elem.body[ "scroll" + name ], doc[ "scroll" + name ],
							elem.body[ "offset" + name ], doc[ "offset" + name ],
							doc[ "client" + name ]
						);
					}

					return value === undefined ?

						// Get width or height on the element, requesting but not forcing parseFloat
						jQuery.css( elem, type, extra ) :

						// Set width or height on the element
						jQuery.style( elem, type, value, extra );
				}, type, chainable ? margin : undefined, chainable );
			};
		} );
	} );


	jQuery.fn.extend( {

		bind: function( types, data, fn ) {
			return this.on( types, null, data, fn );
		},
		unbind: function( types, fn ) {
			return this.off( types, null, fn );
		},

		delegate: function( selector, types, data, fn ) {
			return this.on( types, selector, data, fn );
		},
		undelegate: function( selector, types, fn ) {

			// ( namespace ) or ( selector, types [, fn] )
			return arguments.length === 1 ?
				this.off( selector, "**" ) :
				this.off( types, selector || "**", fn );
		}
	} );

	jQuery.holdReady = function( hold ) {
		if ( hold ) {
			jQuery.readyWait++;
		} else {
			jQuery.ready( true );
		}
	};
	jQuery.isArray = Array.isArray;
	jQuery.parseJSON = JSON.parse;
	jQuery.nodeName = nodeName;




	// Register as a named AMD module, since jQuery can be concatenated with other
	// files that may use define, but not via a proper concatenation script that
	// understands anonymous AMD modules. A named AMD is safest and most robust
	// way to register. Lowercase jquery is used because AMD module names are
	// derived from file names, and jQuery is normally delivered in a lowercase
	// file name. Do this after creating the global so that if an AMD module wants
	// to call noConflict to hide this version of jQuery, it will work.

	// Note that for maximum portability, libraries that are not jQuery should
	// declare themselves as anonymous modules, and avoid setting a global if an
	// AMD loader is present. jQuery is a special case. For more information, see
	// https://github.com/jrburke/requirejs/wiki/Updating-existing-libraries#wiki-anon

	if ( true ) {
		!(__WEBPACK_AMD_DEFINE_ARRAY__ = [], __WEBPACK_AMD_DEFINE_RESULT__ = function() {
			return jQuery;
		}.apply(exports, __WEBPACK_AMD_DEFINE_ARRAY__), __WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));
	}




	var

		// Map over jQuery in case of overwrite
		_jQuery = window.jQuery,

		// Map over the $ in case of overwrite
		_$ = window.$;

	jQuery.noConflict = function( deep ) {
		if ( window.$ === jQuery ) {
			window.$ = _$;
		}

		if ( deep && window.jQuery === jQuery ) {
			window.jQuery = _jQuery;
		}

		return jQuery;
	};

	// Expose jQuery and $ identifiers, even in AMD
	// (#7102#comment:10, https://github.com/jquery/jquery/pull/557)
	// and CommonJS for browser emulators (#13566)
	if ( !noGlobal ) {
		window.jQuery = window.$ = jQuery;
	}




	return jQuery;
	} );


/***/ }),
/* 328 */
/***/ (function(module, exports, __webpack_require__) {

	// https://d3js.org Version 4.13.0. Copyright 2018 Mike Bostock.
	(function (global, factory) {
		 true ? factory(exports) :
		typeof define === 'function' && define.amd ? define(['exports'], factory) :
		(factory((global.d3 = global.d3 || {})));
	}(this, (function (exports) { 'use strict';

	var version = "4.13.0";

	function ascending(a, b) {
	  return a < b ? -1 : a > b ? 1 : a >= b ? 0 : NaN;
	}

	function bisector(compare) {
	  if (compare.length === 1) compare = ascendingComparator(compare);
	  return {
	    left: function(a, x, lo, hi) {
	      if (lo == null) lo = 0;
	      if (hi == null) hi = a.length;
	      while (lo < hi) {
	        var mid = lo + hi >>> 1;
	        if (compare(a[mid], x) < 0) lo = mid + 1;
	        else hi = mid;
	      }
	      return lo;
	    },
	    right: function(a, x, lo, hi) {
	      if (lo == null) lo = 0;
	      if (hi == null) hi = a.length;
	      while (lo < hi) {
	        var mid = lo + hi >>> 1;
	        if (compare(a[mid], x) > 0) hi = mid;
	        else lo = mid + 1;
	      }
	      return lo;
	    }
	  };
	}

	function ascendingComparator(f) {
	  return function(d, x) {
	    return ascending(f(d), x);
	  };
	}

	var ascendingBisect = bisector(ascending);
	var bisectRight = ascendingBisect.right;
	var bisectLeft = ascendingBisect.left;

	function pairs(array, f) {
	  if (f == null) f = pair;
	  var i = 0, n = array.length - 1, p = array[0], pairs = new Array(n < 0 ? 0 : n);
	  while (i < n) pairs[i] = f(p, p = array[++i]);
	  return pairs;
	}

	function pair(a, b) {
	  return [a, b];
	}

	function cross(values0, values1, reduce) {
	  var n0 = values0.length,
	      n1 = values1.length,
	      values = new Array(n0 * n1),
	      i0,
	      i1,
	      i,
	      value0;

	  if (reduce == null) reduce = pair;

	  for (i0 = i = 0; i0 < n0; ++i0) {
	    for (value0 = values0[i0], i1 = 0; i1 < n1; ++i1, ++i) {
	      values[i] = reduce(value0, values1[i1]);
	    }
	  }

	  return values;
	}

	function descending(a, b) {
	  return b < a ? -1 : b > a ? 1 : b >= a ? 0 : NaN;
	}

	function number(x) {
	  return x === null ? NaN : +x;
	}

	function variance(values, valueof) {
	  var n = values.length,
	      m = 0,
	      i = -1,
	      mean = 0,
	      value,
	      delta,
	      sum = 0;

	  if (valueof == null) {
	    while (++i < n) {
	      if (!isNaN(value = number(values[i]))) {
	        delta = value - mean;
	        mean += delta / ++m;
	        sum += delta * (value - mean);
	      }
	    }
	  }

	  else {
	    while (++i < n) {
	      if (!isNaN(value = number(valueof(values[i], i, values)))) {
	        delta = value - mean;
	        mean += delta / ++m;
	        sum += delta * (value - mean);
	      }
	    }
	  }

	  if (m > 1) return sum / (m - 1);
	}

	function deviation(array, f) {
	  var v = variance(array, f);
	  return v ? Math.sqrt(v) : v;
	}

	function extent(values, valueof) {
	  var n = values.length,
	      i = -1,
	      value,
	      min,
	      max;

	  if (valueof == null) {
	    while (++i < n) { // Find the first comparable value.
	      if ((value = values[i]) != null && value >= value) {
	        min = max = value;
	        while (++i < n) { // Compare the remaining values.
	          if ((value = values[i]) != null) {
	            if (min > value) min = value;
	            if (max < value) max = value;
	          }
	        }
	      }
	    }
	  }

	  else {
	    while (++i < n) { // Find the first comparable value.
	      if ((value = valueof(values[i], i, values)) != null && value >= value) {
	        min = max = value;
	        while (++i < n) { // Compare the remaining values.
	          if ((value = valueof(values[i], i, values)) != null) {
	            if (min > value) min = value;
	            if (max < value) max = value;
	          }
	        }
	      }
	    }
	  }

	  return [min, max];
	}

	var array = Array.prototype;

	var slice = array.slice;
	var map = array.map;

	function constant(x) {
	  return function() {
	    return x;
	  };
	}

	function identity(x) {
	  return x;
	}

	function sequence(start, stop, step) {
	  start = +start, stop = +stop, step = (n = arguments.length) < 2 ? (stop = start, start = 0, 1) : n < 3 ? 1 : +step;

	  var i = -1,
	      n = Math.max(0, Math.ceil((stop - start) / step)) | 0,
	      range = new Array(n);

	  while (++i < n) {
	    range[i] = start + i * step;
	  }

	  return range;
	}

	var e10 = Math.sqrt(50);
	var e5 = Math.sqrt(10);
	var e2 = Math.sqrt(2);

	function ticks(start, stop, count) {
	  var reverse,
	      i = -1,
	      n,
	      ticks,
	      step;

	  stop = +stop, start = +start, count = +count;
	  if (start === stop && count > 0) return [start];
	  if (reverse = stop < start) n = start, start = stop, stop = n;
	  if ((step = tickIncrement(start, stop, count)) === 0 || !isFinite(step)) return [];

	  if (step > 0) {
	    start = Math.ceil(start / step);
	    stop = Math.floor(stop / step);
	    ticks = new Array(n = Math.ceil(stop - start + 1));
	    while (++i < n) ticks[i] = (start + i) * step;
	  } else {
	    start = Math.floor(start * step);
	    stop = Math.ceil(stop * step);
	    ticks = new Array(n = Math.ceil(start - stop + 1));
	    while (++i < n) ticks[i] = (start - i) / step;
	  }

	  if (reverse) ticks.reverse();

	  return ticks;
	}

	function tickIncrement(start, stop, count) {
	  var step = (stop - start) / Math.max(0, count),
	      power = Math.floor(Math.log(step) / Math.LN10),
	      error = step / Math.pow(10, power);
	  return power >= 0
	      ? (error >= e10 ? 10 : error >= e5 ? 5 : error >= e2 ? 2 : 1) * Math.pow(10, power)
	      : -Math.pow(10, -power) / (error >= e10 ? 10 : error >= e5 ? 5 : error >= e2 ? 2 : 1);
	}

	function tickStep(start, stop, count) {
	  var step0 = Math.abs(stop - start) / Math.max(0, count),
	      step1 = Math.pow(10, Math.floor(Math.log(step0) / Math.LN10)),
	      error = step0 / step1;
	  if (error >= e10) step1 *= 10;
	  else if (error >= e5) step1 *= 5;
	  else if (error >= e2) step1 *= 2;
	  return stop < start ? -step1 : step1;
	}

	function sturges(values) {
	  return Math.ceil(Math.log(values.length) / Math.LN2) + 1;
	}

	function histogram() {
	  var value = identity,
	      domain = extent,
	      threshold = sturges;

	  function histogram(data) {
	    var i,
	        n = data.length,
	        x,
	        values = new Array(n);

	    for (i = 0; i < n; ++i) {
	      values[i] = value(data[i], i, data);
	    }

	    var xz = domain(values),
	        x0 = xz[0],
	        x1 = xz[1],
	        tz = threshold(values, x0, x1);

	    // Convert number of thresholds into uniform thresholds.
	    if (!Array.isArray(tz)) {
	      tz = tickStep(x0, x1, tz);
	      tz = sequence(Math.ceil(x0 / tz) * tz, Math.floor(x1 / tz) * tz, tz); // exclusive
	    }

	    // Remove any thresholds outside the domain.
	    var m = tz.length;
	    while (tz[0] <= x0) tz.shift(), --m;
	    while (tz[m - 1] > x1) tz.pop(), --m;

	    var bins = new Array(m + 1),
	        bin;

	    // Initialize bins.
	    for (i = 0; i <= m; ++i) {
	      bin = bins[i] = [];
	      bin.x0 = i > 0 ? tz[i - 1] : x0;
	      bin.x1 = i < m ? tz[i] : x1;
	    }

	    // Assign data to bins by value, ignoring any outside the domain.
	    for (i = 0; i < n; ++i) {
	      x = values[i];
	      if (x0 <= x && x <= x1) {
	        bins[bisectRight(tz, x, 0, m)].push(data[i]);
	      }
	    }

	    return bins;
	  }

	  histogram.value = function(_) {
	    return arguments.length ? (value = typeof _ === "function" ? _ : constant(_), histogram) : value;
	  };

	  histogram.domain = function(_) {
	    return arguments.length ? (domain = typeof _ === "function" ? _ : constant([_[0], _[1]]), histogram) : domain;
	  };

	  histogram.thresholds = function(_) {
	    return arguments.length ? (threshold = typeof _ === "function" ? _ : Array.isArray(_) ? constant(slice.call(_)) : constant(_), histogram) : threshold;
	  };

	  return histogram;
	}

	function threshold(values, p, valueof) {
	  if (valueof == null) valueof = number;
	  if (!(n = values.length)) return;
	  if ((p = +p) <= 0 || n < 2) return +valueof(values[0], 0, values);
	  if (p >= 1) return +valueof(values[n - 1], n - 1, values);
	  var n,
	      i = (n - 1) * p,
	      i0 = Math.floor(i),
	      value0 = +valueof(values[i0], i0, values),
	      value1 = +valueof(values[i0 + 1], i0 + 1, values);
	  return value0 + (value1 - value0) * (i - i0);
	}

	function freedmanDiaconis(values, min, max) {
	  values = map.call(values, number).sort(ascending);
	  return Math.ceil((max - min) / (2 * (threshold(values, 0.75) - threshold(values, 0.25)) * Math.pow(values.length, -1 / 3)));
	}

	function scott(values, min, max) {
	  return Math.ceil((max - min) / (3.5 * deviation(values) * Math.pow(values.length, -1 / 3)));
	}

	function max(values, valueof) {
	  var n = values.length,
	      i = -1,
	      value,
	      max;

	  if (valueof == null) {
	    while (++i < n) { // Find the first comparable value.
	      if ((value = values[i]) != null && value >= value) {
	        max = value;
	        while (++i < n) { // Compare the remaining values.
	          if ((value = values[i]) != null && value > max) {
	            max = value;
	          }
	        }
	      }
	    }
	  }

	  else {
	    while (++i < n) { // Find the first comparable value.
	      if ((value = valueof(values[i], i, values)) != null && value >= value) {
	        max = value;
	        while (++i < n) { // Compare the remaining values.
	          if ((value = valueof(values[i], i, values)) != null && value > max) {
	            max = value;
	          }
	        }
	      }
	    }
	  }

	  return max;
	}

	function mean(values, valueof) {
	  var n = values.length,
	      m = n,
	      i = -1,
	      value,
	      sum = 0;

	  if (valueof == null) {
	    while (++i < n) {
	      if (!isNaN(value = number(values[i]))) sum += value;
	      else --m;
	    }
	  }

	  else {
	    while (++i < n) {
	      if (!isNaN(value = number(valueof(values[i], i, values)))) sum += value;
	      else --m;
	    }
	  }

	  if (m) return sum / m;
	}

	function median(values, valueof) {
	  var n = values.length,
	      i = -1,
	      value,
	      numbers = [];

	  if (valueof == null) {
	    while (++i < n) {
	      if (!isNaN(value = number(values[i]))) {
	        numbers.push(value);
	      }
	    }
	  }

	  else {
	    while (++i < n) {
	      if (!isNaN(value = number(valueof(values[i], i, values)))) {
	        numbers.push(value);
	      }
	    }
	  }

	  return threshold(numbers.sort(ascending), 0.5);
	}

	function merge(arrays) {
	  var n = arrays.length,
	      m,
	      i = -1,
	      j = 0,
	      merged,
	      array;

	  while (++i < n) j += arrays[i].length;
	  merged = new Array(j);

	  while (--n >= 0) {
	    array = arrays[n];
	    m = array.length;
	    while (--m >= 0) {
	      merged[--j] = array[m];
	    }
	  }

	  return merged;
	}

	function min(values, valueof) {
	  var n = values.length,
	      i = -1,
	      value,
	      min;

	  if (valueof == null) {
	    while (++i < n) { // Find the first comparable value.
	      if ((value = values[i]) != null && value >= value) {
	        min = value;
	        while (++i < n) { // Compare the remaining values.
	          if ((value = values[i]) != null && min > value) {
	            min = value;
	          }
	        }
	      }
	    }
	  }

	  else {
	    while (++i < n) { // Find the first comparable value.
	      if ((value = valueof(values[i], i, values)) != null && value >= value) {
	        min = value;
	        while (++i < n) { // Compare the remaining values.
	          if ((value = valueof(values[i], i, values)) != null && min > value) {
	            min = value;
	          }
	        }
	      }
	    }
	  }

	  return min;
	}

	function permute(array, indexes) {
	  var i = indexes.length, permutes = new Array(i);
	  while (i--) permutes[i] = array[indexes[i]];
	  return permutes;
	}

	function scan(values, compare) {
	  if (!(n = values.length)) return;
	  var n,
	      i = 0,
	      j = 0,
	      xi,
	      xj = values[j];

	  if (compare == null) compare = ascending;

	  while (++i < n) {
	    if (compare(xi = values[i], xj) < 0 || compare(xj, xj) !== 0) {
	      xj = xi, j = i;
	    }
	  }

	  if (compare(xj, xj) === 0) return j;
	}

	function shuffle(array, i0, i1) {
	  var m = (i1 == null ? array.length : i1) - (i0 = i0 == null ? 0 : +i0),
	      t,
	      i;

	  while (m) {
	    i = Math.random() * m-- | 0;
	    t = array[m + i0];
	    array[m + i0] = array[i + i0];
	    array[i + i0] = t;
	  }

	  return array;
	}

	function sum(values, valueof) {
	  var n = values.length,
	      i = -1,
	      value,
	      sum = 0;

	  if (valueof == null) {
	    while (++i < n) {
	      if (value = +values[i]) sum += value; // Note: zero and null are equivalent.
	    }
	  }

	  else {
	    while (++i < n) {
	      if (value = +valueof(values[i], i, values)) sum += value;
	    }
	  }

	  return sum;
	}

	function transpose(matrix) {
	  if (!(n = matrix.length)) return [];
	  for (var i = -1, m = min(matrix, length), transpose = new Array(m); ++i < m;) {
	    for (var j = -1, n, row = transpose[i] = new Array(n); ++j < n;) {
	      row[j] = matrix[j][i];
	    }
	  }
	  return transpose;
	}

	function length(d) {
	  return d.length;
	}

	function zip() {
	  return transpose(arguments);
	}

	var slice$1 = Array.prototype.slice;

	function identity$1(x) {
	  return x;
	}

	var top = 1;
	var right = 2;
	var bottom = 3;
	var left = 4;
	var epsilon = 1e-6;

	function translateX(x) {
	  return "translate(" + (x + 0.5) + ",0)";
	}

	function translateY(y) {
	  return "translate(0," + (y + 0.5) + ")";
	}

	function number$1(scale) {
	  return function(d) {
	    return +scale(d);
	  };
	}

	function center(scale) {
	  var offset = Math.max(0, scale.bandwidth() - 1) / 2; // Adjust for 0.5px offset.
	  if (scale.round()) offset = Math.round(offset);
	  return function(d) {
	    return +scale(d) + offset;
	  };
	}

	function entering() {
	  return !this.__axis;
	}

	function axis(orient, scale) {
	  var tickArguments = [],
	      tickValues = null,
	      tickFormat = null,
	      tickSizeInner = 6,
	      tickSizeOuter = 6,
	      tickPadding = 3,
	      k = orient === top || orient === left ? -1 : 1,
	      x = orient === left || orient === right ? "x" : "y",
	      transform = orient === top || orient === bottom ? translateX : translateY;

	  function axis(context) {
	    var values = tickValues == null ? (scale.ticks ? scale.ticks.apply(scale, tickArguments) : scale.domain()) : tickValues,
	        format = tickFormat == null ? (scale.tickFormat ? scale.tickFormat.apply(scale, tickArguments) : identity$1) : tickFormat,
	        spacing = Math.max(tickSizeInner, 0) + tickPadding,
	        range = scale.range(),
	        range0 = +range[0] + 0.5,
	        range1 = +range[range.length - 1] + 0.5,
	        position = (scale.bandwidth ? center : number$1)(scale.copy()),
	        selection = context.selection ? context.selection() : context,
	        path = selection.selectAll(".domain").data([null]),
	        tick = selection.selectAll(".tick").data(values, scale).order(),
	        tickExit = tick.exit(),
	        tickEnter = tick.enter().append("g").attr("class", "tick"),
	        line = tick.select("line"),
	        text = tick.select("text");

	    path = path.merge(path.enter().insert("path", ".tick")
	        .attr("class", "domain")
	        .attr("stroke", "#000"));

	    tick = tick.merge(tickEnter);

	    line = line.merge(tickEnter.append("line")
	        .attr("stroke", "#000")
	        .attr(x + "2", k * tickSizeInner));

	    text = text.merge(tickEnter.append("text")
	        .attr("fill", "#000")
	        .attr(x, k * spacing)
	        .attr("dy", orient === top ? "0em" : orient === bottom ? "0.71em" : "0.32em"));

	    if (context !== selection) {
	      path = path.transition(context);
	      tick = tick.transition(context);
	      line = line.transition(context);
	      text = text.transition(context);

	      tickExit = tickExit.transition(context)
	          .attr("opacity", epsilon)
	          .attr("transform", function(d) { return isFinite(d = position(d)) ? transform(d) : this.getAttribute("transform"); });

	      tickEnter
	          .attr("opacity", epsilon)
	          .attr("transform", function(d) { var p = this.parentNode.__axis; return transform(p && isFinite(p = p(d)) ? p : position(d)); });
	    }

	    tickExit.remove();

	    path
	        .attr("d", orient === left || orient == right
	            ? "M" + k * tickSizeOuter + "," + range0 + "H0.5V" + range1 + "H" + k * tickSizeOuter
	            : "M" + range0 + "," + k * tickSizeOuter + "V0.5H" + range1 + "V" + k * tickSizeOuter);

	    tick
	        .attr("opacity", 1)
	        .attr("transform", function(d) { return transform(position(d)); });

	    line
	        .attr(x + "2", k * tickSizeInner);

	    text
	        .attr(x, k * spacing)
	        .text(format);

	    selection.filter(entering)
	        .attr("fill", "none")
	        .attr("font-size", 10)
	        .attr("font-family", "sans-serif")
	        .attr("text-anchor", orient === right ? "start" : orient === left ? "end" : "middle");

	    selection
	        .each(function() { this.__axis = position; });
	  }

	  axis.scale = function(_) {
	    return arguments.length ? (scale = _, axis) : scale;
	  };

	  axis.ticks = function() {
	    return tickArguments = slice$1.call(arguments), axis;
	  };

	  axis.tickArguments = function(_) {
	    return arguments.length ? (tickArguments = _ == null ? [] : slice$1.call(_), axis) : tickArguments.slice();
	  };

	  axis.tickValues = function(_) {
	    return arguments.length ? (tickValues = _ == null ? null : slice$1.call(_), axis) : tickValues && tickValues.slice();
	  };

	  axis.tickFormat = function(_) {
	    return arguments.length ? (tickFormat = _, axis) : tickFormat;
	  };

	  axis.tickSize = function(_) {
	    return arguments.length ? (tickSizeInner = tickSizeOuter = +_, axis) : tickSizeInner;
	  };

	  axis.tickSizeInner = function(_) {
	    return arguments.length ? (tickSizeInner = +_, axis) : tickSizeInner;
	  };

	  axis.tickSizeOuter = function(_) {
	    return arguments.length ? (tickSizeOuter = +_, axis) : tickSizeOuter;
	  };

	  axis.tickPadding = function(_) {
	    return arguments.length ? (tickPadding = +_, axis) : tickPadding;
	  };

	  return axis;
	}

	function axisTop(scale) {
	  return axis(top, scale);
	}

	function axisRight(scale) {
	  return axis(right, scale);
	}

	function axisBottom(scale) {
	  return axis(bottom, scale);
	}

	function axisLeft(scale) {
	  return axis(left, scale);
	}

	var noop = {value: function() {}};

	function dispatch() {
	  for (var i = 0, n = arguments.length, _ = {}, t; i < n; ++i) {
	    if (!(t = arguments[i] + "") || (t in _)) throw new Error("illegal type: " + t);
	    _[t] = [];
	  }
	  return new Dispatch(_);
	}

	function Dispatch(_) {
	  this._ = _;
	}

	function parseTypenames(typenames, types) {
	  return typenames.trim().split(/^|\s+/).map(function(t) {
	    var name = "", i = t.indexOf(".");
	    if (i >= 0) name = t.slice(i + 1), t = t.slice(0, i);
	    if (t && !types.hasOwnProperty(t)) throw new Error("unknown type: " + t);
	    return {type: t, name: name};
	  });
	}

	Dispatch.prototype = dispatch.prototype = {
	  constructor: Dispatch,
	  on: function(typename, callback) {
	    var _ = this._,
	        T = parseTypenames(typename + "", _),
	        t,
	        i = -1,
	        n = T.length;

	    // If no callback was specified, return the callback of the given type and name.
	    if (arguments.length < 2) {
	      while (++i < n) if ((t = (typename = T[i]).type) && (t = get(_[t], typename.name))) return t;
	      return;
	    }

	    // If a type was specified, set the callback for the given type and name.
	    // Otherwise, if a null callback was specified, remove callbacks of the given name.
	    if (callback != null && typeof callback !== "function") throw new Error("invalid callback: " + callback);
	    while (++i < n) {
	      if (t = (typename = T[i]).type) _[t] = set(_[t], typename.name, callback);
	      else if (callback == null) for (t in _) _[t] = set(_[t], typename.name, null);
	    }

	    return this;
	  },
	  copy: function() {
	    var copy = {}, _ = this._;
	    for (var t in _) copy[t] = _[t].slice();
	    return new Dispatch(copy);
	  },
	  call: function(type, that) {
	    if ((n = arguments.length - 2) > 0) for (var args = new Array(n), i = 0, n, t; i < n; ++i) args[i] = arguments[i + 2];
	    if (!this._.hasOwnProperty(type)) throw new Error("unknown type: " + type);
	    for (t = this._[type], i = 0, n = t.length; i < n; ++i) t[i].value.apply(that, args);
	  },
	  apply: function(type, that, args) {
	    if (!this._.hasOwnProperty(type)) throw new Error("unknown type: " + type);
	    for (var t = this._[type], i = 0, n = t.length; i < n; ++i) t[i].value.apply(that, args);
	  }
	};

	function get(type, name) {
	  for (var i = 0, n = type.length, c; i < n; ++i) {
	    if ((c = type[i]).name === name) {
	      return c.value;
	    }
	  }
	}

	function set(type, name, callback) {
	  for (var i = 0, n = type.length; i < n; ++i) {
	    if (type[i].name === name) {
	      type[i] = noop, type = type.slice(0, i).concat(type.slice(i + 1));
	      break;
	    }
	  }
	  if (callback != null) type.push({name: name, value: callback});
	  return type;
	}

	var xhtml = "http://www.w3.org/1999/xhtml";

	var namespaces = {
	  svg: "http://www.w3.org/2000/svg",
	  xhtml: xhtml,
	  xlink: "http://www.w3.org/1999/xlink",
	  xml: "http://www.w3.org/XML/1998/namespace",
	  xmlns: "http://www.w3.org/2000/xmlns/"
	};

	function namespace(name) {
	  var prefix = name += "", i = prefix.indexOf(":");
	  if (i >= 0 && (prefix = name.slice(0, i)) !== "xmlns") name = name.slice(i + 1);
	  return namespaces.hasOwnProperty(prefix) ? {space: namespaces[prefix], local: name} : name;
	}

	function creatorInherit(name) {
	  return function() {
	    var document = this.ownerDocument,
	        uri = this.namespaceURI;
	    return uri === xhtml && document.documentElement.namespaceURI === xhtml
	        ? document.createElement(name)
	        : document.createElementNS(uri, name);
	  };
	}

	function creatorFixed(fullname) {
	  return function() {
	    return this.ownerDocument.createElementNS(fullname.space, fullname.local);
	  };
	}

	function creator(name) {
	  var fullname = namespace(name);
	  return (fullname.local
	      ? creatorFixed
	      : creatorInherit)(fullname);
	}

	function none() {}

	function selector(selector) {
	  return selector == null ? none : function() {
	    return this.querySelector(selector);
	  };
	}

	function selection_select(select) {
	  if (typeof select !== "function") select = selector(select);

	  for (var groups = this._groups, m = groups.length, subgroups = new Array(m), j = 0; j < m; ++j) {
	    for (var group = groups[j], n = group.length, subgroup = subgroups[j] = new Array(n), node, subnode, i = 0; i < n; ++i) {
	      if ((node = group[i]) && (subnode = select.call(node, node.__data__, i, group))) {
	        if ("__data__" in node) subnode.__data__ = node.__data__;
	        subgroup[i] = subnode;
	      }
	    }
	  }

	  return new Selection(subgroups, this._parents);
	}

	function empty$1() {
	  return [];
	}

	function selectorAll(selector) {
	  return selector == null ? empty$1 : function() {
	    return this.querySelectorAll(selector);
	  };
	}

	function selection_selectAll(select) {
	  if (typeof select !== "function") select = selectorAll(select);

	  for (var groups = this._groups, m = groups.length, subgroups = [], parents = [], j = 0; j < m; ++j) {
	    for (var group = groups[j], n = group.length, node, i = 0; i < n; ++i) {
	      if (node = group[i]) {
	        subgroups.push(select.call(node, node.__data__, i, group));
	        parents.push(node);
	      }
	    }
	  }

	  return new Selection(subgroups, parents);
	}

	var matcher = function(selector) {
	  return function() {
	    return this.matches(selector);
	  };
	};

	if (typeof document !== "undefined") {
	  var element = document.documentElement;
	  if (!element.matches) {
	    var vendorMatches = element.webkitMatchesSelector
	        || element.msMatchesSelector
	        || element.mozMatchesSelector
	        || element.oMatchesSelector;
	    matcher = function(selector) {
	      return function() {
	        return vendorMatches.call(this, selector);
	      };
	    };
	  }
	}

	var matcher$1 = matcher;

	function selection_filter(match) {
	  if (typeof match !== "function") match = matcher$1(match);

	  for (var groups = this._groups, m = groups.length, subgroups = new Array(m), j = 0; j < m; ++j) {
	    for (var group = groups[j], n = group.length, subgroup = subgroups[j] = [], node, i = 0; i < n; ++i) {
	      if ((node = group[i]) && match.call(node, node.__data__, i, group)) {
	        subgroup.push(node);
	      }
	    }
	  }

	  return new Selection(subgroups, this._parents);
	}

	function sparse(update) {
	  return new Array(update.length);
	}

	function selection_enter() {
	  return new Selection(this._enter || this._groups.map(sparse), this._parents);
	}

	function EnterNode(parent, datum) {
	  this.ownerDocument = parent.ownerDocument;
	  this.namespaceURI = parent.namespaceURI;
	  this._next = null;
	  this._parent = parent;
	  this.__data__ = datum;
	}

	EnterNode.prototype = {
	  constructor: EnterNode,
	  appendChild: function(child) { return this._parent.insertBefore(child, this._next); },
	  insertBefore: function(child, next) { return this._parent.insertBefore(child, next); },
	  querySelector: function(selector) { return this._parent.querySelector(selector); },
	  querySelectorAll: function(selector) { return this._parent.querySelectorAll(selector); }
	};

	function constant$1(x) {
	  return function() {
	    return x;
	  };
	}

	var keyPrefix = "$"; // Protect against keys like “__proto__”.

	function bindIndex(parent, group, enter, update, exit, data) {
	  var i = 0,
	      node,
	      groupLength = group.length,
	      dataLength = data.length;

	  // Put any non-null nodes that fit into update.
	  // Put any null nodes into enter.
	  // Put any remaining data into enter.
	  for (; i < dataLength; ++i) {
	    if (node = group[i]) {
	      node.__data__ = data[i];
	      update[i] = node;
	    } else {
	      enter[i] = new EnterNode(parent, data[i]);
	    }
	  }

	  // Put any non-null nodes that don’t fit into exit.
	  for (; i < groupLength; ++i) {
	    if (node = group[i]) {
	      exit[i] = node;
	    }
	  }
	}

	function bindKey(parent, group, enter, update, exit, data, key) {
	  var i,
	      node,
	      nodeByKeyValue = {},
	      groupLength = group.length,
	      dataLength = data.length,
	      keyValues = new Array(groupLength),
	      keyValue;

	  // Compute the key for each node.
	  // If multiple nodes have the same key, the duplicates are added to exit.
	  for (i = 0; i < groupLength; ++i) {
	    if (node = group[i]) {
	      keyValues[i] = keyValue = keyPrefix + key.call(node, node.__data__, i, group);
	      if (keyValue in nodeByKeyValue) {
	        exit[i] = node;
	      } else {
	        nodeByKeyValue[keyValue] = node;
	      }
	    }
	  }

	  // Compute the key for each datum.
	  // If there a node associated with this key, join and add it to update.
	  // If there is not (or the key is a duplicate), add it to enter.
	  for (i = 0; i < dataLength; ++i) {
	    keyValue = keyPrefix + key.call(parent, data[i], i, data);
	    if (node = nodeByKeyValue[keyValue]) {
	      update[i] = node;
	      node.__data__ = data[i];
	      nodeByKeyValue[keyValue] = null;
	    } else {
	      enter[i] = new EnterNode(parent, data[i]);
	    }
	  }

	  // Add any remaining nodes that were not bound to data to exit.
	  for (i = 0; i < groupLength; ++i) {
	    if ((node = group[i]) && (nodeByKeyValue[keyValues[i]] === node)) {
	      exit[i] = node;
	    }
	  }
	}

	function selection_data(value, key) {
	  if (!value) {
	    data = new Array(this.size()), j = -1;
	    this.each(function(d) { data[++j] = d; });
	    return data;
	  }

	  var bind = key ? bindKey : bindIndex,
	      parents = this._parents,
	      groups = this._groups;

	  if (typeof value !== "function") value = constant$1(value);

	  for (var m = groups.length, update = new Array(m), enter = new Array(m), exit = new Array(m), j = 0; j < m; ++j) {
	    var parent = parents[j],
	        group = groups[j],
	        groupLength = group.length,
	        data = value.call(parent, parent && parent.__data__, j, parents),
	        dataLength = data.length,
	        enterGroup = enter[j] = new Array(dataLength),
	        updateGroup = update[j] = new Array(dataLength),
	        exitGroup = exit[j] = new Array(groupLength);

	    bind(parent, group, enterGroup, updateGroup, exitGroup, data, key);

	    // Now connect the enter nodes to their following update node, such that
	    // appendChild can insert the materialized enter node before this node,
	    // rather than at the end of the parent node.
	    for (var i0 = 0, i1 = 0, previous, next; i0 < dataLength; ++i0) {
	      if (previous = enterGroup[i0]) {
	        if (i0 >= i1) i1 = i0 + 1;
	        while (!(next = updateGroup[i1]) && ++i1 < dataLength);
	        previous._next = next || null;
	      }
	    }
	  }

	  update = new Selection(update, parents);
	  update._enter = enter;
	  update._exit = exit;
	  return update;
	}

	function selection_exit() {
	  return new Selection(this._exit || this._groups.map(sparse), this._parents);
	}

	function selection_merge(selection$$1) {

	  for (var groups0 = this._groups, groups1 = selection$$1._groups, m0 = groups0.length, m1 = groups1.length, m = Math.min(m0, m1), merges = new Array(m0), j = 0; j < m; ++j) {
	    for (var group0 = groups0[j], group1 = groups1[j], n = group0.length, merge = merges[j] = new Array(n), node, i = 0; i < n; ++i) {
	      if (node = group0[i] || group1[i]) {
	        merge[i] = node;
	      }
	    }
	  }

	  for (; j < m0; ++j) {
	    merges[j] = groups0[j];
	  }

	  return new Selection(merges, this._parents);
	}

	function selection_order() {

	  for (var groups = this._groups, j = -1, m = groups.length; ++j < m;) {
	    for (var group = groups[j], i = group.length - 1, next = group[i], node; --i >= 0;) {
	      if (node = group[i]) {
	        if (next && next !== node.nextSibling) next.parentNode.insertBefore(node, next);
	        next = node;
	      }
	    }
	  }

	  return this;
	}

	function selection_sort(compare) {
	  if (!compare) compare = ascending$1;

	  function compareNode(a, b) {
	    return a && b ? compare(a.__data__, b.__data__) : !a - !b;
	  }

	  for (var groups = this._groups, m = groups.length, sortgroups = new Array(m), j = 0; j < m; ++j) {
	    for (var group = groups[j], n = group.length, sortgroup = sortgroups[j] = new Array(n), node, i = 0; i < n; ++i) {
	      if (node = group[i]) {
	        sortgroup[i] = node;
	      }
	    }
	    sortgroup.sort(compareNode);
	  }

	  return new Selection(sortgroups, this._parents).order();
	}

	function ascending$1(a, b) {
	  return a < b ? -1 : a > b ? 1 : a >= b ? 0 : NaN;
	}

	function selection_call() {
	  var callback = arguments[0];
	  arguments[0] = this;
	  callback.apply(null, arguments);
	  return this;
	}

	function selection_nodes() {
	  var nodes = new Array(this.size()), i = -1;
	  this.each(function() { nodes[++i] = this; });
	  return nodes;
	}

	function selection_node() {

	  for (var groups = this._groups, j = 0, m = groups.length; j < m; ++j) {
	    for (var group = groups[j], i = 0, n = group.length; i < n; ++i) {
	      var node = group[i];
	      if (node) return node;
	    }
	  }

	  return null;
	}

	function selection_size() {
	  var size = 0;
	  this.each(function() { ++size; });
	  return size;
	}

	function selection_empty() {
	  return !this.node();
	}

	function selection_each(callback) {

	  for (var groups = this._groups, j = 0, m = groups.length; j < m; ++j) {
	    for (var group = groups[j], i = 0, n = group.length, node; i < n; ++i) {
	      if (node = group[i]) callback.call(node, node.__data__, i, group);
	    }
	  }

	  return this;
	}

	function attrRemove(name) {
	  return function() {
	    this.removeAttribute(name);
	  };
	}

	function attrRemoveNS(fullname) {
	  return function() {
	    this.removeAttributeNS(fullname.space, fullname.local);
	  };
	}

	function attrConstant(name, value) {
	  return function() {
	    this.setAttribute(name, value);
	  };
	}

	function attrConstantNS(fullname, value) {
	  return function() {
	    this.setAttributeNS(fullname.space, fullname.local, value);
	  };
	}

	function attrFunction(name, value) {
	  return function() {
	    var v = value.apply(this, arguments);
	    if (v == null) this.removeAttribute(name);
	    else this.setAttribute(name, v);
	  };
	}

	function attrFunctionNS(fullname, value) {
	  return function() {
	    var v = value.apply(this, arguments);
	    if (v == null) this.removeAttributeNS(fullname.space, fullname.local);
	    else this.setAttributeNS(fullname.space, fullname.local, v);
	  };
	}

	function selection_attr(name, value) {
	  var fullname = namespace(name);

	  if (arguments.length < 2) {
	    var node = this.node();
	    return fullname.local
	        ? node.getAttributeNS(fullname.space, fullname.local)
	        : node.getAttribute(fullname);
	  }

	  return this.each((value == null
	      ? (fullname.local ? attrRemoveNS : attrRemove) : (typeof value === "function"
	      ? (fullname.local ? attrFunctionNS : attrFunction)
	      : (fullname.local ? attrConstantNS : attrConstant)))(fullname, value));
	}

	function defaultView(node) {
	  return (node.ownerDocument && node.ownerDocument.defaultView) // node is a Node
	      || (node.document && node) // node is a Window
	      || node.defaultView; // node is a Document
	}

	function styleRemove(name) {
	  return function() {
	    this.style.removeProperty(name);
	  };
	}

	function styleConstant(name, value, priority) {
	  return function() {
	    this.style.setProperty(name, value, priority);
	  };
	}

	function styleFunction(name, value, priority) {
	  return function() {
	    var v = value.apply(this, arguments);
	    if (v == null) this.style.removeProperty(name);
	    else this.style.setProperty(name, v, priority);
	  };
	}

	function selection_style(name, value, priority) {
	  return arguments.length > 1
	      ? this.each((value == null
	            ? styleRemove : typeof value === "function"
	            ? styleFunction
	            : styleConstant)(name, value, priority == null ? "" : priority))
	      : styleValue(this.node(), name);
	}

	function styleValue(node, name) {
	  return node.style.getPropertyValue(name)
	      || defaultView(node).getComputedStyle(node, null).getPropertyValue(name);
	}

	function propertyRemove(name) {
	  return function() {
	    delete this[name];
	  };
	}

	function propertyConstant(name, value) {
	  return function() {
	    this[name] = value;
	  };
	}

	function propertyFunction(name, value) {
	  return function() {
	    var v = value.apply(this, arguments);
	    if (v == null) delete this[name];
	    else this[name] = v;
	  };
	}

	function selection_property(name, value) {
	  return arguments.length > 1
	      ? this.each((value == null
	          ? propertyRemove : typeof value === "function"
	          ? propertyFunction
	          : propertyConstant)(name, value))
	      : this.node()[name];
	}

	function classArray(string) {
	  return string.trim().split(/^|\s+/);
	}

	function classList(node) {
	  return node.classList || new ClassList(node);
	}

	function ClassList(node) {
	  this._node = node;
	  this._names = classArray(node.getAttribute("class") || "");
	}

	ClassList.prototype = {
	  add: function(name) {
	    var i = this._names.indexOf(name);
	    if (i < 0) {
	      this._names.push(name);
	      this._node.setAttribute("class", this._names.join(" "));
	    }
	  },
	  remove: function(name) {
	    var i = this._names.indexOf(name);
	    if (i >= 0) {
	      this._names.splice(i, 1);
	      this._node.setAttribute("class", this._names.join(" "));
	    }
	  },
	  contains: function(name) {
	    return this._names.indexOf(name) >= 0;
	  }
	};

	function classedAdd(node, names) {
	  var list = classList(node), i = -1, n = names.length;
	  while (++i < n) list.add(names[i]);
	}

	function classedRemove(node, names) {
	  var list = classList(node), i = -1, n = names.length;
	  while (++i < n) list.remove(names[i]);
	}

	function classedTrue(names) {
	  return function() {
	    classedAdd(this, names);
	  };
	}

	function classedFalse(names) {
	  return function() {
	    classedRemove(this, names);
	  };
	}

	function classedFunction(names, value) {
	  return function() {
	    (value.apply(this, arguments) ? classedAdd : classedRemove)(this, names);
	  };
	}

	function selection_classed(name, value) {
	  var names = classArray(name + "");

	  if (arguments.length < 2) {
	    var list = classList(this.node()), i = -1, n = names.length;
	    while (++i < n) if (!list.contains(names[i])) return false;
	    return true;
	  }

	  return this.each((typeof value === "function"
	      ? classedFunction : value
	      ? classedTrue
	      : classedFalse)(names, value));
	}

	function textRemove() {
	  this.textContent = "";
	}

	function textConstant(value) {
	  return function() {
	    this.textContent = value;
	  };
	}

	function textFunction(value) {
	  return function() {
	    var v = value.apply(this, arguments);
	    this.textContent = v == null ? "" : v;
	  };
	}

	function selection_text(value) {
	  return arguments.length
	      ? this.each(value == null
	          ? textRemove : (typeof value === "function"
	          ? textFunction
	          : textConstant)(value))
	      : this.node().textContent;
	}

	function htmlRemove() {
	  this.innerHTML = "";
	}

	function htmlConstant(value) {
	  return function() {
	    this.innerHTML = value;
	  };
	}

	function htmlFunction(value) {
	  return function() {
	    var v = value.apply(this, arguments);
	    this.innerHTML = v == null ? "" : v;
	  };
	}

	function selection_html(value) {
	  return arguments.length
	      ? this.each(value == null
	          ? htmlRemove : (typeof value === "function"
	          ? htmlFunction
	          : htmlConstant)(value))
	      : this.node().innerHTML;
	}

	function raise() {
	  if (this.nextSibling) this.parentNode.appendChild(this);
	}

	function selection_raise() {
	  return this.each(raise);
	}

	function lower() {
	  if (this.previousSibling) this.parentNode.insertBefore(this, this.parentNode.firstChild);
	}

	function selection_lower() {
	  return this.each(lower);
	}

	function selection_append(name) {
	  var create = typeof name === "function" ? name : creator(name);
	  return this.select(function() {
	    return this.appendChild(create.apply(this, arguments));
	  });
	}

	function constantNull() {
	  return null;
	}

	function selection_insert(name, before) {
	  var create = typeof name === "function" ? name : creator(name),
	      select = before == null ? constantNull : typeof before === "function" ? before : selector(before);
	  return this.select(function() {
	    return this.insertBefore(create.apply(this, arguments), select.apply(this, arguments) || null);
	  });
	}

	function remove() {
	  var parent = this.parentNode;
	  if (parent) parent.removeChild(this);
	}

	function selection_remove() {
	  return this.each(remove);
	}

	function selection_cloneShallow() {
	  return this.parentNode.insertBefore(this.cloneNode(false), this.nextSibling);
	}

	function selection_cloneDeep() {
	  return this.parentNode.insertBefore(this.cloneNode(true), this.nextSibling);
	}

	function selection_clone(deep) {
	  return this.select(deep ? selection_cloneDeep : selection_cloneShallow);
	}

	function selection_datum(value) {
	  return arguments.length
	      ? this.property("__data__", value)
	      : this.node().__data__;
	}

	var filterEvents = {};

	exports.event = null;

	if (typeof document !== "undefined") {
	  var element$1 = document.documentElement;
	  if (!("onmouseenter" in element$1)) {
	    filterEvents = {mouseenter: "mouseover", mouseleave: "mouseout"};
	  }
	}

	function filterContextListener(listener, index, group) {
	  listener = contextListener(listener, index, group);
	  return function(event) {
	    var related = event.relatedTarget;
	    if (!related || (related !== this && !(related.compareDocumentPosition(this) & 8))) {
	      listener.call(this, event);
	    }
	  };
	}

	function contextListener(listener, index, group) {
	  return function(event1) {
	    var event0 = exports.event; // Events can be reentrant (e.g., focus).
	    exports.event = event1;
	    try {
	      listener.call(this, this.__data__, index, group);
	    } finally {
	      exports.event = event0;
	    }
	  };
	}

	function parseTypenames$1(typenames) {
	  return typenames.trim().split(/^|\s+/).map(function(t) {
	    var name = "", i = t.indexOf(".");
	    if (i >= 0) name = t.slice(i + 1), t = t.slice(0, i);
	    return {type: t, name: name};
	  });
	}

	function onRemove(typename) {
	  return function() {
	    var on = this.__on;
	    if (!on) return;
	    for (var j = 0, i = -1, m = on.length, o; j < m; ++j) {
	      if (o = on[j], (!typename.type || o.type === typename.type) && o.name === typename.name) {
	        this.removeEventListener(o.type, o.listener, o.capture);
	      } else {
	        on[++i] = o;
	      }
	    }
	    if (++i) on.length = i;
	    else delete this.__on;
	  };
	}

	function onAdd(typename, value, capture) {
	  var wrap = filterEvents.hasOwnProperty(typename.type) ? filterContextListener : contextListener;
	  return function(d, i, group) {
	    var on = this.__on, o, listener = wrap(value, i, group);
	    if (on) for (var j = 0, m = on.length; j < m; ++j) {
	      if ((o = on[j]).type === typename.type && o.name === typename.name) {
	        this.removeEventListener(o.type, o.listener, o.capture);
	        this.addEventListener(o.type, o.listener = listener, o.capture = capture);
	        o.value = value;
	        return;
	      }
	    }
	    this.addEventListener(typename.type, listener, capture);
	    o = {type: typename.type, name: typename.name, value: value, listener: listener, capture: capture};
	    if (!on) this.__on = [o];
	    else on.push(o);
	  };
	}

	function selection_on(typename, value, capture) {
	  var typenames = parseTypenames$1(typename + ""), i, n = typenames.length, t;

	  if (arguments.length < 2) {
	    var on = this.node().__on;
	    if (on) for (var j = 0, m = on.length, o; j < m; ++j) {
	      for (i = 0, o = on[j]; i < n; ++i) {
	        if ((t = typenames[i]).type === o.type && t.name === o.name) {
	          return o.value;
	        }
	      }
	    }
	    return;
	  }

	  on = value ? onAdd : onRemove;
	  if (capture == null) capture = false;
	  for (i = 0; i < n; ++i) this.each(on(typenames[i], value, capture));
	  return this;
	}

	function customEvent(event1, listener, that, args) {
	  var event0 = exports.event;
	  event1.sourceEvent = exports.event;
	  exports.event = event1;
	  try {
	    return listener.apply(that, args);
	  } finally {
	    exports.event = event0;
	  }
	}

	function dispatchEvent(node, type, params) {
	  var window = defaultView(node),
	      event = window.CustomEvent;

	  if (typeof event === "function") {
	    event = new event(type, params);
	  } else {
	    event = window.document.createEvent("Event");
	    if (params) event.initEvent(type, params.bubbles, params.cancelable), event.detail = params.detail;
	    else event.initEvent(type, false, false);
	  }

	  node.dispatchEvent(event);
	}

	function dispatchConstant(type, params) {
	  return function() {
	    return dispatchEvent(this, type, params);
	  };
	}

	function dispatchFunction(type, params) {
	  return function() {
	    return dispatchEvent(this, type, params.apply(this, arguments));
	  };
	}

	function selection_dispatch(type, params) {
	  return this.each((typeof params === "function"
	      ? dispatchFunction
	      : dispatchConstant)(type, params));
	}

	var root = [null];

	function Selection(groups, parents) {
	  this._groups = groups;
	  this._parents = parents;
	}

	function selection() {
	  return new Selection([[document.documentElement]], root);
	}

	Selection.prototype = selection.prototype = {
	  constructor: Selection,
	  select: selection_select,
	  selectAll: selection_selectAll,
	  filter: selection_filter,
	  data: selection_data,
	  enter: selection_enter,
	  exit: selection_exit,
	  merge: selection_merge,
	  order: selection_order,
	  sort: selection_sort,
	  call: selection_call,
	  nodes: selection_nodes,
	  node: selection_node,
	  size: selection_size,
	  empty: selection_empty,
	  each: selection_each,
	  attr: selection_attr,
	  style: selection_style,
	  property: selection_property,
	  classed: selection_classed,
	  text: selection_text,
	  html: selection_html,
	  raise: selection_raise,
	  lower: selection_lower,
	  append: selection_append,
	  insert: selection_insert,
	  remove: selection_remove,
	  clone: selection_clone,
	  datum: selection_datum,
	  on: selection_on,
	  dispatch: selection_dispatch
	};

	function select(selector) {
	  return typeof selector === "string"
	      ? new Selection([[document.querySelector(selector)]], [document.documentElement])
	      : new Selection([[selector]], root);
	}

	function create(name) {
	  return select(creator(name).call(document.documentElement));
	}

	var nextId = 0;

	function local$1() {
	  return new Local;
	}

	function Local() {
	  this._ = "@" + (++nextId).toString(36);
	}

	Local.prototype = local$1.prototype = {
	  constructor: Local,
	  get: function(node) {
	    var id = this._;
	    while (!(id in node)) if (!(node = node.parentNode)) return;
	    return node[id];
	  },
	  set: function(node, value) {
	    return node[this._] = value;
	  },
	  remove: function(node) {
	    return this._ in node && delete node[this._];
	  },
	  toString: function() {
	    return this._;
	  }
	};

	function sourceEvent() {
	  var current = exports.event, source;
	  while (source = current.sourceEvent) current = source;
	  return current;
	}

	function point(node, event) {
	  var svg = node.ownerSVGElement || node;

	  if (svg.createSVGPoint) {
	    var point = svg.createSVGPoint();
	    point.x = event.clientX, point.y = event.clientY;
	    point = point.matrixTransform(node.getScreenCTM().inverse());
	    return [point.x, point.y];
	  }

	  var rect = node.getBoundingClientRect();
	  return [event.clientX - rect.left - node.clientLeft, event.clientY - rect.top - node.clientTop];
	}

	function mouse(node) {
	  var event = sourceEvent();
	  if (event.changedTouches) event = event.changedTouches[0];
	  return point(node, event);
	}

	function selectAll(selector) {
	  return typeof selector === "string"
	      ? new Selection([document.querySelectorAll(selector)], [document.documentElement])
	      : new Selection([selector == null ? [] : selector], root);
	}

	function touch(node, touches, identifier) {
	  if (arguments.length < 3) identifier = touches, touches = sourceEvent().changedTouches;

	  for (var i = 0, n = touches ? touches.length : 0, touch; i < n; ++i) {
	    if ((touch = touches[i]).identifier === identifier) {
	      return point(node, touch);
	    }
	  }

	  return null;
	}

	function touches(node, touches) {
	  if (touches == null) touches = sourceEvent().touches;

	  for (var i = 0, n = touches ? touches.length : 0, points = new Array(n); i < n; ++i) {
	    points[i] = point(node, touches[i]);
	  }

	  return points;
	}

	function nopropagation() {
	  exports.event.stopImmediatePropagation();
	}

	function noevent() {
	  exports.event.preventDefault();
	  exports.event.stopImmediatePropagation();
	}

	function dragDisable(view) {
	  var root = view.document.documentElement,
	      selection = select(view).on("dragstart.drag", noevent, true);
	  if ("onselectstart" in root) {
	    selection.on("selectstart.drag", noevent, true);
	  } else {
	    root.__noselect = root.style.MozUserSelect;
	    root.style.MozUserSelect = "none";
	  }
	}

	function yesdrag(view, noclick) {
	  var root = view.document.documentElement,
	      selection = select(view).on("dragstart.drag", null);
	  if (noclick) {
	    selection.on("click.drag", noevent, true);
	    setTimeout(function() { selection.on("click.drag", null); }, 0);
	  }
	  if ("onselectstart" in root) {
	    selection.on("selectstart.drag", null);
	  } else {
	    root.style.MozUserSelect = root.__noselect;
	    delete root.__noselect;
	  }
	}

	function constant$2(x) {
	  return function() {
	    return x;
	  };
	}

	function DragEvent(target, type, subject, id, active, x, y, dx, dy, dispatch) {
	  this.target = target;
	  this.type = type;
	  this.subject = subject;
	  this.identifier = id;
	  this.active = active;
	  this.x = x;
	  this.y = y;
	  this.dx = dx;
	  this.dy = dy;
	  this._ = dispatch;
	}

	DragEvent.prototype.on = function() {
	  var value = this._.on.apply(this._, arguments);
	  return value === this._ ? this : value;
	};

	// Ignore right-click, since that should open the context menu.
	function defaultFilter$1() {
	  return !exports.event.button;
	}

	function defaultContainer() {
	  return this.parentNode;
	}

	function defaultSubject(d) {
	  return d == null ? {x: exports.event.x, y: exports.event.y} : d;
	}

	function defaultTouchable() {
	  return "ontouchstart" in this;
	}

	function drag() {
	  var filter = defaultFilter$1,
	      container = defaultContainer,
	      subject = defaultSubject,
	      touchable = defaultTouchable,
	      gestures = {},
	      listeners = dispatch("start", "drag", "end"),
	      active = 0,
	      mousedownx,
	      mousedowny,
	      mousemoving,
	      touchending,
	      clickDistance2 = 0;

	  function drag(selection) {
	    selection
	        .on("mousedown.drag", mousedowned)
	      .filter(touchable)
	        .on("touchstart.drag", touchstarted)
	        .on("touchmove.drag", touchmoved)
	        .on("touchend.drag touchcancel.drag", touchended)
	        .style("touch-action", "none")
	        .style("-webkit-tap-highlight-color", "rgba(0,0,0,0)");
	  }

	  function mousedowned() {
	    if (touchending || !filter.apply(this, arguments)) return;
	    var gesture = beforestart("mouse", container.apply(this, arguments), mouse, this, arguments);
	    if (!gesture) return;
	    select(exports.event.view).on("mousemove.drag", mousemoved, true).on("mouseup.drag", mouseupped, true);
	    dragDisable(exports.event.view);
	    nopropagation();
	    mousemoving = false;
	    mousedownx = exports.event.clientX;
	    mousedowny = exports.event.clientY;
	    gesture("start");
	  }

	  function mousemoved() {
	    noevent();
	    if (!mousemoving) {
	      var dx = exports.event.clientX - mousedownx, dy = exports.event.clientY - mousedowny;
	      mousemoving = dx * dx + dy * dy > clickDistance2;
	    }
	    gestures.mouse("drag");
	  }

	  function mouseupped() {
	    select(exports.event.view).on("mousemove.drag mouseup.drag", null);
	    yesdrag(exports.event.view, mousemoving);
	    noevent();
	    gestures.mouse("end");
	  }

	  function touchstarted() {
	    if (!filter.apply(this, arguments)) return;
	    var touches = exports.event.changedTouches,
	        c = container.apply(this, arguments),
	        n = touches.length, i, gesture;

	    for (i = 0; i < n; ++i) {
	      if (gesture = beforestart(touches[i].identifier, c, touch, this, arguments)) {
	        nopropagation();
	        gesture("start");
	      }
	    }
	  }

	  function touchmoved() {
	    var touches = exports.event.changedTouches,
	        n = touches.length, i, gesture;

	    for (i = 0; i < n; ++i) {
	      if (gesture = gestures[touches[i].identifier]) {
	        noevent();
	        gesture("drag");
	      }
	    }
	  }

	  function touchended() {
	    var touches = exports.event.changedTouches,
	        n = touches.length, i, gesture;

	    if (touchending) clearTimeout(touchending);
	    touchending = setTimeout(function() { touchending = null; }, 500); // Ghost clicks are delayed!
	    for (i = 0; i < n; ++i) {
	      if (gesture = gestures[touches[i].identifier]) {
	        nopropagation();
	        gesture("end");
	      }
	    }
	  }

	  function beforestart(id, container, point, that, args) {
	    var p = point(container, id), s, dx, dy,
	        sublisteners = listeners.copy();

	    if (!customEvent(new DragEvent(drag, "beforestart", s, id, active, p[0], p[1], 0, 0, sublisteners), function() {
	      if ((exports.event.subject = s = subject.apply(that, args)) == null) return false;
	      dx = s.x - p[0] || 0;
	      dy = s.y - p[1] || 0;
	      return true;
	    })) return;

	    return function gesture(type) {
	      var p0 = p, n;
	      switch (type) {
	        case "start": gestures[id] = gesture, n = active++; break;
	        case "end": delete gestures[id], --active; // nobreak
	        case "drag": p = point(container, id), n = active; break;
	      }
	      customEvent(new DragEvent(drag, type, s, id, n, p[0] + dx, p[1] + dy, p[0] - p0[0], p[1] - p0[1], sublisteners), sublisteners.apply, sublisteners, [type, that, args]);
	    };
	  }

	  drag.filter = function(_) {
	    return arguments.length ? (filter = typeof _ === "function" ? _ : constant$2(!!_), drag) : filter;
	  };

	  drag.container = function(_) {
	    return arguments.length ? (container = typeof _ === "function" ? _ : constant$2(_), drag) : container;
	  };

	  drag.subject = function(_) {
	    return arguments.length ? (subject = typeof _ === "function" ? _ : constant$2(_), drag) : subject;
	  };

	  drag.touchable = function(_) {
	    return arguments.length ? (touchable = typeof _ === "function" ? _ : constant$2(!!_), drag) : touchable;
	  };

	  drag.on = function() {
	    var value = listeners.on.apply(listeners, arguments);
	    return value === listeners ? drag : value;
	  };

	  drag.clickDistance = function(_) {
	    return arguments.length ? (clickDistance2 = (_ = +_) * _, drag) : Math.sqrt(clickDistance2);
	  };

	  return drag;
	}

	function define(constructor, factory, prototype) {
	  constructor.prototype = factory.prototype = prototype;
	  prototype.constructor = constructor;
	}

	function extend(parent, definition) {
	  var prototype = Object.create(parent.prototype);
	  for (var key in definition) prototype[key] = definition[key];
	  return prototype;
	}

	function Color() {}

	var darker = 0.7;
	var brighter = 1 / darker;

	var reI = "\\s*([+-]?\\d+)\\s*";
	var reN = "\\s*([+-]?\\d*\\.?\\d+(?:[eE][+-]?\\d+)?)\\s*";
	var reP = "\\s*([+-]?\\d*\\.?\\d+(?:[eE][+-]?\\d+)?)%\\s*";
	var reHex3 = /^#([0-9a-f]{3})$/;
	var reHex6 = /^#([0-9a-f]{6})$/;
	var reRgbInteger = new RegExp("^rgb\\(" + [reI, reI, reI] + "\\)$");
	var reRgbPercent = new RegExp("^rgb\\(" + [reP, reP, reP] + "\\)$");
	var reRgbaInteger = new RegExp("^rgba\\(" + [reI, reI, reI, reN] + "\\)$");
	var reRgbaPercent = new RegExp("^rgba\\(" + [reP, reP, reP, reN] + "\\)$");
	var reHslPercent = new RegExp("^hsl\\(" + [reN, reP, reP] + "\\)$");
	var reHslaPercent = new RegExp("^hsla\\(" + [reN, reP, reP, reN] + "\\)$");

	var named = {
	  aliceblue: 0xf0f8ff,
	  antiquewhite: 0xfaebd7,
	  aqua: 0x00ffff,
	  aquamarine: 0x7fffd4,
	  azure: 0xf0ffff,
	  beige: 0xf5f5dc,
	  bisque: 0xffe4c4,
	  black: 0x000000,
	  blanchedalmond: 0xffebcd,
	  blue: 0x0000ff,
	  blueviolet: 0x8a2be2,
	  brown: 0xa52a2a,
	  burlywood: 0xdeb887,
	  cadetblue: 0x5f9ea0,
	  chartreuse: 0x7fff00,
	  chocolate: 0xd2691e,
	  coral: 0xff7f50,
	  cornflowerblue: 0x6495ed,
	  cornsilk: 0xfff8dc,
	  crimson: 0xdc143c,
	  cyan: 0x00ffff,
	  darkblue: 0x00008b,
	  darkcyan: 0x008b8b,
	  darkgoldenrod: 0xb8860b,
	  darkgray: 0xa9a9a9,
	  darkgreen: 0x006400,
	  darkgrey: 0xa9a9a9,
	  darkkhaki: 0xbdb76b,
	  darkmagenta: 0x8b008b,
	  darkolivegreen: 0x556b2f,
	  darkorange: 0xff8c00,
	  darkorchid: 0x9932cc,
	  darkred: 0x8b0000,
	  darksalmon: 0xe9967a,
	  darkseagreen: 0x8fbc8f,
	  darkslateblue: 0x483d8b,
	  darkslategray: 0x2f4f4f,
	  darkslategrey: 0x2f4f4f,
	  darkturquoise: 0x00ced1,
	  darkviolet: 0x9400d3,
	  deeppink: 0xff1493,
	  deepskyblue: 0x00bfff,
	  dimgray: 0x696969,
	  dimgrey: 0x696969,
	  dodgerblue: 0x1e90ff,
	  firebrick: 0xb22222,
	  floralwhite: 0xfffaf0,
	  forestgreen: 0x228b22,
	  fuchsia: 0xff00ff,
	  gainsboro: 0xdcdcdc,
	  ghostwhite: 0xf8f8ff,
	  gold: 0xffd700,
	  goldenrod: 0xdaa520,
	  gray: 0x808080,
	  green: 0x008000,
	  greenyellow: 0xadff2f,
	  grey: 0x808080,
	  honeydew: 0xf0fff0,
	  hotpink: 0xff69b4,
	  indianred: 0xcd5c5c,
	  indigo: 0x4b0082,
	  ivory: 0xfffff0,
	  khaki: 0xf0e68c,
	  lavender: 0xe6e6fa,
	  lavenderblush: 0xfff0f5,
	  lawngreen: 0x7cfc00,
	  lemonchiffon: 0xfffacd,
	  lightblue: 0xadd8e6,
	  lightcoral: 0xf08080,
	  lightcyan: 0xe0ffff,
	  lightgoldenrodyellow: 0xfafad2,
	  lightgray: 0xd3d3d3,
	  lightgreen: 0x90ee90,
	  lightgrey: 0xd3d3d3,
	  lightpink: 0xffb6c1,
	  lightsalmon: 0xffa07a,
	  lightseagreen: 0x20b2aa,
	  lightskyblue: 0x87cefa,
	  lightslategray: 0x778899,
	  lightslategrey: 0x778899,
	  lightsteelblue: 0xb0c4de,
	  lightyellow: 0xffffe0,
	  lime: 0x00ff00,
	  limegreen: 0x32cd32,
	  linen: 0xfaf0e6,
	  magenta: 0xff00ff,
	  maroon: 0x800000,
	  mediumaquamarine: 0x66cdaa,
	  mediumblue: 0x0000cd,
	  mediumorchid: 0xba55d3,
	  mediumpurple: 0x9370db,
	  mediumseagreen: 0x3cb371,
	  mediumslateblue: 0x7b68ee,
	  mediumspringgreen: 0x00fa9a,
	  mediumturquoise: 0x48d1cc,
	  mediumvioletred: 0xc71585,
	  midnightblue: 0x191970,
	  mintcream: 0xf5fffa,
	  mistyrose: 0xffe4e1,
	  moccasin: 0xffe4b5,
	  navajowhite: 0xffdead,
	  navy: 0x000080,
	  oldlace: 0xfdf5e6,
	  olive: 0x808000,
	  olivedrab: 0x6b8e23,
	  orange: 0xffa500,
	  orangered: 0xff4500,
	  orchid: 0xda70d6,
	  palegoldenrod: 0xeee8aa,
	  palegreen: 0x98fb98,
	  paleturquoise: 0xafeeee,
	  palevioletred: 0xdb7093,
	  papayawhip: 0xffefd5,
	  peachpuff: 0xffdab9,
	  peru: 0xcd853f,
	  pink: 0xffc0cb,
	  plum: 0xdda0dd,
	  powderblue: 0xb0e0e6,
	  purple: 0x800080,
	  rebeccapurple: 0x663399,
	  red: 0xff0000,
	  rosybrown: 0xbc8f8f,
	  royalblue: 0x4169e1,
	  saddlebrown: 0x8b4513,
	  salmon: 0xfa8072,
	  sandybrown: 0xf4a460,
	  seagreen: 0x2e8b57,
	  seashell: 0xfff5ee,
	  sienna: 0xa0522d,
	  silver: 0xc0c0c0,
	  skyblue: 0x87ceeb,
	  slateblue: 0x6a5acd,
	  slategray: 0x708090,
	  slategrey: 0x708090,
	  snow: 0xfffafa,
	  springgreen: 0x00ff7f,
	  steelblue: 0x4682b4,
	  tan: 0xd2b48c,
	  teal: 0x008080,
	  thistle: 0xd8bfd8,
	  tomato: 0xff6347,
	  turquoise: 0x40e0d0,
	  violet: 0xee82ee,
	  wheat: 0xf5deb3,
	  white: 0xffffff,
	  whitesmoke: 0xf5f5f5,
	  yellow: 0xffff00,
	  yellowgreen: 0x9acd32
	};

	define(Color, color, {
	  displayable: function() {
	    return this.rgb().displayable();
	  },
	  toString: function() {
	    return this.rgb() + "";
	  }
	});

	function color(format) {
	  var m;
	  format = (format + "").trim().toLowerCase();
	  return (m = reHex3.exec(format)) ? (m = parseInt(m[1], 16), new Rgb((m >> 8 & 0xf) | (m >> 4 & 0x0f0), (m >> 4 & 0xf) | (m & 0xf0), ((m & 0xf) << 4) | (m & 0xf), 1)) // #f00
	      : (m = reHex6.exec(format)) ? rgbn(parseInt(m[1], 16)) // #ff0000
	      : (m = reRgbInteger.exec(format)) ? new Rgb(m[1], m[2], m[3], 1) // rgb(255, 0, 0)
	      : (m = reRgbPercent.exec(format)) ? new Rgb(m[1] * 255 / 100, m[2] * 255 / 100, m[3] * 255 / 100, 1) // rgb(100%, 0%, 0%)
	      : (m = reRgbaInteger.exec(format)) ? rgba(m[1], m[2], m[3], m[4]) // rgba(255, 0, 0, 1)
	      : (m = reRgbaPercent.exec(format)) ? rgba(m[1] * 255 / 100, m[2] * 255 / 100, m[3] * 255 / 100, m[4]) // rgb(100%, 0%, 0%, 1)
	      : (m = reHslPercent.exec(format)) ? hsla(m[1], m[2] / 100, m[3] / 100, 1) // hsl(120, 50%, 50%)
	      : (m = reHslaPercent.exec(format)) ? hsla(m[1], m[2] / 100, m[3] / 100, m[4]) // hsla(120, 50%, 50%, 1)
	      : named.hasOwnProperty(format) ? rgbn(named[format])
	      : format === "transparent" ? new Rgb(NaN, NaN, NaN, 0)
	      : null;
	}

	function rgbn(n) {
	  return new Rgb(n >> 16 & 0xff, n >> 8 & 0xff, n & 0xff, 1);
	}

	function rgba(r, g, b, a) {
	  if (a <= 0) r = g = b = NaN;
	  return new Rgb(r, g, b, a);
	}

	function rgbConvert(o) {
	  if (!(o instanceof Color)) o = color(o);
	  if (!o) return new Rgb;
	  o = o.rgb();
	  return new Rgb(o.r, o.g, o.b, o.opacity);
	}

	function rgb(r, g, b, opacity) {
	  return arguments.length === 1 ? rgbConvert(r) : new Rgb(r, g, b, opacity == null ? 1 : opacity);
	}

	function Rgb(r, g, b, opacity) {
	  this.r = +r;
	  this.g = +g;
	  this.b = +b;
	  this.opacity = +opacity;
	}

	define(Rgb, rgb, extend(Color, {
	  brighter: function(k) {
	    k = k == null ? brighter : Math.pow(brighter, k);
	    return new Rgb(this.r * k, this.g * k, this.b * k, this.opacity);
	  },
	  darker: function(k) {
	    k = k == null ? darker : Math.pow(darker, k);
	    return new Rgb(this.r * k, this.g * k, this.b * k, this.opacity);
	  },
	  rgb: function() {
	    return this;
	  },
	  displayable: function() {
	    return (0 <= this.r && this.r <= 255)
	        && (0 <= this.g && this.g <= 255)
	        && (0 <= this.b && this.b <= 255)
	        && (0 <= this.opacity && this.opacity <= 1);
	  },
	  toString: function() {
	    var a = this.opacity; a = isNaN(a) ? 1 : Math.max(0, Math.min(1, a));
	    return (a === 1 ? "rgb(" : "rgba(")
	        + Math.max(0, Math.min(255, Math.round(this.r) || 0)) + ", "
	        + Math.max(0, Math.min(255, Math.round(this.g) || 0)) + ", "
	        + Math.max(0, Math.min(255, Math.round(this.b) || 0))
	        + (a === 1 ? ")" : ", " + a + ")");
	  }
	}));

	function hsla(h, s, l, a) {
	  if (a <= 0) h = s = l = NaN;
	  else if (l <= 0 || l >= 1) h = s = NaN;
	  else if (s <= 0) h = NaN;
	  return new Hsl(h, s, l, a);
	}

	function hslConvert(o) {
	  if (o instanceof Hsl) return new Hsl(o.h, o.s, o.l, o.opacity);
	  if (!(o instanceof Color)) o = color(o);
	  if (!o) return new Hsl;
	  if (o instanceof Hsl) return o;
	  o = o.rgb();
	  var r = o.r / 255,
	      g = o.g / 255,
	      b = o.b / 255,
	      min = Math.min(r, g, b),
	      max = Math.max(r, g, b),
	      h = NaN,
	      s = max - min,
	      l = (max + min) / 2;
	  if (s) {
	    if (r === max) h = (g - b) / s + (g < b) * 6;
	    else if (g === max) h = (b - r) / s + 2;
	    else h = (r - g) / s + 4;
	    s /= l < 0.5 ? max + min : 2 - max - min;
	    h *= 60;
	  } else {
	    s = l > 0 && l < 1 ? 0 : h;
	  }
	  return new Hsl(h, s, l, o.opacity);
	}

	function hsl(h, s, l, opacity) {
	  return arguments.length === 1 ? hslConvert(h) : new Hsl(h, s, l, opacity == null ? 1 : opacity);
	}

	function Hsl(h, s, l, opacity) {
	  this.h = +h;
	  this.s = +s;
	  this.l = +l;
	  this.opacity = +opacity;
	}

	define(Hsl, hsl, extend(Color, {
	  brighter: function(k) {
	    k = k == null ? brighter : Math.pow(brighter, k);
	    return new Hsl(this.h, this.s, this.l * k, this.opacity);
	  },
	  darker: function(k) {
	    k = k == null ? darker : Math.pow(darker, k);
	    return new Hsl(this.h, this.s, this.l * k, this.opacity);
	  },
	  rgb: function() {
	    var h = this.h % 360 + (this.h < 0) * 360,
	        s = isNaN(h) || isNaN(this.s) ? 0 : this.s,
	        l = this.l,
	        m2 = l + (l < 0.5 ? l : 1 - l) * s,
	        m1 = 2 * l - m2;
	    return new Rgb(
	      hsl2rgb(h >= 240 ? h - 240 : h + 120, m1, m2),
	      hsl2rgb(h, m1, m2),
	      hsl2rgb(h < 120 ? h + 240 : h - 120, m1, m2),
	      this.opacity
	    );
	  },
	  displayable: function() {
	    return (0 <= this.s && this.s <= 1 || isNaN(this.s))
	        && (0 <= this.l && this.l <= 1)
	        && (0 <= this.opacity && this.opacity <= 1);
	  }
	}));

	/* From FvD 13.37, CSS Color Module Level 3 */
	function hsl2rgb(h, m1, m2) {
	  return (h < 60 ? m1 + (m2 - m1) * h / 60
	      : h < 180 ? m2
	      : h < 240 ? m1 + (m2 - m1) * (240 - h) / 60
	      : m1) * 255;
	}

	var deg2rad = Math.PI / 180;
	var rad2deg = 180 / Math.PI;

	var Kn = 18;
	var Xn = 0.950470;
	var Yn = 1;
	var Zn = 1.088830;
	var t0 = 4 / 29;
	var t1 = 6 / 29;
	var t2 = 3 * t1 * t1;
	var t3 = t1 * t1 * t1;

	function labConvert(o) {
	  if (o instanceof Lab) return new Lab(o.l, o.a, o.b, o.opacity);
	  if (o instanceof Hcl) {
	    var h = o.h * deg2rad;
	    return new Lab(o.l, Math.cos(h) * o.c, Math.sin(h) * o.c, o.opacity);
	  }
	  if (!(o instanceof Rgb)) o = rgbConvert(o);
	  var b = rgb2xyz(o.r),
	      a = rgb2xyz(o.g),
	      l = rgb2xyz(o.b),
	      x = xyz2lab((0.4124564 * b + 0.3575761 * a + 0.1804375 * l) / Xn),
	      y = xyz2lab((0.2126729 * b + 0.7151522 * a + 0.0721750 * l) / Yn),
	      z = xyz2lab((0.0193339 * b + 0.1191920 * a + 0.9503041 * l) / Zn);
	  return new Lab(116 * y - 16, 500 * (x - y), 200 * (y - z), o.opacity);
	}

	function lab(l, a, b, opacity) {
	  return arguments.length === 1 ? labConvert(l) : new Lab(l, a, b, opacity == null ? 1 : opacity);
	}

	function Lab(l, a, b, opacity) {
	  this.l = +l;
	  this.a = +a;
	  this.b = +b;
	  this.opacity = +opacity;
	}

	define(Lab, lab, extend(Color, {
	  brighter: function(k) {
	    return new Lab(this.l + Kn * (k == null ? 1 : k), this.a, this.b, this.opacity);
	  },
	  darker: function(k) {
	    return new Lab(this.l - Kn * (k == null ? 1 : k), this.a, this.b, this.opacity);
	  },
	  rgb: function() {
	    var y = (this.l + 16) / 116,
	        x = isNaN(this.a) ? y : y + this.a / 500,
	        z = isNaN(this.b) ? y : y - this.b / 200;
	    y = Yn * lab2xyz(y);
	    x = Xn * lab2xyz(x);
	    z = Zn * lab2xyz(z);
	    return new Rgb(
	      xyz2rgb( 3.2404542 * x - 1.5371385 * y - 0.4985314 * z), // D65 -> sRGB
	      xyz2rgb(-0.9692660 * x + 1.8760108 * y + 0.0415560 * z),
	      xyz2rgb( 0.0556434 * x - 0.2040259 * y + 1.0572252 * z),
	      this.opacity
	    );
	  }
	}));

	function xyz2lab(t) {
	  return t > t3 ? Math.pow(t, 1 / 3) : t / t2 + t0;
	}

	function lab2xyz(t) {
	  return t > t1 ? t * t * t : t2 * (t - t0);
	}

	function xyz2rgb(x) {
	  return 255 * (x <= 0.0031308 ? 12.92 * x : 1.055 * Math.pow(x, 1 / 2.4) - 0.055);
	}

	function rgb2xyz(x) {
	  return (x /= 255) <= 0.04045 ? x / 12.92 : Math.pow((x + 0.055) / 1.055, 2.4);
	}

	function hclConvert(o) {
	  if (o instanceof Hcl) return new Hcl(o.h, o.c, o.l, o.opacity);
	  if (!(o instanceof Lab)) o = labConvert(o);
	  var h = Math.atan2(o.b, o.a) * rad2deg;
	  return new Hcl(h < 0 ? h + 360 : h, Math.sqrt(o.a * o.a + o.b * o.b), o.l, o.opacity);
	}

	function hcl(h, c, l, opacity) {
	  return arguments.length === 1 ? hclConvert(h) : new Hcl(h, c, l, opacity == null ? 1 : opacity);
	}

	function Hcl(h, c, l, opacity) {
	  this.h = +h;
	  this.c = +c;
	  this.l = +l;
	  this.opacity = +opacity;
	}

	define(Hcl, hcl, extend(Color, {
	  brighter: function(k) {
	    return new Hcl(this.h, this.c, this.l + Kn * (k == null ? 1 : k), this.opacity);
	  },
	  darker: function(k) {
	    return new Hcl(this.h, this.c, this.l - Kn * (k == null ? 1 : k), this.opacity);
	  },
	  rgb: function() {
	    return labConvert(this).rgb();
	  }
	}));

	var A = -0.14861;
	var B = +1.78277;
	var C = -0.29227;
	var D = -0.90649;
	var E = +1.97294;
	var ED = E * D;
	var EB = E * B;
	var BC_DA = B * C - D * A;

	function cubehelixConvert(o) {
	  if (o instanceof Cubehelix) return new Cubehelix(o.h, o.s, o.l, o.opacity);
	  if (!(o instanceof Rgb)) o = rgbConvert(o);
	  var r = o.r / 255,
	      g = o.g / 255,
	      b = o.b / 255,
	      l = (BC_DA * b + ED * r - EB * g) / (BC_DA + ED - EB),
	      bl = b - l,
	      k = (E * (g - l) - C * bl) / D,
	      s = Math.sqrt(k * k + bl * bl) / (E * l * (1 - l)), // NaN if l=0 or l=1
	      h = s ? Math.atan2(k, bl) * rad2deg - 120 : NaN;
	  return new Cubehelix(h < 0 ? h + 360 : h, s, l, o.opacity);
	}

	function cubehelix(h, s, l, opacity) {
	  return arguments.length === 1 ? cubehelixConvert(h) : new Cubehelix(h, s, l, opacity == null ? 1 : opacity);
	}

	function Cubehelix(h, s, l, opacity) {
	  this.h = +h;
	  this.s = +s;
	  this.l = +l;
	  this.opacity = +opacity;
	}

	define(Cubehelix, cubehelix, extend(Color, {
	  brighter: function(k) {
	    k = k == null ? brighter : Math.pow(brighter, k);
	    return new Cubehelix(this.h, this.s, this.l * k, this.opacity);
	  },
	  darker: function(k) {
	    k = k == null ? darker : Math.pow(darker, k);
	    return new Cubehelix(this.h, this.s, this.l * k, this.opacity);
	  },
	  rgb: function() {
	    var h = isNaN(this.h) ? 0 : (this.h + 120) * deg2rad,
	        l = +this.l,
	        a = isNaN(this.s) ? 0 : this.s * l * (1 - l),
	        cosh = Math.cos(h),
	        sinh = Math.sin(h);
	    return new Rgb(
	      255 * (l + a * (A * cosh + B * sinh)),
	      255 * (l + a * (C * cosh + D * sinh)),
	      255 * (l + a * (E * cosh)),
	      this.opacity
	    );
	  }
	}));

	function basis(t1, v0, v1, v2, v3) {
	  var t2 = t1 * t1, t3 = t2 * t1;
	  return ((1 - 3 * t1 + 3 * t2 - t3) * v0
	      + (4 - 6 * t2 + 3 * t3) * v1
	      + (1 + 3 * t1 + 3 * t2 - 3 * t3) * v2
	      + t3 * v3) / 6;
	}

	function basis$1(values) {
	  var n = values.length - 1;
	  return function(t) {
	    var i = t <= 0 ? (t = 0) : t >= 1 ? (t = 1, n - 1) : Math.floor(t * n),
	        v1 = values[i],
	        v2 = values[i + 1],
	        v0 = i > 0 ? values[i - 1] : 2 * v1 - v2,
	        v3 = i < n - 1 ? values[i + 2] : 2 * v2 - v1;
	    return basis((t - i / n) * n, v0, v1, v2, v3);
	  };
	}

	function basisClosed(values) {
	  var n = values.length;
	  return function(t) {
	    var i = Math.floor(((t %= 1) < 0 ? ++t : t) * n),
	        v0 = values[(i + n - 1) % n],
	        v1 = values[i % n],
	        v2 = values[(i + 1) % n],
	        v3 = values[(i + 2) % n];
	    return basis((t - i / n) * n, v0, v1, v2, v3);
	  };
	}

	function constant$3(x) {
	  return function() {
	    return x;
	  };
	}

	function linear(a, d) {
	  return function(t) {
	    return a + t * d;
	  };
	}

	function exponential(a, b, y) {
	  return a = Math.pow(a, y), b = Math.pow(b, y) - a, y = 1 / y, function(t) {
	    return Math.pow(a + t * b, y);
	  };
	}

	function hue(a, b) {
	  var d = b - a;
	  return d ? linear(a, d > 180 || d < -180 ? d - 360 * Math.round(d / 360) : d) : constant$3(isNaN(a) ? b : a);
	}

	function gamma(y) {
	  return (y = +y) === 1 ? nogamma : function(a, b) {
	    return b - a ? exponential(a, b, y) : constant$3(isNaN(a) ? b : a);
	  };
	}

	function nogamma(a, b) {
	  var d = b - a;
	  return d ? linear(a, d) : constant$3(isNaN(a) ? b : a);
	}

	var interpolateRgb = (function rgbGamma(y) {
	  var color$$1 = gamma(y);

	  function rgb$$1(start, end) {
	    var r = color$$1((start = rgb(start)).r, (end = rgb(end)).r),
	        g = color$$1(start.g, end.g),
	        b = color$$1(start.b, end.b),
	        opacity = nogamma(start.opacity, end.opacity);
	    return function(t) {
	      start.r = r(t);
	      start.g = g(t);
	      start.b = b(t);
	      start.opacity = opacity(t);
	      return start + "";
	    };
	  }

	  rgb$$1.gamma = rgbGamma;

	  return rgb$$1;
	})(1);

	function rgbSpline(spline) {
	  return function(colors) {
	    var n = colors.length,
	        r = new Array(n),
	        g = new Array(n),
	        b = new Array(n),
	        i, color$$1;
	    for (i = 0; i < n; ++i) {
	      color$$1 = rgb(colors[i]);
	      r[i] = color$$1.r || 0;
	      g[i] = color$$1.g || 0;
	      b[i] = color$$1.b || 0;
	    }
	    r = spline(r);
	    g = spline(g);
	    b = spline(b);
	    color$$1.opacity = 1;
	    return function(t) {
	      color$$1.r = r(t);
	      color$$1.g = g(t);
	      color$$1.b = b(t);
	      return color$$1 + "";
	    };
	  };
	}

	var rgbBasis = rgbSpline(basis$1);
	var rgbBasisClosed = rgbSpline(basisClosed);

	function array$1(a, b) {
	  var nb = b ? b.length : 0,
	      na = a ? Math.min(nb, a.length) : 0,
	      x = new Array(na),
	      c = new Array(nb),
	      i;

	  for (i = 0; i < na; ++i) x[i] = interpolateValue(a[i], b[i]);
	  for (; i < nb; ++i) c[i] = b[i];

	  return function(t) {
	    for (i = 0; i < na; ++i) c[i] = x[i](t);
	    return c;
	  };
	}

	function date(a, b) {
	  var d = new Date;
	  return a = +a, b -= a, function(t) {
	    return d.setTime(a + b * t), d;
	  };
	}

	function reinterpolate(a, b) {
	  return a = +a, b -= a, function(t) {
	    return a + b * t;
	  };
	}

	function object(a, b) {
	  var i = {},
	      c = {},
	      k;

	  if (a === null || typeof a !== "object") a = {};
	  if (b === null || typeof b !== "object") b = {};

	  for (k in b) {
	    if (k in a) {
	      i[k] = interpolateValue(a[k], b[k]);
	    } else {
	      c[k] = b[k];
	    }
	  }

	  return function(t) {
	    for (k in i) c[k] = i[k](t);
	    return c;
	  };
	}

	var reA = /[-+]?(?:\d+\.?\d*|\.?\d+)(?:[eE][-+]?\d+)?/g;
	var reB = new RegExp(reA.source, "g");

	function zero(b) {
	  return function() {
	    return b;
	  };
	}

	function one(b) {
	  return function(t) {
	    return b(t) + "";
	  };
	}

	function interpolateString(a, b) {
	  var bi = reA.lastIndex = reB.lastIndex = 0, // scan index for next number in b
	      am, // current match in a
	      bm, // current match in b
	      bs, // string preceding current number in b, if any
	      i = -1, // index in s
	      s = [], // string constants and placeholders
	      q = []; // number interpolators

	  // Coerce inputs to strings.
	  a = a + "", b = b + "";

	  // Interpolate pairs of numbers in a & b.
	  while ((am = reA.exec(a))
	      && (bm = reB.exec(b))) {
	    if ((bs = bm.index) > bi) { // a string precedes the next number in b
	      bs = b.slice(bi, bs);
	      if (s[i]) s[i] += bs; // coalesce with previous string
	      else s[++i] = bs;
	    }
	    if ((am = am[0]) === (bm = bm[0])) { // numbers in a & b match
	      if (s[i]) s[i] += bm; // coalesce with previous string
	      else s[++i] = bm;
	    } else { // interpolate non-matching numbers
	      s[++i] = null;
	      q.push({i: i, x: reinterpolate(am, bm)});
	    }
	    bi = reB.lastIndex;
	  }

	  // Add remains of b.
	  if (bi < b.length) {
	    bs = b.slice(bi);
	    if (s[i]) s[i] += bs; // coalesce with previous string
	    else s[++i] = bs;
	  }

	  // Special optimization for only a single match.
	  // Otherwise, interpolate each of the numbers and rejoin the string.
	  return s.length < 2 ? (q[0]
	      ? one(q[0].x)
	      : zero(b))
	      : (b = q.length, function(t) {
	          for (var i = 0, o; i < b; ++i) s[(o = q[i]).i] = o.x(t);
	          return s.join("");
	        });
	}

	function interpolateValue(a, b) {
	  var t = typeof b, c;
	  return b == null || t === "boolean" ? constant$3(b)
	      : (t === "number" ? reinterpolate
	      : t === "string" ? ((c = color(b)) ? (b = c, interpolateRgb) : interpolateString)
	      : b instanceof color ? interpolateRgb
	      : b instanceof Date ? date
	      : Array.isArray(b) ? array$1
	      : typeof b.valueOf !== "function" && typeof b.toString !== "function" || isNaN(b) ? object
	      : reinterpolate)(a, b);
	}

	function interpolateRound(a, b) {
	  return a = +a, b -= a, function(t) {
	    return Math.round(a + b * t);
	  };
	}

	var degrees = 180 / Math.PI;

	var identity$2 = {
	  translateX: 0,
	  translateY: 0,
	  rotate: 0,
	  skewX: 0,
	  scaleX: 1,
	  scaleY: 1
	};

	function decompose(a, b, c, d, e, f) {
	  var scaleX, scaleY, skewX;
	  if (scaleX = Math.sqrt(a * a + b * b)) a /= scaleX, b /= scaleX;
	  if (skewX = a * c + b * d) c -= a * skewX, d -= b * skewX;
	  if (scaleY = Math.sqrt(c * c + d * d)) c /= scaleY, d /= scaleY, skewX /= scaleY;
	  if (a * d < b * c) a = -a, b = -b, skewX = -skewX, scaleX = -scaleX;
	  return {
	    translateX: e,
	    translateY: f,
	    rotate: Math.atan2(b, a) * degrees,
	    skewX: Math.atan(skewX) * degrees,
	    scaleX: scaleX,
	    scaleY: scaleY
	  };
	}

	var cssNode;
	var cssRoot;
	var cssView;
	var svgNode;

	function parseCss(value) {
	  if (value === "none") return identity$2;
	  if (!cssNode) cssNode = document.createElement("DIV"), cssRoot = document.documentElement, cssView = document.defaultView;
	  cssNode.style.transform = value;
	  value = cssView.getComputedStyle(cssRoot.appendChild(cssNode), null).getPropertyValue("transform");
	  cssRoot.removeChild(cssNode);
	  value = value.slice(7, -1).split(",");
	  return decompose(+value[0], +value[1], +value[2], +value[3], +value[4], +value[5]);
	}

	function parseSvg(value) {
	  if (value == null) return identity$2;
	  if (!svgNode) svgNode = document.createElementNS("http://www.w3.org/2000/svg", "g");
	  svgNode.setAttribute("transform", value);
	  if (!(value = svgNode.transform.baseVal.consolidate())) return identity$2;
	  value = value.matrix;
	  return decompose(value.a, value.b, value.c, value.d, value.e, value.f);
	}

	function interpolateTransform(parse, pxComma, pxParen, degParen) {

	  function pop(s) {
	    return s.length ? s.pop() + " " : "";
	  }

	  function translate(xa, ya, xb, yb, s, q) {
	    if (xa !== xb || ya !== yb) {
	      var i = s.push("translate(", null, pxComma, null, pxParen);
	      q.push({i: i - 4, x: reinterpolate(xa, xb)}, {i: i - 2, x: reinterpolate(ya, yb)});
	    } else if (xb || yb) {
	      s.push("translate(" + xb + pxComma + yb + pxParen);
	    }
	  }

	  function rotate(a, b, s, q) {
	    if (a !== b) {
	      if (a - b > 180) b += 360; else if (b - a > 180) a += 360; // shortest path
	      q.push({i: s.push(pop(s) + "rotate(", null, degParen) - 2, x: reinterpolate(a, b)});
	    } else if (b) {
	      s.push(pop(s) + "rotate(" + b + degParen);
	    }
	  }

	  function skewX(a, b, s, q) {
	    if (a !== b) {
	      q.push({i: s.push(pop(s) + "skewX(", null, degParen) - 2, x: reinterpolate(a, b)});
	    } else if (b) {
	      s.push(pop(s) + "skewX(" + b + degParen);
	    }
	  }

	  function scale(xa, ya, xb, yb, s, q) {
	    if (xa !== xb || ya !== yb) {
	      var i = s.push(pop(s) + "scale(", null, ",", null, ")");
	      q.push({i: i - 4, x: reinterpolate(xa, xb)}, {i: i - 2, x: reinterpolate(ya, yb)});
	    } else if (xb !== 1 || yb !== 1) {
	      s.push(pop(s) + "scale(" + xb + "," + yb + ")");
	    }
	  }

	  return function(a, b) {
	    var s = [], // string constants and placeholders
	        q = []; // number interpolators
	    a = parse(a), b = parse(b);
	    translate(a.translateX, a.translateY, b.translateX, b.translateY, s, q);
	    rotate(a.rotate, b.rotate, s, q);
	    skewX(a.skewX, b.skewX, s, q);
	    scale(a.scaleX, a.scaleY, b.scaleX, b.scaleY, s, q);
	    a = b = null; // gc
	    return function(t) {
	      var i = -1, n = q.length, o;
	      while (++i < n) s[(o = q[i]).i] = o.x(t);
	      return s.join("");
	    };
	  };
	}

	var interpolateTransformCss = interpolateTransform(parseCss, "px, ", "px)", "deg)");
	var interpolateTransformSvg = interpolateTransform(parseSvg, ", ", ")", ")");

	var rho = Math.SQRT2;
	var rho2 = 2;
	var rho4 = 4;
	var epsilon2 = 1e-12;

	function cosh(x) {
	  return ((x = Math.exp(x)) + 1 / x) / 2;
	}

	function sinh(x) {
	  return ((x = Math.exp(x)) - 1 / x) / 2;
	}

	function tanh(x) {
	  return ((x = Math.exp(2 * x)) - 1) / (x + 1);
	}

	// p0 = [ux0, uy0, w0]
	// p1 = [ux1, uy1, w1]
	function interpolateZoom(p0, p1) {
	  var ux0 = p0[0], uy0 = p0[1], w0 = p0[2],
	      ux1 = p1[0], uy1 = p1[1], w1 = p1[2],
	      dx = ux1 - ux0,
	      dy = uy1 - uy0,
	      d2 = dx * dx + dy * dy,
	      i,
	      S;

	  // Special case for u0 ≅ u1.
	  if (d2 < epsilon2) {
	    S = Math.log(w1 / w0) / rho;
	    i = function(t) {
	      return [
	        ux0 + t * dx,
	        uy0 + t * dy,
	        w0 * Math.exp(rho * t * S)
	      ];
	    };
	  }

	  // General case.
	  else {
	    var d1 = Math.sqrt(d2),
	        b0 = (w1 * w1 - w0 * w0 + rho4 * d2) / (2 * w0 * rho2 * d1),
	        b1 = (w1 * w1 - w0 * w0 - rho4 * d2) / (2 * w1 * rho2 * d1),
	        r0 = Math.log(Math.sqrt(b0 * b0 + 1) - b0),
	        r1 = Math.log(Math.sqrt(b1 * b1 + 1) - b1);
	    S = (r1 - r0) / rho;
	    i = function(t) {
	      var s = t * S,
	          coshr0 = cosh(r0),
	          u = w0 / (rho2 * d1) * (coshr0 * tanh(rho * s + r0) - sinh(r0));
	      return [
	        ux0 + u * dx,
	        uy0 + u * dy,
	        w0 * coshr0 / cosh(rho * s + r0)
	      ];
	    };
	  }

	  i.duration = S * 1000;

	  return i;
	}

	function hsl$1(hue$$1) {
	  return function(start, end) {
	    var h = hue$$1((start = hsl(start)).h, (end = hsl(end)).h),
	        s = nogamma(start.s, end.s),
	        l = nogamma(start.l, end.l),
	        opacity = nogamma(start.opacity, end.opacity);
	    return function(t) {
	      start.h = h(t);
	      start.s = s(t);
	      start.l = l(t);
	      start.opacity = opacity(t);
	      return start + "";
	    };
	  }
	}

	var hsl$2 = hsl$1(hue);
	var hslLong = hsl$1(nogamma);

	function lab$1(start, end) {
	  var l = nogamma((start = lab(start)).l, (end = lab(end)).l),
	      a = nogamma(start.a, end.a),
	      b = nogamma(start.b, end.b),
	      opacity = nogamma(start.opacity, end.opacity);
	  return function(t) {
	    start.l = l(t);
	    start.a = a(t);
	    start.b = b(t);
	    start.opacity = opacity(t);
	    return start + "";
	  };
	}

	function hcl$1(hue$$1) {
	  return function(start, end) {
	    var h = hue$$1((start = hcl(start)).h, (end = hcl(end)).h),
	        c = nogamma(start.c, end.c),
	        l = nogamma(start.l, end.l),
	        opacity = nogamma(start.opacity, end.opacity);
	    return function(t) {
	      start.h = h(t);
	      start.c = c(t);
	      start.l = l(t);
	      start.opacity = opacity(t);
	      return start + "";
	    };
	  }
	}

	var hcl$2 = hcl$1(hue);
	var hclLong = hcl$1(nogamma);

	function cubehelix$1(hue$$1) {
	  return (function cubehelixGamma(y) {
	    y = +y;

	    function cubehelix$$1(start, end) {
	      var h = hue$$1((start = cubehelix(start)).h, (end = cubehelix(end)).h),
	          s = nogamma(start.s, end.s),
	          l = nogamma(start.l, end.l),
	          opacity = nogamma(start.opacity, end.opacity);
	      return function(t) {
	        start.h = h(t);
	        start.s = s(t);
	        start.l = l(Math.pow(t, y));
	        start.opacity = opacity(t);
	        return start + "";
	      };
	    }

	    cubehelix$$1.gamma = cubehelixGamma;

	    return cubehelix$$1;
	  })(1);
	}

	var cubehelix$2 = cubehelix$1(hue);
	var cubehelixLong = cubehelix$1(nogamma);

	function quantize(interpolator, n) {
	  var samples = new Array(n);
	  for (var i = 0; i < n; ++i) samples[i] = interpolator(i / (n - 1));
	  return samples;
	}

	var frame = 0;
	var timeout = 0;
	var interval = 0;
	var pokeDelay = 1000;
	var taskHead;
	var taskTail;
	var clockLast = 0;
	var clockNow = 0;
	var clockSkew = 0;
	var clock = typeof performance === "object" && performance.now ? performance : Date;
	var setFrame = typeof window === "object" && window.requestAnimationFrame ? window.requestAnimationFrame.bind(window) : function(f) { setTimeout(f, 17); };

	function now() {
	  return clockNow || (setFrame(clearNow), clockNow = clock.now() + clockSkew);
	}

	function clearNow() {
	  clockNow = 0;
	}

	function Timer() {
	  this._call =
	  this._time =
	  this._next = null;
	}

	Timer.prototype = timer.prototype = {
	  constructor: Timer,
	  restart: function(callback, delay, time) {
	    if (typeof callback !== "function") throw new TypeError("callback is not a function");
	    time = (time == null ? now() : +time) + (delay == null ? 0 : +delay);
	    if (!this._next && taskTail !== this) {
	      if (taskTail) taskTail._next = this;
	      else taskHead = this;
	      taskTail = this;
	    }
	    this._call = callback;
	    this._time = time;
	    sleep();
	  },
	  stop: function() {
	    if (this._call) {
	      this._call = null;
	      this._time = Infinity;
	      sleep();
	    }
	  }
	};

	function timer(callback, delay, time) {
	  var t = new Timer;
	  t.restart(callback, delay, time);
	  return t;
	}

	function timerFlush() {
	  now(); // Get the current time, if not already set.
	  ++frame; // Pretend we’ve set an alarm, if we haven’t already.
	  var t = taskHead, e;
	  while (t) {
	    if ((e = clockNow - t._time) >= 0) t._call.call(null, e);
	    t = t._next;
	  }
	  --frame;
	}

	function wake() {
	  clockNow = (clockLast = clock.now()) + clockSkew;
	  frame = timeout = 0;
	  try {
	    timerFlush();
	  } finally {
	    frame = 0;
	    nap();
	    clockNow = 0;
	  }
	}

	function poke() {
	  var now = clock.now(), delay = now - clockLast;
	  if (delay > pokeDelay) clockSkew -= delay, clockLast = now;
	}

	function nap() {
	  var t0, t1 = taskHead, t2, time = Infinity;
	  while (t1) {
	    if (t1._call) {
	      if (time > t1._time) time = t1._time;
	      t0 = t1, t1 = t1._next;
	    } else {
	      t2 = t1._next, t1._next = null;
	      t1 = t0 ? t0._next = t2 : taskHead = t2;
	    }
	  }
	  taskTail = t0;
	  sleep(time);
	}

	function sleep(time) {
	  if (frame) return; // Soonest alarm already set, or will be.
	  if (timeout) timeout = clearTimeout(timeout);
	  var delay = time - clockNow; // Strictly less than if we recomputed clockNow.
	  if (delay > 24) {
	    if (time < Infinity) timeout = setTimeout(wake, time - clock.now() - clockSkew);
	    if (interval) interval = clearInterval(interval);
	  } else {
	    if (!interval) clockLast = clock.now(), interval = setInterval(poke, pokeDelay);
	    frame = 1, setFrame(wake);
	  }
	}

	function timeout$1(callback, delay, time) {
	  var t = new Timer;
	  delay = delay == null ? 0 : +delay;
	  t.restart(function(elapsed) {
	    t.stop();
	    callback(elapsed + delay);
	  }, delay, time);
	  return t;
	}

	function interval$1(callback, delay, time) {
	  var t = new Timer, total = delay;
	  if (delay == null) return t.restart(callback, delay, time), t;
	  delay = +delay, time = time == null ? now() : +time;
	  t.restart(function tick(elapsed) {
	    elapsed += total;
	    t.restart(tick, total += delay, time);
	    callback(elapsed);
	  }, delay, time);
	  return t;
	}

	var emptyOn = dispatch("start", "end", "interrupt");
	var emptyTween = [];

	var CREATED = 0;
	var SCHEDULED = 1;
	var STARTING = 2;
	var STARTED = 3;
	var RUNNING = 4;
	var ENDING = 5;
	var ENDED = 6;

	function schedule(node, name, id, index, group, timing) {
	  var schedules = node.__transition;
	  if (!schedules) node.__transition = {};
	  else if (id in schedules) return;
	  create$1(node, id, {
	    name: name,
	    index: index, // For context during callback.
	    group: group, // For context during callback.
	    on: emptyOn,
	    tween: emptyTween,
	    time: timing.time,
	    delay: timing.delay,
	    duration: timing.duration,
	    ease: timing.ease,
	    timer: null,
	    state: CREATED
	  });
	}

	function init(node, id) {
	  var schedule = get$1(node, id);
	  if (schedule.state > CREATED) throw new Error("too late; already scheduled");
	  return schedule;
	}

	function set$1(node, id) {
	  var schedule = get$1(node, id);
	  if (schedule.state > STARTING) throw new Error("too late; already started");
	  return schedule;
	}

	function get$1(node, id) {
	  var schedule = node.__transition;
	  if (!schedule || !(schedule = schedule[id])) throw new Error("transition not found");
	  return schedule;
	}

	function create$1(node, id, self) {
	  var schedules = node.__transition,
	      tween;

	  // Initialize the self timer when the transition is created.
	  // Note the actual delay is not known until the first callback!
	  schedules[id] = self;
	  self.timer = timer(schedule, 0, self.time);

	  function schedule(elapsed) {
	    self.state = SCHEDULED;
	    self.timer.restart(start, self.delay, self.time);

	    // If the elapsed delay is less than our first sleep, start immediately.
	    if (self.delay <= elapsed) start(elapsed - self.delay);
	  }

	  function start(elapsed) {
	    var i, j, n, o;

	    // If the state is not SCHEDULED, then we previously errored on start.
	    if (self.state !== SCHEDULED) return stop();

	    for (i in schedules) {
	      o = schedules[i];
	      if (o.name !== self.name) continue;

	      // While this element already has a starting transition during this frame,
	      // defer starting an interrupting transition until that transition has a
	      // chance to tick (and possibly end); see d3/d3-transition#54!
	      if (o.state === STARTED) return timeout$1(start);

	      // Interrupt the active transition, if any.
	      // Dispatch the interrupt event.
	      if (o.state === RUNNING) {
	        o.state = ENDED;
	        o.timer.stop();
	        o.on.call("interrupt", node, node.__data__, o.index, o.group);
	        delete schedules[i];
	      }

	      // Cancel any pre-empted transitions. No interrupt event is dispatched
	      // because the cancelled transitions never started. Note that this also
	      // removes this transition from the pending list!
	      else if (+i < id) {
	        o.state = ENDED;
	        o.timer.stop();
	        delete schedules[i];
	      }
	    }

	    // Defer the first tick to end of the current frame; see d3/d3#1576.
	    // Note the transition may be canceled after start and before the first tick!
	    // Note this must be scheduled before the start event; see d3/d3-transition#16!
	    // Assuming this is successful, subsequent callbacks go straight to tick.
	    timeout$1(function() {
	      if (self.state === STARTED) {
	        self.state = RUNNING;
	        self.timer.restart(tick, self.delay, self.time);
	        tick(elapsed);
	      }
	    });

	    // Dispatch the start event.
	    // Note this must be done before the tween are initialized.
	    self.state = STARTING;
	    self.on.call("start", node, node.__data__, self.index, self.group);
	    if (self.state !== STARTING) return; // interrupted
	    self.state = STARTED;

	    // Initialize the tween, deleting null tween.
	    tween = new Array(n = self.tween.length);
	    for (i = 0, j = -1; i < n; ++i) {
	      if (o = self.tween[i].value.call(node, node.__data__, self.index, self.group)) {
	        tween[++j] = o;
	      }
	    }
	    tween.length = j + 1;
	  }

	  function tick(elapsed) {
	    var t = elapsed < self.duration ? self.ease.call(null, elapsed / self.duration) : (self.timer.restart(stop), self.state = ENDING, 1),
	        i = -1,
	        n = tween.length;

	    while (++i < n) {
	      tween[i].call(null, t);
	    }

	    // Dispatch the end event.
	    if (self.state === ENDING) {
	      self.on.call("end", node, node.__data__, self.index, self.group);
	      stop();
	    }
	  }

	  function stop() {
	    self.state = ENDED;
	    self.timer.stop();
	    delete schedules[id];
	    for (var i in schedules) return; // eslint-disable-line no-unused-vars
	    delete node.__transition;
	  }
	}

	function interrupt(node, name) {
	  var schedules = node.__transition,
	      schedule$$1,
	      active,
	      empty = true,
	      i;

	  if (!schedules) return;

	  name = name == null ? null : name + "";

	  for (i in schedules) {
	    if ((schedule$$1 = schedules[i]).name !== name) { empty = false; continue; }
	    active = schedule$$1.state > STARTING && schedule$$1.state < ENDING;
	    schedule$$1.state = ENDED;
	    schedule$$1.timer.stop();
	    if (active) schedule$$1.on.call("interrupt", node, node.__data__, schedule$$1.index, schedule$$1.group);
	    delete schedules[i];
	  }

	  if (empty) delete node.__transition;
	}

	function selection_interrupt(name) {
	  return this.each(function() {
	    interrupt(this, name);
	  });
	}

	function tweenRemove(id, name) {
	  var tween0, tween1;
	  return function() {
	    var schedule$$1 = set$1(this, id),
	        tween = schedule$$1.tween;

	    // If this node shared tween with the previous node,
	    // just assign the updated shared tween and we’re done!
	    // Otherwise, copy-on-write.
	    if (tween !== tween0) {
	      tween1 = tween0 = tween;
	      for (var i = 0, n = tween1.length; i < n; ++i) {
	        if (tween1[i].name === name) {
	          tween1 = tween1.slice();
	          tween1.splice(i, 1);
	          break;
	        }
	      }
	    }

	    schedule$$1.tween = tween1;
	  };
	}

	function tweenFunction(id, name, value) {
	  var tween0, tween1;
	  if (typeof value !== "function") throw new Error;
	  return function() {
	    var schedule$$1 = set$1(this, id),
	        tween = schedule$$1.tween;

	    // If this node shared tween with the previous node,
	    // just assign the updated shared tween and we’re done!
	    // Otherwise, copy-on-write.
	    if (tween !== tween0) {
	      tween1 = (tween0 = tween).slice();
	      for (var t = {name: name, value: value}, i = 0, n = tween1.length; i < n; ++i) {
	        if (tween1[i].name === name) {
	          tween1[i] = t;
	          break;
	        }
	      }
	      if (i === n) tween1.push(t);
	    }

	    schedule$$1.tween = tween1;
	  };
	}

	function transition_tween(name, value) {
	  var id = this._id;

	  name += "";

	  if (arguments.length < 2) {
	    var tween = get$1(this.node(), id).tween;
	    for (var i = 0, n = tween.length, t; i < n; ++i) {
	      if ((t = tween[i]).name === name) {
	        return t.value;
	      }
	    }
	    return null;
	  }

	  return this.each((value == null ? tweenRemove : tweenFunction)(id, name, value));
	}

	function tweenValue(transition, name, value) {
	  var id = transition._id;

	  transition.each(function() {
	    var schedule$$1 = set$1(this, id);
	    (schedule$$1.value || (schedule$$1.value = {}))[name] = value.apply(this, arguments);
	  });

	  return function(node) {
	    return get$1(node, id).value[name];
	  };
	}

	function interpolate(a, b) {
	  var c;
	  return (typeof b === "number" ? reinterpolate
	      : b instanceof color ? interpolateRgb
	      : (c = color(b)) ? (b = c, interpolateRgb)
	      : interpolateString)(a, b);
	}

	function attrRemove$1(name) {
	  return function() {
	    this.removeAttribute(name);
	  };
	}

	function attrRemoveNS$1(fullname) {
	  return function() {
	    this.removeAttributeNS(fullname.space, fullname.local);
	  };
	}

	function attrConstant$1(name, interpolate$$1, value1) {
	  var value00,
	      interpolate0;
	  return function() {
	    var value0 = this.getAttribute(name);
	    return value0 === value1 ? null
	        : value0 === value00 ? interpolate0
	        : interpolate0 = interpolate$$1(value00 = value0, value1);
	  };
	}

	function attrConstantNS$1(fullname, interpolate$$1, value1) {
	  var value00,
	      interpolate0;
	  return function() {
	    var value0 = this.getAttributeNS(fullname.space, fullname.local);
	    return value0 === value1 ? null
	        : value0 === value00 ? interpolate0
	        : interpolate0 = interpolate$$1(value00 = value0, value1);
	  };
	}

	function attrFunction$1(name, interpolate$$1, value) {
	  var value00,
	      value10,
	      interpolate0;
	  return function() {
	    var value0, value1 = value(this);
	    if (value1 == null) return void this.removeAttribute(name);
	    value0 = this.getAttribute(name);
	    return value0 === value1 ? null
	        : value0 === value00 && value1 === value10 ? interpolate0
	        : interpolate0 = interpolate$$1(value00 = value0, value10 = value1);
	  };
	}

	function attrFunctionNS$1(fullname, interpolate$$1, value) {
	  var value00,
	      value10,
	      interpolate0;
	  return function() {
	    var value0, value1 = value(this);
	    if (value1 == null) return void this.removeAttributeNS(fullname.space, fullname.local);
	    value0 = this.getAttributeNS(fullname.space, fullname.local);
	    return value0 === value1 ? null
	        : value0 === value00 && value1 === value10 ? interpolate0
	        : interpolate0 = interpolate$$1(value00 = value0, value10 = value1);
	  };
	}

	function transition_attr(name, value) {
	  var fullname = namespace(name), i = fullname === "transform" ? interpolateTransformSvg : interpolate;
	  return this.attrTween(name, typeof value === "function"
	      ? (fullname.local ? attrFunctionNS$1 : attrFunction$1)(fullname, i, tweenValue(this, "attr." + name, value))
	      : value == null ? (fullname.local ? attrRemoveNS$1 : attrRemove$1)(fullname)
	      : (fullname.local ? attrConstantNS$1 : attrConstant$1)(fullname, i, value + ""));
	}

	function attrTweenNS(fullname, value) {
	  function tween() {
	    var node = this, i = value.apply(node, arguments);
	    return i && function(t) {
	      node.setAttributeNS(fullname.space, fullname.local, i(t));
	    };
	  }
	  tween._value = value;
	  return tween;
	}

	function attrTween(name, value) {
	  function tween() {
	    var node = this, i = value.apply(node, arguments);
	    return i && function(t) {
	      node.setAttribute(name, i(t));
	    };
	  }
	  tween._value = value;
	  return tween;
	}

	function transition_attrTween(name, value) {
	  var key = "attr." + name;
	  if (arguments.length < 2) return (key = this.tween(key)) && key._value;
	  if (value == null) return this.tween(key, null);
	  if (typeof value !== "function") throw new Error;
	  var fullname = namespace(name);
	  return this.tween(key, (fullname.local ? attrTweenNS : attrTween)(fullname, value));
	}

	function delayFunction(id, value) {
	  return function() {
	    init(this, id).delay = +value.apply(this, arguments);
	  };
	}

	function delayConstant(id, value) {
	  return value = +value, function() {
	    init(this, id).delay = value;
	  };
	}

	function transition_delay(value) {
	  var id = this._id;

	  return arguments.length
	      ? this.each((typeof value === "function"
	          ? delayFunction
	          : delayConstant)(id, value))
	      : get$1(this.node(), id).delay;
	}

	function durationFunction(id, value) {
	  return function() {
	    set$1(this, id).duration = +value.apply(this, arguments);
	  };
	}

	function durationConstant(id, value) {
	  return value = +value, function() {
	    set$1(this, id).duration = value;
	  };
	}

	function transition_duration(value) {
	  var id = this._id;

	  return arguments.length
	      ? this.each((typeof value === "function"
	          ? durationFunction
	          : durationConstant)(id, value))
	      : get$1(this.node(), id).duration;
	}

	function easeConstant(id, value) {
	  if (typeof value !== "function") throw new Error;
	  return function() {
	    set$1(this, id).ease = value;
	  };
	}

	function transition_ease(value) {
	  var id = this._id;

	  return arguments.length
	      ? this.each(easeConstant(id, value))
	      : get$1(this.node(), id).ease;
	}

	function transition_filter(match) {
	  if (typeof match !== "function") match = matcher$1(match);

	  for (var groups = this._groups, m = groups.length, subgroups = new Array(m), j = 0; j < m; ++j) {
	    for (var group = groups[j], n = group.length, subgroup = subgroups[j] = [], node, i = 0; i < n; ++i) {
	      if ((node = group[i]) && match.call(node, node.__data__, i, group)) {
	        subgroup.push(node);
	      }
	    }
	  }

	  return new Transition(subgroups, this._parents, this._name, this._id);
	}

	function transition_merge(transition$$1) {
	  if (transition$$1._id !== this._id) throw new Error;

	  for (var groups0 = this._groups, groups1 = transition$$1._groups, m0 = groups0.length, m1 = groups1.length, m = Math.min(m0, m1), merges = new Array(m0), j = 0; j < m; ++j) {
	    for (var group0 = groups0[j], group1 = groups1[j], n = group0.length, merge = merges[j] = new Array(n), node, i = 0; i < n; ++i) {
	      if (node = group0[i] || group1[i]) {
	        merge[i] = node;
	      }
	    }
	  }

	  for (; j < m0; ++j) {
	    merges[j] = groups0[j];
	  }

	  return new Transition(merges, this._parents, this._name, this._id);
	}

	function start(name) {
	  return (name + "").trim().split(/^|\s+/).every(function(t) {
	    var i = t.indexOf(".");
	    if (i >= 0) t = t.slice(0, i);
	    return !t || t === "start";
	  });
	}

	function onFunction(id, name, listener) {
	  var on0, on1, sit = start(name) ? init : set$1;
	  return function() {
	    var schedule$$1 = sit(this, id),
	        on = schedule$$1.on;

	    // If this node shared a dispatch with the previous node,
	    // just assign the updated shared dispatch and we’re done!
	    // Otherwise, copy-on-write.
	    if (on !== on0) (on1 = (on0 = on).copy()).on(name, listener);

	    schedule$$1.on = on1;
	  };
	}

	function transition_on(name, listener) {
	  var id = this._id;

	  return arguments.length < 2
	      ? get$1(this.node(), id).on.on(name)
	      : this.each(onFunction(id, name, listener));
	}

	function removeFunction(id) {
	  return function() {
	    var parent = this.parentNode;
	    for (var i in this.__transition) if (+i !== id) return;
	    if (parent) parent.removeChild(this);
	  };
	}

	function transition_remove() {
	  return this.on("end.remove", removeFunction(this._id));
	}

	function transition_select(select) {
	  var name = this._name,
	      id = this._id;

	  if (typeof select !== "function") select = selector(select);

	  for (var groups = this._groups, m = groups.length, subgroups = new Array(m), j = 0; j < m; ++j) {
	    for (var group = groups[j], n = group.length, subgroup = subgroups[j] = new Array(n), node, subnode, i = 0; i < n; ++i) {
	      if ((node = group[i]) && (subnode = select.call(node, node.__data__, i, group))) {
	        if ("__data__" in node) subnode.__data__ = node.__data__;
	        subgroup[i] = subnode;
	        schedule(subgroup[i], name, id, i, subgroup, get$1(node, id));
	      }
	    }
	  }

	  return new Transition(subgroups, this._parents, name, id);
	}

	function transition_selectAll(select) {
	  var name = this._name,
	      id = this._id;

	  if (typeof select !== "function") select = selectorAll(select);

	  for (var groups = this._groups, m = groups.length, subgroups = [], parents = [], j = 0; j < m; ++j) {
	    for (var group = groups[j], n = group.length, node, i = 0; i < n; ++i) {
	      if (node = group[i]) {
	        for (var children = select.call(node, node.__data__, i, group), child, inherit = get$1(node, id), k = 0, l = children.length; k < l; ++k) {
	          if (child = children[k]) {
	            schedule(child, name, id, k, children, inherit);
	          }
	        }
	        subgroups.push(children);
	        parents.push(node);
	      }
	    }
	  }

	  return new Transition(subgroups, parents, name, id);
	}

	var Selection$1 = selection.prototype.constructor;

	function transition_selection() {
	  return new Selection$1(this._groups, this._parents);
	}

	function styleRemove$1(name, interpolate$$1) {
	  var value00,
	      value10,
	      interpolate0;
	  return function() {
	    var value0 = styleValue(this, name),
	        value1 = (this.style.removeProperty(name), styleValue(this, name));
	    return value0 === value1 ? null
	        : value0 === value00 && value1 === value10 ? interpolate0
	        : interpolate0 = interpolate$$1(value00 = value0, value10 = value1);
	  };
	}

	function styleRemoveEnd(name) {
	  return function() {
	    this.style.removeProperty(name);
	  };
	}

	function styleConstant$1(name, interpolate$$1, value1) {
	  var value00,
	      interpolate0;
	  return function() {
	    var value0 = styleValue(this, name);
	    return value0 === value1 ? null
	        : value0 === value00 ? interpolate0
	        : interpolate0 = interpolate$$1(value00 = value0, value1);
	  };
	}

	function styleFunction$1(name, interpolate$$1, value) {
	  var value00,
	      value10,
	      interpolate0;
	  return function() {
	    var value0 = styleValue(this, name),
	        value1 = value(this);
	    if (value1 == null) value1 = (this.style.removeProperty(name), styleValue(this, name));
	    return value0 === value1 ? null
	        : value0 === value00 && value1 === value10 ? interpolate0
	        : interpolate0 = interpolate$$1(value00 = value0, value10 = value1);
	  };
	}

	function transition_style(name, value, priority) {
	  var i = (name += "") === "transform" ? interpolateTransformCss : interpolate;
	  return value == null ? this
	          .styleTween(name, styleRemove$1(name, i))
	          .on("end.style." + name, styleRemoveEnd(name))
	      : this.styleTween(name, typeof value === "function"
	          ? styleFunction$1(name, i, tweenValue(this, "style." + name, value))
	          : styleConstant$1(name, i, value + ""), priority);
	}

	function styleTween(name, value, priority) {
	  function tween() {
	    var node = this, i = value.apply(node, arguments);
	    return i && function(t) {
	      node.style.setProperty(name, i(t), priority);
	    };
	  }
	  tween._value = value;
	  return tween;
	}

	function transition_styleTween(name, value, priority) {
	  var key = "style." + (name += "");
	  if (arguments.length < 2) return (key = this.tween(key)) && key._value;
	  if (value == null) return this.tween(key, null);
	  if (typeof value !== "function") throw new Error;
	  return this.tween(key, styleTween(name, value, priority == null ? "" : priority));
	}

	function textConstant$1(value) {
	  return function() {
	    this.textContent = value;
	  };
	}

	function textFunction$1(value) {
	  return function() {
	    var value1 = value(this);
	    this.textContent = value1 == null ? "" : value1;
	  };
	}

	function transition_text(value) {
	  return this.tween("text", typeof value === "function"
	      ? textFunction$1(tweenValue(this, "text", value))
	      : textConstant$1(value == null ? "" : value + ""));
	}

	function transition_transition() {
	  var name = this._name,
	      id0 = this._id,
	      id1 = newId();

	  for (var groups = this._groups, m = groups.length, j = 0; j < m; ++j) {
	    for (var group = groups[j], n = group.length, node, i = 0; i < n; ++i) {
	      if (node = group[i]) {
	        var inherit = get$1(node, id0);
	        schedule(node, name, id1, i, group, {
	          time: inherit.time + inherit.delay + inherit.duration,
	          delay: 0,
	          duration: inherit.duration,
	          ease: inherit.ease
	        });
	      }
	    }
	  }

	  return new Transition(groups, this._parents, name, id1);
	}

	var id = 0;

	function Transition(groups, parents, name, id) {
	  this._groups = groups;
	  this._parents = parents;
	  this._name = name;
	  this._id = id;
	}

	function transition(name) {
	  return selection().transition(name);
	}

	function newId() {
	  return ++id;
	}

	var selection_prototype = selection.prototype;

	Transition.prototype = transition.prototype = {
	  constructor: Transition,
	  select: transition_select,
	  selectAll: transition_selectAll,
	  filter: transition_filter,
	  merge: transition_merge,
	  selection: transition_selection,
	  transition: transition_transition,
	  call: selection_prototype.call,
	  nodes: selection_prototype.nodes,
	  node: selection_prototype.node,
	  size: selection_prototype.size,
	  empty: selection_prototype.empty,
	  each: selection_prototype.each,
	  on: transition_on,
	  attr: transition_attr,
	  attrTween: transition_attrTween,
	  style: transition_style,
	  styleTween: transition_styleTween,
	  text: transition_text,
	  remove: transition_remove,
	  tween: transition_tween,
	  delay: transition_delay,
	  duration: transition_duration,
	  ease: transition_ease
	};

	function linear$1(t) {
	  return +t;
	}

	function quadIn(t) {
	  return t * t;
	}

	function quadOut(t) {
	  return t * (2 - t);
	}

	function quadInOut(t) {
	  return ((t *= 2) <= 1 ? t * t : --t * (2 - t) + 1) / 2;
	}

	function cubicIn(t) {
	  return t * t * t;
	}

	function cubicOut(t) {
	  return --t * t * t + 1;
	}

	function cubicInOut(t) {
	  return ((t *= 2) <= 1 ? t * t * t : (t -= 2) * t * t + 2) / 2;
	}

	var exponent = 3;

	var polyIn = (function custom(e) {
	  e = +e;

	  function polyIn(t) {
	    return Math.pow(t, e);
	  }

	  polyIn.exponent = custom;

	  return polyIn;
	})(exponent);

	var polyOut = (function custom(e) {
	  e = +e;

	  function polyOut(t) {
	    return 1 - Math.pow(1 - t, e);
	  }

	  polyOut.exponent = custom;

	  return polyOut;
	})(exponent);

	var polyInOut = (function custom(e) {
	  e = +e;

	  function polyInOut(t) {
	    return ((t *= 2) <= 1 ? Math.pow(t, e) : 2 - Math.pow(2 - t, e)) / 2;
	  }

	  polyInOut.exponent = custom;

	  return polyInOut;
	})(exponent);

	var pi = Math.PI;
	var halfPi = pi / 2;

	function sinIn(t) {
	  return 1 - Math.cos(t * halfPi);
	}

	function sinOut(t) {
	  return Math.sin(t * halfPi);
	}

	function sinInOut(t) {
	  return (1 - Math.cos(pi * t)) / 2;
	}

	function expIn(t) {
	  return Math.pow(2, 10 * t - 10);
	}

	function expOut(t) {
	  return 1 - Math.pow(2, -10 * t);
	}

	function expInOut(t) {
	  return ((t *= 2) <= 1 ? Math.pow(2, 10 * t - 10) : 2 - Math.pow(2, 10 - 10 * t)) / 2;
	}

	function circleIn(t) {
	  return 1 - Math.sqrt(1 - t * t);
	}

	function circleOut(t) {
	  return Math.sqrt(1 - --t * t);
	}

	function circleInOut(t) {
	  return ((t *= 2) <= 1 ? 1 - Math.sqrt(1 - t * t) : Math.sqrt(1 - (t -= 2) * t) + 1) / 2;
	}

	var b1 = 4 / 11;
	var b2 = 6 / 11;
	var b3 = 8 / 11;
	var b4 = 3 / 4;
	var b5 = 9 / 11;
	var b6 = 10 / 11;
	var b7 = 15 / 16;
	var b8 = 21 / 22;
	var b9 = 63 / 64;
	var b0 = 1 / b1 / b1;

	function bounceIn(t) {
	  return 1 - bounceOut(1 - t);
	}

	function bounceOut(t) {
	  return (t = +t) < b1 ? b0 * t * t : t < b3 ? b0 * (t -= b2) * t + b4 : t < b6 ? b0 * (t -= b5) * t + b7 : b0 * (t -= b8) * t + b9;
	}

	function bounceInOut(t) {
	  return ((t *= 2) <= 1 ? 1 - bounceOut(1 - t) : bounceOut(t - 1) + 1) / 2;
	}

	var overshoot = 1.70158;

	var backIn = (function custom(s) {
	  s = +s;

	  function backIn(t) {
	    return t * t * ((s + 1) * t - s);
	  }

	  backIn.overshoot = custom;

	  return backIn;
	})(overshoot);

	var backOut = (function custom(s) {
	  s = +s;

	  function backOut(t) {
	    return --t * t * ((s + 1) * t + s) + 1;
	  }

	  backOut.overshoot = custom;

	  return backOut;
	})(overshoot);

	var backInOut = (function custom(s) {
	  s = +s;

	  function backInOut(t) {
	    return ((t *= 2) < 1 ? t * t * ((s + 1) * t - s) : (t -= 2) * t * ((s + 1) * t + s) + 2) / 2;
	  }

	  backInOut.overshoot = custom;

	  return backInOut;
	})(overshoot);

	var tau = 2 * Math.PI;
	var amplitude = 1;
	var period = 0.3;

	var elasticIn = (function custom(a, p) {
	  var s = Math.asin(1 / (a = Math.max(1, a))) * (p /= tau);

	  function elasticIn(t) {
	    return a * Math.pow(2, 10 * --t) * Math.sin((s - t) / p);
	  }

	  elasticIn.amplitude = function(a) { return custom(a, p * tau); };
	  elasticIn.period = function(p) { return custom(a, p); };

	  return elasticIn;
	})(amplitude, period);

	var elasticOut = (function custom(a, p) {
	  var s = Math.asin(1 / (a = Math.max(1, a))) * (p /= tau);

	  function elasticOut(t) {
	    return 1 - a * Math.pow(2, -10 * (t = +t)) * Math.sin((t + s) / p);
	  }

	  elasticOut.amplitude = function(a) { return custom(a, p * tau); };
	  elasticOut.period = function(p) { return custom(a, p); };

	  return elasticOut;
	})(amplitude, period);

	var elasticInOut = (function custom(a, p) {
	  var s = Math.asin(1 / (a = Math.max(1, a))) * (p /= tau);

	  function elasticInOut(t) {
	    return ((t = t * 2 - 1) < 0
	        ? a * Math.pow(2, 10 * t) * Math.sin((s - t) / p)
	        : 2 - a * Math.pow(2, -10 * t) * Math.sin((s + t) / p)) / 2;
	  }

	  elasticInOut.amplitude = function(a) { return custom(a, p * tau); };
	  elasticInOut.period = function(p) { return custom(a, p); };

	  return elasticInOut;
	})(amplitude, period);

	var defaultTiming = {
	  time: null, // Set on use.
	  delay: 0,
	  duration: 250,
	  ease: cubicInOut
	};

	function inherit(node, id) {
	  var timing;
	  while (!(timing = node.__transition) || !(timing = timing[id])) {
	    if (!(node = node.parentNode)) {
	      return defaultTiming.time = now(), defaultTiming;
	    }
	  }
	  return timing;
	}

	function selection_transition(name) {
	  var id,
	      timing;

	  if (name instanceof Transition) {
	    id = name._id, name = name._name;
	  } else {
	    id = newId(), (timing = defaultTiming).time = now(), name = name == null ? null : name + "";
	  }

	  for (var groups = this._groups, m = groups.length, j = 0; j < m; ++j) {
	    for (var group = groups[j], n = group.length, node, i = 0; i < n; ++i) {
	      if (node = group[i]) {
	        schedule(node, name, id, i, group, timing || inherit(node, id));
	      }
	    }
	  }

	  return new Transition(groups, this._parents, name, id);
	}

	selection.prototype.interrupt = selection_interrupt;
	selection.prototype.transition = selection_transition;

	var root$1 = [null];

	function active(node, name) {
	  var schedules = node.__transition,
	      schedule$$1,
	      i;

	  if (schedules) {
	    name = name == null ? null : name + "";
	    for (i in schedules) {
	      if ((schedule$$1 = schedules[i]).state > SCHEDULED && schedule$$1.name === name) {
	        return new Transition([[node]], root$1, name, +i);
	      }
	    }
	  }

	  return null;
	}

	function constant$4(x) {
	  return function() {
	    return x;
	  };
	}

	function BrushEvent(target, type, selection) {
	  this.target = target;
	  this.type = type;
	  this.selection = selection;
	}

	function nopropagation$1() {
	  exports.event.stopImmediatePropagation();
	}

	function noevent$1() {
	  exports.event.preventDefault();
	  exports.event.stopImmediatePropagation();
	}

	var MODE_DRAG = {name: "drag"};
	var MODE_SPACE = {name: "space"};
	var MODE_HANDLE = {name: "handle"};
	var MODE_CENTER = {name: "center"};

	var X = {
	  name: "x",
	  handles: ["e", "w"].map(type),
	  input: function(x, e) { return x && [[x[0], e[0][1]], [x[1], e[1][1]]]; },
	  output: function(xy) { return xy && [xy[0][0], xy[1][0]]; }
	};

	var Y = {
	  name: "y",
	  handles: ["n", "s"].map(type),
	  input: function(y, e) { return y && [[e[0][0], y[0]], [e[1][0], y[1]]]; },
	  output: function(xy) { return xy && [xy[0][1], xy[1][1]]; }
	};

	var XY = {
	  name: "xy",
	  handles: ["n", "e", "s", "w", "nw", "ne", "se", "sw"].map(type),
	  input: function(xy) { return xy; },
	  output: function(xy) { return xy; }
	};

	var cursors = {
	  overlay: "crosshair",
	  selection: "move",
	  n: "ns-resize",
	  e: "ew-resize",
	  s: "ns-resize",
	  w: "ew-resize",
	  nw: "nwse-resize",
	  ne: "nesw-resize",
	  se: "nwse-resize",
	  sw: "nesw-resize"
	};

	var flipX = {
	  e: "w",
	  w: "e",
	  nw: "ne",
	  ne: "nw",
	  se: "sw",
	  sw: "se"
	};

	var flipY = {
	  n: "s",
	  s: "n",
	  nw: "sw",
	  ne: "se",
	  se: "ne",
	  sw: "nw"
	};

	var signsX = {
	  overlay: +1,
	  selection: +1,
	  n: null,
	  e: +1,
	  s: null,
	  w: -1,
	  nw: -1,
	  ne: +1,
	  se: +1,
	  sw: -1
	};

	var signsY = {
	  overlay: +1,
	  selection: +1,
	  n: -1,
	  e: null,
	  s: +1,
	  w: null,
	  nw: -1,
	  ne: -1,
	  se: +1,
	  sw: +1
	};

	function type(t) {
	  return {type: t};
	}

	// Ignore right-click, since that should open the context menu.
	function defaultFilter() {
	  return !exports.event.button;
	}

	function defaultExtent() {
	  var svg = this.ownerSVGElement || this;
	  return [[0, 0], [svg.width.baseVal.value, svg.height.baseVal.value]];
	}

	// Like d3.local, but with the name “__brush” rather than auto-generated.
	function local(node) {
	  while (!node.__brush) if (!(node = node.parentNode)) return;
	  return node.__brush;
	}

	function empty(extent) {
	  return extent[0][0] === extent[1][0]
	      || extent[0][1] === extent[1][1];
	}

	function brushSelection(node) {
	  var state = node.__brush;
	  return state ? state.dim.output(state.selection) : null;
	}

	function brushX() {
	  return brush$1(X);
	}

	function brushY() {
	  return brush$1(Y);
	}

	function brush() {
	  return brush$1(XY);
	}

	function brush$1(dim) {
	  var extent = defaultExtent,
	      filter = defaultFilter,
	      listeners = dispatch(brush, "start", "brush", "end"),
	      handleSize = 6,
	      touchending;

	  function brush(group) {
	    var overlay = group
	        .property("__brush", initialize)
	      .selectAll(".overlay")
	      .data([type("overlay")]);

	    overlay.enter().append("rect")
	        .attr("class", "overlay")
	        .attr("pointer-events", "all")
	        .attr("cursor", cursors.overlay)
	      .merge(overlay)
	        .each(function() {
	          var extent = local(this).extent;
	          select(this)
	              .attr("x", extent[0][0])
	              .attr("y", extent[0][1])
	              .attr("width", extent[1][0] - extent[0][0])
	              .attr("height", extent[1][1] - extent[0][1]);
	        });

	    group.selectAll(".selection")
	      .data([type("selection")])
	      .enter().append("rect")
	        .attr("class", "selection")
	        .attr("cursor", cursors.selection)
	        .attr("fill", "#777")
	        .attr("fill-opacity", 0.3)
	        .attr("stroke", "#fff")
	        .attr("shape-rendering", "crispEdges");

	    var handle = group.selectAll(".handle")
	      .data(dim.handles, function(d) { return d.type; });

	    handle.exit().remove();

	    handle.enter().append("rect")
	        .attr("class", function(d) { return "handle handle--" + d.type; })
	        .attr("cursor", function(d) { return cursors[d.type]; });

	    group
	        .each(redraw)
	        .attr("fill", "none")
	        .attr("pointer-events", "all")
	        .style("-webkit-tap-highlight-color", "rgba(0,0,0,0)")
	        .on("mousedown.brush touchstart.brush", started);
	  }

	  brush.move = function(group, selection) {
	    if (group.selection) {
	      group
	          .on("start.brush", function() { emitter(this, arguments).beforestart().start(); })
	          .on("interrupt.brush end.brush", function() { emitter(this, arguments).end(); })
	          .tween("brush", function() {
	            var that = this,
	                state = that.__brush,
	                emit = emitter(that, arguments),
	                selection0 = state.selection,
	                selection1 = dim.input(typeof selection === "function" ? selection.apply(this, arguments) : selection, state.extent),
	                i = interpolateValue(selection0, selection1);

	            function tween(t) {
	              state.selection = t === 1 && empty(selection1) ? null : i(t);
	              redraw.call(that);
	              emit.brush();
	            }

	            return selection0 && selection1 ? tween : tween(1);
	          });
	    } else {
	      group
	          .each(function() {
	            var that = this,
	                args = arguments,
	                state = that.__brush,
	                selection1 = dim.input(typeof selection === "function" ? selection.apply(that, args) : selection, state.extent),
	                emit = emitter(that, args).beforestart();

	            interrupt(that);
	            state.selection = selection1 == null || empty(selection1) ? null : selection1;
	            redraw.call(that);
	            emit.start().brush().end();
	          });
	    }
	  };

	  function redraw() {
	    var group = select(this),
	        selection = local(this).selection;

	    if (selection) {
	      group.selectAll(".selection")
	          .style("display", null)
	          .attr("x", selection[0][0])
	          .attr("y", selection[0][1])
	          .attr("width", selection[1][0] - selection[0][0])
	          .attr("height", selection[1][1] - selection[0][1]);

	      group.selectAll(".handle")
	          .style("display", null)
	          .attr("x", function(d) { return d.type[d.type.length - 1] === "e" ? selection[1][0] - handleSize / 2 : selection[0][0] - handleSize / 2; })
	          .attr("y", function(d) { return d.type[0] === "s" ? selection[1][1] - handleSize / 2 : selection[0][1] - handleSize / 2; })
	          .attr("width", function(d) { return d.type === "n" || d.type === "s" ? selection[1][0] - selection[0][0] + handleSize : handleSize; })
	          .attr("height", function(d) { return d.type === "e" || d.type === "w" ? selection[1][1] - selection[0][1] + handleSize : handleSize; });
	    }

	    else {
	      group.selectAll(".selection,.handle")
	          .style("display", "none")
	          .attr("x", null)
	          .attr("y", null)
	          .attr("width", null)
	          .attr("height", null);
	    }
	  }

	  function emitter(that, args) {
	    return that.__brush.emitter || new Emitter(that, args);
	  }

	  function Emitter(that, args) {
	    this.that = that;
	    this.args = args;
	    this.state = that.__brush;
	    this.active = 0;
	  }

	  Emitter.prototype = {
	    beforestart: function() {
	      if (++this.active === 1) this.state.emitter = this, this.starting = true;
	      return this;
	    },
	    start: function() {
	      if (this.starting) this.starting = false, this.emit("start");
	      return this;
	    },
	    brush: function() {
	      this.emit("brush");
	      return this;
	    },
	    end: function() {
	      if (--this.active === 0) delete this.state.emitter, this.emit("end");
	      return this;
	    },
	    emit: function(type) {
	      customEvent(new BrushEvent(brush, type, dim.output(this.state.selection)), listeners.apply, listeners, [type, this.that, this.args]);
	    }
	  };

	  function started() {
	    if (exports.event.touches) { if (exports.event.changedTouches.length < exports.event.touches.length) return noevent$1(); }
	    else if (touchending) return;
	    if (!filter.apply(this, arguments)) return;

	    var that = this,
	        type = exports.event.target.__data__.type,
	        mode = (exports.event.metaKey ? type = "overlay" : type) === "selection" ? MODE_DRAG : (exports.event.altKey ? MODE_CENTER : MODE_HANDLE),
	        signX = dim === Y ? null : signsX[type],
	        signY = dim === X ? null : signsY[type],
	        state = local(that),
	        extent = state.extent,
	        selection = state.selection,
	        W = extent[0][0], w0, w1,
	        N = extent[0][1], n0, n1,
	        E = extent[1][0], e0, e1,
	        S = extent[1][1], s0, s1,
	        dx,
	        dy,
	        moving,
	        shifting = signX && signY && exports.event.shiftKey,
	        lockX,
	        lockY,
	        point0 = mouse(that),
	        point = point0,
	        emit = emitter(that, arguments).beforestart();

	    if (type === "overlay") {
	      state.selection = selection = [
	        [w0 = dim === Y ? W : point0[0], n0 = dim === X ? N : point0[1]],
	        [e0 = dim === Y ? E : w0, s0 = dim === X ? S : n0]
	      ];
	    } else {
	      w0 = selection[0][0];
	      n0 = selection[0][1];
	      e0 = selection[1][0];
	      s0 = selection[1][1];
	    }

	    w1 = w0;
	    n1 = n0;
	    e1 = e0;
	    s1 = s0;

	    var group = select(that)
	        .attr("pointer-events", "none");

	    var overlay = group.selectAll(".overlay")
	        .attr("cursor", cursors[type]);

	    if (exports.event.touches) {
	      group
	          .on("touchmove.brush", moved, true)
	          .on("touchend.brush touchcancel.brush", ended, true);
	    } else {
	      var view = select(exports.event.view)
	          .on("keydown.brush", keydowned, true)
	          .on("keyup.brush", keyupped, true)
	          .on("mousemove.brush", moved, true)
	          .on("mouseup.brush", ended, true);

	      dragDisable(exports.event.view);
	    }

	    nopropagation$1();
	    interrupt(that);
	    redraw.call(that);
	    emit.start();

	    function moved() {
	      var point1 = mouse(that);
	      if (shifting && !lockX && !lockY) {
	        if (Math.abs(point1[0] - point[0]) > Math.abs(point1[1] - point[1])) lockY = true;
	        else lockX = true;
	      }
	      point = point1;
	      moving = true;
	      noevent$1();
	      move();
	    }

	    function move() {
	      var t;

	      dx = point[0] - point0[0];
	      dy = point[1] - point0[1];

	      switch (mode) {
	        case MODE_SPACE:
	        case MODE_DRAG: {
	          if (signX) dx = Math.max(W - w0, Math.min(E - e0, dx)), w1 = w0 + dx, e1 = e0 + dx;
	          if (signY) dy = Math.max(N - n0, Math.min(S - s0, dy)), n1 = n0 + dy, s1 = s0 + dy;
	          break;
	        }
	        case MODE_HANDLE: {
	          if (signX < 0) dx = Math.max(W - w0, Math.min(E - w0, dx)), w1 = w0 + dx, e1 = e0;
	          else if (signX > 0) dx = Math.max(W - e0, Math.min(E - e0, dx)), w1 = w0, e1 = e0 + dx;
	          if (signY < 0) dy = Math.max(N - n0, Math.min(S - n0, dy)), n1 = n0 + dy, s1 = s0;
	          else if (signY > 0) dy = Math.max(N - s0, Math.min(S - s0, dy)), n1 = n0, s1 = s0 + dy;
	          break;
	        }
	        case MODE_CENTER: {
	          if (signX) w1 = Math.max(W, Math.min(E, w0 - dx * signX)), e1 = Math.max(W, Math.min(E, e0 + dx * signX));
	          if (signY) n1 = Math.max(N, Math.min(S, n0 - dy * signY)), s1 = Math.max(N, Math.min(S, s0 + dy * signY));
	          break;
	        }
	      }

	      if (e1 < w1) {
	        signX *= -1;
	        t = w0, w0 = e0, e0 = t;
	        t = w1, w1 = e1, e1 = t;
	        if (type in flipX) overlay.attr("cursor", cursors[type = flipX[type]]);
	      }

	      if (s1 < n1) {
	        signY *= -1;
	        t = n0, n0 = s0, s0 = t;
	        t = n1, n1 = s1, s1 = t;
	        if (type in flipY) overlay.attr("cursor", cursors[type = flipY[type]]);
	      }

	      if (state.selection) selection = state.selection; // May be set by brush.move!
	      if (lockX) w1 = selection[0][0], e1 = selection[1][0];
	      if (lockY) n1 = selection[0][1], s1 = selection[1][1];

	      if (selection[0][0] !== w1
	          || selection[0][1] !== n1
	          || selection[1][0] !== e1
	          || selection[1][1] !== s1) {
	        state.selection = [[w1, n1], [e1, s1]];
	        redraw.call(that);
	        emit.brush();
	      }
	    }

	    function ended() {
	      nopropagation$1();
	      if (exports.event.touches) {
	        if (exports.event.touches.length) return;
	        if (touchending) clearTimeout(touchending);
	        touchending = setTimeout(function() { touchending = null; }, 500); // Ghost clicks are delayed!
	        group.on("touchmove.brush touchend.brush touchcancel.brush", null);
	      } else {
	        yesdrag(exports.event.view, moving);
	        view.on("keydown.brush keyup.brush mousemove.brush mouseup.brush", null);
	      }
	      group.attr("pointer-events", "all");
	      overlay.attr("cursor", cursors.overlay);
	      if (state.selection) selection = state.selection; // May be set by brush.move (on start)!
	      if (empty(selection)) state.selection = null, redraw.call(that);
	      emit.end();
	    }

	    function keydowned() {
	      switch (exports.event.keyCode) {
	        case 16: { // SHIFT
	          shifting = signX && signY;
	          break;
	        }
	        case 18: { // ALT
	          if (mode === MODE_HANDLE) {
	            if (signX) e0 = e1 - dx * signX, w0 = w1 + dx * signX;
	            if (signY) s0 = s1 - dy * signY, n0 = n1 + dy * signY;
	            mode = MODE_CENTER;
	            move();
	          }
	          break;
	        }
	        case 32: { // SPACE; takes priority over ALT
	          if (mode === MODE_HANDLE || mode === MODE_CENTER) {
	            if (signX < 0) e0 = e1 - dx; else if (signX > 0) w0 = w1 - dx;
	            if (signY < 0) s0 = s1 - dy; else if (signY > 0) n0 = n1 - dy;
	            mode = MODE_SPACE;
	            overlay.attr("cursor", cursors.selection);
	            move();
	          }
	          break;
	        }
	        default: return;
	      }
	      noevent$1();
	    }

	    function keyupped() {
	      switch (exports.event.keyCode) {
	        case 16: { // SHIFT
	          if (shifting) {
	            lockX = lockY = shifting = false;
	            move();
	          }
	          break;
	        }
	        case 18: { // ALT
	          if (mode === MODE_CENTER) {
	            if (signX < 0) e0 = e1; else if (signX > 0) w0 = w1;
	            if (signY < 0) s0 = s1; else if (signY > 0) n0 = n1;
	            mode = MODE_HANDLE;
	            move();
	          }
	          break;
	        }
	        case 32: { // SPACE
	          if (mode === MODE_SPACE) {
	            if (exports.event.altKey) {
	              if (signX) e0 = e1 - dx * signX, w0 = w1 + dx * signX;
	              if (signY) s0 = s1 - dy * signY, n0 = n1 + dy * signY;
	              mode = MODE_CENTER;
	            } else {
	              if (signX < 0) e0 = e1; else if (signX > 0) w0 = w1;
	              if (signY < 0) s0 = s1; else if (signY > 0) n0 = n1;
	              mode = MODE_HANDLE;
	            }
	            overlay.attr("cursor", cursors[type]);
	            move();
	          }
	          break;
	        }
	        default: return;
	      }
	      noevent$1();
	    }
	  }

	  function initialize() {
	    var state = this.__brush || {selection: null};
	    state.extent = extent.apply(this, arguments);
	    state.dim = dim;
	    return state;
	  }

	  brush.extent = function(_) {
	    return arguments.length ? (extent = typeof _ === "function" ? _ : constant$4([[+_[0][0], +_[0][1]], [+_[1][0], +_[1][1]]]), brush) : extent;
	  };

	  brush.filter = function(_) {
	    return arguments.length ? (filter = typeof _ === "function" ? _ : constant$4(!!_), brush) : filter;
	  };

	  brush.handleSize = function(_) {
	    return arguments.length ? (handleSize = +_, brush) : handleSize;
	  };

	  brush.on = function() {
	    var value = listeners.on.apply(listeners, arguments);
	    return value === listeners ? brush : value;
	  };

	  return brush;
	}

	var cos = Math.cos;
	var sin = Math.sin;
	var pi$1 = Math.PI;
	var halfPi$1 = pi$1 / 2;
	var tau$1 = pi$1 * 2;
	var max$1 = Math.max;

	function compareValue(compare) {
	  return function(a, b) {
	    return compare(
	      a.source.value + a.target.value,
	      b.source.value + b.target.value
	    );
	  };
	}

	function chord() {
	  var padAngle = 0,
	      sortGroups = null,
	      sortSubgroups = null,
	      sortChords = null;

	  function chord(matrix) {
	    var n = matrix.length,
	        groupSums = [],
	        groupIndex = sequence(n),
	        subgroupIndex = [],
	        chords = [],
	        groups = chords.groups = new Array(n),
	        subgroups = new Array(n * n),
	        k,
	        x,
	        x0,
	        dx,
	        i,
	        j;

	    // Compute the sum.
	    k = 0, i = -1; while (++i < n) {
	      x = 0, j = -1; while (++j < n) {
	        x += matrix[i][j];
	      }
	      groupSums.push(x);
	      subgroupIndex.push(sequence(n));
	      k += x;
	    }

	    // Sort groups…
	    if (sortGroups) groupIndex.sort(function(a, b) {
	      return sortGroups(groupSums[a], groupSums[b]);
	    });

	    // Sort subgroups…
	    if (sortSubgroups) subgroupIndex.forEach(function(d, i) {
	      d.sort(function(a, b) {
	        return sortSubgroups(matrix[i][a], matrix[i][b]);
	      });
	    });

	    // Convert the sum to scaling factor for [0, 2pi].
	    // TODO Allow start and end angle to be specified?
	    // TODO Allow padding to be specified as percentage?
	    k = max$1(0, tau$1 - padAngle * n) / k;
	    dx = k ? padAngle : tau$1 / n;

	    // Compute the start and end angle for each group and subgroup.
	    // Note: Opera has a bug reordering object literal properties!
	    x = 0, i = -1; while (++i < n) {
	      x0 = x, j = -1; while (++j < n) {
	        var di = groupIndex[i],
	            dj = subgroupIndex[di][j],
	            v = matrix[di][dj],
	            a0 = x,
	            a1 = x += v * k;
	        subgroups[dj * n + di] = {
	          index: di,
	          subindex: dj,
	          startAngle: a0,
	          endAngle: a1,
	          value: v
	        };
	      }
	      groups[di] = {
	        index: di,
	        startAngle: x0,
	        endAngle: x,
	        value: groupSums[di]
	      };
	      x += dx;
	    }

	    // Generate chords for each (non-empty) subgroup-subgroup link.
	    i = -1; while (++i < n) {
	      j = i - 1; while (++j < n) {
	        var source = subgroups[j * n + i],
	            target = subgroups[i * n + j];
	        if (source.value || target.value) {
	          chords.push(source.value < target.value
	              ? {source: target, target: source}
	              : {source: source, target: target});
	        }
	      }
	    }

	    return sortChords ? chords.sort(sortChords) : chords;
	  }

	  chord.padAngle = function(_) {
	    return arguments.length ? (padAngle = max$1(0, _), chord) : padAngle;
	  };

	  chord.sortGroups = function(_) {
	    return arguments.length ? (sortGroups = _, chord) : sortGroups;
	  };

	  chord.sortSubgroups = function(_) {
	    return arguments.length ? (sortSubgroups = _, chord) : sortSubgroups;
	  };

	  chord.sortChords = function(_) {
	    return arguments.length ? (_ == null ? sortChords = null : (sortChords = compareValue(_))._ = _, chord) : sortChords && sortChords._;
	  };

	  return chord;
	}

	var slice$2 = Array.prototype.slice;

	function constant$5(x) {
	  return function() {
	    return x;
	  };
	}

	var pi$2 = Math.PI;
	var tau$2 = 2 * pi$2;
	var epsilon$1 = 1e-6;
	var tauEpsilon = tau$2 - epsilon$1;

	function Path() {
	  this._x0 = this._y0 = // start of current subpath
	  this._x1 = this._y1 = null; // end of current subpath
	  this._ = "";
	}

	function path() {
	  return new Path;
	}

	Path.prototype = path.prototype = {
	  constructor: Path,
	  moveTo: function(x, y) {
	    this._ += "M" + (this._x0 = this._x1 = +x) + "," + (this._y0 = this._y1 = +y);
	  },
	  closePath: function() {
	    if (this._x1 !== null) {
	      this._x1 = this._x0, this._y1 = this._y0;
	      this._ += "Z";
	    }
	  },
	  lineTo: function(x, y) {
	    this._ += "L" + (this._x1 = +x) + "," + (this._y1 = +y);
	  },
	  quadraticCurveTo: function(x1, y1, x, y) {
	    this._ += "Q" + (+x1) + "," + (+y1) + "," + (this._x1 = +x) + "," + (this._y1 = +y);
	  },
	  bezierCurveTo: function(x1, y1, x2, y2, x, y) {
	    this._ += "C" + (+x1) + "," + (+y1) + "," + (+x2) + "," + (+y2) + "," + (this._x1 = +x) + "," + (this._y1 = +y);
	  },
	  arcTo: function(x1, y1, x2, y2, r) {
	    x1 = +x1, y1 = +y1, x2 = +x2, y2 = +y2, r = +r;
	    var x0 = this._x1,
	        y0 = this._y1,
	        x21 = x2 - x1,
	        y21 = y2 - y1,
	        x01 = x0 - x1,
	        y01 = y0 - y1,
	        l01_2 = x01 * x01 + y01 * y01;

	    // Is the radius negative? Error.
	    if (r < 0) throw new Error("negative radius: " + r);

	    // Is this path empty? Move to (x1,y1).
	    if (this._x1 === null) {
	      this._ += "M" + (this._x1 = x1) + "," + (this._y1 = y1);
	    }

	    // Or, is (x1,y1) coincident with (x0,y0)? Do nothing.
	    else if (!(l01_2 > epsilon$1)) {}

	    // Or, are (x0,y0), (x1,y1) and (x2,y2) collinear?
	    // Equivalently, is (x1,y1) coincident with (x2,y2)?
	    // Or, is the radius zero? Line to (x1,y1).
	    else if (!(Math.abs(y01 * x21 - y21 * x01) > epsilon$1) || !r) {
	      this._ += "L" + (this._x1 = x1) + "," + (this._y1 = y1);
	    }

	    // Otherwise, draw an arc!
	    else {
	      var x20 = x2 - x0,
	          y20 = y2 - y0,
	          l21_2 = x21 * x21 + y21 * y21,
	          l20_2 = x20 * x20 + y20 * y20,
	          l21 = Math.sqrt(l21_2),
	          l01 = Math.sqrt(l01_2),
	          l = r * Math.tan((pi$2 - Math.acos((l21_2 + l01_2 - l20_2) / (2 * l21 * l01))) / 2),
	          t01 = l / l01,
	          t21 = l / l21;

	      // If the start tangent is not coincident with (x0,y0), line to.
	      if (Math.abs(t01 - 1) > epsilon$1) {
	        this._ += "L" + (x1 + t01 * x01) + "," + (y1 + t01 * y01);
	      }

	      this._ += "A" + r + "," + r + ",0,0," + (+(y01 * x20 > x01 * y20)) + "," + (this._x1 = x1 + t21 * x21) + "," + (this._y1 = y1 + t21 * y21);
	    }
	  },
	  arc: function(x, y, r, a0, a1, ccw) {
	    x = +x, y = +y, r = +r;
	    var dx = r * Math.cos(a0),
	        dy = r * Math.sin(a0),
	        x0 = x + dx,
	        y0 = y + dy,
	        cw = 1 ^ ccw,
	        da = ccw ? a0 - a1 : a1 - a0;

	    // Is the radius negative? Error.
	    if (r < 0) throw new Error("negative radius: " + r);

	    // Is this path empty? Move to (x0,y0).
	    if (this._x1 === null) {
	      this._ += "M" + x0 + "," + y0;
	    }

	    // Or, is (x0,y0) not coincident with the previous point? Line to (x0,y0).
	    else if (Math.abs(this._x1 - x0) > epsilon$1 || Math.abs(this._y1 - y0) > epsilon$1) {
	      this._ += "L" + x0 + "," + y0;
	    }

	    // Is this arc empty? We’re done.
	    if (!r) return;

	    // Does the angle go the wrong way? Flip the direction.
	    if (da < 0) da = da % tau$2 + tau$2;

	    // Is this a complete circle? Draw two arcs to complete the circle.
	    if (da > tauEpsilon) {
	      this._ += "A" + r + "," + r + ",0,1," + cw + "," + (x - dx) + "," + (y - dy) + "A" + r + "," + r + ",0,1," + cw + "," + (this._x1 = x0) + "," + (this._y1 = y0);
	    }

	    // Is this arc non-empty? Draw an arc!
	    else if (da > epsilon$1) {
	      this._ += "A" + r + "," + r + ",0," + (+(da >= pi$2)) + "," + cw + "," + (this._x1 = x + r * Math.cos(a1)) + "," + (this._y1 = y + r * Math.sin(a1));
	    }
	  },
	  rect: function(x, y, w, h) {
	    this._ += "M" + (this._x0 = this._x1 = +x) + "," + (this._y0 = this._y1 = +y) + "h" + (+w) + "v" + (+h) + "h" + (-w) + "Z";
	  },
	  toString: function() {
	    return this._;
	  }
	};

	function defaultSource(d) {
	  return d.source;
	}

	function defaultTarget(d) {
	  return d.target;
	}

	function defaultRadius(d) {
	  return d.radius;
	}

	function defaultStartAngle(d) {
	  return d.startAngle;
	}

	function defaultEndAngle(d) {
	  return d.endAngle;
	}

	function ribbon() {
	  var source = defaultSource,
	      target = defaultTarget,
	      radius = defaultRadius,
	      startAngle = defaultStartAngle,
	      endAngle = defaultEndAngle,
	      context = null;

	  function ribbon() {
	    var buffer,
	        argv = slice$2.call(arguments),
	        s = source.apply(this, argv),
	        t = target.apply(this, argv),
	        sr = +radius.apply(this, (argv[0] = s, argv)),
	        sa0 = startAngle.apply(this, argv) - halfPi$1,
	        sa1 = endAngle.apply(this, argv) - halfPi$1,
	        sx0 = sr * cos(sa0),
	        sy0 = sr * sin(sa0),
	        tr = +radius.apply(this, (argv[0] = t, argv)),
	        ta0 = startAngle.apply(this, argv) - halfPi$1,
	        ta1 = endAngle.apply(this, argv) - halfPi$1;

	    if (!context) context = buffer = path();

	    context.moveTo(sx0, sy0);
	    context.arc(0, 0, sr, sa0, sa1);
	    if (sa0 !== ta0 || sa1 !== ta1) { // TODO sr !== tr?
	      context.quadraticCurveTo(0, 0, tr * cos(ta0), tr * sin(ta0));
	      context.arc(0, 0, tr, ta0, ta1);
	    }
	    context.quadraticCurveTo(0, 0, sx0, sy0);
	    context.closePath();

	    if (buffer) return context = null, buffer + "" || null;
	  }

	  ribbon.radius = function(_) {
	    return arguments.length ? (radius = typeof _ === "function" ? _ : constant$5(+_), ribbon) : radius;
	  };

	  ribbon.startAngle = function(_) {
	    return arguments.length ? (startAngle = typeof _ === "function" ? _ : constant$5(+_), ribbon) : startAngle;
	  };

	  ribbon.endAngle = function(_) {
	    return arguments.length ? (endAngle = typeof _ === "function" ? _ : constant$5(+_), ribbon) : endAngle;
	  };

	  ribbon.source = function(_) {
	    return arguments.length ? (source = _, ribbon) : source;
	  };

	  ribbon.target = function(_) {
	    return arguments.length ? (target = _, ribbon) : target;
	  };

	  ribbon.context = function(_) {
	    return arguments.length ? (context = _ == null ? null : _, ribbon) : context;
	  };

	  return ribbon;
	}

	var prefix = "$";

	function Map() {}

	Map.prototype = map$1.prototype = {
	  constructor: Map,
	  has: function(key) {
	    return (prefix + key) in this;
	  },
	  get: function(key) {
	    return this[prefix + key];
	  },
	  set: function(key, value) {
	    this[prefix + key] = value;
	    return this;
	  },
	  remove: function(key) {
	    var property = prefix + key;
	    return property in this && delete this[property];
	  },
	  clear: function() {
	    for (var property in this) if (property[0] === prefix) delete this[property];
	  },
	  keys: function() {
	    var keys = [];
	    for (var property in this) if (property[0] === prefix) keys.push(property.slice(1));
	    return keys;
	  },
	  values: function() {
	    var values = [];
	    for (var property in this) if (property[0] === prefix) values.push(this[property]);
	    return values;
	  },
	  entries: function() {
	    var entries = [];
	    for (var property in this) if (property[0] === prefix) entries.push({key: property.slice(1), value: this[property]});
	    return entries;
	  },
	  size: function() {
	    var size = 0;
	    for (var property in this) if (property[0] === prefix) ++size;
	    return size;
	  },
	  empty: function() {
	    for (var property in this) if (property[0] === prefix) return false;
	    return true;
	  },
	  each: function(f) {
	    for (var property in this) if (property[0] === prefix) f(this[property], property.slice(1), this);
	  }
	};

	function map$1(object, f) {
	  var map = new Map;

	  // Copy constructor.
	  if (object instanceof Map) object.each(function(value, key) { map.set(key, value); });

	  // Index array by numeric index or specified key function.
	  else if (Array.isArray(object)) {
	    var i = -1,
	        n = object.length,
	        o;

	    if (f == null) while (++i < n) map.set(i, object[i]);
	    else while (++i < n) map.set(f(o = object[i], i, object), o);
	  }

	  // Convert object to map.
	  else if (object) for (var key in object) map.set(key, object[key]);

	  return map;
	}

	function nest() {
	  var keys = [],
	      sortKeys = [],
	      sortValues,
	      rollup,
	      nest;

	  function apply(array, depth, createResult, setResult) {
	    if (depth >= keys.length) {
	      if (sortValues != null) array.sort(sortValues);
	      return rollup != null ? rollup(array) : array;
	    }

	    var i = -1,
	        n = array.length,
	        key = keys[depth++],
	        keyValue,
	        value,
	        valuesByKey = map$1(),
	        values,
	        result = createResult();

	    while (++i < n) {
	      if (values = valuesByKey.get(keyValue = key(value = array[i]) + "")) {
	        values.push(value);
	      } else {
	        valuesByKey.set(keyValue, [value]);
	      }
	    }

	    valuesByKey.each(function(values, key) {
	      setResult(result, key, apply(values, depth, createResult, setResult));
	    });

	    return result;
	  }

	  function entries(map, depth) {
	    if (++depth > keys.length) return map;
	    var array, sortKey = sortKeys[depth - 1];
	    if (rollup != null && depth >= keys.length) array = map.entries();
	    else array = [], map.each(function(v, k) { array.push({key: k, values: entries(v, depth)}); });
	    return sortKey != null ? array.sort(function(a, b) { return sortKey(a.key, b.key); }) : array;
	  }

	  return nest = {
	    object: function(array) { return apply(array, 0, createObject, setObject); },
	    map: function(array) { return apply(array, 0, createMap, setMap); },
	    entries: function(array) { return entries(apply(array, 0, createMap, setMap), 0); },
	    key: function(d) { keys.push(d); return nest; },
	    sortKeys: function(order) { sortKeys[keys.length - 1] = order; return nest; },
	    sortValues: function(order) { sortValues = order; return nest; },
	    rollup: function(f) { rollup = f; return nest; }
	  };
	}

	function createObject() {
	  return {};
	}

	function setObject(object, key, value) {
	  object[key] = value;
	}

	function createMap() {
	  return map$1();
	}

	function setMap(map, key, value) {
	  map.set(key, value);
	}

	function Set() {}

	var proto = map$1.prototype;

	Set.prototype = set$2.prototype = {
	  constructor: Set,
	  has: proto.has,
	  add: function(value) {
	    value += "";
	    this[prefix + value] = value;
	    return this;
	  },
	  remove: proto.remove,
	  clear: proto.clear,
	  values: proto.keys,
	  size: proto.size,
	  empty: proto.empty,
	  each: proto.each
	};

	function set$2(object, f) {
	  var set = new Set;

	  // Copy constructor.
	  if (object instanceof Set) object.each(function(value) { set.add(value); });

	  // Otherwise, assume it’s an array.
	  else if (object) {
	    var i = -1, n = object.length;
	    if (f == null) while (++i < n) set.add(object[i]);
	    else while (++i < n) set.add(f(object[i], i, object));
	  }

	  return set;
	}

	function keys(map) {
	  var keys = [];
	  for (var key in map) keys.push(key);
	  return keys;
	}

	function values(map) {
	  var values = [];
	  for (var key in map) values.push(map[key]);
	  return values;
	}

	function entries(map) {
	  var entries = [];
	  for (var key in map) entries.push({key: key, value: map[key]});
	  return entries;
	}

	var EOL = {};
	var EOF = {};
	var QUOTE = 34;
	var NEWLINE = 10;
	var RETURN = 13;

	function objectConverter(columns) {
	  return new Function("d", "return {" + columns.map(function(name, i) {
	    return JSON.stringify(name) + ": d[" + i + "]";
	  }).join(",") + "}");
	}

	function customConverter(columns, f) {
	  var object = objectConverter(columns);
	  return function(row, i) {
	    return f(object(row), i, columns);
	  };
	}

	// Compute unique columns in order of discovery.
	function inferColumns(rows) {
	  var columnSet = Object.create(null),
	      columns = [];

	  rows.forEach(function(row) {
	    for (var column in row) {
	      if (!(column in columnSet)) {
	        columns.push(columnSet[column] = column);
	      }
	    }
	  });

	  return columns;
	}

	function dsv(delimiter) {
	  var reFormat = new RegExp("[\"" + delimiter + "\n\r]"),
	      DELIMITER = delimiter.charCodeAt(0);

	  function parse(text, f) {
	    var convert, columns, rows = parseRows(text, function(row, i) {
	      if (convert) return convert(row, i - 1);
	      columns = row, convert = f ? customConverter(row, f) : objectConverter(row);
	    });
	    rows.columns = columns || [];
	    return rows;
	  }

	  function parseRows(text, f) {
	    var rows = [], // output rows
	        N = text.length,
	        I = 0, // current character index
	        n = 0, // current line number
	        t, // current token
	        eof = N <= 0, // current token followed by EOF?
	        eol = false; // current token followed by EOL?

	    // Strip the trailing newline.
	    if (text.charCodeAt(N - 1) === NEWLINE) --N;
	    if (text.charCodeAt(N - 1) === RETURN) --N;

	    function token() {
	      if (eof) return EOF;
	      if (eol) return eol = false, EOL;

	      // Unescape quotes.
	      var i, j = I, c;
	      if (text.charCodeAt(j) === QUOTE) {
	        while (I++ < N && text.charCodeAt(I) !== QUOTE || text.charCodeAt(++I) === QUOTE);
	        if ((i = I) >= N) eof = true;
	        else if ((c = text.charCodeAt(I++)) === NEWLINE) eol = true;
	        else if (c === RETURN) { eol = true; if (text.charCodeAt(I) === NEWLINE) ++I; }
	        return text.slice(j + 1, i - 1).replace(/""/g, "\"");
	      }

	      // Find next delimiter or newline.
	      while (I < N) {
	        if ((c = text.charCodeAt(i = I++)) === NEWLINE) eol = true;
	        else if (c === RETURN) { eol = true; if (text.charCodeAt(I) === NEWLINE) ++I; }
	        else if (c !== DELIMITER) continue;
	        return text.slice(j, i);
	      }

	      // Return last token before EOF.
	      return eof = true, text.slice(j, N);
	    }

	    while ((t = token()) !== EOF) {
	      var row = [];
	      while (t !== EOL && t !== EOF) row.push(t), t = token();
	      if (f && (row = f(row, n++)) == null) continue;
	      rows.push(row);
	    }

	    return rows;
	  }

	  function format(rows, columns) {
	    if (columns == null) columns = inferColumns(rows);
	    return [columns.map(formatValue).join(delimiter)].concat(rows.map(function(row) {
	      return columns.map(function(column) {
	        return formatValue(row[column]);
	      }).join(delimiter);
	    })).join("\n");
	  }

	  function formatRows(rows) {
	    return rows.map(formatRow).join("\n");
	  }

	  function formatRow(row) {
	    return row.map(formatValue).join(delimiter);
	  }

	  function formatValue(text) {
	    return text == null ? ""
	        : reFormat.test(text += "") ? "\"" + text.replace(/"/g, "\"\"") + "\""
	        : text;
	  }

	  return {
	    parse: parse,
	    parseRows: parseRows,
	    format: format,
	    formatRows: formatRows
	  };
	}

	var csv = dsv(",");

	var csvParse = csv.parse;
	var csvParseRows = csv.parseRows;
	var csvFormat = csv.format;
	var csvFormatRows = csv.formatRows;

	var tsv = dsv("\t");

	var tsvParse = tsv.parse;
	var tsvParseRows = tsv.parseRows;
	var tsvFormat = tsv.format;
	var tsvFormatRows = tsv.formatRows;

	function center$1(x, y) {
	  var nodes;

	  if (x == null) x = 0;
	  if (y == null) y = 0;

	  function force() {
	    var i,
	        n = nodes.length,
	        node,
	        sx = 0,
	        sy = 0;

	    for (i = 0; i < n; ++i) {
	      node = nodes[i], sx += node.x, sy += node.y;
	    }

	    for (sx = sx / n - x, sy = sy / n - y, i = 0; i < n; ++i) {
	      node = nodes[i], node.x -= sx, node.y -= sy;
	    }
	  }

	  force.initialize = function(_) {
	    nodes = _;
	  };

	  force.x = function(_) {
	    return arguments.length ? (x = +_, force) : x;
	  };

	  force.y = function(_) {
	    return arguments.length ? (y = +_, force) : y;
	  };

	  return force;
	}

	function constant$6(x) {
	  return function() {
	    return x;
	  };
	}

	function jiggle() {
	  return (Math.random() - 0.5) * 1e-6;
	}

	function tree_add(d) {
	  var x = +this._x.call(null, d),
	      y = +this._y.call(null, d);
	  return add(this.cover(x, y), x, y, d);
	}

	function add(tree, x, y, d) {
	  if (isNaN(x) || isNaN(y)) return tree; // ignore invalid points

	  var parent,
	      node = tree._root,
	      leaf = {data: d},
	      x0 = tree._x0,
	      y0 = tree._y0,
	      x1 = tree._x1,
	      y1 = tree._y1,
	      xm,
	      ym,
	      xp,
	      yp,
	      right,
	      bottom,
	      i,
	      j;

	  // If the tree is empty, initialize the root as a leaf.
	  if (!node) return tree._root = leaf, tree;

	  // Find the existing leaf for the new point, or add it.
	  while (node.length) {
	    if (right = x >= (xm = (x0 + x1) / 2)) x0 = xm; else x1 = xm;
	    if (bottom = y >= (ym = (y0 + y1) / 2)) y0 = ym; else y1 = ym;
	    if (parent = node, !(node = node[i = bottom << 1 | right])) return parent[i] = leaf, tree;
	  }

	  // Is the new point is exactly coincident with the existing point?
	  xp = +tree._x.call(null, node.data);
	  yp = +tree._y.call(null, node.data);
	  if (x === xp && y === yp) return leaf.next = node, parent ? parent[i] = leaf : tree._root = leaf, tree;

	  // Otherwise, split the leaf node until the old and new point are separated.
	  do {
	    parent = parent ? parent[i] = new Array(4) : tree._root = new Array(4);
	    if (right = x >= (xm = (x0 + x1) / 2)) x0 = xm; else x1 = xm;
	    if (bottom = y >= (ym = (y0 + y1) / 2)) y0 = ym; else y1 = ym;
	  } while ((i = bottom << 1 | right) === (j = (yp >= ym) << 1 | (xp >= xm)));
	  return parent[j] = node, parent[i] = leaf, tree;
	}

	function addAll(data) {
	  var d, i, n = data.length,
	      x,
	      y,
	      xz = new Array(n),
	      yz = new Array(n),
	      x0 = Infinity,
	      y0 = Infinity,
	      x1 = -Infinity,
	      y1 = -Infinity;

	  // Compute the points and their extent.
	  for (i = 0; i < n; ++i) {
	    if (isNaN(x = +this._x.call(null, d = data[i])) || isNaN(y = +this._y.call(null, d))) continue;
	    xz[i] = x;
	    yz[i] = y;
	    if (x < x0) x0 = x;
	    if (x > x1) x1 = x;
	    if (y < y0) y0 = y;
	    if (y > y1) y1 = y;
	  }

	  // If there were no (valid) points, inherit the existing extent.
	  if (x1 < x0) x0 = this._x0, x1 = this._x1;
	  if (y1 < y0) y0 = this._y0, y1 = this._y1;

	  // Expand the tree to cover the new points.
	  this.cover(x0, y0).cover(x1, y1);

	  // Add the new points.
	  for (i = 0; i < n; ++i) {
	    add(this, xz[i], yz[i], data[i]);
	  }

	  return this;
	}

	function tree_cover(x, y) {
	  if (isNaN(x = +x) || isNaN(y = +y)) return this; // ignore invalid points

	  var x0 = this._x0,
	      y0 = this._y0,
	      x1 = this._x1,
	      y1 = this._y1;

	  // If the quadtree has no extent, initialize them.
	  // Integer extent are necessary so that if we later double the extent,
	  // the existing quadrant boundaries don’t change due to floating point error!
	  if (isNaN(x0)) {
	    x1 = (x0 = Math.floor(x)) + 1;
	    y1 = (y0 = Math.floor(y)) + 1;
	  }

	  // Otherwise, double repeatedly to cover.
	  else if (x0 > x || x > x1 || y0 > y || y > y1) {
	    var z = x1 - x0,
	        node = this._root,
	        parent,
	        i;

	    switch (i = (y < (y0 + y1) / 2) << 1 | (x < (x0 + x1) / 2)) {
	      case 0: {
	        do parent = new Array(4), parent[i] = node, node = parent;
	        while (z *= 2, x1 = x0 + z, y1 = y0 + z, x > x1 || y > y1);
	        break;
	      }
	      case 1: {
	        do parent = new Array(4), parent[i] = node, node = parent;
	        while (z *= 2, x0 = x1 - z, y1 = y0 + z, x0 > x || y > y1);
	        break;
	      }
	      case 2: {
	        do parent = new Array(4), parent[i] = node, node = parent;
	        while (z *= 2, x1 = x0 + z, y0 = y1 - z, x > x1 || y0 > y);
	        break;
	      }
	      case 3: {
	        do parent = new Array(4), parent[i] = node, node = parent;
	        while (z *= 2, x0 = x1 - z, y0 = y1 - z, x0 > x || y0 > y);
	        break;
	      }
	    }

	    if (this._root && this._root.length) this._root = node;
	  }

	  // If the quadtree covers the point already, just return.
	  else return this;

	  this._x0 = x0;
	  this._y0 = y0;
	  this._x1 = x1;
	  this._y1 = y1;
	  return this;
	}

	function tree_data() {
	  var data = [];
	  this.visit(function(node) {
	    if (!node.length) do data.push(node.data); while (node = node.next)
	  });
	  return data;
	}

	function tree_extent(_) {
	  return arguments.length
	      ? this.cover(+_[0][0], +_[0][1]).cover(+_[1][0], +_[1][1])
	      : isNaN(this._x0) ? undefined : [[this._x0, this._y0], [this._x1, this._y1]];
	}

	function Quad(node, x0, y0, x1, y1) {
	  this.node = node;
	  this.x0 = x0;
	  this.y0 = y0;
	  this.x1 = x1;
	  this.y1 = y1;
	}

	function tree_find(x, y, radius) {
	  var data,
	      x0 = this._x0,
	      y0 = this._y0,
	      x1,
	      y1,
	      x2,
	      y2,
	      x3 = this._x1,
	      y3 = this._y1,
	      quads = [],
	      node = this._root,
	      q,
	      i;

	  if (node) quads.push(new Quad(node, x0, y0, x3, y3));
	  if (radius == null) radius = Infinity;
	  else {
	    x0 = x - radius, y0 = y - radius;
	    x3 = x + radius, y3 = y + radius;
	    radius *= radius;
	  }

	  while (q = quads.pop()) {

	    // Stop searching if this quadrant can’t contain a closer node.
	    if (!(node = q.node)
	        || (x1 = q.x0) > x3
	        || (y1 = q.y0) > y3
	        || (x2 = q.x1) < x0
	        || (y2 = q.y1) < y0) continue;

	    // Bisect the current quadrant.
	    if (node.length) {
	      var xm = (x1 + x2) / 2,
	          ym = (y1 + y2) / 2;

	      quads.push(
	        new Quad(node[3], xm, ym, x2, y2),
	        new Quad(node[2], x1, ym, xm, y2),
	        new Quad(node[1], xm, y1, x2, ym),
	        new Quad(node[0], x1, y1, xm, ym)
	      );

	      // Visit the closest quadrant first.
	      if (i = (y >= ym) << 1 | (x >= xm)) {
	        q = quads[quads.length - 1];
	        quads[quads.length - 1] = quads[quads.length - 1 - i];
	        quads[quads.length - 1 - i] = q;
	      }
	    }

	    // Visit this point. (Visiting coincident points isn’t necessary!)
	    else {
	      var dx = x - +this._x.call(null, node.data),
	          dy = y - +this._y.call(null, node.data),
	          d2 = dx * dx + dy * dy;
	      if (d2 < radius) {
	        var d = Math.sqrt(radius = d2);
	        x0 = x - d, y0 = y - d;
	        x3 = x + d, y3 = y + d;
	        data = node.data;
	      }
	    }
	  }

	  return data;
	}

	function tree_remove(d) {
	  if (isNaN(x = +this._x.call(null, d)) || isNaN(y = +this._y.call(null, d))) return this; // ignore invalid points

	  var parent,
	      node = this._root,
	      retainer,
	      previous,
	      next,
	      x0 = this._x0,
	      y0 = this._y0,
	      x1 = this._x1,
	      y1 = this._y1,
	      x,
	      y,
	      xm,
	      ym,
	      right,
	      bottom,
	      i,
	      j;

	  // If the tree is empty, initialize the root as a leaf.
	  if (!node) return this;

	  // Find the leaf node for the point.
	  // While descending, also retain the deepest parent with a non-removed sibling.
	  if (node.length) while (true) {
	    if (right = x >= (xm = (x0 + x1) / 2)) x0 = xm; else x1 = xm;
	    if (bottom = y >= (ym = (y0 + y1) / 2)) y0 = ym; else y1 = ym;
	    if (!(parent = node, node = node[i = bottom << 1 | right])) return this;
	    if (!node.length) break;
	    if (parent[(i + 1) & 3] || parent[(i + 2) & 3] || parent[(i + 3) & 3]) retainer = parent, j = i;
	  }

	  // Find the point to remove.
	  while (node.data !== d) if (!(previous = node, node = node.next)) return this;
	  if (next = node.next) delete node.next;

	  // If there are multiple coincident points, remove just the point.
	  if (previous) return next ? previous.next = next : delete previous.next, this;

	  // If this is the root point, remove it.
	  if (!parent) return this._root = next, this;

	  // Remove this leaf.
	  next ? parent[i] = next : delete parent[i];

	  // If the parent now contains exactly one leaf, collapse superfluous parents.
	  if ((node = parent[0] || parent[1] || parent[2] || parent[3])
	      && node === (parent[3] || parent[2] || parent[1] || parent[0])
	      && !node.length) {
	    if (retainer) retainer[j] = node;
	    else this._root = node;
	  }

	  return this;
	}

	function removeAll(data) {
	  for (var i = 0, n = data.length; i < n; ++i) this.remove(data[i]);
	  return this;
	}

	function tree_root() {
	  return this._root;
	}

	function tree_size() {
	  var size = 0;
	  this.visit(function(node) {
	    if (!node.length) do ++size; while (node = node.next)
	  });
	  return size;
	}

	function tree_visit(callback) {
	  var quads = [], q, node = this._root, child, x0, y0, x1, y1;
	  if (node) quads.push(new Quad(node, this._x0, this._y0, this._x1, this._y1));
	  while (q = quads.pop()) {
	    if (!callback(node = q.node, x0 = q.x0, y0 = q.y0, x1 = q.x1, y1 = q.y1) && node.length) {
	      var xm = (x0 + x1) / 2, ym = (y0 + y1) / 2;
	      if (child = node[3]) quads.push(new Quad(child, xm, ym, x1, y1));
	      if (child = node[2]) quads.push(new Quad(child, x0, ym, xm, y1));
	      if (child = node[1]) quads.push(new Quad(child, xm, y0, x1, ym));
	      if (child = node[0]) quads.push(new Quad(child, x0, y0, xm, ym));
	    }
	  }
	  return this;
	}

	function tree_visitAfter(callback) {
	  var quads = [], next = [], q;
	  if (this._root) quads.push(new Quad(this._root, this._x0, this._y0, this._x1, this._y1));
	  while (q = quads.pop()) {
	    var node = q.node;
	    if (node.length) {
	      var child, x0 = q.x0, y0 = q.y0, x1 = q.x1, y1 = q.y1, xm = (x0 + x1) / 2, ym = (y0 + y1) / 2;
	      if (child = node[0]) quads.push(new Quad(child, x0, y0, xm, ym));
	      if (child = node[1]) quads.push(new Quad(child, xm, y0, x1, ym));
	      if (child = node[2]) quads.push(new Quad(child, x0, ym, xm, y1));
	      if (child = node[3]) quads.push(new Quad(child, xm, ym, x1, y1));
	    }
	    next.push(q);
	  }
	  while (q = next.pop()) {
	    callback(q.node, q.x0, q.y0, q.x1, q.y1);
	  }
	  return this;
	}

	function defaultX(d) {
	  return d[0];
	}

	function tree_x(_) {
	  return arguments.length ? (this._x = _, this) : this._x;
	}

	function defaultY(d) {
	  return d[1];
	}

	function tree_y(_) {
	  return arguments.length ? (this._y = _, this) : this._y;
	}

	function quadtree(nodes, x, y) {
	  var tree = new Quadtree(x == null ? defaultX : x, y == null ? defaultY : y, NaN, NaN, NaN, NaN);
	  return nodes == null ? tree : tree.addAll(nodes);
	}

	function Quadtree(x, y, x0, y0, x1, y1) {
	  this._x = x;
	  this._y = y;
	  this._x0 = x0;
	  this._y0 = y0;
	  this._x1 = x1;
	  this._y1 = y1;
	  this._root = undefined;
	}

	function leaf_copy(leaf) {
	  var copy = {data: leaf.data}, next = copy;
	  while (leaf = leaf.next) next = next.next = {data: leaf.data};
	  return copy;
	}

	var treeProto = quadtree.prototype = Quadtree.prototype;

	treeProto.copy = function() {
	  var copy = new Quadtree(this._x, this._y, this._x0, this._y0, this._x1, this._y1),
	      node = this._root,
	      nodes,
	      child;

	  if (!node) return copy;

	  if (!node.length) return copy._root = leaf_copy(node), copy;

	  nodes = [{source: node, target: copy._root = new Array(4)}];
	  while (node = nodes.pop()) {
	    for (var i = 0; i < 4; ++i) {
	      if (child = node.source[i]) {
	        if (child.length) nodes.push({source: child, target: node.target[i] = new Array(4)});
	        else node.target[i] = leaf_copy(child);
	      }
	    }
	  }

	  return copy;
	};

	treeProto.add = tree_add;
	treeProto.addAll = addAll;
	treeProto.cover = tree_cover;
	treeProto.data = tree_data;
	treeProto.extent = tree_extent;
	treeProto.find = tree_find;
	treeProto.remove = tree_remove;
	treeProto.removeAll = removeAll;
	treeProto.root = tree_root;
	treeProto.size = tree_size;
	treeProto.visit = tree_visit;
	treeProto.visitAfter = tree_visitAfter;
	treeProto.x = tree_x;
	treeProto.y = tree_y;

	function x(d) {
	  return d.x + d.vx;
	}

	function y(d) {
	  return d.y + d.vy;
	}

	function collide(radius) {
	  var nodes,
	      radii,
	      strength = 1,
	      iterations = 1;

	  if (typeof radius !== "function") radius = constant$6(radius == null ? 1 : +radius);

	  function force() {
	    var i, n = nodes.length,
	        tree,
	        node,
	        xi,
	        yi,
	        ri,
	        ri2;

	    for (var k = 0; k < iterations; ++k) {
	      tree = quadtree(nodes, x, y).visitAfter(prepare);
	      for (i = 0; i < n; ++i) {
	        node = nodes[i];
	        ri = radii[node.index], ri2 = ri * ri;
	        xi = node.x + node.vx;
	        yi = node.y + node.vy;
	        tree.visit(apply);
	      }
	    }

	    function apply(quad, x0, y0, x1, y1) {
	      var data = quad.data, rj = quad.r, r = ri + rj;
	      if (data) {
	        if (data.index > node.index) {
	          var x = xi - data.x - data.vx,
	              y = yi - data.y - data.vy,
	              l = x * x + y * y;
	          if (l < r * r) {
	            if (x === 0) x = jiggle(), l += x * x;
	            if (y === 0) y = jiggle(), l += y * y;
	            l = (r - (l = Math.sqrt(l))) / l * strength;
	            node.vx += (x *= l) * (r = (rj *= rj) / (ri2 + rj));
	            node.vy += (y *= l) * r;
	            data.vx -= x * (r = 1 - r);
	            data.vy -= y * r;
	          }
	        }
	        return;
	      }
	      return x0 > xi + r || x1 < xi - r || y0 > yi + r || y1 < yi - r;
	    }
	  }

	  function prepare(quad) {
	    if (quad.data) return quad.r = radii[quad.data.index];
	    for (var i = quad.r = 0; i < 4; ++i) {
	      if (quad[i] && quad[i].r > quad.r) {
	        quad.r = quad[i].r;
	      }
	    }
	  }

	  function initialize() {
	    if (!nodes) return;
	    var i, n = nodes.length, node;
	    radii = new Array(n);
	    for (i = 0; i < n; ++i) node = nodes[i], radii[node.index] = +radius(node, i, nodes);
	  }

	  force.initialize = function(_) {
	    nodes = _;
	    initialize();
	  };

	  force.iterations = function(_) {
	    return arguments.length ? (iterations = +_, force) : iterations;
	  };

	  force.strength = function(_) {
	    return arguments.length ? (strength = +_, force) : strength;
	  };

	  force.radius = function(_) {
	    return arguments.length ? (radius = typeof _ === "function" ? _ : constant$6(+_), initialize(), force) : radius;
	  };

	  return force;
	}

	function index(d) {
	  return d.index;
	}

	function find(nodeById, nodeId) {
	  var node = nodeById.get(nodeId);
	  if (!node) throw new Error("missing: " + nodeId);
	  return node;
	}

	function link(links) {
	  var id = index,
	      strength = defaultStrength,
	      strengths,
	      distance = constant$6(30),
	      distances,
	      nodes,
	      count,
	      bias,
	      iterations = 1;

	  if (links == null) links = [];

	  function defaultStrength(link) {
	    return 1 / Math.min(count[link.source.index], count[link.target.index]);
	  }

	  function force(alpha) {
	    for (var k = 0, n = links.length; k < iterations; ++k) {
	      for (var i = 0, link, source, target, x, y, l, b; i < n; ++i) {
	        link = links[i], source = link.source, target = link.target;
	        x = target.x + target.vx - source.x - source.vx || jiggle();
	        y = target.y + target.vy - source.y - source.vy || jiggle();
	        l = Math.sqrt(x * x + y * y);
	        l = (l - distances[i]) / l * alpha * strengths[i];
	        x *= l, y *= l;
	        target.vx -= x * (b = bias[i]);
	        target.vy -= y * b;
	        source.vx += x * (b = 1 - b);
	        source.vy += y * b;
	      }
	    }
	  }

	  function initialize() {
	    if (!nodes) return;

	    var i,
	        n = nodes.length,
	        m = links.length,
	        nodeById = map$1(nodes, id),
	        link;

	    for (i = 0, count = new Array(n); i < m; ++i) {
	      link = links[i], link.index = i;
	      if (typeof link.source !== "object") link.source = find(nodeById, link.source);
	      if (typeof link.target !== "object") link.target = find(nodeById, link.target);
	      count[link.source.index] = (count[link.source.index] || 0) + 1;
	      count[link.target.index] = (count[link.target.index] || 0) + 1;
	    }

	    for (i = 0, bias = new Array(m); i < m; ++i) {
	      link = links[i], bias[i] = count[link.source.index] / (count[link.source.index] + count[link.target.index]);
	    }

	    strengths = new Array(m), initializeStrength();
	    distances = new Array(m), initializeDistance();
	  }

	  function initializeStrength() {
	    if (!nodes) return;

	    for (var i = 0, n = links.length; i < n; ++i) {
	      strengths[i] = +strength(links[i], i, links);
	    }
	  }

	  function initializeDistance() {
	    if (!nodes) return;

	    for (var i = 0, n = links.length; i < n; ++i) {
	      distances[i] = +distance(links[i], i, links);
	    }
	  }

	  force.initialize = function(_) {
	    nodes = _;
	    initialize();
	  };

	  force.links = function(_) {
	    return arguments.length ? (links = _, initialize(), force) : links;
	  };

	  force.id = function(_) {
	    return arguments.length ? (id = _, force) : id;
	  };

	  force.iterations = function(_) {
	    return arguments.length ? (iterations = +_, force) : iterations;
	  };

	  force.strength = function(_) {
	    return arguments.length ? (strength = typeof _ === "function" ? _ : constant$6(+_), initializeStrength(), force) : strength;
	  };

	  force.distance = function(_) {
	    return arguments.length ? (distance = typeof _ === "function" ? _ : constant$6(+_), initializeDistance(), force) : distance;
	  };

	  return force;
	}

	function x$1(d) {
	  return d.x;
	}

	function y$1(d) {
	  return d.y;
	}

	var initialRadius = 10;
	var initialAngle = Math.PI * (3 - Math.sqrt(5));

	function simulation(nodes) {
	  var simulation,
	      alpha = 1,
	      alphaMin = 0.001,
	      alphaDecay = 1 - Math.pow(alphaMin, 1 / 300),
	      alphaTarget = 0,
	      velocityDecay = 0.6,
	      forces = map$1(),
	      stepper = timer(step),
	      event = dispatch("tick", "end");

	  if (nodes == null) nodes = [];

	  function step() {
	    tick();
	    event.call("tick", simulation);
	    if (alpha < alphaMin) {
	      stepper.stop();
	      event.call("end", simulation);
	    }
	  }

	  function tick() {
	    var i, n = nodes.length, node;

	    alpha += (alphaTarget - alpha) * alphaDecay;

	    forces.each(function(force) {
	      force(alpha);
	    });

	    for (i = 0; i < n; ++i) {
	      node = nodes[i];
	      if (node.fx == null) node.x += node.vx *= velocityDecay;
	      else node.x = node.fx, node.vx = 0;
	      if (node.fy == null) node.y += node.vy *= velocityDecay;
	      else node.y = node.fy, node.vy = 0;
	    }
	  }

	  function initializeNodes() {
	    for (var i = 0, n = nodes.length, node; i < n; ++i) {
	      node = nodes[i], node.index = i;
	      if (isNaN(node.x) || isNaN(node.y)) {
	        var radius = initialRadius * Math.sqrt(i), angle = i * initialAngle;
	        node.x = radius * Math.cos(angle);
	        node.y = radius * Math.sin(angle);
	      }
	      if (isNaN(node.vx) || isNaN(node.vy)) {
	        node.vx = node.vy = 0;
	      }
	    }
	  }

	  function initializeForce(force) {
	    if (force.initialize) force.initialize(nodes);
	    return force;
	  }

	  initializeNodes();

	  return simulation = {
	    tick: tick,

	    restart: function() {
	      return stepper.restart(step), simulation;
	    },

	    stop: function() {
	      return stepper.stop(), simulation;
	    },

	    nodes: function(_) {
	      return arguments.length ? (nodes = _, initializeNodes(), forces.each(initializeForce), simulation) : nodes;
	    },

	    alpha: function(_) {
	      return arguments.length ? (alpha = +_, simulation) : alpha;
	    },

	    alphaMin: function(_) {
	      return arguments.length ? (alphaMin = +_, simulation) : alphaMin;
	    },

	    alphaDecay: function(_) {
	      return arguments.length ? (alphaDecay = +_, simulation) : +alphaDecay;
	    },

	    alphaTarget: function(_) {
	      return arguments.length ? (alphaTarget = +_, simulation) : alphaTarget;
	    },

	    velocityDecay: function(_) {
	      return arguments.length ? (velocityDecay = 1 - _, simulation) : 1 - velocityDecay;
	    },

	    force: function(name, _) {
	      return arguments.length > 1 ? (_ == null ? forces.remove(name) : forces.set(name, initializeForce(_)), simulation) : forces.get(name);
	    },

	    find: function(x, y, radius) {
	      var i = 0,
	          n = nodes.length,
	          dx,
	          dy,
	          d2,
	          node,
	          closest;

	      if (radius == null) radius = Infinity;
	      else radius *= radius;

	      for (i = 0; i < n; ++i) {
	        node = nodes[i];
	        dx = x - node.x;
	        dy = y - node.y;
	        d2 = dx * dx + dy * dy;
	        if (d2 < radius) closest = node, radius = d2;
	      }

	      return closest;
	    },

	    on: function(name, _) {
	      return arguments.length > 1 ? (event.on(name, _), simulation) : event.on(name);
	    }
	  };
	}

	function manyBody() {
	  var nodes,
	      node,
	      alpha,
	      strength = constant$6(-30),
	      strengths,
	      distanceMin2 = 1,
	      distanceMax2 = Infinity,
	      theta2 = 0.81;

	  function force(_) {
	    var i, n = nodes.length, tree = quadtree(nodes, x$1, y$1).visitAfter(accumulate);
	    for (alpha = _, i = 0; i < n; ++i) node = nodes[i], tree.visit(apply);
	  }

	  function initialize() {
	    if (!nodes) return;
	    var i, n = nodes.length, node;
	    strengths = new Array(n);
	    for (i = 0; i < n; ++i) node = nodes[i], strengths[node.index] = +strength(node, i, nodes);
	  }

	  function accumulate(quad) {
	    var strength = 0, q, c, weight = 0, x, y, i;

	    // For internal nodes, accumulate forces from child quadrants.
	    if (quad.length) {
	      for (x = y = i = 0; i < 4; ++i) {
	        if ((q = quad[i]) && (c = Math.abs(q.value))) {
	          strength += q.value, weight += c, x += c * q.x, y += c * q.y;
	        }
	      }
	      quad.x = x / weight;
	      quad.y = y / weight;
	    }

	    // For leaf nodes, accumulate forces from coincident quadrants.
	    else {
	      q = quad;
	      q.x = q.data.x;
	      q.y = q.data.y;
	      do strength += strengths[q.data.index];
	      while (q = q.next);
	    }

	    quad.value = strength;
	  }

	  function apply(quad, x1, _, x2) {
	    if (!quad.value) return true;

	    var x = quad.x - node.x,
	        y = quad.y - node.y,
	        w = x2 - x1,
	        l = x * x + y * y;

	    // Apply the Barnes-Hut approximation if possible.
	    // Limit forces for very close nodes; randomize direction if coincident.
	    if (w * w / theta2 < l) {
	      if (l < distanceMax2) {
	        if (x === 0) x = jiggle(), l += x * x;
	        if (y === 0) y = jiggle(), l += y * y;
	        if (l < distanceMin2) l = Math.sqrt(distanceMin2 * l);
	        node.vx += x * quad.value * alpha / l;
	        node.vy += y * quad.value * alpha / l;
	      }
	      return true;
	    }

	    // Otherwise, process points directly.
	    else if (quad.length || l >= distanceMax2) return;

	    // Limit forces for very close nodes; randomize direction if coincident.
	    if (quad.data !== node || quad.next) {
	      if (x === 0) x = jiggle(), l += x * x;
	      if (y === 0) y = jiggle(), l += y * y;
	      if (l < distanceMin2) l = Math.sqrt(distanceMin2 * l);
	    }

	    do if (quad.data !== node) {
	      w = strengths[quad.data.index] * alpha / l;
	      node.vx += x * w;
	      node.vy += y * w;
	    } while (quad = quad.next);
	  }

	  force.initialize = function(_) {
	    nodes = _;
	    initialize();
	  };

	  force.strength = function(_) {
	    return arguments.length ? (strength = typeof _ === "function" ? _ : constant$6(+_), initialize(), force) : strength;
	  };

	  force.distanceMin = function(_) {
	    return arguments.length ? (distanceMin2 = _ * _, force) : Math.sqrt(distanceMin2);
	  };

	  force.distanceMax = function(_) {
	    return arguments.length ? (distanceMax2 = _ * _, force) : Math.sqrt(distanceMax2);
	  };

	  force.theta = function(_) {
	    return arguments.length ? (theta2 = _ * _, force) : Math.sqrt(theta2);
	  };

	  return force;
	}

	function radial(radius, x, y) {
	  var nodes,
	      strength = constant$6(0.1),
	      strengths,
	      radiuses;

	  if (typeof radius !== "function") radius = constant$6(+radius);
	  if (x == null) x = 0;
	  if (y == null) y = 0;

	  function force(alpha) {
	    for (var i = 0, n = nodes.length; i < n; ++i) {
	      var node = nodes[i],
	          dx = node.x - x || 1e-6,
	          dy = node.y - y || 1e-6,
	          r = Math.sqrt(dx * dx + dy * dy),
	          k = (radiuses[i] - r) * strengths[i] * alpha / r;
	      node.vx += dx * k;
	      node.vy += dy * k;
	    }
	  }

	  function initialize() {
	    if (!nodes) return;
	    var i, n = nodes.length;
	    strengths = new Array(n);
	    radiuses = new Array(n);
	    for (i = 0; i < n; ++i) {
	      radiuses[i] = +radius(nodes[i], i, nodes);
	      strengths[i] = isNaN(radiuses[i]) ? 0 : +strength(nodes[i], i, nodes);
	    }
	  }

	  force.initialize = function(_) {
	    nodes = _, initialize();
	  };

	  force.strength = function(_) {
	    return arguments.length ? (strength = typeof _ === "function" ? _ : constant$6(+_), initialize(), force) : strength;
	  };

	  force.radius = function(_) {
	    return arguments.length ? (radius = typeof _ === "function" ? _ : constant$6(+_), initialize(), force) : radius;
	  };

	  force.x = function(_) {
	    return arguments.length ? (x = +_, force) : x;
	  };

	  force.y = function(_) {
	    return arguments.length ? (y = +_, force) : y;
	  };

	  return force;
	}

	function x$2(x) {
	  var strength = constant$6(0.1),
	      nodes,
	      strengths,
	      xz;

	  if (typeof x !== "function") x = constant$6(x == null ? 0 : +x);

	  function force(alpha) {
	    for (var i = 0, n = nodes.length, node; i < n; ++i) {
	      node = nodes[i], node.vx += (xz[i] - node.x) * strengths[i] * alpha;
	    }
	  }

	  function initialize() {
	    if (!nodes) return;
	    var i, n = nodes.length;
	    strengths = new Array(n);
	    xz = new Array(n);
	    for (i = 0; i < n; ++i) {
	      strengths[i] = isNaN(xz[i] = +x(nodes[i], i, nodes)) ? 0 : +strength(nodes[i], i, nodes);
	    }
	  }

	  force.initialize = function(_) {
	    nodes = _;
	    initialize();
	  };

	  force.strength = function(_) {
	    return arguments.length ? (strength = typeof _ === "function" ? _ : constant$6(+_), initialize(), force) : strength;
	  };

	  force.x = function(_) {
	    return arguments.length ? (x = typeof _ === "function" ? _ : constant$6(+_), initialize(), force) : x;
	  };

	  return force;
	}

	function y$2(y) {
	  var strength = constant$6(0.1),
	      nodes,
	      strengths,
	      yz;

	  if (typeof y !== "function") y = constant$6(y == null ? 0 : +y);

	  function force(alpha) {
	    for (var i = 0, n = nodes.length, node; i < n; ++i) {
	      node = nodes[i], node.vy += (yz[i] - node.y) * strengths[i] * alpha;
	    }
	  }

	  function initialize() {
	    if (!nodes) return;
	    var i, n = nodes.length;
	    strengths = new Array(n);
	    yz = new Array(n);
	    for (i = 0; i < n; ++i) {
	      strengths[i] = isNaN(yz[i] = +y(nodes[i], i, nodes)) ? 0 : +strength(nodes[i], i, nodes);
	    }
	  }

	  force.initialize = function(_) {
	    nodes = _;
	    initialize();
	  };

	  force.strength = function(_) {
	    return arguments.length ? (strength = typeof _ === "function" ? _ : constant$6(+_), initialize(), force) : strength;
	  };

	  force.y = function(_) {
	    return arguments.length ? (y = typeof _ === "function" ? _ : constant$6(+_), initialize(), force) : y;
	  };

	  return force;
	}

	// Computes the decimal coefficient and exponent of the specified number x with
	// significant digits p, where x is positive and p is in [1, 21] or undefined.
	// For example, formatDecimal(1.23) returns ["123", 0].
	function formatDecimal(x, p) {
	  if ((i = (x = p ? x.toExponential(p - 1) : x.toExponential()).indexOf("e")) < 0) return null; // NaN, ±Infinity
	  var i, coefficient = x.slice(0, i);

	  // The string returned by toExponential either has the form \d\.\d+e[-+]\d+
	  // (e.g., 1.2e+3) or the form \de[-+]\d+ (e.g., 1e+3).
	  return [
	    coefficient.length > 1 ? coefficient[0] + coefficient.slice(2) : coefficient,
	    +x.slice(i + 1)
	  ];
	}

	function exponent$1(x) {
	  return x = formatDecimal(Math.abs(x)), x ? x[1] : NaN;
	}

	function formatGroup(grouping, thousands) {
	  return function(value, width) {
	    var i = value.length,
	        t = [],
	        j = 0,
	        g = grouping[0],
	        length = 0;

	    while (i > 0 && g > 0) {
	      if (length + g + 1 > width) g = Math.max(1, width - length);
	      t.push(value.substring(i -= g, i + g));
	      if ((length += g + 1) > width) break;
	      g = grouping[j = (j + 1) % grouping.length];
	    }

	    return t.reverse().join(thousands);
	  };
	}

	function formatNumerals(numerals) {
	  return function(value) {
	    return value.replace(/[0-9]/g, function(i) {
	      return numerals[+i];
	    });
	  };
	}

	function formatDefault(x, p) {
	  x = x.toPrecision(p);

	  out: for (var n = x.length, i = 1, i0 = -1, i1; i < n; ++i) {
	    switch (x[i]) {
	      case ".": i0 = i1 = i; break;
	      case "0": if (i0 === 0) i0 = i; i1 = i; break;
	      case "e": break out;
	      default: if (i0 > 0) i0 = 0; break;
	    }
	  }

	  return i0 > 0 ? x.slice(0, i0) + x.slice(i1 + 1) : x;
	}

	var prefixExponent;

	function formatPrefixAuto(x, p) {
	  var d = formatDecimal(x, p);
	  if (!d) return x + "";
	  var coefficient = d[0],
	      exponent = d[1],
	      i = exponent - (prefixExponent = Math.max(-8, Math.min(8, Math.floor(exponent / 3))) * 3) + 1,
	      n = coefficient.length;
	  return i === n ? coefficient
	      : i > n ? coefficient + new Array(i - n + 1).join("0")
	      : i > 0 ? coefficient.slice(0, i) + "." + coefficient.slice(i)
	      : "0." + new Array(1 - i).join("0") + formatDecimal(x, Math.max(0, p + i - 1))[0]; // less than 1y!
	}

	function formatRounded(x, p) {
	  var d = formatDecimal(x, p);
	  if (!d) return x + "";
	  var coefficient = d[0],
	      exponent = d[1];
	  return exponent < 0 ? "0." + new Array(-exponent).join("0") + coefficient
	      : coefficient.length > exponent + 1 ? coefficient.slice(0, exponent + 1) + "." + coefficient.slice(exponent + 1)
	      : coefficient + new Array(exponent - coefficient.length + 2).join("0");
	}

	var formatTypes = {
	  "": formatDefault,
	  "%": function(x, p) { return (x * 100).toFixed(p); },
	  "b": function(x) { return Math.round(x).toString(2); },
	  "c": function(x) { return x + ""; },
	  "d": function(x) { return Math.round(x).toString(10); },
	  "e": function(x, p) { return x.toExponential(p); },
	  "f": function(x, p) { return x.toFixed(p); },
	  "g": function(x, p) { return x.toPrecision(p); },
	  "o": function(x) { return Math.round(x).toString(8); },
	  "p": function(x, p) { return formatRounded(x * 100, p); },
	  "r": formatRounded,
	  "s": formatPrefixAuto,
	  "X": function(x) { return Math.round(x).toString(16).toUpperCase(); },
	  "x": function(x) { return Math.round(x).toString(16); }
	};

	// [[fill]align][sign][symbol][0][width][,][.precision][type]
	var re = /^(?:(.)?([<>=^]))?([+\-\( ])?([$#])?(0)?(\d+)?(,)?(\.\d+)?([a-z%])?$/i;

	function formatSpecifier(specifier) {
	  return new FormatSpecifier(specifier);
	}

	formatSpecifier.prototype = FormatSpecifier.prototype; // instanceof

	function FormatSpecifier(specifier) {
	  if (!(match = re.exec(specifier))) throw new Error("invalid format: " + specifier);

	  var match,
	      fill = match[1] || " ",
	      align = match[2] || ">",
	      sign = match[3] || "-",
	      symbol = match[4] || "",
	      zero = !!match[5],
	      width = match[6] && +match[6],
	      comma = !!match[7],
	      precision = match[8] && +match[8].slice(1),
	      type = match[9] || "";

	  // The "n" type is an alias for ",g".
	  if (type === "n") comma = true, type = "g";

	  // Map invalid types to the default format.
	  else if (!formatTypes[type]) type = "";

	  // If zero fill is specified, padding goes after sign and before digits.
	  if (zero || (fill === "0" && align === "=")) zero = true, fill = "0", align = "=";

	  this.fill = fill;
	  this.align = align;
	  this.sign = sign;
	  this.symbol = symbol;
	  this.zero = zero;
	  this.width = width;
	  this.comma = comma;
	  this.precision = precision;
	  this.type = type;
	}

	FormatSpecifier.prototype.toString = function() {
	  return this.fill
	      + this.align
	      + this.sign
	      + this.symbol
	      + (this.zero ? "0" : "")
	      + (this.width == null ? "" : Math.max(1, this.width | 0))
	      + (this.comma ? "," : "")
	      + (this.precision == null ? "" : "." + Math.max(0, this.precision | 0))
	      + this.type;
	};

	function identity$3(x) {
	  return x;
	}

	var prefixes = ["y","z","a","f","p","n","\xB5","m","","k","M","G","T","P","E","Z","Y"];

	function formatLocale(locale) {
	  var group = locale.grouping && locale.thousands ? formatGroup(locale.grouping, locale.thousands) : identity$3,
	      currency = locale.currency,
	      decimal = locale.decimal,
	      numerals = locale.numerals ? formatNumerals(locale.numerals) : identity$3,
	      percent = locale.percent || "%";

	  function newFormat(specifier) {
	    specifier = formatSpecifier(specifier);

	    var fill = specifier.fill,
	        align = specifier.align,
	        sign = specifier.sign,
	        symbol = specifier.symbol,
	        zero = specifier.zero,
	        width = specifier.width,
	        comma = specifier.comma,
	        precision = specifier.precision,
	        type = specifier.type;

	    // Compute the prefix and suffix.
	    // For SI-prefix, the suffix is lazily computed.
	    var prefix = symbol === "$" ? currency[0] : symbol === "#" && /[boxX]/.test(type) ? "0" + type.toLowerCase() : "",
	        suffix = symbol === "$" ? currency[1] : /[%p]/.test(type) ? percent : "";

	    // What format function should we use?
	    // Is this an integer type?
	    // Can this type generate exponential notation?
	    var formatType = formatTypes[type],
	        maybeSuffix = !type || /[defgprs%]/.test(type);

	    // Set the default precision if not specified,
	    // or clamp the specified precision to the supported range.
	    // For significant precision, it must be in [1, 21].
	    // For fixed precision, it must be in [0, 20].
	    precision = precision == null ? (type ? 6 : 12)
	        : /[gprs]/.test(type) ? Math.max(1, Math.min(21, precision))
	        : Math.max(0, Math.min(20, precision));

	    function format(value) {
	      var valuePrefix = prefix,
	          valueSuffix = suffix,
	          i, n, c;

	      if (type === "c") {
	        valueSuffix = formatType(value) + valueSuffix;
	        value = "";
	      } else {
	        value = +value;

	        // Perform the initial formatting.
	        var valueNegative = value < 0;
	        value = formatType(Math.abs(value), precision);

	        // If a negative value rounds to zero during formatting, treat as positive.
	        if (valueNegative && +value === 0) valueNegative = false;

	        // Compute the prefix and suffix.
	        valuePrefix = (valueNegative ? (sign === "(" ? sign : "-") : sign === "-" || sign === "(" ? "" : sign) + valuePrefix;
	        valueSuffix = (type === "s" ? prefixes[8 + prefixExponent / 3] : "") + valueSuffix + (valueNegative && sign === "(" ? ")" : "");

	        // Break the formatted value into the integer “value” part that can be
	        // grouped, and fractional or exponential “suffix” part that is not.
	        if (maybeSuffix) {
	          i = -1, n = value.length;
	          while (++i < n) {
	            if (c = value.charCodeAt(i), 48 > c || c > 57) {
	              valueSuffix = (c === 46 ? decimal + value.slice(i + 1) : value.slice(i)) + valueSuffix;
	              value = value.slice(0, i);
	              break;
	            }
	          }
	        }
	      }

	      // If the fill character is not "0", grouping is applied before padding.
	      if (comma && !zero) value = group(value, Infinity);

	      // Compute the padding.
	      var length = valuePrefix.length + value.length + valueSuffix.length,
	          padding = length < width ? new Array(width - length + 1).join(fill) : "";

	      // If the fill character is "0", grouping is applied after padding.
	      if (comma && zero) value = group(padding + value, padding.length ? width - valueSuffix.length : Infinity), padding = "";

	      // Reconstruct the final output based on the desired alignment.
	      switch (align) {
	        case "<": value = valuePrefix + value + valueSuffix + padding; break;
	        case "=": value = valuePrefix + padding + value + valueSuffix; break;
	        case "^": value = padding.slice(0, length = padding.length >> 1) + valuePrefix + value + valueSuffix + padding.slice(length); break;
	        default: value = padding + valuePrefix + value + valueSuffix; break;
	      }

	      return numerals(value);
	    }

	    format.toString = function() {
	      return specifier + "";
	    };

	    return format;
	  }

	  function formatPrefix(specifier, value) {
	    var f = newFormat((specifier = formatSpecifier(specifier), specifier.type = "f", specifier)),
	        e = Math.max(-8, Math.min(8, Math.floor(exponent$1(value) / 3))) * 3,
	        k = Math.pow(10, -e),
	        prefix = prefixes[8 + e / 3];
	    return function(value) {
	      return f(k * value) + prefix;
	    };
	  }

	  return {
	    format: newFormat,
	    formatPrefix: formatPrefix
	  };
	}

	var locale;



	defaultLocale({
	  decimal: ".",
	  thousands: ",",
	  grouping: [3],
	  currency: ["$", ""]
	});

	function defaultLocale(definition) {
	  locale = formatLocale(definition);
	  exports.format = locale.format;
	  exports.formatPrefix = locale.formatPrefix;
	  return locale;
	}

	function precisionFixed(step) {
	  return Math.max(0, -exponent$1(Math.abs(step)));
	}

	function precisionPrefix(step, value) {
	  return Math.max(0, Math.max(-8, Math.min(8, Math.floor(exponent$1(value) / 3))) * 3 - exponent$1(Math.abs(step)));
	}

	function precisionRound(step, max) {
	  step = Math.abs(step), max = Math.abs(max) - step;
	  return Math.max(0, exponent$1(max) - exponent$1(step)) + 1;
	}

	// Adds floating point numbers with twice the normal precision.
	// Reference: J. R. Shewchuk, Adaptive Precision Floating-Point Arithmetic and
	// Fast Robust Geometric Predicates, Discrete & Computational Geometry 18(3)
	// 305–363 (1997).
	// Code adapted from GeographicLib by Charles F. F. Karney,
	// http://geographiclib.sourceforge.net/

	function adder() {
	  return new Adder;
	}

	function Adder() {
	  this.reset();
	}

	Adder.prototype = {
	  constructor: Adder,
	  reset: function() {
	    this.s = // rounded value
	    this.t = 0; // exact error
	  },
	  add: function(y) {
	    add$1(temp, y, this.t);
	    add$1(this, temp.s, this.s);
	    if (this.s) this.t += temp.t;
	    else this.s = temp.t;
	  },
	  valueOf: function() {
	    return this.s;
	  }
	};

	var temp = new Adder;

	function add$1(adder, a, b) {
	  var x = adder.s = a + b,
	      bv = x - a,
	      av = x - bv;
	  adder.t = (a - av) + (b - bv);
	}

	var epsilon$2 = 1e-6;
	var epsilon2$1 = 1e-12;
	var pi$3 = Math.PI;
	var halfPi$2 = pi$3 / 2;
	var quarterPi = pi$3 / 4;
	var tau$3 = pi$3 * 2;

	var degrees$1 = 180 / pi$3;
	var radians = pi$3 / 180;

	var abs = Math.abs;
	var atan = Math.atan;
	var atan2 = Math.atan2;
	var cos$1 = Math.cos;
	var ceil = Math.ceil;
	var exp = Math.exp;

	var log = Math.log;
	var pow = Math.pow;
	var sin$1 = Math.sin;
	var sign = Math.sign || function(x) { return x > 0 ? 1 : x < 0 ? -1 : 0; };
	var sqrt = Math.sqrt;
	var tan = Math.tan;

	function acos(x) {
	  return x > 1 ? 0 : x < -1 ? pi$3 : Math.acos(x);
	}

	function asin(x) {
	  return x > 1 ? halfPi$2 : x < -1 ? -halfPi$2 : Math.asin(x);
	}

	function haversin(x) {
	  return (x = sin$1(x / 2)) * x;
	}

	function noop$1() {}

	function streamGeometry(geometry, stream) {
	  if (geometry && streamGeometryType.hasOwnProperty(geometry.type)) {
	    streamGeometryType[geometry.type](geometry, stream);
	  }
	}

	var streamObjectType = {
	  Feature: function(object, stream) {
	    streamGeometry(object.geometry, stream);
	  },
	  FeatureCollection: function(object, stream) {
	    var features = object.features, i = -1, n = features.length;
	    while (++i < n) streamGeometry(features[i].geometry, stream);
	  }
	};

	var streamGeometryType = {
	  Sphere: function(object, stream) {
	    stream.sphere();
	  },
	  Point: function(object, stream) {
	    object = object.coordinates;
	    stream.point(object[0], object[1], object[2]);
	  },
	  MultiPoint: function(object, stream) {
	    var coordinates = object.coordinates, i = -1, n = coordinates.length;
	    while (++i < n) object = coordinates[i], stream.point(object[0], object[1], object[2]);
	  },
	  LineString: function(object, stream) {
	    streamLine(object.coordinates, stream, 0);
	  },
	  MultiLineString: function(object, stream) {
	    var coordinates = object.coordinates, i = -1, n = coordinates.length;
	    while (++i < n) streamLine(coordinates[i], stream, 0);
	  },
	  Polygon: function(object, stream) {
	    streamPolygon(object.coordinates, stream);
	  },
	  MultiPolygon: function(object, stream) {
	    var coordinates = object.coordinates, i = -1, n = coordinates.length;
	    while (++i < n) streamPolygon(coordinates[i], stream);
	  },
	  GeometryCollection: function(object, stream) {
	    var geometries = object.geometries, i = -1, n = geometries.length;
	    while (++i < n) streamGeometry(geometries[i], stream);
	  }
	};

	function streamLine(coordinates, stream, closed) {
	  var i = -1, n = coordinates.length - closed, coordinate;
	  stream.lineStart();
	  while (++i < n) coordinate = coordinates[i], stream.point(coordinate[0], coordinate[1], coordinate[2]);
	  stream.lineEnd();
	}

	function streamPolygon(coordinates, stream) {
	  var i = -1, n = coordinates.length;
	  stream.polygonStart();
	  while (++i < n) streamLine(coordinates[i], stream, 1);
	  stream.polygonEnd();
	}

	function geoStream(object, stream) {
	  if (object && streamObjectType.hasOwnProperty(object.type)) {
	    streamObjectType[object.type](object, stream);
	  } else {
	    streamGeometry(object, stream);
	  }
	}

	var areaRingSum = adder();

	var areaSum = adder();
	var lambda00;
	var phi00;
	var lambda0;
	var cosPhi0;
	var sinPhi0;

	var areaStream = {
	  point: noop$1,
	  lineStart: noop$1,
	  lineEnd: noop$1,
	  polygonStart: function() {
	    areaRingSum.reset();
	    areaStream.lineStart = areaRingStart;
	    areaStream.lineEnd = areaRingEnd;
	  },
	  polygonEnd: function() {
	    var areaRing = +areaRingSum;
	    areaSum.add(areaRing < 0 ? tau$3 + areaRing : areaRing);
	    this.lineStart = this.lineEnd = this.point = noop$1;
	  },
	  sphere: function() {
	    areaSum.add(tau$3);
	  }
	};

	function areaRingStart() {
	  areaStream.point = areaPointFirst;
	}

	function areaRingEnd() {
	  areaPoint(lambda00, phi00);
	}

	function areaPointFirst(lambda, phi) {
	  areaStream.point = areaPoint;
	  lambda00 = lambda, phi00 = phi;
	  lambda *= radians, phi *= radians;
	  lambda0 = lambda, cosPhi0 = cos$1(phi = phi / 2 + quarterPi), sinPhi0 = sin$1(phi);
	}

	function areaPoint(lambda, phi) {
	  lambda *= radians, phi *= radians;
	  phi = phi / 2 + quarterPi; // half the angular distance from south pole

	  // Spherical excess E for a spherical triangle with vertices: south pole,
	  // previous point, current point.  Uses a formula derived from Cagnoli’s
	  // theorem.  See Todhunter, Spherical Trig. (1871), Sec. 103, Eq. (2).
	  var dLambda = lambda - lambda0,
	      sdLambda = dLambda >= 0 ? 1 : -1,
	      adLambda = sdLambda * dLambda,
	      cosPhi = cos$1(phi),
	      sinPhi = sin$1(phi),
	      k = sinPhi0 * sinPhi,
	      u = cosPhi0 * cosPhi + k * cos$1(adLambda),
	      v = k * sdLambda * sin$1(adLambda);
	  areaRingSum.add(atan2(v, u));

	  // Advance the previous points.
	  lambda0 = lambda, cosPhi0 = cosPhi, sinPhi0 = sinPhi;
	}

	function area(object) {
	  areaSum.reset();
	  geoStream(object, areaStream);
	  return areaSum * 2;
	}

	function spherical(cartesian) {
	  return [atan2(cartesian[1], cartesian[0]), asin(cartesian[2])];
	}

	function cartesian(spherical) {
	  var lambda = spherical[0], phi = spherical[1], cosPhi = cos$1(phi);
	  return [cosPhi * cos$1(lambda), cosPhi * sin$1(lambda), sin$1(phi)];
	}

	function cartesianDot(a, b) {
	  return a[0] * b[0] + a[1] * b[1] + a[2] * b[2];
	}

	function cartesianCross(a, b) {
	  return [a[1] * b[2] - a[2] * b[1], a[2] * b[0] - a[0] * b[2], a[0] * b[1] - a[1] * b[0]];
	}

	// TODO return a
	function cartesianAddInPlace(a, b) {
	  a[0] += b[0], a[1] += b[1], a[2] += b[2];
	}

	function cartesianScale(vector, k) {
	  return [vector[0] * k, vector[1] * k, vector[2] * k];
	}

	// TODO return d
	function cartesianNormalizeInPlace(d) {
	  var l = sqrt(d[0] * d[0] + d[1] * d[1] + d[2] * d[2]);
	  d[0] /= l, d[1] /= l, d[2] /= l;
	}

	var lambda0$1;
	var phi0;
	var lambda1;
	var phi1;
	var lambda2;
	var lambda00$1;
	var phi00$1;
	var p0;
	var deltaSum = adder();
	var ranges;
	var range;

	var boundsStream = {
	  point: boundsPoint,
	  lineStart: boundsLineStart,
	  lineEnd: boundsLineEnd,
	  polygonStart: function() {
	    boundsStream.point = boundsRingPoint;
	    boundsStream.lineStart = boundsRingStart;
	    boundsStream.lineEnd = boundsRingEnd;
	    deltaSum.reset();
	    areaStream.polygonStart();
	  },
	  polygonEnd: function() {
	    areaStream.polygonEnd();
	    boundsStream.point = boundsPoint;
	    boundsStream.lineStart = boundsLineStart;
	    boundsStream.lineEnd = boundsLineEnd;
	    if (areaRingSum < 0) lambda0$1 = -(lambda1 = 180), phi0 = -(phi1 = 90);
	    else if (deltaSum > epsilon$2) phi1 = 90;
	    else if (deltaSum < -epsilon$2) phi0 = -90;
	    range[0] = lambda0$1, range[1] = lambda1;
	  }
	};

	function boundsPoint(lambda, phi) {
	  ranges.push(range = [lambda0$1 = lambda, lambda1 = lambda]);
	  if (phi < phi0) phi0 = phi;
	  if (phi > phi1) phi1 = phi;
	}

	function linePoint(lambda, phi) {
	  var p = cartesian([lambda * radians, phi * radians]);
	  if (p0) {
	    var normal = cartesianCross(p0, p),
	        equatorial = [normal[1], -normal[0], 0],
	        inflection = cartesianCross(equatorial, normal);
	    cartesianNormalizeInPlace(inflection);
	    inflection = spherical(inflection);
	    var delta = lambda - lambda2,
	        sign$$1 = delta > 0 ? 1 : -1,
	        lambdai = inflection[0] * degrees$1 * sign$$1,
	        phii,
	        antimeridian = abs(delta) > 180;
	    if (antimeridian ^ (sign$$1 * lambda2 < lambdai && lambdai < sign$$1 * lambda)) {
	      phii = inflection[1] * degrees$1;
	      if (phii > phi1) phi1 = phii;
	    } else if (lambdai = (lambdai + 360) % 360 - 180, antimeridian ^ (sign$$1 * lambda2 < lambdai && lambdai < sign$$1 * lambda)) {
	      phii = -inflection[1] * degrees$1;
	      if (phii < phi0) phi0 = phii;
	    } else {
	      if (phi < phi0) phi0 = phi;
	      if (phi > phi1) phi1 = phi;
	    }
	    if (antimeridian) {
	      if (lambda < lambda2) {
	        if (angle(lambda0$1, lambda) > angle(lambda0$1, lambda1)) lambda1 = lambda;
	      } else {
	        if (angle(lambda, lambda1) > angle(lambda0$1, lambda1)) lambda0$1 = lambda;
	      }
	    } else {
	      if (lambda1 >= lambda0$1) {
	        if (lambda < lambda0$1) lambda0$1 = lambda;
	        if (lambda > lambda1) lambda1 = lambda;
	      } else {
	        if (lambda > lambda2) {
	          if (angle(lambda0$1, lambda) > angle(lambda0$1, lambda1)) lambda1 = lambda;
	        } else {
	          if (angle(lambda, lambda1) > angle(lambda0$1, lambda1)) lambda0$1 = lambda;
	        }
	      }
	    }
	  } else {
	    ranges.push(range = [lambda0$1 = lambda, lambda1 = lambda]);
	  }
	  if (phi < phi0) phi0 = phi;
	  if (phi > phi1) phi1 = phi;
	  p0 = p, lambda2 = lambda;
	}

	function boundsLineStart() {
	  boundsStream.point = linePoint;
	}

	function boundsLineEnd() {
	  range[0] = lambda0$1, range[1] = lambda1;
	  boundsStream.point = boundsPoint;
	  p0 = null;
	}

	function boundsRingPoint(lambda, phi) {
	  if (p0) {
	    var delta = lambda - lambda2;
	    deltaSum.add(abs(delta) > 180 ? delta + (delta > 0 ? 360 : -360) : delta);
	  } else {
	    lambda00$1 = lambda, phi00$1 = phi;
	  }
	  areaStream.point(lambda, phi);
	  linePoint(lambda, phi);
	}

	function boundsRingStart() {
	  areaStream.lineStart();
	}

	function boundsRingEnd() {
	  boundsRingPoint(lambda00$1, phi00$1);
	  areaStream.lineEnd();
	  if (abs(deltaSum) > epsilon$2) lambda0$1 = -(lambda1 = 180);
	  range[0] = lambda0$1, range[1] = lambda1;
	  p0 = null;
	}

	// Finds the left-right distance between two longitudes.
	// This is almost the same as (lambda1 - lambda0 + 360°) % 360°, except that we want
	// the distance between ±180° to be 360°.
	function angle(lambda0, lambda1) {
	  return (lambda1 -= lambda0) < 0 ? lambda1 + 360 : lambda1;
	}

	function rangeCompare(a, b) {
	  return a[0] - b[0];
	}

	function rangeContains(range, x) {
	  return range[0] <= range[1] ? range[0] <= x && x <= range[1] : x < range[0] || range[1] < x;
	}

	function bounds(feature) {
	  var i, n, a, b, merged, deltaMax, delta;

	  phi1 = lambda1 = -(lambda0$1 = phi0 = Infinity);
	  ranges = [];
	  geoStream(feature, boundsStream);

	  // First, sort ranges by their minimum longitudes.
	  if (n = ranges.length) {
	    ranges.sort(rangeCompare);

	    // Then, merge any ranges that overlap.
	    for (i = 1, a = ranges[0], merged = [a]; i < n; ++i) {
	      b = ranges[i];
	      if (rangeContains(a, b[0]) || rangeContains(a, b[1])) {
	        if (angle(a[0], b[1]) > angle(a[0], a[1])) a[1] = b[1];
	        if (angle(b[0], a[1]) > angle(a[0], a[1])) a[0] = b[0];
	      } else {
	        merged.push(a = b);
	      }
	    }

	    // Finally, find the largest gap between the merged ranges.
	    // The final bounding box will be the inverse of this gap.
	    for (deltaMax = -Infinity, n = merged.length - 1, i = 0, a = merged[n]; i <= n; a = b, ++i) {
	      b = merged[i];
	      if ((delta = angle(a[1], b[0])) > deltaMax) deltaMax = delta, lambda0$1 = b[0], lambda1 = a[1];
	    }
	  }

	  ranges = range = null;

	  return lambda0$1 === Infinity || phi0 === Infinity
	      ? [[NaN, NaN], [NaN, NaN]]
	      : [[lambda0$1, phi0], [lambda1, phi1]];
	}

	var W0;
	var W1;
	var X0;
	var Y0;
	var Z0;
	var X1;
	var Y1;
	var Z1;
	var X2;
	var Y2;
	var Z2;
	var lambda00$2;
	var phi00$2;
	var x0;
	var y0;
	var z0; // previous point

	var centroidStream = {
	  sphere: noop$1,
	  point: centroidPoint,
	  lineStart: centroidLineStart,
	  lineEnd: centroidLineEnd,
	  polygonStart: function() {
	    centroidStream.lineStart = centroidRingStart;
	    centroidStream.lineEnd = centroidRingEnd;
	  },
	  polygonEnd: function() {
	    centroidStream.lineStart = centroidLineStart;
	    centroidStream.lineEnd = centroidLineEnd;
	  }
	};

	// Arithmetic mean of Cartesian vectors.
	function centroidPoint(lambda, phi) {
	  lambda *= radians, phi *= radians;
	  var cosPhi = cos$1(phi);
	  centroidPointCartesian(cosPhi * cos$1(lambda), cosPhi * sin$1(lambda), sin$1(phi));
	}

	function centroidPointCartesian(x, y, z) {
	  ++W0;
	  X0 += (x - X0) / W0;
	  Y0 += (y - Y0) / W0;
	  Z0 += (z - Z0) / W0;
	}

	function centroidLineStart() {
	  centroidStream.point = centroidLinePointFirst;
	}

	function centroidLinePointFirst(lambda, phi) {
	  lambda *= radians, phi *= radians;
	  var cosPhi = cos$1(phi);
	  x0 = cosPhi * cos$1(lambda);
	  y0 = cosPhi * sin$1(lambda);
	  z0 = sin$1(phi);
	  centroidStream.point = centroidLinePoint;
	  centroidPointCartesian(x0, y0, z0);
	}

	function centroidLinePoint(lambda, phi) {
	  lambda *= radians, phi *= radians;
	  var cosPhi = cos$1(phi),
	      x = cosPhi * cos$1(lambda),
	      y = cosPhi * sin$1(lambda),
	      z = sin$1(phi),
	      w = atan2(sqrt((w = y0 * z - z0 * y) * w + (w = z0 * x - x0 * z) * w + (w = x0 * y - y0 * x) * w), x0 * x + y0 * y + z0 * z);
	  W1 += w;
	  X1 += w * (x0 + (x0 = x));
	  Y1 += w * (y0 + (y0 = y));
	  Z1 += w * (z0 + (z0 = z));
	  centroidPointCartesian(x0, y0, z0);
	}

	function centroidLineEnd() {
	  centroidStream.point = centroidPoint;
	}

	// See J. E. Brock, The Inertia Tensor for a Spherical Triangle,
	// J. Applied Mechanics 42, 239 (1975).
	function centroidRingStart() {
	  centroidStream.point = centroidRingPointFirst;
	}

	function centroidRingEnd() {
	  centroidRingPoint(lambda00$2, phi00$2);
	  centroidStream.point = centroidPoint;
	}

	function centroidRingPointFirst(lambda, phi) {
	  lambda00$2 = lambda, phi00$2 = phi;
	  lambda *= radians, phi *= radians;
	  centroidStream.point = centroidRingPoint;
	  var cosPhi = cos$1(phi);
	  x0 = cosPhi * cos$1(lambda);
	  y0 = cosPhi * sin$1(lambda);
	  z0 = sin$1(phi);
	  centroidPointCartesian(x0, y0, z0);
	}

	function centroidRingPoint(lambda, phi) {
	  lambda *= radians, phi *= radians;
	  var cosPhi = cos$1(phi),
	      x = cosPhi * cos$1(lambda),
	      y = cosPhi * sin$1(lambda),
	      z = sin$1(phi),
	      cx = y0 * z - z0 * y,
	      cy = z0 * x - x0 * z,
	      cz = x0 * y - y0 * x,
	      m = sqrt(cx * cx + cy * cy + cz * cz),
	      w = asin(m), // line weight = angle
	      v = m && -w / m; // area weight multiplier
	  X2 += v * cx;
	  Y2 += v * cy;
	  Z2 += v * cz;
	  W1 += w;
	  X1 += w * (x0 + (x0 = x));
	  Y1 += w * (y0 + (y0 = y));
	  Z1 += w * (z0 + (z0 = z));
	  centroidPointCartesian(x0, y0, z0);
	}

	function centroid(object) {
	  W0 = W1 =
	  X0 = Y0 = Z0 =
	  X1 = Y1 = Z1 =
	  X2 = Y2 = Z2 = 0;
	  geoStream(object, centroidStream);

	  var x = X2,
	      y = Y2,
	      z = Z2,
	      m = x * x + y * y + z * z;

	  // If the area-weighted ccentroid is undefined, fall back to length-weighted ccentroid.
	  if (m < epsilon2$1) {
	    x = X1, y = Y1, z = Z1;
	    // If the feature has zero length, fall back to arithmetic mean of point vectors.
	    if (W1 < epsilon$2) x = X0, y = Y0, z = Z0;
	    m = x * x + y * y + z * z;
	    // If the feature still has an undefined ccentroid, then return.
	    if (m < epsilon2$1) return [NaN, NaN];
	  }

	  return [atan2(y, x) * degrees$1, asin(z / sqrt(m)) * degrees$1];
	}

	function constant$7(x) {
	  return function() {
	    return x;
	  };
	}

	function compose(a, b) {

	  function compose(x, y) {
	    return x = a(x, y), b(x[0], x[1]);
	  }

	  if (a.invert && b.invert) compose.invert = function(x, y) {
	    return x = b.invert(x, y), x && a.invert(x[0], x[1]);
	  };

	  return compose;
	}

	function rotationIdentity(lambda, phi) {
	  return [lambda > pi$3 ? lambda - tau$3 : lambda < -pi$3 ? lambda + tau$3 : lambda, phi];
	}

	rotationIdentity.invert = rotationIdentity;

	function rotateRadians(deltaLambda, deltaPhi, deltaGamma) {
	  return (deltaLambda %= tau$3) ? (deltaPhi || deltaGamma ? compose(rotationLambda(deltaLambda), rotationPhiGamma(deltaPhi, deltaGamma))
	    : rotationLambda(deltaLambda))
	    : (deltaPhi || deltaGamma ? rotationPhiGamma(deltaPhi, deltaGamma)
	    : rotationIdentity);
	}

	function forwardRotationLambda(deltaLambda) {
	  return function(lambda, phi) {
	    return lambda += deltaLambda, [lambda > pi$3 ? lambda - tau$3 : lambda < -pi$3 ? lambda + tau$3 : lambda, phi];
	  };
	}

	function rotationLambda(deltaLambda) {
	  var rotation = forwardRotationLambda(deltaLambda);
	  rotation.invert = forwardRotationLambda(-deltaLambda);
	  return rotation;
	}

	function rotationPhiGamma(deltaPhi, deltaGamma) {
	  var cosDeltaPhi = cos$1(deltaPhi),
	      sinDeltaPhi = sin$1(deltaPhi),
	      cosDeltaGamma = cos$1(deltaGamma),
	      sinDeltaGamma = sin$1(deltaGamma);

	  function rotation(lambda, phi) {
	    var cosPhi = cos$1(phi),
	        x = cos$1(lambda) * cosPhi,
	        y = sin$1(lambda) * cosPhi,
	        z = sin$1(phi),
	        k = z * cosDeltaPhi + x * sinDeltaPhi;
	    return [
	      atan2(y * cosDeltaGamma - k * sinDeltaGamma, x * cosDeltaPhi - z * sinDeltaPhi),
	      asin(k * cosDeltaGamma + y * sinDeltaGamma)
	    ];
	  }

	  rotation.invert = function(lambda, phi) {
	    var cosPhi = cos$1(phi),
	        x = cos$1(lambda) * cosPhi,
	        y = sin$1(lambda) * cosPhi,
	        z = sin$1(phi),
	        k = z * cosDeltaGamma - y * sinDeltaGamma;
	    return [
	      atan2(y * cosDeltaGamma + z * sinDeltaGamma, x * cosDeltaPhi + k * sinDeltaPhi),
	      asin(k * cosDeltaPhi - x * sinDeltaPhi)
	    ];
	  };

	  return rotation;
	}

	function rotation(rotate) {
	  rotate = rotateRadians(rotate[0] * radians, rotate[1] * radians, rotate.length > 2 ? rotate[2] * radians : 0);

	  function forward(coordinates) {
	    coordinates = rotate(coordinates[0] * radians, coordinates[1] * radians);
	    return coordinates[0] *= degrees$1, coordinates[1] *= degrees$1, coordinates;
	  }

	  forward.invert = function(coordinates) {
	    coordinates = rotate.invert(coordinates[0] * radians, coordinates[1] * radians);
	    return coordinates[0] *= degrees$1, coordinates[1] *= degrees$1, coordinates;
	  };

	  return forward;
	}

	// Generates a circle centered at [0°, 0°], with a given radius and precision.
	function circleStream(stream, radius, delta, direction, t0, t1) {
	  if (!delta) return;
	  var cosRadius = cos$1(radius),
	      sinRadius = sin$1(radius),
	      step = direction * delta;
	  if (t0 == null) {
	    t0 = radius + direction * tau$3;
	    t1 = radius - step / 2;
	  } else {
	    t0 = circleRadius(cosRadius, t0);
	    t1 = circleRadius(cosRadius, t1);
	    if (direction > 0 ? t0 < t1 : t0 > t1) t0 += direction * tau$3;
	  }
	  for (var point, t = t0; direction > 0 ? t > t1 : t < t1; t -= step) {
	    point = spherical([cosRadius, -sinRadius * cos$1(t), -sinRadius * sin$1(t)]);
	    stream.point(point[0], point[1]);
	  }
	}

	// Returns the signed angle of a cartesian point relative to [cosRadius, 0, 0].
	function circleRadius(cosRadius, point) {
	  point = cartesian(point), point[0] -= cosRadius;
	  cartesianNormalizeInPlace(point);
	  var radius = acos(-point[1]);
	  return ((-point[2] < 0 ? -radius : radius) + tau$3 - epsilon$2) % tau$3;
	}

	function circle() {
	  var center = constant$7([0, 0]),
	      radius = constant$7(90),
	      precision = constant$7(6),
	      ring,
	      rotate,
	      stream = {point: point};

	  function point(x, y) {
	    ring.push(x = rotate(x, y));
	    x[0] *= degrees$1, x[1] *= degrees$1;
	  }

	  function circle() {
	    var c = center.apply(this, arguments),
	        r = radius.apply(this, arguments) * radians,
	        p = precision.apply(this, arguments) * radians;
	    ring = [];
	    rotate = rotateRadians(-c[0] * radians, -c[1] * radians, 0).invert;
	    circleStream(stream, r, p, 1);
	    c = {type: "Polygon", coordinates: [ring]};
	    ring = rotate = null;
	    return c;
	  }

	  circle.center = function(_) {
	    return arguments.length ? (center = typeof _ === "function" ? _ : constant$7([+_[0], +_[1]]), circle) : center;
	  };

	  circle.radius = function(_) {
	    return arguments.length ? (radius = typeof _ === "function" ? _ : constant$7(+_), circle) : radius;
	  };

	  circle.precision = function(_) {
	    return arguments.length ? (precision = typeof _ === "function" ? _ : constant$7(+_), circle) : precision;
	  };

	  return circle;
	}

	function clipBuffer() {
	  var lines = [],
	      line;
	  return {
	    point: function(x, y) {
	      line.push([x, y]);
	    },
	    lineStart: function() {
	      lines.push(line = []);
	    },
	    lineEnd: noop$1,
	    rejoin: function() {
	      if (lines.length > 1) lines.push(lines.pop().concat(lines.shift()));
	    },
	    result: function() {
	      var result = lines;
	      lines = [];
	      line = null;
	      return result;
	    }
	  };
	}

	function pointEqual(a, b) {
	  return abs(a[0] - b[0]) < epsilon$2 && abs(a[1] - b[1]) < epsilon$2;
	}

	function Intersection(point, points, other, entry) {
	  this.x = point;
	  this.z = points;
	  this.o = other; // another intersection
	  this.e = entry; // is an entry?
	  this.v = false; // visited
	  this.n = this.p = null; // next & previous
	}

	// A generalized polygon clipping algorithm: given a polygon that has been cut
	// into its visible line segments, and rejoins the segments by interpolating
	// along the clip edge.
	function clipRejoin(segments, compareIntersection, startInside, interpolate, stream) {
	  var subject = [],
	      clip = [],
	      i,
	      n;

	  segments.forEach(function(segment) {
	    if ((n = segment.length - 1) <= 0) return;
	    var n, p0 = segment[0], p1 = segment[n], x;

	    // If the first and last points of a segment are coincident, then treat as a
	    // closed ring. TODO if all rings are closed, then the winding order of the
	    // exterior ring should be checked.
	    if (pointEqual(p0, p1)) {
	      stream.lineStart();
	      for (i = 0; i < n; ++i) stream.point((p0 = segment[i])[0], p0[1]);
	      stream.lineEnd();
	      return;
	    }

	    subject.push(x = new Intersection(p0, segment, null, true));
	    clip.push(x.o = new Intersection(p0, null, x, false));
	    subject.push(x = new Intersection(p1, segment, null, false));
	    clip.push(x.o = new Intersection(p1, null, x, true));
	  });

	  if (!subject.length) return;

	  clip.sort(compareIntersection);
	  link$1(subject);
	  link$1(clip);

	  for (i = 0, n = clip.length; i < n; ++i) {
	    clip[i].e = startInside = !startInside;
	  }

	  var start = subject[0],
	      points,
	      point;

	  while (1) {
	    // Find first unvisited intersection.
	    var current = start,
	        isSubject = true;
	    while (current.v) if ((current = current.n) === start) return;
	    points = current.z;
	    stream.lineStart();
	    do {
	      current.v = current.o.v = true;
	      if (current.e) {
	        if (isSubject) {
	          for (i = 0, n = points.length; i < n; ++i) stream.point((point = points[i])[0], point[1]);
	        } else {
	          interpolate(current.x, current.n.x, 1, stream);
	        }
	        current = current.n;
	      } else {
	        if (isSubject) {
	          points = current.p.z;
	          for (i = points.length - 1; i >= 0; --i) stream.point((point = points[i])[0], point[1]);
	        } else {
	          interpolate(current.x, current.p.x, -1, stream);
	        }
	        current = current.p;
	      }
	      current = current.o;
	      points = current.z;
	      isSubject = !isSubject;
	    } while (!current.v);
	    stream.lineEnd();
	  }
	}

	function link$1(array) {
	  if (!(n = array.length)) return;
	  var n,
	      i = 0,
	      a = array[0],
	      b;
	  while (++i < n) {
	    a.n = b = array[i];
	    b.p = a;
	    a = b;
	  }
	  a.n = b = array[0];
	  b.p = a;
	}

	var sum$1 = adder();

	function polygonContains(polygon, point) {
	  var lambda = point[0],
	      phi = point[1],
	      normal = [sin$1(lambda), -cos$1(lambda), 0],
	      angle = 0,
	      winding = 0;

	  sum$1.reset();

	  for (var i = 0, n = polygon.length; i < n; ++i) {
	    if (!(m = (ring = polygon[i]).length)) continue;
	    var ring,
	        m,
	        point0 = ring[m - 1],
	        lambda0 = point0[0],
	        phi0 = point0[1] / 2 + quarterPi,
	        sinPhi0 = sin$1(phi0),
	        cosPhi0 = cos$1(phi0);

	    for (var j = 0; j < m; ++j, lambda0 = lambda1, sinPhi0 = sinPhi1, cosPhi0 = cosPhi1, point0 = point1) {
	      var point1 = ring[j],
	          lambda1 = point1[0],
	          phi1 = point1[1] / 2 + quarterPi,
	          sinPhi1 = sin$1(phi1),
	          cosPhi1 = cos$1(phi1),
	          delta = lambda1 - lambda0,
	          sign$$1 = delta >= 0 ? 1 : -1,
	          absDelta = sign$$1 * delta,
	          antimeridian = absDelta > pi$3,
	          k = sinPhi0 * sinPhi1;

	      sum$1.add(atan2(k * sign$$1 * sin$1(absDelta), cosPhi0 * cosPhi1 + k * cos$1(absDelta)));
	      angle += antimeridian ? delta + sign$$1 * tau$3 : delta;

	      // Are the longitudes either side of the point’s meridian (lambda),
	      // and are the latitudes smaller than the parallel (phi)?
	      if (antimeridian ^ lambda0 >= lambda ^ lambda1 >= lambda) {
	        var arc = cartesianCross(cartesian(point0), cartesian(point1));
	        cartesianNormalizeInPlace(arc);
	        var intersection = cartesianCross(normal, arc);
	        cartesianNormalizeInPlace(intersection);
	        var phiArc = (antimeridian ^ delta >= 0 ? -1 : 1) * asin(intersection[2]);
	        if (phi > phiArc || phi === phiArc && (arc[0] || arc[1])) {
	          winding += antimeridian ^ delta >= 0 ? 1 : -1;
	        }
	      }
	    }
	  }

	  // First, determine whether the South pole is inside or outside:
	  //
	  // It is inside if:
	  // * the polygon winds around it in a clockwise direction.
	  // * the polygon does not (cumulatively) wind around it, but has a negative
	  //   (counter-clockwise) area.
	  //
	  // Second, count the (signed) number of times a segment crosses a lambda
	  // from the point to the South pole.  If it is zero, then the point is the
	  // same side as the South pole.

	  return (angle < -epsilon$2 || angle < epsilon$2 && sum$1 < -epsilon$2) ^ (winding & 1);
	}

	function clip(pointVisible, clipLine, interpolate, start) {
	  return function(sink) {
	    var line = clipLine(sink),
	        ringBuffer = clipBuffer(),
	        ringSink = clipLine(ringBuffer),
	        polygonStarted = false,
	        polygon,
	        segments,
	        ring;

	    var clip = {
	      point: point,
	      lineStart: lineStart,
	      lineEnd: lineEnd,
	      polygonStart: function() {
	        clip.point = pointRing;
	        clip.lineStart = ringStart;
	        clip.lineEnd = ringEnd;
	        segments = [];
	        polygon = [];
	      },
	      polygonEnd: function() {
	        clip.point = point;
	        clip.lineStart = lineStart;
	        clip.lineEnd = lineEnd;
	        segments = merge(segments);
	        var startInside = polygonContains(polygon, start);
	        if (segments.length) {
	          if (!polygonStarted) sink.polygonStart(), polygonStarted = true;
	          clipRejoin(segments, compareIntersection, startInside, interpolate, sink);
	        } else if (startInside) {
	          if (!polygonStarted) sink.polygonStart(), polygonStarted = true;
	          sink.lineStart();
	          interpolate(null, null, 1, sink);
	          sink.lineEnd();
	        }
	        if (polygonStarted) sink.polygonEnd(), polygonStarted = false;
	        segments = polygon = null;
	      },
	      sphere: function() {
	        sink.polygonStart();
	        sink.lineStart();
	        interpolate(null, null, 1, sink);
	        sink.lineEnd();
	        sink.polygonEnd();
	      }
	    };

	    function point(lambda, phi) {
	      if (pointVisible(lambda, phi)) sink.point(lambda, phi);
	    }

	    function pointLine(lambda, phi) {
	      line.point(lambda, phi);
	    }

	    function lineStart() {
	      clip.point = pointLine;
	      line.lineStart();
	    }

	    function lineEnd() {
	      clip.point = point;
	      line.lineEnd();
	    }

	    function pointRing(lambda, phi) {
	      ring.push([lambda, phi]);
	      ringSink.point(lambda, phi);
	    }

	    function ringStart() {
	      ringSink.lineStart();
	      ring = [];
	    }

	    function ringEnd() {
	      pointRing(ring[0][0], ring[0][1]);
	      ringSink.lineEnd();

	      var clean = ringSink.clean(),
	          ringSegments = ringBuffer.result(),
	          i, n = ringSegments.length, m,
	          segment,
	          point;

	      ring.pop();
	      polygon.push(ring);
	      ring = null;

	      if (!n) return;

	      // No intersections.
	      if (clean & 1) {
	        segment = ringSegments[0];
	        if ((m = segment.length - 1) > 0) {
	          if (!polygonStarted) sink.polygonStart(), polygonStarted = true;
	          sink.lineStart();
	          for (i = 0; i < m; ++i) sink.point((point = segment[i])[0], point[1]);
	          sink.lineEnd();
	        }
	        return;
	      }

	      // Rejoin connected segments.
	      // TODO reuse ringBuffer.rejoin()?
	      if (n > 1 && clean & 2) ringSegments.push(ringSegments.pop().concat(ringSegments.shift()));

	      segments.push(ringSegments.filter(validSegment));
	    }

	    return clip;
	  };
	}

	function validSegment(segment) {
	  return segment.length > 1;
	}

	// Intersections are sorted along the clip edge. For both antimeridian cutting
	// and circle clipping, the same comparison is used.
	function compareIntersection(a, b) {
	  return ((a = a.x)[0] < 0 ? a[1] - halfPi$2 - epsilon$2 : halfPi$2 - a[1])
	       - ((b = b.x)[0] < 0 ? b[1] - halfPi$2 - epsilon$2 : halfPi$2 - b[1]);
	}

	var clipAntimeridian = clip(
	  function() { return true; },
	  clipAntimeridianLine,
	  clipAntimeridianInterpolate,
	  [-pi$3, -halfPi$2]
	);

	// Takes a line and cuts into visible segments. Return values: 0 - there were
	// intersections or the line was empty; 1 - no intersections; 2 - there were
	// intersections, and the first and last segments should be rejoined.
	function clipAntimeridianLine(stream) {
	  var lambda0 = NaN,
	      phi0 = NaN,
	      sign0 = NaN,
	      clean; // no intersections

	  return {
	    lineStart: function() {
	      stream.lineStart();
	      clean = 1;
	    },
	    point: function(lambda1, phi1) {
	      var sign1 = lambda1 > 0 ? pi$3 : -pi$3,
	          delta = abs(lambda1 - lambda0);
	      if (abs(delta - pi$3) < epsilon$2) { // line crosses a pole
	        stream.point(lambda0, phi0 = (phi0 + phi1) / 2 > 0 ? halfPi$2 : -halfPi$2);
	        stream.point(sign0, phi0);
	        stream.lineEnd();
	        stream.lineStart();
	        stream.point(sign1, phi0);
	        stream.point(lambda1, phi0);
	        clean = 0;
	      } else if (sign0 !== sign1 && delta >= pi$3) { // line crosses antimeridian
	        if (abs(lambda0 - sign0) < epsilon$2) lambda0 -= sign0 * epsilon$2; // handle degeneracies
	        if (abs(lambda1 - sign1) < epsilon$2) lambda1 -= sign1 * epsilon$2;
	        phi0 = clipAntimeridianIntersect(lambda0, phi0, lambda1, phi1);
	        stream.point(sign0, phi0);
	        stream.lineEnd();
	        stream.lineStart();
	        stream.point(sign1, phi0);
	        clean = 0;
	      }
	      stream.point(lambda0 = lambda1, phi0 = phi1);
	      sign0 = sign1;
	    },
	    lineEnd: function() {
	      stream.lineEnd();
	      lambda0 = phi0 = NaN;
	    },
	    clean: function() {
	      return 2 - clean; // if intersections, rejoin first and last segments
	    }
	  };
	}

	function clipAntimeridianIntersect(lambda0, phi0, lambda1, phi1) {
	  var cosPhi0,
	      cosPhi1,
	      sinLambda0Lambda1 = sin$1(lambda0 - lambda1);
	  return abs(sinLambda0Lambda1) > epsilon$2
	      ? atan((sin$1(phi0) * (cosPhi1 = cos$1(phi1)) * sin$1(lambda1)
	          - sin$1(phi1) * (cosPhi0 = cos$1(phi0)) * sin$1(lambda0))
	          / (cosPhi0 * cosPhi1 * sinLambda0Lambda1))
	      : (phi0 + phi1) / 2;
	}

	function clipAntimeridianInterpolate(from, to, direction, stream) {
	  var phi;
	  if (from == null) {
	    phi = direction * halfPi$2;
	    stream.point(-pi$3, phi);
	    stream.point(0, phi);
	    stream.point(pi$3, phi);
	    stream.point(pi$3, 0);
	    stream.point(pi$3, -phi);
	    stream.point(0, -phi);
	    stream.point(-pi$3, -phi);
	    stream.point(-pi$3, 0);
	    stream.point(-pi$3, phi);
	  } else if (abs(from[0] - to[0]) > epsilon$2) {
	    var lambda = from[0] < to[0] ? pi$3 : -pi$3;
	    phi = direction * lambda / 2;
	    stream.point(-lambda, phi);
	    stream.point(0, phi);
	    stream.point(lambda, phi);
	  } else {
	    stream.point(to[0], to[1]);
	  }
	}

	function clipCircle(radius) {
	  var cr = cos$1(radius),
	      delta = 6 * radians,
	      smallRadius = cr > 0,
	      notHemisphere = abs(cr) > epsilon$2; // TODO optimise for this common case

	  function interpolate(from, to, direction, stream) {
	    circleStream(stream, radius, delta, direction, from, to);
	  }

	  function visible(lambda, phi) {
	    return cos$1(lambda) * cos$1(phi) > cr;
	  }

	  // Takes a line and cuts into visible segments. Return values used for polygon
	  // clipping: 0 - there were intersections or the line was empty; 1 - no
	  // intersections 2 - there were intersections, and the first and last segments
	  // should be rejoined.
	  function clipLine(stream) {
	    var point0, // previous point
	        c0, // code for previous point
	        v0, // visibility of previous point
	        v00, // visibility of first point
	        clean; // no intersections
	    return {
	      lineStart: function() {
	        v00 = v0 = false;
	        clean = 1;
	      },
	      point: function(lambda, phi) {
	        var point1 = [lambda, phi],
	            point2,
	            v = visible(lambda, phi),
	            c = smallRadius
	              ? v ? 0 : code(lambda, phi)
	              : v ? code(lambda + (lambda < 0 ? pi$3 : -pi$3), phi) : 0;
	        if (!point0 && (v00 = v0 = v)) stream.lineStart();
	        // Handle degeneracies.
	        // TODO ignore if not clipping polygons.
	        if (v !== v0) {
	          point2 = intersect(point0, point1);
	          if (!point2 || pointEqual(point0, point2) || pointEqual(point1, point2)) {
	            point1[0] += epsilon$2;
	            point1[1] += epsilon$2;
	            v = visible(point1[0], point1[1]);
	          }
	        }
	        if (v !== v0) {
	          clean = 0;
	          if (v) {
	            // outside going in
	            stream.lineStart();
	            point2 = intersect(point1, point0);
	            stream.point(point2[0], point2[1]);
	          } else {
	            // inside going out
	            point2 = intersect(point0, point1);
	            stream.point(point2[0], point2[1]);
	            stream.lineEnd();
	          }
	          point0 = point2;
	        } else if (notHemisphere && point0 && smallRadius ^ v) {
	          var t;
	          // If the codes for two points are different, or are both zero,
	          // and there this segment intersects with the small circle.
	          if (!(c & c0) && (t = intersect(point1, point0, true))) {
	            clean = 0;
	            if (smallRadius) {
	              stream.lineStart();
	              stream.point(t[0][0], t[0][1]);
	              stream.point(t[1][0], t[1][1]);
	              stream.lineEnd();
	            } else {
	              stream.point(t[1][0], t[1][1]);
	              stream.lineEnd();
	              stream.lineStart();
	              stream.point(t[0][0], t[0][1]);
	            }
	          }
	        }
	        if (v && (!point0 || !pointEqual(point0, point1))) {
	          stream.point(point1[0], point1[1]);
	        }
	        point0 = point1, v0 = v, c0 = c;
	      },
	      lineEnd: function() {
	        if (v0) stream.lineEnd();
	        point0 = null;
	      },
	      // Rejoin first and last segments if there were intersections and the first
	      // and last points were visible.
	      clean: function() {
	        return clean | ((v00 && v0) << 1);
	      }
	    };
	  }

	  // Intersects the great circle between a and b with the clip circle.
	  function intersect(a, b, two) {
	    var pa = cartesian(a),
	        pb = cartesian(b);

	    // We have two planes, n1.p = d1 and n2.p = d2.
	    // Find intersection line p(t) = c1 n1 + c2 n2 + t (n1 ⨯ n2).
	    var n1 = [1, 0, 0], // normal
	        n2 = cartesianCross(pa, pb),
	        n2n2 = cartesianDot(n2, n2),
	        n1n2 = n2[0], // cartesianDot(n1, n2),
	        determinant = n2n2 - n1n2 * n1n2;

	    // Two polar points.
	    if (!determinant) return !two && a;

	    var c1 =  cr * n2n2 / determinant,
	        c2 = -cr * n1n2 / determinant,
	        n1xn2 = cartesianCross(n1, n2),
	        A = cartesianScale(n1, c1),
	        B = cartesianScale(n2, c2);
	    cartesianAddInPlace(A, B);

	    // Solve |p(t)|^2 = 1.
	    var u = n1xn2,
	        w = cartesianDot(A, u),
	        uu = cartesianDot(u, u),
	        t2 = w * w - uu * (cartesianDot(A, A) - 1);

	    if (t2 < 0) return;

	    var t = sqrt(t2),
	        q = cartesianScale(u, (-w - t) / uu);
	    cartesianAddInPlace(q, A);
	    q = spherical(q);

	    if (!two) return q;

	    // Two intersection points.
	    var lambda0 = a[0],
	        lambda1 = b[0],
	        phi0 = a[1],
	        phi1 = b[1],
	        z;

	    if (lambda1 < lambda0) z = lambda0, lambda0 = lambda1, lambda1 = z;

	    var delta = lambda1 - lambda0,
	        polar = abs(delta - pi$3) < epsilon$2,
	        meridian = polar || delta < epsilon$2;

	    if (!polar && phi1 < phi0) z = phi0, phi0 = phi1, phi1 = z;

	    // Check that the first point is between a and b.
	    if (meridian
	        ? polar
	          ? phi0 + phi1 > 0 ^ q[1] < (abs(q[0] - lambda0) < epsilon$2 ? phi0 : phi1)
	          : phi0 <= q[1] && q[1] <= phi1
	        : delta > pi$3 ^ (lambda0 <= q[0] && q[0] <= lambda1)) {
	      var q1 = cartesianScale(u, (-w + t) / uu);
	      cartesianAddInPlace(q1, A);
	      return [q, spherical(q1)];
	    }
	  }

	  // Generates a 4-bit vector representing the location of a point relative to
	  // the small circle's bounding box.
	  function code(lambda, phi) {
	    var r = smallRadius ? radius : pi$3 - radius,
	        code = 0;
	    if (lambda < -r) code |= 1; // left
	    else if (lambda > r) code |= 2; // right
	    if (phi < -r) code |= 4; // below
	    else if (phi > r) code |= 8; // above
	    return code;
	  }

	  return clip(visible, clipLine, interpolate, smallRadius ? [0, -radius] : [-pi$3, radius - pi$3]);
	}

	function clipLine(a, b, x0, y0, x1, y1) {
	  var ax = a[0],
	      ay = a[1],
	      bx = b[0],
	      by = b[1],
	      t0 = 0,
	      t1 = 1,
	      dx = bx - ax,
	      dy = by - ay,
	      r;

	  r = x0 - ax;
	  if (!dx && r > 0) return;
	  r /= dx;
	  if (dx < 0) {
	    if (r < t0) return;
	    if (r < t1) t1 = r;
	  } else if (dx > 0) {
	    if (r > t1) return;
	    if (r > t0) t0 = r;
	  }

	  r = x1 - ax;
	  if (!dx && r < 0) return;
	  r /= dx;
	  if (dx < 0) {
	    if (r > t1) return;
	    if (r > t0) t0 = r;
	  } else if (dx > 0) {
	    if (r < t0) return;
	    if (r < t1) t1 = r;
	  }

	  r = y0 - ay;
	  if (!dy && r > 0) return;
	  r /= dy;
	  if (dy < 0) {
	    if (r < t0) return;
	    if (r < t1) t1 = r;
	  } else if (dy > 0) {
	    if (r > t1) return;
	    if (r > t0) t0 = r;
	  }

	  r = y1 - ay;
	  if (!dy && r < 0) return;
	  r /= dy;
	  if (dy < 0) {
	    if (r > t1) return;
	    if (r > t0) t0 = r;
	  } else if (dy > 0) {
	    if (r < t0) return;
	    if (r < t1) t1 = r;
	  }

	  if (t0 > 0) a[0] = ax + t0 * dx, a[1] = ay + t0 * dy;
	  if (t1 < 1) b[0] = ax + t1 * dx, b[1] = ay + t1 * dy;
	  return true;
	}

	var clipMax = 1e9;
	var clipMin = -clipMax;

	// TODO Use d3-polygon’s polygonContains here for the ring check?
	// TODO Eliminate duplicate buffering in clipBuffer and polygon.push?

	function clipRectangle(x0, y0, x1, y1) {

	  function visible(x, y) {
	    return x0 <= x && x <= x1 && y0 <= y && y <= y1;
	  }

	  function interpolate(from, to, direction, stream) {
	    var a = 0, a1 = 0;
	    if (from == null
	        || (a = corner(from, direction)) !== (a1 = corner(to, direction))
	        || comparePoint(from, to) < 0 ^ direction > 0) {
	      do stream.point(a === 0 || a === 3 ? x0 : x1, a > 1 ? y1 : y0);
	      while ((a = (a + direction + 4) % 4) !== a1);
	    } else {
	      stream.point(to[0], to[1]);
	    }
	  }

	  function corner(p, direction) {
	    return abs(p[0] - x0) < epsilon$2 ? direction > 0 ? 0 : 3
	        : abs(p[0] - x1) < epsilon$2 ? direction > 0 ? 2 : 1
	        : abs(p[1] - y0) < epsilon$2 ? direction > 0 ? 1 : 0
	        : direction > 0 ? 3 : 2; // abs(p[1] - y1) < epsilon
	  }

	  function compareIntersection(a, b) {
	    return comparePoint(a.x, b.x);
	  }

	  function comparePoint(a, b) {
	    var ca = corner(a, 1),
	        cb = corner(b, 1);
	    return ca !== cb ? ca - cb
	        : ca === 0 ? b[1] - a[1]
	        : ca === 1 ? a[0] - b[0]
	        : ca === 2 ? a[1] - b[1]
	        : b[0] - a[0];
	  }

	  return function(stream) {
	    var activeStream = stream,
	        bufferStream = clipBuffer(),
	        segments,
	        polygon,
	        ring,
	        x__, y__, v__, // first point
	        x_, y_, v_, // previous point
	        first,
	        clean;

	    var clipStream = {
	      point: point,
	      lineStart: lineStart,
	      lineEnd: lineEnd,
	      polygonStart: polygonStart,
	      polygonEnd: polygonEnd
	    };

	    function point(x, y) {
	      if (visible(x, y)) activeStream.point(x, y);
	    }

	    function polygonInside() {
	      var winding = 0;

	      for (var i = 0, n = polygon.length; i < n; ++i) {
	        for (var ring = polygon[i], j = 1, m = ring.length, point = ring[0], a0, a1, b0 = point[0], b1 = point[1]; j < m; ++j) {
	          a0 = b0, a1 = b1, point = ring[j], b0 = point[0], b1 = point[1];
	          if (a1 <= y1) { if (b1 > y1 && (b0 - a0) * (y1 - a1) > (b1 - a1) * (x0 - a0)) ++winding; }
	          else { if (b1 <= y1 && (b0 - a0) * (y1 - a1) < (b1 - a1) * (x0 - a0)) --winding; }
	        }
	      }

	      return winding;
	    }

	    // Buffer geometry within a polygon and then clip it en masse.
	    function polygonStart() {
	      activeStream = bufferStream, segments = [], polygon = [], clean = true;
	    }

	    function polygonEnd() {
	      var startInside = polygonInside(),
	          cleanInside = clean && startInside,
	          visible = (segments = merge(segments)).length;
	      if (cleanInside || visible) {
	        stream.polygonStart();
	        if (cleanInside) {
	          stream.lineStart();
	          interpolate(null, null, 1, stream);
	          stream.lineEnd();
	        }
	        if (visible) {
	          clipRejoin(segments, compareIntersection, startInside, interpolate, stream);
	        }
	        stream.polygonEnd();
	      }
	      activeStream = stream, segments = polygon = ring = null;
	    }

	    function lineStart() {
	      clipStream.point = linePoint;
	      if (polygon) polygon.push(ring = []);
	      first = true;
	      v_ = false;
	      x_ = y_ = NaN;
	    }

	    // TODO rather than special-case polygons, simply handle them separately.
	    // Ideally, coincident intersection points should be jittered to avoid
	    // clipping issues.
	    function lineEnd() {
	      if (segments) {
	        linePoint(x__, y__);
	        if (v__ && v_) bufferStream.rejoin();
	        segments.push(bufferStream.result());
	      }
	      clipStream.point = point;
	      if (v_) activeStream.lineEnd();
	    }

	    function linePoint(x, y) {
	      var v = visible(x, y);
	      if (polygon) ring.push([x, y]);
	      if (first) {
	        x__ = x, y__ = y, v__ = v;
	        first = false;
	        if (v) {
	          activeStream.lineStart();
	          activeStream.point(x, y);
	        }
	      } else {
	        if (v && v_) activeStream.point(x, y);
	        else {
	          var a = [x_ = Math.max(clipMin, Math.min(clipMax, x_)), y_ = Math.max(clipMin, Math.min(clipMax, y_))],
	              b = [x = Math.max(clipMin, Math.min(clipMax, x)), y = Math.max(clipMin, Math.min(clipMax, y))];
	          if (clipLine(a, b, x0, y0, x1, y1)) {
	            if (!v_) {
	              activeStream.lineStart();
	              activeStream.point(a[0], a[1]);
	            }
	            activeStream.point(b[0], b[1]);
	            if (!v) activeStream.lineEnd();
	            clean = false;
	          } else if (v) {
	            activeStream.lineStart();
	            activeStream.point(x, y);
	            clean = false;
	          }
	        }
	      }
	      x_ = x, y_ = y, v_ = v;
	    }

	    return clipStream;
	  };
	}

	function extent$1() {
	  var x0 = 0,
	      y0 = 0,
	      x1 = 960,
	      y1 = 500,
	      cache,
	      cacheStream,
	      clip;

	  return clip = {
	    stream: function(stream) {
	      return cache && cacheStream === stream ? cache : cache = clipRectangle(x0, y0, x1, y1)(cacheStream = stream);
	    },
	    extent: function(_) {
	      return arguments.length ? (x0 = +_[0][0], y0 = +_[0][1], x1 = +_[1][0], y1 = +_[1][1], cache = cacheStream = null, clip) : [[x0, y0], [x1, y1]];
	    }
	  };
	}

	var lengthSum = adder();
	var lambda0$2;
	var sinPhi0$1;
	var cosPhi0$1;

	var lengthStream = {
	  sphere: noop$1,
	  point: noop$1,
	  lineStart: lengthLineStart,
	  lineEnd: noop$1,
	  polygonStart: noop$1,
	  polygonEnd: noop$1
	};

	function lengthLineStart() {
	  lengthStream.point = lengthPointFirst;
	  lengthStream.lineEnd = lengthLineEnd;
	}

	function lengthLineEnd() {
	  lengthStream.point = lengthStream.lineEnd = noop$1;
	}

	function lengthPointFirst(lambda, phi) {
	  lambda *= radians, phi *= radians;
	  lambda0$2 = lambda, sinPhi0$1 = sin$1(phi), cosPhi0$1 = cos$1(phi);
	  lengthStream.point = lengthPoint;
	}

	function lengthPoint(lambda, phi) {
	  lambda *= radians, phi *= radians;
	  var sinPhi = sin$1(phi),
	      cosPhi = cos$1(phi),
	      delta = abs(lambda - lambda0$2),
	      cosDelta = cos$1(delta),
	      sinDelta = sin$1(delta),
	      x = cosPhi * sinDelta,
	      y = cosPhi0$1 * sinPhi - sinPhi0$1 * cosPhi * cosDelta,
	      z = sinPhi0$1 * sinPhi + cosPhi0$1 * cosPhi * cosDelta;
	  lengthSum.add(atan2(sqrt(x * x + y * y), z));
	  lambda0$2 = lambda, sinPhi0$1 = sinPhi, cosPhi0$1 = cosPhi;
	}

	function length$1(object) {
	  lengthSum.reset();
	  geoStream(object, lengthStream);
	  return +lengthSum;
	}

	var coordinates = [null, null];
	var object$1 = {type: "LineString", coordinates: coordinates};

	function distance(a, b) {
	  coordinates[0] = a;
	  coordinates[1] = b;
	  return length$1(object$1);
	}

	var containsObjectType = {
	  Feature: function(object, point) {
	    return containsGeometry(object.geometry, point);
	  },
	  FeatureCollection: function(object, point) {
	    var features = object.features, i = -1, n = features.length;
	    while (++i < n) if (containsGeometry(features[i].geometry, point)) return true;
	    return false;
	  }
	};

	var containsGeometryType = {
	  Sphere: function() {
	    return true;
	  },
	  Point: function(object, point) {
	    return containsPoint(object.coordinates, point);
	  },
	  MultiPoint: function(object, point) {
	    var coordinates = object.coordinates, i = -1, n = coordinates.length;
	    while (++i < n) if (containsPoint(coordinates[i], point)) return true;
	    return false;
	  },
	  LineString: function(object, point) {
	    return containsLine(object.coordinates, point);
	  },
	  MultiLineString: function(object, point) {
	    var coordinates = object.coordinates, i = -1, n = coordinates.length;
	    while (++i < n) if (containsLine(coordinates[i], point)) return true;
	    return false;
	  },
	  Polygon: function(object, point) {
	    return containsPolygon(object.coordinates, point);
	  },
	  MultiPolygon: function(object, point) {
	    var coordinates = object.coordinates, i = -1, n = coordinates.length;
	    while (++i < n) if (containsPolygon(coordinates[i], point)) return true;
	    return false;
	  },
	  GeometryCollection: function(object, point) {
	    var geometries = object.geometries, i = -1, n = geometries.length;
	    while (++i < n) if (containsGeometry(geometries[i], point)) return true;
	    return false;
	  }
	};

	function containsGeometry(geometry, point) {
	  return geometry && containsGeometryType.hasOwnProperty(geometry.type)
	      ? containsGeometryType[geometry.type](geometry, point)
	      : false;
	}

	function containsPoint(coordinates, point) {
	  return distance(coordinates, point) === 0;
	}

	function containsLine(coordinates, point) {
	  var ab = distance(coordinates[0], coordinates[1]),
	      ao = distance(coordinates[0], point),
	      ob = distance(point, coordinates[1]);
	  return ao + ob <= ab + epsilon$2;
	}

	function containsPolygon(coordinates, point) {
	  return !!polygonContains(coordinates.map(ringRadians), pointRadians(point));
	}

	function ringRadians(ring) {
	  return ring = ring.map(pointRadians), ring.pop(), ring;
	}

	function pointRadians(point) {
	  return [point[0] * radians, point[1] * radians];
	}

	function contains(object, point) {
	  return (object && containsObjectType.hasOwnProperty(object.type)
	      ? containsObjectType[object.type]
	      : containsGeometry)(object, point);
	}

	function graticuleX(y0, y1, dy) {
	  var y = sequence(y0, y1 - epsilon$2, dy).concat(y1);
	  return function(x) { return y.map(function(y) { return [x, y]; }); };
	}

	function graticuleY(x0, x1, dx) {
	  var x = sequence(x0, x1 - epsilon$2, dx).concat(x1);
	  return function(y) { return x.map(function(x) { return [x, y]; }); };
	}

	function graticule() {
	  var x1, x0, X1, X0,
	      y1, y0, Y1, Y0,
	      dx = 10, dy = dx, DX = 90, DY = 360,
	      x, y, X, Y,
	      precision = 2.5;

	  function graticule() {
	    return {type: "MultiLineString", coordinates: lines()};
	  }

	  function lines() {
	    return sequence(ceil(X0 / DX) * DX, X1, DX).map(X)
	        .concat(sequence(ceil(Y0 / DY) * DY, Y1, DY).map(Y))
	        .concat(sequence(ceil(x0 / dx) * dx, x1, dx).filter(function(x) { return abs(x % DX) > epsilon$2; }).map(x))
	        .concat(sequence(ceil(y0 / dy) * dy, y1, dy).filter(function(y) { return abs(y % DY) > epsilon$2; }).map(y));
	  }

	  graticule.lines = function() {
	    return lines().map(function(coordinates) { return {type: "LineString", coordinates: coordinates}; });
	  };

	  graticule.outline = function() {
	    return {
	      type: "Polygon",
	      coordinates: [
	        X(X0).concat(
	        Y(Y1).slice(1),
	        X(X1).reverse().slice(1),
	        Y(Y0).reverse().slice(1))
	      ]
	    };
	  };

	  graticule.extent = function(_) {
	    if (!arguments.length) return graticule.extentMinor();
	    return graticule.extentMajor(_).extentMinor(_);
	  };

	  graticule.extentMajor = function(_) {
	    if (!arguments.length) return [[X0, Y0], [X1, Y1]];
	    X0 = +_[0][0], X1 = +_[1][0];
	    Y0 = +_[0][1], Y1 = +_[1][1];
	    if (X0 > X1) _ = X0, X0 = X1, X1 = _;
	    if (Y0 > Y1) _ = Y0, Y0 = Y1, Y1 = _;
	    return graticule.precision(precision);
	  };

	  graticule.extentMinor = function(_) {
	    if (!arguments.length) return [[x0, y0], [x1, y1]];
	    x0 = +_[0][0], x1 = +_[1][0];
	    y0 = +_[0][1], y1 = +_[1][1];
	    if (x0 > x1) _ = x0, x0 = x1, x1 = _;
	    if (y0 > y1) _ = y0, y0 = y1, y1 = _;
	    return graticule.precision(precision);
	  };

	  graticule.step = function(_) {
	    if (!arguments.length) return graticule.stepMinor();
	    return graticule.stepMajor(_).stepMinor(_);
	  };

	  graticule.stepMajor = function(_) {
	    if (!arguments.length) return [DX, DY];
	    DX = +_[0], DY = +_[1];
	    return graticule;
	  };

	  graticule.stepMinor = function(_) {
	    if (!arguments.length) return [dx, dy];
	    dx = +_[0], dy = +_[1];
	    return graticule;
	  };

	  graticule.precision = function(_) {
	    if (!arguments.length) return precision;
	    precision = +_;
	    x = graticuleX(y0, y1, 90);
	    y = graticuleY(x0, x1, precision);
	    X = graticuleX(Y0, Y1, 90);
	    Y = graticuleY(X0, X1, precision);
	    return graticule;
	  };

	  return graticule
	      .extentMajor([[-180, -90 + epsilon$2], [180, 90 - epsilon$2]])
	      .extentMinor([[-180, -80 - epsilon$2], [180, 80 + epsilon$2]]);
	}

	function graticule10() {
	  return graticule()();
	}

	function interpolate$1(a, b) {
	  var x0 = a[0] * radians,
	      y0 = a[1] * radians,
	      x1 = b[0] * radians,
	      y1 = b[1] * radians,
	      cy0 = cos$1(y0),
	      sy0 = sin$1(y0),
	      cy1 = cos$1(y1),
	      sy1 = sin$1(y1),
	      kx0 = cy0 * cos$1(x0),
	      ky0 = cy0 * sin$1(x0),
	      kx1 = cy1 * cos$1(x1),
	      ky1 = cy1 * sin$1(x1),
	      d = 2 * asin(sqrt(haversin(y1 - y0) + cy0 * cy1 * haversin(x1 - x0))),
	      k = sin$1(d);

	  var interpolate = d ? function(t) {
	    var B = sin$1(t *= d) / k,
	        A = sin$1(d - t) / k,
	        x = A * kx0 + B * kx1,
	        y = A * ky0 + B * ky1,
	        z = A * sy0 + B * sy1;
	    return [
	      atan2(y, x) * degrees$1,
	      atan2(z, sqrt(x * x + y * y)) * degrees$1
	    ];
	  } : function() {
	    return [x0 * degrees$1, y0 * degrees$1];
	  };

	  interpolate.distance = d;

	  return interpolate;
	}

	function identity$4(x) {
	  return x;
	}

	var areaSum$1 = adder();
	var areaRingSum$1 = adder();
	var x00;
	var y00;
	var x0$1;
	var y0$1;

	var areaStream$1 = {
	  point: noop$1,
	  lineStart: noop$1,
	  lineEnd: noop$1,
	  polygonStart: function() {
	    areaStream$1.lineStart = areaRingStart$1;
	    areaStream$1.lineEnd = areaRingEnd$1;
	  },
	  polygonEnd: function() {
	    areaStream$1.lineStart = areaStream$1.lineEnd = areaStream$1.point = noop$1;
	    areaSum$1.add(abs(areaRingSum$1));
	    areaRingSum$1.reset();
	  },
	  result: function() {
	    var area = areaSum$1 / 2;
	    areaSum$1.reset();
	    return area;
	  }
	};

	function areaRingStart$1() {
	  areaStream$1.point = areaPointFirst$1;
	}

	function areaPointFirst$1(x, y) {
	  areaStream$1.point = areaPoint$1;
	  x00 = x0$1 = x, y00 = y0$1 = y;
	}

	function areaPoint$1(x, y) {
	  areaRingSum$1.add(y0$1 * x - x0$1 * y);
	  x0$1 = x, y0$1 = y;
	}

	function areaRingEnd$1() {
	  areaPoint$1(x00, y00);
	}

	var x0$2 = Infinity;
	var y0$2 = x0$2;
	var x1 = -x0$2;
	var y1 = x1;

	var boundsStream$1 = {
	  point: boundsPoint$1,
	  lineStart: noop$1,
	  lineEnd: noop$1,
	  polygonStart: noop$1,
	  polygonEnd: noop$1,
	  result: function() {
	    var bounds = [[x0$2, y0$2], [x1, y1]];
	    x1 = y1 = -(y0$2 = x0$2 = Infinity);
	    return bounds;
	  }
	};

	function boundsPoint$1(x, y) {
	  if (x < x0$2) x0$2 = x;
	  if (x > x1) x1 = x;
	  if (y < y0$2) y0$2 = y;
	  if (y > y1) y1 = y;
	}

	// TODO Enforce positive area for exterior, negative area for interior?

	var X0$1 = 0;
	var Y0$1 = 0;
	var Z0$1 = 0;
	var X1$1 = 0;
	var Y1$1 = 0;
	var Z1$1 = 0;
	var X2$1 = 0;
	var Y2$1 = 0;
	var Z2$1 = 0;
	var x00$1;
	var y00$1;
	var x0$3;
	var y0$3;

	var centroidStream$1 = {
	  point: centroidPoint$1,
	  lineStart: centroidLineStart$1,
	  lineEnd: centroidLineEnd$1,
	  polygonStart: function() {
	    centroidStream$1.lineStart = centroidRingStart$1;
	    centroidStream$1.lineEnd = centroidRingEnd$1;
	  },
	  polygonEnd: function() {
	    centroidStream$1.point = centroidPoint$1;
	    centroidStream$1.lineStart = centroidLineStart$1;
	    centroidStream$1.lineEnd = centroidLineEnd$1;
	  },
	  result: function() {
	    var centroid = Z2$1 ? [X2$1 / Z2$1, Y2$1 / Z2$1]
	        : Z1$1 ? [X1$1 / Z1$1, Y1$1 / Z1$1]
	        : Z0$1 ? [X0$1 / Z0$1, Y0$1 / Z0$1]
	        : [NaN, NaN];
	    X0$1 = Y0$1 = Z0$1 =
	    X1$1 = Y1$1 = Z1$1 =
	    X2$1 = Y2$1 = Z2$1 = 0;
	    return centroid;
	  }
	};

	function centroidPoint$1(x, y) {
	  X0$1 += x;
	  Y0$1 += y;
	  ++Z0$1;
	}

	function centroidLineStart$1() {
	  centroidStream$1.point = centroidPointFirstLine;
	}

	function centroidPointFirstLine(x, y) {
	  centroidStream$1.point = centroidPointLine;
	  centroidPoint$1(x0$3 = x, y0$3 = y);
	}

	function centroidPointLine(x, y) {
	  var dx = x - x0$3, dy = y - y0$3, z = sqrt(dx * dx + dy * dy);
	  X1$1 += z * (x0$3 + x) / 2;
	  Y1$1 += z * (y0$3 + y) / 2;
	  Z1$1 += z;
	  centroidPoint$1(x0$3 = x, y0$3 = y);
	}

	function centroidLineEnd$1() {
	  centroidStream$1.point = centroidPoint$1;
	}

	function centroidRingStart$1() {
	  centroidStream$1.point = centroidPointFirstRing;
	}

	function centroidRingEnd$1() {
	  centroidPointRing(x00$1, y00$1);
	}

	function centroidPointFirstRing(x, y) {
	  centroidStream$1.point = centroidPointRing;
	  centroidPoint$1(x00$1 = x0$3 = x, y00$1 = y0$3 = y);
	}

	function centroidPointRing(x, y) {
	  var dx = x - x0$3,
	      dy = y - y0$3,
	      z = sqrt(dx * dx + dy * dy);

	  X1$1 += z * (x0$3 + x) / 2;
	  Y1$1 += z * (y0$3 + y) / 2;
	  Z1$1 += z;

	  z = y0$3 * x - x0$3 * y;
	  X2$1 += z * (x0$3 + x);
	  Y2$1 += z * (y0$3 + y);
	  Z2$1 += z * 3;
	  centroidPoint$1(x0$3 = x, y0$3 = y);
	}

	function PathContext(context) {
	  this._context = context;
	}

	PathContext.prototype = {
	  _radius: 4.5,
	  pointRadius: function(_) {
	    return this._radius = _, this;
	  },
	  polygonStart: function() {
	    this._line = 0;
	  },
	  polygonEnd: function() {
	    this._line = NaN;
	  },
	  lineStart: function() {
	    this._point = 0;
	  },
	  lineEnd: function() {
	    if (this._line === 0) this._context.closePath();
	    this._point = NaN;
	  },
	  point: function(x, y) {
	    switch (this._point) {
	      case 0: {
	        this._context.moveTo(x, y);
	        this._point = 1;
	        break;
	      }
	      case 1: {
	        this._context.lineTo(x, y);
	        break;
	      }
	      default: {
	        this._context.moveTo(x + this._radius, y);
	        this._context.arc(x, y, this._radius, 0, tau$3);
	        break;
	      }
	    }
	  },
	  result: noop$1
	};

	var lengthSum$1 = adder();
	var lengthRing;
	var x00$2;
	var y00$2;
	var x0$4;
	var y0$4;

	var lengthStream$1 = {
	  point: noop$1,
	  lineStart: function() {
	    lengthStream$1.point = lengthPointFirst$1;
	  },
	  lineEnd: function() {
	    if (lengthRing) lengthPoint$1(x00$2, y00$2);
	    lengthStream$1.point = noop$1;
	  },
	  polygonStart: function() {
	    lengthRing = true;
	  },
	  polygonEnd: function() {
	    lengthRing = null;
	  },
	  result: function() {
	    var length = +lengthSum$1;
	    lengthSum$1.reset();
	    return length;
	  }
	};

	function lengthPointFirst$1(x, y) {
	  lengthStream$1.point = lengthPoint$1;
	  x00$2 = x0$4 = x, y00$2 = y0$4 = y;
	}

	function lengthPoint$1(x, y) {
	  x0$4 -= x, y0$4 -= y;
	  lengthSum$1.add(sqrt(x0$4 * x0$4 + y0$4 * y0$4));
	  x0$4 = x, y0$4 = y;
	}

	function PathString() {
	  this._string = [];
	}

	PathString.prototype = {
	  _radius: 4.5,
	  _circle: circle$1(4.5),
	  pointRadius: function(_) {
	    if ((_ = +_) !== this._radius) this._radius = _, this._circle = null;
	    return this;
	  },
	  polygonStart: function() {
	    this._line = 0;
	  },
	  polygonEnd: function() {
	    this._line = NaN;
	  },
	  lineStart: function() {
	    this._point = 0;
	  },
	  lineEnd: function() {
	    if (this._line === 0) this._string.push("Z");
	    this._point = NaN;
	  },
	  point: function(x, y) {
	    switch (this._point) {
	      case 0: {
	        this._string.push("M", x, ",", y);
	        this._point = 1;
	        break;
	      }
	      case 1: {
	        this._string.push("L", x, ",", y);
	        break;
	      }
	      default: {
	        if (this._circle == null) this._circle = circle$1(this._radius);
	        this._string.push("M", x, ",", y, this._circle);
	        break;
	      }
	    }
	  },
	  result: function() {
	    if (this._string.length) {
	      var result = this._string.join("");
	      this._string = [];
	      return result;
	    } else {
	      return null;
	    }
	  }
	};

	function circle$1(radius) {
	  return "m0," + radius
	      + "a" + radius + "," + radius + " 0 1,1 0," + -2 * radius
	      + "a" + radius + "," + radius + " 0 1,1 0," + 2 * radius
	      + "z";
	}

	function index$1(projection, context) {
	  var pointRadius = 4.5,
	      projectionStream,
	      contextStream;

	  function path(object) {
	    if (object) {
	      if (typeof pointRadius === "function") contextStream.pointRadius(+pointRadius.apply(this, arguments));
	      geoStream(object, projectionStream(contextStream));
	    }
	    return contextStream.result();
	  }

	  path.area = function(object) {
	    geoStream(object, projectionStream(areaStream$1));
	    return areaStream$1.result();
	  };

	  path.measure = function(object) {
	    geoStream(object, projectionStream(lengthStream$1));
	    return lengthStream$1.result();
	  };

	  path.bounds = function(object) {
	    geoStream(object, projectionStream(boundsStream$1));
	    return boundsStream$1.result();
	  };

	  path.centroid = function(object) {
	    geoStream(object, projectionStream(centroidStream$1));
	    return centroidStream$1.result();
	  };

	  path.projection = function(_) {
	    return arguments.length ? (projectionStream = _ == null ? (projection = null, identity$4) : (projection = _).stream, path) : projection;
	  };

	  path.context = function(_) {
	    if (!arguments.length) return context;
	    contextStream = _ == null ? (context = null, new PathString) : new PathContext(context = _);
	    if (typeof pointRadius !== "function") contextStream.pointRadius(pointRadius);
	    return path;
	  };

	  path.pointRadius = function(_) {
	    if (!arguments.length) return pointRadius;
	    pointRadius = typeof _ === "function" ? _ : (contextStream.pointRadius(+_), +_);
	    return path;
	  };

	  return path.projection(projection).context(context);
	}

	function transform(methods) {
	  return {
	    stream: transformer(methods)
	  };
	}

	function transformer(methods) {
	  return function(stream) {
	    var s = new TransformStream;
	    for (var key in methods) s[key] = methods[key];
	    s.stream = stream;
	    return s;
	  };
	}

	function TransformStream() {}

	TransformStream.prototype = {
	  constructor: TransformStream,
	  point: function(x, y) { this.stream.point(x, y); },
	  sphere: function() { this.stream.sphere(); },
	  lineStart: function() { this.stream.lineStart(); },
	  lineEnd: function() { this.stream.lineEnd(); },
	  polygonStart: function() { this.stream.polygonStart(); },
	  polygonEnd: function() { this.stream.polygonEnd(); }
	};

	function fit(projection, fitBounds, object) {
	  var clip = projection.clipExtent && projection.clipExtent();
	  projection.scale(150).translate([0, 0]);
	  if (clip != null) projection.clipExtent(null);
	  geoStream(object, projection.stream(boundsStream$1));
	  fitBounds(boundsStream$1.result());
	  if (clip != null) projection.clipExtent(clip);
	  return projection;
	}

	function fitExtent(projection, extent, object) {
	  return fit(projection, function(b) {
	    var w = extent[1][0] - extent[0][0],
	        h = extent[1][1] - extent[0][1],
	        k = Math.min(w / (b[1][0] - b[0][0]), h / (b[1][1] - b[0][1])),
	        x = +extent[0][0] + (w - k * (b[1][0] + b[0][0])) / 2,
	        y = +extent[0][1] + (h - k * (b[1][1] + b[0][1])) / 2;
	    projection.scale(150 * k).translate([x, y]);
	  }, object);
	}

	function fitSize(projection, size, object) {
	  return fitExtent(projection, [[0, 0], size], object);
	}

	function fitWidth(projection, width, object) {
	  return fit(projection, function(b) {
	    var w = +width,
	        k = w / (b[1][0] - b[0][0]),
	        x = (w - k * (b[1][0] + b[0][0])) / 2,
	        y = -k * b[0][1];
	    projection.scale(150 * k).translate([x, y]);
	  }, object);
	}

	function fitHeight(projection, height, object) {
	  return fit(projection, function(b) {
	    var h = +height,
	        k = h / (b[1][1] - b[0][1]),
	        x = -k * b[0][0],
	        y = (h - k * (b[1][1] + b[0][1])) / 2;
	    projection.scale(150 * k).translate([x, y]);
	  }, object);
	}

	var maxDepth = 16;
	var cosMinDistance = cos$1(30 * radians); // cos(minimum angular distance)

	function resample(project, delta2) {
	  return +delta2 ? resample$1(project, delta2) : resampleNone(project);
	}

	function resampleNone(project) {
	  return transformer({
	    point: function(x, y) {
	      x = project(x, y);
	      this.stream.point(x[0], x[1]);
	    }
	  });
	}

	function resample$1(project, delta2) {

	  function resampleLineTo(x0, y0, lambda0, a0, b0, c0, x1, y1, lambda1, a1, b1, c1, depth, stream) {
	    var dx = x1 - x0,
	        dy = y1 - y0,
	        d2 = dx * dx + dy * dy;
	    if (d2 > 4 * delta2 && depth--) {
	      var a = a0 + a1,
	          b = b0 + b1,
	          c = c0 + c1,
	          m = sqrt(a * a + b * b + c * c),
	          phi2 = asin(c /= m),
	          lambda2 = abs(abs(c) - 1) < epsilon$2 || abs(lambda0 - lambda1) < epsilon$2 ? (lambda0 + lambda1) / 2 : atan2(b, a),
	          p = project(lambda2, phi2),
	          x2 = p[0],
	          y2 = p[1],
	          dx2 = x2 - x0,
	          dy2 = y2 - y0,
	          dz = dy * dx2 - dx * dy2;
	      if (dz * dz / d2 > delta2 // perpendicular projected distance
	          || abs((dx * dx2 + dy * dy2) / d2 - 0.5) > 0.3 // midpoint close to an end
	          || a0 * a1 + b0 * b1 + c0 * c1 < cosMinDistance) { // angular distance
	        resampleLineTo(x0, y0, lambda0, a0, b0, c0, x2, y2, lambda2, a /= m, b /= m, c, depth, stream);
	        stream.point(x2, y2);
	        resampleLineTo(x2, y2, lambda2, a, b, c, x1, y1, lambda1, a1, b1, c1, depth, stream);
	      }
	    }
	  }
	  return function(stream) {
	    var lambda00, x00, y00, a00, b00, c00, // first point
	        lambda0, x0, y0, a0, b0, c0; // previous point

	    var resampleStream = {
	      point: point,
	      lineStart: lineStart,
	      lineEnd: lineEnd,
	      polygonStart: function() { stream.polygonStart(); resampleStream.lineStart = ringStart; },
	      polygonEnd: function() { stream.polygonEnd(); resampleStream.lineStart = lineStart; }
	    };

	    function point(x, y) {
	      x = project(x, y);
	      stream.point(x[0], x[1]);
	    }

	    function lineStart() {
	      x0 = NaN;
	      resampleStream.point = linePoint;
	      stream.lineStart();
	    }

	    function linePoint(lambda, phi) {
	      var c = cartesian([lambda, phi]), p = project(lambda, phi);
	      resampleLineTo(x0, y0, lambda0, a0, b0, c0, x0 = p[0], y0 = p[1], lambda0 = lambda, a0 = c[0], b0 = c[1], c0 = c[2], maxDepth, stream);
	      stream.point(x0, y0);
	    }

	    function lineEnd() {
	      resampleStream.point = point;
	      stream.lineEnd();
	    }

	    function ringStart() {
	      lineStart();
	      resampleStream.point = ringPoint;
	      resampleStream.lineEnd = ringEnd;
	    }

	    function ringPoint(lambda, phi) {
	      linePoint(lambda00 = lambda, phi), x00 = x0, y00 = y0, a00 = a0, b00 = b0, c00 = c0;
	      resampleStream.point = linePoint;
	    }

	    function ringEnd() {
	      resampleLineTo(x0, y0, lambda0, a0, b0, c0, x00, y00, lambda00, a00, b00, c00, maxDepth, stream);
	      resampleStream.lineEnd = lineEnd;
	      lineEnd();
	    }

	    return resampleStream;
	  };
	}

	var transformRadians = transformer({
	  point: function(x, y) {
	    this.stream.point(x * radians, y * radians);
	  }
	});

	function transformRotate(rotate) {
	  return transformer({
	    point: function(x, y) {
	      var r = rotate(x, y);
	      return this.stream.point(r[0], r[1]);
	    }
	  });
	}

	function projection(project) {
	  return projectionMutator(function() { return project; })();
	}

	function projectionMutator(projectAt) {
	  var project,
	      k = 150, // scale
	      x = 480, y = 250, // translate
	      dx, dy, lambda = 0, phi = 0, // center
	      deltaLambda = 0, deltaPhi = 0, deltaGamma = 0, rotate, projectRotate, // rotate
	      theta = null, preclip = clipAntimeridian, // clip angle
	      x0 = null, y0, x1, y1, postclip = identity$4, // clip extent
	      delta2 = 0.5, projectResample = resample(projectTransform, delta2), // precision
	      cache,
	      cacheStream;

	  function projection(point) {
	    point = projectRotate(point[0] * radians, point[1] * radians);
	    return [point[0] * k + dx, dy - point[1] * k];
	  }

	  function invert(point) {
	    point = projectRotate.invert((point[0] - dx) / k, (dy - point[1]) / k);
	    return point && [point[0] * degrees$1, point[1] * degrees$1];
	  }

	  function projectTransform(x, y) {
	    return x = project(x, y), [x[0] * k + dx, dy - x[1] * k];
	  }

	  projection.stream = function(stream) {
	    return cache && cacheStream === stream ? cache : cache = transformRadians(transformRotate(rotate)(preclip(projectResample(postclip(cacheStream = stream)))));
	  };

	  projection.preclip = function(_) {
	    return arguments.length ? (preclip = _, theta = undefined, reset()) : preclip;
	  };

	  projection.postclip = function(_) {
	    return arguments.length ? (postclip = _, x0 = y0 = x1 = y1 = null, reset()) : postclip;
	  };

	  projection.clipAngle = function(_) {
	    return arguments.length ? (preclip = +_ ? clipCircle(theta = _ * radians) : (theta = null, clipAntimeridian), reset()) : theta * degrees$1;
	  };

	  projection.clipExtent = function(_) {
	    return arguments.length ? (postclip = _ == null ? (x0 = y0 = x1 = y1 = null, identity$4) : clipRectangle(x0 = +_[0][0], y0 = +_[0][1], x1 = +_[1][0], y1 = +_[1][1]), reset()) : x0 == null ? null : [[x0, y0], [x1, y1]];
	  };

	  projection.scale = function(_) {
	    return arguments.length ? (k = +_, recenter()) : k;
	  };

	  projection.translate = function(_) {
	    return arguments.length ? (x = +_[0], y = +_[1], recenter()) : [x, y];
	  };

	  projection.center = function(_) {
	    return arguments.length ? (lambda = _[0] % 360 * radians, phi = _[1] % 360 * radians, recenter()) : [lambda * degrees$1, phi * degrees$1];
	  };

	  projection.rotate = function(_) {
	    return arguments.length ? (deltaLambda = _[0] % 360 * radians, deltaPhi = _[1] % 360 * radians, deltaGamma = _.length > 2 ? _[2] % 360 * radians : 0, recenter()) : [deltaLambda * degrees$1, deltaPhi * degrees$1, deltaGamma * degrees$1];
	  };

	  projection.precision = function(_) {
	    return arguments.length ? (projectResample = resample(projectTransform, delta2 = _ * _), reset()) : sqrt(delta2);
	  };

	  projection.fitExtent = function(extent, object) {
	    return fitExtent(projection, extent, object);
	  };

	  projection.fitSize = function(size, object) {
	    return fitSize(projection, size, object);
	  };

	  projection.fitWidth = function(width, object) {
	    return fitWidth(projection, width, object);
	  };

	  projection.fitHeight = function(height, object) {
	    return fitHeight(projection, height, object);
	  };

	  function recenter() {
	    projectRotate = compose(rotate = rotateRadians(deltaLambda, deltaPhi, deltaGamma), project);
	    var center = project(lambda, phi);
	    dx = x - center[0] * k;
	    dy = y + center[1] * k;
	    return reset();
	  }

	  function reset() {
	    cache = cacheStream = null;
	    return projection;
	  }

	  return function() {
	    project = projectAt.apply(this, arguments);
	    projection.invert = project.invert && invert;
	    return recenter();
	  };
	}

	function conicProjection(projectAt) {
	  var phi0 = 0,
	      phi1 = pi$3 / 3,
	      m = projectionMutator(projectAt),
	      p = m(phi0, phi1);

	  p.parallels = function(_) {
	    return arguments.length ? m(phi0 = _[0] * radians, phi1 = _[1] * radians) : [phi0 * degrees$1, phi1 * degrees$1];
	  };

	  return p;
	}

	function cylindricalEqualAreaRaw(phi0) {
	  var cosPhi0 = cos$1(phi0);

	  function forward(lambda, phi) {
	    return [lambda * cosPhi0, sin$1(phi) / cosPhi0];
	  }

	  forward.invert = function(x, y) {
	    return [x / cosPhi0, asin(y * cosPhi0)];
	  };

	  return forward;
	}

	function conicEqualAreaRaw(y0, y1) {
	  var sy0 = sin$1(y0), n = (sy0 + sin$1(y1)) / 2;

	  // Are the parallels symmetrical around the Equator?
	  if (abs(n) < epsilon$2) return cylindricalEqualAreaRaw(y0);

	  var c = 1 + sy0 * (2 * n - sy0), r0 = sqrt(c) / n;

	  function project(x, y) {
	    var r = sqrt(c - 2 * n * sin$1(y)) / n;
	    return [r * sin$1(x *= n), r0 - r * cos$1(x)];
	  }

	  project.invert = function(x, y) {
	    var r0y = r0 - y;
	    return [atan2(x, abs(r0y)) / n * sign(r0y), asin((c - (x * x + r0y * r0y) * n * n) / (2 * n))];
	  };

	  return project;
	}

	function conicEqualArea() {
	  return conicProjection(conicEqualAreaRaw)
	      .scale(155.424)
	      .center([0, 33.6442]);
	}

	function albers() {
	  return conicEqualArea()
	      .parallels([29.5, 45.5])
	      .scale(1070)
	      .translate([480, 250])
	      .rotate([96, 0])
	      .center([-0.6, 38.7]);
	}

	// The projections must have mutually exclusive clip regions on the sphere,
	// as this will avoid emitting interleaving lines and polygons.
	function multiplex(streams) {
	  var n = streams.length;
	  return {
	    point: function(x, y) { var i = -1; while (++i < n) streams[i].point(x, y); },
	    sphere: function() { var i = -1; while (++i < n) streams[i].sphere(); },
	    lineStart: function() { var i = -1; while (++i < n) streams[i].lineStart(); },
	    lineEnd: function() { var i = -1; while (++i < n) streams[i].lineEnd(); },
	    polygonStart: function() { var i = -1; while (++i < n) streams[i].polygonStart(); },
	    polygonEnd: function() { var i = -1; while (++i < n) streams[i].polygonEnd(); }
	  };
	}

	// A composite projection for the United States, configured by default for
	// 960×500. The projection also works quite well at 960×600 if you change the
	// scale to 1285 and adjust the translate accordingly. The set of standard
	// parallels for each region comes from USGS, which is published here:
	// http://egsc.usgs.gov/isb/pubs/MapProjections/projections.html#albers
	function albersUsa() {
	  var cache,
	      cacheStream,
	      lower48 = albers(), lower48Point,
	      alaska = conicEqualArea().rotate([154, 0]).center([-2, 58.5]).parallels([55, 65]), alaskaPoint, // EPSG:3338
	      hawaii = conicEqualArea().rotate([157, 0]).center([-3, 19.9]).parallels([8, 18]), hawaiiPoint, // ESRI:102007
	      point, pointStream = {point: function(x, y) { point = [x, y]; }};

	  function albersUsa(coordinates) {
	    var x = coordinates[0], y = coordinates[1];
	    return point = null, (lower48Point.point(x, y), point)
	        || (alaskaPoint.point(x, y), point)
	        || (hawaiiPoint.point(x, y), point);
	  }

	  albersUsa.invert = function(coordinates) {
	    var k = lower48.scale(),
	        t = lower48.translate(),
	        x = (coordinates[0] - t[0]) / k,
	        y = (coordinates[1] - t[1]) / k;
	    return (y >= 0.120 && y < 0.234 && x >= -0.425 && x < -0.214 ? alaska
	        : y >= 0.166 && y < 0.234 && x >= -0.214 && x < -0.115 ? hawaii
	        : lower48).invert(coordinates);
	  };

	  albersUsa.stream = function(stream) {
	    return cache && cacheStream === stream ? cache : cache = multiplex([lower48.stream(cacheStream = stream), alaska.stream(stream), hawaii.stream(stream)]);
	  };

	  albersUsa.precision = function(_) {
	    if (!arguments.length) return lower48.precision();
	    lower48.precision(_), alaska.precision(_), hawaii.precision(_);
	    return reset();
	  };

	  albersUsa.scale = function(_) {
	    if (!arguments.length) return lower48.scale();
	    lower48.scale(_), alaska.scale(_ * 0.35), hawaii.scale(_);
	    return albersUsa.translate(lower48.translate());
	  };

	  albersUsa.translate = function(_) {
	    if (!arguments.length) return lower48.translate();
	    var k = lower48.scale(), x = +_[0], y = +_[1];

	    lower48Point = lower48
	        .translate(_)
	        .clipExtent([[x - 0.455 * k, y - 0.238 * k], [x + 0.455 * k, y + 0.238 * k]])
	        .stream(pointStream);

	    alaskaPoint = alaska
	        .translate([x - 0.307 * k, y + 0.201 * k])
	        .clipExtent([[x - 0.425 * k + epsilon$2, y + 0.120 * k + epsilon$2], [x - 0.214 * k - epsilon$2, y + 0.234 * k - epsilon$2]])
	        .stream(pointStream);

	    hawaiiPoint = hawaii
	        .translate([x - 0.205 * k, y + 0.212 * k])
	        .clipExtent([[x - 0.214 * k + epsilon$2, y + 0.166 * k + epsilon$2], [x - 0.115 * k - epsilon$2, y + 0.234 * k - epsilon$2]])
	        .stream(pointStream);

	    return reset();
	  };

	  albersUsa.fitExtent = function(extent, object) {
	    return fitExtent(albersUsa, extent, object);
	  };

	  albersUsa.fitSize = function(size, object) {
	    return fitSize(albersUsa, size, object);
	  };

	  albersUsa.fitWidth = function(width, object) {
	    return fitWidth(albersUsa, width, object);
	  };

	  albersUsa.fitHeight = function(height, object) {
	    return fitHeight(albersUsa, height, object);
	  };

	  function reset() {
	    cache = cacheStream = null;
	    return albersUsa;
	  }

	  return albersUsa.scale(1070);
	}

	function azimuthalRaw(scale) {
	  return function(x, y) {
	    var cx = cos$1(x),
	        cy = cos$1(y),
	        k = scale(cx * cy);
	    return [
	      k * cy * sin$1(x),
	      k * sin$1(y)
	    ];
	  }
	}

	function azimuthalInvert(angle) {
	  return function(x, y) {
	    var z = sqrt(x * x + y * y),
	        c = angle(z),
	        sc = sin$1(c),
	        cc = cos$1(c);
	    return [
	      atan2(x * sc, z * cc),
	      asin(z && y * sc / z)
	    ];
	  }
	}

	var azimuthalEqualAreaRaw = azimuthalRaw(function(cxcy) {
	  return sqrt(2 / (1 + cxcy));
	});

	azimuthalEqualAreaRaw.invert = azimuthalInvert(function(z) {
	  return 2 * asin(z / 2);
	});

	function azimuthalEqualArea() {
	  return projection(azimuthalEqualAreaRaw)
	      .scale(124.75)
	      .clipAngle(180 - 1e-3);
	}

	var azimuthalEquidistantRaw = azimuthalRaw(function(c) {
	  return (c = acos(c)) && c / sin$1(c);
	});

	azimuthalEquidistantRaw.invert = azimuthalInvert(function(z) {
	  return z;
	});

	function azimuthalEquidistant() {
	  return projection(azimuthalEquidistantRaw)
	      .scale(79.4188)
	      .clipAngle(180 - 1e-3);
	}

	function mercatorRaw(lambda, phi) {
	  return [lambda, log(tan((halfPi$2 + phi) / 2))];
	}

	mercatorRaw.invert = function(x, y) {
	  return [x, 2 * atan(exp(y)) - halfPi$2];
	};

	function mercator() {
	  return mercatorProjection(mercatorRaw)
	      .scale(961 / tau$3);
	}

	function mercatorProjection(project) {
	  var m = projection(project),
	      center = m.center,
	      scale = m.scale,
	      translate = m.translate,
	      clipExtent = m.clipExtent,
	      x0 = null, y0, x1, y1; // clip extent

	  m.scale = function(_) {
	    return arguments.length ? (scale(_), reclip()) : scale();
	  };

	  m.translate = function(_) {
	    return arguments.length ? (translate(_), reclip()) : translate();
	  };

	  m.center = function(_) {
	    return arguments.length ? (center(_), reclip()) : center();
	  };

	  m.clipExtent = function(_) {
	    return arguments.length ? (_ == null ? x0 = y0 = x1 = y1 = null : (x0 = +_[0][0], y0 = +_[0][1], x1 = +_[1][0], y1 = +_[1][1]), reclip()) : x0 == null ? null : [[x0, y0], [x1, y1]];
	  };

	  function reclip() {
	    var k = pi$3 * scale(),
	        t = m(rotation(m.rotate()).invert([0, 0]));
	    return clipExtent(x0 == null
	        ? [[t[0] - k, t[1] - k], [t[0] + k, t[1] + k]] : project === mercatorRaw
	        ? [[Math.max(t[0] - k, x0), y0], [Math.min(t[0] + k, x1), y1]]
	        : [[x0, Math.max(t[1] - k, y0)], [x1, Math.min(t[1] + k, y1)]]);
	  }

	  return reclip();
	}

	function tany(y) {
	  return tan((halfPi$2 + y) / 2);
	}

	function conicConformalRaw(y0, y1) {
	  var cy0 = cos$1(y0),
	      n = y0 === y1 ? sin$1(y0) : log(cy0 / cos$1(y1)) / log(tany(y1) / tany(y0)),
	      f = cy0 * pow(tany(y0), n) / n;

	  if (!n) return mercatorRaw;

	  function project(x, y) {
	    if (f > 0) { if (y < -halfPi$2 + epsilon$2) y = -halfPi$2 + epsilon$2; }
	    else { if (y > halfPi$2 - epsilon$2) y = halfPi$2 - epsilon$2; }
	    var r = f / pow(tany(y), n);
	    return [r * sin$1(n * x), f - r * cos$1(n * x)];
	  }

	  project.invert = function(x, y) {
	    var fy = f - y, r = sign(n) * sqrt(x * x + fy * fy);
	    return [atan2(x, abs(fy)) / n * sign(fy), 2 * atan(pow(f / r, 1 / n)) - halfPi$2];
	  };

	  return project;
	}

	function conicConformal() {
	  return conicProjection(conicConformalRaw)
	      .scale(109.5)
	      .parallels([30, 30]);
	}

	function equirectangularRaw(lambda, phi) {
	  return [lambda, phi];
	}

	equirectangularRaw.invert = equirectangularRaw;

	function equirectangular() {
	  return projection(equirectangularRaw)
	      .scale(152.63);
	}

	function conicEquidistantRaw(y0, y1) {
	  var cy0 = cos$1(y0),
	      n = y0 === y1 ? sin$1(y0) : (cy0 - cos$1(y1)) / (y1 - y0),
	      g = cy0 / n + y0;

	  if (abs(n) < epsilon$2) return equirectangularRaw;

	  function project(x, y) {
	    var gy = g - y, nx = n * x;
	    return [gy * sin$1(nx), g - gy * cos$1(nx)];
	  }

	  project.invert = function(x, y) {
	    var gy = g - y;
	    return [atan2(x, abs(gy)) / n * sign(gy), g - sign(n) * sqrt(x * x + gy * gy)];
	  };

	  return project;
	}

	function conicEquidistant() {
	  return conicProjection(conicEquidistantRaw)
	      .scale(131.154)
	      .center([0, 13.9389]);
	}

	function gnomonicRaw(x, y) {
	  var cy = cos$1(y), k = cos$1(x) * cy;
	  return [cy * sin$1(x) / k, sin$1(y) / k];
	}

	gnomonicRaw.invert = azimuthalInvert(atan);

	function gnomonic() {
	  return projection(gnomonicRaw)
	      .scale(144.049)
	      .clipAngle(60);
	}

	function scaleTranslate(kx, ky, tx, ty) {
	  return kx === 1 && ky === 1 && tx === 0 && ty === 0 ? identity$4 : transformer({
	    point: function(x, y) {
	      this.stream.point(x * kx + tx, y * ky + ty);
	    }
	  });
	}

	function identity$5() {
	  var k = 1, tx = 0, ty = 0, sx = 1, sy = 1, transform$$1 = identity$4, // scale, translate and reflect
	      x0 = null, y0, x1, y1, // clip extent
	      postclip = identity$4,
	      cache,
	      cacheStream,
	      projection;

	  function reset() {
	    cache = cacheStream = null;
	    return projection;
	  }

	  return projection = {
	    stream: function(stream) {
	      return cache && cacheStream === stream ? cache : cache = transform$$1(postclip(cacheStream = stream));
	    },
	    postclip: function(_) {
	      return arguments.length ? (postclip = _, x0 = y0 = x1 = y1 = null, reset()) : postclip;
	    },
	    clipExtent: function(_) {
	      return arguments.length ? (postclip = _ == null ? (x0 = y0 = x1 = y1 = null, identity$4) : clipRectangle(x0 = +_[0][0], y0 = +_[0][1], x1 = +_[1][0], y1 = +_[1][1]), reset()) : x0 == null ? null : [[x0, y0], [x1, y1]];
	    },
	    scale: function(_) {
	      return arguments.length ? (transform$$1 = scaleTranslate((k = +_) * sx, k * sy, tx, ty), reset()) : k;
	    },
	    translate: function(_) {
	      return arguments.length ? (transform$$1 = scaleTranslate(k * sx, k * sy, tx = +_[0], ty = +_[1]), reset()) : [tx, ty];
	    },
	    reflectX: function(_) {
	      return arguments.length ? (transform$$1 = scaleTranslate(k * (sx = _ ? -1 : 1), k * sy, tx, ty), reset()) : sx < 0;
	    },
	    reflectY: function(_) {
	      return arguments.length ? (transform$$1 = scaleTranslate(k * sx, k * (sy = _ ? -1 : 1), tx, ty), reset()) : sy < 0;
	    },
	    fitExtent: function(extent, object) {
	      return fitExtent(projection, extent, object);
	    },
	    fitSize: function(size, object) {
	      return fitSize(projection, size, object);
	    },
	    fitWidth: function(width, object) {
	      return fitWidth(projection, width, object);
	    },
	    fitHeight: function(height, object) {
	      return fitHeight(projection, height, object);
	    }
	  };
	}

	function naturalEarth1Raw(lambda, phi) {
	  var phi2 = phi * phi, phi4 = phi2 * phi2;
	  return [
	    lambda * (0.8707 - 0.131979 * phi2 + phi4 * (-0.013791 + phi4 * (0.003971 * phi2 - 0.001529 * phi4))),
	    phi * (1.007226 + phi2 * (0.015085 + phi4 * (-0.044475 + 0.028874 * phi2 - 0.005916 * phi4)))
	  ];
	}

	naturalEarth1Raw.invert = function(x, y) {
	  var phi = y, i = 25, delta;
	  do {
	    var phi2 = phi * phi, phi4 = phi2 * phi2;
	    phi -= delta = (phi * (1.007226 + phi2 * (0.015085 + phi4 * (-0.044475 + 0.028874 * phi2 - 0.005916 * phi4))) - y) /
	        (1.007226 + phi2 * (0.015085 * 3 + phi4 * (-0.044475 * 7 + 0.028874 * 9 * phi2 - 0.005916 * 11 * phi4)));
	  } while (abs(delta) > epsilon$2 && --i > 0);
	  return [
	    x / (0.8707 + (phi2 = phi * phi) * (-0.131979 + phi2 * (-0.013791 + phi2 * phi2 * phi2 * (0.003971 - 0.001529 * phi2)))),
	    phi
	  ];
	};

	function naturalEarth1() {
	  return projection(naturalEarth1Raw)
	      .scale(175.295);
	}

	function orthographicRaw(x, y) {
	  return [cos$1(y) * sin$1(x), sin$1(y)];
	}

	orthographicRaw.invert = azimuthalInvert(asin);

	function orthographic() {
	  return projection(orthographicRaw)
	      .scale(249.5)
	      .clipAngle(90 + epsilon$2);
	}

	function stereographicRaw(x, y) {
	  var cy = cos$1(y), k = 1 + cos$1(x) * cy;
	  return [cy * sin$1(x) / k, sin$1(y) / k];
	}

	stereographicRaw.invert = azimuthalInvert(function(z) {
	  return 2 * atan(z);
	});

	function stereographic() {
	  return projection(stereographicRaw)
	      .scale(250)
	      .clipAngle(142);
	}

	function transverseMercatorRaw(lambda, phi) {
	  return [log(tan((halfPi$2 + phi) / 2)), -lambda];
	}

	transverseMercatorRaw.invert = function(x, y) {
	  return [-y, 2 * atan(exp(x)) - halfPi$2];
	};

	function transverseMercator() {
	  var m = mercatorProjection(transverseMercatorRaw),
	      center = m.center,
	      rotate = m.rotate;

	  m.center = function(_) {
	    return arguments.length ? center([-_[1], _[0]]) : (_ = center(), [_[1], -_[0]]);
	  };

	  m.rotate = function(_) {
	    return arguments.length ? rotate([_[0], _[1], _.length > 2 ? _[2] + 90 : 90]) : (_ = rotate(), [_[0], _[1], _[2] - 90]);
	  };

	  return rotate([0, 0, 90])
	      .scale(159.155);
	}

	function defaultSeparation(a, b) {
	  return a.parent === b.parent ? 1 : 2;
	}

	function meanX(children) {
	  return children.reduce(meanXReduce, 0) / children.length;
	}

	function meanXReduce(x, c) {
	  return x + c.x;
	}

	function maxY(children) {
	  return 1 + children.reduce(maxYReduce, 0);
	}

	function maxYReduce(y, c) {
	  return Math.max(y, c.y);
	}

	function leafLeft(node) {
	  var children;
	  while (children = node.children) node = children[0];
	  return node;
	}

	function leafRight(node) {
	  var children;
	  while (children = node.children) node = children[children.length - 1];
	  return node;
	}

	function cluster() {
	  var separation = defaultSeparation,
	      dx = 1,
	      dy = 1,
	      nodeSize = false;

	  function cluster(root) {
	    var previousNode,
	        x = 0;

	    // First walk, computing the initial x & y values.
	    root.eachAfter(function(node) {
	      var children = node.children;
	      if (children) {
	        node.x = meanX(children);
	        node.y = maxY(children);
	      } else {
	        node.x = previousNode ? x += separation(node, previousNode) : 0;
	        node.y = 0;
	        previousNode = node;
	      }
	    });

	    var left = leafLeft(root),
	        right = leafRight(root),
	        x0 = left.x - separation(left, right) / 2,
	        x1 = right.x + separation(right, left) / 2;

	    // Second walk, normalizing x & y to the desired size.
	    return root.eachAfter(nodeSize ? function(node) {
	      node.x = (node.x - root.x) * dx;
	      node.y = (root.y - node.y) * dy;
	    } : function(node) {
	      node.x = (node.x - x0) / (x1 - x0) * dx;
	      node.y = (1 - (root.y ? node.y / root.y : 1)) * dy;
	    });
	  }

	  cluster.separation = function(x) {
	    return arguments.length ? (separation = x, cluster) : separation;
	  };

	  cluster.size = function(x) {
	    return arguments.length ? (nodeSize = false, dx = +x[0], dy = +x[1], cluster) : (nodeSize ? null : [dx, dy]);
	  };

	  cluster.nodeSize = function(x) {
	    return arguments.length ? (nodeSize = true, dx = +x[0], dy = +x[1], cluster) : (nodeSize ? [dx, dy] : null);
	  };

	  return cluster;
	}

	function count(node) {
	  var sum = 0,
	      children = node.children,
	      i = children && children.length;
	  if (!i) sum = 1;
	  else while (--i >= 0) sum += children[i].value;
	  node.value = sum;
	}

	function node_count() {
	  return this.eachAfter(count);
	}

	function node_each(callback) {
	  var node = this, current, next = [node], children, i, n;
	  do {
	    current = next.reverse(), next = [];
	    while (node = current.pop()) {
	      callback(node), children = node.children;
	      if (children) for (i = 0, n = children.length; i < n; ++i) {
	        next.push(children[i]);
	      }
	    }
	  } while (next.length);
	  return this;
	}

	function node_eachBefore(callback) {
	  var node = this, nodes = [node], children, i;
	  while (node = nodes.pop()) {
	    callback(node), children = node.children;
	    if (children) for (i = children.length - 1; i >= 0; --i) {
	      nodes.push(children[i]);
	    }
	  }
	  return this;
	}

	function node_eachAfter(callback) {
	  var node = this, nodes = [node], next = [], children, i, n;
	  while (node = nodes.pop()) {
	    next.push(node), children = node.children;
	    if (children) for (i = 0, n = children.length; i < n; ++i) {
	      nodes.push(children[i]);
	    }
	  }
	  while (node = next.pop()) {
	    callback(node);
	  }
	  return this;
	}

	function node_sum(value) {
	  return this.eachAfter(function(node) {
	    var sum = +value(node.data) || 0,
	        children = node.children,
	        i = children && children.length;
	    while (--i >= 0) sum += children[i].value;
	    node.value = sum;
	  });
	}

	function node_sort(compare) {
	  return this.eachBefore(function(node) {
	    if (node.children) {
	      node.children.sort(compare);
	    }
	  });
	}

	function node_path(end) {
	  var start = this,
	      ancestor = leastCommonAncestor(start, end),
	      nodes = [start];
	  while (start !== ancestor) {
	    start = start.parent;
	    nodes.push(start);
	  }
	  var k = nodes.length;
	  while (end !== ancestor) {
	    nodes.splice(k, 0, end);
	    end = end.parent;
	  }
	  return nodes;
	}

	function leastCommonAncestor(a, b) {
	  if (a === b) return a;
	  var aNodes = a.ancestors(),
	      bNodes = b.ancestors(),
	      c = null;
	  a = aNodes.pop();
	  b = bNodes.pop();
	  while (a === b) {
	    c = a;
	    a = aNodes.pop();
	    b = bNodes.pop();
	  }
	  return c;
	}

	function node_ancestors() {
	  var node = this, nodes = [node];
	  while (node = node.parent) {
	    nodes.push(node);
	  }
	  return nodes;
	}

	function node_descendants() {
	  var nodes = [];
	  this.each(function(node) {
	    nodes.push(node);
	  });
	  return nodes;
	}

	function node_leaves() {
	  var leaves = [];
	  this.eachBefore(function(node) {
	    if (!node.children) {
	      leaves.push(node);
	    }
	  });
	  return leaves;
	}

	function node_links() {
	  var root = this, links = [];
	  root.each(function(node) {
	    if (node !== root) { // Don’t include the root’s parent, if any.
	      links.push({source: node.parent, target: node});
	    }
	  });
	  return links;
	}

	function hierarchy(data, children) {
	  var root = new Node(data),
	      valued = +data.value && (root.value = data.value),
	      node,
	      nodes = [root],
	      child,
	      childs,
	      i,
	      n;

	  if (children == null) children = defaultChildren;

	  while (node = nodes.pop()) {
	    if (valued) node.value = +node.data.value;
	    if ((childs = children(node.data)) && (n = childs.length)) {
	      node.children = new Array(n);
	      for (i = n - 1; i >= 0; --i) {
	        nodes.push(child = node.children[i] = new Node(childs[i]));
	        child.parent = node;
	        child.depth = node.depth + 1;
	      }
	    }
	  }

	  return root.eachBefore(computeHeight);
	}

	function node_copy() {
	  return hierarchy(this).eachBefore(copyData);
	}

	function defaultChildren(d) {
	  return d.children;
	}

	function copyData(node) {
	  node.data = node.data.data;
	}

	function computeHeight(node) {
	  var height = 0;
	  do node.height = height;
	  while ((node = node.parent) && (node.height < ++height));
	}

	function Node(data) {
	  this.data = data;
	  this.depth =
	  this.height = 0;
	  this.parent = null;
	}

	Node.prototype = hierarchy.prototype = {
	  constructor: Node,
	  count: node_count,
	  each: node_each,
	  eachAfter: node_eachAfter,
	  eachBefore: node_eachBefore,
	  sum: node_sum,
	  sort: node_sort,
	  path: node_path,
	  ancestors: node_ancestors,
	  descendants: node_descendants,
	  leaves: node_leaves,
	  links: node_links,
	  copy: node_copy
	};

	var slice$3 = Array.prototype.slice;

	function shuffle$1(array) {
	  var m = array.length,
	      t,
	      i;

	  while (m) {
	    i = Math.random() * m-- | 0;
	    t = array[m];
	    array[m] = array[i];
	    array[i] = t;
	  }

	  return array;
	}

	function enclose(circles) {
	  var i = 0, n = (circles = shuffle$1(slice$3.call(circles))).length, B = [], p, e;

	  while (i < n) {
	    p = circles[i];
	    if (e && enclosesWeak(e, p)) ++i;
	    else e = encloseBasis(B = extendBasis(B, p)), i = 0;
	  }

	  return e;
	}

	function extendBasis(B, p) {
	  var i, j;

	  if (enclosesWeakAll(p, B)) return [p];

	  // If we get here then B must have at least one element.
	  for (i = 0; i < B.length; ++i) {
	    if (enclosesNot(p, B[i])
	        && enclosesWeakAll(encloseBasis2(B[i], p), B)) {
	      return [B[i], p];
	    }
	  }

	  // If we get here then B must have at least two elements.
	  for (i = 0; i < B.length - 1; ++i) {
	    for (j = i + 1; j < B.length; ++j) {
	      if (enclosesNot(encloseBasis2(B[i], B[j]), p)
	          && enclosesNot(encloseBasis2(B[i], p), B[j])
	          && enclosesNot(encloseBasis2(B[j], p), B[i])
	          && enclosesWeakAll(encloseBasis3(B[i], B[j], p), B)) {
	        return [B[i], B[j], p];
	      }
	    }
	  }

	  // If we get here then something is very wrong.
	  throw new Error;
	}

	function enclosesNot(a, b) {
	  var dr = a.r - b.r, dx = b.x - a.x, dy = b.y - a.y;
	  return dr < 0 || dr * dr < dx * dx + dy * dy;
	}

	function enclosesWeak(a, b) {
	  var dr = a.r - b.r + 1e-6, dx = b.x - a.x, dy = b.y - a.y;
	  return dr > 0 && dr * dr > dx * dx + dy * dy;
	}

	function enclosesWeakAll(a, B) {
	  for (var i = 0; i < B.length; ++i) {
	    if (!enclosesWeak(a, B[i])) {
	      return false;
	    }
	  }
	  return true;
	}

	function encloseBasis(B) {
	  switch (B.length) {
	    case 1: return encloseBasis1(B[0]);
	    case 2: return encloseBasis2(B[0], B[1]);
	    case 3: return encloseBasis3(B[0], B[1], B[2]);
	  }
	}

	function encloseBasis1(a) {
	  return {
	    x: a.x,
	    y: a.y,
	    r: a.r
	  };
	}

	function encloseBasis2(a, b) {
	  var x1 = a.x, y1 = a.y, r1 = a.r,
	      x2 = b.x, y2 = b.y, r2 = b.r,
	      x21 = x2 - x1, y21 = y2 - y1, r21 = r2 - r1,
	      l = Math.sqrt(x21 * x21 + y21 * y21);
	  return {
	    x: (x1 + x2 + x21 / l * r21) / 2,
	    y: (y1 + y2 + y21 / l * r21) / 2,
	    r: (l + r1 + r2) / 2
	  };
	}

	function encloseBasis3(a, b, c) {
	  var x1 = a.x, y1 = a.y, r1 = a.r,
	      x2 = b.x, y2 = b.y, r2 = b.r,
	      x3 = c.x, y3 = c.y, r3 = c.r,
	      a2 = x1 - x2,
	      a3 = x1 - x3,
	      b2 = y1 - y2,
	      b3 = y1 - y3,
	      c2 = r2 - r1,
	      c3 = r3 - r1,
	      d1 = x1 * x1 + y1 * y1 - r1 * r1,
	      d2 = d1 - x2 * x2 - y2 * y2 + r2 * r2,
	      d3 = d1 - x3 * x3 - y3 * y3 + r3 * r3,
	      ab = a3 * b2 - a2 * b3,
	      xa = (b2 * d3 - b3 * d2) / (ab * 2) - x1,
	      xb = (b3 * c2 - b2 * c3) / ab,
	      ya = (a3 * d2 - a2 * d3) / (ab * 2) - y1,
	      yb = (a2 * c3 - a3 * c2) / ab,
	      A = xb * xb + yb * yb - 1,
	      B = 2 * (r1 + xa * xb + ya * yb),
	      C = xa * xa + ya * ya - r1 * r1,
	      r = -(A ? (B + Math.sqrt(B * B - 4 * A * C)) / (2 * A) : C / B);
	  return {
	    x: x1 + xa + xb * r,
	    y: y1 + ya + yb * r,
	    r: r
	  };
	}

	function place(a, b, c) {
	  var ax = a.x,
	      ay = a.y,
	      da = b.r + c.r,
	      db = a.r + c.r,
	      dx = b.x - ax,
	      dy = b.y - ay,
	      dc = dx * dx + dy * dy;
	  if (dc) {
	    var x = 0.5 + ((db *= db) - (da *= da)) / (2 * dc),
	        y = Math.sqrt(Math.max(0, 2 * da * (db + dc) - (db -= dc) * db - da * da)) / (2 * dc);
	    c.x = ax + x * dx + y * dy;
	    c.y = ay + x * dy - y * dx;
	  } else {
	    c.x = ax + db;
	    c.y = ay;
	  }
	}

	function intersects(a, b) {
	  var dx = b.x - a.x,
	      dy = b.y - a.y,
	      dr = a.r + b.r;
	  return dr * dr - 1e-6 > dx * dx + dy * dy;
	}

	function score(node) {
	  var a = node._,
	      b = node.next._,
	      ab = a.r + b.r,
	      dx = (a.x * b.r + b.x * a.r) / ab,
	      dy = (a.y * b.r + b.y * a.r) / ab;
	  return dx * dx + dy * dy;
	}

	function Node$1(circle) {
	  this._ = circle;
	  this.next = null;
	  this.previous = null;
	}

	function packEnclose(circles) {
	  if (!(n = circles.length)) return 0;

	  var a, b, c, n, aa, ca, i, j, k, sj, sk;

	  // Place the first circle.
	  a = circles[0], a.x = 0, a.y = 0;
	  if (!(n > 1)) return a.r;

	  // Place the second circle.
	  b = circles[1], a.x = -b.r, b.x = a.r, b.y = 0;
	  if (!(n > 2)) return a.r + b.r;

	  // Place the third circle.
	  place(b, a, c = circles[2]);

	  // Initialize the front-chain using the first three circles a, b and c.
	  a = new Node$1(a), b = new Node$1(b), c = new Node$1(c);
	  a.next = c.previous = b;
	  b.next = a.previous = c;
	  c.next = b.previous = a;

	  // Attempt to place each remaining circle…
	  pack: for (i = 3; i < n; ++i) {
	    place(a._, b._, c = circles[i]), c = new Node$1(c);

	    // Find the closest intersecting circle on the front-chain, if any.
	    // “Closeness” is determined by linear distance along the front-chain.
	    // “Ahead” or “behind” is likewise determined by linear distance.
	    j = b.next, k = a.previous, sj = b._.r, sk = a._.r;
	    do {
	      if (sj <= sk) {
	        if (intersects(j._, c._)) {
	          b = j, a.next = b, b.previous = a, --i;
	          continue pack;
	        }
	        sj += j._.r, j = j.next;
	      } else {
	        if (intersects(k._, c._)) {
	          a = k, a.next = b, b.previous = a, --i;
	          continue pack;
	        }
	        sk += k._.r, k = k.previous;
	      }
	    } while (j !== k.next);

	    // Success! Insert the new circle c between a and b.
	    c.previous = a, c.next = b, a.next = b.previous = b = c;

	    // Compute the new closest circle pair to the centroid.
	    aa = score(a);
	    while ((c = c.next) !== b) {
	      if ((ca = score(c)) < aa) {
	        a = c, aa = ca;
	      }
	    }
	    b = a.next;
	  }

	  // Compute the enclosing circle of the front chain.
	  a = [b._], c = b; while ((c = c.next) !== b) a.push(c._); c = enclose(a);

	  // Translate the circles to put the enclosing circle around the origin.
	  for (i = 0; i < n; ++i) a = circles[i], a.x -= c.x, a.y -= c.y;

	  return c.r;
	}

	function siblings(circles) {
	  packEnclose(circles);
	  return circles;
	}

	function optional(f) {
	  return f == null ? null : required(f);
	}

	function required(f) {
	  if (typeof f !== "function") throw new Error;
	  return f;
	}

	function constantZero() {
	  return 0;
	}

	function constant$8(x) {
	  return function() {
	    return x;
	  };
	}

	function defaultRadius$1(d) {
	  return Math.sqrt(d.value);
	}

	function index$2() {
	  var radius = null,
	      dx = 1,
	      dy = 1,
	      padding = constantZero;

	  function pack(root) {
	    root.x = dx / 2, root.y = dy / 2;
	    if (radius) {
	      root.eachBefore(radiusLeaf(radius))
	          .eachAfter(packChildren(padding, 0.5))
	          .eachBefore(translateChild(1));
	    } else {
	      root.eachBefore(radiusLeaf(defaultRadius$1))
	          .eachAfter(packChildren(constantZero, 1))
	          .eachAfter(packChildren(padding, root.r / Math.min(dx, dy)))
	          .eachBefore(translateChild(Math.min(dx, dy) / (2 * root.r)));
	    }
	    return root;
	  }

	  pack.radius = function(x) {
	    return arguments.length ? (radius = optional(x), pack) : radius;
	  };

	  pack.size = function(x) {
	    return arguments.length ? (dx = +x[0], dy = +x[1], pack) : [dx, dy];
	  };

	  pack.padding = function(x) {
	    return arguments.length ? (padding = typeof x === "function" ? x : constant$8(+x), pack) : padding;
	  };

	  return pack;
	}

	function radiusLeaf(radius) {
	  return function(node) {
	    if (!node.children) {
	      node.r = Math.max(0, +radius(node) || 0);
	    }
	  };
	}

	function packChildren(padding, k) {
	  return function(node) {
	    if (children = node.children) {
	      var children,
	          i,
	          n = children.length,
	          r = padding(node) * k || 0,
	          e;

	      if (r) for (i = 0; i < n; ++i) children[i].r += r;
	      e = packEnclose(children);
	      if (r) for (i = 0; i < n; ++i) children[i].r -= r;
	      node.r = e + r;
	    }
	  };
	}

	function translateChild(k) {
	  return function(node) {
	    var parent = node.parent;
	    node.r *= k;
	    if (parent) {
	      node.x = parent.x + k * node.x;
	      node.y = parent.y + k * node.y;
	    }
	  };
	}

	function roundNode(node) {
	  node.x0 = Math.round(node.x0);
	  node.y0 = Math.round(node.y0);
	  node.x1 = Math.round(node.x1);
	  node.y1 = Math.round(node.y1);
	}

	function treemapDice(parent, x0, y0, x1, y1) {
	  var nodes = parent.children,
	      node,
	      i = -1,
	      n = nodes.length,
	      k = parent.value && (x1 - x0) / parent.value;

	  while (++i < n) {
	    node = nodes[i], node.y0 = y0, node.y1 = y1;
	    node.x0 = x0, node.x1 = x0 += node.value * k;
	  }
	}

	function partition() {
	  var dx = 1,
	      dy = 1,
	      padding = 0,
	      round = false;

	  function partition(root) {
	    var n = root.height + 1;
	    root.x0 =
	    root.y0 = padding;
	    root.x1 = dx;
	    root.y1 = dy / n;
	    root.eachBefore(positionNode(dy, n));
	    if (round) root.eachBefore(roundNode);
	    return root;
	  }

	  function positionNode(dy, n) {
	    return function(node) {
	      if (node.children) {
	        treemapDice(node, node.x0, dy * (node.depth + 1) / n, node.x1, dy * (node.depth + 2) / n);
	      }
	      var x0 = node.x0,
	          y0 = node.y0,
	          x1 = node.x1 - padding,
	          y1 = node.y1 - padding;
	      if (x1 < x0) x0 = x1 = (x0 + x1) / 2;
	      if (y1 < y0) y0 = y1 = (y0 + y1) / 2;
	      node.x0 = x0;
	      node.y0 = y0;
	      node.x1 = x1;
	      node.y1 = y1;
	    };
	  }

	  partition.round = function(x) {
	    return arguments.length ? (round = !!x, partition) : round;
	  };

	  partition.size = function(x) {
	    return arguments.length ? (dx = +x[0], dy = +x[1], partition) : [dx, dy];
	  };

	  partition.padding = function(x) {
	    return arguments.length ? (padding = +x, partition) : padding;
	  };

	  return partition;
	}

	var keyPrefix$1 = "$";
	var preroot = {depth: -1};
	var ambiguous = {};

	function defaultId(d) {
	  return d.id;
	}

	function defaultParentId(d) {
	  return d.parentId;
	}

	function stratify() {
	  var id = defaultId,
	      parentId = defaultParentId;

	  function stratify(data) {
	    var d,
	        i,
	        n = data.length,
	        root,
	        parent,
	        node,
	        nodes = new Array(n),
	        nodeId,
	        nodeKey,
	        nodeByKey = {};

	    for (i = 0; i < n; ++i) {
	      d = data[i], node = nodes[i] = new Node(d);
	      if ((nodeId = id(d, i, data)) != null && (nodeId += "")) {
	        nodeKey = keyPrefix$1 + (node.id = nodeId);
	        nodeByKey[nodeKey] = nodeKey in nodeByKey ? ambiguous : node;
	      }
	    }

	    for (i = 0; i < n; ++i) {
	      node = nodes[i], nodeId = parentId(data[i], i, data);
	      if (nodeId == null || !(nodeId += "")) {
	        if (root) throw new Error("multiple roots");
	        root = node;
	      } else {
	        parent = nodeByKey[keyPrefix$1 + nodeId];
	        if (!parent) throw new Error("missing: " + nodeId);
	        if (parent === ambiguous) throw new Error("ambiguous: " + nodeId);
	        if (parent.children) parent.children.push(node);
	        else parent.children = [node];
	        node.parent = parent;
	      }
	    }

	    if (!root) throw new Error("no root");
	    root.parent = preroot;
	    root.eachBefore(function(node) { node.depth = node.parent.depth + 1; --n; }).eachBefore(computeHeight);
	    root.parent = null;
	    if (n > 0) throw new Error("cycle");

	    return root;
	  }

	  stratify.id = function(x) {
	    return arguments.length ? (id = required(x), stratify) : id;
	  };

	  stratify.parentId = function(x) {
	    return arguments.length ? (parentId = required(x), stratify) : parentId;
	  };

	  return stratify;
	}

	function defaultSeparation$1(a, b) {
	  return a.parent === b.parent ? 1 : 2;
	}

	// function radialSeparation(a, b) {
	//   return (a.parent === b.parent ? 1 : 2) / a.depth;
	// }

	// This function is used to traverse the left contour of a subtree (or
	// subforest). It returns the successor of v on this contour. This successor is
	// either given by the leftmost child of v or by the thread of v. The function
	// returns null if and only if v is on the highest level of its subtree.
	function nextLeft(v) {
	  var children = v.children;
	  return children ? children[0] : v.t;
	}

	// This function works analogously to nextLeft.
	function nextRight(v) {
	  var children = v.children;
	  return children ? children[children.length - 1] : v.t;
	}

	// Shifts the current subtree rooted at w+. This is done by increasing
	// prelim(w+) and mod(w+) by shift.
	function moveSubtree(wm, wp, shift) {
	  var change = shift / (wp.i - wm.i);
	  wp.c -= change;
	  wp.s += shift;
	  wm.c += change;
	  wp.z += shift;
	  wp.m += shift;
	}

	// All other shifts, applied to the smaller subtrees between w- and w+, are
	// performed by this function. To prepare the shifts, we have to adjust
	// change(w+), shift(w+), and change(w-).
	function executeShifts(v) {
	  var shift = 0,
	      change = 0,
	      children = v.children,
	      i = children.length,
	      w;
	  while (--i >= 0) {
	    w = children[i];
	    w.z += shift;
	    w.m += shift;
	    shift += w.s + (change += w.c);
	  }
	}

	// If vi-’s ancestor is a sibling of v, returns vi-’s ancestor. Otherwise,
	// returns the specified (default) ancestor.
	function nextAncestor(vim, v, ancestor) {
	  return vim.a.parent === v.parent ? vim.a : ancestor;
	}

	function TreeNode(node, i) {
	  this._ = node;
	  this.parent = null;
	  this.children = null;
	  this.A = null; // default ancestor
	  this.a = this; // ancestor
	  this.z = 0; // prelim
	  this.m = 0; // mod
	  this.c = 0; // change
	  this.s = 0; // shift
	  this.t = null; // thread
	  this.i = i; // number
	}

	TreeNode.prototype = Object.create(Node.prototype);

	function treeRoot(root) {
	  var tree = new TreeNode(root, 0),
	      node,
	      nodes = [tree],
	      child,
	      children,
	      i,
	      n;

	  while (node = nodes.pop()) {
	    if (children = node._.children) {
	      node.children = new Array(n = children.length);
	      for (i = n - 1; i >= 0; --i) {
	        nodes.push(child = node.children[i] = new TreeNode(children[i], i));
	        child.parent = node;
	      }
	    }
	  }

	  (tree.parent = new TreeNode(null, 0)).children = [tree];
	  return tree;
	}

	// Node-link tree diagram using the Reingold-Tilford "tidy" algorithm
	function tree() {
	  var separation = defaultSeparation$1,
	      dx = 1,
	      dy = 1,
	      nodeSize = null;

	  function tree(root) {
	    var t = treeRoot(root);

	    // Compute the layout using Buchheim et al.’s algorithm.
	    t.eachAfter(firstWalk), t.parent.m = -t.z;
	    t.eachBefore(secondWalk);

	    // If a fixed node size is specified, scale x and y.
	    if (nodeSize) root.eachBefore(sizeNode);

	    // If a fixed tree size is specified, scale x and y based on the extent.
	    // Compute the left-most, right-most, and depth-most nodes for extents.
	    else {
	      var left = root,
	          right = root,
	          bottom = root;
	      root.eachBefore(function(node) {
	        if (node.x < left.x) left = node;
	        if (node.x > right.x) right = node;
	        if (node.depth > bottom.depth) bottom = node;
	      });
	      var s = left === right ? 1 : separation(left, right) / 2,
	          tx = s - left.x,
	          kx = dx / (right.x + s + tx),
	          ky = dy / (bottom.depth || 1);
	      root.eachBefore(function(node) {
	        node.x = (node.x + tx) * kx;
	        node.y = node.depth * ky;
	      });
	    }

	    return root;
	  }

	  // Computes a preliminary x-coordinate for v. Before that, FIRST WALK is
	  // applied recursively to the children of v, as well as the function
	  // APPORTION. After spacing out the children by calling EXECUTE SHIFTS, the
	  // node v is placed to the midpoint of its outermost children.
	  function firstWalk(v) {
	    var children = v.children,
	        siblings = v.parent.children,
	        w = v.i ? siblings[v.i - 1] : null;
	    if (children) {
	      executeShifts(v);
	      var midpoint = (children[0].z + children[children.length - 1].z) / 2;
	      if (w) {
	        v.z = w.z + separation(v._, w._);
	        v.m = v.z - midpoint;
	      } else {
	        v.z = midpoint;
	      }
	    } else if (w) {
	      v.z = w.z + separation(v._, w._);
	    }
	    v.parent.A = apportion(v, w, v.parent.A || siblings[0]);
	  }

	  // Computes all real x-coordinates by summing up the modifiers recursively.
	  function secondWalk(v) {
	    v._.x = v.z + v.parent.m;
	    v.m += v.parent.m;
	  }

	  // The core of the algorithm. Here, a new subtree is combined with the
	  // previous subtrees. Threads are used to traverse the inside and outside
	  // contours of the left and right subtree up to the highest common level. The
	  // vertices used for the traversals are vi+, vi-, vo-, and vo+, where the
	  // superscript o means outside and i means inside, the subscript - means left
	  // subtree and + means right subtree. For summing up the modifiers along the
	  // contour, we use respective variables si+, si-, so-, and so+. Whenever two
	  // nodes of the inside contours conflict, we compute the left one of the
	  // greatest uncommon ancestors using the function ANCESTOR and call MOVE
	  // SUBTREE to shift the subtree and prepare the shifts of smaller subtrees.
	  // Finally, we add a new thread (if necessary).
	  function apportion(v, w, ancestor) {
	    if (w) {
	      var vip = v,
	          vop = v,
	          vim = w,
	          vom = vip.parent.children[0],
	          sip = vip.m,
	          sop = vop.m,
	          sim = vim.m,
	          som = vom.m,
	          shift;
	      while (vim = nextRight(vim), vip = nextLeft(vip), vim && vip) {
	        vom = nextLeft(vom);
	        vop = nextRight(vop);
	        vop.a = v;
	        shift = vim.z + sim - vip.z - sip + separation(vim._, vip._);
	        if (shift > 0) {
	          moveSubtree(nextAncestor(vim, v, ancestor), v, shift);
	          sip += shift;
	          sop += shift;
	        }
	        sim += vim.m;
	        sip += vip.m;
	        som += vom.m;
	        sop += vop.m;
	      }
	      if (vim && !nextRight(vop)) {
	        vop.t = vim;
	        vop.m += sim - sop;
	      }
	      if (vip && !nextLeft(vom)) {
	        vom.t = vip;
	        vom.m += sip - som;
	        ancestor = v;
	      }
	    }
	    return ancestor;
	  }

	  function sizeNode(node) {
	    node.x *= dx;
	    node.y = node.depth * dy;
	  }

	  tree.separation = function(x) {
	    return arguments.length ? (separation = x, tree) : separation;
	  };

	  tree.size = function(x) {
	    return arguments.length ? (nodeSize = false, dx = +x[0], dy = +x[1], tree) : (nodeSize ? null : [dx, dy]);
	  };

	  tree.nodeSize = function(x) {
	    return arguments.length ? (nodeSize = true, dx = +x[0], dy = +x[1], tree) : (nodeSize ? [dx, dy] : null);
	  };

	  return tree;
	}

	function treemapSlice(parent, x0, y0, x1, y1) {
	  var nodes = parent.children,
	      node,
	      i = -1,
	      n = nodes.length,
	      k = parent.value && (y1 - y0) / parent.value;

	  while (++i < n) {
	    node = nodes[i], node.x0 = x0, node.x1 = x1;
	    node.y0 = y0, node.y1 = y0 += node.value * k;
	  }
	}

	var phi = (1 + Math.sqrt(5)) / 2;

	function squarifyRatio(ratio, parent, x0, y0, x1, y1) {
	  var rows = [],
	      nodes = parent.children,
	      row,
	      nodeValue,
	      i0 = 0,
	      i1 = 0,
	      n = nodes.length,
	      dx, dy,
	      value = parent.value,
	      sumValue,
	      minValue,
	      maxValue,
	      newRatio,
	      minRatio,
	      alpha,
	      beta;

	  while (i0 < n) {
	    dx = x1 - x0, dy = y1 - y0;

	    // Find the next non-empty node.
	    do sumValue = nodes[i1++].value; while (!sumValue && i1 < n);
	    minValue = maxValue = sumValue;
	    alpha = Math.max(dy / dx, dx / dy) / (value * ratio);
	    beta = sumValue * sumValue * alpha;
	    minRatio = Math.max(maxValue / beta, beta / minValue);

	    // Keep adding nodes while the aspect ratio maintains or improves.
	    for (; i1 < n; ++i1) {
	      sumValue += nodeValue = nodes[i1].value;
	      if (nodeValue < minValue) minValue = nodeValue;
	      if (nodeValue > maxValue) maxValue = nodeValue;
	      beta = sumValue * sumValue * alpha;
	      newRatio = Math.max(maxValue / beta, beta / minValue);
	      if (newRatio > minRatio) { sumValue -= nodeValue; break; }
	      minRatio = newRatio;
	    }

	    // Position and record the row orientation.
	    rows.push(row = {value: sumValue, dice: dx < dy, children: nodes.slice(i0, i1)});
	    if (row.dice) treemapDice(row, x0, y0, x1, value ? y0 += dy * sumValue / value : y1);
	    else treemapSlice(row, x0, y0, value ? x0 += dx * sumValue / value : x1, y1);
	    value -= sumValue, i0 = i1;
	  }

	  return rows;
	}

	var squarify = (function custom(ratio) {

	  function squarify(parent, x0, y0, x1, y1) {
	    squarifyRatio(ratio, parent, x0, y0, x1, y1);
	  }

	  squarify.ratio = function(x) {
	    return custom((x = +x) > 1 ? x : 1);
	  };

	  return squarify;
	})(phi);

	function index$3() {
	  var tile = squarify,
	      round = false,
	      dx = 1,
	      dy = 1,
	      paddingStack = [0],
	      paddingInner = constantZero,
	      paddingTop = constantZero,
	      paddingRight = constantZero,
	      paddingBottom = constantZero,
	      paddingLeft = constantZero;

	  function treemap(root) {
	    root.x0 =
	    root.y0 = 0;
	    root.x1 = dx;
	    root.y1 = dy;
	    root.eachBefore(positionNode);
	    paddingStack = [0];
	    if (round) root.eachBefore(roundNode);
	    return root;
	  }

	  function positionNode(node) {
	    var p = paddingStack[node.depth],
	        x0 = node.x0 + p,
	        y0 = node.y0 + p,
	        x1 = node.x1 - p,
	        y1 = node.y1 - p;
	    if (x1 < x0) x0 = x1 = (x0 + x1) / 2;
	    if (y1 < y0) y0 = y1 = (y0 + y1) / 2;
	    node.x0 = x0;
	    node.y0 = y0;
	    node.x1 = x1;
	    node.y1 = y1;
	    if (node.children) {
	      p = paddingStack[node.depth + 1] = paddingInner(node) / 2;
	      x0 += paddingLeft(node) - p;
	      y0 += paddingTop(node) - p;
	      x1 -= paddingRight(node) - p;
	      y1 -= paddingBottom(node) - p;
	      if (x1 < x0) x0 = x1 = (x0 + x1) / 2;
	      if (y1 < y0) y0 = y1 = (y0 + y1) / 2;
	      tile(node, x0, y0, x1, y1);
	    }
	  }

	  treemap.round = function(x) {
	    return arguments.length ? (round = !!x, treemap) : round;
	  };

	  treemap.size = function(x) {
	    return arguments.length ? (dx = +x[0], dy = +x[1], treemap) : [dx, dy];
	  };

	  treemap.tile = function(x) {
	    return arguments.length ? (tile = required(x), treemap) : tile;
	  };

	  treemap.padding = function(x) {
	    return arguments.length ? treemap.paddingInner(x).paddingOuter(x) : treemap.paddingInner();
	  };

	  treemap.paddingInner = function(x) {
	    return arguments.length ? (paddingInner = typeof x === "function" ? x : constant$8(+x), treemap) : paddingInner;
	  };

	  treemap.paddingOuter = function(x) {
	    return arguments.length ? treemap.paddingTop(x).paddingRight(x).paddingBottom(x).paddingLeft(x) : treemap.paddingTop();
	  };

	  treemap.paddingTop = function(x) {
	    return arguments.length ? (paddingTop = typeof x === "function" ? x : constant$8(+x), treemap) : paddingTop;
	  };

	  treemap.paddingRight = function(x) {
	    return arguments.length ? (paddingRight = typeof x === "function" ? x : constant$8(+x), treemap) : paddingRight;
	  };

	  treemap.paddingBottom = function(x) {
	    return arguments.length ? (paddingBottom = typeof x === "function" ? x : constant$8(+x), treemap) : paddingBottom;
	  };

	  treemap.paddingLeft = function(x) {
	    return arguments.length ? (paddingLeft = typeof x === "function" ? x : constant$8(+x), treemap) : paddingLeft;
	  };

	  return treemap;
	}

	function binary(parent, x0, y0, x1, y1) {
	  var nodes = parent.children,
	      i, n = nodes.length,
	      sum, sums = new Array(n + 1);

	  for (sums[0] = sum = i = 0; i < n; ++i) {
	    sums[i + 1] = sum += nodes[i].value;
	  }

	  partition(0, n, parent.value, x0, y0, x1, y1);

	  function partition(i, j, value, x0, y0, x1, y1) {
	    if (i >= j - 1) {
	      var node = nodes[i];
	      node.x0 = x0, node.y0 = y0;
	      node.x1 = x1, node.y1 = y1;
	      return;
	    }

	    var valueOffset = sums[i],
	        valueTarget = (value / 2) + valueOffset,
	        k = i + 1,
	        hi = j - 1;

	    while (k < hi) {
	      var mid = k + hi >>> 1;
	      if (sums[mid] < valueTarget) k = mid + 1;
	      else hi = mid;
	    }

	    if ((valueTarget - sums[k - 1]) < (sums[k] - valueTarget) && i + 1 < k) --k;

	    var valueLeft = sums[k] - valueOffset,
	        valueRight = value - valueLeft;

	    if ((x1 - x0) > (y1 - y0)) {
	      var xk = (x0 * valueRight + x1 * valueLeft) / value;
	      partition(i, k, valueLeft, x0, y0, xk, y1);
	      partition(k, j, valueRight, xk, y0, x1, y1);
	    } else {
	      var yk = (y0 * valueRight + y1 * valueLeft) / value;
	      partition(i, k, valueLeft, x0, y0, x1, yk);
	      partition(k, j, valueRight, x0, yk, x1, y1);
	    }
	  }
	}

	function sliceDice(parent, x0, y0, x1, y1) {
	  (parent.depth & 1 ? treemapSlice : treemapDice)(parent, x0, y0, x1, y1);
	}

	var resquarify = (function custom(ratio) {

	  function resquarify(parent, x0, y0, x1, y1) {
	    if ((rows = parent._squarify) && (rows.ratio === ratio)) {
	      var rows,
	          row,
	          nodes,
	          i,
	          j = -1,
	          n,
	          m = rows.length,
	          value = parent.value;

	      while (++j < m) {
	        row = rows[j], nodes = row.children;
	        for (i = row.value = 0, n = nodes.length; i < n; ++i) row.value += nodes[i].value;
	        if (row.dice) treemapDice(row, x0, y0, x1, y0 += (y1 - y0) * row.value / value);
	        else treemapSlice(row, x0, y0, x0 += (x1 - x0) * row.value / value, y1);
	        value -= row.value;
	      }
	    } else {
	      parent._squarify = rows = squarifyRatio(ratio, parent, x0, y0, x1, y1);
	      rows.ratio = ratio;
	    }
	  }

	  resquarify.ratio = function(x) {
	    return custom((x = +x) > 1 ? x : 1);
	  };

	  return resquarify;
	})(phi);

	function area$1(polygon) {
	  var i = -1,
	      n = polygon.length,
	      a,
	      b = polygon[n - 1],
	      area = 0;

	  while (++i < n) {
	    a = b;
	    b = polygon[i];
	    area += a[1] * b[0] - a[0] * b[1];
	  }

	  return area / 2;
	}

	function centroid$1(polygon) {
	  var i = -1,
	      n = polygon.length,
	      x = 0,
	      y = 0,
	      a,
	      b = polygon[n - 1],
	      c,
	      k = 0;

	  while (++i < n) {
	    a = b;
	    b = polygon[i];
	    k += c = a[0] * b[1] - b[0] * a[1];
	    x += (a[0] + b[0]) * c;
	    y += (a[1] + b[1]) * c;
	  }

	  return k *= 3, [x / k, y / k];
	}

	// Returns the 2D cross product of AB and AC vectors, i.e., the z-component of
	// the 3D cross product in a quadrant I Cartesian coordinate system (+x is
	// right, +y is up). Returns a positive value if ABC is counter-clockwise,
	// negative if clockwise, and zero if the points are collinear.
	function cross$1(a, b, c) {
	  return (b[0] - a[0]) * (c[1] - a[1]) - (b[1] - a[1]) * (c[0] - a[0]);
	}

	function lexicographicOrder(a, b) {
	  return a[0] - b[0] || a[1] - b[1];
	}

	// Computes the upper convex hull per the monotone chain algorithm.
	// Assumes points.length >= 3, is sorted by x, unique in y.
	// Returns an array of indices into points in left-to-right order.
	function computeUpperHullIndexes(points) {
	  var n = points.length,
	      indexes = [0, 1],
	      size = 2;

	  for (var i = 2; i < n; ++i) {
	    while (size > 1 && cross$1(points[indexes[size - 2]], points[indexes[size - 1]], points[i]) <= 0) --size;
	    indexes[size++] = i;
	  }

	  return indexes.slice(0, size); // remove popped points
	}

	function hull(points) {
	  if ((n = points.length) < 3) return null;

	  var i,
	      n,
	      sortedPoints = new Array(n),
	      flippedPoints = new Array(n);

	  for (i = 0; i < n; ++i) sortedPoints[i] = [+points[i][0], +points[i][1], i];
	  sortedPoints.sort(lexicographicOrder);
	  for (i = 0; i < n; ++i) flippedPoints[i] = [sortedPoints[i][0], -sortedPoints[i][1]];

	  var upperIndexes = computeUpperHullIndexes(sortedPoints),
	      lowerIndexes = computeUpperHullIndexes(flippedPoints);

	  // Construct the hull polygon, removing possible duplicate endpoints.
	  var skipLeft = lowerIndexes[0] === upperIndexes[0],
	      skipRight = lowerIndexes[lowerIndexes.length - 1] === upperIndexes[upperIndexes.length - 1],
	      hull = [];

	  // Add upper hull in right-to-l order.
	  // Then add lower hull in left-to-right order.
	  for (i = upperIndexes.length - 1; i >= 0; --i) hull.push(points[sortedPoints[upperIndexes[i]][2]]);
	  for (i = +skipLeft; i < lowerIndexes.length - skipRight; ++i) hull.push(points[sortedPoints[lowerIndexes[i]][2]]);

	  return hull;
	}

	function contains$1(polygon, point) {
	  var n = polygon.length,
	      p = polygon[n - 1],
	      x = point[0], y = point[1],
	      x0 = p[0], y0 = p[1],
	      x1, y1,
	      inside = false;

	  for (var i = 0; i < n; ++i) {
	    p = polygon[i], x1 = p[0], y1 = p[1];
	    if (((y1 > y) !== (y0 > y)) && (x < (x0 - x1) * (y - y1) / (y0 - y1) + x1)) inside = !inside;
	    x0 = x1, y0 = y1;
	  }

	  return inside;
	}

	function length$2(polygon) {
	  var i = -1,
	      n = polygon.length,
	      b = polygon[n - 1],
	      xa,
	      ya,
	      xb = b[0],
	      yb = b[1],
	      perimeter = 0;

	  while (++i < n) {
	    xa = xb;
	    ya = yb;
	    b = polygon[i];
	    xb = b[0];
	    yb = b[1];
	    xa -= xb;
	    ya -= yb;
	    perimeter += Math.sqrt(xa * xa + ya * ya);
	  }

	  return perimeter;
	}

	var slice$4 = [].slice;

	var noabort = {};

	function Queue(size) {
	  this._size = size;
	  this._call =
	  this._error = null;
	  this._tasks = [];
	  this._data = [];
	  this._waiting =
	  this._active =
	  this._ended =
	  this._start = 0; // inside a synchronous task callback?
	}

	Queue.prototype = queue.prototype = {
	  constructor: Queue,
	  defer: function(callback) {
	    if (typeof callback !== "function") throw new Error("invalid callback");
	    if (this._call) throw new Error("defer after await");
	    if (this._error != null) return this;
	    var t = slice$4.call(arguments, 1);
	    t.push(callback);
	    ++this._waiting, this._tasks.push(t);
	    poke$1(this);
	    return this;
	  },
	  abort: function() {
	    if (this._error == null) abort(this, new Error("abort"));
	    return this;
	  },
	  await: function(callback) {
	    if (typeof callback !== "function") throw new Error("invalid callback");
	    if (this._call) throw new Error("multiple await");
	    this._call = function(error, results) { callback.apply(null, [error].concat(results)); };
	    maybeNotify(this);
	    return this;
	  },
	  awaitAll: function(callback) {
	    if (typeof callback !== "function") throw new Error("invalid callback");
	    if (this._call) throw new Error("multiple await");
	    this._call = callback;
	    maybeNotify(this);
	    return this;
	  }
	};

	function poke$1(q) {
	  if (!q._start) {
	    try { start$1(q); } // let the current task complete
	    catch (e) {
	      if (q._tasks[q._ended + q._active - 1]) abort(q, e); // task errored synchronously
	      else if (!q._data) throw e; // await callback errored synchronously
	    }
	  }
	}

	function start$1(q) {
	  while (q._start = q._waiting && q._active < q._size) {
	    var i = q._ended + q._active,
	        t = q._tasks[i],
	        j = t.length - 1,
	        c = t[j];
	    t[j] = end(q, i);
	    --q._waiting, ++q._active;
	    t = c.apply(null, t);
	    if (!q._tasks[i]) continue; // task finished synchronously
	    q._tasks[i] = t || noabort;
	  }
	}

	function end(q, i) {
	  return function(e, r) {
	    if (!q._tasks[i]) return; // ignore multiple callbacks
	    --q._active, ++q._ended;
	    q._tasks[i] = null;
	    if (q._error != null) return; // ignore secondary errors
	    if (e != null) {
	      abort(q, e);
	    } else {
	      q._data[i] = r;
	      if (q._waiting) poke$1(q);
	      else maybeNotify(q);
	    }
	  };
	}

	function abort(q, e) {
	  var i = q._tasks.length, t;
	  q._error = e; // ignore active callbacks
	  q._data = undefined; // allow gc
	  q._waiting = NaN; // prevent starting

	  while (--i >= 0) {
	    if (t = q._tasks[i]) {
	      q._tasks[i] = null;
	      if (t.abort) {
	        try { t.abort(); }
	        catch (e) { /* ignore */ }
	      }
	    }
	  }

	  q._active = NaN; // allow notification
	  maybeNotify(q);
	}

	function maybeNotify(q) {
	  if (!q._active && q._call) {
	    var d = q._data;
	    q._data = undefined; // allow gc
	    q._call(q._error, d);
	  }
	}

	function queue(concurrency) {
	  if (concurrency == null) concurrency = Infinity;
	  else if (!((concurrency = +concurrency) >= 1)) throw new Error("invalid concurrency");
	  return new Queue(concurrency);
	}

	function defaultSource$1() {
	  return Math.random();
	}

	var uniform = (function sourceRandomUniform(source) {
	  function randomUniform(min, max) {
	    min = min == null ? 0 : +min;
	    max = max == null ? 1 : +max;
	    if (arguments.length === 1) max = min, min = 0;
	    else max -= min;
	    return function() {
	      return source() * max + min;
	    };
	  }

	  randomUniform.source = sourceRandomUniform;

	  return randomUniform;
	})(defaultSource$1);

	var normal = (function sourceRandomNormal(source) {
	  function randomNormal(mu, sigma) {
	    var x, r;
	    mu = mu == null ? 0 : +mu;
	    sigma = sigma == null ? 1 : +sigma;
	    return function() {
	      var y;

	      // If available, use the second previously-generated uniform random.
	      if (x != null) y = x, x = null;

	      // Otherwise, generate a new x and y.
	      else do {
	        x = source() * 2 - 1;
	        y = source() * 2 - 1;
	        r = x * x + y * y;
	      } while (!r || r > 1);

	      return mu + sigma * y * Math.sqrt(-2 * Math.log(r) / r);
	    };
	  }

	  randomNormal.source = sourceRandomNormal;

	  return randomNormal;
	})(defaultSource$1);

	var logNormal = (function sourceRandomLogNormal(source) {
	  function randomLogNormal() {
	    var randomNormal = normal.source(source).apply(this, arguments);
	    return function() {
	      return Math.exp(randomNormal());
	    };
	  }

	  randomLogNormal.source = sourceRandomLogNormal;

	  return randomLogNormal;
	})(defaultSource$1);

	var irwinHall = (function sourceRandomIrwinHall(source) {
	  function randomIrwinHall(n) {
	    return function() {
	      for (var sum = 0, i = 0; i < n; ++i) sum += source();
	      return sum;
	    };
	  }

	  randomIrwinHall.source = sourceRandomIrwinHall;

	  return randomIrwinHall;
	})(defaultSource$1);

	var bates = (function sourceRandomBates(source) {
	  function randomBates(n) {
	    var randomIrwinHall = irwinHall.source(source)(n);
	    return function() {
	      return randomIrwinHall() / n;
	    };
	  }

	  randomBates.source = sourceRandomBates;

	  return randomBates;
	})(defaultSource$1);

	var exponential$1 = (function sourceRandomExponential(source) {
	  function randomExponential(lambda) {
	    return function() {
	      return -Math.log(1 - source()) / lambda;
	    };
	  }

	  randomExponential.source = sourceRandomExponential;

	  return randomExponential;
	})(defaultSource$1);

	function request(url, callback) {
	  var request,
	      event = dispatch("beforesend", "progress", "load", "error"),
	      mimeType,
	      headers = map$1(),
	      xhr = new XMLHttpRequest,
	      user = null,
	      password = null,
	      response,
	      responseType,
	      timeout = 0;

	  // If IE does not support CORS, use XDomainRequest.
	  if (typeof XDomainRequest !== "undefined"
	      && !("withCredentials" in xhr)
	      && /^(http(s)?:)?\/\//.test(url)) xhr = new XDomainRequest;

	  "onload" in xhr
	      ? xhr.onload = xhr.onerror = xhr.ontimeout = respond
	      : xhr.onreadystatechange = function(o) { xhr.readyState > 3 && respond(o); };

	  function respond(o) {
	    var status = xhr.status, result;
	    if (!status && hasResponse(xhr)
	        || status >= 200 && status < 300
	        || status === 304) {
	      if (response) {
	        try {
	          result = response.call(request, xhr);
	        } catch (e) {
	          event.call("error", request, e);
	          return;
	        }
	      } else {
	        result = xhr;
	      }
	      event.call("load", request, result);
	    } else {
	      event.call("error", request, o);
	    }
	  }

	  xhr.onprogress = function(e) {
	    event.call("progress", request, e);
	  };

	  request = {
	    header: function(name, value) {
	      name = (name + "").toLowerCase();
	      if (arguments.length < 2) return headers.get(name);
	      if (value == null) headers.remove(name);
	      else headers.set(name, value + "");
	      return request;
	    },

	    // If mimeType is non-null and no Accept header is set, a default is used.
	    mimeType: function(value) {
	      if (!arguments.length) return mimeType;
	      mimeType = value == null ? null : value + "";
	      return request;
	    },

	    // Specifies what type the response value should take;
	    // for instance, arraybuffer, blob, document, or text.
	    responseType: function(value) {
	      if (!arguments.length) return responseType;
	      responseType = value;
	      return request;
	    },

	    timeout: function(value) {
	      if (!arguments.length) return timeout;
	      timeout = +value;
	      return request;
	    },

	    user: function(value) {
	      return arguments.length < 1 ? user : (user = value == null ? null : value + "", request);
	    },

	    password: function(value) {
	      return arguments.length < 1 ? password : (password = value == null ? null : value + "", request);
	    },

	    // Specify how to convert the response content to a specific type;
	    // changes the callback value on "load" events.
	    response: function(value) {
	      response = value;
	      return request;
	    },

	    // Alias for send("GET", …).
	    get: function(data, callback) {
	      return request.send("GET", data, callback);
	    },

	    // Alias for send("POST", …).
	    post: function(data, callback) {
	      return request.send("POST", data, callback);
	    },

	    // If callback is non-null, it will be used for error and load events.
	    send: function(method, data, callback) {
	      xhr.open(method, url, true, user, password);
	      if (mimeType != null && !headers.has("accept")) headers.set("accept", mimeType + ",*/*");
	      if (xhr.setRequestHeader) headers.each(function(value, name) { xhr.setRequestHeader(name, value); });
	      if (mimeType != null && xhr.overrideMimeType) xhr.overrideMimeType(mimeType);
	      if (responseType != null) xhr.responseType = responseType;
	      if (timeout > 0) xhr.timeout = timeout;
	      if (callback == null && typeof data === "function") callback = data, data = null;
	      if (callback != null && callback.length === 1) callback = fixCallback(callback);
	      if (callback != null) request.on("error", callback).on("load", function(xhr) { callback(null, xhr); });
	      event.call("beforesend", request, xhr);
	      xhr.send(data == null ? null : data);
	      return request;
	    },

	    abort: function() {
	      xhr.abort();
	      return request;
	    },

	    on: function() {
	      var value = event.on.apply(event, arguments);
	      return value === event ? request : value;
	    }
	  };

	  if (callback != null) {
	    if (typeof callback !== "function") throw new Error("invalid callback: " + callback);
	    return request.get(callback);
	  }

	  return request;
	}

	function fixCallback(callback) {
	  return function(error, xhr) {
	    callback(error == null ? xhr : null);
	  };
	}

	function hasResponse(xhr) {
	  var type = xhr.responseType;
	  return type && type !== "text"
	      ? xhr.response // null on error
	      : xhr.responseText; // "" on error
	}

	function type$1(defaultMimeType, response) {
	  return function(url, callback) {
	    var r = request(url).mimeType(defaultMimeType).response(response);
	    if (callback != null) {
	      if (typeof callback !== "function") throw new Error("invalid callback: " + callback);
	      return r.get(callback);
	    }
	    return r;
	  };
	}

	var html = type$1("text/html", function(xhr) {
	  return document.createRange().createContextualFragment(xhr.responseText);
	});

	var json = type$1("application/json", function(xhr) {
	  return JSON.parse(xhr.responseText);
	});

	var text = type$1("text/plain", function(xhr) {
	  return xhr.responseText;
	});

	var xml = type$1("application/xml", function(xhr) {
	  var xml = xhr.responseXML;
	  if (!xml) throw new Error("parse error");
	  return xml;
	});

	function dsv$1(defaultMimeType, parse) {
	  return function(url, row, callback) {
	    if (arguments.length < 3) callback = row, row = null;
	    var r = request(url).mimeType(defaultMimeType);
	    r.row = function(_) { return arguments.length ? r.response(responseOf(parse, row = _)) : row; };
	    r.row(row);
	    return callback ? r.get(callback) : r;
	  };
	}

	function responseOf(parse, row) {
	  return function(request$$1) {
	    return parse(request$$1.responseText, row);
	  };
	}

	var csv$1 = dsv$1("text/csv", csvParse);

	var tsv$1 = dsv$1("text/tab-separated-values", tsvParse);

	var array$2 = Array.prototype;

	var map$3 = array$2.map;
	var slice$5 = array$2.slice;

	var implicit = {name: "implicit"};

	function ordinal(range) {
	  var index = map$1(),
	      domain = [],
	      unknown = implicit;

	  range = range == null ? [] : slice$5.call(range);

	  function scale(d) {
	    var key = d + "", i = index.get(key);
	    if (!i) {
	      if (unknown !== implicit) return unknown;
	      index.set(key, i = domain.push(d));
	    }
	    return range[(i - 1) % range.length];
	  }

	  scale.domain = function(_) {
	    if (!arguments.length) return domain.slice();
	    domain = [], index = map$1();
	    var i = -1, n = _.length, d, key;
	    while (++i < n) if (!index.has(key = (d = _[i]) + "")) index.set(key, domain.push(d));
	    return scale;
	  };

	  scale.range = function(_) {
	    return arguments.length ? (range = slice$5.call(_), scale) : range.slice();
	  };

	  scale.unknown = function(_) {
	    return arguments.length ? (unknown = _, scale) : unknown;
	  };

	  scale.copy = function() {
	    return ordinal()
	        .domain(domain)
	        .range(range)
	        .unknown(unknown);
	  };

	  return scale;
	}

	function band() {
	  var scale = ordinal().unknown(undefined),
	      domain = scale.domain,
	      ordinalRange = scale.range,
	      range$$1 = [0, 1],
	      step,
	      bandwidth,
	      round = false,
	      paddingInner = 0,
	      paddingOuter = 0,
	      align = 0.5;

	  delete scale.unknown;

	  function rescale() {
	    var n = domain().length,
	        reverse = range$$1[1] < range$$1[0],
	        start = range$$1[reverse - 0],
	        stop = range$$1[1 - reverse];
	    step = (stop - start) / Math.max(1, n - paddingInner + paddingOuter * 2);
	    if (round) step = Math.floor(step);
	    start += (stop - start - step * (n - paddingInner)) * align;
	    bandwidth = step * (1 - paddingInner);
	    if (round) start = Math.round(start), bandwidth = Math.round(bandwidth);
	    var values = sequence(n).map(function(i) { return start + step * i; });
	    return ordinalRange(reverse ? values.reverse() : values);
	  }

	  scale.domain = function(_) {
	    return arguments.length ? (domain(_), rescale()) : domain();
	  };

	  scale.range = function(_) {
	    return arguments.length ? (range$$1 = [+_[0], +_[1]], rescale()) : range$$1.slice();
	  };

	  scale.rangeRound = function(_) {
	    return range$$1 = [+_[0], +_[1]], round = true, rescale();
	  };

	  scale.bandwidth = function() {
	    return bandwidth;
	  };

	  scale.step = function() {
	    return step;
	  };

	  scale.round = function(_) {
	    return arguments.length ? (round = !!_, rescale()) : round;
	  };

	  scale.padding = function(_) {
	    return arguments.length ? (paddingInner = paddingOuter = Math.max(0, Math.min(1, _)), rescale()) : paddingInner;
	  };

	  scale.paddingInner = function(_) {
	    return arguments.length ? (paddingInner = Math.max(0, Math.min(1, _)), rescale()) : paddingInner;
	  };

	  scale.paddingOuter = function(_) {
	    return arguments.length ? (paddingOuter = Math.max(0, Math.min(1, _)), rescale()) : paddingOuter;
	  };

	  scale.align = function(_) {
	    return arguments.length ? (align = Math.max(0, Math.min(1, _)), rescale()) : align;
	  };

	  scale.copy = function() {
	    return band()
	        .domain(domain())
	        .range(range$$1)
	        .round(round)
	        .paddingInner(paddingInner)
	        .paddingOuter(paddingOuter)
	        .align(align);
	  };

	  return rescale();
	}

	function pointish(scale) {
	  var copy = scale.copy;

	  scale.padding = scale.paddingOuter;
	  delete scale.paddingInner;
	  delete scale.paddingOuter;

	  scale.copy = function() {
	    return pointish(copy());
	  };

	  return scale;
	}

	function point$1() {
	  return pointish(band().paddingInner(1));
	}

	function constant$9(x) {
	  return function() {
	    return x;
	  };
	}

	function number$2(x) {
	  return +x;
	}

	var unit = [0, 1];

	function deinterpolateLinear(a, b) {
	  return (b -= (a = +a))
	      ? function(x) { return (x - a) / b; }
	      : constant$9(b);
	}

	function deinterpolateClamp(deinterpolate) {
	  return function(a, b) {
	    var d = deinterpolate(a = +a, b = +b);
	    return function(x) { return x <= a ? 0 : x >= b ? 1 : d(x); };
	  };
	}

	function reinterpolateClamp(reinterpolate) {
	  return function(a, b) {
	    var r = reinterpolate(a = +a, b = +b);
	    return function(t) { return t <= 0 ? a : t >= 1 ? b : r(t); };
	  };
	}

	function bimap(domain, range, deinterpolate, reinterpolate) {
	  var d0 = domain[0], d1 = domain[1], r0 = range[0], r1 = range[1];
	  if (d1 < d0) d0 = deinterpolate(d1, d0), r0 = reinterpolate(r1, r0);
	  else d0 = deinterpolate(d0, d1), r0 = reinterpolate(r0, r1);
	  return function(x) { return r0(d0(x)); };
	}

	function polymap(domain, range, deinterpolate, reinterpolate) {
	  var j = Math.min(domain.length, range.length) - 1,
	      d = new Array(j),
	      r = new Array(j),
	      i = -1;

	  // Reverse descending domains.
	  if (domain[j] < domain[0]) {
	    domain = domain.slice().reverse();
	    range = range.slice().reverse();
	  }

	  while (++i < j) {
	    d[i] = deinterpolate(domain[i], domain[i + 1]);
	    r[i] = reinterpolate(range[i], range[i + 1]);
	  }

	  return function(x) {
	    var i = bisectRight(domain, x, 1, j) - 1;
	    return r[i](d[i](x));
	  };
	}

	function copy(source, target) {
	  return target
	      .domain(source.domain())
	      .range(source.range())
	      .interpolate(source.interpolate())
	      .clamp(source.clamp());
	}

	// deinterpolate(a, b)(x) takes a domain value x in [a,b] and returns the corresponding parameter t in [0,1].
	// reinterpolate(a, b)(t) takes a parameter t in [0,1] and returns the corresponding domain value x in [a,b].
	function continuous(deinterpolate, reinterpolate) {
	  var domain = unit,
	      range = unit,
	      interpolate$$1 = interpolateValue,
	      clamp = false,
	      piecewise,
	      output,
	      input;

	  function rescale() {
	    piecewise = Math.min(domain.length, range.length) > 2 ? polymap : bimap;
	    output = input = null;
	    return scale;
	  }

	  function scale(x) {
	    return (output || (output = piecewise(domain, range, clamp ? deinterpolateClamp(deinterpolate) : deinterpolate, interpolate$$1)))(+x);
	  }

	  scale.invert = function(y) {
	    return (input || (input = piecewise(range, domain, deinterpolateLinear, clamp ? reinterpolateClamp(reinterpolate) : reinterpolate)))(+y);
	  };

	  scale.domain = function(_) {
	    return arguments.length ? (domain = map$3.call(_, number$2), rescale()) : domain.slice();
	  };

	  scale.range = function(_) {
	    return arguments.length ? (range = slice$5.call(_), rescale()) : range.slice();
	  };

	  scale.rangeRound = function(_) {
	    return range = slice$5.call(_), interpolate$$1 = interpolateRound, rescale();
	  };

	  scale.clamp = function(_) {
	    return arguments.length ? (clamp = !!_, rescale()) : clamp;
	  };

	  scale.interpolate = function(_) {
	    return arguments.length ? (interpolate$$1 = _, rescale()) : interpolate$$1;
	  };

	  return rescale();
	}

	function tickFormat(domain, count, specifier) {
	  var start = domain[0],
	      stop = domain[domain.length - 1],
	      step = tickStep(start, stop, count == null ? 10 : count),
	      precision;
	  specifier = formatSpecifier(specifier == null ? ",f" : specifier);
	  switch (specifier.type) {
	    case "s": {
	      var value = Math.max(Math.abs(start), Math.abs(stop));
	      if (specifier.precision == null && !isNaN(precision = precisionPrefix(step, value))) specifier.precision = precision;
	      return exports.formatPrefix(specifier, value);
	    }
	    case "":
	    case "e":
	    case "g":
	    case "p":
	    case "r": {
	      if (specifier.precision == null && !isNaN(precision = precisionRound(step, Math.max(Math.abs(start), Math.abs(stop))))) specifier.precision = precision - (specifier.type === "e");
	      break;
	    }
	    case "f":
	    case "%": {
	      if (specifier.precision == null && !isNaN(precision = precisionFixed(step))) specifier.precision = precision - (specifier.type === "%") * 2;
	      break;
	    }
	  }
	  return exports.format(specifier);
	}

	function linearish(scale) {
	  var domain = scale.domain;

	  scale.ticks = function(count) {
	    var d = domain();
	    return ticks(d[0], d[d.length - 1], count == null ? 10 : count);
	  };

	  scale.tickFormat = function(count, specifier) {
	    return tickFormat(domain(), count, specifier);
	  };

	  scale.nice = function(count) {
	    if (count == null) count = 10;

	    var d = domain(),
	        i0 = 0,
	        i1 = d.length - 1,
	        start = d[i0],
	        stop = d[i1],
	        step;

	    if (stop < start) {
	      step = start, start = stop, stop = step;
	      step = i0, i0 = i1, i1 = step;
	    }

	    step = tickIncrement(start, stop, count);

	    if (step > 0) {
	      start = Math.floor(start / step) * step;
	      stop = Math.ceil(stop / step) * step;
	      step = tickIncrement(start, stop, count);
	    } else if (step < 0) {
	      start = Math.ceil(start * step) / step;
	      stop = Math.floor(stop * step) / step;
	      step = tickIncrement(start, stop, count);
	    }

	    if (step > 0) {
	      d[i0] = Math.floor(start / step) * step;
	      d[i1] = Math.ceil(stop / step) * step;
	      domain(d);
	    } else if (step < 0) {
	      d[i0] = Math.ceil(start * step) / step;
	      d[i1] = Math.floor(stop * step) / step;
	      domain(d);
	    }

	    return scale;
	  };

	  return scale;
	}

	function linear$2() {
	  var scale = continuous(deinterpolateLinear, reinterpolate);

	  scale.copy = function() {
	    return copy(scale, linear$2());
	  };

	  return linearish(scale);
	}

	function identity$6() {
	  var domain = [0, 1];

	  function scale(x) {
	    return +x;
	  }

	  scale.invert = scale;

	  scale.domain = scale.range = function(_) {
	    return arguments.length ? (domain = map$3.call(_, number$2), scale) : domain.slice();
	  };

	  scale.copy = function() {
	    return identity$6().domain(domain);
	  };

	  return linearish(scale);
	}

	function nice(domain, interval) {
	  domain = domain.slice();

	  var i0 = 0,
	      i1 = domain.length - 1,
	      x0 = domain[i0],
	      x1 = domain[i1],
	      t;

	  if (x1 < x0) {
	    t = i0, i0 = i1, i1 = t;
	    t = x0, x0 = x1, x1 = t;
	  }

	  domain[i0] = interval.floor(x0);
	  domain[i1] = interval.ceil(x1);
	  return domain;
	}

	function deinterpolate(a, b) {
	  return (b = Math.log(b / a))
	      ? function(x) { return Math.log(x / a) / b; }
	      : constant$9(b);
	}

	function reinterpolate$1(a, b) {
	  return a < 0
	      ? function(t) { return -Math.pow(-b, t) * Math.pow(-a, 1 - t); }
	      : function(t) { return Math.pow(b, t) * Math.pow(a, 1 - t); };
	}

	function pow10(x) {
	  return isFinite(x) ? +("1e" + x) : x < 0 ? 0 : x;
	}

	function powp(base) {
	  return base === 10 ? pow10
	      : base === Math.E ? Math.exp
	      : function(x) { return Math.pow(base, x); };
	}

	function logp(base) {
	  return base === Math.E ? Math.log
	      : base === 10 && Math.log10
	      || base === 2 && Math.log2
	      || (base = Math.log(base), function(x) { return Math.log(x) / base; });
	}

	function reflect(f) {
	  return function(x) {
	    return -f(-x);
	  };
	}

	function log$1() {
	  var scale = continuous(deinterpolate, reinterpolate$1).domain([1, 10]),
	      domain = scale.domain,
	      base = 10,
	      logs = logp(10),
	      pows = powp(10);

	  function rescale() {
	    logs = logp(base), pows = powp(base);
	    if (domain()[0] < 0) logs = reflect(logs), pows = reflect(pows);
	    return scale;
	  }

	  scale.base = function(_) {
	    return arguments.length ? (base = +_, rescale()) : base;
	  };

	  scale.domain = function(_) {
	    return arguments.length ? (domain(_), rescale()) : domain();
	  };

	  scale.ticks = function(count) {
	    var d = domain(),
	        u = d[0],
	        v = d[d.length - 1],
	        r;

	    if (r = v < u) i = u, u = v, v = i;

	    var i = logs(u),
	        j = logs(v),
	        p,
	        k,
	        t,
	        n = count == null ? 10 : +count,
	        z = [];

	    if (!(base % 1) && j - i < n) {
	      i = Math.round(i) - 1, j = Math.round(j) + 1;
	      if (u > 0) for (; i < j; ++i) {
	        for (k = 1, p = pows(i); k < base; ++k) {
	          t = p * k;
	          if (t < u) continue;
	          if (t > v) break;
	          z.push(t);
	        }
	      } else for (; i < j; ++i) {
	        for (k = base - 1, p = pows(i); k >= 1; --k) {
	          t = p * k;
	          if (t < u) continue;
	          if (t > v) break;
	          z.push(t);
	        }
	      }
	    } else {
	      z = ticks(i, j, Math.min(j - i, n)).map(pows);
	    }

	    return r ? z.reverse() : z;
	  };

	  scale.tickFormat = function(count, specifier) {
	    if (specifier == null) specifier = base === 10 ? ".0e" : ",";
	    if (typeof specifier !== "function") specifier = exports.format(specifier);
	    if (count === Infinity) return specifier;
	    if (count == null) count = 10;
	    var k = Math.max(1, base * count / scale.ticks().length); // TODO fast estimate?
	    return function(d) {
	      var i = d / pows(Math.round(logs(d)));
	      if (i * base < base - 0.5) i *= base;
	      return i <= k ? specifier(d) : "";
	    };
	  };

	  scale.nice = function() {
	    return domain(nice(domain(), {
	      floor: function(x) { return pows(Math.floor(logs(x))); },
	      ceil: function(x) { return pows(Math.ceil(logs(x))); }
	    }));
	  };

	  scale.copy = function() {
	    return copy(scale, log$1().base(base));
	  };

	  return scale;
	}

	function raise$1(x, exponent) {
	  return x < 0 ? -Math.pow(-x, exponent) : Math.pow(x, exponent);
	}

	function pow$1() {
	  var exponent = 1,
	      scale = continuous(deinterpolate, reinterpolate),
	      domain = scale.domain;

	  function deinterpolate(a, b) {
	    return (b = raise$1(b, exponent) - (a = raise$1(a, exponent)))
	        ? function(x) { return (raise$1(x, exponent) - a) / b; }
	        : constant$9(b);
	  }

	  function reinterpolate(a, b) {
	    b = raise$1(b, exponent) - (a = raise$1(a, exponent));
	    return function(t) { return raise$1(a + b * t, 1 / exponent); };
	  }

	  scale.exponent = function(_) {
	    return arguments.length ? (exponent = +_, domain(domain())) : exponent;
	  };

	  scale.copy = function() {
	    return copy(scale, pow$1().exponent(exponent));
	  };

	  return linearish(scale);
	}

	function sqrt$1() {
	  return pow$1().exponent(0.5);
	}

	function quantile$$1() {
	  var domain = [],
	      range = [],
	      thresholds = [];

	  function rescale() {
	    var i = 0, n = Math.max(1, range.length);
	    thresholds = new Array(n - 1);
	    while (++i < n) thresholds[i - 1] = threshold(domain, i / n);
	    return scale;
	  }

	  function scale(x) {
	    if (!isNaN(x = +x)) return range[bisectRight(thresholds, x)];
	  }

	  scale.invertExtent = function(y) {
	    var i = range.indexOf(y);
	    return i < 0 ? [NaN, NaN] : [
	      i > 0 ? thresholds[i - 1] : domain[0],
	      i < thresholds.length ? thresholds[i] : domain[domain.length - 1]
	    ];
	  };

	  scale.domain = function(_) {
	    if (!arguments.length) return domain.slice();
	    domain = [];
	    for (var i = 0, n = _.length, d; i < n; ++i) if (d = _[i], d != null && !isNaN(d = +d)) domain.push(d);
	    domain.sort(ascending);
	    return rescale();
	  };

	  scale.range = function(_) {
	    return arguments.length ? (range = slice$5.call(_), rescale()) : range.slice();
	  };

	  scale.quantiles = function() {
	    return thresholds.slice();
	  };

	  scale.copy = function() {
	    return quantile$$1()
	        .domain(domain)
	        .range(range);
	  };

	  return scale;
	}

	function quantize$1() {
	  var x0 = 0,
	      x1 = 1,
	      n = 1,
	      domain = [0.5],
	      range = [0, 1];

	  function scale(x) {
	    if (x <= x) return range[bisectRight(domain, x, 0, n)];
	  }

	  function rescale() {
	    var i = -1;
	    domain = new Array(n);
	    while (++i < n) domain[i] = ((i + 1) * x1 - (i - n) * x0) / (n + 1);
	    return scale;
	  }

	  scale.domain = function(_) {
	    return arguments.length ? (x0 = +_[0], x1 = +_[1], rescale()) : [x0, x1];
	  };

	  scale.range = function(_) {
	    return arguments.length ? (n = (range = slice$5.call(_)).length - 1, rescale()) : range.slice();
	  };

	  scale.invertExtent = function(y) {
	    var i = range.indexOf(y);
	    return i < 0 ? [NaN, NaN]
	        : i < 1 ? [x0, domain[0]]
	        : i >= n ? [domain[n - 1], x1]
	        : [domain[i - 1], domain[i]];
	  };

	  scale.copy = function() {
	    return quantize$1()
	        .domain([x0, x1])
	        .range(range);
	  };

	  return linearish(scale);
	}

	function threshold$1() {
	  var domain = [0.5],
	      range = [0, 1],
	      n = 1;

	  function scale(x) {
	    if (x <= x) return range[bisectRight(domain, x, 0, n)];
	  }

	  scale.domain = function(_) {
	    return arguments.length ? (domain = slice$5.call(_), n = Math.min(domain.length, range.length - 1), scale) : domain.slice();
	  };

	  scale.range = function(_) {
	    return arguments.length ? (range = slice$5.call(_), n = Math.min(domain.length, range.length - 1), scale) : range.slice();
	  };

	  scale.invertExtent = function(y) {
	    var i = range.indexOf(y);
	    return [domain[i - 1], domain[i]];
	  };

	  scale.copy = function() {
	    return threshold$1()
	        .domain(domain)
	        .range(range);
	  };

	  return scale;
	}

	var t0$1 = new Date;
	var t1$1 = new Date;

	function newInterval(floori, offseti, count, field) {

	  function interval(date) {
	    return floori(date = new Date(+date)), date;
	  }

	  interval.floor = interval;

	  interval.ceil = function(date) {
	    return floori(date = new Date(date - 1)), offseti(date, 1), floori(date), date;
	  };

	  interval.round = function(date) {
	    var d0 = interval(date),
	        d1 = interval.ceil(date);
	    return date - d0 < d1 - date ? d0 : d1;
	  };

	  interval.offset = function(date, step) {
	    return offseti(date = new Date(+date), step == null ? 1 : Math.floor(step)), date;
	  };

	  interval.range = function(start, stop, step) {
	    var range = [], previous;
	    start = interval.ceil(start);
	    step = step == null ? 1 : Math.floor(step);
	    if (!(start < stop) || !(step > 0)) return range; // also handles Invalid Date
	    do range.push(previous = new Date(+start)), offseti(start, step), floori(start);
	    while (previous < start && start < stop);
	    return range;
	  };

	  interval.filter = function(test) {
	    return newInterval(function(date) {
	      if (date >= date) while (floori(date), !test(date)) date.setTime(date - 1);
	    }, function(date, step) {
	      if (date >= date) {
	        if (step < 0) while (++step <= 0) {
	          while (offseti(date, -1), !test(date)) {} // eslint-disable-line no-empty
	        } else while (--step >= 0) {
	          while (offseti(date, +1), !test(date)) {} // eslint-disable-line no-empty
	        }
	      }
	    });
	  };

	  if (count) {
	    interval.count = function(start, end) {
	      t0$1.setTime(+start), t1$1.setTime(+end);
	      floori(t0$1), floori(t1$1);
	      return Math.floor(count(t0$1, t1$1));
	    };

	    interval.every = function(step) {
	      step = Math.floor(step);
	      return !isFinite(step) || !(step > 0) ? null
	          : !(step > 1) ? interval
	          : interval.filter(field
	              ? function(d) { return field(d) % step === 0; }
	              : function(d) { return interval.count(0, d) % step === 0; });
	    };
	  }

	  return interval;
	}

	var millisecond = newInterval(function() {
	  // noop
	}, function(date, step) {
	  date.setTime(+date + step);
	}, function(start, end) {
	  return end - start;
	});

	// An optimized implementation for this simple case.
	millisecond.every = function(k) {
	  k = Math.floor(k);
	  if (!isFinite(k) || !(k > 0)) return null;
	  if (!(k > 1)) return millisecond;
	  return newInterval(function(date) {
	    date.setTime(Math.floor(date / k) * k);
	  }, function(date, step) {
	    date.setTime(+date + step * k);
	  }, function(start, end) {
	    return (end - start) / k;
	  });
	};

	var milliseconds = millisecond.range;

	var durationSecond$1 = 1e3;
	var durationMinute$1 = 6e4;
	var durationHour$1 = 36e5;
	var durationDay$1 = 864e5;
	var durationWeek$1 = 6048e5;

	var second = newInterval(function(date) {
	  date.setTime(Math.floor(date / durationSecond$1) * durationSecond$1);
	}, function(date, step) {
	  date.setTime(+date + step * durationSecond$1);
	}, function(start, end) {
	  return (end - start) / durationSecond$1;
	}, function(date) {
	  return date.getUTCSeconds();
	});

	var seconds = second.range;

	var minute = newInterval(function(date) {
	  date.setTime(Math.floor(date / durationMinute$1) * durationMinute$1);
	}, function(date, step) {
	  date.setTime(+date + step * durationMinute$1);
	}, function(start, end) {
	  return (end - start) / durationMinute$1;
	}, function(date) {
	  return date.getMinutes();
	});

	var minutes = minute.range;

	var hour = newInterval(function(date) {
	  var offset = date.getTimezoneOffset() * durationMinute$1 % durationHour$1;
	  if (offset < 0) offset += durationHour$1;
	  date.setTime(Math.floor((+date - offset) / durationHour$1) * durationHour$1 + offset);
	}, function(date, step) {
	  date.setTime(+date + step * durationHour$1);
	}, function(start, end) {
	  return (end - start) / durationHour$1;
	}, function(date) {
	  return date.getHours();
	});

	var hours = hour.range;

	var day = newInterval(function(date) {
	  date.setHours(0, 0, 0, 0);
	}, function(date, step) {
	  date.setDate(date.getDate() + step);
	}, function(start, end) {
	  return (end - start - (end.getTimezoneOffset() - start.getTimezoneOffset()) * durationMinute$1) / durationDay$1;
	}, function(date) {
	  return date.getDate() - 1;
	});

	var days = day.range;

	function weekday(i) {
	  return newInterval(function(date) {
	    date.setDate(date.getDate() - (date.getDay() + 7 - i) % 7);
	    date.setHours(0, 0, 0, 0);
	  }, function(date, step) {
	    date.setDate(date.getDate() + step * 7);
	  }, function(start, end) {
	    return (end - start - (end.getTimezoneOffset() - start.getTimezoneOffset()) * durationMinute$1) / durationWeek$1;
	  });
	}

	var sunday = weekday(0);
	var monday = weekday(1);
	var tuesday = weekday(2);
	var wednesday = weekday(3);
	var thursday = weekday(4);
	var friday = weekday(5);
	var saturday = weekday(6);

	var sundays = sunday.range;
	var mondays = monday.range;
	var tuesdays = tuesday.range;
	var wednesdays = wednesday.range;
	var thursdays = thursday.range;
	var fridays = friday.range;
	var saturdays = saturday.range;

	var month = newInterval(function(date) {
	  date.setDate(1);
	  date.setHours(0, 0, 0, 0);
	}, function(date, step) {
	  date.setMonth(date.getMonth() + step);
	}, function(start, end) {
	  return end.getMonth() - start.getMonth() + (end.getFullYear() - start.getFullYear()) * 12;
	}, function(date) {
	  return date.getMonth();
	});

	var months = month.range;

	var year = newInterval(function(date) {
	  date.setMonth(0, 1);
	  date.setHours(0, 0, 0, 0);
	}, function(date, step) {
	  date.setFullYear(date.getFullYear() + step);
	}, function(start, end) {
	  return end.getFullYear() - start.getFullYear();
	}, function(date) {
	  return date.getFullYear();
	});

	// An optimized implementation for this simple case.
	year.every = function(k) {
	  return !isFinite(k = Math.floor(k)) || !(k > 0) ? null : newInterval(function(date) {
	    date.setFullYear(Math.floor(date.getFullYear() / k) * k);
	    date.setMonth(0, 1);
	    date.setHours(0, 0, 0, 0);
	  }, function(date, step) {
	    date.setFullYear(date.getFullYear() + step * k);
	  });
	};

	var years = year.range;

	var utcMinute = newInterval(function(date) {
	  date.setUTCSeconds(0, 0);
	}, function(date, step) {
	  date.setTime(+date + step * durationMinute$1);
	}, function(start, end) {
	  return (end - start) / durationMinute$1;
	}, function(date) {
	  return date.getUTCMinutes();
	});

	var utcMinutes = utcMinute.range;

	var utcHour = newInterval(function(date) {
	  date.setUTCMinutes(0, 0, 0);
	}, function(date, step) {
	  date.setTime(+date + step * durationHour$1);
	}, function(start, end) {
	  return (end - start) / durationHour$1;
	}, function(date) {
	  return date.getUTCHours();
	});

	var utcHours = utcHour.range;

	var utcDay = newInterval(function(date) {
	  date.setUTCHours(0, 0, 0, 0);
	}, function(date, step) {
	  date.setUTCDate(date.getUTCDate() + step);
	}, function(start, end) {
	  return (end - start) / durationDay$1;
	}, function(date) {
	  return date.getUTCDate() - 1;
	});

	var utcDays = utcDay.range;

	function utcWeekday(i) {
	  return newInterval(function(date) {
	    date.setUTCDate(date.getUTCDate() - (date.getUTCDay() + 7 - i) % 7);
	    date.setUTCHours(0, 0, 0, 0);
	  }, function(date, step) {
	    date.setUTCDate(date.getUTCDate() + step * 7);
	  }, function(start, end) {
	    return (end - start) / durationWeek$1;
	  });
	}

	var utcSunday = utcWeekday(0);
	var utcMonday = utcWeekday(1);
	var utcTuesday = utcWeekday(2);
	var utcWednesday = utcWeekday(3);
	var utcThursday = utcWeekday(4);
	var utcFriday = utcWeekday(5);
	var utcSaturday = utcWeekday(6);

	var utcSundays = utcSunday.range;
	var utcMondays = utcMonday.range;
	var utcTuesdays = utcTuesday.range;
	var utcWednesdays = utcWednesday.range;
	var utcThursdays = utcThursday.range;
	var utcFridays = utcFriday.range;
	var utcSaturdays = utcSaturday.range;

	var utcMonth = newInterval(function(date) {
	  date.setUTCDate(1);
	  date.setUTCHours(0, 0, 0, 0);
	}, function(date, step) {
	  date.setUTCMonth(date.getUTCMonth() + step);
	}, function(start, end) {
	  return end.getUTCMonth() - start.getUTCMonth() + (end.getUTCFullYear() - start.getUTCFullYear()) * 12;
	}, function(date) {
	  return date.getUTCMonth();
	});

	var utcMonths = utcMonth.range;

	var utcYear = newInterval(function(date) {
	  date.setUTCMonth(0, 1);
	  date.setUTCHours(0, 0, 0, 0);
	}, function(date, step) {
	  date.setUTCFullYear(date.getUTCFullYear() + step);
	}, function(start, end) {
	  return end.getUTCFullYear() - start.getUTCFullYear();
	}, function(date) {
	  return date.getUTCFullYear();
	});

	// An optimized implementation for this simple case.
	utcYear.every = function(k) {
	  return !isFinite(k = Math.floor(k)) || !(k > 0) ? null : newInterval(function(date) {
	    date.setUTCFullYear(Math.floor(date.getUTCFullYear() / k) * k);
	    date.setUTCMonth(0, 1);
	    date.setUTCHours(0, 0, 0, 0);
	  }, function(date, step) {
	    date.setUTCFullYear(date.getUTCFullYear() + step * k);
	  });
	};

	var utcYears = utcYear.range;

	function localDate(d) {
	  if (0 <= d.y && d.y < 100) {
	    var date = new Date(-1, d.m, d.d, d.H, d.M, d.S, d.L);
	    date.setFullYear(d.y);
	    return date;
	  }
	  return new Date(d.y, d.m, d.d, d.H, d.M, d.S, d.L);
	}

	function utcDate(d) {
	  if (0 <= d.y && d.y < 100) {
	    var date = new Date(Date.UTC(-1, d.m, d.d, d.H, d.M, d.S, d.L));
	    date.setUTCFullYear(d.y);
	    return date;
	  }
	  return new Date(Date.UTC(d.y, d.m, d.d, d.H, d.M, d.S, d.L));
	}

	function newYear(y) {
	  return {y: y, m: 0, d: 1, H: 0, M: 0, S: 0, L: 0};
	}

	function formatLocale$1(locale) {
	  var locale_dateTime = locale.dateTime,
	      locale_date = locale.date,
	      locale_time = locale.time,
	      locale_periods = locale.periods,
	      locale_weekdays = locale.days,
	      locale_shortWeekdays = locale.shortDays,
	      locale_months = locale.months,
	      locale_shortMonths = locale.shortMonths;

	  var periodRe = formatRe(locale_periods),
	      periodLookup = formatLookup(locale_periods),
	      weekdayRe = formatRe(locale_weekdays),
	      weekdayLookup = formatLookup(locale_weekdays),
	      shortWeekdayRe = formatRe(locale_shortWeekdays),
	      shortWeekdayLookup = formatLookup(locale_shortWeekdays),
	      monthRe = formatRe(locale_months),
	      monthLookup = formatLookup(locale_months),
	      shortMonthRe = formatRe(locale_shortMonths),
	      shortMonthLookup = formatLookup(locale_shortMonths);

	  var formats = {
	    "a": formatShortWeekday,
	    "A": formatWeekday,
	    "b": formatShortMonth,
	    "B": formatMonth,
	    "c": null,
	    "d": formatDayOfMonth,
	    "e": formatDayOfMonth,
	    "f": formatMicroseconds,
	    "H": formatHour24,
	    "I": formatHour12,
	    "j": formatDayOfYear,
	    "L": formatMilliseconds,
	    "m": formatMonthNumber,
	    "M": formatMinutes,
	    "p": formatPeriod,
	    "Q": formatUnixTimestamp,
	    "s": formatUnixTimestampSeconds,
	    "S": formatSeconds,
	    "u": formatWeekdayNumberMonday,
	    "U": formatWeekNumberSunday,
	    "V": formatWeekNumberISO,
	    "w": formatWeekdayNumberSunday,
	    "W": formatWeekNumberMonday,
	    "x": null,
	    "X": null,
	    "y": formatYear,
	    "Y": formatFullYear,
	    "Z": formatZone,
	    "%": formatLiteralPercent
	  };

	  var utcFormats = {
	    "a": formatUTCShortWeekday,
	    "A": formatUTCWeekday,
	    "b": formatUTCShortMonth,
	    "B": formatUTCMonth,
	    "c": null,
	    "d": formatUTCDayOfMonth,
	    "e": formatUTCDayOfMonth,
	    "f": formatUTCMicroseconds,
	    "H": formatUTCHour24,
	    "I": formatUTCHour12,
	    "j": formatUTCDayOfYear,
	    "L": formatUTCMilliseconds,
	    "m": formatUTCMonthNumber,
	    "M": formatUTCMinutes,
	    "p": formatUTCPeriod,
	    "Q": formatUnixTimestamp,
	    "s": formatUnixTimestampSeconds,
	    "S": formatUTCSeconds,
	    "u": formatUTCWeekdayNumberMonday,
	    "U": formatUTCWeekNumberSunday,
	    "V": formatUTCWeekNumberISO,
	    "w": formatUTCWeekdayNumberSunday,
	    "W": formatUTCWeekNumberMonday,
	    "x": null,
	    "X": null,
	    "y": formatUTCYear,
	    "Y": formatUTCFullYear,
	    "Z": formatUTCZone,
	    "%": formatLiteralPercent
	  };

	  var parses = {
	    "a": parseShortWeekday,
	    "A": parseWeekday,
	    "b": parseShortMonth,
	    "B": parseMonth,
	    "c": parseLocaleDateTime,
	    "d": parseDayOfMonth,
	    "e": parseDayOfMonth,
	    "f": parseMicroseconds,
	    "H": parseHour24,
	    "I": parseHour24,
	    "j": parseDayOfYear,
	    "L": parseMilliseconds,
	    "m": parseMonthNumber,
	    "M": parseMinutes,
	    "p": parsePeriod,
	    "Q": parseUnixTimestamp,
	    "s": parseUnixTimestampSeconds,
	    "S": parseSeconds,
	    "u": parseWeekdayNumberMonday,
	    "U": parseWeekNumberSunday,
	    "V": parseWeekNumberISO,
	    "w": parseWeekdayNumberSunday,
	    "W": parseWeekNumberMonday,
	    "x": parseLocaleDate,
	    "X": parseLocaleTime,
	    "y": parseYear,
	    "Y": parseFullYear,
	    "Z": parseZone,
	    "%": parseLiteralPercent
	  };

	  // These recursive directive definitions must be deferred.
	  formats.x = newFormat(locale_date, formats);
	  formats.X = newFormat(locale_time, formats);
	  formats.c = newFormat(locale_dateTime, formats);
	  utcFormats.x = newFormat(locale_date, utcFormats);
	  utcFormats.X = newFormat(locale_time, utcFormats);
	  utcFormats.c = newFormat(locale_dateTime, utcFormats);

	  function newFormat(specifier, formats) {
	    return function(date) {
	      var string = [],
	          i = -1,
	          j = 0,
	          n = specifier.length,
	          c,
	          pad,
	          format;

	      if (!(date instanceof Date)) date = new Date(+date);

	      while (++i < n) {
	        if (specifier.charCodeAt(i) === 37) {
	          string.push(specifier.slice(j, i));
	          if ((pad = pads[c = specifier.charAt(++i)]) != null) c = specifier.charAt(++i);
	          else pad = c === "e" ? " " : "0";
	          if (format = formats[c]) c = format(date, pad);
	          string.push(c);
	          j = i + 1;
	        }
	      }

	      string.push(specifier.slice(j, i));
	      return string.join("");
	    };
	  }

	  function newParse(specifier, newDate) {
	    return function(string) {
	      var d = newYear(1900),
	          i = parseSpecifier(d, specifier, string += "", 0),
	          week, day$$1;
	      if (i != string.length) return null;

	      // If a UNIX timestamp is specified, return it.
	      if ("Q" in d) return new Date(d.Q);

	      // The am-pm flag is 0 for AM, and 1 for PM.
	      if ("p" in d) d.H = d.H % 12 + d.p * 12;

	      // Convert day-of-week and week-of-year to day-of-year.
	      if ("V" in d) {
	        if (d.V < 1 || d.V > 53) return null;
	        if (!("w" in d)) d.w = 1;
	        if ("Z" in d) {
	          week = utcDate(newYear(d.y)), day$$1 = week.getUTCDay();
	          week = day$$1 > 4 || day$$1 === 0 ? utcMonday.ceil(week) : utcMonday(week);
	          week = utcDay.offset(week, (d.V - 1) * 7);
	          d.y = week.getUTCFullYear();
	          d.m = week.getUTCMonth();
	          d.d = week.getUTCDate() + (d.w + 6) % 7;
	        } else {
	          week = newDate(newYear(d.y)), day$$1 = week.getDay();
	          week = day$$1 > 4 || day$$1 === 0 ? monday.ceil(week) : monday(week);
	          week = day.offset(week, (d.V - 1) * 7);
	          d.y = week.getFullYear();
	          d.m = week.getMonth();
	          d.d = week.getDate() + (d.w + 6) % 7;
	        }
	      } else if ("W" in d || "U" in d) {
	        if (!("w" in d)) d.w = "u" in d ? d.u % 7 : "W" in d ? 1 : 0;
	        day$$1 = "Z" in d ? utcDate(newYear(d.y)).getUTCDay() : newDate(newYear(d.y)).getDay();
	        d.m = 0;
	        d.d = "W" in d ? (d.w + 6) % 7 + d.W * 7 - (day$$1 + 5) % 7 : d.w + d.U * 7 - (day$$1 + 6) % 7;
	      }

	      // If a time zone is specified, all fields are interpreted as UTC and then
	      // offset according to the specified time zone.
	      if ("Z" in d) {
	        d.H += d.Z / 100 | 0;
	        d.M += d.Z % 100;
	        return utcDate(d);
	      }

	      // Otherwise, all fields are in local time.
	      return newDate(d);
	    };
	  }

	  function parseSpecifier(d, specifier, string, j) {
	    var i = 0,
	        n = specifier.length,
	        m = string.length,
	        c,
	        parse;

	    while (i < n) {
	      if (j >= m) return -1;
	      c = specifier.charCodeAt(i++);
	      if (c === 37) {
	        c = specifier.charAt(i++);
	        parse = parses[c in pads ? specifier.charAt(i++) : c];
	        if (!parse || ((j = parse(d, string, j)) < 0)) return -1;
	      } else if (c != string.charCodeAt(j++)) {
	        return -1;
	      }
	    }

	    return j;
	  }

	  function parsePeriod(d, string, i) {
	    var n = periodRe.exec(string.slice(i));
	    return n ? (d.p = periodLookup[n[0].toLowerCase()], i + n[0].length) : -1;
	  }

	  function parseShortWeekday(d, string, i) {
	    var n = shortWeekdayRe.exec(string.slice(i));
	    return n ? (d.w = shortWeekdayLookup[n[0].toLowerCase()], i + n[0].length) : -1;
	  }

	  function parseWeekday(d, string, i) {
	    var n = weekdayRe.exec(string.slice(i));
	    return n ? (d.w = weekdayLookup[n[0].toLowerCase()], i + n[0].length) : -1;
	  }

	  function parseShortMonth(d, string, i) {
	    var n = shortMonthRe.exec(string.slice(i));
	    return n ? (d.m = shortMonthLookup[n[0].toLowerCase()], i + n[0].length) : -1;
	  }

	  function parseMonth(d, string, i) {
	    var n = monthRe.exec(string.slice(i));
	    return n ? (d.m = monthLookup[n[0].toLowerCase()], i + n[0].length) : -1;
	  }

	  function parseLocaleDateTime(d, string, i) {
	    return parseSpecifier(d, locale_dateTime, string, i);
	  }

	  function parseLocaleDate(d, string, i) {
	    return parseSpecifier(d, locale_date, string, i);
	  }

	  function parseLocaleTime(d, string, i) {
	    return parseSpecifier(d, locale_time, string, i);
	  }

	  function formatShortWeekday(d) {
	    return locale_shortWeekdays[d.getDay()];
	  }

	  function formatWeekday(d) {
	    return locale_weekdays[d.getDay()];
	  }

	  function formatShortMonth(d) {
	    return locale_shortMonths[d.getMonth()];
	  }

	  function formatMonth(d) {
	    return locale_months[d.getMonth()];
	  }

	  function formatPeriod(d) {
	    return locale_periods[+(d.getHours() >= 12)];
	  }

	  function formatUTCShortWeekday(d) {
	    return locale_shortWeekdays[d.getUTCDay()];
	  }

	  function formatUTCWeekday(d) {
	    return locale_weekdays[d.getUTCDay()];
	  }

	  function formatUTCShortMonth(d) {
	    return locale_shortMonths[d.getUTCMonth()];
	  }

	  function formatUTCMonth(d) {
	    return locale_months[d.getUTCMonth()];
	  }

	  function formatUTCPeriod(d) {
	    return locale_periods[+(d.getUTCHours() >= 12)];
	  }

	  return {
	    format: function(specifier) {
	      var f = newFormat(specifier += "", formats);
	      f.toString = function() { return specifier; };
	      return f;
	    },
	    parse: function(specifier) {
	      var p = newParse(specifier += "", localDate);
	      p.toString = function() { return specifier; };
	      return p;
	    },
	    utcFormat: function(specifier) {
	      var f = newFormat(specifier += "", utcFormats);
	      f.toString = function() { return specifier; };
	      return f;
	    },
	    utcParse: function(specifier) {
	      var p = newParse(specifier, utcDate);
	      p.toString = function() { return specifier; };
	      return p;
	    }
	  };
	}

	var pads = {"-": "", "_": " ", "0": "0"};
	var numberRe = /^\s*\d+/;
	var percentRe = /^%/;
	var requoteRe = /[\\^$*+?|[\]().{}]/g;

	function pad(value, fill, width) {
	  var sign = value < 0 ? "-" : "",
	      string = (sign ? -value : value) + "",
	      length = string.length;
	  return sign + (length < width ? new Array(width - length + 1).join(fill) + string : string);
	}

	function requote(s) {
	  return s.replace(requoteRe, "\\$&");
	}

	function formatRe(names) {
	  return new RegExp("^(?:" + names.map(requote).join("|") + ")", "i");
	}

	function formatLookup(names) {
	  var map = {}, i = -1, n = names.length;
	  while (++i < n) map[names[i].toLowerCase()] = i;
	  return map;
	}

	function parseWeekdayNumberSunday(d, string, i) {
	  var n = numberRe.exec(string.slice(i, i + 1));
	  return n ? (d.w = +n[0], i + n[0].length) : -1;
	}

	function parseWeekdayNumberMonday(d, string, i) {
	  var n = numberRe.exec(string.slice(i, i + 1));
	  return n ? (d.u = +n[0], i + n[0].length) : -1;
	}

	function parseWeekNumberSunday(d, string, i) {
	  var n = numberRe.exec(string.slice(i, i + 2));
	  return n ? (d.U = +n[0], i + n[0].length) : -1;
	}

	function parseWeekNumberISO(d, string, i) {
	  var n = numberRe.exec(string.slice(i, i + 2));
	  return n ? (d.V = +n[0], i + n[0].length) : -1;
	}

	function parseWeekNumberMonday(d, string, i) {
	  var n = numberRe.exec(string.slice(i, i + 2));
	  return n ? (d.W = +n[0], i + n[0].length) : -1;
	}

	function parseFullYear(d, string, i) {
	  var n = numberRe.exec(string.slice(i, i + 4));
	  return n ? (d.y = +n[0], i + n[0].length) : -1;
	}

	function parseYear(d, string, i) {
	  var n = numberRe.exec(string.slice(i, i + 2));
	  return n ? (d.y = +n[0] + (+n[0] > 68 ? 1900 : 2000), i + n[0].length) : -1;
	}

	function parseZone(d, string, i) {
	  var n = /^(Z)|([+-]\d\d)(?::?(\d\d))?/.exec(string.slice(i, i + 6));
	  return n ? (d.Z = n[1] ? 0 : -(n[2] + (n[3] || "00")), i + n[0].length) : -1;
	}

	function parseMonthNumber(d, string, i) {
	  var n = numberRe.exec(string.slice(i, i + 2));
	  return n ? (d.m = n[0] - 1, i + n[0].length) : -1;
	}

	function parseDayOfMonth(d, string, i) {
	  var n = numberRe.exec(string.slice(i, i + 2));
	  return n ? (d.d = +n[0], i + n[0].length) : -1;
	}

	function parseDayOfYear(d, string, i) {
	  var n = numberRe.exec(string.slice(i, i + 3));
	  return n ? (d.m = 0, d.d = +n[0], i + n[0].length) : -1;
	}

	function parseHour24(d, string, i) {
	  var n = numberRe.exec(string.slice(i, i + 2));
	  return n ? (d.H = +n[0], i + n[0].length) : -1;
	}

	function parseMinutes(d, string, i) {
	  var n = numberRe.exec(string.slice(i, i + 2));
	  return n ? (d.M = +n[0], i + n[0].length) : -1;
	}

	function parseSeconds(d, string, i) {
	  var n = numberRe.exec(string.slice(i, i + 2));
	  return n ? (d.S = +n[0], i + n[0].length) : -1;
	}

	function parseMilliseconds(d, string, i) {
	  var n = numberRe.exec(string.slice(i, i + 3));
	  return n ? (d.L = +n[0], i + n[0].length) : -1;
	}

	function parseMicroseconds(d, string, i) {
	  var n = numberRe.exec(string.slice(i, i + 6));
	  return n ? (d.L = Math.floor(n[0] / 1000), i + n[0].length) : -1;
	}

	function parseLiteralPercent(d, string, i) {
	  var n = percentRe.exec(string.slice(i, i + 1));
	  return n ? i + n[0].length : -1;
	}

	function parseUnixTimestamp(d, string, i) {
	  var n = numberRe.exec(string.slice(i));
	  return n ? (d.Q = +n[0], i + n[0].length) : -1;
	}

	function parseUnixTimestampSeconds(d, string, i) {
	  var n = numberRe.exec(string.slice(i));
	  return n ? (d.Q = (+n[0]) * 1000, i + n[0].length) : -1;
	}

	function formatDayOfMonth(d, p) {
	  return pad(d.getDate(), p, 2);
	}

	function formatHour24(d, p) {
	  return pad(d.getHours(), p, 2);
	}

	function formatHour12(d, p) {
	  return pad(d.getHours() % 12 || 12, p, 2);
	}

	function formatDayOfYear(d, p) {
	  return pad(1 + day.count(year(d), d), p, 3);
	}

	function formatMilliseconds(d, p) {
	  return pad(d.getMilliseconds(), p, 3);
	}

	function formatMicroseconds(d, p) {
	  return formatMilliseconds(d, p) + "000";
	}

	function formatMonthNumber(d, p) {
	  return pad(d.getMonth() + 1, p, 2);
	}

	function formatMinutes(d, p) {
	  return pad(d.getMinutes(), p, 2);
	}

	function formatSeconds(d, p) {
	  return pad(d.getSeconds(), p, 2);
	}

	function formatWeekdayNumberMonday(d) {
	  var day$$1 = d.getDay();
	  return day$$1 === 0 ? 7 : day$$1;
	}

	function formatWeekNumberSunday(d, p) {
	  return pad(sunday.count(year(d), d), p, 2);
	}

	function formatWeekNumberISO(d, p) {
	  var day$$1 = d.getDay();
	  d = (day$$1 >= 4 || day$$1 === 0) ? thursday(d) : thursday.ceil(d);
	  return pad(thursday.count(year(d), d) + (year(d).getDay() === 4), p, 2);
	}

	function formatWeekdayNumberSunday(d) {
	  return d.getDay();
	}

	function formatWeekNumberMonday(d, p) {
	  return pad(monday.count(year(d), d), p, 2);
	}

	function formatYear(d, p) {
	  return pad(d.getFullYear() % 100, p, 2);
	}

	function formatFullYear(d, p) {
	  return pad(d.getFullYear() % 10000, p, 4);
	}

	function formatZone(d) {
	  var z = d.getTimezoneOffset();
	  return (z > 0 ? "-" : (z *= -1, "+"))
	      + pad(z / 60 | 0, "0", 2)
	      + pad(z % 60, "0", 2);
	}

	function formatUTCDayOfMonth(d, p) {
	  return pad(d.getUTCDate(), p, 2);
	}

	function formatUTCHour24(d, p) {
	  return pad(d.getUTCHours(), p, 2);
	}

	function formatUTCHour12(d, p) {
	  return pad(d.getUTCHours() % 12 || 12, p, 2);
	}

	function formatUTCDayOfYear(d, p) {
	  return pad(1 + utcDay.count(utcYear(d), d), p, 3);
	}

	function formatUTCMilliseconds(d, p) {
	  return pad(d.getUTCMilliseconds(), p, 3);
	}

	function formatUTCMicroseconds(d, p) {
	  return formatUTCMilliseconds(d, p) + "000";
	}

	function formatUTCMonthNumber(d, p) {
	  return pad(d.getUTCMonth() + 1, p, 2);
	}

	function formatUTCMinutes(d, p) {
	  return pad(d.getUTCMinutes(), p, 2);
	}

	function formatUTCSeconds(d, p) {
	  return pad(d.getUTCSeconds(), p, 2);
	}

	function formatUTCWeekdayNumberMonday(d) {
	  var dow = d.getUTCDay();
	  return dow === 0 ? 7 : dow;
	}

	function formatUTCWeekNumberSunday(d, p) {
	  return pad(utcSunday.count(utcYear(d), d), p, 2);
	}

	function formatUTCWeekNumberISO(d, p) {
	  var day$$1 = d.getUTCDay();
	  d = (day$$1 >= 4 || day$$1 === 0) ? utcThursday(d) : utcThursday.ceil(d);
	  return pad(utcThursday.count(utcYear(d), d) + (utcYear(d).getUTCDay() === 4), p, 2);
	}

	function formatUTCWeekdayNumberSunday(d) {
	  return d.getUTCDay();
	}

	function formatUTCWeekNumberMonday(d, p) {
	  return pad(utcMonday.count(utcYear(d), d), p, 2);
	}

	function formatUTCYear(d, p) {
	  return pad(d.getUTCFullYear() % 100, p, 2);
	}

	function formatUTCFullYear(d, p) {
	  return pad(d.getUTCFullYear() % 10000, p, 4);
	}

	function formatUTCZone() {
	  return "+0000";
	}

	function formatLiteralPercent() {
	  return "%";
	}

	function formatUnixTimestamp(d) {
	  return +d;
	}

	function formatUnixTimestampSeconds(d) {
	  return Math.floor(+d / 1000);
	}

	var locale$1;





	defaultLocale$1({
	  dateTime: "%x, %X",
	  date: "%-m/%-d/%Y",
	  time: "%-I:%M:%S %p",
	  periods: ["AM", "PM"],
	  days: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
	  shortDays: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
	  months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
	  shortMonths: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
	});

	function defaultLocale$1(definition) {
	  locale$1 = formatLocale$1(definition);
	  exports.timeFormat = locale$1.format;
	  exports.timeParse = locale$1.parse;
	  exports.utcFormat = locale$1.utcFormat;
	  exports.utcParse = locale$1.utcParse;
	  return locale$1;
	}

	var isoSpecifier = "%Y-%m-%dT%H:%M:%S.%LZ";

	function formatIsoNative(date) {
	  return date.toISOString();
	}

	var formatIso = Date.prototype.toISOString
	    ? formatIsoNative
	    : exports.utcFormat(isoSpecifier);

	function parseIsoNative(string) {
	  var date = new Date(string);
	  return isNaN(date) ? null : date;
	}

	var parseIso = +new Date("2000-01-01T00:00:00.000Z")
	    ? parseIsoNative
	    : exports.utcParse(isoSpecifier);

	var durationSecond = 1000;
	var durationMinute = durationSecond * 60;
	var durationHour = durationMinute * 60;
	var durationDay = durationHour * 24;
	var durationWeek = durationDay * 7;
	var durationMonth = durationDay * 30;
	var durationYear = durationDay * 365;

	function date$1(t) {
	  return new Date(t);
	}

	function number$3(t) {
	  return t instanceof Date ? +t : +new Date(+t);
	}

	function calendar(year$$1, month$$1, week, day$$1, hour$$1, minute$$1, second$$1, millisecond$$1, format) {
	  var scale = continuous(deinterpolateLinear, reinterpolate),
	      invert = scale.invert,
	      domain = scale.domain;

	  var formatMillisecond = format(".%L"),
	      formatSecond = format(":%S"),
	      formatMinute = format("%I:%M"),
	      formatHour = format("%I %p"),
	      formatDay = format("%a %d"),
	      formatWeek = format("%b %d"),
	      formatMonth = format("%B"),
	      formatYear = format("%Y");

	  var tickIntervals = [
	    [second$$1,  1,      durationSecond],
	    [second$$1,  5,  5 * durationSecond],
	    [second$$1, 15, 15 * durationSecond],
	    [second$$1, 30, 30 * durationSecond],
	    [minute$$1,  1,      durationMinute],
	    [minute$$1,  5,  5 * durationMinute],
	    [minute$$1, 15, 15 * durationMinute],
	    [minute$$1, 30, 30 * durationMinute],
	    [  hour$$1,  1,      durationHour  ],
	    [  hour$$1,  3,  3 * durationHour  ],
	    [  hour$$1,  6,  6 * durationHour  ],
	    [  hour$$1, 12, 12 * durationHour  ],
	    [   day$$1,  1,      durationDay   ],
	    [   day$$1,  2,  2 * durationDay   ],
	    [  week,  1,      durationWeek  ],
	    [ month$$1,  1,      durationMonth ],
	    [ month$$1,  3,  3 * durationMonth ],
	    [  year$$1,  1,      durationYear  ]
	  ];

	  function tickFormat(date) {
	    return (second$$1(date) < date ? formatMillisecond
	        : minute$$1(date) < date ? formatSecond
	        : hour$$1(date) < date ? formatMinute
	        : day$$1(date) < date ? formatHour
	        : month$$1(date) < date ? (week(date) < date ? formatDay : formatWeek)
	        : year$$1(date) < date ? formatMonth
	        : formatYear)(date);
	  }

	  function tickInterval(interval, start, stop, step) {
	    if (interval == null) interval = 10;

	    // If a desired tick count is specified, pick a reasonable tick interval
	    // based on the extent of the domain and a rough estimate of tick size.
	    // Otherwise, assume interval is already a time interval and use it.
	    if (typeof interval === "number") {
	      var target = Math.abs(stop - start) / interval,
	          i = bisector(function(i) { return i[2]; }).right(tickIntervals, target);
	      if (i === tickIntervals.length) {
	        step = tickStep(start / durationYear, stop / durationYear, interval);
	        interval = year$$1;
	      } else if (i) {
	        i = tickIntervals[target / tickIntervals[i - 1][2] < tickIntervals[i][2] / target ? i - 1 : i];
	        step = i[1];
	        interval = i[0];
	      } else {
	        step = Math.max(tickStep(start, stop, interval), 1);
	        interval = millisecond$$1;
	      }
	    }

	    return step == null ? interval : interval.every(step);
	  }

	  scale.invert = function(y) {
	    return new Date(invert(y));
	  };

	  scale.domain = function(_) {
	    return arguments.length ? domain(map$3.call(_, number$3)) : domain().map(date$1);
	  };

	  scale.ticks = function(interval, step) {
	    var d = domain(),
	        t0 = d[0],
	        t1 = d[d.length - 1],
	        r = t1 < t0,
	        t;
	    if (r) t = t0, t0 = t1, t1 = t;
	    t = tickInterval(interval, t0, t1, step);
	    t = t ? t.range(t0, t1 + 1) : []; // inclusive stop
	    return r ? t.reverse() : t;
	  };

	  scale.tickFormat = function(count, specifier) {
	    return specifier == null ? tickFormat : format(specifier);
	  };

	  scale.nice = function(interval, step) {
	    var d = domain();
	    return (interval = tickInterval(interval, d[0], d[d.length - 1], step))
	        ? domain(nice(d, interval))
	        : scale;
	  };

	  scale.copy = function() {
	    return copy(scale, calendar(year$$1, month$$1, week, day$$1, hour$$1, minute$$1, second$$1, millisecond$$1, format));
	  };

	  return scale;
	}

	function time() {
	  return calendar(year, month, sunday, day, hour, minute, second, millisecond, exports.timeFormat).domain([new Date(2000, 0, 1), new Date(2000, 0, 2)]);
	}

	function utcTime() {
	  return calendar(utcYear, utcMonth, utcSunday, utcDay, utcHour, utcMinute, second, millisecond, exports.utcFormat).domain([Date.UTC(2000, 0, 1), Date.UTC(2000, 0, 2)]);
	}

	function colors(s) {
	  return s.match(/.{6}/g).map(function(x) {
	    return "#" + x;
	  });
	}

	var category10 = colors("1f77b4ff7f0e2ca02cd627289467bd8c564be377c27f7f7fbcbd2217becf");

	var category20b = colors("393b795254a36b6ecf9c9ede6379398ca252b5cf6bcedb9c8c6d31bd9e39e7ba52e7cb94843c39ad494ad6616be7969c7b4173a55194ce6dbdde9ed6");

	var category20c = colors("3182bd6baed69ecae1c6dbefe6550dfd8d3cfdae6bfdd0a231a35474c476a1d99bc7e9c0756bb19e9ac8bcbddcdadaeb636363969696bdbdbdd9d9d9");

	var category20 = colors("1f77b4aec7e8ff7f0effbb782ca02c98df8ad62728ff98969467bdc5b0d58c564bc49c94e377c2f7b6d27f7f7fc7c7c7bcbd22dbdb8d17becf9edae5");

	var cubehelix$3 = cubehelixLong(cubehelix(300, 0.5, 0.0), cubehelix(-240, 0.5, 1.0));

	var warm = cubehelixLong(cubehelix(-100, 0.75, 0.35), cubehelix(80, 1.50, 0.8));

	var cool = cubehelixLong(cubehelix(260, 0.75, 0.35), cubehelix(80, 1.50, 0.8));

	var rainbow = cubehelix();

	function rainbow$1(t) {
	  if (t < 0 || t > 1) t -= Math.floor(t);
	  var ts = Math.abs(t - 0.5);
	  rainbow.h = 360 * t - 100;
	  rainbow.s = 1.5 - 1.5 * ts;
	  rainbow.l = 0.8 - 0.9 * ts;
	  return rainbow + "";
	}

	function ramp(range) {
	  var n = range.length;
	  return function(t) {
	    return range[Math.max(0, Math.min(n - 1, Math.floor(t * n)))];
	  };
	}

	var viridis = ramp(colors(""));

	var magma = ramp(colors(""));

	var inferno = ramp(colors(""));

	var plasma = ramp(colors(""));

	function sequential(interpolator) {
	  var x0 = 0,
	      x1 = 1,
	      clamp = false;

	  function scale(x) {
	    var t = (x - x0) / (x1 - x0);
	    return interpolator(clamp ? Math.max(0, Math.min(1, t)) : t);
	  }

	  scale.domain = function(_) {
	    return arguments.length ? (x0 = +_[0], x1 = +_[1], scale) : [x0, x1];
	  };

	  scale.clamp = function(_) {
	    return arguments.length ? (clamp = !!_, scale) : clamp;
	  };

	  scale.interpolator = function(_) {
	    return arguments.length ? (interpolator = _, scale) : interpolator;
	  };

	  scale.copy = function() {
	    return sequential(interpolator).domain([x0, x1]).clamp(clamp);
	  };

	  return linearish(scale);
	}

	function constant$10(x) {
	  return function constant() {
	    return x;
	  };
	}

	var abs$1 = Math.abs;
	var atan2$1 = Math.atan2;
	var cos$2 = Math.cos;
	var max$2 = Math.max;
	var min$1 = Math.min;
	var sin$2 = Math.sin;
	var sqrt$2 = Math.sqrt;

	var epsilon$3 = 1e-12;
	var pi$4 = Math.PI;
	var halfPi$3 = pi$4 / 2;
	var tau$4 = 2 * pi$4;

	function acos$1(x) {
	  return x > 1 ? 0 : x < -1 ? pi$4 : Math.acos(x);
	}

	function asin$1(x) {
	  return x >= 1 ? halfPi$3 : x <= -1 ? -halfPi$3 : Math.asin(x);
	}

	function arcInnerRadius(d) {
	  return d.innerRadius;
	}

	function arcOuterRadius(d) {
	  return d.outerRadius;
	}

	function arcStartAngle(d) {
	  return d.startAngle;
	}

	function arcEndAngle(d) {
	  return d.endAngle;
	}

	function arcPadAngle(d) {
	  return d && d.padAngle; // Note: optional!
	}

	function intersect(x0, y0, x1, y1, x2, y2, x3, y3) {
	  var x10 = x1 - x0, y10 = y1 - y0,
	      x32 = x3 - x2, y32 = y3 - y2,
	      t = (x32 * (y0 - y2) - y32 * (x0 - x2)) / (y32 * x10 - x32 * y10);
	  return [x0 + t * x10, y0 + t * y10];
	}

	// Compute perpendicular offset line of length rc.
	// http://mathworld.wolfram.com/Circle-LineIntersection.html
	function cornerTangents(x0, y0, x1, y1, r1, rc, cw) {
	  var x01 = x0 - x1,
	      y01 = y0 - y1,
	      lo = (cw ? rc : -rc) / sqrt$2(x01 * x01 + y01 * y01),
	      ox = lo * y01,
	      oy = -lo * x01,
	      x11 = x0 + ox,
	      y11 = y0 + oy,
	      x10 = x1 + ox,
	      y10 = y1 + oy,
	      x00 = (x11 + x10) / 2,
	      y00 = (y11 + y10) / 2,
	      dx = x10 - x11,
	      dy = y10 - y11,
	      d2 = dx * dx + dy * dy,
	      r = r1 - rc,
	      D = x11 * y10 - x10 * y11,
	      d = (dy < 0 ? -1 : 1) * sqrt$2(max$2(0, r * r * d2 - D * D)),
	      cx0 = (D * dy - dx * d) / d2,
	      cy0 = (-D * dx - dy * d) / d2,
	      cx1 = (D * dy + dx * d) / d2,
	      cy1 = (-D * dx + dy * d) / d2,
	      dx0 = cx0 - x00,
	      dy0 = cy0 - y00,
	      dx1 = cx1 - x00,
	      dy1 = cy1 - y00;

	  // Pick the closer of the two intersection points.
	  // TODO Is there a faster way to determine which intersection to use?
	  if (dx0 * dx0 + dy0 * dy0 > dx1 * dx1 + dy1 * dy1) cx0 = cx1, cy0 = cy1;

	  return {
	    cx: cx0,
	    cy: cy0,
	    x01: -ox,
	    y01: -oy,
	    x11: cx0 * (r1 / r - 1),
	    y11: cy0 * (r1 / r - 1)
	  };
	}

	function arc() {
	  var innerRadius = arcInnerRadius,
	      outerRadius = arcOuterRadius,
	      cornerRadius = constant$10(0),
	      padRadius = null,
	      startAngle = arcStartAngle,
	      endAngle = arcEndAngle,
	      padAngle = arcPadAngle,
	      context = null;

	  function arc() {
	    var buffer,
	        r,
	        r0 = +innerRadius.apply(this, arguments),
	        r1 = +outerRadius.apply(this, arguments),
	        a0 = startAngle.apply(this, arguments) - halfPi$3,
	        a1 = endAngle.apply(this, arguments) - halfPi$3,
	        da = abs$1(a1 - a0),
	        cw = a1 > a0;

	    if (!context) context = buffer = path();

	    // Ensure that the outer radius is always larger than the inner radius.
	    if (r1 < r0) r = r1, r1 = r0, r0 = r;

	    // Is it a point?
	    if (!(r1 > epsilon$3)) context.moveTo(0, 0);

	    // Or is it a circle or annulus?
	    else if (da > tau$4 - epsilon$3) {
	      context.moveTo(r1 * cos$2(a0), r1 * sin$2(a0));
	      context.arc(0, 0, r1, a0, a1, !cw);
	      if (r0 > epsilon$3) {
	        context.moveTo(r0 * cos$2(a1), r0 * sin$2(a1));
	        context.arc(0, 0, r0, a1, a0, cw);
	      }
	    }

	    // Or is it a circular or annular sector?
	    else {
	      var a01 = a0,
	          a11 = a1,
	          a00 = a0,
	          a10 = a1,
	          da0 = da,
	          da1 = da,
	          ap = padAngle.apply(this, arguments) / 2,
	          rp = (ap > epsilon$3) && (padRadius ? +padRadius.apply(this, arguments) : sqrt$2(r0 * r0 + r1 * r1)),
	          rc = min$1(abs$1(r1 - r0) / 2, +cornerRadius.apply(this, arguments)),
	          rc0 = rc,
	          rc1 = rc,
	          t0,
	          t1;

	      // Apply padding? Note that since r1 ≥ r0, da1 ≥ da0.
	      if (rp > epsilon$3) {
	        var p0 = asin$1(rp / r0 * sin$2(ap)),
	            p1 = asin$1(rp / r1 * sin$2(ap));
	        if ((da0 -= p0 * 2) > epsilon$3) p0 *= (cw ? 1 : -1), a00 += p0, a10 -= p0;
	        else da0 = 0, a00 = a10 = (a0 + a1) / 2;
	        if ((da1 -= p1 * 2) > epsilon$3) p1 *= (cw ? 1 : -1), a01 += p1, a11 -= p1;
	        else da1 = 0, a01 = a11 = (a0 + a1) / 2;
	      }

	      var x01 = r1 * cos$2(a01),
	          y01 = r1 * sin$2(a01),
	          x10 = r0 * cos$2(a10),
	          y10 = r0 * sin$2(a10);

	      // Apply rounded corners?
	      if (rc > epsilon$3) {
	        var x11 = r1 * cos$2(a11),
	            y11 = r1 * sin$2(a11),
	            x00 = r0 * cos$2(a00),
	            y00 = r0 * sin$2(a00);

	        // Restrict the corner radius according to the sector angle.
	        if (da < pi$4) {
	          var oc = da0 > epsilon$3 ? intersect(x01, y01, x00, y00, x11, y11, x10, y10) : [x10, y10],
	              ax = x01 - oc[0],
	              ay = y01 - oc[1],
	              bx = x11 - oc[0],
	              by = y11 - oc[1],
	              kc = 1 / sin$2(acos$1((ax * bx + ay * by) / (sqrt$2(ax * ax + ay * ay) * sqrt$2(bx * bx + by * by))) / 2),
	              lc = sqrt$2(oc[0] * oc[0] + oc[1] * oc[1]);
	          rc0 = min$1(rc, (r0 - lc) / (kc - 1));
	          rc1 = min$1(rc, (r1 - lc) / (kc + 1));
	        }
	      }

	      // Is the sector collapsed to a line?
	      if (!(da1 > epsilon$3)) context.moveTo(x01, y01);

	      // Does the sector’s outer ring have rounded corners?
	      else if (rc1 > epsilon$3) {
	        t0 = cornerTangents(x00, y00, x01, y01, r1, rc1, cw);
	        t1 = cornerTangents(x11, y11, x10, y10, r1, rc1, cw);

	        context.moveTo(t0.cx + t0.x01, t0.cy + t0.y01);

	        // Have the corners merged?
	        if (rc1 < rc) context.arc(t0.cx, t0.cy, rc1, atan2$1(t0.y01, t0.x01), atan2$1(t1.y01, t1.x01), !cw);

	        // Otherwise, draw the two corners and the ring.
	        else {
	          context.arc(t0.cx, t0.cy, rc1, atan2$1(t0.y01, t0.x01), atan2$1(t0.y11, t0.x11), !cw);
	          context.arc(0, 0, r1, atan2$1(t0.cy + t0.y11, t0.cx + t0.x11), atan2$1(t1.cy + t1.y11, t1.cx + t1.x11), !cw);
	          context.arc(t1.cx, t1.cy, rc1, atan2$1(t1.y11, t1.x11), atan2$1(t1.y01, t1.x01), !cw);
	        }
	      }

	      // Or is the outer ring just a circular arc?
	      else context.moveTo(x01, y01), context.arc(0, 0, r1, a01, a11, !cw);

	      // Is there no inner ring, and it’s a circular sector?
	      // Or perhaps it’s an annular sector collapsed due to padding?
	      if (!(r0 > epsilon$3) || !(da0 > epsilon$3)) context.lineTo(x10, y10);

	      // Does the sector’s inner ring (or point) have rounded corners?
	      else if (rc0 > epsilon$3) {
	        t0 = cornerTangents(x10, y10, x11, y11, r0, -rc0, cw);
	        t1 = cornerTangents(x01, y01, x00, y00, r0, -rc0, cw);

	        context.lineTo(t0.cx + t0.x01, t0.cy + t0.y01);

	        // Have the corners merged?
	        if (rc0 < rc) context.arc(t0.cx, t0.cy, rc0, atan2$1(t0.y01, t0.x01), atan2$1(t1.y01, t1.x01), !cw);

	        // Otherwise, draw the two corners and the ring.
	        else {
	          context.arc(t0.cx, t0.cy, rc0, atan2$1(t0.y01, t0.x01), atan2$1(t0.y11, t0.x11), !cw);
	          context.arc(0, 0, r0, atan2$1(t0.cy + t0.y11, t0.cx + t0.x11), atan2$1(t1.cy + t1.y11, t1.cx + t1.x11), cw);
	          context.arc(t1.cx, t1.cy, rc0, atan2$1(t1.y11, t1.x11), atan2$1(t1.y01, t1.x01), !cw);
	        }
	      }

	      // Or is the inner ring just a circular arc?
	      else context.arc(0, 0, r0, a10, a00, cw);
	    }

	    context.closePath();

	    if (buffer) return context = null, buffer + "" || null;
	  }

	  arc.centroid = function() {
	    var r = (+innerRadius.apply(this, arguments) + +outerRadius.apply(this, arguments)) / 2,
	        a = (+startAngle.apply(this, arguments) + +endAngle.apply(this, arguments)) / 2 - pi$4 / 2;
	    return [cos$2(a) * r, sin$2(a) * r];
	  };

	  arc.innerRadius = function(_) {
	    return arguments.length ? (innerRadius = typeof _ === "function" ? _ : constant$10(+_), arc) : innerRadius;
	  };

	  arc.outerRadius = function(_) {
	    return arguments.length ? (outerRadius = typeof _ === "function" ? _ : constant$10(+_), arc) : outerRadius;
	  };

	  arc.cornerRadius = function(_) {
	    return arguments.length ? (cornerRadius = typeof _ === "function" ? _ : constant$10(+_), arc) : cornerRadius;
	  };

	  arc.padRadius = function(_) {
	    return arguments.length ? (padRadius = _ == null ? null : typeof _ === "function" ? _ : constant$10(+_), arc) : padRadius;
	  };

	  arc.startAngle = function(_) {
	    return arguments.length ? (startAngle = typeof _ === "function" ? _ : constant$10(+_), arc) : startAngle;
	  };

	  arc.endAngle = function(_) {
	    return arguments.length ? (endAngle = typeof _ === "function" ? _ : constant$10(+_), arc) : endAngle;
	  };

	  arc.padAngle = function(_) {
	    return arguments.length ? (padAngle = typeof _ === "function" ? _ : constant$10(+_), arc) : padAngle;
	  };

	  arc.context = function(_) {
	    return arguments.length ? (context = _ == null ? null : _, arc) : context;
	  };

	  return arc;
	}

	function Linear(context) {
	  this._context = context;
	}

	Linear.prototype = {
	  areaStart: function() {
	    this._line = 0;
	  },
	  areaEnd: function() {
	    this._line = NaN;
	  },
	  lineStart: function() {
	    this._point = 0;
	  },
	  lineEnd: function() {
	    if (this._line || (this._line !== 0 && this._point === 1)) this._context.closePath();
	    this._line = 1 - this._line;
	  },
	  point: function(x, y) {
	    x = +x, y = +y;
	    switch (this._point) {
	      case 0: this._point = 1; this._line ? this._context.lineTo(x, y) : this._context.moveTo(x, y); break;
	      case 1: this._point = 2; // proceed
	      default: this._context.lineTo(x, y); break;
	    }
	  }
	};

	function curveLinear(context) {
	  return new Linear(context);
	}

	function x$3(p) {
	  return p[0];
	}

	function y$3(p) {
	  return p[1];
	}

	function line() {
	  var x$$1 = x$3,
	      y$$1 = y$3,
	      defined = constant$10(true),
	      context = null,
	      curve = curveLinear,
	      output = null;

	  function line(data) {
	    var i,
	        n = data.length,
	        d,
	        defined0 = false,
	        buffer;

	    if (context == null) output = curve(buffer = path());

	    for (i = 0; i <= n; ++i) {
	      if (!(i < n && defined(d = data[i], i, data)) === defined0) {
	        if (defined0 = !defined0) output.lineStart();
	        else output.lineEnd();
	      }
	      if (defined0) output.point(+x$$1(d, i, data), +y$$1(d, i, data));
	    }

	    if (buffer) return output = null, buffer + "" || null;
	  }

	  line.x = function(_) {
	    return arguments.length ? (x$$1 = typeof _ === "function" ? _ : constant$10(+_), line) : x$$1;
	  };

	  line.y = function(_) {
	    return arguments.length ? (y$$1 = typeof _ === "function" ? _ : constant$10(+_), line) : y$$1;
	  };

	  line.defined = function(_) {
	    return arguments.length ? (defined = typeof _ === "function" ? _ : constant$10(!!_), line) : defined;
	  };

	  line.curve = function(_) {
	    return arguments.length ? (curve = _, context != null && (output = curve(context)), line) : curve;
	  };

	  line.context = function(_) {
	    return arguments.length ? (_ == null ? context = output = null : output = curve(context = _), line) : context;
	  };

	  return line;
	}

	function area$2() {
	  var x0 = x$3,
	      x1 = null,
	      y0 = constant$10(0),
	      y1 = y$3,
	      defined = constant$10(true),
	      context = null,
	      curve = curveLinear,
	      output = null;

	  function area(data) {
	    var i,
	        j,
	        k,
	        n = data.length,
	        d,
	        defined0 = false,
	        buffer,
	        x0z = new Array(n),
	        y0z = new Array(n);

	    if (context == null) output = curve(buffer = path());

	    for (i = 0; i <= n; ++i) {
	      if (!(i < n && defined(d = data[i], i, data)) === defined0) {
	        if (defined0 = !defined0) {
	          j = i;
	          output.areaStart();
	          output.lineStart();
	        } else {
	          output.lineEnd();
	          output.lineStart();
	          for (k = i - 1; k >= j; --k) {
	            output.point(x0z[k], y0z[k]);
	          }
	          output.lineEnd();
	          output.areaEnd();
	        }
	      }
	      if (defined0) {
	        x0z[i] = +x0(d, i, data), y0z[i] = +y0(d, i, data);
	        output.point(x1 ? +x1(d, i, data) : x0z[i], y1 ? +y1(d, i, data) : y0z[i]);
	      }
	    }

	    if (buffer) return output = null, buffer + "" || null;
	  }

	  function arealine() {
	    return line().defined(defined).curve(curve).context(context);
	  }

	  area.x = function(_) {
	    return arguments.length ? (x0 = typeof _ === "function" ? _ : constant$10(+_), x1 = null, area) : x0;
	  };

	  area.x0 = function(_) {
	    return arguments.length ? (x0 = typeof _ === "function" ? _ : constant$10(+_), area) : x0;
	  };

	  area.x1 = function(_) {
	    return arguments.length ? (x1 = _ == null ? null : typeof _ === "function" ? _ : constant$10(+_), area) : x1;
	  };

	  area.y = function(_) {
	    return arguments.length ? (y0 = typeof _ === "function" ? _ : constant$10(+_), y1 = null, area) : y0;
	  };

	  area.y0 = function(_) {
	    return arguments.length ? (y0 = typeof _ === "function" ? _ : constant$10(+_), area) : y0;
	  };

	  area.y1 = function(_) {
	    return arguments.length ? (y1 = _ == null ? null : typeof _ === "function" ? _ : constant$10(+_), area) : y1;
	  };

	  area.lineX0 =
	  area.lineY0 = function() {
	    return arealine().x(x0).y(y0);
	  };

	  area.lineY1 = function() {
	    return arealine().x(x0).y(y1);
	  };

	  area.lineX1 = function() {
	    return arealine().x(x1).y(y0);
	  };

	  area.defined = function(_) {
	    return arguments.length ? (defined = typeof _ === "function" ? _ : constant$10(!!_), area) : defined;
	  };

	  area.curve = function(_) {
	    return arguments.length ? (curve = _, context != null && (output = curve(context)), area) : curve;
	  };

	  area.context = function(_) {
	    return arguments.length ? (_ == null ? context = output = null : output = curve(context = _), area) : context;
	  };

	  return area;
	}

	function descending$1(a, b) {
	  return b < a ? -1 : b > a ? 1 : b >= a ? 0 : NaN;
	}

	function identity$7(d) {
	  return d;
	}

	function pie() {
	  var value = identity$7,
	      sortValues = descending$1,
	      sort = null,
	      startAngle = constant$10(0),
	      endAngle = constant$10(tau$4),
	      padAngle = constant$10(0);

	  function pie(data) {
	    var i,
	        n = data.length,
	        j,
	        k,
	        sum = 0,
	        index = new Array(n),
	        arcs = new Array(n),
	        a0 = +startAngle.apply(this, arguments),
	        da = Math.min(tau$4, Math.max(-tau$4, endAngle.apply(this, arguments) - a0)),
	        a1,
	        p = Math.min(Math.abs(da) / n, padAngle.apply(this, arguments)),
	        pa = p * (da < 0 ? -1 : 1),
	        v;

	    for (i = 0; i < n; ++i) {
	      if ((v = arcs[index[i] = i] = +value(data[i], i, data)) > 0) {
	        sum += v;
	      }
	    }

	    // Optionally sort the arcs by previously-computed values or by data.
	    if (sortValues != null) index.sort(function(i, j) { return sortValues(arcs[i], arcs[j]); });
	    else if (sort != null) index.sort(function(i, j) { return sort(data[i], data[j]); });

	    // Compute the arcs! They are stored in the original data's order.
	    for (i = 0, k = sum ? (da - n * pa) / sum : 0; i < n; ++i, a0 = a1) {
	      j = index[i], v = arcs[j], a1 = a0 + (v > 0 ? v * k : 0) + pa, arcs[j] = {
	        data: data[j],
	        index: i,
	        value: v,
	        startAngle: a0,
	        endAngle: a1,
	        padAngle: p
	      };
	    }

	    return arcs;
	  }

	  pie.value = function(_) {
	    return arguments.length ? (value = typeof _ === "function" ? _ : constant$10(+_), pie) : value;
	  };

	  pie.sortValues = function(_) {
	    return arguments.length ? (sortValues = _, sort = null, pie) : sortValues;
	  };

	  pie.sort = function(_) {
	    return arguments.length ? (sort = _, sortValues = null, pie) : sort;
	  };

	  pie.startAngle = function(_) {
	    return arguments.length ? (startAngle = typeof _ === "function" ? _ : constant$10(+_), pie) : startAngle;
	  };

	  pie.endAngle = function(_) {
	    return arguments.length ? (endAngle = typeof _ === "function" ? _ : constant$10(+_), pie) : endAngle;
	  };

	  pie.padAngle = function(_) {
	    return arguments.length ? (padAngle = typeof _ === "function" ? _ : constant$10(+_), pie) : padAngle;
	  };

	  return pie;
	}

	var curveRadialLinear = curveRadial(curveLinear);

	function Radial(curve) {
	  this._curve = curve;
	}

	Radial.prototype = {
	  areaStart: function() {
	    this._curve.areaStart();
	  },
	  areaEnd: function() {
	    this._curve.areaEnd();
	  },
	  lineStart: function() {
	    this._curve.lineStart();
	  },
	  lineEnd: function() {
	    this._curve.lineEnd();
	  },
	  point: function(a, r) {
	    this._curve.point(r * Math.sin(a), r * -Math.cos(a));
	  }
	};

	function curveRadial(curve) {

	  function radial(context) {
	    return new Radial(curve(context));
	  }

	  radial._curve = curve;

	  return radial;
	}

	function lineRadial(l) {
	  var c = l.curve;

	  l.angle = l.x, delete l.x;
	  l.radius = l.y, delete l.y;

	  l.curve = function(_) {
	    return arguments.length ? c(curveRadial(_)) : c()._curve;
	  };

	  return l;
	}

	function lineRadial$1() {
	  return lineRadial(line().curve(curveRadialLinear));
	}

	function areaRadial() {
	  var a = area$2().curve(curveRadialLinear),
	      c = a.curve,
	      x0 = a.lineX0,
	      x1 = a.lineX1,
	      y0 = a.lineY0,
	      y1 = a.lineY1;

	  a.angle = a.x, delete a.x;
	  a.startAngle = a.x0, delete a.x0;
	  a.endAngle = a.x1, delete a.x1;
	  a.radius = a.y, delete a.y;
	  a.innerRadius = a.y0, delete a.y0;
	  a.outerRadius = a.y1, delete a.y1;
	  a.lineStartAngle = function() { return lineRadial(x0()); }, delete a.lineX0;
	  a.lineEndAngle = function() { return lineRadial(x1()); }, delete a.lineX1;
	  a.lineInnerRadius = function() { return lineRadial(y0()); }, delete a.lineY0;
	  a.lineOuterRadius = function() { return lineRadial(y1()); }, delete a.lineY1;

	  a.curve = function(_) {
	    return arguments.length ? c(curveRadial(_)) : c()._curve;
	  };

	  return a;
	}

	function pointRadial(x, y) {
	  return [(y = +y) * Math.cos(x -= Math.PI / 2), y * Math.sin(x)];
	}

	var slice$6 = Array.prototype.slice;

	function linkSource(d) {
	  return d.source;
	}

	function linkTarget(d) {
	  return d.target;
	}

	function link$2(curve) {
	  var source = linkSource,
	      target = linkTarget,
	      x$$1 = x$3,
	      y$$1 = y$3,
	      context = null;

	  function link() {
	    var buffer, argv = slice$6.call(arguments), s = source.apply(this, argv), t = target.apply(this, argv);
	    if (!context) context = buffer = path();
	    curve(context, +x$$1.apply(this, (argv[0] = s, argv)), +y$$1.apply(this, argv), +x$$1.apply(this, (argv[0] = t, argv)), +y$$1.apply(this, argv));
	    if (buffer) return context = null, buffer + "" || null;
	  }

	  link.source = function(_) {
	    return arguments.length ? (source = _, link) : source;
	  };

	  link.target = function(_) {
	    return arguments.length ? (target = _, link) : target;
	  };

	  link.x = function(_) {
	    return arguments.length ? (x$$1 = typeof _ === "function" ? _ : constant$10(+_), link) : x$$1;
	  };

	  link.y = function(_) {
	    return arguments.length ? (y$$1 = typeof _ === "function" ? _ : constant$10(+_), link) : y$$1;
	  };

	  link.context = function(_) {
	    return arguments.length ? (context = _ == null ? null : _, link) : context;
	  };

	  return link;
	}

	function curveHorizontal(context, x0, y0, x1, y1) {
	  context.moveTo(x0, y0);
	  context.bezierCurveTo(x0 = (x0 + x1) / 2, y0, x0, y1, x1, y1);
	}

	function curveVertical(context, x0, y0, x1, y1) {
	  context.moveTo(x0, y0);
	  context.bezierCurveTo(x0, y0 = (y0 + y1) / 2, x1, y0, x1, y1);
	}

	function curveRadial$1(context, x0, y0, x1, y1) {
	  var p0 = pointRadial(x0, y0),
	      p1 = pointRadial(x0, y0 = (y0 + y1) / 2),
	      p2 = pointRadial(x1, y0),
	      p3 = pointRadial(x1, y1);
	  context.moveTo(p0[0], p0[1]);
	  context.bezierCurveTo(p1[0], p1[1], p2[0], p2[1], p3[0], p3[1]);
	}

	function linkHorizontal() {
	  return link$2(curveHorizontal);
	}

	function linkVertical() {
	  return link$2(curveVertical);
	}

	function linkRadial() {
	  var l = link$2(curveRadial$1);
	  l.angle = l.x, delete l.x;
	  l.radius = l.y, delete l.y;
	  return l;
	}

	var circle$2 = {
	  draw: function(context, size) {
	    var r = Math.sqrt(size / pi$4);
	    context.moveTo(r, 0);
	    context.arc(0, 0, r, 0, tau$4);
	  }
	};

	var cross$2 = {
	  draw: function(context, size) {
	    var r = Math.sqrt(size / 5) / 2;
	    context.moveTo(-3 * r, -r);
	    context.lineTo(-r, -r);
	    context.lineTo(-r, -3 * r);
	    context.lineTo(r, -3 * r);
	    context.lineTo(r, -r);
	    context.lineTo(3 * r, -r);
	    context.lineTo(3 * r, r);
	    context.lineTo(r, r);
	    context.lineTo(r, 3 * r);
	    context.lineTo(-r, 3 * r);
	    context.lineTo(-r, r);
	    context.lineTo(-3 * r, r);
	    context.closePath();
	  }
	};

	var tan30 = Math.sqrt(1 / 3);
	var tan30_2 = tan30 * 2;

	var diamond = {
	  draw: function(context, size) {
	    var y = Math.sqrt(size / tan30_2),
	        x = y * tan30;
	    context.moveTo(0, -y);
	    context.lineTo(x, 0);
	    context.lineTo(0, y);
	    context.lineTo(-x, 0);
	    context.closePath();
	  }
	};

	var ka = 0.89081309152928522810;
	var kr = Math.sin(pi$4 / 10) / Math.sin(7 * pi$4 / 10);
	var kx = Math.sin(tau$4 / 10) * kr;
	var ky = -Math.cos(tau$4 / 10) * kr;

	var star = {
	  draw: function(context, size) {
	    var r = Math.sqrt(size * ka),
	        x = kx * r,
	        y = ky * r;
	    context.moveTo(0, -r);
	    context.lineTo(x, y);
	    for (var i = 1; i < 5; ++i) {
	      var a = tau$4 * i / 5,
	          c = Math.cos(a),
	          s = Math.sin(a);
	      context.lineTo(s * r, -c * r);
	      context.lineTo(c * x - s * y, s * x + c * y);
	    }
	    context.closePath();
	  }
	};

	var square = {
	  draw: function(context, size) {
	    var w = Math.sqrt(size),
	        x = -w / 2;
	    context.rect(x, x, w, w);
	  }
	};

	var sqrt3 = Math.sqrt(3);

	var triangle = {
	  draw: function(context, size) {
	    var y = -Math.sqrt(size / (sqrt3 * 3));
	    context.moveTo(0, y * 2);
	    context.lineTo(-sqrt3 * y, -y);
	    context.lineTo(sqrt3 * y, -y);
	    context.closePath();
	  }
	};

	var c = -0.5;
	var s = Math.sqrt(3) / 2;
	var k = 1 / Math.sqrt(12);
	var a = (k / 2 + 1) * 3;

	var wye = {
	  draw: function(context, size) {
	    var r = Math.sqrt(size / a),
	        x0 = r / 2,
	        y0 = r * k,
	        x1 = x0,
	        y1 = r * k + r,
	        x2 = -x1,
	        y2 = y1;
	    context.moveTo(x0, y0);
	    context.lineTo(x1, y1);
	    context.lineTo(x2, y2);
	    context.lineTo(c * x0 - s * y0, s * x0 + c * y0);
	    context.lineTo(c * x1 - s * y1, s * x1 + c * y1);
	    context.lineTo(c * x2 - s * y2, s * x2 + c * y2);
	    context.lineTo(c * x0 + s * y0, c * y0 - s * x0);
	    context.lineTo(c * x1 + s * y1, c * y1 - s * x1);
	    context.lineTo(c * x2 + s * y2, c * y2 - s * x2);
	    context.closePath();
	  }
	};

	var symbols = [
	  circle$2,
	  cross$2,
	  diamond,
	  square,
	  star,
	  triangle,
	  wye
	];

	function symbol() {
	  var type = constant$10(circle$2),
	      size = constant$10(64),
	      context = null;

	  function symbol() {
	    var buffer;
	    if (!context) context = buffer = path();
	    type.apply(this, arguments).draw(context, +size.apply(this, arguments));
	    if (buffer) return context = null, buffer + "" || null;
	  }

	  symbol.type = function(_) {
	    return arguments.length ? (type = typeof _ === "function" ? _ : constant$10(_), symbol) : type;
	  };

	  symbol.size = function(_) {
	    return arguments.length ? (size = typeof _ === "function" ? _ : constant$10(+_), symbol) : size;
	  };

	  symbol.context = function(_) {
	    return arguments.length ? (context = _ == null ? null : _, symbol) : context;
	  };

	  return symbol;
	}

	function noop$2() {}

	function point$2(that, x, y) {
	  that._context.bezierCurveTo(
	    (2 * that._x0 + that._x1) / 3,
	    (2 * that._y0 + that._y1) / 3,
	    (that._x0 + 2 * that._x1) / 3,
	    (that._y0 + 2 * that._y1) / 3,
	    (that._x0 + 4 * that._x1 + x) / 6,
	    (that._y0 + 4 * that._y1 + y) / 6
	  );
	}

	function Basis(context) {
	  this._context = context;
	}

	Basis.prototype = {
	  areaStart: function() {
	    this._line = 0;
	  },
	  areaEnd: function() {
	    this._line = NaN;
	  },
	  lineStart: function() {
	    this._x0 = this._x1 =
	    this._y0 = this._y1 = NaN;
	    this._point = 0;
	  },
	  lineEnd: function() {
	    switch (this._point) {
	      case 3: point$2(this, this._x1, this._y1); // proceed
	      case 2: this._context.lineTo(this._x1, this._y1); break;
	    }
	    if (this._line || (this._line !== 0 && this._point === 1)) this._context.closePath();
	    this._line = 1 - this._line;
	  },
	  point: function(x, y) {
	    x = +x, y = +y;
	    switch (this._point) {
	      case 0: this._point = 1; this._line ? this._context.lineTo(x, y) : this._context.moveTo(x, y); break;
	      case 1: this._point = 2; break;
	      case 2: this._point = 3; this._context.lineTo((5 * this._x0 + this._x1) / 6, (5 * this._y0 + this._y1) / 6); // proceed
	      default: point$2(this, x, y); break;
	    }
	    this._x0 = this._x1, this._x1 = x;
	    this._y0 = this._y1, this._y1 = y;
	  }
	};

	function basis$2(context) {
	  return new Basis(context);
	}

	function BasisClosed(context) {
	  this._context = context;
	}

	BasisClosed.prototype = {
	  areaStart: noop$2,
	  areaEnd: noop$2,
	  lineStart: function() {
	    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 =
	    this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = NaN;
	    this._point = 0;
	  },
	  lineEnd: function() {
	    switch (this._point) {
	      case 1: {
	        this._context.moveTo(this._x2, this._y2);
	        this._context.closePath();
	        break;
	      }
	      case 2: {
	        this._context.moveTo((this._x2 + 2 * this._x3) / 3, (this._y2 + 2 * this._y3) / 3);
	        this._context.lineTo((this._x3 + 2 * this._x2) / 3, (this._y3 + 2 * this._y2) / 3);
	        this._context.closePath();
	        break;
	      }
	      case 3: {
	        this.point(this._x2, this._y2);
	        this.point(this._x3, this._y3);
	        this.point(this._x4, this._y4);
	        break;
	      }
	    }
	  },
	  point: function(x, y) {
	    x = +x, y = +y;
	    switch (this._point) {
	      case 0: this._point = 1; this._x2 = x, this._y2 = y; break;
	      case 1: this._point = 2; this._x3 = x, this._y3 = y; break;
	      case 2: this._point = 3; this._x4 = x, this._y4 = y; this._context.moveTo((this._x0 + 4 * this._x1 + x) / 6, (this._y0 + 4 * this._y1 + y) / 6); break;
	      default: point$2(this, x, y); break;
	    }
	    this._x0 = this._x1, this._x1 = x;
	    this._y0 = this._y1, this._y1 = y;
	  }
	};

	function basisClosed$1(context) {
	  return new BasisClosed(context);
	}

	function BasisOpen(context) {
	  this._context = context;
	}

	BasisOpen.prototype = {
	  areaStart: function() {
	    this._line = 0;
	  },
	  areaEnd: function() {
	    this._line = NaN;
	  },
	  lineStart: function() {
	    this._x0 = this._x1 =
	    this._y0 = this._y1 = NaN;
	    this._point = 0;
	  },
	  lineEnd: function() {
	    if (this._line || (this._line !== 0 && this._point === 3)) this._context.closePath();
	    this._line = 1 - this._line;
	  },
	  point: function(x, y) {
	    x = +x, y = +y;
	    switch (this._point) {
	      case 0: this._point = 1; break;
	      case 1: this._point = 2; break;
	      case 2: this._point = 3; var x0 = (this._x0 + 4 * this._x1 + x) / 6, y0 = (this._y0 + 4 * this._y1 + y) / 6; this._line ? this._context.lineTo(x0, y0) : this._context.moveTo(x0, y0); break;
	      case 3: this._point = 4; // proceed
	      default: point$2(this, x, y); break;
	    }
	    this._x0 = this._x1, this._x1 = x;
	    this._y0 = this._y1, this._y1 = y;
	  }
	};

	function basisOpen(context) {
	  return new BasisOpen(context);
	}

	function Bundle(context, beta) {
	  this._basis = new Basis(context);
	  this._beta = beta;
	}

	Bundle.prototype = {
	  lineStart: function() {
	    this._x = [];
	    this._y = [];
	    this._basis.lineStart();
	  },
	  lineEnd: function() {
	    var x = this._x,
	        y = this._y,
	        j = x.length - 1;

	    if (j > 0) {
	      var x0 = x[0],
	          y0 = y[0],
	          dx = x[j] - x0,
	          dy = y[j] - y0,
	          i = -1,
	          t;

	      while (++i <= j) {
	        t = i / j;
	        this._basis.point(
	          this._beta * x[i] + (1 - this._beta) * (x0 + t * dx),
	          this._beta * y[i] + (1 - this._beta) * (y0 + t * dy)
	        );
	      }
	    }

	    this._x = this._y = null;
	    this._basis.lineEnd();
	  },
	  point: function(x, y) {
	    this._x.push(+x);
	    this._y.push(+y);
	  }
	};

	var bundle = (function custom(beta) {

	  function bundle(context) {
	    return beta === 1 ? new Basis(context) : new Bundle(context, beta);
	  }

	  bundle.beta = function(beta) {
	    return custom(+beta);
	  };

	  return bundle;
	})(0.85);

	function point$3(that, x, y) {
	  that._context.bezierCurveTo(
	    that._x1 + that._k * (that._x2 - that._x0),
	    that._y1 + that._k * (that._y2 - that._y0),
	    that._x2 + that._k * (that._x1 - x),
	    that._y2 + that._k * (that._y1 - y),
	    that._x2,
	    that._y2
	  );
	}

	function Cardinal(context, tension) {
	  this._context = context;
	  this._k = (1 - tension) / 6;
	}

	Cardinal.prototype = {
	  areaStart: function() {
	    this._line = 0;
	  },
	  areaEnd: function() {
	    this._line = NaN;
	  },
	  lineStart: function() {
	    this._x0 = this._x1 = this._x2 =
	    this._y0 = this._y1 = this._y2 = NaN;
	    this._point = 0;
	  },
	  lineEnd: function() {
	    switch (this._point) {
	      case 2: this._context.lineTo(this._x2, this._y2); break;
	      case 3: point$3(this, this._x1, this._y1); break;
	    }
	    if (this._line || (this._line !== 0 && this._point === 1)) this._context.closePath();
	    this._line = 1 - this._line;
	  },
	  point: function(x, y) {
	    x = +x, y = +y;
	    switch (this._point) {
	      case 0: this._point = 1; this._line ? this._context.lineTo(x, y) : this._context.moveTo(x, y); break;
	      case 1: this._point = 2; this._x1 = x, this._y1 = y; break;
	      case 2: this._point = 3; // proceed
	      default: point$3(this, x, y); break;
	    }
	    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x;
	    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y;
	  }
	};

	var cardinal = (function custom(tension) {

	  function cardinal(context) {
	    return new Cardinal(context, tension);
	  }

	  cardinal.tension = function(tension) {
	    return custom(+tension);
	  };

	  return cardinal;
	})(0);

	function CardinalClosed(context, tension) {
	  this._context = context;
	  this._k = (1 - tension) / 6;
	}

	CardinalClosed.prototype = {
	  areaStart: noop$2,
	  areaEnd: noop$2,
	  lineStart: function() {
	    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._x5 =
	    this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = this._y5 = NaN;
	    this._point = 0;
	  },
	  lineEnd: function() {
	    switch (this._point) {
	      case 1: {
	        this._context.moveTo(this._x3, this._y3);
	        this._context.closePath();
	        break;
	      }
	      case 2: {
	        this._context.lineTo(this._x3, this._y3);
	        this._context.closePath();
	        break;
	      }
	      case 3: {
	        this.point(this._x3, this._y3);
	        this.point(this._x4, this._y4);
	        this.point(this._x5, this._y5);
	        break;
	      }
	    }
	  },
	  point: function(x, y) {
	    x = +x, y = +y;
	    switch (this._point) {
	      case 0: this._point = 1; this._x3 = x, this._y3 = y; break;
	      case 1: this._point = 2; this._context.moveTo(this._x4 = x, this._y4 = y); break;
	      case 2: this._point = 3; this._x5 = x, this._y5 = y; break;
	      default: point$3(this, x, y); break;
	    }
	    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x;
	    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y;
	  }
	};

	var cardinalClosed = (function custom(tension) {

	  function cardinal$$1(context) {
	    return new CardinalClosed(context, tension);
	  }

	  cardinal$$1.tension = function(tension) {
	    return custom(+tension);
	  };

	  return cardinal$$1;
	})(0);

	function CardinalOpen(context, tension) {
	  this._context = context;
	  this._k = (1 - tension) / 6;
	}

	CardinalOpen.prototype = {
	  areaStart: function() {
	    this._line = 0;
	  },
	  areaEnd: function() {
	    this._line = NaN;
	  },
	  lineStart: function() {
	    this._x0 = this._x1 = this._x2 =
	    this._y0 = this._y1 = this._y2 = NaN;
	    this._point = 0;
	  },
	  lineEnd: function() {
	    if (this._line || (this._line !== 0 && this._point === 3)) this._context.closePath();
	    this._line = 1 - this._line;
	  },
	  point: function(x, y) {
	    x = +x, y = +y;
	    switch (this._point) {
	      case 0: this._point = 1; break;
	      case 1: this._point = 2; break;
	      case 2: this._point = 3; this._line ? this._context.lineTo(this._x2, this._y2) : this._context.moveTo(this._x2, this._y2); break;
	      case 3: this._point = 4; // proceed
	      default: point$3(this, x, y); break;
	    }
	    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x;
	    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y;
	  }
	};

	var cardinalOpen = (function custom(tension) {

	  function cardinal$$1(context) {
	    return new CardinalOpen(context, tension);
	  }

	  cardinal$$1.tension = function(tension) {
	    return custom(+tension);
	  };

	  return cardinal$$1;
	})(0);

	function point$4(that, x, y) {
	  var x1 = that._x1,
	      y1 = that._y1,
	      x2 = that._x2,
	      y2 = that._y2;

	  if (that._l01_a > epsilon$3) {
	    var a = 2 * that._l01_2a + 3 * that._l01_a * that._l12_a + that._l12_2a,
	        n = 3 * that._l01_a * (that._l01_a + that._l12_a);
	    x1 = (x1 * a - that._x0 * that._l12_2a + that._x2 * that._l01_2a) / n;
	    y1 = (y1 * a - that._y0 * that._l12_2a + that._y2 * that._l01_2a) / n;
	  }

	  if (that._l23_a > epsilon$3) {
	    var b = 2 * that._l23_2a + 3 * that._l23_a * that._l12_a + that._l12_2a,
	        m = 3 * that._l23_a * (that._l23_a + that._l12_a);
	    x2 = (x2 * b + that._x1 * that._l23_2a - x * that._l12_2a) / m;
	    y2 = (y2 * b + that._y1 * that._l23_2a - y * that._l12_2a) / m;
	  }

	  that._context.bezierCurveTo(x1, y1, x2, y2, that._x2, that._y2);
	}

	function CatmullRom(context, alpha) {
	  this._context = context;
	  this._alpha = alpha;
	}

	CatmullRom.prototype = {
	  areaStart: function() {
	    this._line = 0;
	  },
	  areaEnd: function() {
	    this._line = NaN;
	  },
	  lineStart: function() {
	    this._x0 = this._x1 = this._x2 =
	    this._y0 = this._y1 = this._y2 = NaN;
	    this._l01_a = this._l12_a = this._l23_a =
	    this._l01_2a = this._l12_2a = this._l23_2a =
	    this._point = 0;
	  },
	  lineEnd: function() {
	    switch (this._point) {
	      case 2: this._context.lineTo(this._x2, this._y2); break;
	      case 3: this.point(this._x2, this._y2); break;
	    }
	    if (this._line || (this._line !== 0 && this._point === 1)) this._context.closePath();
	    this._line = 1 - this._line;
	  },
	  point: function(x, y) {
	    x = +x, y = +y;

	    if (this._point) {
	      var x23 = this._x2 - x,
	          y23 = this._y2 - y;
	      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
	    }

	    switch (this._point) {
	      case 0: this._point = 1; this._line ? this._context.lineTo(x, y) : this._context.moveTo(x, y); break;
	      case 1: this._point = 2; break;
	      case 2: this._point = 3; // proceed
	      default: point$4(this, x, y); break;
	    }

	    this._l01_a = this._l12_a, this._l12_a = this._l23_a;
	    this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
	    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x;
	    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y;
	  }
	};

	var catmullRom = (function custom(alpha) {

	  function catmullRom(context) {
	    return alpha ? new CatmullRom(context, alpha) : new Cardinal(context, 0);
	  }

	  catmullRom.alpha = function(alpha) {
	    return custom(+alpha);
	  };

	  return catmullRom;
	})(0.5);

	function CatmullRomClosed(context, alpha) {
	  this._context = context;
	  this._alpha = alpha;
	}

	CatmullRomClosed.prototype = {
	  areaStart: noop$2,
	  areaEnd: noop$2,
	  lineStart: function() {
	    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._x5 =
	    this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = this._y5 = NaN;
	    this._l01_a = this._l12_a = this._l23_a =
	    this._l01_2a = this._l12_2a = this._l23_2a =
	    this._point = 0;
	  },
	  lineEnd: function() {
	    switch (this._point) {
	      case 1: {
	        this._context.moveTo(this._x3, this._y3);
	        this._context.closePath();
	        break;
	      }
	      case 2: {
	        this._context.lineTo(this._x3, this._y3);
	        this._context.closePath();
	        break;
	      }
	      case 3: {
	        this.point(this._x3, this._y3);
	        this.point(this._x4, this._y4);
	        this.point(this._x5, this._y5);
	        break;
	      }
	    }
	  },
	  point: function(x, y) {
	    x = +x, y = +y;

	    if (this._point) {
	      var x23 = this._x2 - x,
	          y23 = this._y2 - y;
	      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
	    }

	    switch (this._point) {
	      case 0: this._point = 1; this._x3 = x, this._y3 = y; break;
	      case 1: this._point = 2; this._context.moveTo(this._x4 = x, this._y4 = y); break;
	      case 2: this._point = 3; this._x5 = x, this._y5 = y; break;
	      default: point$4(this, x, y); break;
	    }

	    this._l01_a = this._l12_a, this._l12_a = this._l23_a;
	    this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
	    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x;
	    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y;
	  }
	};

	var catmullRomClosed = (function custom(alpha) {

	  function catmullRom$$1(context) {
	    return alpha ? new CatmullRomClosed(context, alpha) : new CardinalClosed(context, 0);
	  }

	  catmullRom$$1.alpha = function(alpha) {
	    return custom(+alpha);
	  };

	  return catmullRom$$1;
	})(0.5);

	function CatmullRomOpen(context, alpha) {
	  this._context = context;
	  this._alpha = alpha;
	}

	CatmullRomOpen.prototype = {
	  areaStart: function() {
	    this._line = 0;
	  },
	  areaEnd: function() {
	    this._line = NaN;
	  },
	  lineStart: function() {
	    this._x0 = this._x1 = this._x2 =
	    this._y0 = this._y1 = this._y2 = NaN;
	    this._l01_a = this._l12_a = this._l23_a =
	    this._l01_2a = this._l12_2a = this._l23_2a =
	    this._point = 0;
	  },
	  lineEnd: function() {
	    if (this._line || (this._line !== 0 && this._point === 3)) this._context.closePath();
	    this._line = 1 - this._line;
	  },
	  point: function(x, y) {
	    x = +x, y = +y;

	    if (this._point) {
	      var x23 = this._x2 - x,
	          y23 = this._y2 - y;
	      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
	    }

	    switch (this._point) {
	      case 0: this._point = 1; break;
	      case 1: this._point = 2; break;
	      case 2: this._point = 3; this._line ? this._context.lineTo(this._x2, this._y2) : this._context.moveTo(this._x2, this._y2); break;
	      case 3: this._point = 4; // proceed
	      default: point$4(this, x, y); break;
	    }

	    this._l01_a = this._l12_a, this._l12_a = this._l23_a;
	    this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
	    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x;
	    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y;
	  }
	};

	var catmullRomOpen = (function custom(alpha) {

	  function catmullRom$$1(context) {
	    return alpha ? new CatmullRomOpen(context, alpha) : new CardinalOpen(context, 0);
	  }

	  catmullRom$$1.alpha = function(alpha) {
	    return custom(+alpha);
	  };

	  return catmullRom$$1;
	})(0.5);

	function LinearClosed(context) {
	  this._context = context;
	}

	LinearClosed.prototype = {
	  areaStart: noop$2,
	  areaEnd: noop$2,
	  lineStart: function() {
	    this._point = 0;
	  },
	  lineEnd: function() {
	    if (this._point) this._context.closePath();
	  },
	  point: function(x, y) {
	    x = +x, y = +y;
	    if (this._point) this._context.lineTo(x, y);
	    else this._point = 1, this._context.moveTo(x, y);
	  }
	};

	function linearClosed(context) {
	  return new LinearClosed(context);
	}

	function sign$1(x) {
	  return x < 0 ? -1 : 1;
	}

	// Calculate the slopes of the tangents (Hermite-type interpolation) based on
	// the following paper: Steffen, M. 1990. A Simple Method for Monotonic
	// Interpolation in One Dimension. Astronomy and Astrophysics, Vol. 239, NO.
	// NOV(II), P. 443, 1990.
	function slope3(that, x2, y2) {
	  var h0 = that._x1 - that._x0,
	      h1 = x2 - that._x1,
	      s0 = (that._y1 - that._y0) / (h0 || h1 < 0 && -0),
	      s1 = (y2 - that._y1) / (h1 || h0 < 0 && -0),
	      p = (s0 * h1 + s1 * h0) / (h0 + h1);
	  return (sign$1(s0) + sign$1(s1)) * Math.min(Math.abs(s0), Math.abs(s1), 0.5 * Math.abs(p)) || 0;
	}

	// Calculate a one-sided slope.
	function slope2(that, t) {
	  var h = that._x1 - that._x0;
	  return h ? (3 * (that._y1 - that._y0) / h - t) / 2 : t;
	}

	// According to https://en.wikipedia.org/wiki/Cubic_Hermite_spline#Representations
	// "you can express cubic Hermite interpolation in terms of cubic Bézier curves
	// with respect to the four values p0, p0 + m0 / 3, p1 - m1 / 3, p1".
	function point$5(that, t0, t1) {
	  var x0 = that._x0,
	      y0 = that._y0,
	      x1 = that._x1,
	      y1 = that._y1,
	      dx = (x1 - x0) / 3;
	  that._context.bezierCurveTo(x0 + dx, y0 + dx * t0, x1 - dx, y1 - dx * t1, x1, y1);
	}

	function MonotoneX(context) {
	  this._context = context;
	}

	MonotoneX.prototype = {
	  areaStart: function() {
	    this._line = 0;
	  },
	  areaEnd: function() {
	    this._line = NaN;
	  },
	  lineStart: function() {
	    this._x0 = this._x1 =
	    this._y0 = this._y1 =
	    this._t0 = NaN;
	    this._point = 0;
	  },
	  lineEnd: function() {
	    switch (this._point) {
	      case 2: this._context.lineTo(this._x1, this._y1); break;
	      case 3: point$5(this, this._t0, slope2(this, this._t0)); break;
	    }
	    if (this._line || (this._line !== 0 && this._point === 1)) this._context.closePath();
	    this._line = 1 - this._line;
	  },
	  point: function(x, y) {
	    var t1 = NaN;

	    x = +x, y = +y;
	    if (x === this._x1 && y === this._y1) return; // Ignore coincident points.
	    switch (this._point) {
	      case 0: this._point = 1; this._line ? this._context.lineTo(x, y) : this._context.moveTo(x, y); break;
	      case 1: this._point = 2; break;
	      case 2: this._point = 3; point$5(this, slope2(this, t1 = slope3(this, x, y)), t1); break;
	      default: point$5(this, this._t0, t1 = slope3(this, x, y)); break;
	    }

	    this._x0 = this._x1, this._x1 = x;
	    this._y0 = this._y1, this._y1 = y;
	    this._t0 = t1;
	  }
	};

	function MonotoneY(context) {
	  this._context = new ReflectContext(context);
	}

	(MonotoneY.prototype = Object.create(MonotoneX.prototype)).point = function(x, y) {
	  MonotoneX.prototype.point.call(this, y, x);
	};

	function ReflectContext(context) {
	  this._context = context;
	}

	ReflectContext.prototype = {
	  moveTo: function(x, y) { this._context.moveTo(y, x); },
	  closePath: function() { this._context.closePath(); },
	  lineTo: function(x, y) { this._context.lineTo(y, x); },
	  bezierCurveTo: function(x1, y1, x2, y2, x, y) { this._context.bezierCurveTo(y1, x1, y2, x2, y, x); }
	};

	function monotoneX(context) {
	  return new MonotoneX(context);
	}

	function monotoneY(context) {
	  return new MonotoneY(context);
	}

	function Natural(context) {
	  this._context = context;
	}

	Natural.prototype = {
	  areaStart: function() {
	    this._line = 0;
	  },
	  areaEnd: function() {
	    this._line = NaN;
	  },
	  lineStart: function() {
	    this._x = [];
	    this._y = [];
	  },
	  lineEnd: function() {
	    var x = this._x,
	        y = this._y,
	        n = x.length;

	    if (n) {
	      this._line ? this._context.lineTo(x[0], y[0]) : this._context.moveTo(x[0], y[0]);
	      if (n === 2) {
	        this._context.lineTo(x[1], y[1]);
	      } else {
	        var px = controlPoints(x),
	            py = controlPoints(y);
	        for (var i0 = 0, i1 = 1; i1 < n; ++i0, ++i1) {
	          this._context.bezierCurveTo(px[0][i0], py[0][i0], px[1][i0], py[1][i0], x[i1], y[i1]);
	        }
	      }
	    }

	    if (this._line || (this._line !== 0 && n === 1)) this._context.closePath();
	    this._line = 1 - this._line;
	    this._x = this._y = null;
	  },
	  point: function(x, y) {
	    this._x.push(+x);
	    this._y.push(+y);
	  }
	};

	// See https://www.particleincell.com/2012/bezier-splines/ for derivation.
	function controlPoints(x) {
	  var i,
	      n = x.length - 1,
	      m,
	      a = new Array(n),
	      b = new Array(n),
	      r = new Array(n);
	  a[0] = 0, b[0] = 2, r[0] = x[0] + 2 * x[1];
	  for (i = 1; i < n - 1; ++i) a[i] = 1, b[i] = 4, r[i] = 4 * x[i] + 2 * x[i + 1];
	  a[n - 1] = 2, b[n - 1] = 7, r[n - 1] = 8 * x[n - 1] + x[n];
	  for (i = 1; i < n; ++i) m = a[i] / b[i - 1], b[i] -= m, r[i] -= m * r[i - 1];
	  a[n - 1] = r[n - 1] / b[n - 1];
	  for (i = n - 2; i >= 0; --i) a[i] = (r[i] - a[i + 1]) / b[i];
	  b[n - 1] = (x[n] + a[n - 1]) / 2;
	  for (i = 0; i < n - 1; ++i) b[i] = 2 * x[i + 1] - a[i + 1];
	  return [a, b];
	}

	function natural(context) {
	  return new Natural(context);
	}

	function Step(context, t) {
	  this._context = context;
	  this._t = t;
	}

	Step.prototype = {
	  areaStart: function() {
	    this._line = 0;
	  },
	  areaEnd: function() {
	    this._line = NaN;
	  },
	  lineStart: function() {
	    this._x = this._y = NaN;
	    this._point = 0;
	  },
	  lineEnd: function() {
	    if (0 < this._t && this._t < 1 && this._point === 2) this._context.lineTo(this._x, this._y);
	    if (this._line || (this._line !== 0 && this._point === 1)) this._context.closePath();
	    if (this._line >= 0) this._t = 1 - this._t, this._line = 1 - this._line;
	  },
	  point: function(x, y) {
	    x = +x, y = +y;
	    switch (this._point) {
	      case 0: this._point = 1; this._line ? this._context.lineTo(x, y) : this._context.moveTo(x, y); break;
	      case 1: this._point = 2; // proceed
	      default: {
	        if (this._t <= 0) {
	          this._context.lineTo(this._x, y);
	          this._context.lineTo(x, y);
	        } else {
	          var x1 = this._x * (1 - this._t) + x * this._t;
	          this._context.lineTo(x1, this._y);
	          this._context.lineTo(x1, y);
	        }
	        break;
	      }
	    }
	    this._x = x, this._y = y;
	  }
	};

	function step(context) {
	  return new Step(context, 0.5);
	}

	function stepBefore(context) {
	  return new Step(context, 0);
	}

	function stepAfter(context) {
	  return new Step(context, 1);
	}

	function none$1(series, order) {
	  if (!((n = series.length) > 1)) return;
	  for (var i = 1, j, s0, s1 = series[order[0]], n, m = s1.length; i < n; ++i) {
	    s0 = s1, s1 = series[order[i]];
	    for (j = 0; j < m; ++j) {
	      s1[j][1] += s1[j][0] = isNaN(s0[j][1]) ? s0[j][0] : s0[j][1];
	    }
	  }
	}

	function none$2(series) {
	  var n = series.length, o = new Array(n);
	  while (--n >= 0) o[n] = n;
	  return o;
	}

	function stackValue(d, key) {
	  return d[key];
	}

	function stack() {
	  var keys = constant$10([]),
	      order = none$2,
	      offset = none$1,
	      value = stackValue;

	  function stack(data) {
	    var kz = keys.apply(this, arguments),
	        i,
	        m = data.length,
	        n = kz.length,
	        sz = new Array(n),
	        oz;

	    for (i = 0; i < n; ++i) {
	      for (var ki = kz[i], si = sz[i] = new Array(m), j = 0, sij; j < m; ++j) {
	        si[j] = sij = [0, +value(data[j], ki, j, data)];
	        sij.data = data[j];
	      }
	      si.key = ki;
	    }

	    for (i = 0, oz = order(sz); i < n; ++i) {
	      sz[oz[i]].index = i;
	    }

	    offset(sz, oz);
	    return sz;
	  }

	  stack.keys = function(_) {
	    return arguments.length ? (keys = typeof _ === "function" ? _ : constant$10(slice$6.call(_)), stack) : keys;
	  };

	  stack.value = function(_) {
	    return arguments.length ? (value = typeof _ === "function" ? _ : constant$10(+_), stack) : value;
	  };

	  stack.order = function(_) {
	    return arguments.length ? (order = _ == null ? none$2 : typeof _ === "function" ? _ : constant$10(slice$6.call(_)), stack) : order;
	  };

	  stack.offset = function(_) {
	    return arguments.length ? (offset = _ == null ? none$1 : _, stack) : offset;
	  };

	  return stack;
	}

	function expand(series, order) {
	  if (!((n = series.length) > 0)) return;
	  for (var i, n, j = 0, m = series[0].length, y; j < m; ++j) {
	    for (y = i = 0; i < n; ++i) y += series[i][j][1] || 0;
	    if (y) for (i = 0; i < n; ++i) series[i][j][1] /= y;
	  }
	  none$1(series, order);
	}

	function diverging(series, order) {
	  if (!((n = series.length) > 1)) return;
	  for (var i, j = 0, d, dy, yp, yn, n, m = series[order[0]].length; j < m; ++j) {
	    for (yp = yn = 0, i = 0; i < n; ++i) {
	      if ((dy = (d = series[order[i]][j])[1] - d[0]) >= 0) {
	        d[0] = yp, d[1] = yp += dy;
	      } else if (dy < 0) {
	        d[1] = yn, d[0] = yn += dy;
	      } else {
	        d[0] = yp;
	      }
	    }
	  }
	}

	function silhouette(series, order) {
	  if (!((n = series.length) > 0)) return;
	  for (var j = 0, s0 = series[order[0]], n, m = s0.length; j < m; ++j) {
	    for (var i = 0, y = 0; i < n; ++i) y += series[i][j][1] || 0;
	    s0[j][1] += s0[j][0] = -y / 2;
	  }
	  none$1(series, order);
	}

	function wiggle(series, order) {
	  if (!((n = series.length) > 0) || !((m = (s0 = series[order[0]]).length) > 0)) return;
	  for (var y = 0, j = 1, s0, m, n; j < m; ++j) {
	    for (var i = 0, s1 = 0, s2 = 0; i < n; ++i) {
	      var si = series[order[i]],
	          sij0 = si[j][1] || 0,
	          sij1 = si[j - 1][1] || 0,
	          s3 = (sij0 - sij1) / 2;
	      for (var k = 0; k < i; ++k) {
	        var sk = series[order[k]],
	            skj0 = sk[j][1] || 0,
	            skj1 = sk[j - 1][1] || 0;
	        s3 += skj0 - skj1;
	      }
	      s1 += sij0, s2 += s3 * sij0;
	    }
	    s0[j - 1][1] += s0[j - 1][0] = y;
	    if (s1) y -= s2 / s1;
	  }
	  s0[j - 1][1] += s0[j - 1][0] = y;
	  none$1(series, order);
	}

	function ascending$2(series) {
	  var sums = series.map(sum$2);
	  return none$2(series).sort(function(a, b) { return sums[a] - sums[b]; });
	}

	function sum$2(series) {
	  var s = 0, i = -1, n = series.length, v;
	  while (++i < n) if (v = +series[i][1]) s += v;
	  return s;
	}

	function descending$2(series) {
	  return ascending$2(series).reverse();
	}

	function insideOut(series) {
	  var n = series.length,
	      i,
	      j,
	      sums = series.map(sum$2),
	      order = none$2(series).sort(function(a, b) { return sums[b] - sums[a]; }),
	      top = 0,
	      bottom = 0,
	      tops = [],
	      bottoms = [];

	  for (i = 0; i < n; ++i) {
	    j = order[i];
	    if (top < bottom) {
	      top += sums[j];
	      tops.push(j);
	    } else {
	      bottom += sums[j];
	      bottoms.push(j);
	    }
	  }

	  return bottoms.reverse().concat(tops);
	}

	function reverse(series) {
	  return none$2(series).reverse();
	}

	function constant$11(x) {
	  return function() {
	    return x;
	  };
	}

	function x$4(d) {
	  return d[0];
	}

	function y$4(d) {
	  return d[1];
	}

	function RedBlackTree() {
	  this._ = null; // root node
	}

	function RedBlackNode(node) {
	  node.U = // parent node
	  node.C = // color - true for red, false for black
	  node.L = // left node
	  node.R = // right node
	  node.P = // previous node
	  node.N = null; // next node
	}

	RedBlackTree.prototype = {
	  constructor: RedBlackTree,

	  insert: function(after, node) {
	    var parent, grandpa, uncle;

	    if (after) {
	      node.P = after;
	      node.N = after.N;
	      if (after.N) after.N.P = node;
	      after.N = node;
	      if (after.R) {
	        after = after.R;
	        while (after.L) after = after.L;
	        after.L = node;
	      } else {
	        after.R = node;
	      }
	      parent = after;
	    } else if (this._) {
	      after = RedBlackFirst(this._);
	      node.P = null;
	      node.N = after;
	      after.P = after.L = node;
	      parent = after;
	    } else {
	      node.P = node.N = null;
	      this._ = node;
	      parent = null;
	    }
	    node.L = node.R = null;
	    node.U = parent;
	    node.C = true;

	    after = node;
	    while (parent && parent.C) {
	      grandpa = parent.U;
	      if (parent === grandpa.L) {
	        uncle = grandpa.R;
	        if (uncle && uncle.C) {
	          parent.C = uncle.C = false;
	          grandpa.C = true;
	          after = grandpa;
	        } else {
	          if (after === parent.R) {
	            RedBlackRotateLeft(this, parent);
	            after = parent;
	            parent = after.U;
	          }
	          parent.C = false;
	          grandpa.C = true;
	          RedBlackRotateRight(this, grandpa);
	        }
	      } else {
	        uncle = grandpa.L;
	        if (uncle && uncle.C) {
	          parent.C = uncle.C = false;
	          grandpa.C = true;
	          after = grandpa;
	        } else {
	          if (after === parent.L) {
	            RedBlackRotateRight(this, parent);
	            after = parent;
	            parent = after.U;
	          }
	          parent.C = false;
	          grandpa.C = true;
	          RedBlackRotateLeft(this, grandpa);
	        }
	      }
	      parent = after.U;
	    }
	    this._.C = false;
	  },

	  remove: function(node) {
	    if (node.N) node.N.P = node.P;
	    if (node.P) node.P.N = node.N;
	    node.N = node.P = null;

	    var parent = node.U,
	        sibling,
	        left = node.L,
	        right = node.R,
	        next,
	        red;

	    if (!left) next = right;
	    else if (!right) next = left;
	    else next = RedBlackFirst(right);

	    if (parent) {
	      if (parent.L === node) parent.L = next;
	      else parent.R = next;
	    } else {
	      this._ = next;
	    }

	    if (left && right) {
	      red = next.C;
	      next.C = node.C;
	      next.L = left;
	      left.U = next;
	      if (next !== right) {
	        parent = next.U;
	        next.U = node.U;
	        node = next.R;
	        parent.L = node;
	        next.R = right;
	        right.U = next;
	      } else {
	        next.U = parent;
	        parent = next;
	        node = next.R;
	      }
	    } else {
	      red = node.C;
	      node = next;
	    }

	    if (node) node.U = parent;
	    if (red) return;
	    if (node && node.C) { node.C = false; return; }

	    do {
	      if (node === this._) break;
	      if (node === parent.L) {
	        sibling = parent.R;
	        if (sibling.C) {
	          sibling.C = false;
	          parent.C = true;
	          RedBlackRotateLeft(this, parent);
	          sibling = parent.R;
	        }
	        if ((sibling.L && sibling.L.C)
	            || (sibling.R && sibling.R.C)) {
	          if (!sibling.R || !sibling.R.C) {
	            sibling.L.C = false;
	            sibling.C = true;
	            RedBlackRotateRight(this, sibling);
	            sibling = parent.R;
	          }
	          sibling.C = parent.C;
	          parent.C = sibling.R.C = false;
	          RedBlackRotateLeft(this, parent);
	          node = this._;
	          break;
	        }
	      } else {
	        sibling = parent.L;
	        if (sibling.C) {
	          sibling.C = false;
	          parent.C = true;
	          RedBlackRotateRight(this, parent);
	          sibling = parent.L;
	        }
	        if ((sibling.L && sibling.L.C)
	          || (sibling.R && sibling.R.C)) {
	          if (!sibling.L || !sibling.L.C) {
	            sibling.R.C = false;
	            sibling.C = true;
	            RedBlackRotateLeft(this, sibling);
	            sibling = parent.L;
	          }
	          sibling.C = parent.C;
	          parent.C = sibling.L.C = false;
	          RedBlackRotateRight(this, parent);
	          node = this._;
	          break;
	        }
	      }
	      sibling.C = true;
	      node = parent;
	      parent = parent.U;
	    } while (!node.C);

	    if (node) node.C = false;
	  }
	};

	function RedBlackRotateLeft(tree, node) {
	  var p = node,
	      q = node.R,
	      parent = p.U;

	  if (parent) {
	    if (parent.L === p) parent.L = q;
	    else parent.R = q;
	  } else {
	    tree._ = q;
	  }

	  q.U = parent;
	  p.U = q;
	  p.R = q.L;
	  if (p.R) p.R.U = p;
	  q.L = p;
	}

	function RedBlackRotateRight(tree, node) {
	  var p = node,
	      q = node.L,
	      parent = p.U;

	  if (parent) {
	    if (parent.L === p) parent.L = q;
	    else parent.R = q;
	  } else {
	    tree._ = q;
	  }

	  q.U = parent;
	  p.U = q;
	  p.L = q.R;
	  if (p.L) p.L.U = p;
	  q.R = p;
	}

	function RedBlackFirst(node) {
	  while (node.L) node = node.L;
	  return node;
	}

	function createEdge(left, right, v0, v1) {
	  var edge = [null, null],
	      index = edges.push(edge) - 1;
	  edge.left = left;
	  edge.right = right;
	  if (v0) setEdgeEnd(edge, left, right, v0);
	  if (v1) setEdgeEnd(edge, right, left, v1);
	  cells[left.index].halfedges.push(index);
	  cells[right.index].halfedges.push(index);
	  return edge;
	}

	function createBorderEdge(left, v0, v1) {
	  var edge = [v0, v1];
	  edge.left = left;
	  return edge;
	}

	function setEdgeEnd(edge, left, right, vertex) {
	  if (!edge[0] && !edge[1]) {
	    edge[0] = vertex;
	    edge.left = left;
	    edge.right = right;
	  } else if (edge.left === right) {
	    edge[1] = vertex;
	  } else {
	    edge[0] = vertex;
	  }
	}

	// Liang–Barsky line clipping.
	function clipEdge(edge, x0, y0, x1, y1) {
	  var a = edge[0],
	      b = edge[1],
	      ax = a[0],
	      ay = a[1],
	      bx = b[0],
	      by = b[1],
	      t0 = 0,
	      t1 = 1,
	      dx = bx - ax,
	      dy = by - ay,
	      r;

	  r = x0 - ax;
	  if (!dx && r > 0) return;
	  r /= dx;
	  if (dx < 0) {
	    if (r < t0) return;
	    if (r < t1) t1 = r;
	  } else if (dx > 0) {
	    if (r > t1) return;
	    if (r > t0) t0 = r;
	  }

	  r = x1 - ax;
	  if (!dx && r < 0) return;
	  r /= dx;
	  if (dx < 0) {
	    if (r > t1) return;
	    if (r > t0) t0 = r;
	  } else if (dx > 0) {
	    if (r < t0) return;
	    if (r < t1) t1 = r;
	  }

	  r = y0 - ay;
	  if (!dy && r > 0) return;
	  r /= dy;
	  if (dy < 0) {
	    if (r < t0) return;
	    if (r < t1) t1 = r;
	  } else if (dy > 0) {
	    if (r > t1) return;
	    if (r > t0) t0 = r;
	  }

	  r = y1 - ay;
	  if (!dy && r < 0) return;
	  r /= dy;
	  if (dy < 0) {
	    if (r > t1) return;
	    if (r > t0) t0 = r;
	  } else if (dy > 0) {
	    if (r < t0) return;
	    if (r < t1) t1 = r;
	  }

	  if (!(t0 > 0) && !(t1 < 1)) return true; // TODO Better check?

	  if (t0 > 0) edge[0] = [ax + t0 * dx, ay + t0 * dy];
	  if (t1 < 1) edge[1] = [ax + t1 * dx, ay + t1 * dy];
	  return true;
	}

	function connectEdge(edge, x0, y0, x1, y1) {
	  var v1 = edge[1];
	  if (v1) return true;

	  var v0 = edge[0],
	      left = edge.left,
	      right = edge.right,
	      lx = left[0],
	      ly = left[1],
	      rx = right[0],
	      ry = right[1],
	      fx = (lx + rx) / 2,
	      fy = (ly + ry) / 2,
	      fm,
	      fb;

	  if (ry === ly) {
	    if (fx < x0 || fx >= x1) return;
	    if (lx > rx) {
	      if (!v0) v0 = [fx, y0];
	      else if (v0[1] >= y1) return;
	      v1 = [fx, y1];
	    } else {
	      if (!v0) v0 = [fx, y1];
	      else if (v0[1] < y0) return;
	      v1 = [fx, y0];
	    }
	  } else {
	    fm = (lx - rx) / (ry - ly);
	    fb = fy - fm * fx;
	    if (fm < -1 || fm > 1) {
	      if (lx > rx) {
	        if (!v0) v0 = [(y0 - fb) / fm, y0];
	        else if (v0[1] >= y1) return;
	        v1 = [(y1 - fb) / fm, y1];
	      } else {
	        if (!v0) v0 = [(y1 - fb) / fm, y1];
	        else if (v0[1] < y0) return;
	        v1 = [(y0 - fb) / fm, y0];
	      }
	    } else {
	      if (ly < ry) {
	        if (!v0) v0 = [x0, fm * x0 + fb];
	        else if (v0[0] >= x1) return;
	        v1 = [x1, fm * x1 + fb];
	      } else {
	        if (!v0) v0 = [x1, fm * x1 + fb];
	        else if (v0[0] < x0) return;
	        v1 = [x0, fm * x0 + fb];
	      }
	    }
	  }

	  edge[0] = v0;
	  edge[1] = v1;
	  return true;
	}

	function clipEdges(x0, y0, x1, y1) {
	  var i = edges.length,
	      edge;

	  while (i--) {
	    if (!connectEdge(edge = edges[i], x0, y0, x1, y1)
	        || !clipEdge(edge, x0, y0, x1, y1)
	        || !(Math.abs(edge[0][0] - edge[1][0]) > epsilon$4
	            || Math.abs(edge[0][1] - edge[1][1]) > epsilon$4)) {
	      delete edges[i];
	    }
	  }
	}

	function createCell(site) {
	  return cells[site.index] = {
	    site: site,
	    halfedges: []
	  };
	}

	function cellHalfedgeAngle(cell, edge) {
	  var site = cell.site,
	      va = edge.left,
	      vb = edge.right;
	  if (site === vb) vb = va, va = site;
	  if (vb) return Math.atan2(vb[1] - va[1], vb[0] - va[0]);
	  if (site === va) va = edge[1], vb = edge[0];
	  else va = edge[0], vb = edge[1];
	  return Math.atan2(va[0] - vb[0], vb[1] - va[1]);
	}

	function cellHalfedgeStart(cell, edge) {
	  return edge[+(edge.left !== cell.site)];
	}

	function cellHalfedgeEnd(cell, edge) {
	  return edge[+(edge.left === cell.site)];
	}

	function sortCellHalfedges() {
	  for (var i = 0, n = cells.length, cell, halfedges, j, m; i < n; ++i) {
	    if ((cell = cells[i]) && (m = (halfedges = cell.halfedges).length)) {
	      var index = new Array(m),
	          array = new Array(m);
	      for (j = 0; j < m; ++j) index[j] = j, array[j] = cellHalfedgeAngle(cell, edges[halfedges[j]]);
	      index.sort(function(i, j) { return array[j] - array[i]; });
	      for (j = 0; j < m; ++j) array[j] = halfedges[index[j]];
	      for (j = 0; j < m; ++j) halfedges[j] = array[j];
	    }
	  }
	}

	function clipCells(x0, y0, x1, y1) {
	  var nCells = cells.length,
	      iCell,
	      cell,
	      site,
	      iHalfedge,
	      halfedges,
	      nHalfedges,
	      start,
	      startX,
	      startY,
	      end,
	      endX,
	      endY,
	      cover = true;

	  for (iCell = 0; iCell < nCells; ++iCell) {
	    if (cell = cells[iCell]) {
	      site = cell.site;
	      halfedges = cell.halfedges;
	      iHalfedge = halfedges.length;

	      // Remove any dangling clipped edges.
	      while (iHalfedge--) {
	        if (!edges[halfedges[iHalfedge]]) {
	          halfedges.splice(iHalfedge, 1);
	        }
	      }

	      // Insert any border edges as necessary.
	      iHalfedge = 0, nHalfedges = halfedges.length;
	      while (iHalfedge < nHalfedges) {
	        end = cellHalfedgeEnd(cell, edges[halfedges[iHalfedge]]), endX = end[0], endY = end[1];
	        start = cellHalfedgeStart(cell, edges[halfedges[++iHalfedge % nHalfedges]]), startX = start[0], startY = start[1];
	        if (Math.abs(endX - startX) > epsilon$4 || Math.abs(endY - startY) > epsilon$4) {
	          halfedges.splice(iHalfedge, 0, edges.push(createBorderEdge(site, end,
	              Math.abs(endX - x0) < epsilon$4 && y1 - endY > epsilon$4 ? [x0, Math.abs(startX - x0) < epsilon$4 ? startY : y1]
	              : Math.abs(endY - y1) < epsilon$4 && x1 - endX > epsilon$4 ? [Math.abs(startY - y1) < epsilon$4 ? startX : x1, y1]
	              : Math.abs(endX - x1) < epsilon$4 && endY - y0 > epsilon$4 ? [x1, Math.abs(startX - x1) < epsilon$4 ? startY : y0]
	              : Math.abs(endY - y0) < epsilon$4 && endX - x0 > epsilon$4 ? [Math.abs(startY - y0) < epsilon$4 ? startX : x0, y0]
	              : null)) - 1);
	          ++nHalfedges;
	        }
	      }

	      if (nHalfedges) cover = false;
	    }
	  }

	  // If there weren’t any edges, have the closest site cover the extent.
	  // It doesn’t matter which corner of the extent we measure!
	  if (cover) {
	    var dx, dy, d2, dc = Infinity;

	    for (iCell = 0, cover = null; iCell < nCells; ++iCell) {
	      if (cell = cells[iCell]) {
	        site = cell.site;
	        dx = site[0] - x0;
	        dy = site[1] - y0;
	        d2 = dx * dx + dy * dy;
	        if (d2 < dc) dc = d2, cover = cell;
	      }
	    }

	    if (cover) {
	      var v00 = [x0, y0], v01 = [x0, y1], v11 = [x1, y1], v10 = [x1, y0];
	      cover.halfedges.push(
	        edges.push(createBorderEdge(site = cover.site, v00, v01)) - 1,
	        edges.push(createBorderEdge(site, v01, v11)) - 1,
	        edges.push(createBorderEdge(site, v11, v10)) - 1,
	        edges.push(createBorderEdge(site, v10, v00)) - 1
	      );
	    }
	  }

	  // Lastly delete any cells with no edges; these were entirely clipped.
	  for (iCell = 0; iCell < nCells; ++iCell) {
	    if (cell = cells[iCell]) {
	      if (!cell.halfedges.length) {
	        delete cells[iCell];
	      }
	    }
	  }
	}

	var circlePool = [];

	var firstCircle;

	function Circle() {
	  RedBlackNode(this);
	  this.x =
	  this.y =
	  this.arc =
	  this.site =
	  this.cy = null;
	}

	function attachCircle(arc) {
	  var lArc = arc.P,
	      rArc = arc.N;

	  if (!lArc || !rArc) return;

	  var lSite = lArc.site,
	      cSite = arc.site,
	      rSite = rArc.site;

	  if (lSite === rSite) return;

	  var bx = cSite[0],
	      by = cSite[1],
	      ax = lSite[0] - bx,
	      ay = lSite[1] - by,
	      cx = rSite[0] - bx,
	      cy = rSite[1] - by;

	  var d = 2 * (ax * cy - ay * cx);
	  if (d >= -epsilon2$2) return;

	  var ha = ax * ax + ay * ay,
	      hc = cx * cx + cy * cy,
	      x = (cy * ha - ay * hc) / d,
	      y = (ax * hc - cx * ha) / d;

	  var circle = circlePool.pop() || new Circle;
	  circle.arc = arc;
	  circle.site = cSite;
	  circle.x = x + bx;
	  circle.y = (circle.cy = y + by) + Math.sqrt(x * x + y * y); // y bottom

	  arc.circle = circle;

	  var before = null,
	      node = circles._;

	  while (node) {
	    if (circle.y < node.y || (circle.y === node.y && circle.x <= node.x)) {
	      if (node.L) node = node.L;
	      else { before = node.P; break; }
	    } else {
	      if (node.R) node = node.R;
	      else { before = node; break; }
	    }
	  }

	  circles.insert(before, circle);
	  if (!before) firstCircle = circle;
	}

	function detachCircle(arc) {
	  var circle = arc.circle;
	  if (circle) {
	    if (!circle.P) firstCircle = circle.N;
	    circles.remove(circle);
	    circlePool.push(circle);
	    RedBlackNode(circle);
	    arc.circle = null;
	  }
	}

	var beachPool = [];

	function Beach() {
	  RedBlackNode(this);
	  this.edge =
	  this.site =
	  this.circle = null;
	}

	function createBeach(site) {
	  var beach = beachPool.pop() || new Beach;
	  beach.site = site;
	  return beach;
	}

	function detachBeach(beach) {
	  detachCircle(beach);
	  beaches.remove(beach);
	  beachPool.push(beach);
	  RedBlackNode(beach);
	}

	function removeBeach(beach) {
	  var circle = beach.circle,
	      x = circle.x,
	      y = circle.cy,
	      vertex = [x, y],
	      previous = beach.P,
	      next = beach.N,
	      disappearing = [beach];

	  detachBeach(beach);

	  var lArc = previous;
	  while (lArc.circle
	      && Math.abs(x - lArc.circle.x) < epsilon$4
	      && Math.abs(y - lArc.circle.cy) < epsilon$4) {
	    previous = lArc.P;
	    disappearing.unshift(lArc);
	    detachBeach(lArc);
	    lArc = previous;
	  }

	  disappearing.unshift(lArc);
	  detachCircle(lArc);

	  var rArc = next;
	  while (rArc.circle
	      && Math.abs(x - rArc.circle.x) < epsilon$4
	      && Math.abs(y - rArc.circle.cy) < epsilon$4) {
	    next = rArc.N;
	    disappearing.push(rArc);
	    detachBeach(rArc);
	    rArc = next;
	  }

	  disappearing.push(rArc);
	  detachCircle(rArc);

	  var nArcs = disappearing.length,
	      iArc;
	  for (iArc = 1; iArc < nArcs; ++iArc) {
	    rArc = disappearing[iArc];
	    lArc = disappearing[iArc - 1];
	    setEdgeEnd(rArc.edge, lArc.site, rArc.site, vertex);
	  }

	  lArc = disappearing[0];
	  rArc = disappearing[nArcs - 1];
	  rArc.edge = createEdge(lArc.site, rArc.site, null, vertex);

	  attachCircle(lArc);
	  attachCircle(rArc);
	}

	function addBeach(site) {
	  var x = site[0],
	      directrix = site[1],
	      lArc,
	      rArc,
	      dxl,
	      dxr,
	      node = beaches._;

	  while (node) {
	    dxl = leftBreakPoint(node, directrix) - x;
	    if (dxl > epsilon$4) node = node.L; else {
	      dxr = x - rightBreakPoint(node, directrix);
	      if (dxr > epsilon$4) {
	        if (!node.R) {
	          lArc = node;
	          break;
	        }
	        node = node.R;
	      } else {
	        if (dxl > -epsilon$4) {
	          lArc = node.P;
	          rArc = node;
	        } else if (dxr > -epsilon$4) {
	          lArc = node;
	          rArc = node.N;
	        } else {
	          lArc = rArc = node;
	        }
	        break;
	      }
	    }
	  }

	  createCell(site);
	  var newArc = createBeach(site);
	  beaches.insert(lArc, newArc);

	  if (!lArc && !rArc) return;

	  if (lArc === rArc) {
	    detachCircle(lArc);
	    rArc = createBeach(lArc.site);
	    beaches.insert(newArc, rArc);
	    newArc.edge = rArc.edge = createEdge(lArc.site, newArc.site);
	    attachCircle(lArc);
	    attachCircle(rArc);
	    return;
	  }

	  if (!rArc) { // && lArc
	    newArc.edge = createEdge(lArc.site, newArc.site);
	    return;
	  }

	  // else lArc !== rArc
	  detachCircle(lArc);
	  detachCircle(rArc);

	  var lSite = lArc.site,
	      ax = lSite[0],
	      ay = lSite[1],
	      bx = site[0] - ax,
	      by = site[1] - ay,
	      rSite = rArc.site,
	      cx = rSite[0] - ax,
	      cy = rSite[1] - ay,
	      d = 2 * (bx * cy - by * cx),
	      hb = bx * bx + by * by,
	      hc = cx * cx + cy * cy,
	      vertex = [(cy * hb - by * hc) / d + ax, (bx * hc - cx * hb) / d + ay];

	  setEdgeEnd(rArc.edge, lSite, rSite, vertex);
	  newArc.edge = createEdge(lSite, site, null, vertex);
	  rArc.edge = createEdge(site, rSite, null, vertex);
	  attachCircle(lArc);
	  attachCircle(rArc);
	}

	function leftBreakPoint(arc, directrix) {
	  var site = arc.site,
	      rfocx = site[0],
	      rfocy = site[1],
	      pby2 = rfocy - directrix;

	  if (!pby2) return rfocx;

	  var lArc = arc.P;
	  if (!lArc) return -Infinity;

	  site = lArc.site;
	  var lfocx = site[0],
	      lfocy = site[1],
	      plby2 = lfocy - directrix;

	  if (!plby2) return lfocx;

	  var hl = lfocx - rfocx,
	      aby2 = 1 / pby2 - 1 / plby2,
	      b = hl / plby2;

	  if (aby2) return (-b + Math.sqrt(b * b - 2 * aby2 * (hl * hl / (-2 * plby2) - lfocy + plby2 / 2 + rfocy - pby2 / 2))) / aby2 + rfocx;

	  return (rfocx + lfocx) / 2;
	}

	function rightBreakPoint(arc, directrix) {
	  var rArc = arc.N;
	  if (rArc) return leftBreakPoint(rArc, directrix);
	  var site = arc.site;
	  return site[1] === directrix ? site[0] : Infinity;
	}

	var epsilon$4 = 1e-6;
	var epsilon2$2 = 1e-12;
	var beaches;
	var cells;
	var circles;
	var edges;

	function triangleArea(a, b, c) {
	  return (a[0] - c[0]) * (b[1] - a[1]) - (a[0] - b[0]) * (c[1] - a[1]);
	}

	function lexicographic(a, b) {
	  return b[1] - a[1]
	      || b[0] - a[0];
	}

	function Diagram(sites, extent) {
	  var site = sites.sort(lexicographic).pop(),
	      x,
	      y,
	      circle;

	  edges = [];
	  cells = new Array(sites.length);
	  beaches = new RedBlackTree;
	  circles = new RedBlackTree;

	  while (true) {
	    circle = firstCircle;
	    if (site && (!circle || site[1] < circle.y || (site[1] === circle.y && site[0] < circle.x))) {
	      if (site[0] !== x || site[1] !== y) {
	        addBeach(site);
	        x = site[0], y = site[1];
	      }
	      site = sites.pop();
	    } else if (circle) {
	      removeBeach(circle.arc);
	    } else {
	      break;
	    }
	  }

	  sortCellHalfedges();

	  if (extent) {
	    var x0 = +extent[0][0],
	        y0 = +extent[0][1],
	        x1 = +extent[1][0],
	        y1 = +extent[1][1];
	    clipEdges(x0, y0, x1, y1);
	    clipCells(x0, y0, x1, y1);
	  }

	  this.edges = edges;
	  this.cells = cells;

	  beaches =
	  circles =
	  edges =
	  cells = null;
	}

	Diagram.prototype = {
	  constructor: Diagram,

	  polygons: function() {
	    var edges = this.edges;

	    return this.cells.map(function(cell) {
	      var polygon = cell.halfedges.map(function(i) { return cellHalfedgeStart(cell, edges[i]); });
	      polygon.data = cell.site.data;
	      return polygon;
	    });
	  },

	  triangles: function() {
	    var triangles = [],
	        edges = this.edges;

	    this.cells.forEach(function(cell, i) {
	      if (!(m = (halfedges = cell.halfedges).length)) return;
	      var site = cell.site,
	          halfedges,
	          j = -1,
	          m,
	          s0,
	          e1 = edges[halfedges[m - 1]],
	          s1 = e1.left === site ? e1.right : e1.left;

	      while (++j < m) {
	        s0 = s1;
	        e1 = edges[halfedges[j]];
	        s1 = e1.left === site ? e1.right : e1.left;
	        if (s0 && s1 && i < s0.index && i < s1.index && triangleArea(site, s0, s1) < 0) {
	          triangles.push([site.data, s0.data, s1.data]);
	        }
	      }
	    });

	    return triangles;
	  },

	  links: function() {
	    return this.edges.filter(function(edge) {
	      return edge.right;
	    }).map(function(edge) {
	      return {
	        source: edge.left.data,
	        target: edge.right.data
	      };
	    });
	  },

	  find: function(x, y, radius) {
	    var that = this, i0, i1 = that._found || 0, n = that.cells.length, cell;

	    // Use the previously-found cell, or start with an arbitrary one.
	    while (!(cell = that.cells[i1])) if (++i1 >= n) return null;
	    var dx = x - cell.site[0], dy = y - cell.site[1], d2 = dx * dx + dy * dy;

	    // Traverse the half-edges to find a closer cell, if any.
	    do {
	      cell = that.cells[i0 = i1], i1 = null;
	      cell.halfedges.forEach(function(e) {
	        var edge = that.edges[e], v = edge.left;
	        if ((v === cell.site || !v) && !(v = edge.right)) return;
	        var vx = x - v[0], vy = y - v[1], v2 = vx * vx + vy * vy;
	        if (v2 < d2) d2 = v2, i1 = v.index;
	      });
	    } while (i1 !== null);

	    that._found = i0;

	    return radius == null || d2 <= radius * radius ? cell.site : null;
	  }
	};

	function voronoi() {
	  var x$$1 = x$4,
	      y$$1 = y$4,
	      extent = null;

	  function voronoi(data) {
	    return new Diagram(data.map(function(d, i) {
	      var s = [Math.round(x$$1(d, i, data) / epsilon$4) * epsilon$4, Math.round(y$$1(d, i, data) / epsilon$4) * epsilon$4];
	      s.index = i;
	      s.data = d;
	      return s;
	    }), extent);
	  }

	  voronoi.polygons = function(data) {
	    return voronoi(data).polygons();
	  };

	  voronoi.links = function(data) {
	    return voronoi(data).links();
	  };

	  voronoi.triangles = function(data) {
	    return voronoi(data).triangles();
	  };

	  voronoi.x = function(_) {
	    return arguments.length ? (x$$1 = typeof _ === "function" ? _ : constant$11(+_), voronoi) : x$$1;
	  };

	  voronoi.y = function(_) {
	    return arguments.length ? (y$$1 = typeof _ === "function" ? _ : constant$11(+_), voronoi) : y$$1;
	  };

	  voronoi.extent = function(_) {
	    return arguments.length ? (extent = _ == null ? null : [[+_[0][0], +_[0][1]], [+_[1][0], +_[1][1]]], voronoi) : extent && [[extent[0][0], extent[0][1]], [extent[1][0], extent[1][1]]];
	  };

	  voronoi.size = function(_) {
	    return arguments.length ? (extent = _ == null ? null : [[0, 0], [+_[0], +_[1]]], voronoi) : extent && [extent[1][0] - extent[0][0], extent[1][1] - extent[0][1]];
	  };

	  return voronoi;
	}

	function constant$12(x) {
	  return function() {
	    return x;
	  };
	}

	function ZoomEvent(target, type, transform) {
	  this.target = target;
	  this.type = type;
	  this.transform = transform;
	}

	function Transform(k, x, y) {
	  this.k = k;
	  this.x = x;
	  this.y = y;
	}

	Transform.prototype = {
	  constructor: Transform,
	  scale: function(k) {
	    return k === 1 ? this : new Transform(this.k * k, this.x, this.y);
	  },
	  translate: function(x, y) {
	    return x === 0 & y === 0 ? this : new Transform(this.k, this.x + this.k * x, this.y + this.k * y);
	  },
	  apply: function(point) {
	    return [point[0] * this.k + this.x, point[1] * this.k + this.y];
	  },
	  applyX: function(x) {
	    return x * this.k + this.x;
	  },
	  applyY: function(y) {
	    return y * this.k + this.y;
	  },
	  invert: function(location) {
	    return [(location[0] - this.x) / this.k, (location[1] - this.y) / this.k];
	  },
	  invertX: function(x) {
	    return (x - this.x) / this.k;
	  },
	  invertY: function(y) {
	    return (y - this.y) / this.k;
	  },
	  rescaleX: function(x) {
	    return x.copy().domain(x.range().map(this.invertX, this).map(x.invert, x));
	  },
	  rescaleY: function(y) {
	    return y.copy().domain(y.range().map(this.invertY, this).map(y.invert, y));
	  },
	  toString: function() {
	    return "translate(" + this.x + "," + this.y + ") scale(" + this.k + ")";
	  }
	};

	var identity$8 = new Transform(1, 0, 0);

	transform$1.prototype = Transform.prototype;

	function transform$1(node) {
	  return node.__zoom || identity$8;
	}

	function nopropagation$2() {
	  exports.event.stopImmediatePropagation();
	}

	function noevent$2() {
	  exports.event.preventDefault();
	  exports.event.stopImmediatePropagation();
	}

	// Ignore right-click, since that should open the context menu.
	function defaultFilter$2() {
	  return !exports.event.button;
	}

	function defaultExtent$1() {
	  var e = this, w, h;
	  if (e instanceof SVGElement) {
	    e = e.ownerSVGElement || e;
	    w = e.width.baseVal.value;
	    h = e.height.baseVal.value;
	  } else {
	    w = e.clientWidth;
	    h = e.clientHeight;
	  }
	  return [[0, 0], [w, h]];
	}

	function defaultTransform() {
	  return this.__zoom || identity$8;
	}

	function defaultWheelDelta() {
	  return -exports.event.deltaY * (exports.event.deltaMode ? 120 : 1) / 500;
	}

	function defaultTouchable$1() {
	  return "ontouchstart" in this;
	}

	function defaultConstrain(transform$$1, extent, translateExtent) {
	  var dx0 = transform$$1.invertX(extent[0][0]) - translateExtent[0][0],
	      dx1 = transform$$1.invertX(extent[1][0]) - translateExtent[1][0],
	      dy0 = transform$$1.invertY(extent[0][1]) - translateExtent[0][1],
	      dy1 = transform$$1.invertY(extent[1][1]) - translateExtent[1][1];
	  return transform$$1.translate(
	    dx1 > dx0 ? (dx0 + dx1) / 2 : Math.min(0, dx0) || Math.max(0, dx1),
	    dy1 > dy0 ? (dy0 + dy1) / 2 : Math.min(0, dy0) || Math.max(0, dy1)
	  );
	}

	function zoom() {
	  var filter = defaultFilter$2,
	      extent = defaultExtent$1,
	      constrain = defaultConstrain,
	      wheelDelta = defaultWheelDelta,
	      touchable = defaultTouchable$1,
	      scaleExtent = [0, Infinity],
	      translateExtent = [[-Infinity, -Infinity], [Infinity, Infinity]],
	      duration = 250,
	      interpolate = interpolateZoom,
	      gestures = [],
	      listeners = dispatch("start", "zoom", "end"),
	      touchstarting,
	      touchending,
	      touchDelay = 500,
	      wheelDelay = 150,
	      clickDistance2 = 0;

	  function zoom(selection) {
	    selection
	        .property("__zoom", defaultTransform)
	        .on("wheel.zoom", wheeled)
	        .on("mousedown.zoom", mousedowned)
	        .on("dblclick.zoom", dblclicked)
	      .filter(touchable)
	        .on("touchstart.zoom", touchstarted)
	        .on("touchmove.zoom", touchmoved)
	        .on("touchend.zoom touchcancel.zoom", touchended)
	        .style("touch-action", "none")
	        .style("-webkit-tap-highlight-color", "rgba(0,0,0,0)");
	  }

	  zoom.transform = function(collection, transform$$1) {
	    var selection = collection.selection ? collection.selection() : collection;
	    selection.property("__zoom", defaultTransform);
	    if (collection !== selection) {
	      schedule(collection, transform$$1);
	    } else {
	      selection.interrupt().each(function() {
	        gesture(this, arguments)
	            .start()
	            .zoom(null, typeof transform$$1 === "function" ? transform$$1.apply(this, arguments) : transform$$1)
	            .end();
	      });
	    }
	  };

	  zoom.scaleBy = function(selection, k) {
	    zoom.scaleTo(selection, function() {
	      var k0 = this.__zoom.k,
	          k1 = typeof k === "function" ? k.apply(this, arguments) : k;
	      return k0 * k1;
	    });
	  };

	  zoom.scaleTo = function(selection, k) {
	    zoom.transform(selection, function() {
	      var e = extent.apply(this, arguments),
	          t0 = this.__zoom,
	          p0 = centroid(e),
	          p1 = t0.invert(p0),
	          k1 = typeof k === "function" ? k.apply(this, arguments) : k;
	      return constrain(translate(scale(t0, k1), p0, p1), e, translateExtent);
	    });
	  };

	  zoom.translateBy = function(selection, x, y) {
	    zoom.transform(selection, function() {
	      return constrain(this.__zoom.translate(
	        typeof x === "function" ? x.apply(this, arguments) : x,
	        typeof y === "function" ? y.apply(this, arguments) : y
	      ), extent.apply(this, arguments), translateExtent);
	    });
	  };

	  zoom.translateTo = function(selection, x, y) {
	    zoom.transform(selection, function() {
	      var e = extent.apply(this, arguments),
	          t = this.__zoom,
	          p = centroid(e);
	      return constrain(identity$8.translate(p[0], p[1]).scale(t.k).translate(
	        typeof x === "function" ? -x.apply(this, arguments) : -x,
	        typeof y === "function" ? -y.apply(this, arguments) : -y
	      ), e, translateExtent);
	    });
	  };

	  function scale(transform$$1, k) {
	    k = Math.max(scaleExtent[0], Math.min(scaleExtent[1], k));
	    return k === transform$$1.k ? transform$$1 : new Transform(k, transform$$1.x, transform$$1.y);
	  }

	  function translate(transform$$1, p0, p1) {
	    var x = p0[0] - p1[0] * transform$$1.k, y = p0[1] - p1[1] * transform$$1.k;
	    return x === transform$$1.x && y === transform$$1.y ? transform$$1 : new Transform(transform$$1.k, x, y);
	  }

	  function centroid(extent) {
	    return [(+extent[0][0] + +extent[1][0]) / 2, (+extent[0][1] + +extent[1][1]) / 2];
	  }

	  function schedule(transition, transform$$1, center) {
	    transition
	        .on("start.zoom", function() { gesture(this, arguments).start(); })
	        .on("interrupt.zoom end.zoom", function() { gesture(this, arguments).end(); })
	        .tween("zoom", function() {
	          var that = this,
	              args = arguments,
	              g = gesture(that, args),
	              e = extent.apply(that, args),
	              p = center || centroid(e),
	              w = Math.max(e[1][0] - e[0][0], e[1][1] - e[0][1]),
	              a = that.__zoom,
	              b = typeof transform$$1 === "function" ? transform$$1.apply(that, args) : transform$$1,
	              i = interpolate(a.invert(p).concat(w / a.k), b.invert(p).concat(w / b.k));
	          return function(t) {
	            if (t === 1) t = b; // Avoid rounding error on end.
	            else { var l = i(t), k = w / l[2]; t = new Transform(k, p[0] - l[0] * k, p[1] - l[1] * k); }
	            g.zoom(null, t);
	          };
	        });
	  }

	  function gesture(that, args) {
	    for (var i = 0, n = gestures.length, g; i < n; ++i) {
	      if ((g = gestures[i]).that === that) {
	        return g;
	      }
	    }
	    return new Gesture(that, args);
	  }

	  function Gesture(that, args) {
	    this.that = that;
	    this.args = args;
	    this.index = -1;
	    this.active = 0;
	    this.extent = extent.apply(that, args);
	  }

	  Gesture.prototype = {
	    start: function() {
	      if (++this.active === 1) {
	        this.index = gestures.push(this) - 1;
	        this.emit("start");
	      }
	      return this;
	    },
	    zoom: function(key, transform$$1) {
	      if (this.mouse && key !== "mouse") this.mouse[1] = transform$$1.invert(this.mouse[0]);
	      if (this.touch0 && key !== "touch") this.touch0[1] = transform$$1.invert(this.touch0[0]);
	      if (this.touch1 && key !== "touch") this.touch1[1] = transform$$1.invert(this.touch1[0]);
	      this.that.__zoom = transform$$1;
	      this.emit("zoom");
	      return this;
	    },
	    end: function() {
	      if (--this.active === 0) {
	        gestures.splice(this.index, 1);
	        this.index = -1;
	        this.emit("end");
	      }
	      return this;
	    },
	    emit: function(type) {
	      customEvent(new ZoomEvent(zoom, type, this.that.__zoom), listeners.apply, listeners, [type, this.that, this.args]);
	    }
	  };

	  function wheeled() {
	    if (!filter.apply(this, arguments)) return;
	    var g = gesture(this, arguments),
	        t = this.__zoom,
	        k = Math.max(scaleExtent[0], Math.min(scaleExtent[1], t.k * Math.pow(2, wheelDelta.apply(this, arguments)))),
	        p = mouse(this);

	    // If the mouse is in the same location as before, reuse it.
	    // If there were recent wheel events, reset the wheel idle timeout.
	    if (g.wheel) {
	      if (g.mouse[0][0] !== p[0] || g.mouse[0][1] !== p[1]) {
	        g.mouse[1] = t.invert(g.mouse[0] = p);
	      }
	      clearTimeout(g.wheel);
	    }

	    // If this wheel event won’t trigger a transform change, ignore it.
	    else if (t.k === k) return;

	    // Otherwise, capture the mouse point and location at the start.
	    else {
	      g.mouse = [p, t.invert(p)];
	      interrupt(this);
	      g.start();
	    }

	    noevent$2();
	    g.wheel = setTimeout(wheelidled, wheelDelay);
	    g.zoom("mouse", constrain(translate(scale(t, k), g.mouse[0], g.mouse[1]), g.extent, translateExtent));

	    function wheelidled() {
	      g.wheel = null;
	      g.end();
	    }
	  }

	  function mousedowned() {
	    if (touchending || !filter.apply(this, arguments)) return;
	    var g = gesture(this, arguments),
	        v = select(exports.event.view).on("mousemove.zoom", mousemoved, true).on("mouseup.zoom", mouseupped, true),
	        p = mouse(this),
	        x0 = exports.event.clientX,
	        y0 = exports.event.clientY;

	    dragDisable(exports.event.view);
	    nopropagation$2();
	    g.mouse = [p, this.__zoom.invert(p)];
	    interrupt(this);
	    g.start();

	    function mousemoved() {
	      noevent$2();
	      if (!g.moved) {
	        var dx = exports.event.clientX - x0, dy = exports.event.clientY - y0;
	        g.moved = dx * dx + dy * dy > clickDistance2;
	      }
	      g.zoom("mouse", constrain(translate(g.that.__zoom, g.mouse[0] = mouse(g.that), g.mouse[1]), g.extent, translateExtent));
	    }

	    function mouseupped() {
	      v.on("mousemove.zoom mouseup.zoom", null);
	      yesdrag(exports.event.view, g.moved);
	      noevent$2();
	      g.end();
	    }
	  }

	  function dblclicked() {
	    if (!filter.apply(this, arguments)) return;
	    var t0 = this.__zoom,
	        p0 = mouse(this),
	        p1 = t0.invert(p0),
	        k1 = t0.k * (exports.event.shiftKey ? 0.5 : 2),
	        t1 = constrain(translate(scale(t0, k1), p0, p1), extent.apply(this, arguments), translateExtent);

	    noevent$2();
	    if (duration > 0) select(this).transition().duration(duration).call(schedule, t1, p0);
	    else select(this).call(zoom.transform, t1);
	  }

	  function touchstarted() {
	    if (!filter.apply(this, arguments)) return;
	    var g = gesture(this, arguments),
	        touches = exports.event.changedTouches,
	        started,
	        n = touches.length, i, t, p;

	    nopropagation$2();
	    for (i = 0; i < n; ++i) {
	      t = touches[i], p = touch(this, touches, t.identifier);
	      p = [p, this.__zoom.invert(p), t.identifier];
	      if (!g.touch0) g.touch0 = p, started = true;
	      else if (!g.touch1) g.touch1 = p;
	    }

	    // If this is a dbltap, reroute to the (optional) dblclick.zoom handler.
	    if (touchstarting) {
	      touchstarting = clearTimeout(touchstarting);
	      if (!g.touch1) {
	        g.end();
	        p = select(this).on("dblclick.zoom");
	        if (p) p.apply(this, arguments);
	        return;
	      }
	    }

	    if (started) {
	      touchstarting = setTimeout(function() { touchstarting = null; }, touchDelay);
	      interrupt(this);
	      g.start();
	    }
	  }

	  function touchmoved() {
	    var g = gesture(this, arguments),
	        touches = exports.event.changedTouches,
	        n = touches.length, i, t, p, l;

	    noevent$2();
	    if (touchstarting) touchstarting = clearTimeout(touchstarting);
	    for (i = 0; i < n; ++i) {
	      t = touches[i], p = touch(this, touches, t.identifier);
	      if (g.touch0 && g.touch0[2] === t.identifier) g.touch0[0] = p;
	      else if (g.touch1 && g.touch1[2] === t.identifier) g.touch1[0] = p;
	    }
	    t = g.that.__zoom;
	    if (g.touch1) {
	      var p0 = g.touch0[0], l0 = g.touch0[1],
	          p1 = g.touch1[0], l1 = g.touch1[1],
	          dp = (dp = p1[0] - p0[0]) * dp + (dp = p1[1] - p0[1]) * dp,
	          dl = (dl = l1[0] - l0[0]) * dl + (dl = l1[1] - l0[1]) * dl;
	      t = scale(t, Math.sqrt(dp / dl));
	      p = [(p0[0] + p1[0]) / 2, (p0[1] + p1[1]) / 2];
	      l = [(l0[0] + l1[0]) / 2, (l0[1] + l1[1]) / 2];
	    }
	    else if (g.touch0) p = g.touch0[0], l = g.touch0[1];
	    else return;
	    g.zoom("touch", constrain(translate(t, p, l), g.extent, translateExtent));
	  }

	  function touchended() {
	    var g = gesture(this, arguments),
	        touches = exports.event.changedTouches,
	        n = touches.length, i, t;

	    nopropagation$2();
	    if (touchending) clearTimeout(touchending);
	    touchending = setTimeout(function() { touchending = null; }, touchDelay);
	    for (i = 0; i < n; ++i) {
	      t = touches[i];
	      if (g.touch0 && g.touch0[2] === t.identifier) delete g.touch0;
	      else if (g.touch1 && g.touch1[2] === t.identifier) delete g.touch1;
	    }
	    if (g.touch1 && !g.touch0) g.touch0 = g.touch1, delete g.touch1;
	    if (g.touch0) g.touch0[1] = this.__zoom.invert(g.touch0[0]);
	    else g.end();
	  }

	  zoom.wheelDelta = function(_) {
	    return arguments.length ? (wheelDelta = typeof _ === "function" ? _ : constant$12(+_), zoom) : wheelDelta;
	  };

	  zoom.filter = function(_) {
	    return arguments.length ? (filter = typeof _ === "function" ? _ : constant$12(!!_), zoom) : filter;
	  };

	  zoom.touchable = function(_) {
	    return arguments.length ? (touchable = typeof _ === "function" ? _ : constant$12(!!_), zoom) : touchable;
	  };

	  zoom.extent = function(_) {
	    return arguments.length ? (extent = typeof _ === "function" ? _ : constant$12([[+_[0][0], +_[0][1]], [+_[1][0], +_[1][1]]]), zoom) : extent;
	  };

	  zoom.scaleExtent = function(_) {
	    return arguments.length ? (scaleExtent[0] = +_[0], scaleExtent[1] = +_[1], zoom) : [scaleExtent[0], scaleExtent[1]];
	  };

	  zoom.translateExtent = function(_) {
	    return arguments.length ? (translateExtent[0][0] = +_[0][0], translateExtent[1][0] = +_[1][0], translateExtent[0][1] = +_[0][1], translateExtent[1][1] = +_[1][1], zoom) : [[translateExtent[0][0], translateExtent[0][1]], [translateExtent[1][0], translateExtent[1][1]]];
	  };

	  zoom.constrain = function(_) {
	    return arguments.length ? (constrain = _, zoom) : constrain;
	  };

	  zoom.duration = function(_) {
	    return arguments.length ? (duration = +_, zoom) : duration;
	  };

	  zoom.interpolate = function(_) {
	    return arguments.length ? (interpolate = _, zoom) : interpolate;
	  };

	  zoom.on = function() {
	    var value = listeners.on.apply(listeners, arguments);
	    return value === listeners ? zoom : value;
	  };

	  zoom.clickDistance = function(_) {
	    return arguments.length ? (clickDistance2 = (_ = +_) * _, zoom) : Math.sqrt(clickDistance2);
	  };

	  return zoom;
	}

	exports.version = version;
	exports.bisect = bisectRight;
	exports.bisectRight = bisectRight;
	exports.bisectLeft = bisectLeft;
	exports.ascending = ascending;
	exports.bisector = bisector;
	exports.cross = cross;
	exports.descending = descending;
	exports.deviation = deviation;
	exports.extent = extent;
	exports.histogram = histogram;
	exports.thresholdFreedmanDiaconis = freedmanDiaconis;
	exports.thresholdScott = scott;
	exports.thresholdSturges = sturges;
	exports.max = max;
	exports.mean = mean;
	exports.median = median;
	exports.merge = merge;
	exports.min = min;
	exports.pairs = pairs;
	exports.permute = permute;
	exports.quantile = threshold;
	exports.range = sequence;
	exports.scan = scan;
	exports.shuffle = shuffle;
	exports.sum = sum;
	exports.ticks = ticks;
	exports.tickIncrement = tickIncrement;
	exports.tickStep = tickStep;
	exports.transpose = transpose;
	exports.variance = variance;
	exports.zip = zip;
	exports.axisTop = axisTop;
	exports.axisRight = axisRight;
	exports.axisBottom = axisBottom;
	exports.axisLeft = axisLeft;
	exports.brush = brush;
	exports.brushX = brushX;
	exports.brushY = brushY;
	exports.brushSelection = brushSelection;
	exports.chord = chord;
	exports.ribbon = ribbon;
	exports.nest = nest;
	exports.set = set$2;
	exports.map = map$1;
	exports.keys = keys;
	exports.values = values;
	exports.entries = entries;
	exports.color = color;
	exports.rgb = rgb;
	exports.hsl = hsl;
	exports.lab = lab;
	exports.hcl = hcl;
	exports.cubehelix = cubehelix;
	exports.dispatch = dispatch;
	exports.drag = drag;
	exports.dragDisable = dragDisable;
	exports.dragEnable = yesdrag;
	exports.dsvFormat = dsv;
	exports.csvParse = csvParse;
	exports.csvParseRows = csvParseRows;
	exports.csvFormat = csvFormat;
	exports.csvFormatRows = csvFormatRows;
	exports.tsvParse = tsvParse;
	exports.tsvParseRows = tsvParseRows;
	exports.tsvFormat = tsvFormat;
	exports.tsvFormatRows = tsvFormatRows;
	exports.easeLinear = linear$1;
	exports.easeQuad = quadInOut;
	exports.easeQuadIn = quadIn;
	exports.easeQuadOut = quadOut;
	exports.easeQuadInOut = quadInOut;
	exports.easeCubic = cubicInOut;
	exports.easeCubicIn = cubicIn;
	exports.easeCubicOut = cubicOut;
	exports.easeCubicInOut = cubicInOut;
	exports.easePoly = polyInOut;
	exports.easePolyIn = polyIn;
	exports.easePolyOut = polyOut;
	exports.easePolyInOut = polyInOut;
	exports.easeSin = sinInOut;
	exports.easeSinIn = sinIn;
	exports.easeSinOut = sinOut;
	exports.easeSinInOut = sinInOut;
	exports.easeExp = expInOut;
	exports.easeExpIn = expIn;
	exports.easeExpOut = expOut;
	exports.easeExpInOut = expInOut;
	exports.easeCircle = circleInOut;
	exports.easeCircleIn = circleIn;
	exports.easeCircleOut = circleOut;
	exports.easeCircleInOut = circleInOut;
	exports.easeBounce = bounceOut;
	exports.easeBounceIn = bounceIn;
	exports.easeBounceOut = bounceOut;
	exports.easeBounceInOut = bounceInOut;
	exports.easeBack = backInOut;
	exports.easeBackIn = backIn;
	exports.easeBackOut = backOut;
	exports.easeBackInOut = backInOut;
	exports.easeElastic = elasticOut;
	exports.easeElasticIn = elasticIn;
	exports.easeElasticOut = elasticOut;
	exports.easeElasticInOut = elasticInOut;
	exports.forceCenter = center$1;
	exports.forceCollide = collide;
	exports.forceLink = link;
	exports.forceManyBody = manyBody;
	exports.forceRadial = radial;
	exports.forceSimulation = simulation;
	exports.forceX = x$2;
	exports.forceY = y$2;
	exports.formatDefaultLocale = defaultLocale;
	exports.formatLocale = formatLocale;
	exports.formatSpecifier = formatSpecifier;
	exports.precisionFixed = precisionFixed;
	exports.precisionPrefix = precisionPrefix;
	exports.precisionRound = precisionRound;
	exports.geoArea = area;
	exports.geoBounds = bounds;
	exports.geoCentroid = centroid;
	exports.geoCircle = circle;
	exports.geoClipAntimeridian = clipAntimeridian;
	exports.geoClipCircle = clipCircle;
	exports.geoClipExtent = extent$1;
	exports.geoClipRectangle = clipRectangle;
	exports.geoContains = contains;
	exports.geoDistance = distance;
	exports.geoGraticule = graticule;
	exports.geoGraticule10 = graticule10;
	exports.geoInterpolate = interpolate$1;
	exports.geoLength = length$1;
	exports.geoPath = index$1;
	exports.geoAlbers = albers;
	exports.geoAlbersUsa = albersUsa;
	exports.geoAzimuthalEqualArea = azimuthalEqualArea;
	exports.geoAzimuthalEqualAreaRaw = azimuthalEqualAreaRaw;
	exports.geoAzimuthalEquidistant = azimuthalEquidistant;
	exports.geoAzimuthalEquidistantRaw = azimuthalEquidistantRaw;
	exports.geoConicConformal = conicConformal;
	exports.geoConicConformalRaw = conicConformalRaw;
	exports.geoConicEqualArea = conicEqualArea;
	exports.geoConicEqualAreaRaw = conicEqualAreaRaw;
	exports.geoConicEquidistant = conicEquidistant;
	exports.geoConicEquidistantRaw = conicEquidistantRaw;
	exports.geoEquirectangular = equirectangular;
	exports.geoEquirectangularRaw = equirectangularRaw;
	exports.geoGnomonic = gnomonic;
	exports.geoGnomonicRaw = gnomonicRaw;
	exports.geoIdentity = identity$5;
	exports.geoProjection = projection;
	exports.geoProjectionMutator = projectionMutator;
	exports.geoMercator = mercator;
	exports.geoMercatorRaw = mercatorRaw;
	exports.geoNaturalEarth1 = naturalEarth1;
	exports.geoNaturalEarth1Raw = naturalEarth1Raw;
	exports.geoOrthographic = orthographic;
	exports.geoOrthographicRaw = orthographicRaw;
	exports.geoStereographic = stereographic;
	exports.geoStereographicRaw = stereographicRaw;
	exports.geoTransverseMercator = transverseMercator;
	exports.geoTransverseMercatorRaw = transverseMercatorRaw;
	exports.geoRotation = rotation;
	exports.geoStream = geoStream;
	exports.geoTransform = transform;
	exports.cluster = cluster;
	exports.hierarchy = hierarchy;
	exports.pack = index$2;
	exports.packSiblings = siblings;
	exports.packEnclose = enclose;
	exports.partition = partition;
	exports.stratify = stratify;
	exports.tree = tree;
	exports.treemap = index$3;
	exports.treemapBinary = binary;
	exports.treemapDice = treemapDice;
	exports.treemapSlice = treemapSlice;
	exports.treemapSliceDice = sliceDice;
	exports.treemapSquarify = squarify;
	exports.treemapResquarify = resquarify;
	exports.interpolate = interpolateValue;
	exports.interpolateArray = array$1;
	exports.interpolateBasis = basis$1;
	exports.interpolateBasisClosed = basisClosed;
	exports.interpolateDate = date;
	exports.interpolateNumber = reinterpolate;
	exports.interpolateObject = object;
	exports.interpolateRound = interpolateRound;
	exports.interpolateString = interpolateString;
	exports.interpolateTransformCss = interpolateTransformCss;
	exports.interpolateTransformSvg = interpolateTransformSvg;
	exports.interpolateZoom = interpolateZoom;
	exports.interpolateRgb = interpolateRgb;
	exports.interpolateRgbBasis = rgbBasis;
	exports.interpolateRgbBasisClosed = rgbBasisClosed;
	exports.interpolateHsl = hsl$2;
	exports.interpolateHslLong = hslLong;
	exports.interpolateLab = lab$1;
	exports.interpolateHcl = hcl$2;
	exports.interpolateHclLong = hclLong;
	exports.interpolateCubehelix = cubehelix$2;
	exports.interpolateCubehelixLong = cubehelixLong;
	exports.quantize = quantize;
	exports.path = path;
	exports.polygonArea = area$1;
	exports.polygonCentroid = centroid$1;
	exports.polygonHull = hull;
	exports.polygonContains = contains$1;
	exports.polygonLength = length$2;
	exports.quadtree = quadtree;
	exports.queue = queue;
	exports.randomUniform = uniform;
	exports.randomNormal = normal;
	exports.randomLogNormal = logNormal;
	exports.randomBates = bates;
	exports.randomIrwinHall = irwinHall;
	exports.randomExponential = exponential$1;
	exports.request = request;
	exports.html = html;
	exports.json = json;
	exports.text = text;
	exports.xml = xml;
	exports.csv = csv$1;
	exports.tsv = tsv$1;
	exports.scaleBand = band;
	exports.scalePoint = point$1;
	exports.scaleIdentity = identity$6;
	exports.scaleLinear = linear$2;
	exports.scaleLog = log$1;
	exports.scaleOrdinal = ordinal;
	exports.scaleImplicit = implicit;
	exports.scalePow = pow$1;
	exports.scaleSqrt = sqrt$1;
	exports.scaleQuantile = quantile$$1;
	exports.scaleQuantize = quantize$1;
	exports.scaleThreshold = threshold$1;
	exports.scaleTime = time;
	exports.scaleUtc = utcTime;
	exports.schemeCategory10 = category10;
	exports.schemeCategory20b = category20b;
	exports.schemeCategory20c = category20c;
	exports.schemeCategory20 = category20;
	exports.interpolateCubehelixDefault = cubehelix$3;
	exports.interpolateRainbow = rainbow$1;
	exports.interpolateWarm = warm;
	exports.interpolateCool = cool;
	exports.interpolateViridis = viridis;
	exports.interpolateMagma = magma;
	exports.interpolateInferno = inferno;
	exports.interpolatePlasma = plasma;
	exports.scaleSequential = sequential;
	exports.create = create;
	exports.creator = creator;
	exports.local = local$1;
	exports.matcher = matcher$1;
	exports.mouse = mouse;
	exports.namespace = namespace;
	exports.namespaces = namespaces;
	exports.clientPoint = point;
	exports.select = select;
	exports.selectAll = selectAll;
	exports.selection = selection;
	exports.selector = selector;
	exports.selectorAll = selectorAll;
	exports.style = styleValue;
	exports.touch = touch;
	exports.touches = touches;
	exports.window = defaultView;
	exports.customEvent = customEvent;
	exports.arc = arc;
	exports.area = area$2;
	exports.line = line;
	exports.pie = pie;
	exports.areaRadial = areaRadial;
	exports.radialArea = areaRadial;
	exports.lineRadial = lineRadial$1;
	exports.radialLine = lineRadial$1;
	exports.pointRadial = pointRadial;
	exports.linkHorizontal = linkHorizontal;
	exports.linkVertical = linkVertical;
	exports.linkRadial = linkRadial;
	exports.symbol = symbol;
	exports.symbols = symbols;
	exports.symbolCircle = circle$2;
	exports.symbolCross = cross$2;
	exports.symbolDiamond = diamond;
	exports.symbolSquare = square;
	exports.symbolStar = star;
	exports.symbolTriangle = triangle;
	exports.symbolWye = wye;
	exports.curveBasisClosed = basisClosed$1;
	exports.curveBasisOpen = basisOpen;
	exports.curveBasis = basis$2;
	exports.curveBundle = bundle;
	exports.curveCardinalClosed = cardinalClosed;
	exports.curveCardinalOpen = cardinalOpen;
	exports.curveCardinal = cardinal;
	exports.curveCatmullRomClosed = catmullRomClosed;
	exports.curveCatmullRomOpen = catmullRomOpen;
	exports.curveCatmullRom = catmullRom;
	exports.curveLinearClosed = linearClosed;
	exports.curveLinear = curveLinear;
	exports.curveMonotoneX = monotoneX;
	exports.curveMonotoneY = monotoneY;
	exports.curveNatural = natural;
	exports.curveStep = step;
	exports.curveStepAfter = stepAfter;
	exports.curveStepBefore = stepBefore;
	exports.stack = stack;
	exports.stackOffsetExpand = expand;
	exports.stackOffsetDiverging = diverging;
	exports.stackOffsetNone = none$1;
	exports.stackOffsetSilhouette = silhouette;
	exports.stackOffsetWiggle = wiggle;
	exports.stackOrderAscending = ascending$2;
	exports.stackOrderDescending = descending$2;
	exports.stackOrderInsideOut = insideOut;
	exports.stackOrderNone = none$2;
	exports.stackOrderReverse = reverse;
	exports.timeInterval = newInterval;
	exports.timeMillisecond = millisecond;
	exports.timeMilliseconds = milliseconds;
	exports.utcMillisecond = millisecond;
	exports.utcMilliseconds = milliseconds;
	exports.timeSecond = second;
	exports.timeSeconds = seconds;
	exports.utcSecond = second;
	exports.utcSeconds = seconds;
	exports.timeMinute = minute;
	exports.timeMinutes = minutes;
	exports.timeHour = hour;
	exports.timeHours = hours;
	exports.timeDay = day;
	exports.timeDays = days;
	exports.timeWeek = sunday;
	exports.timeWeeks = sundays;
	exports.timeSunday = sunday;
	exports.timeSundays = sundays;
	exports.timeMonday = monday;
	exports.timeMondays = mondays;
	exports.timeTuesday = tuesday;
	exports.timeTuesdays = tuesdays;
	exports.timeWednesday = wednesday;
	exports.timeWednesdays = wednesdays;
	exports.timeThursday = thursday;
	exports.timeThursdays = thursdays;
	exports.timeFriday = friday;
	exports.timeFridays = fridays;
	exports.timeSaturday = saturday;
	exports.timeSaturdays = saturdays;
	exports.timeMonth = month;
	exports.timeMonths = months;
	exports.timeYear = year;
	exports.timeYears = years;
	exports.utcMinute = utcMinute;
	exports.utcMinutes = utcMinutes;
	exports.utcHour = utcHour;
	exports.utcHours = utcHours;
	exports.utcDay = utcDay;
	exports.utcDays = utcDays;
	exports.utcWeek = utcSunday;
	exports.utcWeeks = utcSundays;
	exports.utcSunday = utcSunday;
	exports.utcSundays = utcSundays;
	exports.utcMonday = utcMonday;
	exports.utcMondays = utcMondays;
	exports.utcTuesday = utcTuesday;
	exports.utcTuesdays = utcTuesdays;
	exports.utcWednesday = utcWednesday;
	exports.utcWednesdays = utcWednesdays;
	exports.utcThursday = utcThursday;
	exports.utcThursdays = utcThursdays;
	exports.utcFriday = utcFriday;
	exports.utcFridays = utcFridays;
	exports.utcSaturday = utcSaturday;
	exports.utcSaturdays = utcSaturdays;
	exports.utcMonth = utcMonth;
	exports.utcMonths = utcMonths;
	exports.utcYear = utcYear;
	exports.utcYears = utcYears;
	exports.timeFormatDefaultLocale = defaultLocale$1;
	exports.timeFormatLocale = formatLocale$1;
	exports.isoFormat = formatIso;
	exports.isoParse = parseIso;
	exports.now = now;
	exports.timer = timer;
	exports.timerFlush = timerFlush;
	exports.timeout = timeout$1;
	exports.interval = interval$1;
	exports.transition = transition;
	exports.active = active;
	exports.interrupt = interrupt;
	exports.voronoi = voronoi;
	exports.zoom = zoom;
	exports.zoomTransform = transform$1;
	exports.zoomIdentity = identity$8;

	Object.defineProperty(exports, '__esModule', { value: true });

	})));


/***/ }),
/* 329 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.type17 = exports.type17CN = exports.colorRef = exports.patternTime = exports.flowInterval = exports.stationFlow = exports.redScale = exports.maxScale = exports.reverseMap = exports.maxStopTime = exports.trajPatternSetComplete = exports.trajPatternSet = exports.pattern = exports.matchingTraj = exports.sites = exports.filtedTraj = exports.drawTraj = exports.peopleTraj = exports.siteCondition = exports.standardVector = exports.poiResults = exports.dataReady = exports.topic2WordIndex = exports.wordIndex2Topic = exports.siteTopic = undefined;

	var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

	var _slicedToArray = function () { function sliceIterator(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"]) _i["return"](); } finally { if (_d) throw _e; } } return _arr; } return function (arr, i) { if (Array.isArray(arr)) { return arr; } else if (Symbol.iterator in Object(arr)) { return sliceIterator(arr, i); } else { throw new TypeError("Invalid attempt to destructure non-iterable instance"); } }; }();

	exports.init = init;
	exports.addPoiResults = addPoiResults;
	exports.removePoiResults = removePoiResults;
	exports.getIdxInPoiResultsByCondition = getIdxInPoiResultsByCondition;
	exports.getPoiResultsByCondition = getPoiResultsByCondition;
	exports.modifyPoiResultsOne = modifyPoiResultsOne;
	exports.modifyPoiResultsShowType = modifyPoiResultsShowType;
	exports.getPoiResultsShowType = getPoiResultsShowType;
	exports.modifyPoiResultsGroup = modifyPoiResultsGroup;
	exports.modifyPoiResults = modifyPoiResults;
	exports.modifyPoiResultsShow = modifyPoiResultsShow;
	exports.modifyPoisBound = modifyPoisBound;
	exports.getSelectSites = getSelectSites;
	exports.setPeopleTraj = setPeopleTraj;
	exports.handleTraj = handleTraj;
	exports.getStandardVector = getStandardVector;
	exports.getdistance = getdistance;

	var _querydb = __webpack_require__(330);

	var QueryDb = _interopRequireWildcard(_querydb);

	var _jquery = __webpack_require__(327);

	var _jquery2 = _interopRequireDefault(_jquery);

	var _d2 = __webpack_require__(328);

	var d3 = _interopRequireWildcard(_d2);

	var _newqueryview = __webpack_require__(331);

	var NewQueryView = _interopRequireWildcard(_newqueryview);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }
	// import * as myKMeans from './mykmeans.js';
	// import * as MapRender from './maprender.js';
	// import * as ConfigPanel from './configpanel.js';


	// import * as TrajMatrix from './trajmatrix.js';
	// import * as GroupMap from './groupmap.js';

	var siteTopic = exports.siteTopic = new Map();
	var wordIndex2Topic = exports.wordIndex2Topic = new Map();
	var topic2WordIndex = exports.topic2WordIndex = new Map();
	var dataReady = exports.dataReady = false;
	var poiResults = exports.poiResults = [];
	var standardVector = exports.standardVector = []; // 标准向量
	var siteCondition = exports.siteCondition = new Map(); // site id 到 某几个condition的映射
	// traj
	var peopleTraj = exports.peopleTraj = void 0;
	var drawTraj = exports.drawTraj = [];
	var filtedTraj = exports.filtedTraj = [];
	var sites = exports.sites = new Map();
	var matchingTraj = exports.matchingTraj = new Map();
	var pattern = exports.pattern = new Map();
	var trajPatternSet = exports.trajPatternSet = new Map();
	var trajPatternSetComplete = exports.trajPatternSetComplete = new Map();
	var maxStopTime = exports.maxStopTime = new Map();
	var reverseMap = exports.reverseMap = new Map();
	var maxScale = exports.maxScale = 0;
	var redScale = exports.redScale = void 0;
	var stationFlow = exports.stationFlow = new Map();
	var flowInterval = exports.flowInterval = 30;
	//pattern各时间段流量
	var patternTime = exports.patternTime = new Map();
	var colorRef = exports.colorRef = new Map();

	function init() {
	  return getTopicPoi().then(function (o) {
	    return getIndexTopic();
	  }).then(function (o) {
	    return QueryDb.getSites();
	  });
	  // .then(o => loadImg());
	}

	var type17CN = exports.type17CN = ["公司企业", "购物", "丽人", "休闲娱乐", "医疗", "教育培训", "汽车服务", "美食", "宾馆", "旅游景点", "生活服务", "运动健身", "交通设施", "政府机构", "房地产", "自然地物", "金融", '医疗生活'];
	var type17 = exports.type17 = ['Enterprise', 'Shop', 'Beauty', 'Entertainment', 'Hospital', 'Education', 'Car', 'Food', 'Hotel', 'Tourist_attractions', 'Life', 'Sports', 'Traffic', 'Government', 'Uptown', 'Scenicspot', 'Finance', 'Hospital'];
	function getTopicPoi() {
	  return QueryDb.pquery({ // 23 551865  || 5 469070
	    reqType: "readText",
	    begin: 3,
	    fileName: "type.txt",
	    readLineLimit: 308
	  }).then(function (result) {
	    _jquery2.default.each(result, function (i, o) {
	      var maxIndex = -1,
	          maxValue = 0;
	      o.map(function (x, j) {
	        // if (x.indexOf("E") == -1) {
	        // let nowV = parseFloat(x);
	        if (x - maxValue > 0) {
	          maxValue = x;
	          maxIndex = j;
	        }
	        // }
	      });
	      wordIndex2Topic.set(i * 1, type17[maxIndex]);
	      topic2WordIndex.set(type17[maxIndex], i * 1);
	    });
	    var tmp = [].concat(_toConsumableArray(topic2WordIndex)).sort(function (a, b) {
	      return a[1] - b[1];
	    });
	    // for (let i = 0; i < tmp.length; ++ i) {
	    //   colorRef.set(tmp[i][0], GroupMap.topicColor[i]);
	    // }
	    // console.log(colorRef);
	    console.log(topic2WordIndex);
	  });
	}

	function getIndexTopic() {
	  return QueryDb.pquery({ // 23 551865  || 5 469070
	    reqType: "readText",
	    begin: 23,
	    fileName: "topic17.txt",
	    readLineLimit: 496678
	  }).then(function (result) {
	    _jquery2.default.each(result, function (i, o) {
	      var tmp = new Map();
	      o.map(function (x, j) {
	        x = parseFloat(x);
	        var topic = wordIndex2Topic.get(j);
	        if (!tmp.has(topic)) {
	          tmp.set(topic, x);
	        }
	        var pre = tmp.get(topic);
	        if (x > pre) {
	          tmp.set(topic, pre);
	        }
	      });
	      var aRes = [];
	      tmp.forEach(function (val, topic) {
	        aRes.push({ topic: topic, val: val });
	      });
	      aRes.sort(function (a, b) {
	        return b.val - a.val;
	      });
	      siteTopic.set(i + '', aRes);
	    });
	  });
	};

	//----------------华丽的分割线-----------------------
	// poiResults

	function addPoiResults(condition, results) {
	  var nowList = new Map();
	  if (getIdxInPoiResultsByCondition(condition) != -1) {
	    return;
	  }
	  if (results.length == 0) {
	    return;
	  }
	  results.forEach(function (poi) {
	    // let idx;
	    // for (idx = 0; idx < type17CN.length; idx ++) {
	    //   if (poi.type.indexOf(type17CN[idx]) != -1) {
	    //     break;
	    //   }
	    // }
	    // console.log(poi, ' ###')
	    var type = siteTopic.get(poi.site + '')[0].topic;
	    if (!type) {
	      return;
	    }
	    poi.isSelect = true;
	    poi.isMouseover = false;
	    if (nowList.has(type)) {
	      nowList.get(type).push(poi);
	    } else {
	      var tmp = [];
	      tmp.isSelect = true;
	      tmp.isMouseover = false;
	      tmp.isShow = false;
	      tmp.push(poi);
	      nowList.set(type, tmp);
	    }
	  });
	  var data = [].concat(_toConsumableArray(nowList));
	  data[0][1].isShow = true;
	  poiResults.push({ condition: condition, data: data, isShow: true, showType: 0 });
	}

	function removePoiResults(dif) {
	  var _iteratorNormalCompletion = true;
	  var _didIteratorError = false;
	  var _iteratorError = undefined;

	  try {
	    for (var _iterator = dif[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
	      var o = _step.value;

	      var idx = getIdxInPoiResultsByCondition(o);
	      poiResults.splice(idx, 1);
	    }
	  } catch (err) {
	    _didIteratorError = true;
	    _iteratorError = err;
	  } finally {
	    try {
	      if (!_iteratorNormalCompletion && _iterator.return) {
	        _iterator.return();
	      }
	    } finally {
	      if (_didIteratorError) {
	        throw _iteratorError;
	      }
	    }
	  }
	}

	function getIdxInPoiResultsByCondition(condition) {
	  var ans = -1;
	  poiResults.forEach(function (data, i) {
	    if (data.condition == condition) ans = i;
	  });
	  return ans;
	}

	function getPoiResultsByCondition(condition) {
	  for (var i = 0; i < poiResults.length; i++) {
	    if (poiResults[i].condition == condition) {
	      return poiResults[i];
	    }
	  }
	}

	//延迟更新？
	function modifyPoiResultsOne(ids, property, val) {
	  var sum = 0;
	  var tmp = poiResults[ids[0]].data[ids[1]][1];
	  tmp[ids[2]][property] = val;
	  // 不更新上一层 以后解决   用一个sum位记录？
	  for (var i = 0; i < tmp.length; i++) {
	    if (tmp[i][property] == val) {
	      sum++;
	    }
	  }
	  if (sum == tmp.length) {
	    tmp[property] = val;
	  }
	}
	function modifyPoiResultsShowType(ids) {
	  var preId = getPoiResultsShowType(ids);
	  poiResults[ids[0]].data[preId][1].isShow = false;
	  poiResults[ids[0]].isShow = true;
	  poiResults[ids[0]].showType = ids[1];
	  poiResults[ids[0]].data[ids[1]][1].isShow = true;
	}

	function getPoiResultsShowType(ids) {
	  return poiResults[ids[0]].showType;
	}

	function modifyPoiResultsGroup(ids, property, val) {
	  var tmp = poiResults[ids[0]].data[ids[1]][1];
	  tmp[property] = val;
	  for (var i = 0; i < tmp.length; i++) {
	    tmp[i][property] = val;
	  }
	}

	function modifyPoiResults(id, property, val) {
	  var tmp = poiResults[id].data;
	  for (var i = 0; i < tmp.length; i++) {
	    for (var j = 0; j < tmp[i][1].length; j++) {
	      tmp[i][1][j][property] = val;
	    }
	    tmp[i][1][property] = val;
	  }
	}

	function modifyPoiResultsShow(id, val) {
	  poiResults[id].isShow = !poiResults[id].isShow;
	}

	function modifyPoisBound(NW, SE) {
	  var flag = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;
	  var id = arguments[3];

	  var now = poiResults[id].data;
	  for (var i = 0; i < now.length; i++) {
	    var sum = 0;
	    for (var j = 0; j < now[i][1].length; j++) {
	      if (inTheBound.apply(undefined, _toConsumableArray(NW).concat(_toConsumableArray(SE), [now[i][1][j].latitude * 1, now[i][1][j].longitude]))) {
	        sum++;
	        now[i][1][j].isSelect = flag;
	      } else if (now[i][1][j].isSelect == flag) {
	        sum++;
	      }
	    }
	    // 同样只更新了一层
	    if (sum == now[i][1].length) {
	      now[i][1].isSelect = flag;
	    }
	  }
	}

	function inTheBound(lat0, lng0, lat1, lng1, lat, lng) {
	  if (lat >= lat0 && lat <= lat1 && lng >= lng0 && lng <= lng1) {
	    return true;
	  }
	  return false;
	}

	function getSelectSites() {
	  var result = new Set();
	  siteCondition.clear();
	  for (var i = 0; i < poiResults.length; i++) {
	    var tmp = poiResults[i].data;
	    for (var j = 0; j < tmp.length; j++) {
	      if (!tmp[j][1].isSelect) {
	        continue;
	      }
	      for (var k = 0; k < tmp[j][1].length; k++) {
	        if (tmp[j][1][k].isSelect) {
	          result.add(tmp[j][1][k].site);
	          var S = siteCondition.get(tmp[j][1][k].site) || 0;
	          S |= 1 << i;
	          siteCondition.set(tmp[j][1][k].site, S);
	        }
	      }
	    }
	  }
	  console.log('siteCondition:', siteCondition);
	  return [].concat(_toConsumableArray(result));
	}

	//people traj
	function setPeopleTraj(traj) {
	  exports.peopleTraj = peopleTraj = traj;
	}

	function handleTraj() {
	  detectTrajStopPoint();
	  detectTrajMatchLocation();
	  NewQueryView.filterAll();
	  // MapRender.reRendering();
	  // TrajList.showSemanticList(drawTraj);
	  loadMatchingTraj();
	  updateTrajPatternAndSet();
	  // myKMeans.clusterDataByKMeans();

	}

	function detectTrajStopPoint() {
	  var stopTime = 20;
	  peopleTraj.forEach(function (trajs) {
	    for (var i = 0; i < trajs.traj.length; i++) {
	      if (i == 0) {
	        trajs.traj[i].isStop = true;
	      } else {
	        var preTime = new Date(trajs.traj[i - 1].time).getTime();
	        var nowTime = new Date(trajs.traj[i].time).getTime();
	        if (nowTime - preTime >= 1000 * 60 * stopTime) {
	          trajs.traj[i].isStop = true;
	        } else {
	          trajs.traj[i].isStop = false;
	        }
	      }
	    }
	    trajs.matching = false;
	    trajs.filter = false;
	  });

	  console.log('detect Traj Stop Point done!');
	}

	function detectTrajMatchLocation() {
	  var tarLen = poiResults.length;
	  drawTraj.length = 0;
	  filtedTraj.length = 0;
	  peopleTraj.forEach(function (trajs) {
	    var S = 0,
	        num = 0;
	    for (var i = 0; i < trajs.traj.length; i++) {
	      if (trajs.traj[i].isStop && siteCondition.has(trajs.traj[i].site * 1)) {
	        trajs.traj[i].stopCondition = siteCondition.get(trajs.traj[i].site * 1) || 0;
	        // 必须是先经过A后经过B
	        if ((1 << num & trajs.traj[i].stopCondition) != 0) {
	          trajs.traj[i].stoppoint = num++;
	        }
	        if (num == tarLen) {
	          break;
	        }
	      }
	    }
	    if (num == tarLen) {
	      trajs.matching = true;
	      drawTraj.push(trajs);
	    }
	  });
	  console.log('match traj location done!', peopleTraj.size, drawTraj.length);
	  console.log(drawTraj, ' drawTraj');

	  console.log(sites, ' sites');
	}

	function loadMatchingTraj() {
	  matchingTraj.clear();
	  var _iteratorNormalCompletion2 = true;
	  var _didIteratorError2 = false;
	  var _iteratorError2 = undefined;

	  try {
	    for (var _iterator2 = drawTraj[Symbol.iterator](), _step2; !(_iteratorNormalCompletion2 = (_step2 = _iterator2.next()).done); _iteratorNormalCompletion2 = true) {
	      var _peopleTraj = _step2.value;

	      if (!_peopleTraj.matching) continue;
	      if (_peopleTraj.filter) continue;
	      var matchStr = '';
	      var _iteratorNormalCompletion3 = true;
	      var _didIteratorError3 = false;
	      var _iteratorError3 = undefined;

	      try {
	        for (var _iterator3 = _peopleTraj.traj[Symbol.iterator](), _step3; !(_iteratorNormalCompletion3 = (_step3 = _iterator3.next()).done); _iteratorNormalCompletion3 = true) {
	          var traj = _step3.value;

	          if (traj.isStop) {
	            matchStr += ',' + traj.site;
	          }
	        }
	      } catch (err) {
	        _didIteratorError3 = true;
	        _iteratorError3 = err;
	      } finally {
	        try {
	          if (!_iteratorNormalCompletion3 && _iterator3.return) {
	            _iterator3.return();
	          }
	        } finally {
	          if (_didIteratorError3) {
	            throw _iteratorError3;
	          }
	        }
	      }

	      addMatchingTraj(matchStr, _peopleTraj);
	    }
	    // console.log('matchingTraj:', matchingTraj);
	  } catch (err) {
	    _didIteratorError2 = true;
	    _iteratorError2 = err;
	  } finally {
	    try {
	      if (!_iteratorNormalCompletion2 && _iterator2.return) {
	        _iterator2.return();
	      }
	    } finally {
	      if (_didIteratorError2) {
	        throw _iteratorError2;
	      }
	    }
	  }

	  console.log('load matching traj complete!');
	}

	function addMatchingTraj(matchStr, peopleTraj) {
	  if (matchingTraj.has(matchStr)) {
	    matchingTraj.get(matchStr).push(peopleTraj);
	  } else {
	    matchingTraj.set(matchStr, [peopleTraj]);
	  }
	}

	function updateTrajPatternAndSet() {
	  pattern.clear();
	  trajPatternSet.clear();
	  trajPatternSetComplete.clear();
	  stationFlow.clear();
	  var _iteratorNormalCompletion4 = true;
	  var _didIteratorError4 = false;
	  var _iteratorError4 = undefined;

	  try {
	    for (var _iterator4 = matchingTraj[Symbol.iterator](), _step4; !(_iteratorNormalCompletion4 = (_step4 = _iterator4.next()).done); _iteratorNormalCompletion4 = true) {
	      var _step4$value = _slicedToArray(_step4.value, 2),
	          key = _step4$value[0],
	          peopleTrajs = _step4$value[1];

	      var _loop = function _loop(i) {
	        exports.peopleTraj = peopleTraj = peopleTrajs[i];
	        if (!peopleTraj.matching) return {
	            v: void 0
	          };
	        var trajs = [],
	            preTime = void 0,
	            nowTime = void 0;
	        peopleTraj.traj.forEach(function (traj, j) {
	          nowTime = new Date(traj.time);
	          if (j == 0) {
	            preTime = new Date(new Date(traj.time).toLocaleDateString() + ' 24:00:00');
	          }
	          if (traj.isStop) {
	            traj.stopTime = (preTime.getTime() - nowTime.getTime() + 86400000) % 86400000;
	            trajs.push(_jquery2.default.extend(true, {}, traj)); //push后会是深复制
	            if (traj.hasOwnProperty('stoppoint')) {
	              if (!maxStopTime.has(traj.site)) {
	                maxStopTime.set(traj.site, traj.stopTime);
	              } else {
	                var preMax = maxStopTime.get(traj.site);
	                maxStopTime.set(traj.site, Math.max(preMax, preMax));
	              }
	              updateStationFlow(traj.site, nowTime);
	            }
	          }
	          preTime = nowTime;
	        });
	        trajs.reverse();
	        var patternStr = trajToPatternStr(peopleTraj.traj);
	        // -- updateScale
	        updateScale(patternStr);
	        addTrajPatternSet(patternStr, trajs);
	        updatePatternTime(patternStr, trajs);
	        addTrajPatternSetComplete(patternStr, peopleTraj.traj);
	        if (!pattern.has(patternStr)) {
	          pattern.set(patternStr, { num: 1, data: trajs });
	        } else {
	          var pre = pattern.get(patternStr);
	          pre.data.forEach(function (traj, i) {
	            if (traj.hasOwnProperty('stoppoint')) {
	              return;
	            } else {
	              traj.stopTime += trajs[i].stopTime;
	            }
	          });
	          pre.num++;
	          // pattern.set(patternStr, pre);  //   没有必要
	        }
	      };

	      // peopleTrajs.forEach((peopleTraj, i) => {
	      //改为从后向前
	      for (var i = peopleTrajs.length - 1; i >= 0; --i) {
	        var _ret = _loop(i);

	        if ((typeof _ret === 'undefined' ? 'undefined' : _typeof(_ret)) === "object") return _ret.v;
	      }
	    }
	    // myKMeans.updateRawDocuments(pattern);
	  } catch (err) {
	    _didIteratorError4 = true;
	    _iteratorError4 = err;
	  } finally {
	    try {
	      if (!_iteratorNormalCompletion4 && _iterator4.return) {
	        _iterator4.return();
	      }
	    } finally {
	      if (_didIteratorError4) {
	        throw _iteratorError4;
	      }
	    }
	  }

	  var _iteratorNormalCompletion5 = true;
	  var _didIteratorError5 = false;
	  var _iteratorError5 = undefined;

	  try {
	    var _loop2 = function _loop2() {
	      var _step5$value = _slicedToArray(_step5.value, 2),
	          pat = _step5$value[0],
	          d = _step5$value[1];

	      d.data.forEach(function (o, i) {
	        o.pattern = pat;
	        if (o.hasOwnProperty('stoppoint')) {
	          return;
	        } else {
	          o.stopTime /= d.num;
	        }
	      });
	    };

	    for (var _iterator5 = pattern[Symbol.iterator](), _step5; !(_iteratorNormalCompletion5 = (_step5 = _iterator5.next()).done); _iteratorNormalCompletion5 = true) {
	      _loop2();
	    }
	  } catch (err) {
	    _didIteratorError5 = true;
	    _iteratorError5 = err;
	  } finally {
	    try {
	      if (!_iteratorNormalCompletion5 && _iterator5.return) {
	        _iterator5.return();
	      }
	    } finally {
	      if (_didIteratorError5) {
	        throw _iteratorError5;
	      }
	    }
	  }

	  console.log('pattern:', pattern);
	  console.log('trajPatternSet:', trajPatternSet);
	  console.log('patternTime:', patternTime);

	  console.log('siteTopic:', siteTopic);
	  // GroupMap.run(pattern);
	}

	function updatePatternTime(pat, trajs) {
	  var sites = pat.split(',').filter(function (o) {
	    return o.indexOf('-') >= 0;
	  }).map(function (o) {
	    return o * -1;
	  }),
	      len = sites.length;
	  var interval = 10;
	  var intCnt = 24 * 60 / interval;
	  if (!patternTime.has(pat)) {
	    var _init = [];
	    for (var i = 0; i < len; ++i) {
	      var timeArr = [];
	      for (var j = 0; j < intCnt; ++j) {
	        timeArr[j] = 0;
	      }
	      _init.push(timeArr);
	    }
	    patternTime.set(pat, _init);
	  }
	  //目前只统计stoppoint
	  var now = patternTime.get(pat);

	  var _loop3 = function _loop3(_i) {
	    if (trajs[_i].hasOwnProperty('stoppoint')) {
	      var id = sites.findIndex(function (o) {
	        return o == 1 * trajs[_i].site;
	      }),
	          thisDate = new Date(trajs[_i].time),
	          hour = thisDate.getHours(),
	          minu = thisDate.getMinutes() + hour * 60,
	          start = parseInt(minu / interval),
	          end = (parseInt((start + trajs[_i].stopTime / 1000 / 60) / interval) + 1) % intCnt;
	      now[id][start]++;
	      // for (let j = start; j != end; ) {
	      //   now[id][j] ++;
	      //   j = (j + 1) % intCnt;
	      // }
	    }
	  };

	  for (var _i = 0; _i < trajs.length; ++_i) {
	    _loop3(_i);
	  }
	}

	function updateStationFlow(id, time) {
	  if (!stationFlow.get(id * 1)) {
	    stationFlow.set(id * 1, new Map());
	  }
	  var sf = stationFlow.get(id * 1),
	      _time$toString$split$ = time.toString().split(' ')[4].split(':').map(function (o) {
	    return o * 1;
	  }),
	      _time$toString$split$2 = _slicedToArray(_time$toString$split$, 3),
	      H = _time$toString$split$2[0],
	      M = _time$toString$split$2[1],
	      S = _time$toString$split$2[2],
	      s = H * 3600 + M * 60 + S;

	  var now = parseInt(s / flowInterval);
	  if (!sf.has(now)) {
	    sf.set(now, 0);
	  }
	  var num = sf.get(now);
	  sf.set(now, num + 1);
	}

	function trajToPatternStr(aTraj) {
	  var t = [],
	      cont = 0,
	      siteSets_ = new Set();
	  aTraj.forEach(function (traj, j) {
	    if (traj.isStop) {
	      if (traj.hasOwnProperty("stoppoint")) {
	        siteSets_.add(traj.stoppoint);
	        if (cont > 0) {
	          t.push(cont);
	        }
	        t.push("-" + traj.site);
	        cont = 0;
	      } else {
	        cont++;
	      }
	    }
	  });
	  if (cont > 0) {
	    t.push(cont);
	  }
	  return t.toString();
	}

	function addTrajPatternSet(pattern, traj) {
	  if (!trajPatternSet.has(pattern)) {
	    trajPatternSet.set(pattern, []);
	  }
	  trajPatternSet.get(pattern).push(traj);
	}

	function addTrajPatternSetComplete(pattern, traj) {
	  if (!trajPatternSetComplete.has(pattern)) {
	    trajPatternSetComplete.set(pattern, []);
	  }
	  trajPatternSetComplete.get(pattern).push(traj);
	}

	function updateScale(pat) {
	  var sites = pat.split(',').filter(function (o) {
	    return o.indexOf('-') >= 0;
	  }).map(function (o) {
	    return o * -1;
	  });
	  var vecs = [];
	  for (var j = 0; j < sites.length; j++) {
	    var now = siteTopic.get(sites[j] + '');
	    var node = new Map();
	    for (var i = 0; i < now.length; i++) {
	      node.set(now[i].topic, now[i].val);
	    }
	    vecs.push(node);
	  }
	  var d = getdistance(vecs);
	  exports.maxScale = maxScale = Math.max(maxScale, Math.sqrt(d));
	  exports.redScale = redScale = d3.scaleLinear().domain([0, maxScale])
	  // .range(['rgb(3, 78, 123)', 'rgb(255, 247, 251)'])
	  .range(['rgb(216, 234, 248)', 'rgb(20, 104, 171)']);
	}

	//standardVector
	function getStandardVector() {
	  standardVector.length = 0;
	  poiResults.forEach(function (condition) {
	    var num = 0;
	    var vector = new Map();
	    condition.data.forEach(function (data) {
	      if (!data[1].isSelect) {
	        return;
	      }
	      data[1].forEach(function (node) {
	        if (node.isSelect) {
	          num++;
	          var topics = siteTopic.get(node.site + '');
	          var _iteratorNormalCompletion6 = true;
	          var _didIteratorError6 = false;
	          var _iteratorError6 = undefined;

	          try {
	            for (var _iterator6 = topics[Symbol.iterator](), _step6; !(_iteratorNormalCompletion6 = (_step6 = _iterator6.next()).done); _iteratorNormalCompletion6 = true) {
	              var o = _step6.value;

	              if (!vector.has(o.topic)) {
	                vector.set(o.topic, 0);
	              }
	              vector.set(o.topic, vector.get(o.topic) + o.val);
	            }
	          } catch (err) {
	            _didIteratorError6 = true;
	            _iteratorError6 = err;
	          } finally {
	            try {
	              if (!_iteratorNormalCompletion6 && _iterator6.return) {
	                _iterator6.return();
	              }
	            } finally {
	              if (_didIteratorError6) {
	                throw _iteratorError6;
	              }
	            }
	          }
	        }
	      });
	    });
	    var _iteratorNormalCompletion7 = true;
	    var _didIteratorError7 = false;
	    var _iteratorError7 = undefined;

	    try {
	      for (var _iterator7 = vector[Symbol.iterator](), _step7; !(_iteratorNormalCompletion7 = (_step7 = _iterator7.next()).done); _iteratorNormalCompletion7 = true) {
	        var _step7$value = _slicedToArray(_step7.value, 2),
	            topic = _step7$value[0],
	            val = _step7$value[1];

	        vector.set(topic, val / num);
	      }
	    } catch (err) {
	      _didIteratorError7 = true;
	      _iteratorError7 = err;
	    } finally {
	      try {
	        if (!_iteratorNormalCompletion7 && _iterator7.return) {
	          _iterator7.return();
	        }
	      } finally {
	        if (_didIteratorError7) {
	          throw _iteratorError7;
	        }
	      }
	    }

	    standardVector.push(vector);
	  });
	  console.log('standardVector: ', standardVector);
	}

	function getdistance(trajdata) {
	  var distance = 0;
	  var sta = standardVector;
	  // console.log('distance  trajdata:', trajdata);
	  trajdata.forEach(function (condition, i) {
	    var _iteratorNormalCompletion8 = true;
	    var _didIteratorError8 = false;
	    var _iteratorError8 = undefined;

	    try {
	      for (var _iterator8 = condition.keys()[Symbol.iterator](), _step8; !(_iteratorNormalCompletion8 = (_step8 = _iterator8.next()).done); _iteratorNormalCompletion8 = true) {
	        var ele = _step8.value;

	        // distance += Math.abs(condition.get(ele)-sta[i].get(ele)) * 
	        //     NewQueryView.vec[i].get(ele);
	        var w = NewQueryView.vec[i] && NewQueryView.vec[i].has(ele) ? NewQueryView.vec[i].get(ele) : 1;
	        distance += Math.abs(condition.get(ele) - sta[i].get(ele)) * w;
	      }
	    } catch (err) {
	      _didIteratorError8 = true;
	      _iteratorError8 = err;
	    } finally {
	      try {
	        if (!_iteratorNormalCompletion8 && _iterator8.return) {
	          _iterator8.return();
	        }
	      } finally {
	        if (_didIteratorError8) {
	          throw _iteratorError8;
	        }
	      }
	    }
	  });
	  return distance;
	}

/***/ }),
/* 330 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _slicedToArray = function () { function sliceIterator(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"]) _i["return"](); } finally { if (_d) throw _e; } } return _arr; } return function (arr, i) { if (Array.isArray(arr)) { return arr; } else if (Symbol.iterator in Object(arr)) { return sliceIterator(arr, i); } else { throw new TypeError("Invalid attempt to destructure non-iterable instance"); } }; }();

	exports.pquery = pquery;
	exports.getSites = getSites;
	exports.getPidByCondition = getPidByCondition;
	exports.getTrajByPid = getTrajByPid;

	var _jquery = __webpack_require__(327);

	var _jquery2 = _interopRequireDefault(_jquery);

	var _datamanager = __webpack_require__(329);

	var DataManager = _interopRequireWildcard(_datamanager);

	var _d2 = __webpack_require__(328);

	var d3 = _interopRequireWildcard(_d2);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

	function pquery(myData) {
	  return new Promise(function (resolve, reject) {
	    var ws = new WebSocket('ws://localhost:8000/ws/');
	    ws.onopen = function () {
	      // console.log(JSON.stringify(myData));
	      ws.send(JSON.stringify(myData));
	    };
	    ws.onmessage = function (e) {
	      // console.log(JSON.parse(e.data), 'query WebSocket')
	      resolve(JSON.parse(e.data));
	    };
	    // $.ajax({
	    //   dataType: "json",
	    //   url: "http://localhost:3001/",
	    //   type: "POST",
	    //   data: myData,
	    //   success: resolve,
	    //   error: reject,
	    // });
	  });
	}

	function getSites() {
	  var data = {
	    reqType: "queryDb",
	    operate: "select",
	    column: "id, longitude, latitude, vertice, neighbor, base_station",
	    table: "site",
	    limit: 'where id >= 0 and id <= 28746' //  all stations in 温州
	  };
	  return pquery(data).then(function (result) {
	    var sites = DataManager.sites;
	    result.forEach(function (o) {
	      sites.set(o.id, {
	        longitude: o.longitude,
	        latitude: o.latitude,
	        vertice: o.vertice,
	        neighbor: o.neighbor,
	        base_station: o.base_station
	      });
	    });
	    console.log("read sites done");
	  });
	};

	function getPidByCondition(siteList, conditionNum) {
	  var time1 = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : '2014-01-14 00:00:00.00';
	  var time2 = arguments.length > 3 && arguments[3] !== undefined ? arguments[3] : '2014-01-14 00:20:00.00';

	  var siteCondition = 'site in (' + siteList.toString() + ')';
	  var timeFormat = d3.timeFormat('%m%d%H%M');
	  var begin = timeFormat(new Date(time1)) * 1;
	  var end = timeFormat(new Date(time2)) * 1;
	  var timeCondition = '';
	  for (var time = Math.floor(begin / 10) * 10; time <= (Math.floor(end / 10) + 1) * 10; time += 10) {
	    timeCondition += "'" + time + "',";
	  }
	  timeCondition = timeCondition.substring(0, timeCondition.length - 1);
	  var data = {
	    reqType: "queryDb",
	    operate: "select",
	    column: "*",
	    table: "phonetrajectory_index_bysite",
	    limit: 'where datetime in (' + timeCondition + ') and ' + siteCondition
	  };
	  console.log('getPidByCondition ... ');
	  return pquery(data).then(function (result) {
	    return getPidByResult(result, siteList, conditionNum);
	  });
	}

	// 合并people id : 经过所有要查找的点的pid
	function getPidByResult(result, siteList, conditionNum) {
	  console.log('getPidByCondition', result);
	  var siteCondition = DataManager.siteCondition;
	  var ans = new Set();
	  var pidSites = new Map();
	  var tar = (1 << conditionNum) - 1;
	  result.forEach(function (data) {
	    var _iteratorNormalCompletion = true;
	    var _didIteratorError = false;
	    var _iteratorError = undefined;

	    try {
	      for (var _iterator = data.plist.split(';')[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
	        var pid = _step.value;

	        if (!pidSites.has(pid)) {
	          pidSites.set(pid, 0);
	        }
	        var preS = pidSites.get(pid);
	        var newS = siteCondition.get(data.site * 1);
	        if ((preS | newS) == tar) {
	          ans.add(pid);
	          continue;
	        }
	        pidSites.set(pid, preS | newS);
	      }
	    } catch (err) {
	      _didIteratorError = true;
	      _iteratorError = err;
	    } finally {
	      try {
	        if (!_iteratorNormalCompletion && _iterator.return) {
	          _iterator.return();
	        }
	      } finally {
	        if (_didIteratorError) {
	          throw _iteratorError;
	        }
	      }
	    }
	  });
	  // console.log([...ans], ' ------ ');
	  // for (let [pid, sites] of pidSites) {
	  //   let s = 0;
	  //   for (let site of sites) {
	  //     const newS = siteCondition.get(site*1);
	  //     if ((s | newS) == tar) {
	  //       ans.add(pid);
	  //       break;
	  //     }
	  //     s |= newS;
	  //   }
	  // }
	  if (ans.size > 31000) {
	    console.log('pid is too large ans split');
	  }
	  console.log('pid length:', ans.size);
	  return [].concat(_toConsumableArray(ans)).slice(0, 31000);
	}

	function getTrajByPid(pidList) {
	  var time1 = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '2014-01-14 00:00:00.00';
	  var time2 = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : '2014-01-14 00:20:00.00';

	  var pCondition = '';
	  // for (let pid of pidList) {
	  //   pCondition += "'" + pid + "',";
	  // }

	  //*************  for Test *************   
	  var _iteratorNormalCompletion2 = true;
	  var _didIteratorError2 = false;
	  var _iteratorError2 = undefined;

	  try {
	    for (var _iterator2 = pidList.slice(0, 100)[Symbol.iterator](), _step2; !(_iteratorNormalCompletion2 = (_step2 = _iterator2.next()).done); _iteratorNormalCompletion2 = true) {
	      var pid = _step2.value;

	      pCondition += "'" + pid + "',";
	    }
	  } catch (err) {
	    _didIteratorError2 = true;
	    _iteratorError2 = err;
	  } finally {
	    try {
	      if (!_iteratorNormalCompletion2 && _iterator2.return) {
	        _iterator2.return();
	      }
	    } finally {
	      if (_didIteratorError2) {
	        throw _iteratorError2;
	      }
	    }
	  }

	  pCondition = pCondition.substring(0, pCondition.length - 1);
	  var timeFormat = d3.timeFormat('%m%d'); // day
	  var begin = timeFormat(new Date(time1));
	  var end = timeFormat(new Date(time2));
	  var dateCondition = '';
	  for (var time = begin; time <= end; time++) {
	    dateCondition += "'" + time + "',";
	  }
	  dateCondition = dateCondition.substring(0, dateCondition.length - 1);
	  var data = {
	    reqType: 'queryDb',
	    operate: 'select',
	    column: '*',
	    table: 'phonetrajectory_sortbyid',
	    limit: 'where date in (' + dateCondition + ') and peopleid in (' + pCondition + ')'
	  };
	  console.log(data);
	  return pquery(data).then(function (result) {
	    return parseTraj(result);
	  });
	}

	function parseTraj(result) {
	  console.log('traj', result);
	  var ans = new Map();
	  var _iteratorNormalCompletion3 = true;
	  var _didIteratorError3 = false;
	  var _iteratorError3 = undefined;

	  try {
	    var _loop = function _loop() {
	      var data = _step3.value;

	      if (!ans.has(data.peopleid)) {
	        ans.set(data.peopleid, {
	          pid: data.peopleid,
	          traj: []
	        });
	      }
	      var preTraj = ans.get(data.peopleid).traj;
	      data.traj.split(';').forEach(function (trajPoint) {
	        if (!trajPoint) {
	          return;
	        }

	        var _trajPoint$split = trajPoint.split(','),
	            _trajPoint$split2 = _slicedToArray(_trajPoint$split, 2),
	            time = _trajPoint$split2[0],
	            site = _trajPoint$split2[1];

	        if (preTraj.length == 0 || preTraj[preTraj.length - 1].site != site) {
	          preTraj.push({
	            time: time,
	            site: site
	          });
	        }
	      });
	    };

	    for (var _iterator3 = result[Symbol.iterator](), _step3; !(_iteratorNormalCompletion3 = (_step3 = _iterator3.next()).done); _iteratorNormalCompletion3 = true) {
	      _loop();
	    }
	  } catch (err) {
	    _didIteratorError3 = true;
	    _iteratorError3 = err;
	  } finally {
	    try {
	      if (!_iteratorNormalCompletion3 && _iterator3.return) {
	        _iterator3.return();
	      }
	    } finally {
	      if (_didIteratorError3) {
	        throw _iteratorError3;
	      }
	    }
	  }

	  return ans;
	}

/***/ }),
/* 331 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.filterOptions = exports.vec = exports.conditionQueue = exports.selectRegion = exports.addFlag = exports.oldConditionList = undefined;

	var _slicedToArray = function () { function sliceIterator(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"]) _i["return"](); } finally { if (_d) throw _e; } } return _arr; } return function (arr, i) { if (Array.isArray(arr)) { return arr; } else if (Symbol.iterator in Object(arr)) { return sliceIterator(arr, i); } else { throw new TypeError("Invalid attempt to destructure non-iterable instance"); } }; }(); // import '../css/newqueryview.css';
	// import '../assets/css/flat-ui.css';
	// import 'jquery-ui/ui/widgets/slider';
	// import 'jquery-ui/ui/widgets/datepicker';
	// import 'jquery-ui/ui/widgets/spinner';
	// import 'jquery-ui/themes/base/all.css';

	// import '../css/jquery.datetimepicker.min.css';
	// import './jquery.datetimepicker.full.min.js';


	exports.init = init;
	exports.getConditionList = getConditionList;
	exports.renderPoilistsFoot = renderPoilistsFoot;
	exports.getIdsOnPoiResult = getIdsOnPoiResult;
	exports.filterAll = filterAll;

	var _jquery = __webpack_require__(327);

	var _jquery2 = _interopRequireDefault(_jquery);

	var _d2 = __webpack_require__(328);

	var d3 = _interopRequireWildcard(_d2);

	var _querydb = __webpack_require__(330);

	var QueryDb = _interopRequireWildcard(_querydb);

	var _datamanager = __webpack_require__(329);

	var DataManager = _interopRequireWildcard(_datamanager);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _toConsumableArray(arr) { if (Array.isArray(arr)) { for (var i = 0, arr2 = Array(arr.length); i < arr.length; i++) { arr2[i] = arr[i]; } return arr2; } else { return Array.from(arr); } }

	// import * as ShowMap from './showmap.js';
	// import * as ConfigPanel from './configpanel.js';
	// import * as MapRender from './maprender.js';
	// import * as VectorConfig from './vectorconfig.js';

	var conditionList = new Set();
	var oldConditionList = exports.oldConditionList = new Set();
	var addFlag = exports.addFlag = true;
	var selectRegion = exports.selectRegion = void 0;
	var conditionQueue = exports.conditionQueue = [];
	var vec = exports.vec = [];

	var SELECT = '-----';

	var poiType = [SELECT, '丽人', '公司企业', '购物', '休闲娱乐', '医疗', '教育培训', '汽车服务', '美食', '宾馆', '旅游景点', '生活服务', '运动健身', '交通设施', '政府机构', '行政区划', '房地产', '自然地物', '金融', '文化传媒'];
	var topicType = [SELECT, "finance", "shop", "traffic", "scenicspot", "uptown", "food", "hospital", "life", "hotel", "shop", "government", "enterprise", "beauty", "education"];

	var filterOptions = exports.filterOptions = new Map();

	function init() {
	  var container = d3.select('#leftcolumn').append('div').attr('id', 'inputcontainer');
	  var inputBar = container.append('div').attr('id', 'inputBar');
	  inputBar.append('input').attr('id', 'inputhide').classed('inputtext', true);

	  var searchInput = inputBar.append('div').classed('bootstrap-tagsinput', true);
	  searchInput.append('span').classed('searchtext', true);
	  searchInput.append('input').attr('id', 'inputbox').attr('type', 'text').attr('placeholder', '');
	  var searchButton = container.append('div').attr('id', 'inputquerycontainer').append('span').attr('id', 'inputquery').style('border', '1px black solid')
	  // .style('stroke-width', '1px')
	  .text('query');
	  // .attr('src', '../images/query.svg');
	  addInputListener(searchInput);
	  addSearchListener(searchButton);

	  // //test 
	  (0, _jquery2.default)('#inputbox').val('pass trainstation during [2014-1-14]');
	  updateTag(searchInput, { keyCode: '32' });
	  updateConditionQueue();
	  updateData();
	}

	function addInputListener(o) {
	  o.on("keyup", function (e) {
	    if (!e) {
	      e = window.event;
	    }
	    // updateTag(o, e);
	    if (e.keyCode == "13" || e.keyCode == "32" || e.keyCode == '8') {
	      updateConditionQueue();
	      updateData();
	    }
	  });
	  // .on("click", function(){
	  //   updateConditionQueue();
	  //   updateData();
	  // })
	}

	function addSearchListener(o) {
	  o.on('click', function (d, i) {
	    if (DataManager.poiResults.length >= 30) {
	      alert('poiResults\'s length must < 30');
	      return;
	    }
	    var selectSites = DataManager.getSelectSites();
	    DataManager.getStandardVector();
	    if (selectSites.length == 0) {
	      console.log('select site number is 0');
	      return;
	    }

	    var _findFilterRange = findFilterRange(),
	        _findFilterRange2 = _slicedToArray(_findFilterRange, 2),
	        time0 = _findFilterRange2[0],
	        time1 = _findFilterRange2[1];

	    console.log(time0, time1, '---------');
	    QueryDb.getPidByCondition(selectSites, DataManager.poiResults.length, time0, time1).then(function (result) {
	      return QueryDb.getTrajByPid(result);
	    }).then(function (result) {
	      return DataManager.setPeopleTraj(result);
	    }).then(function (result) {
	      return DataManager.handleTraj();
	    }).then(function (result) {
	      return dataTrans_YKJ();
	    }).catch(function (error) {
	      console.log(error);
	    });
	    // .then(result => ConfigPanel.init())
	    // .then(result => MapRender.reRendering())
	  });
	}
	// modified by ykj
	function dataTrans_YKJ() {
	  var trajs = DataManager.drawTraj;
	  var sites = DataManager.sites;
	  var siteTopic = DataManager.siteTopic;

	  trajs.forEach(function (traj) {
	    var _traj = traj.traj;
	    _traj.forEach(function (p) {
	      var s = p.site;
	      var site = sites.get(+s);
	      var sitetopic = siteTopic.get(s);

	      p.latitude = site.latitude;
	      p.longitude = site.longitude;
	      p.topics = sitetopic;
	    });
	  });

	  var storage = window.localStorage;
	  var d = JSON.stringify(trajs);
	  storage.setItem("DM", d);
	  console.log(storage['DM']);
	}

	function updateTag(node, e) {
	  var strs = (0, _jquery2.default)('#inputbox').val().split(' ').filter(function (o) {
	    return o;
	  }),
	      preStrs = ((0, _jquery2.default)('#inputhide').val() || '').split(',').filter(function (o) {
	    return o;
	  });
	  var nowStrs = void 0;
	  if (e.keyCode == '32' || e.keyCode == '13') {
	    // 空格或者回车
	    nowStrs = [].concat(_toConsumableArray(preStrs), _toConsumableArray(strs));
	  } else if (e.keyCode == '8') {
	    if (strs.length > 0) {
	      if ((0, _jquery2.default)('#inputbox').val().length == 1) {
	        d3.select('#inputbox').classed('notempty', true);
	      }
	      return;
	    }
	    if (d3.select('#inputbox').classed('notempty')) {
	      d3.select('#inputbox').classed('notempty', false);
	      return;
	    }
	    nowStrs = preStrs.filter(function (d, i, x) {
	      return i != x.length - 1;
	    });
	  } else {
	    return;
	  }
	  var data = node.selectAll('span').data(str2typeStr(nowStrs), function (d, i) {
	    return d + i;
	  });
	  var span = data.enter().insert('span', '#inputbox').classed('searchtext', true);
	  span.append('div').attr('class', function (d) {
	    return d.type + '_spantext';
	  }).classed('searchicon', true);
	  span.append('div').text(function (d) {
	    return d.name;
	  }).attr('class', 'searchtexttext').attr('spantype', function (d) {
	    return d.type;
	  }).on('click', function (d) {
	    if (!d3.select(this).classed('clicked')) {
	      if (d.type == 'normal') {
	        cleanPopup();
	        openPopup(d.name, (0, _jquery2.default)(this).offset().left - 18);
	      } else if (d.type == 'time') {
	        cleanPopup();
	        openTimePopup(d3.select(this), d.belong, d.name, (0, _jquery2.default)(this).offset().left - 18);
	      }
	      d3.select(this).classed('clicked', true);
	    } else {
	      cleanPopup();
	      d3.select(this).classed('clicked', false);
	    }
	  });
	  data.exit().remove();
	  (0, _jquery2.default)('#inputbox').val('');
	  (0, _jquery2.default)('#inputhide').val(nowStrs);
	}

	function str2typeStr(str) {
	  var ans = [];
	  var pre = void 0;
	  for (var i = 0; i < str.length; i++) {
	    var type = void 0;
	    if (str[i] == 'pass') {
	      type = 'pass';
	    } else if (str[i] == 'during') {
	      type = 'during';
	    } else if (str[i].indexOf('[') >= 0) {
	      type = 'time';
	    } else if (str[i] == 'and' || str[i] == 'or') {
	      type = 'conjunction';
	    } else {
	      type = 'normal';
	      pre = str[i];
	    }
	    ans.push({
	      type: type,
	      name: str[i]
	    });
	    if (type == 'time') {
	      ans[ans.length - 1].belong = pre;
	    }
	  }
	  return ans;
	}

	function updateConditionQueue() {
	  var strs = str2typeStr(((0, _jquery2.default)('#inputhide').val() || '').split(',').filter(function (o) {
	    return o;
	  }));
	  conditionQueue.length = 0;
	  for (var i = 0; i < strs.length; i++) {
	    if (strs[i].type == 'pass' || strs[i].type == 'during' || strs[i].type == 'conjunction') {
	      continue;
	    } else if (strs[i].type == 'normal') {
	      conditionQueue.push({
	        name: strs[i].name,
	        time: '',
	        data: [],
	        select: []
	      });
	    } else if (strs[i].type == 'time') {
	      var idx = conditionQueue.length - 1;
	      conditionQueue[idx].time = strs[i].name;
	    }
	  }
	}

	function updateData() {
	  // DataManager.poiResults.length = 0;
	  getPoisByCondition(getConditionList())
	  // .then(o => DataManager.removePoiResults(dif2))
	  .then(function (o) {
	    return console.log(DataManager.poiResults);
	  })
	  // .then(o => renderPoilists())
	  // .then(o => ShowMap.renderPois())
	  .then(function (o) {
	    return updateFilterTimes();
	  });
	  // .then(o => VectorConfig.init())
	}

	function getConditionList() {
	  var ans = [];
	  for (var i = 0; i < conditionQueue.length; i++) {
	    ans.push(conditionQueue[i].name);
	  }
	  return ans;
	}

	function getPoisByCondition(lists) {
	  var str = '';
	  if (lists.length == 0) {
	    return new Promise(function (resolve, reject) {
	      resolve();
	    });
	  }
	  var condition = lists.pop(),
	      o = condition;
	  if (o == 'trainstation') {
	    o = '火车站';
	  }
	  if (o == 'restaurant') {
	    o = '餐厅';
	  }
	  if (o == 'hospital') {
	    o = '医院';
	  }
	  if (o == 'college') {
	    o = '大学';
	  }
	  if (o.split(':').length > 1) {
	    str += 'type like "%' + o.split(":")[1] + '%" ';
	  } else {
	    str += 'name like "%' + o + '%" ';
	  }
	  var data = {
	    reqType: "queryDb",
	    operate: "select",
	    column: "*",
	    table: "site_poi",
	    limit: 'where (' + str + ')'
	  };
	  return QueryDb.pquery(data).then(function (results) {
	    if (results.length == 0) {
	      if (lists.length > 0) {
	        getPoisByCondition(lists);
	      } else {
	        return;
	      }
	    }
	    DataManager.addPoiResults(condition, results);
	    addVec(condition);
	    if (lists.length > 0) {
	      getPoisByCondition(lists);
	    }
	  });
	}

	function addVec(condition) {
	  var idx = DataManager.getIdxInPoiResultsByCondition(condition);
	  if (vec[idx]) {
	    return;
	  }
	  vec[idx] = new Map();
	  DataManager.topic2WordIndex.forEach(function (i, d) {
	    vec[idx].set(d, 1);
	  });
	}

	function cleanPopup(name) {
	  d3.selectAll('.conditionpopup').remove();
	  d3.selectAll('.conditiontimepopup').remove();
	}

	function openPopup(name, x) {
	  var node = d3.select('#leftcolumn').append('div').attr('class', 'conditionpopup').attr('identify', name).style('left', x).style('top', '-7px');
	  // node
	  //     .style('height', 275)
	  //[DataManager.getPoiResultsByCondition(name)] 是深赋值????
	  var idx = DataManager.getIdxInPoiResultsByCondition(name);
	  // const vp = node.append('div')
	  //     .attr('index', idx)
	  //     .attr('class', 'vectorparameters')
	  //     .call(renderVectorParameters);
	  var topicLayer = node.append('div').attr('class', 'queryviewtopiclayer').attr('dataindex', idx);
	  topicLayer.append('div').text('Filter').classed('topic-filter-text', true);
	  addTopicTypeLayer(topicLayer, topicType, 'topic');
	  node.append('div').text('POIs').classed('topic-filter-text', true);
	  var poiList = node.append('div').datum(DataManager.getPoiResultsByCondition(name)).attr('class', 'poilistcontainer');
	  renderPoilistsFoot(poiList);
	}

	function openTimePopup(tag, name, time, x) {
	  var idx = DataManager.getIdxInPoiResultsByCondition(name),
	      thisTime = new Date(time.split('[')[1].split(']')[0]);
	  var ymd = thisTime.toLocaleDateString();
	  var node = d3.select('#leftcolumn').append('div').attr('class', 'conditiontimepopup').attr('identify', name).style('left', x).style('top', '-7px').style('height', 230);
	  node.append('div').attr('id', 'timeselection');
	  (0, _jquery2.default)('#timeselection').datetimepicker({
	    format: 'Y-m-d',
	    value: ymd,
	    inline: true,
	    timepicker: false,
	    onSelectDate: function onSelectDate(ct, $i) {
	      tag.text('[' + d3.timeFormat('%Y-%m-%d')(ct) + ']');
	      ymd = d3.timeFormat('%Y-%m-%d')(ct);
	    }
	  });
	  var timeRange = node.append('div').attr('id', 'timerange');
	  timeRange.append('span').attr('id', 'timerangetitle').text('time:');
	  timeRange.append('span').attr('id', 'timerangetext').text('00:00:00 - 24:00:00');
	  timeRange.append('div').attr('id', 'timerangeslider');
	  (0, _jquery2.default)('#timerangeslider').slider({
	    range: true,
	    min: new Date('2014-1-14 00:00:00').getTime() / 1000,
	    max: new Date('2014-1-14 23:59:59').getTime() / 1000,
	    values: [new Date('2014-1-14 00:00:00').getTime() / 1000, new Date('2014-1-14 00:20:00.00').getTime() / 1000],
	    slide: function slide(event, ui) {
	      var val0 = new Date(ui.values[0] * 1000).toTimeString().split(' ')[0],
	          val1 = new Date(ui.values[1] * 1000).toTimeString().split(' ')[0];
	      d3.select('#timerangetext').text(val0 + ' - ' + val1);
	      var date0 = ymd + ' ' + val0,
	          date1 = ymd + ' ' + val1;
	      // console.log(date0, date1);
	      updateTime(idx, [date0, date1]);
	    }
	  });
	}

	function renderPoilistsFoot(node) {
	  // node.append('div')
	  //     .text('-----------  POIs -----------')
	  //     .classed('topic-filter-text', true)
	  node.attr('dataindex', function (d, i) {
	    return +this.parentNode.getAttribute('dataindex');
	  });
	  var layerData = node.selectAll('.poilistlayer').data(function (d) {
	    return d.data;
	  }, function (d) {
	    return d[0];
	  });
	  var layer = layerData.enter().append('div').attr('class', 'poilistlayer').attr('dataindex', function (d, i) {
	    return +this.parentNode.getAttribute('dataindex') + '-' + i;
	  });
	  var elementData = layer.merge(layerData).selectAll('.poilistelement').data(function (d) {
	    return d[1];
	  }, function (d) {
	    return d.name;
	  });
	  elementData.exit().remove();
	  var element = elementData.enter().append('div').classed('poilistelement', true).call(createElement);
	  element.merge(elementData).classed('isselect', function (d) {
	    return d.isSelect;
	  });
	  element.append('div').classed('poilisttext', true).text(function (o) {
	    return o.name;
	  });
	}

	function createElement(node) {
	  node.on('click', function (d, i) {
	    var ids = getIdsOnPoiResult(this, i);
	    d3.select(this).classed('isselect', !d.isSelect);
	    DataManager.modifyPoiResultsOne(ids, 'isSelect', !d.isSelect);
	    // ShowMap.renderPois();
	  }).on('mouseenter', function (d, i) {
	    var ids = getIdsOnPoiResult(this, i);
	    DataManager.modifyPoiResultsOne(ids, 'isMouseover', true);
	    // ShowMap.renderPois();
	  }).on('mouseleave', function (d, i) {
	    var ids = getIdsOnPoiResult(this, i);
	    DataManager.modifyPoiResultsOne(ids, 'isMouseover', false);
	    // ShowMap.renderPois();
	  });
	}

	function getIdsOnPoiResult(o, thisId) {
	  var ids = o.parentNode.getAttribute('dataindex').split('-').map(function (d) {
	    return d * 1;
	  });
	  ids.push(thisId);
	  return ids;
	}

	function renderVectorParameters(node) {
	  var cv = node.selectAll('.customervector').data([].concat(_toConsumableArray(DataManager.topic2WordIndex.keys())), function (d) {
	    return d;
	  }).enter().append('div').attr('class', 'customervector');
	  cv.append('div').attr('class', 'customervectorname').text(function (d) {
	    return d;
	  }).append('input').attr('class', 'form-control').attr('type', 'text').attr('name', function (d) {
	    return d;
	  }).attr('value', 1).on('keyup', function () {
	    var e = window.event;
	    var nextVal = void 0;
	    if (e.keyCode == '38') {
	      nextVal = (0, _jquery2.default)(this).val() * 1 + 1;
	      vec[this.parentNode.parentNode.getAttribute('index') * 1].set(this.getAttribute('name'), nextVal);
	      (0, _jquery2.default)(this).val(nextVal);
	    } else if (e.keyCode == '40') {
	      nextVal = (0, _jquery2.default)(this).val() * 1 - 1;
	      vec[this.parentNode.parentNode.getAttribute('index') * 1].set(this.getAttribute('name'), nextVal);
	      (0, _jquery2.default)(this).val(nextVal);
	    }
	  });
	}

	var sliderId = 0;

	function addTopicTypeLayer(node, data, type) {
	  var selects = node.append('div').classed('queryviewselects', true);
	  var selectData = selects.append('select').classed('queryviewselect', true).on('change', function () {
	    var now = d3.select(this);
	    if (now.property('value') !== SELECT && !now.classed('queryviewchanged')) {
	      var select = selects.append('div').classed('queryviewslide', true).attr('name', now.property('value')).attr('id', function (o) {
	        return 'queryviewselect' + sliderId++;
	      });
	      var idx = node.attr('dataindex') * 1;
	      var propertyname = now.property('value');
	      var beginCursor = selects.append('div').text(0).classed('begincursortxt2', true);
	      var endCursor = selects.append('div').text(100).classed('endcursortxt2', true);
	      (0, _jquery2.default)('#queryviewselect' + (sliderId - 1)).slider({
	        range: true,
	        min: 0,
	        max: 100,
	        values: [0, 100],
	        slide: function slide(event, ui) {
	          (0, _jquery2.default)(this).next('.begincursortxt2').css('left', 119 + ui.values[0] / 100 * 160).text(ui.values[0]);
	          (0, _jquery2.default)(this).next().next('.endcursortxt2').css('left', 114 + ui.values[1] / 100 * 160).text(ui.values[1]);
	          updateTopicType(idx, type, propertyname, [ui.values[0] / 100, ui.values[1] / 100]);
	        }
	      });
	      now.classed('queryviewc hanged', true);
	      addTopicTypeLayer(node, data, type);
	    }
	  }).selectAll("option").data(data);
	  selectData.enter().append('option').text(function (o) {
	    return o;
	  });
	}

	function findFilterRange() {
	  var time0 = void 0,
	      time1 = void 0;
	  var _iteratorNormalCompletion = true;
	  var _didIteratorError = false;
	  var _iteratorError = undefined;

	  try {
	    for (var _iterator = filterOptions[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
	      var _step$value = _slicedToArray(_step.value, 2),
	          idx = _step$value[0],
	          _filter = _step$value[1];

	      if (_filter.has('time')) {
	        var _filter$get = _filter.get('time'),
	            _filter$get2 = _slicedToArray(_filter$get, 2),
	            time3 = _filter$get2[0],
	            time4 = _filter$get2[1];

	        if (!time0 || time0.getTime() > new Date(time3).getTime()) {
	          time0 = new Date(time3);
	        }
	        if (!time1 || time1.getTime() < new Date(time4).getTime()) {
	          time1 = new Date(time4);
	        }
	      }
	    }
	  } catch (err) {
	    _didIteratorError = true;
	    _iteratorError = err;
	  } finally {
	    try {
	      if (!_iteratorNormalCompletion && _iterator.return) {
	        _iterator.return();
	      }
	    } finally {
	      if (_didIteratorError) {
	        throw _iteratorError;
	      }
	    }
	  }

	  if (!time0 || !time1) {
	    return ['2014-01-14 00:00:00.00', '2014-01-14 00:20:00.00'];
	  }
	  var parse = d3.timeFormat('%Y-%m-%d %H:%M:%S.%L');
	  return [parse(time0), parse(time1)];
	}

	function getFilterTime(idx) {
	  if (!filterOptions.has(idx) || !filterOptions.get(idx).get('time')) {
	    return ['2014-01-14 00:00:00.00', '2014-01-14 00:20:00.00'];
	  }
	  return filterOptions.get(idx).get('time');
	}

	function updateMoment(idx, time) {
	  if (!filterOptions.has(idx)) {
	    filterOptions.set(idx, new Map());
	  }
	  filterOptions.get(idx).set('moment', time);
	}

	function updateFilterTimes() {
	  for (var i = 0; i < conditionQueue.length; ++i) {
	    if (conditionQueue[i].time) {
	      var idx = DataManager.getIdxInPoiResultsByCondition(conditionQueue[i].name);
	      if (filterOptions.get(idx) && !filterOptions.get(idx).get('time')) {
	        updateTime(idx, conditionQueue[i].time);
	      }
	    }
	  }
	}

	function updateTime(idx, time) {
	  if (!filterOptions.has(idx)) {
	    filterOptions.set(idx, new Map());
	  }
	  if (time instanceof Array) {
	    filterOptions.get(idx).set('time', time);
	  } else {
	    var nextTime = new Date(new Date(time).getTime() + 20 * 60 * 1000),
	        time2 = d3.timeFormat('%Y-%m-%d %H:%M:%S.%L')(nextTime);
	    console.log('next time: ', time2);
	    filterOptions.get(idx).set('time', [time, time2]);
	  }
	}

	function updateTopicType(idx, type, propertyName, range) {
	  if (!filterOptions.has(idx)) {
	    filterOptions.set(idx, new Map());
	  }
	  var data = new Map();
	  if (filterOptions.get(idx).has(type)) {
	    data = filterOptions.get(idx).get(type);
	  }
	  data.set(propertyName, range);
	  filterOptions.get(idx).set(type, data);
	}

	function filterAll() {
	  for (var i = 0; i < DataManager.drawTraj.length; i++) {
	    if (!DataManager.drawTraj[i].matching) {
	      continue;
	    }
	    var _iteratorNormalCompletion2 = true;
	    var _didIteratorError2 = false;
	    var _iteratorError2 = undefined;

	    try {
	      for (var _iterator2 = DataManager.drawTraj[i].traj[Symbol.iterator](), _step2; !(_iteratorNormalCompletion2 = (_step2 = _iterator2.next()).done); _iteratorNormalCompletion2 = true) {
	        var traj = _step2.value;

	        if (traj.stoppoint) {
	          DataManager.drawTraj[i].filter = !checkTraj(traj, traj.stoppoint * 1);
	        }
	      }
	    } catch (err) {
	      _didIteratorError2 = true;
	      _iteratorError2 = err;
	    } finally {
	      try {
	        if (!_iteratorNormalCompletion2 && _iterator2.return) {
	          _iterator2.return();
	        }
	      } finally {
	        if (_didIteratorError2) {
	          throw _iteratorError2;
	        }
	      }
	    }
	  }
	  for (var _i = DataManager.drawTraj.length - 1; _i >= 0; --_i) {
	    if (!DataManager.drawTraj[_i].matching || DataManager.drawTraj[_i].filter) {
	      DataManager.drawTraj.splice(_i, 1);
	    }
	  }
	}

	function filter(idx) {
	  for (var i = 0; i < drawTraj.length; i++) {
	    if (!drawTraj[i].matching) {
	      continue;
	    }
	    var _iteratorNormalCompletion3 = true;
	    var _didIteratorError3 = false;
	    var _iteratorError3 = undefined;

	    try {
	      for (var _iterator3 = drawTraj[i].traj[Symbol.iterator](), _step3; !(_iteratorNormalCompletion3 = (_step3 = _iterator3.next()).done); _iteratorNormalCompletion3 = true) {
	        var traj = _step3.value;

	        if (traj.stoppoint * 1 == idx) {
	          drawTraj[i].filter = !checkTraj(traj, idx);
	        }
	      }
	    } catch (err) {
	      _didIteratorError3 = true;
	      _iteratorError3 = err;
	    } finally {
	      try {
	        if (!_iteratorNormalCompletion3 && _iterator3.return) {
	          _iterator3.return();
	        }
	      } finally {
	        if (_didIteratorError3) {
	          throw _iteratorError3;
	        }
	      }
	    }
	  }
	};

	function checkTraj(traj, idx) {
	  var limit = filterOptions.get(idx);
	  var _iteratorNormalCompletion4 = true;
	  var _didIteratorError4 = false;
	  var _iteratorError4 = undefined;

	  try {
	    for (var _iterator4 = limit[Symbol.iterator](), _step4; !(_iteratorNormalCompletion4 = (_step4 = _iterator4.next()).done); _iteratorNormalCompletion4 = true) {
	      var _step4$value = _slicedToArray(_step4.value, 2),
	          key = _step4$value[0],
	          value = _step4$value[1];

	      if (key == 'time') {
	        console.log(value[0], ' ^^^^^^', value[1]);
	        var nowTime = new Date(traj.time).getTime();
	        if (nowTime >= new Date(value[0]).getTime() && nowTime <= new Date(value[1]).getTime()) {
	          continue;
	        } else {
	          return false;
	        }
	      } else if (key == 'moment') {
	        var now = traj.stopTime / 1000 / 60 / 60 / 24;
	        if (now >= value[0] * 1 && now <= value[1] * 1) {
	          continue;
	        } else {
	          return false;
	        }
	      } else if (key == 'topic') {
	        var topics = DataManager.siteTopic.get(traj.site);
	        for (var i = 0; i < topics.length; i++) {
	          if (value.has(topics[i].topic)) {
	            var val = value.get(topics[i].topic) * 1;
	            if (val[0] <= topics[i].val && val[1] >= topics[i].val) {
	              continue;
	            } else {
	              return false;
	            }
	          }
	        }
	      } else if (key == 'type') {
	        // let types = QueryDb.sitePoiType.get(traj.traj+"");
	        // let sum = types.get('num') * 1;
	        // for (let [typeName, range] of value) {
	        //   if (!types.has(typeName)) {
	        //     if (range[0] > 0) {
	        //       return false;
	        //     } else {
	        //       continue;
	        //     }
	        //   }
	        //   let now = types.get(typeName)*1 / sum;
	        //   if (now < range[0] || now > range[1]) {
	        //     return false;
	        //   }
	        // }
	      }
	    }
	  } catch (err) {
	    _didIteratorError4 = true;
	    _iteratorError4 = err;
	  } finally {
	    try {
	      if (!_iteratorNormalCompletion4 && _iterator4.return) {
	        _iterator4.return();
	      }
	    } finally {
	      if (_didIteratorError4) {
	        throw _iteratorError4;
	      }
	    }
	  }

	  return true;
	};

/***/ }),
/* 332 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.getMerge_data = getMerge_data;
	exports.get_data = get_data;
	exports.init = init;

	var _queryutil = __webpack_require__(333);

	var QueryUtil = _interopRequireWildcard(_queryutil);

	var _jquery = __webpack_require__(327);

	var _jquery2 = _interopRequireDefault(_jquery);

	var _datamanager = __webpack_require__(329);

	var DataManager = _interopRequireWildcard(_datamanager);

	var _app = __webpack_require__(326);

	var _node_operate = __webpack_require__(334);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	var textData = []; //import * as d3 from 'd3';


	var name2POIMap = new Map();

	var tag_diff_data = {};

	function getMerge_data(_name) {
	  console.log(_name);
	  var _textData = textData.slice();
	  _textData.push([_name, 'cc']);
	  if (!tag_diff_data.hasOwnProperty(_name)) tag_diff_data[_name] = 'cc';
	  console.log(_textData);
	  return QueryUtil.get_poi_layer(_textData).then(function (results) {
	    results.forEach(function (poi) {
	      var name = poi.name,
	          data = poi.data;

	      name2POIMap.set(name, {
	        name: name,
	        data: data
	      });
	    });
	  }).then(function () {
	    return name2POIMap.get(_name);
	  });
	}

	function get_data(name) {
	  return name2POIMap.get(name);
	}

	function init() {
	  var searchInput = d3.select('#input-wrapper');
	  var button = d3.select('#search-button');
	  addInputListener(searchInput);
	  addSearchListener(button);
	}

	function addInputListener(o) {
	  o.on('compositionstart', function () {
	    // 拼音开始，暂不处理
	  });

	  o.on('compositionend', function () {
	    // 拼音结束
	    addParticle();
	  });

	  o.on("keyup", function (e) {
	    if (!e) {
	      e = window.event;
	    }
	    // 13：回车，32：空格，8：删除
	    if (e.keyCode == "13" || e.keyCode == "32") {
	      //检查是否已经输入了该词

	      addParticle();
	    } else if (e.keyCode == '8') {
	      var nowText = (0, _jquery2.default)('#search-input-text').val().trim();

	      if (nowText.length == 0) {
	        //只有删除这个词才会更新
	        // get_participle('',false)
	        removeParticle();
	      } else {
	        // get_participle(nowText)
	      }
	    }
	  });
	}

	function addParticle() {
	  // console.log('add')
	  var name = (0, _jquery2.default)('#search-input-text').val();
	  var rawText = textData.map(function (d) {
	    return d[0];
	  }).join('') + name;
	  QueryUtil.get_participle(rawText).then(function (o) {
	    o = o.filter(function (d) {
	      return d[0].trim().length > 0;
	    });
	    textData = o; // 获取词性
	    createTabs(o);
	    return o;
	  }).then(function (o) {
	    // 只有名称才会被添加
	    o.forEach(function (d) {
	      if (d[0] == name && d[1] == 'n' || d[1] == 'ns') {
	        addPOI(name);
	      }
	    });
	  }).then(function () {
	    (0, _jquery2.default)('#search-input-text').val('');
	  });
	}

	function removeParticle() {
	  console.log('rm');
	  var name = textData.pop()[0];
	  var rawText = textData.map(function (d) {
	    return d[0];
	  }).join('');
	  QueryUtil.get_participle(rawText) // 获取词性
	  .then(function (o) {
	    o = o.filter(function (d) {
	      return d[0].trim().length > 0;
	    });
	    textData = o;
	    createTabs(o);
	  }).then(function (o) {
	    var nodelist = __webpack_require__(336);
	    nodelist.delete_node_byName(name);
	  });
	}

	function addPOI(_name) {
	  QueryUtil.get_poi_layer(textData).then(function (results) {
	    console.log(results);
	    results.forEach(function (poi) {
	      var name = poi.name,
	          data = poi.data;

	      if (_name == name) {
	        var nodelist = __webpack_require__(336);
	        nodelist.append_node({
	          name: name,
	          data: data
	        });
	      }

	      name2POIMap.set(name, {
	        name: name,
	        data: data
	      });
	    });
	  });
	}

	// query button   查询按钮监听click事件，首先查询符合条件的轨迹，然后查询POI的层次信息
	function addSearchListener(object) {

	  object.on('click', function (d, i) {
	    console.log('Start Getting Data ...');
	    var t1 = new Date().getTime(),
	        nodelist = __webpack_require__(336),
	        sites_arr = [];
	    // 按照顺序 插入
	    nodelist.order.forEach(function (d) {
	      //  d = >  condition_node1 、condition_node2
	      var name = d3.select('#' + d).select('.title').select('.text').text();

	      var pois = nodelist.searchSiteList.get(name);
	      console.log(pois);
	      var sites = pois.map(function (p) {
	        return p.poi.site_id;
	      });
	      console.log(sites);

	      sites_arr.push(sites);
	    });

	    QueryUtil.get_trajs_new(sites_arr).then(function (results) {
	      DataManager.drawTraj = results;
	      console.log('_____', results.length);
	      console.log('GetData: ' + (Date().getTime() - t1) + 'ms');
	      return results;
	    }).then(function (results) {
	      return dataTrans_YKJ();
	    });
	  });
	}

	// modified by ykj

	function dataTrans_YKJ() {
	  console.log('Start Process Data ...');

	  var t1 = new Date().getTime();

	  var trajs = DataManager.drawTraj;
	  var sites = DataManager.sites;
	  var siteTopic = DataManager.siteTopic;

	  trajs.forEach(function (traj) {
	    var _traj = traj.traj;
	    _traj.forEach(function (p) {
	      var s = p.site;
	      var site = sites.get(+s);
	      var sitetopic = siteTopic.get(s);

	      p.latitude = site.latitude;
	      p.longitude = site.longitude;
	      p.topics = sitetopic;
	    });
	    traj.pid = traj.peopleid;
	  });

	  var t2 = new Date().getTime();
	  console.log('TransData: ' + (t2 - t1) + 'ms');

	  console.log(trajs);
	  (0, _app.setGlobalTrajData)(trajs);
	}

	var preClickedIndex = null;

	function createTabs(data) {
	  var container = d3.select('.search-container');
	  var divData = container.selectAll('.word-tab').data(data, function (d, i) {
	    return d[0] + '_' + d[i];
	  });
	  divData.classed('word-tab-clicked', function (d, i) {
	    return i == preClickedIndex;
	  });
	  divData.select('.tab-text-container .tab-text').text(function (d) {
	    return d[0].split('_').join('');
	  });
	  var div = divData.enter().insert('div', '#input-wrapper').attr('class', 'word-tab').call(d3.drag().on('start', _node_operate.word_tab_start).on('drag', _node_operate.word_tab_move).on('end', _node_operate.word_tab_end));

	  div.append('div').attr('class', 'tab-image-container').append('img').attr('src', function (d) {
	    if (d[1].indexOf('n') != -1) {
	      return '../assets/icons/POI.svg';
	    } else if (d[1].indexOf('t') != -1) {
	      return '../assets/icons/time.svg';
	    } else if (d[1].indexOf('v') != -1) {
	      return '../assets/icons/action.svg';
	    } else {
	      return '../assets/icons/others.svg';
	    }
	  });
	  div.append('div').attr('class', 'tab-text-container').append('div').attr('class', 'tab-text').text(function (d) {
	    return d[0].split('_').join('');
	  });
	  divData.exit().remove();

	  // div.on('click', function(clickedData) {
	  //   let flag = d3.select(this).classed('word-tab-clicked');
	  //   flag = !flag;
	  //   d3.select(this).classed('word-tab-clicked', flag);
	  //   // 此处需要重新获取下标，这是由于原始数据导致原始下标可能会发生变化
	  //   const nowIdx = textData.indexOf(clickedData);
	  //   if (flag) {
	  //     if (preClickedIndex && Math.abs(preClickedIndex - nowIdx) == 1) {
	  //       const minIdx = Math.min(preClickedIndex, nowIdx),
	  //           maxIdx = Math.max(preClickedIndex, nowIdx);
	  //       textData[minIdx][0] += '_' + textData[maxIdx][0];
	  //       textData[minIdx][1] = 'cc'
	  //       textData.splice(maxIdx, 1);
	  //       preClickedIndex = minIdx;
	  //     } else {
	  //       preClickedIndex = nowIdx;
	  //     }
	  //     createNewTab(textData)
	  //     updatePOILayer();
	  //   } else {
	  //     preClickedIndex = null;
	  //   }
	  // })


	  syncInputWrapperLength();
	}

	function syncInputWrapperLength() {
	  var allWidth = +d3.select('.search-container').style('width').slice(0, -2);
	  var sum = 0;
	  d3.selectAll('.word-tab').each(function () {
	    sum += +d3.select(this).style('width').slice(0, -2);
	    sum += 25;
	  });
	  d3.select('#input-wrapper').style('width', allWidth - sum + 'px');
	}

	function render_MDS(data, name, x, y) {
	  var x_extent = d3.extent(data, function (d) {
	    return d[2][0];
	  });
	  var y_extent = d3.extent(data, function (d) {
	    return d[2][1];
	  });
	  d3.select('#word-vecs-mds').remove();
	  var svg = d3.select('.search-container').append('svg').attr('id', 'word-vecs-mds').style('top', y + 40).style('left', x);
	  var g = svg.append('g').attr('transform', 'translate(60, 60)');
	  g.selectAll('circle').data(data, function (d) {
	    return d[0];
	  }).enter().append('circle').attr('class', 'mds-point').attr('cx', function (d) {
	    return (d[2][0] - x_extent[0]) * 110 / (x_extent[1] - x_extent[0]) - 55;
	  }).attr('cy', function (d) {
	    return (d[2][1] - y_extent[0]) * 110 / (y_extent[1] - y_extent[0]) - 55;
	  }).attr('r', 5).attr('fill', function (d) {
	    return d[0] == name ? '#444444' : 'white';
	  }).on('mouseover', function (d) {
	    console.log(d[0], name);
	  });
	}

/***/ }),
/* 333 */
/***/ (function(module, exports) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.pquery = pquery;
	exports.get_participle = get_participle;
	exports.get_trajs = get_trajs;
	exports.get_k_vecs = get_k_vecs;
	exports.get_poi_layer = get_poi_layer;
	exports.get_trajs_new = get_trajs_new;
	exports.send_cache = send_cache;
	exports.send_sites = send_sites;
	var IPADDRESS = 'ws://localhost:8000/';

	function _query(url) {
	  var myData = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};

	  return new Promise(function (resolve, reject) {
	    var ws = new WebSocket(IPADDRESS + url);
	    ws.onopen = function () {
	      ws.send(JSON.stringify(myData));
	    };
	    ws.onmessage = function (e) {
	      resolve(JSON.parse(e.data));
	    };
	  });
	}

	function pquery(myData) {
	  return new Promise(function (resolve, reject) {
	    var ws = new WebSocket(IPADDRESS);
	    ws.onopen = function () {
	      // console.log(JSON.stringify(myData));
	      ws.send(JSON.stringify(myData));
	    };
	    ws.onmessage = function (e) {
	      resolve(JSON.parse(e.data));
	    };
	  });
	}

	function get_participle(data) {
	  return _query('nlp/participle/', { 'text': data });
	}

	function get_trajs(data) {
	  return _query('nlp/trajs/', { 'text': data });
	}

	function get_k_vecs(data) {
	  return _query('nlp/k_vecs/', { 'text': data });
	}

	function get_poi_layer(data) {
	  return _query('nlp/poi_layer/', { 'text': JSON.stringify(data) });
	}

	// 可以根据用户的选择，合并分词，对于合并的词，采用BM25算法
	function get_trajs_new(data) {
	  return _query('nlp/trajs_new/', {
	    'text': JSON.stringify(data)
	  });
	}

	function send_cache(data) {
	  return _query('nlp/cache/', { 'trajs': data });
	}

	function send_sites(data) {
	  return _query('nlp/send_sites/', { 'sites': data });
	}

/***/ }),
/* 334 */
/***/ (function(module, exports, __webpack_require__) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.add_condition_node = add_condition_node;
	exports.initial_right_content = initial_right_content;
	exports.init_slider = init_slider;
	exports.renderingwordslist = renderingwordslist;
	exports.renderingPOIlist = renderingPOIlist;
	exports.fresh_list_width = fresh_list_width;

	var _searchbar = __webpack_require__(332);

	var _jquery = __webpack_require__(327);

	var _jquery2 = _interopRequireDefault(_jquery);

	var _node_interaction = __webpack_require__(335);

	var _poi = __webpack_require__(342);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function add_condition_node() {
	  var nodelist = __webpack_require__(336);
	  nodelist.append_node({ name: '', data: [] });
	}

	function initial_right_content() {
	  var nodelist = __webpack_require__(336);
	  nodelist.container.select('.right_content').remove();
	  var legend_list = [{
	    id: 'Relevance_Information',
	    name: 'Relevance Information:',
	    color: ['#7a0177', '#c51b8a', '#f768a1', '#fa9fb5', '#fcc5c0', '#feebe2']
	  }];
	  var right_content = nodelist.container.append('div').classed('right_content', true);
	  right_content.append('div').classed('add_condition_node', true).text('+').on('click', add_condition_node);
	  legend_list.map(function (x, y) {
	    var legend = d3.select('#Specification_view').append('div').classed('legend', true).attr('id', x.id).style('position', 'fixed').style('right', '70px').style('top', '380px').style('padding', '0 10px');
	    legend.append('div').classed('text', true).text(x.name);
	    var content = legend.append('div').classed('content', true);
	    content.append('div').classed('max', true).text(0);
	    x.color.map(function (color, i) {
	      content.append('div').classed('color_bar', true).style('background', color);
	    });
	    content.append('div').classed('min', true).text(0);
	  });
	}

	function init_slider(svg, text) {
	  var margin = {
	    right: 10,
	    left: 10
	  },
	      width = +svg.attr("width") - margin.left - margin.right,
	      height = +svg.attr("height");

	  var x = d3.scaleLinear().domain([0, 1]).range([0, width]).clamp(true);

	  var slider = svg.append("g").attr("class", "slider").attr("transform", "translate(" + margin.left + "," + height / 2 + ")");

	  slider.append("line").attr("class", "track").attr("x1", x.range()[0]).attr("x2", x.range()[1]).select(function () {
	    return this.parentNode.appendChild(this.cloneNode(true));
	  }).attr("class", "track-inset").select(function () {
	    return this.parentNode.appendChild(this.cloneNode(true));
	  }).attr("class", "track-overlay").call(d3.drag().on("start.interrupt", function () {
	    slider.interrupt();
	  }).on("start drag", function () {
	    hue(x.invert(d3.event.x), handle);
	  }));

	  var handle = slider.insert("circle", ".track-overlay").attr("class", "handle").attr("r", 4);

	  hue(1, handle);

	  function hue(h, handle) {
	    text.text(d3.format(".2f")(h));
	    handle.attr("cx", x(h));
	  }
	}

	function renderingwordslist(mergenode) {
	  var allwords = mergenode.select(".spatial_words").selectAll(".Worddiv").data(function (d) {
	    var heightset = [];
	    for (var i = 0; i < d.data.length; i++) {
	      heightset.push(d.data[i].data.length);
	      var same_word = -1;
	      for (var j = 0; j < d.data[i].data.length; j++) {
	        if (d.data[i].name == d.data[i].data[j].name) {
	          same_word = j;
	        } else d.data[i].data[j].index = (i, j);
	      }
	    }
	    return d.data;
	  }, function (d, i) {
	    return d.name;
	  });

	  allwords.exit().remove();
	  var addwords = allwords.enter().append("div").classed("Worddiv", true).attr('id', function (d, i) {
	    d.data.sort(function (a, b) {
	      return b.val - a.val;
	    });
	    return 'Worddiv' + (i + 1);
	  }).style("border", "1px dashed rgb(232, 232, 232)").style("padding", "3px 0").style("margin", "2px");

	  var mergewords = addwords.merge(allwords);
	  mergewords.style("top", function (d, i) {
	    return i * 24 + "px";
	  });

	  var addwordtitle = addwords.append("div").classed("wordtitle", true);
	  addwords.append("div").classed("wordsubtitle", true);
	  addwords.append("div").classed("nei_words", true);
	  var show_hide_div = addwordtitle.append('div').classed('hide_nei_words', true).style("background-image", "url(../icon/tri.png)").attr("isshow", true);
	  show_hide_div.on('click', _node_interaction.show_hide);
	  addwordtitle.append('div').classed('real_wordtitle', true).text(function (d, i) {
	    return d.name;
	  }).append('div').classed('delete', true).text('X').style('margin-right', '5px').on('click', function (d, i) {
	    var nodelist = __webpack_require__(336);
	    var subtitle = d.name;
	    var node_order = d3.select(this.parentNode.parentNode.parentNode.parentNode.parentNode).attr('id');
	    node_order = parseInt(node_order.substr(node_order.length - 1, 1));
	    var words = d3.select('#condition_node' + node_order).select('.title').select('.text').text().split('_');
	    words.splice(words.indexOf(subtitle), 1);
	    if (words.length == 0) {
	      nodelist.delete_node_byOrder(node_order);
	    }
	    if (words.length == 1) {
	      var data = (0, _searchbar.get_data)(words[0]);
	      data.order = node_order;
	      nodelist.data[node_order - 1] = data;
	      nodelist.node_rendering(data, node_order);
	    }
	    if (words.length > 1) {
	      (0, _searchbar.getMerge_data)(words.join('_')).then(function (merge_data) {
	        merge_data.order = node_order;
	        nodelist.data[node_order - 1] = merge_data;
	        nodelist.node_rendering(merge_data, node_order);
	      });
	    }
	  });
	  var allneiwords = mergewords.select(".nei_words").selectAll(".neiwordsdiv").data(function (d) {
	    return d.data;
	  });

	  allneiwords.exit().remove();
	  var addneiwords = allneiwords.enter().append("div").classed("neiwordsdiv", true).each(function (d) {
	    var real_word = d3.select(this.parentNode.parentNode).select('.wordtitle').select('.real_wordtitle').text();
	    real_word = real_word.substr(0, real_word.length - 1);
	    if (d.name == real_word) {
	      (0, _jquery2.default)(this).remove();
	    }
	  });
	  addneiwords.append('div').classed('wordsval', true);
	  addneiwords.append('div').classed('neiwordsdiv_word', true); //addneiwords
	  var mergeneiwords = addneiwords.merge(allneiwords);
	  mergeneiwords.style("top", function (d, i) {
	    return i * 24 + "px";
	  });
	  mergeneiwords.selectAll('.neiwordsdiv_word').text(function (d) {
	    return d.name;
	  }).style('width', function (d) {
	    return parseFloat(d.val) * 120 + "px";
	  });
	  mergeneiwords.selectAll('.wordsval').text(function (d) {
	    return parseFloat(d.val).toFixed(2);
	  }).style('width', '4px').style('right', function (d) {
	    return parseFloat(d.val) * 72 + 'px';
	  });

	  return show_hide_div;
	}

	function renderingPOIlist(mergenode) {
	  var max_num = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 20;

	  //locationlistdiv
	  // by ykj
	  // 列表现实的前 max_num  poi 存储在 POIS 中
	  var POIS = [];
	  //当前标签中POI列表的最大值和最小值，用于归一化来显示POI的div长度
	  var poi_max = 0;
	  var poi_min = 0;

	  if (max_num == 0) {
	    mergenode.select(".spatial_POIs").selectAll(".POIrect").remove();
	    return;
	  }
	  var allPOI = mergenode.select(".spatial_POIs").selectAll(".POIrect").data(function (d) {
	    var textcolorscale = 0;

	    // data
	    var pois = [];
	    var poi_map = {};

	    for (var i = 0; i < d.data.length; i++) {
	      var cur_max_map = {}; //用于存储当前关键词的各个POI的val的分别最大值
	      for (var j = 0; j < d.data[i].data.length; j++) {
	        for (var m = 0; m < d.data[i].data[j].data.length; m++) {
	          var poi = d.data[i].data[j].data[m];
	          if (cur_max_map.hasOwnProperty(poi.name)) {
	            if (cur_max_map[poi.name].val < poi.val) cur_max_map[poi.name] = { 'poi': poi, 'S': j };
	          } else {
	            if (poi.name != '医学院') cur_max_map[poi.name] = { 'poi': poi, 'S': j };
	          }
	        }
	      }
	      for (var k in cur_max_map) {
	        var poi_name = cur_max_map[k].poi.name;
	        if (poi_map.hasOwnProperty(poi_name)) {
	          var index = poi_map[poi_name];
	          if (pois[index].poi.val < cur_max_map[k].poi.val) pois[index].poi.val = cur_max_map[k].poi.val;
	        } else {
	          if (cur_max_map[k].poi.latitude > 27.9248561995 && cur_max_map[k].poi.latitude < 28.0769120675 && cur_max_map[k].poi.longitude > 120.5833650410 && cur_max_map[k].poi.longitude < 120.7579719628) {
	            poi_map[poi_name] = pois.length;
	            var _j = parseInt(cur_max_map[k]['S']);
	            pois.push({
	              index: pois.length,
	              poi: cur_max_map[k].poi,
	              words: {
	                F: (i, d.data[i].name),
	                S: (_j, d.data[i].data[_j].name)
	              }
	            });
	          }
	        }
	      }
	    }

	    //sort by val
	    pois.sort(function (a, b) {
	      return b.poi.val - a.poi.val;
	    });
	    poi_max = pois[0].poi.val;
	    poi_min = pois[pois.length - 1].poi.val;

	    if (pois.length < max_num) {
	      mergenode.each(function () {
	        d3.select(this.parentNode.parentNode.parentNode).select('.max_node_num').select('.node_num').property('value', pois.length);
	      });
	    } else pois = pois.slice(0, max_num);
	    d3.select('#condition_node' + d.order).attr('max_val', pois[0].poi.val).attr('min_val', pois[pois.length - 1].poi.val);
	    textcolorscale = pois[0].poi.val;
	    //update data index and color
	    for (var _i = 0; _i < pois.length; _i++) {
	      pois[_i].order = _i;
	      pois[_i].color = pois[_i].poi.val > textcolorscale / 2 ? "white" : "rgb(28,28,28)";
	    }

	    // by ykj
	    // 用于向后台返回数据
	    POIS = pois.slice();
	    var node_name = d.name;
	    var nodelist = __webpack_require__(336);
	    nodelist.searchSiteList.set(node_name, POIS);

	    return pois;
	  }, function (d) {
	    return d.poi.name;
	  });

	  allPOI.exit().remove();
	  var addPOI = allPOI.enter().append("div").classed("POIrect", true);
	  addPOI.append('div').classed('POIdiv', true);
	  addPOI.append('div').classed('POIval', true);
	  addPOI.append('div').classed('POIname', true);
	  var POIs = allPOI.merge(addPOI).style("top", function (d) {
	    return d.order * 27 + "px";
	  }).attr('val', function (d) {
	    return d.poi.val;
	  }).on('mouseenter', function (d) {
	    (0, _poi.draw)([d.poi]);
	  }).on('mouseleave', function (d) {
	    (0, _poi.remove)();
	  });
	  POIs.selectAll('.POIdiv').style('width', function (d) {
	    return parseFloat((parseFloat(d.poi.val) - poi_min) / (poi_max - poi_min)) * 70 + 'px';
	  });
	  POIs.selectAll('.POIval').text(function (d) {
	    return parseFloat(d.poi.val).toFixed(2);
	  }); //(parseFloat(d.poi.val)-poi_min)/(poi_max-poi_min)
	  POIs.selectAll('.POIname').text(function (d) {
	    return d.poi.name;
	  });
	  d3.select('#' + mergenode.attr('id').replace('locationlistdiv', 'condition_node')).select('.title').on('mouseenter', function (d) {
	    var _pois_show_in_map = POIS.map(function (p) {
	      return p.poi;
	    });
	    (0, _poi.draw)(_pois_show_in_map);
	  }).on('mouseleave', function () {
	    (0, _poi.remove)();
	  });
	}

	function fresh_list_width() {
	  //condition_node_list的宽度
	  var nodelist = __webpack_require__(336);
	  var current_width = nodelist.data.length * 652 + 50;
	  var spe_width = parseInt(document.getElementById('Specification_view').offsetWidth);
	  var width = current_width > spe_width ? current_width : spe_width;
	  nodelist.container.style('width', width + 'px');
	}

/***/ }),
/* 335 */
/***/ (function(module, exports, __webpack_require__) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});
	exports.path_colorsdomain = exports.path_colorscale = undefined;
	exports.drag_start = drag_start;
	exports.newdrag = newdrag;
	exports.drag_end = drag_end;
	exports.show_hide = show_hide;
	exports.initial_line = initial_line;
	exports.refresh_line = refresh_line;
	exports.refresh_path_color = refresh_path_color;
	exports.get_left_nodes = get_left_nodes;
	exports.decrease_locationlist = decrease_locationlist;
	exports.increase_locationlist = increase_locationlist;
	exports.refresh_POI_length = refresh_POI_length;

	var _Node = __webpack_require__(336);

	var _poi = __webpack_require__(342);

	var _node_operate = __webpack_require__(334);

	var path_colorscale = exports.path_colorscale = d3.scaleQuantize().range(['#feebe2', '#fcc5c0', '#fa9fb5', '#f768a1', '#c51b8a', '#7a0177']);
	// .range(['#fc4e2a','#fd8d3c','#feb24c','#fed976','#ffeda0','#ffffcc']);
	var path_colorsdomain = exports.path_colorsdomain = { max: 0, min: 0

	    //标签的拖拽，start时修改target的z-index及其动画时长
	};function drag_start() {
	    d3.select(this.parentNode).style("z-index", 10000).style('-webkit-transition-duration', '0s');
	}
	//标签处于drag状态，实时更新标签的位置（修改left），并根据位置确定是否切换标签
	function newdrag() {
	    var nodelist = __webpack_require__(336);
	    var condition_node = this.parentNode;
	    var dx = d3.event.sourceEvent.movementX;
	    var prex = parseInt(d3.select(condition_node).style('left'));
	    if (dx + prex <= 0) d3.select(condition_node).style('left', prex);else {
	        d3.select(condition_node).style('left', dx + prex + 'px');
	        var current_id = d3.select(condition_node).attr('id');
	        var current_location = nodelist.order.indexOf(current_id);
	        var next_node = null;
	        if (current_location + 1 < nodelist.order.length) next_node = d3.select('#' + nodelist.order[current_location + 1]);
	        var prev_node = null;
	        if (current_location - 1 >= 0) prev_node = d3.select('#' + nodelist.order[current_location - 1]);
	        if (next_node != null && parseInt(next_node.style('left')) - parseInt(d3.select(condition_node).style('left')) < parseInt(d3.select(condition_node).style('width')) / 2) {
	            var next_left = parseInt(next_node.style('left'));
	            var now_left = next_left - parseInt(d3.select(condition_node).style('width')) - 22;
	            next_node.style('left', now_left + 'px');
	            var current_num = d3.select(this).select('.constraints_order').text();
	            var next_num = next_node.select('.title').select('.constraints_order').text();
	            d3.select(this).select('.constraints_order').text(next_num);
	            next_node.select('.title').select('.constraints_order').text(current_num);
	            var temp = nodelist.order[current_location];
	            nodelist.order[current_location] = nodelist.order[current_location + 1];
	            nodelist.order[current_location + 1] = temp;
	        } else {
	            if (prev_node != null && parseInt(d3.select(condition_node).style('left')) - parseInt(prev_node.style('left')) < parseInt(d3.select(condition_node).style('width')) / 2) {
	                var prev_left = parseInt(prev_node.style('left'));
	                var _now_left = parseInt(d3.select(condition_node).style('width')) + 22 + prev_left;
	                prev_node.style('left', _now_left + 'px');
	                var _current_num = d3.select(this).select('.constraints_order').text();
	                var prev_num = prev_node.select('.title').select('.constraints_order').text();
	                d3.select(this).select('.constraints_order').text(prev_num);
	                prev_node.select('.title').select('.constraints_order').text(_current_num);
	                var _temp = nodelist.order[current_location];
	                nodelist.order[current_location] = nodelist.order[current_location - 1];
	                nodelist.order[current_location - 1] = _temp;
	            }
	        }
	    }
	}
	//各标签归原位
	function drag_end() {
	    var nodelist = __webpack_require__(336);
	    var condition_node = this.parentNode;
	    d3.select(condition_node).style('-webkit-transition-duration', '0.5s').style("z-index", 0);
	    var current_id = d3.select(condition_node).attr('id');
	    var current_location = 0;
	    for (var i = 0; i < nodelist.order.length; i++) {
	        if (current_id == nodelist.order[i]) {
	            current_location = i;
	            break;
	        }
	    }
	    d3.select('#' + nodelist.order[current_location]).style('left', current_location * (parseInt(d3.select(condition_node).style('width')) + 22) + "px");
	    d3.select(condition_node).style('z-index', 0);
	}

	//切换关键词是否显示，并对相应的连线进行刷新
	function show_hide() {
	    var nei_words = d3.select(this.parentNode.parentNode).select('.nei_words');
	    var wordsubtitle = d3.select(this.parentNode.parentNode).select('.wordsubtitle');
	    var index = d3.select(this.parentNode.parentNode.parentNode.parentNode).attr('id').replace('spatial_left', 'condition_node');
	    index = index.substr(index.length - 1, 1);
	    if (d3.select(this).attr("isshow") == "false") //show
	        {
	            nei_words.style('display', 'block');
	            wordsubtitle.style('display', 'block');
	            d3.select(this).style("background-image", "url(../icon/tri.png)").attr("isshow", true);
	        } else {
	        nei_words.style('display', 'none');
	        wordsubtitle.style('display', 'none');
	        d3.select(this).style("background-image", "url(../icon/trijian.png)").attr("isshow", false);
	    }
	    get_left_nodes(index);
	}

	//每次滚动都要调用该方法
	function initial_line(index) {
	    //condition_node1
	    var locationlistdiv_id = index.replace('condition_node', 'locationlistdiv');
	    var nodelist = __webpack_require__(336);
	    var left_nodes = _Node.line_data[index].left;
	    var right_nodes = [];
	    d3.select('#' + locationlistdiv_id).select('.spatial_POIs').selectAll('.POIrect').each(function () {
	        right_nodes.push(this);
	    });
	    if (left_nodes.length > 0 && right_nodes.length > 0) {
	        (function () {
	            var current_line_dta = [];
	            var data_num = index.substr(index.length - 1, 1);
	            var second_third_index = {};
	            var max_val = 0;
	            var min_val = 0;

	            var _loop = function _loop(i) {
	                var second_title = '';
	                var third_title = '';
	                if (String($(left_nodes[i]).attr('class')) == 'real_wordtitle') {
	                    second_title = d3.select(left_nodes[i]).text();
	                    second_title = second_title.substr(0, second_title.length - 1); //title后面会有个X，把它删掉
	                    third_title = second_title;
	                } else {
	                    second_title = d3.select(left_nodes[i].parentNode.parentNode).select('.wordtitle').select('.real_wordtitle').text();
	                    second_title = second_title.substr(0, second_title.length - 1); //title后面会有个X，把它删掉
	                    third_title = d3.select(left_nodes[i]).select('.neiwordsdiv_word').text();
	                }

	                if (!second_third_index.hasOwnProperty(second_title)) {
	                    for (var _i = 0; _i < nodelist.data[data_num - 1].data.length; _i++) {
	                        if (second_title == nodelist.data[data_num - 1].data[_i].name) {
	                            second_third_index[second_title] = nodelist.data[data_num - 1].data[_i].data;
	                            break;
	                        }
	                    }
	                }
	                var third_forth_data = second_third_index[second_title];
	                var forth_data = []; //location list
	                for (var j = 0; j < third_forth_data.length; j++) {
	                    if (third_title == third_forth_data[j].name) {
	                        forth_data = third_forth_data[j].data;
	                        break;
	                    }
	                }
	                right_nodes.map(function (x, y) {
	                    var right_nodename = d3.select(x).select('.POIname').text();
	                    for (var n = 0; n < forth_data.length; n++) {
	                        if (right_nodename == forth_data[n].name) {
	                            if (max_val < forth_data[n].relation_val) max_val = forth_data[n].relation_val;
	                            if (min_val == 0 || min_val > forth_data[n].relation_val) min_val = forth_data[n].relation_val;
	                            current_line_dta.push({ left: left_nodes[i], right: x, relation_val: forth_data[n].relation_val, poi: forth_data[n] });
	                        }
	                    }
	                });
	            };

	            for (var i = 0; i < left_nodes.length; i++) {
	                _loop(i);
	            }

	            path_colorsdomain.max = max_val > path_colorsdomain.max ? max_val : path_colorsdomain.max;
	            path_colorsdomain.min = min_val < path_colorsdomain.min || path_colorsdomain.min == 0 ? min_val : path_colorsdomain.min;
	            path_colorscale.domain([path_colorsdomain.min, path_colorsdomain.max]);
	            d3.select('#' + index).attr('max_relation_val', max_val).attr('min_relation_val', min_val);
	            d3.select('#' + index).select('.spatial_lines').selectAll('path').remove();
	            d3.select('#' + index).select('.spatial_lines').selectAll('circle').remove();
	            var spatial_lines = d3.select('#' + index).select('.spatial_lines').selectAll('path').data(current_line_dta).enter();
	            spatial_lines.append('path').attr('d', function (d) {
	                var left_y = parseInt(d3.select(d.left).attr('current_top')) + parseInt(d3.select(d.left).style('height')) / 2;
	                var right_x = parseInt(d3.select(this.parentNode).style('width')) + 1;
	                var right_y = parseInt(d3.select(d.right).style('top')) + parseInt(d3.select(d.right).style('height')) / 2 - 4; //-$('#'+locationlistdiv_id).scrollTop()
	                var path_way = 'M 8 ' + left_y + ' ' + 'C ' + (8 + right_x / 3) + ' ' + left_y + ' ' + (8 + right_x * 2 / 3) + ' ' + right_y + ' ' + right_x + ' ' + right_y;
	                return path_way;
	            }).attr('stroke', function (d) {
	                return path_colorscale(d.relation_val);
	            }).attr('stroke-width', 2).attr('fill', 'none').attr('relation_val', function (d) {
	                return d.relation_val;
	            }).attr('initial_y', function (d) {
	                return parseInt(d3.select(d.right).style('top')) + parseInt(d3.select(d.right).style('height')) / 2;
	            }) //-$('#'+locationlistdiv_id).scrollTop()
	            .attr('start_y', function (d) {
	                parseInt(d3.select(d.left).attr('current_top')) + parseInt(d3.select(d.left).style('height')) / 2;
	            });
	            spatial_lines.append('circle').attr('cx', '5').attr('cy', function (d) {
	                return parseInt(d3.select(d.left).attr('current_top')) + parseInt(d3.select(d.left).style('height')) / 2;
	            }).attr('r', '2').attr('stroke-width', '2').attr('stroke', 'rgb(46,117,182)').attr('fill', 'white');
	            refresh_path_color(); //对已存在的线进行重新刷新颜色比例尺
	            left_nodes.forEach(function (left) {
	                var hover_nodes = [];
	                var hover_POIs = [];
	                current_line_dta.forEach(function (item) {
	                    if (item.left == left) {
	                        hover_nodes.push(item.right);
	                        hover_POIs.push(item.poi);
	                    }
	                });
	                d3.select(left).on('mouseenter', function () {
	                    right_nodes.forEach(function (right) {
	                        return d3.select(right).style('filter', 'opacity(40%)');
	                    });
	                    hover_nodes.forEach(function (node) {
	                        return d3.select(node).style('filter', 'opacity(100%)');
	                    });
	                    (0, _poi.draw)(hover_POIs);
	                }).on('mouseleave', function () {
	                    (0, _poi.remove)();
	                    right_nodes.forEach(function (right) {
	                        return d3.select(right).style('filter', 'opacity(100%)');
	                    });
	                });
	            });
	            right_nodes.forEach(function (right, index1) {
	                var hover_nodes = [];
	                var hover_POIs = '';
	                current_line_dta.forEach(function (item, index2) {
	                    if (item.right == right) {
	                        hover_nodes.push(item.left);
	                        hover_POIs = item.poi;
	                    }
	                });
	                d3.select(right).on('mouseenter', function () {
	                    left_nodes.forEach(function (left) {
	                        return d3.select(left).select('.neiwordsdiv_word').style('filter', 'opacity(40%)');
	                    });
	                    hover_nodes.forEach(function (node) {
	                        return d3.select(node).select('.neiwordsdiv_word').style('filter', 'opacity(100%)');
	                    });
	                    (0, _poi.draw)([hover_POIs]);
	                }).on('mouseleave', function () {
	                    (0, _poi.remove)();
	                    left_nodes.forEach(function (left) {
	                        return d3.select(left).select('.neiwordsdiv_word').style('filter', 'opacity(100%)');
	                    });
	                });
	            });
	        })();
	    } else {
	        d3.select('#' + index).select('.spatial_lines').selectAll('path').remove();
	        d3.select('#' + index).select('.spatial_lines').selectAll('circle').remove();
	    }
	}
	//滚动POIlist时刷新连线位置
	function refresh_line(order) {
	    //locationlistdiv1
	    var top_height = $('#locationlistdiv' + order).scrollTop();
	    $('#locationlistdiv' + order).parent().find('path').each(function (index, e) {
	        var d = $(e).attr('d').trim().split(' '); //["M", "-0", "56.5", "C", "30", "199.5","30", "199.5", "90", "542"]
	        d[d.length - 1] = parseFloat($(e).attr('initial_y')) - top_height; //right_y,d[2]:left_y
	        d[d.length - 3] = d[d.length - 1];
	        $(e).attr('d', d.join(' '));
	    });
	}
	//更新path_colorsdomain并刷新连线颜色
	function refresh_path_color() {
	    var max_ralation_vals = [];
	    d3.selectAll('.condition_node').each(function () {
	        max_ralation_vals.push(d3.select(this).attr('max_relation_val'));
	    });
	    var min_ralation_vals = [];
	    d3.selectAll('.condition_node').each(function () {
	        min_ralation_vals.push(d3.select(this).attr('min_relation_val'));
	    });
	    if (min_ralation_vals.length > 0) {
	        path_colorsdomain.min = d3.min(min_ralation_vals);
	        path_colorsdomain.max = d3.max(max_ralation_vals);
	        path_colorscale.domain([path_colorsdomain.min, path_colorsdomain.max]);
	        d3.select('#Relevance_Information').select('.content').select('.max').text(parseFloat(path_colorsdomain.max).toFixed(1));
	        d3.select('#Relevance_Information').select('.content').select('.min').text(parseFloat(path_colorsdomain.min).toFixed(1));
	        d3.selectAll('.spatial_lines').selectAll('path').attr('stroke', function (d) {
	            return path_colorscale(d3.select(this).attr('relation_val'));
	        });
	    } else {
	        d3.select('#Relevance_Information').select('.content').select('.max').text(0);
	        d3.select('#Relevance_Information').select('.content').select('.min').text(0);
	    }
	}
	//关键词那栏show，hide或滚动时都更新左侧节点位置，用于更新线的位置
	function get_left_nodes(index) {
	    //index:1,2,3...
	    _Node.line_data['condition_node' + index].left = [];
	    var top_height = $('#spatial_left' + index).scrollTop();
	    d3.select('#spatial_left' + index).select('.spatial_words').selectAll('.Worddiv') //主题词div集合
	    .each(function (d, i) {
	        $(this).find('.real_wordtitle').each(function () {
	            var current_element_top = $(this).offset().top - $(this.parentNode.parentNode.parentNode).offset().top;
	            d3.select(this).attr('current_top', current_element_top - top_height);
	            _Node.line_data['condition_node' + index].left.push(this);
	        });

	        var order = $(this).attr('id');
	        order = parseInt(order.substr(order.length - 1, 1));
	        var top_length = 0;
	        for (var _i2 = 1; _i2 <= order; _i2++) {
	            top_length = top_length + $("#Worddiv" + _i2)[0].getBoundingClientRect().height;
	        } //包括margin
	        var show_hide = $(this).find('.hide_nei_words').attr('isshow');
	        if (show_hide == 'true') {
	            $(this).find('.neiwordsdiv').each(function () {
	                var current_element_top = $(this).offset().top - $(this.parentNode.parentNode.parentNode).offset().top; //parseInt(d3.select(this).style('top')) + top_length;
	                d3.select(this).attr('current_top', current_element_top - top_height);
	                _Node.line_data['condition_node' + index].left.push(this);
	            });
	        }
	    });
	    initial_line('condition_node' + index);
	    refresh_line(index);
	}

	function decrease_locationlist() {
	    var nodelist = __webpack_require__(336);
	    var current_max = parseInt(d3.select(this.parentNode).select('.node_num').property('value'));
	    if (current_max - 1 >= d3.select(this.parentNode).select('.node_num').attr('min')) {
	        d3.select(this.parentNode).select('.node_num').property('value', current_max - 1);
	        var condition_nodeid = d3.select(this.parentNode.parentNode.parentNode).attr('id');
	        var current_conditionnode_order = condition_nodeid.substr(condition_nodeid.length - 1, 1);
	        var current_data = [];
	        for (var i = 0; i < nodelist.data.length; i++) {
	            if (nodelist.data[i].order == current_conditionnode_order) current_data = nodelist.data[i];
	        }(0, _node_operate.renderingPOIlist)(d3.select('#locationlistdiv' + current_conditionnode_order).data([current_data]), current_max - 1);
	        initial_line('condition_node' + current_conditionnode_order);
	        refresh_line(current_conditionnode_order);
	    }
	}
	function increase_locationlist() {
	    var nodelist = __webpack_require__(336);
	    var current_max = parseInt(d3.select(this.parentNode).select('.node_num').property('value'));
	    if (current_max + 1 <= d3.select(this.parentNode).select('.node_num').attr('max')) {
	        d3.select(this.parentNode).select('.node_num').property('value', current_max + 1);
	        var condition_nodeid = d3.select(this.parentNode.parentNode.parentNode).attr('id');
	        var current_conditionnode_order = condition_nodeid.substr(condition_nodeid.length - 1, 1);
	        var current_data = [];
	        for (var i = 0; i < nodelist.data.length; i++) {
	            if (nodelist.data[i].order == current_conditionnode_order) current_data = nodelist.data[i];
	        }(0, _node_operate.renderingPOIlist)(d3.select('#locationlistdiv' + current_conditionnode_order).data([current_data]), current_max + 1);
	        initial_line('condition_node' + current_conditionnode_order);
	        refresh_line(current_conditionnode_order);
	    }
	}

	//更新POI的长度
	function refresh_POI_length() {
	    var max_vals = [];
	    d3.selectAll('.condition_node').each(function () {
	        var max = d3.select(this).attr('max_val');
	        if (max != null) max_vals.push(max);
	    });
	    var min_vals = [];
	    d3.selectAll('.condition_node').each(function () {
	        var min = d3.select(this).attr('min_val');
	        if (min != null) min_vals.push(min);
	    });
	    if (min_vals.length > 0) {
	        _Node.poi_colordomain.min = d3.min(min_vals);
	        _Node.poi_colordomain.max = d3.max(max_vals);
	        d3.selectAll('.POIrect').each(function () {
	            d3.select(this).select('.POIdiv').style('width', function () {
	                return parseFloat(d3.select(this.parentNode).attr('val')) / _Node.poi_colordomain.max * 60 + "px";
	            });
	        });
	    } else {
	        _Node.poi_colordomain.min = 0;
	        _Node.poi_colordomain.max = 0;
	    }
	}

/***/ }),
/* 336 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.poi_colordomain = exports.POI_colorscale = exports.is_initial_right_content = exports.line_data = undefined;

	var _param = __webpack_require__(337);

	var _time = __webpack_require__(347);

	var _app = __webpack_require__(326);

	var _node_interaction = __webpack_require__(335);

	var _node_operate = __webpack_require__(334);

	var _word_tabs = __webpack_require__(348);

	var _data_processing = __webpack_require__(349);

	var _poi = __webpack_require__(342);

	var _jquery = __webpack_require__(327);

	var _jquery2 = _interopRequireDefault(_jquery);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	var nodelist = {
	  container: d3.select("#condition_node_list"),
	  data: [],
	  //poi包含relation_val:当前POI与对应的搜索词的初始相似度
	  // max_val:单个搜索词时max_val等同于val，搜索词合并后等于poi与各个搜索词的max val的总和
	  // val:当前POI与他所对应的搜索词的相似度，初始val=relation_val,之后会随参数调整而变化
	  order: [],

	  siteScore: new Map(),
	  searchSiteList: new Map(),
	  time: [{ 'y': '', 'month': '', 'd': '', 'o': '', 'm': '' }, { 'y': '', 'month': '', 'd': '', 'o': '', 'm': '' }] //[{'y':2018,'m':'01','d':'10','t':'9:00'},{'y':2018,'m':'01','d':'10','t':'11:00'}]//保存时间约束的起止时间
	}; // import * as d3 from 'd3';
	// import $ from 'jquery';

	var line_data = exports.line_data = [];
	var is_initial_right_content = exports.is_initial_right_content = false;
	var POI_colorscale = exports.POI_colorscale = d3.scaleQuantize().range(['#ffffcc', '#ffeda0', '#fed976', '#feb24c', '#fd8d3c', '#fc4e2a']);
	var poi_colordomain = exports.poi_colordomain = {
	  max: 0,
	  min: 0
	};

	nodelist.reOrder = function refresh_list(a, current_node_id) {
	  var alpha = a;
	  var current_conditionnode_order = current_node_id.substr(current_node_id.length - 1, 1);
	  var current_data = [];
	  for (var i = 0; i < nodelist.data.length; i++) {
	    if (nodelist.data[i].order == current_conditionnode_order) {
	      current_data = nodelist.data[i]; //或者给这个结构在外面再套一层，跟listnode.data一样的结构
	      break;
	    }
	  }
	  (0, _data_processing.initial_siteScore)(current_data, alpha, true);
	  //normalization(current_data)
	  (0, _node_operate.renderingPOIlist)(d3.select('#locationlistdiv' + current_conditionnode_order).data([current_data]), parseInt((0, _jquery2.default)('#' + current_node_id).find('.node_num').prop('value')));
	  (0, _node_interaction.initial_line)('condition_node' + current_conditionnode_order);
	  (0, _node_interaction.refresh_line)(current_conditionnode_order);

	  (0, _app.calTrajsOrder)(); //重新调整轨迹的order
	};
	nodelist.delete_node_byOrder = function (index) {
	  //例如删除condition_node1则index为1
	  d3.select('#condition_node' + index).remove();
	  for (var i = 0; i < nodelist.data.length; i++) {
	    if (nodelist.data[i].order == index) {

	      nodelist.data.splice(i, 1);
	      for (var j = i; j < nodelist.data.length; j++) {
	        d3.select('#condition_node' + nodelist.data[j].order).attr('id', 'condition_node' + (nodelist.data[j].order - 1));
	        d3.select('#spatial_left' + nodelist.data[j].order).attr('id', 'spatial_left' + (nodelist.data[j].order - 1));
	        d3.select('#locationlistdiv' + nodelist.data[j].order).attr('id', 'locationlistdiv' + (nodelist.data[j].order - 1));
	        var target_index = nodelist.order.indexOf("condition_node" + nodelist.data[j].order);
	        nodelist.order[target_index] = 'condition_node' + (nodelist.data[j].order - 1);
	        nodelist.data[j].order = nodelist.data[j].order - 1;
	      }
	      break;
	    }
	  }
	  //把当前卡片所对应的order里面的元素删掉
	  var current_order = nodelist.order.indexOf("condition_node" + index);
	  for (var m = current_order; m < nodelist.order.length - 1; m++) {
	    nodelist.order[m] = nodelist.order[m + 1];
	    d3.select('#' + nodelist.order[m]).style('left', 692 * m + 'px').select('.title').select('.constraints_order').text(m + 1);
	  }
	  nodelist.order.pop(); //最后一个没用，删掉
	  (0, _node_operate.fresh_list_width)();
	  (0, _node_interaction.refresh_path_color)();
	  //refresh_POI_length()
	  (0, _data_processing.initial_siteScore)(nodelist); //更新sitescore
	  (0, _poi.remove)(); //清空map中的POI
	};
	nodelist.node_rendering = function (initial_node_data, index) {
	  (0, _poi.remove)();
	  if (initial_node_data.data.length != 0) {
	    (0, _data_processing.max_value_POI)(initial_node_data);
	    //normalization(initial_node_data)
	  }
	  (0, _data_processing.initial_siteScore)(nodelist); //用于轨迹list的相似度计算
	  initial_node_data.order = index;
	  var node_data = [];
	  node_data.push(initial_node_data);

	  (0, _node_operate.fresh_list_width)();
	  if (!is_initial_right_content) {
	    (0, _node_operate.initial_right_content)();
	    exports.is_initial_right_content = is_initial_right_content = true;
	  }
	  var current_node = (0, _jquery2.default)("#condition_node" + index);
	  if (current_node.length > 0) {
	    current_node.empty();
	    current_node = d3.select("#condition_node" + index).data(node_data);
	  } else {
	    current_node = nodelist.container.append("div").data(node_data).classed("condition_node", true).style('-webkit-transition-duration', '0.5s').attr('id', function (d) {
	      nodelist.order.push('condition_node' + index);
	      return 'condition_node' + index;
	    }).each(function () {
	      line_data[d3.select(this).attr('id')] = {
	        left: [],
	        right: []
	      };
	    });
	    current_node.style("left", function (d) {
	      return 692 * (index - 1) + 'px';
	    });
	  }

	  //title
	  var title = current_node.append("div").classed("title", true);
	  title.append("div").classed("constraints_order", true);
	  title.append("div").classed("text", true).style('display', 'inline-block');
	  title.append('div').classed('delete', true).text('X').style('line-height', '22px').style('background-size', '22px').style('width', '22px').style('background-image', 'url(../icon/circle.png)').on('click', function (d) {
	    nodelist.delete_node_byOrder(d.order);
	  });

	  current_node.select(".title").select(".constraints_order").text(function (d) {
	    return d.order;
	  }).style('background-image', 'url(../icon/circle.png)');
	  current_node.select(".title").select(".text").text(function (d) {
	    return d.name;
	  });
	  current_node.selectAll(".title").call(d3.drag().on("start", _node_interaction.drag_start).on("drag", _node_interaction.newdrag).on('end', _node_interaction.drag_end));

	  //time constraints
	  var timeconstraings = current_node.append("div").classed("timeconstraints", true).style("margin", "5px").style("height", "38px").style("border", "1px dashed #e8e8e8");

	  timeconstraings.append("div").classed("node_subtitle", true);
	  var timecontainer = timeconstraings.append("div");
	  var temp = timecontainer.append("div").classed("textcon", true) //.classed("text", true)
	  .style('width', '130px').append('div').classed('text', true).style("margin", "0px 10px 0px 5px").style("color", "#2e75b6").style("text-align", "center").style("font-size", "15px").style("line-height", "38px").style('display', 'inline-block').style('width', '110px');
	  timecontainer.select('.textcon').append('div').classed('delete', true).style('margin-top', '0px').style('margin-right', '10px').style('font-size', '15px').text('X').style('position', 'absolute').on('click', function () {
	    d3.select(this.parentNode.parentNode).selectAll('input').property('value', '');
	    d3.select(this.parentNode).select('.text').text('');
	    nodelist.time = [{ 'y': '', 'month': '', 'd': '', 'o': '', 'm': '' }, { 'y': '', 'month': '', 'd': '', 'o': '', 'm': '' }];
	    //.append(this)
	  });
	  timecontainer.append("input").classed("starttime", true).classed("textinput", true).each(function () {
	    (0, _jquery2.default)(this).bind('input propertychange', function () {
	      var nodelist = __webpack_require__(336);
	      var time = d3.select(this).property('value').split(' ')[0].split('.');
	      if (time[0]) nodelist.time[0]['y'] = time[0];
	      if (time[1]) nodelist.time[0]['month'] = time[1];
	      if (time[2]) nodelist.time[0]['d'] = time[2];
	      var time_w = '';
	      if (nodelist.time[0]['y'] != '') time_w += nodelist.time[0]['y'] + "年";
	      if (nodelist.time[0]['month'] != '') time_w += nodelist.time[0]['month'] + "月";
	      if (nodelist.time[0]['d'] != '') time_w += nodelist.time[0]['d'] + "日";
	      d3.select(this.parentNode).select('.textcon').select('.text').text(time_w);
	    });
	  });
	  timecontainer.append("div").classed("conj", true).classed("textcon", true);
	  timecontainer.append("input").classed("endtime", true).classed("textinput", true).each(function () {
	    (0, _jquery2.default)(this).bind('input propertychange', function () {
	      var nodelist = __webpack_require__(336);
	      var time = d3.select(this).property('value').split(' ')[0].split('.');
	      if (time[0]) nodelist.time[1]['y'] = time[0];
	      if (time[1]) nodelist.time[1]['month'] = time[1];
	      if (time[2]) nodelist.time[1]['d'] = time[2];
	      var time_w = '';
	      if (nodelist.time[1]['y'] != '') time_w += nodelist.time[1]['y'] + "年";
	      if (nodelist.time[1]['month'] != '') time_w += nodelist.time[1]['month'] + "月";
	      if (nodelist.time[1]['d'] != '') time_w += nodelist.time[1]['d'] + "日";
	      d3.select(this.parentNode).select('.textcon').select('.text').text(time_w);
	    });
	  });

	  current_node.select(".timeconstraints").select(".node_subtitle").text("Temporal Constraints");
	  // current_node.select(".timeconstraints").select(".text").text("")

	  current_node.select(".timeconstraints").select(".starttime").property('value', ""); //2018.01.10 9:00
	  current_node.select(".timeconstraints").select(".conj").text("~");
	  current_node.select(".timeconstraints").select(".endtime").property('value', ""); //2018.01.10 11:00
	  current_node.select(".timeconstraints").each(_word_tabs.change_cur_time);

	  //spatial constraints
	  var spatial_constraints = current_node.append("div").classed("spatial_constraints", true);
	  spatial_constraints.append("div").classed("spatial_constraints_title", true).text("Extracted POIs");

	  spatial_constraints.append('div').classed('max_node_num', true).style("width", "120px");
	  spatial_constraints.select('.max_node_num').append('div').classed('text', true).text('Number');
	  spatial_constraints.select('.max_node_num').append('div').classed('decrease', true).text('-').on('click', _node_interaction.decrease_locationlist).style("background-image", "url(../icon/circleblue.png)");
	  spatial_constraints.select('.max_node_num').append('input').classed('node_num', true).style("float", "left").style("line-height", "31px").style("background", "transparent").style(" text-align", "center").style("width", "22px").style("text-align", "center").attr('type', 'number').attr('min', 0).attr('max', function (d) {
	    var sum_location = 0;
	    d.data.forEach(function (x, i) {
	      x.data.forEach(function (y, j) {
	        y.data.forEach(function (z, m) {
	          return sum_location++;
	        });
	      });
	    });
	    if (sum_location < 20) {
	      d3.select(this).property('value', sum_location);
	    } else d3.select(this).property('value', 20);
	    return parseInt(sum_location);
	  }).on('change', function () {
	    if (d3.select(this).property('value') > d3.select(this).attr('max')) d3.select(this).property('value', d3.select(this).attr('max'));
	    if (d3.select(this).property('value') < d3.select(this).attr('min')) d3.select(this).property('value', d3.select(this).attr('min'));
	    if (d3.select(this).property('value') <= parseInt(d3.select(this).attr('max')) && d3.select(this).property('value') >= parseInt(d3.select(this).attr('min'))) {

	      var condition_nodeid = d3.select(this.parentNode.parentNode.parentNode.parentNode.parentNode).attr('id');
	      var current_conditionnode_order = condition_nodeid.substr(condition_nodeid.length - 1, 1);
	      var current_data = [];
	      for (var i = 0; i < nodelist.data.length; i++) {
	        if (nodelist.data[i].order == current_conditionnode_order) current_data = nodelist.data[i];
	      }(0, _node_operate.renderingPOIlist)(d3.select('#locationlistdiv' + current_conditionnode_order).data([current_data]), d3.select(this).property('value'));
	      (0, _node_interaction.initial_line)('condition_node' + current_conditionnode_order);
	      (0, _node_interaction.refresh_line)(current_conditionnode_order);
	    }
	  });
	  spatial_constraints.select('.max_node_num').append('div').classed('increase', true).text('+').on('click', _node_interaction.increase_locationlist).style("background-image", "url(../icon/circleblue.png)");
	  var spatial_cc = spatial_constraints.append("div").style("height", "calc(100% - 33px)").style("position", "absolute").style("width", "100%");

	  var spatialwordcontainer = spatial_cc.append("div").attr('id', function (d) {
	    return 'spatial_left' + d.order;
	  }).style("width", "197px") // "182px")
	  .style("height", "calc(100% - 5px)").style("margin", "3px").style('overflow-y', 'auto')
	  // .style("border", "1px dashed rgb(232, 232, 232)")
	  .style("position", "absolute").each(function () {
	    (0, _jquery2.default)(this).scroll(function () {
	      var id = d3.select(this).attr('id');
	      id = parseInt(id.substr(id.length - 1, 1));
	      (0, _node_interaction.get_left_nodes)(id);
	    });
	  });

	  spatialwordcontainer.append("div").classed("spatiallogoback", true);
	  spatialwordcontainer.append("div").classed("spatiallogo", true).text("Spatial Constraints Words");
	  spatialwordcontainer.append("div").classed("spatial_words", true).style("overflow-y", "auto").attr("dir", "rtl").style("width", "calc(100% - 41px)").style("padding", "3px 1px").attr("height", "calc(100% - 6px)").style("overflow-y", "auto").style("position", "absolute").style("right", "0").style("padding", "3px");

	  // .style("font-size", "15px").style("float", "left").style("width", "185px")


	  spatial_cc.append("svg").style("width", "64px").style("position", "absolute").style("height", "calc(100% - 6px)").style("left", "189px").style("margin", "3px 0").style("z-index", "3").classed("spatial_lines", true);

	  var locationlistdiv = spatial_cc.append("div").classed("locationlistdiv", true).attr('id', function (d, i) {
	    return 'locationlistdiv' + d.order;
	  }).append("div").classed("spatial_POIs", true).style("width", "100%");

	  spatial_cc.append("div").classed("POIback", true);
	  spatial_cc.append("div").classed("POIlogo", true).text("Location List");

	  var show_hide_div = (0, _node_operate.renderingwordslist)(current_node);
	  (0, _node_operate.renderingPOIlist)(spatial_cc.select('.locationlistdiv')); //待修改-----------------


	  //semantic constraints
	  var semantic_constraints = current_node.append("div").classed("semantic_constraints", true);
	  semantic_constraints.append("div").classed("spatial_constraints_title", true).text("Configuration Panel");

	  (0, _param.appendParamWidges)(semantic_constraints);
	  (0, _time.bindTimeChangeEvent)();

	  show_hide_div.each(function () {
	    /*d3.select(this.parentNode.parentNode).select('.nei_words').style('visibility', 'visible');
	    d3.select(this.parentNode.parentNode).select('.wordsubtitle').style('visibility', 'visible');*/
	    d3.select(this.parentNode.parentNode).select('.nei_words').style('display', 'block');
	    d3.select(this.parentNode.parentNode).select('.wordsubtitle').style('display', 'block');
	    d3.select(this).text('-');
	    //let current_id = d3.select(this.parentNode.parentNode).attr('id').replace('Worddiv', 'spatial_left');
	    var index = d3.select(this.parentNode.parentNode.parentNode.parentNode).attr('id').replace('spatial_left', 'condition_node'); //修改不知道对不对------------------------------------------------------------
	    index = index.substr(index.length - 1, 1);
	    (0, _node_interaction.get_left_nodes)(index);
	  });
	  locationlistdiv.each(function (d) {
	    //d3.select(this.parentNode)
	    var current_id = d3.select(this.parentNode).attr('id');
	    (0, _jquery2.default)(this.parentNode).scroll(function () {
	      var id = d3.select(this).attr('id');
	      id = parseInt(id.substr(id.length - 1, 1));
	      (0, _node_interaction.refresh_line)(id);
	    });
	  });
	};
	nodelist.append_node = function (data) {
	  var param = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : [];

	  if (param.length == 0) {
	    data.order = nodelist.data.length + 1;
	    nodelist.data.push(data);
	    nodelist.node_rendering(data, nodelist.data.length);
	  } else {
	    data.order = param[1];
	    nodelist.data[param[0]] = data;
	    nodelist.node_rendering(data, param[1]);
	  }
	};
	nodelist.delete_node_byName = function (name) {
	  for (var i = 0; i < nodelist.data.length; i++) {
	    if (nodelist.data[i].name == name) {
	      nodelist.delete_node_byOrder(nodelist.data[i].order);
	      break;
	    }
	  }
	};

	module.exports = nodelist;

/***/ }),
/* 337 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.appendParamWidges = appendParamWidges;

	var _d = __webpack_require__(328);

	var d3 = _interopRequireWildcard(_d);

	var _config = __webpack_require__(338);

	var Config = _interopRequireWildcard(_config);

	var _Node = __webpack_require__(336);

	var _datamanager = __webpack_require__(329);

	var DataManager = _interopRequireWildcard(_datamanager);

	var _pic = __webpack_require__(339);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	var scale = d3.scaleLinear().range([0, 1]);

	var scaleAB = d3.scaleLinear().range([0, 1]);

	function appendParamWidges(roots) {
		roots.each(function () {
			var root = d3.select(this);
			addParamRects(root);
		});
	}

	function addParamRects(root) {

		root.append('div').attr('class', 'high-level-desc').text('Parameters for Relevance Computation');
		addAB(root);
		root.append('div').attr('class', 'high-level-desc').text('Function Specification');
		Config.topicNames.forEach(function (topic, i) {
			addOneParamRect(root, i);
		});
	}
	function addOneParamRect(root, i) {
		var current_node_id = '';
		root.each(function () {
			current_node_id = d3.select(this.parentNode).attr('id');
		});

		var visBox = document.getElementsByClassName("semantic_constraints")[0];
		var height = visBox.offsetHeight; //高度
		var width = visBox.offsetWidth; //宽度

		var rectWidth = 55,
		    rectHeight = 10;

		scale.domain([0, rectWidth - 15]);

		var group = root.append('div').attr('class', 'param-rect');
		group.append('div').attr('class', 'param-name').text(function () {
			switch (Config.topicNames[i].name) {
				case "住宅":
					return 'Residential Related';
					break;
				case "娱乐商业":
					return 'Entertainment Related';
					break;
				case "办公":
					return 'Business Related';
					break;
				case "医疗":
					return 'Medical Related';
					break;
				case "交通":
					return 'Traffic Related';
					break;
				case "教育":
					return 'Educational Related';
					break;
			}
		});
		var drag_bar = group.append('div').style('height', rectHeight + 4).style('width', rectWidth);

		drag_bar.append('rect').style('position', 'absolute').style('width', rectWidth + 'px').style('height', 6 + 'px').style('margin', '9px').attr('y', 5).each(function () {
			$(this).slider().on("slide", function (event, ui) {
				d3.select(this.parentNode).select('.param-num').text(parseFloat(ui.value).toFixed(1));
				calcSims(current_node_id);
			}).slider("option", "min", 0).slider("option", "max", 1).slider("option", "step", 0.1).slider("value", 1);
			d3.select(this).select("span").style("width", "8px").style("margin-left", "-4px").style("height", "14px");
		});
		drag_bar.append('div').attr('class', 'param-num').style('position', 'absolute').style('right', '1px').style('line-height', '30px').text(1);
	}

	function addAB(root) {
		var current_node_id = '';
		root.each(function () {
			current_node_id = d3.select(this.parentNode).attr('id');
		});
		var group = root.append('div').attr('class', 'param-ab-rect');
		var Agroup = group.append('div').attr('class', 'param-half-rect param-a');
		var Bgroup = group.append('div').attr('class', 'param-half-rect param-b');

		var rectWidth = 35,
		    rectHeight = 10;

		scaleAB.domain([0, rectWidth - 15]);

		group.selectAll('.param-half-rect').append('div').attr('class', 'param-name');

		group.selectAll('.param-half-rect').append('div').style('width', '50px').style('position', 'absolute').style('height', 6 + 'px').style('margin', '15px').each(function () {
			$(this).slider().on("slide", function (event, ui) {
				var num = parseFloat(ui.value).toFixed(1);
				d3.select(this.parentNode).select('.param-num').text(num);
				if (d3.select(this.parentNode).attr('class') == 'param-half-rect param-a') {
					var another = (1 - num).toFixed(1);
					d3.select(this.parentNode.parentNode).selectAll('div').each(function () {
						if (d3.select(this).attr('class') == 'param-half-rect param-b') {
							d3.select(this).select('.param-num').text(another);
							d3.select(this).select('.ui-slider').each(function () {
								$(this).slider('value', another);
							});
						}
					});
				} else {
					var _another = (1 - num).toFixed(1);
					d3.select(this.parentNode.parentNode).selectAll('div').each(function () {
						if (d3.select(this).attr('class') == 'param-half-rect param-a') {
							d3.select(this).select('.param-num').text(_another);
							d3.select(this).select('.ui-slider').each(function () {
								$(this).slider('value', _another);
							});
						}
					});
				}
				calcSims(current_node_id);
			}).slider("option", "min", 0).slider("option", "max", 1).slider("option", "step", 0.1).slider("value", 1 /*function(){
	                                                                                                           console.log(this,'this--------------------------')
	                                                                                                           if(d3.select(this.parentNode).attr('class')=='param-half-rect param-a')
	                                                                                                           return 1
	                                                                                                           else
	                                                                                                           return 0
	                                                                                                           }*/);
			d3.select(this).select("span").style("width", "8px").style("margin-left", "-4px").style("height", "14px");
		});
		group.selectAll('.param-half-rect').append('div').attr('class', 'param-num');
		Agroup.select('.param-name').text('α');
		Agroup.select('.param-num').text(1);

		Bgroup.select('.param-name').text('β');
		Bgroup.select('.param-num').text(0);
	}

	function getParamVals(nodeId) {
		// 第几个 
		var container = d3.select('#' + nodeId).select('.semantic_constraints');
		var params = {
			'T_I': []
		};
		container.selectAll('.param-rect').each(function () {
			var name = d3.select(this).select('.param-name').text();
			var val = d3.select(this).select('.param-num').text();
			params['T_I'].push(+val);
		});
		container.selectAll('.param-half-rect').each(function () {
			var name = d3.select(this).select('.param-name').text();
			var val = d3.select(this).select('.param-num').text();
			params[name] = +val;
		});

		return params;
	}

	function calcSims(nodeId) {

		var nodelist = __webpack_require__(336);

		var order = +nodeId.slice(-1);

		var params = getParamVals(nodeId);

		// console.log( params)

		// return

		var T_I = params['T_I'],
		    a = params['α'],
		    b = params['β'];

		nodelist.data.forEach(function (constrain) {
			if (constrain.order == order) {
				constrain.data.forEach(function (words) {

					words.data.forEach(function (locations) {
						locations.data.forEach(function (location, i) {
							location.simT = b * getSim(T_I, location.site_id);
						});
					});
				});
			}
		});

		nodelist.reOrder(a, nodeId);
	}

	function getSim(T_I, siteId) {
		var siteTopic = DataManager.siteTopic;
		var topics = siteTopic.get('' + siteId);
		var T_L = [0, 0, 0, 0, 0, 0];

		if (!topics) {
			console.log(" empty topics in site ", siteId);
			return 0;
		}

		topics.forEach(function (topic) {
			var name = topic.topic,
			    val = topic.val;

			Config.topicNames.forEach(function (topic_, i) {
				if (topic_.contain.indexOf(name) != -1) {
					T_L[i] += val;
				}
			});
		});

		return sim(T_I, T_L);
	}

	function sim(I, L) {

		var i = 0,
		    denom = 0,
		    nume = 0,
		    l_I = 0,
		    l_L = 0;

		// 分子
		for (i = 0; i < 6; i++) {
			nume += I[i] * L[i];
		}
		//分母
		for (i = 0; i < 6; i++) {
			l_I += I[i] * I[i];
			l_L += L[i] * L[i];
		}
		l_I = Math.sqrt(l_I);
		l_L = Math.sqrt(l_L);
		denom = l_I * l_L;

		return nume / denom;
	}

/***/ }),
/* 338 */
/***/ (function(module, exports) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.transTopicHexa = transTopicHexa;
	// Configs 
	var topicNameList = exports.topicNameList = ["Beauty", "Food", "Shop", "Uptown", "Education", "Hospital", "Hotel", "Life", "Finance", "Traffic", "Enterprise", "Scenicspot", "Government"];

	// export let colorList = ['#8dd3c7','#bebada','#fb8072','#28B78D','#fdb462',
	// 				'#FFD034','#fccde5','#d9d9d9','#bc80bd','#ccebc5',
	// 				'#80b1d3','#F40076','#b3de69','#fccde5']


	var duration = exports.duration = 400;

	var colorList = exports.colorList = ['#8dd3c7', '#fb8072', '#fdb462', '#28B78D', '#bebada', '#f8b739'];
	var iconSrcUrl = exports.iconSrcUrl = '../semantictrajdemo/assets/icon/';
	var topicThemesConfig = exports.topicThemesConfig = {
	  min_width: 50, // 显示 icon
	  oneBoxWidth: 50 // 单个 icon box 的宽度
	};

	var vRectHeight = exports.vRectHeight = 27;

	var topicNames = exports.topicNames = [{
	  name: '住宅',
	  contain: ['Uptown', 'Life', 'Scenicspot', 'Education', 'Hospital'],
	  percent: {
	    'Uptown': 0.3,
	    'Life': 0.3,
	    'Scenicspot': 0.2,
	    'Education': 0.1,
	    'Hospital': 0.1
	  },
	  icon: 'Residential_white.png'
	}, {
	  name: '娱乐商业',
	  contain: ['Beauty', 'Food', 'Finance', 'Shop', 'Hotel'],
	  percent: {
	    'Beauty': 0.3,
	    'Food': 0.2,
	    'Finance': 0.2,
	    'Shop': 0.2,
	    'Hotel': 0.1
	  },
	  icon: 'Entertainment_white.png'
	}, {
	  name: '办公',
	  contain: ['Enterprise', 'Government', 'Finance'],
	  percent: {
	    'Enterprise': 0.4,
	    'Government': 0.4,
	    'Finance': 0.2
	  },
	  icon: 'Business_white.png'
	}, {
	  name: '医疗',
	  contain: ['Hospital'],
	  percent: {
	    'Hospital': 1
	  },
	  icon: 'Medical_white.png'
	}, {
	  name: '交通',
	  contain: ['Traffic'],
	  percent: {
	    'Traffic': 1
	  },
	  icon: 'Traffic_white.png'
	}, {
	  name: '教育',
	  contain: ['Education'],
	  percent: {
	    'Education': 1
	  },
	  icon: 'Educational_white.png'
	}];

	function transTopicHexa(topics) {
	  if (!topics) return null;
	  var topicsHexa = topicNames.map(function (t, i) {
	    return {
	      name: t.name,
	      val: 0,
	      icon: t.icon,
	      color: colorList[i]
	    };
	  });
	  topics.forEach(function (t) {
	    topicNames.forEach(function (T, i) {
	      if (T.contain.indexOf(t.topic) != -1) {
	        topicsHexa[i].val += t.val * T.percent[t.topic];
	      }
	    });
	  });

	  var topicsHexaSort = topicsHexa.sort(function (t1, t2) {
	    var v1 = t1.val,
	        v2 = t2.val;
	    return v2 - v1;
	  });
	  return topicsHexaSort;
	}

/***/ }),
/* 339 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.stack = exports.filter = undefined;

	var _slicedToArray = function () { function sliceIterator(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"]) _i["return"](); } finally { if (_d) throw _e; } } return _arr; } return function (arr, i) { if (Array.isArray(arr)) { return arr; } else if (Symbol.iterator in Object(arr)) { return sliceIterator(arr, i); } else { throw new TypeError("Invalid attempt to destructure non-iterable instance"); } }; }();

	exports.draw = draw;
	exports.shadowCanvas = shadowCanvas;
	exports.unShadowCanvas = unShadowCanvas;

	var _clip = __webpack_require__(340);

	var _index = __webpack_require__(341);

	var _util = __webpack_require__(343);

	var _app = __webpack_require__(326);

	var _config = __webpack_require__(346);

	var Config = _interopRequireWildcard(_config);

	var _index2 = __webpack_require__(341);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	var trajsData = void 0;

	var stack = [];
	/*
		stack = [{
			latlng : 经纬度表示的在边界内的轨迹,              // stack[0]表示未经过框选的
			pixel  : 像素表示的在边界内的轨迹,
			url    : 绘制的图形的url,
			filter : 被筛去的轨迹id 数组
		},{
			rect   : 选择框的周围点经纬度表示,					// stack[1-n]表示每一次框选操作的结果
			latlng : 同上（经过本次框选后的）,
			pixel  : 同上（经过本次框选后的）.
			url    : 对应的图形的url，
			filter ， 同时加上上一次筛去的
		},{
			...
		}]
	*/

	// 库函数 用于 判断直线是否经过矩形

	function draw(data) {
		var isChanged = (0, _util.updateTileBoundry)(),
		    originPiexelPoints = void 0,
		    selectPiexelPoints = void 0,
		    urlCache = {};

		// 视窗变化时 选择框需要重新绘制
		clearCanvas();

		if (data != undefined) {
			//data 来自全局 ，表示数据更新

			trajsData = data;

			if (!Config.PicUpdateFlag) {
				//timeSelect 时需要将stack rect 寸下来
				exports.stack = stack = []; //数据重置
				processTrajsData(trajsData);
				originPiexelPoints = stack[0].pixel;
				selectPiexelPoints = null;
				stack[0].url = GetTrajsPicUrl(originPiexelPoints, 0);
			}
			console.log(trajsData);

			// drawheatmap(trajsData)
		} else {
			if (!trajsData) return; //没有数据 且 无data 
		}

		if (data == undefined && isChanged || Config.PicUpdateFlag) {
			//数据未改变 ，视图边界变化了 需要重新绘制 

			// 将 stack 原 rect 数据记录下来
			var newStack = [];
			for (var s = 1; s < stack.length; s++) {
				newStack[s] = {
					rect: stack[s].rect
				};
			}
			exports.stack = stack = [];

			processTrajsData(trajsData);
			originPiexelPoints = stack[0].pixel;
			selectPiexelPoints = null;
			stack[0].url = GetTrajsPicUrl(originPiexelPoints, 0);

			//替换
			for (var _s = 1; _s < newStack.length; _s++) {
				stack[_s] = newStack[_s];
			}

			if (Config.PicUpdateFlag) Config.PicUpdateFlag = false;
		}

		// ----------------------------------------
		// 进行框选动作 
		// ----------------------------------------

		if (stack.length >= 2) {

			if (!stack[stack.length - 1].pixel && !stack[stack.length - 2].pixel) {
				//顺序被打乱 重头filter
				for (var i = 1; i < stack.length; i++) {
					var rect = stack[i].rect;
					filter(rect.bottom_left, rect.top_right, i);
				}

				selectPiexelPoints = stack[stack.length - 1].pixel; // 只生成最后一项 的url ? 画selct图时只需要最后一项的url
				stack[stack.length - 1].url = GetTrajsPicUrl(selectPiexelPoints, 1);
			} else if (stack[stack.length - 1].rect && !stack[stack.length - 1].pixel) {
				//刚增加一个选择框 , 只需filter最后一项

				var _rect = stack[stack.length - 1].rect;
				filter(_rect.bottom_left, _rect.top_right, stack.length - 1);
				selectPiexelPoints = stack[stack.length - 1].pixel;
				stack[stack.length - 1].url = GetTrajsPicUrl(selectPiexelPoints, 1);
			} else {
				if (!stack[stack.length - 1].url) {
					//删去最后一个选择框，但是上一次是重排过的
					selectPiexelPoints = stack[stack.length - 1].pixel;
					stack[stack.length - 1].url = GetTrajsPicUrl(selectPiexelPoints, 1);
				}
			}

			urlCache = {
				origin: stack[0].url,
				select: stack[stack.length - 1].url,
				originData: stack[0].latlng,
				selectData: stack[stack.length - 1].latlng
			};
		} else {
			urlCache = {
				origin: stack[0].url,
				select: null,
				originData: stack[0].latlng,
				selectData: null
			};
		}

		(0, _app.filterGlobalData)(stack[stack.length - 1].filter);
		// console.log("Stack",stack)
		drawTrajs(urlCache);
	}

	// #canvas-upon-map 	   上绘制未选择的轨迹
	// #canvas-upon-map-select 上为选择后留下的轨迹
	function drawTrajs(url) {
		if (url.select) {
			var canvasSelect = document.getElementById('canvas-upon-map-select');
			var ctxSelect = canvasSelect.getContext('2d');

			var t3 = new Date().getTime();
			canvasSelect.width = _index.width;
			canvasSelect.height = _index.height;
			ctxSelect.clearRect(0, 0, _index.width, _index.height);

			var imgSelect = new Image();
			imgSelect.src = url.select;
			imgSelect.onload = function () {
				ctxSelect.drawImage(imgSelect, _util.left, _util.top, _index.width, _index.height, 0, 0, _index.width, _index.height);
				var t4 = new Date().getTime();
				// console.log('draw select pic: ' + (t4-t3) + 'ms')
			};

			(0, _index2.drawheatmap)(url.selectData);

			// 不画 origin 轨迹
			return;
		}

		if (url.origin) {
			var canvasOrigin = document.getElementById('canvas-upon-map');
			var ctxOrigin = canvasOrigin.getContext('2d');

			var t1 = new Date().getTime();
			canvasOrigin.width = _index.width;
			canvasOrigin.height = _index.height;
			ctxOrigin.clearRect(0, 0, _index.width, _index.height);

			if (url.select) {
				ctxOrigin.globalAlpha = 0.9;
			}

			var imgOrigin = new Image();
			imgOrigin.src = url.origin;
			imgOrigin.onload = function () {
				ctxOrigin.drawImage(imgOrigin, _util.left, _util.top, _index.width, _index.height, 0, 0, _index.width, _index.height);
				var t2 = new Date().getTime();
				// console.log('draw origin pic: ' + (t2-t1) + 'ms')
			};

			(0, _index2.drawheatmap)(url.originData);
		}
	}

	function clearCanvas() {
		var canvasOrigin = document.getElementById('canvas-upon-map');
		var ctxOrigin = canvasOrigin.getContext('2d');
		ctxOrigin.clearRect(0, 0, _index.width, _index.height);

		var canvasSelect = document.getElementById('canvas-upon-map-select');
		var ctxSelect = canvasSelect.getContext('2d');
		ctxSelect.clearRect(0, 0, _index.width, _index.height);
	}

	function processTrajsData(data) {
		//处理原始数据在 boundry 内的轨迹，未涉及到框选

		var trajs_pixelpoints = [],
		    trajs_in_tile_boundry = [],
		    latlng_points = void 0,
		    trajdataInTileBoundry = void 0,
		    tileBox = [_util.t_boundry.bottom_left.lng, _util.t_boundry.bottom_left.lat, _util.t_boundry.top_right.lng, _util.t_boundry.top_right.lat],
		    filteredPids = [];

		data.forEach(function (t) {
			//  t 代表一条轨迹
			var withinTraj = false;
			var pixel_points = [],
			    latlng_points_in_tile = [];

			latlng_points = t.traj;
			if (t.pid == '460000000000000') return true; //这条轨迹的点特别多

			for (var i = 1; i < latlng_points.length; i++) {
				var a = [latlng_points[i - 1].longitude, latlng_points[i - 1].latitude],
				    b = [latlng_points[i].longitude, latlng_points[i].latitude],
				    da = [],
				    db = [],
				    res = void 0;

				res = (0, _clip.clip)(a, b, tileBox, da, db); // 1 表示经过 ， 0 表示未经过

				if (res) {
					if (a[0] == da[0] && a[1] == da[1] && i != 1) {
						// latlng_points_in_tile.push({
						// 	longitude : db[0],
						// 	latitude  : db[1]	
						// })
						pixel_points.push((0, _util._l2p)(db[1], db[0]));
					} else {
						// latlng_points_in_tile.push({
						// 	longitude : da[0],
						// 	latitude  : da[1]	
						// })
						// latlng_points_in_tile.push({
						// 	longitude : db[0],
						// 	latitude  : db[1]	
						// })

						// pixel_points.push( _l2p(da[1] , da[0]) )
						pixel_points.push((0, _util._l2p)(db[1], db[0]));
					}
				}
			}

			if (pixel_points.length) {
				// trajs_in_tile_boundry.push({
				// 	pid :  t.pid,
				// 	traj : latlng_points_in_tile
				// })
				trajs_pixelpoints.push({
					pid: t.pid,
					traj: pixel_points
				});
			} else {}
			// filteredPids.push(t.pid)


			// trajdataInTileBoundry = trajs_in_tile_boundry
		});

		stack.push({
			latlng: data,
			pixel: trajs_pixelpoints,
			filter: []
		});
	}

	// 遍历所有轨迹 找出 经过 矩形框的轨迹 
	function filter(s_bottom_left, s_top_right, stackIndex) {
		var selectedTrajs = [],
		    res = void 0,
		    tileBox = [_util.t_boundry.bottom_left.lng, _util.t_boundry.bottom_left.lat, _util.t_boundry.top_right.lng, _util.t_boundry.top_right.lat],
		    selectBox = [s_bottom_left.lng, s_bottom_left.lat, s_top_right.lng, s_top_right.lat];

		var t1 = new Date().getTime();

		//数据来源上一个 选择 filter 后的数据
		var trajdataInTileBoundry = stack[stackIndex - 1].latlng,
		    newTrajdataInTileBoundry = [],
		    filteredPids = [];

		if (!trajdataInTileBoundry) return;

		trajdataInTileBoundry.forEach(function (t) {
			var points = t.traj;
			var isClip = false;
			var latlng_points_in_tile = [];

			for (var i = 1; i < points.length; i++) {
				var a = [points[i - 1].longitude, points[i - 1].latitude],
				    b = [points[i].longitude, points[i].latitude],
				    da = [],
				    db = [],
				    _res = void 0;

				_res = (0, _clip.clip)(a, b, selectBox, da, db); // 1 表示经过 ， 0 表示未经过

				if (_res) {
					isClip = true;
					break;
				}
			}

			if (isClip) {
				var latlng_points = points,
				    pixel_points = [];

				for (var _i = 1; _i < latlng_points.length; _i++) {
					var _a = [latlng_points[_i - 1].longitude, latlng_points[_i - 1].latitude],
					    _b = [latlng_points[_i].longitude, latlng_points[_i].latitude],
					    _da = [],
					    _db = [],
					    _res2 = void 0;

					_res2 = (0, _clip.clip)(_a, _b, tileBox, _da, _db); // 1 表示经过 ， 0 表示未经过

					if (_res2) {
						if (_a[0] == _da[0] && _a[1] == _da[1] && _i != 1) {
							latlng_points_in_tile.push({
								longitude: _da[0],
								latitude: _da[1]
							});

							pixel_points.push((0, _util._l2p)(_db[1], _db[0]));
						} else {
							pixel_points.push((0, _util._l2p)(_da[1], _da[0]));
							pixel_points.push((0, _util._l2p)(_db[1], _db[0]));

							latlng_points_in_tile.push({
								longitude: _da[0],
								latitude: _da[1]
							});
							latlng_points_in_tile.push({
								longitude: _db[0],
								latitude: _db[1]
							});
						}
					}
				}

				selectedTrajs.push({
					pid: t.pid,
					traj: pixel_points
				});
				newTrajdataInTileBoundry.push({
					pid: t.pid,
					traj: latlng_points_in_tile
				});
			} else {
				filteredPids.push(t.pid);
			}
		});

		var t2 = new Date().getTime();
		console.log('process select : ' + (t2 - t1) + 'ms');

		stack[stackIndex]['latlng'] = newTrajdataInTileBoundry;
		stack[stackIndex]['pixel'] = selectedTrajs;
		stack[stackIndex]['filter'] = stack[stackIndex - 1]['filter'].concat(filteredPids);
	}

	function GetTrajsPicUrl(trajs, stackIndex) {
		if (!trajs) return null;
		if (stackIndex < 0) stackIndex = 0;

		var canvas = document.createElement('canvas');
		var ctx = canvas.getContext('2d');

		canvas.width = _util.t_width;
		canvas.height = _util.t_height;
		ctx.clearRect(0, 0, _util.t_width, _util.t_height);

		// color and opacity


		if (stackIndex == 0) {
			ctx.strokeStyle = (0, _util.color2rgb)(Config.picTrajColor, Config.picTrajOpacity);
		} else {
			ctx.strokeStyle = (0, _util.color2rgb)(Config.picTrajColor, Config.picTrajOpacity);
		}

		ctx.lineJoin = 'round';
		ctx.lineWidth = Config.picTrajLineWidth;

		console.log("轨迹条数：", trajs.length, " 线色：", ctx.strokeStyle, "  线宽：", ctx.lineWidth);
		console.log('Drawing....');
		var t3 = new Date().getTime();
		trajs.forEach(function (t) {
			drawOneCurveTraj(ctx, t.traj);
		});
		var t4 = new Date().getTime();
		console.log('__draw: ' + (t4 - t3) + 'ms');

		return canvas.toDataURL();
	}

	function drawOneTraj(ctx, points) {
		ctx.beginPath();
		var beginPoint = points[0];
		ctx.moveTo(beginPoint[0], beginPoint[1]);

		for (var i = 1; i < points.length; i++) {
			var _points$i = _slicedToArray(points[i], 2),
			    x = _points$i[0],
			    y = _points$i[1];

			ctx.lineTo(x, y);
		}

		ctx.closePath();
		ctx.stroke();
	}
	function drawOneCurveTraj(ctx, points) {
		ctx.beginPath();
		var beginPoint = points[0];
		ctx.moveTo(beginPoint[0], beginPoint[1]);

		var i = void 0;
		for (i = 1; i < points.length - 1; i++) {
			var last = points[i - 1],
			    now = points[i],
			    next = points[i + 1],
			    center = [Math.floor((now[0] + next[0]) / 2), Math.floor((now[1] + next[1]) / 2)];

			ctx.quadraticCurveTo(now[0], now[1], center[0], center[1]);
			ctx.moveTo(center[0], center[1]);
		}
		ctx.stroke();
	}
	function lineShowConfig(len) {
		var ops = [0.004, 0.1, 0.171, 0.357, 0.386, 0.42, 0.461, 0.569, 0.643, 0.735, 0.843, 0.945, 0.994];

		var width = void 0,
		    opacity = void 0;
		// if(len > 10000){
		// 	width = 1,
		// 	opacity = ops[0]
		// }else if( len > 6000){
		// 	width = 2
		// 	opacity = ops[0]
		// }else if( len > 2000){
		// 	width = 2
		// 	opacity = ops[0]
		// }else if( len > 120){
		// 	width = 3
		// 	opacity = ops[0]
		// }
		// else if( len > 50){
		// 	width = 2
		// 	opacity = ops[1]
		// }else{
		// 	width = 4
		// 	opacity = ops[1]
		// }

		width = 2;
		opacity = ops[1];

		return {
			width: width,
			opacity: opacity
		};
	}

	function shadowCanvas() {
		d3.select('#canvas-upon-map').style('opacity', 0.5);
		d3.select('#canvas-upon-map-select').style('opacity', 0.5);
	}
	function unShadowCanvas() {
		d3.select('#canvas-upon-map').style('opacity', 1);
		d3.select('#canvas-upon-map-select').style('opacity', 1);
	}

	// 变量
	exports.filter = filter;
	exports.stack = stack;

/***/ }),
/* 340 */
/***/ (function(module, exports) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});

	var _slicedToArray = function () { function sliceIterator(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"]) _i["return"](); } finally { if (_d) throw _e; } } return _arr; } return function (arr, i) { if (Array.isArray(arr)) { return arr; } else if (Symbol.iterator in Object(arr)) { return sliceIterator(arr, i); } else { throw new TypeError("Invalid attempt to destructure non-iterable instance"); } }; }();

	/*
	  参考 https://github.com/w8r/liang-barsky
	*/

	/**
	 * @preserve
	 * Fast, destructive implemetation of Liang-Barsky line clipping algorithm.
	 * It clips a 2D segment by a rectangle.
	 * @author Alexander Milevski <info@w8r.name>
	 * @license MIT
	 */

	var EPSILON = 1e-9;
	var abs = Math.abs;

	var INSIDE = 1;
	var OUTSIDE = 0;

	function clipT(num, denom, c) {
	  var _c = _slicedToArray(c, 2),
	      tE = _c[0],
	      tL = _c[1];

	  if (abs(denom) < EPSILON) return num < 0;
	  var t = num / denom;

	  if (denom > 0) {
	    if (t > tL) return 0;
	    if (t > tE) c[0] = t;
	  } else {
	    if (t < tE) return 0;
	    if (t < tL) c[1] = t;
	  }
	  return 1;
	}

	/**
	 * @param  {Array<number>} a
	 * @param  {Array<number>} b
	 * @param  {Array<number>} box [xmin, ymin, xmax, ymax]
	 * @param  {Array<number>} [da]
	 * @param  {Array<number>} [db]
	 * @return {number}
	 */
	function clip(a, b, box, da, db) {
	  var _a = _slicedToArray(a, 2),
	      x1 = _a[0],
	      y1 = _a[1];

	  var _b = _slicedToArray(b, 2),
	      x2 = _b[0],
	      y2 = _b[1];

	  var dx = x2 - x1;
	  var dy = y2 - y1;

	  if (arguments.length === 3) {
	    da = a;
	    db = b;
	  } else {
	    da[0] = a[0];
	    da[1] = a[1];
	    db[0] = b[0];
	    db[1] = b[1];
	  }

	  if (abs(dx) < EPSILON && abs(dy) < EPSILON && x1 >= box[0] && x1 <= box[2] && y1 >= box[1] && y1 <= box[3]) {
	    return INSIDE;
	  }

	  var c = [0, 1];
	  if (clipT(box[0] - x1, dx, c) && clipT(x1 - box[2], -dx, c) && clipT(box[1] - y1, dy, c) && clipT(y1 - box[3], -dy, c)) {
	    var tE = c[0],
	        tL = c[1];

	    if (tL < 1) {
	      db[0] = x1 + tL * dx;
	      db[1] = y1 + tL * dy;
	    }
	    if (tE > 0) {
	      da[0] += tE * dx;
	      da[1] += tE * dy;
	    }
	    return INSIDE;
	  }
	  return OUTSIDE;
	}

	exports.clip = clip;

/***/ }),
/* 341 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.heatpoint = exports.heatmap = exports.drawheatmap = exports.height = exports.width = exports.zoom = exports.boundry = exports.map = undefined;

	var _d = __webpack_require__(328);

	var d3 = _interopRequireWildcard(_d);

	var _pic = __webpack_require__(339);

	var _poi = __webpack_require__(342);

	var _select = __webpack_require__(344);

	var _widget = __webpack_require__(345);

	var _config = __webpack_require__(346);

	var Config = _interopRequireWildcard(_config);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	var event_queue = false;
	// import { draw as drawTraj } from './traj'

	// import $ from 'jquery';

	var visBox = document.getElementById("map-container");
	var height = visBox.offsetHeight; //高度
	var width = visBox.offsetWidth; //宽度

	// console.log(height,width)

	//1. 创建地图 、 创建绘制层
	// let tilemapservice = 'http://webrd02.is.autonavi.com/appmaptile?lang=zh_cn&size=1&scale=1&style=8&x={x}&y={y}&z={z}'
	// let tilemapservice = 'http://b.tile2.opencyclemap.org/transport/{z}/{x}/{y}.png'
	// let tilemapservice = 'http://map.geoq.cn/ArcGIS/rest/services/ChinaOnlineStreetGray/MapServer/tile/{z}/{y}/{x}'
	// let tilemapservice = 'https://maps.wikimedia.org/osm-intl/{z}/{x}/{y}@2x.png'

	var tilemapservice = 'https://api.mapbox.com/styles/v1/ykjage/cjtrcn5nj64921fp0xk3n99nt/tiles/256/{z}/{x}/{y}@2x?access_token=pk.eyJ1IjoieWtqYWdlIiwiYSI6ImNqdHF3NHhlMzBqc2Y0NGxiaTZyMmozNmMifQ.iM0sVr3-IG1fxE2vP9a4-w';
	var zoomRate = 12;
	var map = L.map('map-container').setView([28.0152686, 120.6558736], zoomRate);
	var osmLayer = L.tileLayer(tilemapservice).addTo(map);

	var heatmap = L.heatLayer([]);
	var heatpoint = 0;
	heatmap.addTo(map);

	(0, _widget.appendWidget)();

	function drawheatmap(trajsData) {
		if (trajsData.length == 0) return;
		var heatmapdata = [];
		exports.heatpoint = heatpoint = 0;
		var _iteratorNormalCompletion = true;
		var _didIteratorError = false;
		var _iteratorError = undefined;

		try {
			for (var _iterator = trajsData[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
				var traj = _step.value;
				var _iteratorNormalCompletion2 = true;
				var _didIteratorError2 = false;
				var _iteratorError2 = undefined;

				try {
					for (var _iterator2 = traj.traj[Symbol.iterator](), _step2; !(_iteratorNormalCompletion2 = (_step2 = _iterator2.next()).done); _iteratorNormalCompletion2 = true) {
						var point = _step2.value;

						heatmapdata.push([point.latitude, point.longitude]);
						exports.heatpoint = heatpoint += 1;
					}
				} catch (err) {
					_didIteratorError2 = true;
					_iteratorError2 = err;
				} finally {
					try {
						if (!_iteratorNormalCompletion2 && _iterator2.return) {
							_iterator2.return();
						}
					} finally {
						if (_didIteratorError2) {
							throw _iteratorError2;
						}
					}
				}
			}
		} catch (err) {
			_didIteratorError = true;
			_iteratorError = err;
		} finally {
			try {
				if (!_iteratorNormalCompletion && _iterator.return) {
					_iterator.return();
				}
			} finally {
				if (_didIteratorError) {
					throw _iteratorError;
				}
			}
		}

		Config.heatMapIntensity = 2 / heatpoint;
		heatmap.setLatLngs(heatmapdata);

		var v = +d3.select('#intensity-num').text() || 70;
		var max = heatpoint * Config.heatMapIntensity * v;

		var maxtext = Math.floor(heatpoint * v / 200);
		if (v == 200) {
			max = Infinity;maxtext = Infinity;
		}

		d3.select('#heatmap-legend').select('#heatmap-legend-max').text(maxtext);

		heatmap.setOptions({
			radius: 12,
			max: max,
			gradient: {
				0.25: Config.heatMapColor[0],
				0.55: Config.heatMapColor[1],
				0.75: Config.heatMapColor[2],
				1: Config.heatMapColor[3]
			}
		});
	}

	var canvas = d3.select(map.getPanes().overlayPane).append("canvas").attr('id', 'canvas-upon-map');
	d3.select(map.getPanes().overlayPane).append("canvas").attr('id', 'canvas-upon-map-select');

	var poiSvg = d3.select(map.getPanes().overlayPane).append("svg").attr('id', 'svg-poi').attr("width", width).attr("height", height);

	var selectSvg = d3.select(map.getPanes().overlayPane).append("svg").attr('id', 'svg-select').attr("width", width).attr("height", height);

	var boundry = void 0,
	    pixelBoundry = void 0,
	    zoom = void 0;

	map.on("moveend", function () {
		resize();
	});
	resize();

	function resize() {

		d3.select('#svg-poi').selectAll('*').remove();

		event_queue = true;

		exports.zoom = zoom = map.getZoom();

		exports.boundry = boundry = {
			bottom_left: map.getBounds().getSouthWest(),
			top_right: map.getBounds().getNorthEast()
		};
		pixelBoundry = {
			bottom_left: map.latLngToLayerPoint(boundry.bottom_left),
			top_right: map.latLngToLayerPoint(boundry.top_right)
			// console.log(boundry)

		};debounceResize(900)();
	}

	function drawPic() {

		// console.log('draw')

		var left = -(map.getPixelOrigin().x - map.getPixelBounds().min.x) + 'px',
		    top = -(map.getPixelOrigin().y - map.getPixelBounds().min.y) + 'px';

		// layer 偏移
		d3.select('#canvas-upon-map').style("transform", "translate(" + left + "," + top + ")");

		d3.select('#canvas-upon-map-select').style("transform", "translate(" + left + "," + top + ")");

		selectSvg.style("left", left).style("top", top);

		poiSvg.style("left", left).style("top", top);

		(0, _pic.draw)(); //绘制轨迹
		// drawTraj() //绘制勾选的轨迹
	}

	var debounceTimer = void 0;
	function debounceResize(delay) {

		var delaySecond = delay || 3000;
		return function () {
			// console.log('debounce')
			var context = this;
			clearTimeout(debounceTimer);
			debounceTimer = setTimeout(function () {

				drawPic(); // 在地图上绘制轨迹
				(0, _select.draw)(); //添加选择事件
			}, delaySecond);
		};
	}

	exports.map = map;
	exports.boundry = boundry;
	exports.zoom = zoom;
	exports.width = width;
	exports.height = height;
	exports.drawheatmap = drawheatmap;
	exports.heatmap = heatmap;
	exports.heatpoint = heatpoint;

/***/ }),
/* 342 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.draw = draw;
	exports.remove = remove;

	var _d = __webpack_require__(328);

	var d3 = _interopRequireWildcard(_d);

	var _clip = __webpack_require__(340);

	var _index = __webpack_require__(341);

	var _util = __webpack_require__(343);

	var _queryutil = __webpack_require__(333);

	var QueryUtil = _interopRequireWildcard(_queryutil);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	// 库函数 用于 判断直线是否经过矩形
	var colorList = ['#e41a1c', '#377eb8', '#4daf4a', '#984ea3', '#ff7f00'];

	//本地poi数据


	var poiData = void 0; //全局数据 ，保存


	//在地圖上绘制Poi

	var iconLayers = void 0;

	function draw(pois) {
		iconLayers = [];
		// var PoiIcon = L.icon({
		//     iconUrl: '../icon/POIicon.png',
		//     iconSize:     [21, 24], // size of the icon
		// });
		pois.forEach(function (poi) {
			var iconLayer = L.marker([poi.latitude, poi.longitude], { title: poi.name });
			iconLayer.addTo(_index.map)
			// .bindPopup(poi.name)
			// .openPopup();
			.bindTooltip('Name:' + poi.name + ', Type:' + poi.type, { direction: 'top', offset: [0, -6], opacity: 0.9 }).openTooltip();
			iconLayers.push(iconLayer);
		});

		// console.log(iconLayers)
	}

	function remove() {
		if (iconLayers != null) iconLayers.forEach(function (iconLayer) {
			iconLayer.remove();
		});
	}

	/*
	export  function draw(data) {

	 let poi = pois[0]['data'][0]['data'][0]

	 if(!ifOutofBoundry(poi.latitude, poi.longitude)  && 
	     poi.name ){
	     drawPoi(poi)
	 }
	}

	function removePoi(){
		d3.select('#svg-poi').selectAll('text').remove()
		d3.select('#poi-icon')
			.style('display','none')

	}

	function drawPoi(poi){
		let p = _l2pb(poi.latitude , poi.longitude)
		d3.select('#svg-poi')
			.append('text')
			.attr('cx',p[0])
			.attr('cy',p[1])
					.text(poi.name)
					.attr('x',p[0] + 18)
					.attr('y',p[1] + 12)
					.style('font-size','10px')
		d3.select('#poi-icon')
			.style('left',p[0] + 'px')
			.style('top', p[1] + 'px')
			.style('display','block')

	}

	function ifOutofBoundry(lat,lng){
		if(lat < boundry.bottom_left.lat || lng < boundry.bottom_left.lng ||
			lat > boundry.top_right.lat || lng > boundry.top_right.lng)
			return true   // 超出边界
		return false
	}
	*/

/***/ }),
/* 343 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.color2rgb = exports.top = exports.left = exports.t_height = exports.t_width = exports.t_boundry = exports.loadTrajsData = exports.hideLoading = exports.showLoading = exports.updateTileBoundry = exports._p2l = exports._l2pb = exports._l2p = undefined;

	var loadTrajsData = function () {
		var _ref = _asyncToGenerator( /*#__PURE__*/regeneratorRuntime.mark(function _callee() {
			var t1, data, t2;
			return regeneratorRuntime.wrap(function _callee$(_context) {
				while (1) {
					switch (_context.prev = _context.next) {
						case 0:
							t1 = new Date().getTime();

							console.log("Loading Data....");
							_context.next = 4;
							return getData();

						case 4:
							data = _context.sent;
							t2 = new Date().getTime();

							console.log('loaddata : ' + (t2 - t1) + 'ms');
							// return data.slice(0,10)
							return _context.abrupt('return', data);

						case 8:
						case 'end':
							return _context.stop();
					}
				}
			}, _callee, this);
		}));

		return function loadTrajsData() {
			return _ref.apply(this, arguments);
		};
	}();

	/*16进制颜色转为RGB格式*/


	var _d = __webpack_require__(328);

	var d3 = _interopRequireWildcard(_d);

	var _index = __webpack_require__(341);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	function _asyncToGenerator(fn) { return function () { var gen = fn.apply(this, arguments); return new Promise(function (resolve, reject) { function step(key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { return Promise.resolve(value).then(function (value) { step("next", value); }, function (err) { step("throw", err); }); } } return step("next"); }); }; } /* 数据转换
	                                                                                                                                                                                                                                                                                                                                                                                                                                                                           	l : 经纬度
	                                                                                                                                                                                                                                                                                                                                                                                                                                                                           	p : 相对于 左上角 的 像素 位置
	                                                                                                                                                                                                                                                                                                                                                                                                                                                                           	i : 矩阵 index
	                                                                                                                                                                                                                                                                                                                                                                                                                                                                           */

	var visBox = document.getElementById("map-container");
	var height = visBox.offsetHeight; //高度
	var width = visBox.offsetWidth; //宽度


	var t_boundry = void 0,
	    t_width = void 0,
	    t_height = void 0,
	    left = void 0,
	    top = void 0,
	    old_t_boundry = void 0,
	    old_zoom = void 0;

	/*
		经纬度排列
		--------->  lng
		^
		|
		|
		|
		|  lat

		pixel 排序
		--------->  x
		|
		|
		|
		|
		V  y
	*/

	// latlng -> pixel position
	//使用  t_boundry  大的瓦片 boundry
	// 根据 boundryPixel 等比例转换 相对于左上角的位置


	function _l2p(lat, lng) {
		var lngWidth = t_boundry.top_right.lng - t_boundry.bottom_left.lng,
		    latHeight = t_boundry.top_right.lat - t_boundry.bottom_left.lat,
		    widthPixel = t_width,
		    heightPixel = t_height;

		if (lat > t_boundry.top_right.lat || lat < t_boundry.bottom_left.lat || lng > t_boundry.top_right.lng || lng < t_boundry.bottom_left.lng) //超出视窗
			return [-1, -1];

		var x = Math.round((lng - t_boundry.bottom_left.lng) / lngWidth * widthPixel),
		    y = Math.round((t_boundry.top_right.lat - lat) / latHeight * heightPixel);

		// console.log(x,y)
		return [x, y];
	}

	// 与上个函不能互相转换 
	// 使用 v_boundry 视窗的边界
	function _p2l(x, y) {
		var v_boundry = _index.boundry;

		var lngWidth = v_boundry.top_right.lng - v_boundry.bottom_left.lng,
		    latHeight = v_boundry.top_right.lat - v_boundry.bottom_left.lat,
		    widthPixel = width,
		    heightPixel = height;

		if (x > width || x < 0 || y > height || y < 0) return { lng: -1, lat: -1 };

		var lng = x / widthPixel * lngWidth + v_boundry.bottom_left.lng,
		    lat = v_boundry.top_right.lat - y / height * latHeight;

		// 0.00001 => 赤道长 1米
		lng = lng.toFixed(5);
		lat = lat.toFixed(5);

		return {
			lng: lng,
			lat: lat
		};
	}

	// 相对视图边界的像素坐标
	function _l2pb(lat, lng) {
		lat = parseFloat(lat);
		lng = parseFloat(lng);

		var lngWidth = _index.boundry.top_right.lng - _index.boundry.bottom_left.lng,
		    latHeight = _index.boundry.top_right.lat - _index.boundry.bottom_left.lat,
		    widthPixel = width,
		    heightPixel = height;

		var re = [0, 0];

		// if( lat > boundry.top_right.lat || lat < boundry.bottom_left.lat 
		// 	|| lng > boundry.top_right.lng || lng < boundry.bottom_left.lng)  //超出视窗
		// 	return [-1,-1]

		if (lat >= _index.boundry.top_right.lat || lat <= _index.boundry.bottom_left.lat) {
			if (lat >= _index.boundry.top_right.lat) re[1] = 0;
			if (lat <= _index.boundry.bottom_left.lat) re[1] = heightPixel;
		} else {
			re[1] = Math.round((_index.boundry.top_right.lat - lat) / latHeight * heightPixel);
		}

		if (lng >= _index.boundry.top_right.lng || lng <= _index.boundry.bottom_left.lng) {
			if (lng >= _index.boundry.top_right.lng) re[0] = widthPixel;
			if (lng <= _index.boundry.bottom_left.lng) re[0] = 0;
		} else {
			re[0] = Math.round((lng - _index.boundry.bottom_left.lng) / lngWidth * widthPixel);
		}

		return re;
		// return [x,y]
	}

	/* 经纬度转成瓦片编号（左上角）

		tile 序号排列
		--------->  x
		|
		|
		|
		|
		v  y

	*/

	function _l2t(lon, lat) {
		var x = long2tilex(lon, _index.zoom),
		    y = lat2tiley(lat, _index.zoom);

		return [x, y];
	}
	function long2tilex(lon) {
		var z = _index.zoom;
		return Math.floor((lon + 180) / 360 * Math.pow(2, z));
	}
	function lat2tiley(lat) {
		var z = _index.zoom;
		return Math.floor((1 - Math.log(Math.tan(lat * Math.PI / 180) + 1 / Math.cos(lat * Math.PI / 180)) / Math.PI) / 2 * Math.pow(2, z));
	}

	function tile2long(x) {
		var z = _index.zoom;
		return x / Math.pow(2, z) * 360 - 180;
	}
	function tile2lat(y) {
		var z = _index.zoom;
		var n = Math.PI - 2 * Math.PI * y / Math.pow(2, z);return 180 / Math.PI * Math.atan(0.5 * (Math.exp(n) - Math.exp(-n)));
	}

	// 获取 瓦片视图上的 位置
	// 每当 boundry 变化需要更新
	function updateTileBoundry() {
		var v_boundry = _index.boundry,
		    v_number = void 0;
		var ic = isChanged(v_boundry);

		if (ic) {

			v_number = {
				top_right: {
					x: long2tilex(v_boundry.top_right.lng) + 2,
					y: lat2tiley(v_boundry.top_right.lat) - 1
				},
				bottom_left: {
					x: long2tilex(v_boundry.bottom_left.lng) - 1,
					y: lat2tiley(v_boundry.bottom_left.lat) + 2
				}
			};
			exports.t_boundry = t_boundry = {
				top_right: {
					lng: tile2long(v_number.top_right.x),
					lat: tile2lat(v_number.top_right.y)
				},
				bottom_left: {
					lng: tile2long(v_number.bottom_left.x),
					lat: tile2lat(v_number.bottom_left.y)
				}
			};

			old_t_boundry = t_boundry;
		}

		exports.t_width = t_width = Math.floor(width * (t_boundry.top_right.lng - t_boundry.bottom_left.lng) / (v_boundry.top_right.lng - v_boundry.bottom_left.lng));
		exports.t_height = t_height = Math.floor(height * (t_boundry.top_right.lat - t_boundry.bottom_left.lat) / (v_boundry.top_right.lat - v_boundry.bottom_left.lat));

		exports.left = left = Math.floor(t_width * (v_boundry.bottom_left.lng - t_boundry.bottom_left.lng) / (t_boundry.top_right.lng - t_boundry.bottom_left.lng));
		exports.top = top = Math.floor(t_height * (t_boundry.top_right.lat - v_boundry.top_right.lat) / (t_boundry.top_right.lat - t_boundry.bottom_left.lat));

		// console.log(v_number, v_boundry, t_boundry,width,height,t_width,t_height,left,top)


		return ic;
	}

	function isChanged(new_t_boundrt) {
		var old_t_boundry = t_boundry;

		if (!old_zoom || old_zoom != _index.zoom) {
			old_zoom = _index.zoom;
			return true;
		}

		if (old_t_boundry && new_t_boundrt.top_right.lng <= old_t_boundry.top_right.lng && new_t_boundrt.top_right.lat <= old_t_boundry.top_right.lat && new_t_boundrt.bottom_left.lng >= old_t_boundry.bottom_left.lng && new_t_boundrt.bottom_left.lat >= old_t_boundry.bottom_left.lat) return false;
		return true;
	}

	// loading 效果
	var loading = void 0;

	function showLoading() {
		if (!loading) loading = d3.select('#loading');
		loading.style("display", 'block');
	}

	function hideLoading() {
		loading.style("display", 'none');
	}

	//  data
	function getData(data) {
		var url = "http://localhost:3000/" + 'trajdata';
		return fetch(url, {
			body: JSON.stringify(data), // must match 'Content-Type' header
			cache: 'no-cache', // *default, no-cache, reload, force-cache, only-if-cached
			credentials: 'same-origin', // include, same-origin, *omit
			headers: {
				'user-agent': 'Mozilla/4.0 MDN Example',
				'content-type': 'application/json'
			},
			method: 'GET', // *GET, POST, PUT, DELETE, etc.
			mode: 'cors', // no-cors, cors, *same-origin
			redirect: 'follow', // manual, *follow, error
			referrer: 'no-referrer' // *client, no-referrer
		}).then(function (response) {
			return response.json();
		}); // parses response to JSON
	}

	function color2rgb(hexoColor, opacity) {
		var reg = /^#([0-9a-fA-f]{3}|[0-9a-fA-f]{6})$/;
		var sColor = hexoColor.toLowerCase();
		if (sColor && reg.test(sColor)) {
			if (sColor.length === 4) {
				var sColorNew = "#";
				for (var i = 1; i < 4; i += 1) {
					sColorNew += sColor.slice(i, i + 1).concat(sColor.slice(i, i + 1));
				}
				sColor = sColorNew;
			}
			//处理六位的颜色值  
			var sColorChange = [];
			for (var i = 1; i < 7; i += 2) {
				sColorChange.push(parseInt("0x" + sColor.slice(i, i + 2)));
			}

			//转换为rgba,透明度为传递的参数x；

			return "rgba(" + sColorChange.join(",") + "," + opacity + ")";

			//转换为rgb，此时不用传递参数      

			//return "rgb(" + sColorChange.join(",") + ")";   
		} else {
			return sColor;
		}
	};

	exports._l2p = _l2p;
	exports._l2pb = _l2pb;
	exports._p2l = _p2l;
	exports.updateTileBoundry = updateTileBoundry;
	exports.showLoading = showLoading;
	exports.hideLoading = hideLoading;
	exports.loadTrajsData = loadTrajsData;
	exports.t_boundry = t_boundry;
	exports.t_width = t_width;
	exports.t_height = t_height;
	exports.left = left;
	exports.top = top;
	exports.color2rgb = color2rgb;

/***/ }),
/* 344 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.draw = draw;
	exports.addSelect = addSelect;

	var _d = __webpack_require__(328);

	var d3 = _interopRequireWildcard(_d);

	var _index = __webpack_require__(341);

	var _util = __webpack_require__(343);

	var _pic = __webpack_require__(339);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	var svg = void 0;

	/*
	结构
	.leaflet-pane .leaflet-overlay-pane
		canvas #canvas-upon-map
		canvas #canvas-upon-map-select
		svg #svg-select
			g .rect-group
				rect .selection      //名字可能会被重名  被其他 css 修改掉
				g .del-icon
			g .rect-group
			g .rect-group
		svg #svg-poi
	*/

	function draw() {
		addSelect();

		if (_pic.stack.length >= 2) {
			// 删除原有的
			d3.select('#svg-select').selectAll('.rect-group').remove();

			// 创建新的
			for (var i = 1; i < _pic.stack.length; i++) {
				var _stack$i$rect = _pic.stack[i].rect,
				    top_right = _stack$i$rect.top_right,
				    bottom_left = _stack$i$rect.bottom_left;
				//经纬度转像素

				var p_top_right = (0, _util._l2pb)(top_right.lat, top_right.lng),
				    p_bottom_left = (0, _util._l2pb)(bottom_left.lat, bottom_left.lng),
				    p_top_left = {
					x: p_bottom_left[0],
					y: p_top_right[1]
				},
				    width = p_top_right[0] - p_bottom_left[0],
				    height = p_bottom_left[1] - p_top_right[1];
				console.log(p_top_left, width, height);

				svg.append('g').attr('class', 'rect-group').append("rect").attr('class', 'selection').attr('x', p_top_left.x).attr('y', p_top_left.y).attr('width', width).attr('height', height);
				addRectListener(i);
			}
		}
	}

	function selectFunctionEventHandler() {
		d3.select('#svg-poi').style('display', 'none');
		// d3.select('#svg-poi').remove()

		svg = d3.select('#svg-select');
		svg.style('cursor', 'crosshair');
		_index.map.dragging.disable();
		addSelectRect();
	}

	function addSelect() {

		// 筛选按钮
		var btn = document.getElementById('select-btn');

		// 考虑重复添加事件
		btn.removeEventListener('click', selectFunctionEventHandler);
		btn.addEventListener('click', selectFunctionEventHandler);
	}

	// 创建选择框 
	function addSelectRect() {
		var id = _pic.stack.length;
		svg.on("mousedown", function () {
			var v_p = d3.mouse(this);
			svg.append('g').attr('class', 'rect-group').append("rect").attr('class', 'selection').attr('x', v_p[0]).attr('y', v_p[1]).attr('rx', 3).attr('ry', 3).attr('width', 3).attr('height', 3);
		});

		// 选择框 移动
		svg.on("mousemove", function () {
			var s = d3.select(".rect-group:nth-child( " + id + ")").select('.selection');
			if (s.empty()) return;

			var p = d3.mouse(this);

			var d = {
				x: parseInt(s.attr("x"), 10),
				y: parseInt(s.attr("y"), 10),
				width: parseInt(s.attr("width"), 10),
				height: parseInt(s.attr("height"), 10)
			};
			var move = {
				x: p[0] - d.x,
				y: p[1] - d.y
			};

			if (move.x < 1 || move.x * 2 < d.width) {
				d.x = p[0];
				d.width -= move.x;
			} else {
				d.width = move.x;
			}

			if (move.y < 1 || move.y * 2 < d.height) {
				d.y = p[1];
				d.height -= move.y;
			} else {
				d.height = move.y;
			}

			s.attr("x", d.x).attr("y", d.y).attr("width", d.width).attr("height", d.height);
		});

		//释放筛选 
		svg.on("mouseup", function () {
			svg.on("mousemove", null);
			svg.on("mousedown", null);
			svg.on("mouseup", null);

			addRectListener();
			svg.style('cursor', 'grab');
			_index.map.dragging.enable();
			addSelection();

			d3.select('#svg-poi').style('display', 'block');
		});
	}

	function addRectListener(index) {

		var id = index ? index : _pic.stack.length; // 指定index 表示更新后重绘的 ,stack length 表示为后续加上的

		var g = d3.select(".rect-group:nth-child( " + id + ")"),
		    r = g.select('.selection'),
		    p = {
			x: +r.attr('x') + +r.attr('width'),
			y: +r.attr('y')
		},
		    w = 20,
		    q = 3,
		    l = 5;

		r.on('mouseenter', function () {
			svg.style('cursor', 'auto');

			var del = g.append('g').attr('class', 'del-icon').attr('stroke', 'black');

			// 叉叉 造型
			del.append('circle').attr('cx', p.x).attr('cy', p.y).attr('r', 10).attr('fill', 'none');

			del.append('line').attr('x1', p.x - l).attr('y1', p.y + l).attr('x2', p.x + l).attr('y2', p.y - l);

			del.append('line').attr('x1', p.x - l).attr('y1', p.y - l).attr('x2', p.x + l).attr('y2', p.y + l);
		});

		r.on('mousemove', function () {
			var v_p = d3.mouse(this),
			    x = v_p[0],
			    y = v_p[1];

			if (x > p.x - w && x < p.x && y > p.y && y < p.y + w) {
				svg.style('cursor', 'pointer');
			} else {
				svg.style('cursor', 'auto');
			}
		});

		// 点击关闭按钮
		r.on('mousedown', function () {
			var v_p = d3.mouse(this),
			    x = v_p[0],
			    y = v_p[1];

			if (x > p.x - w && x < p.x && y > p.y && y < p.y + w) {
				svg.style('cursor', 'auto');

				var deleteId = 0;
				d3.selectAll('.rect-group').each(function (d, i) {

					if (this == r.node().parentNode) {
						d3.select(this).remove();
						deleteId = i + 1;
					}
				});

				// 遍历，重新获取编号 id
				// console.log('旧id: ',id ,)
				removeSelection(deleteId);
			}
		});

		r.on('mouseleave', function () {
			g.select('.del-icon').remove();
		});
	}

	function removeSelection(i) {
		console.log(_pic.stack, i);
		if (i == _pic.stack.length - 1) {
			//最后一个
			_pic.stack.pop();
		} else {
			var newStak = [];

			for (var j = 1; j < _pic.stack.length; j++) {
				if (j != i) {
					newStak.push({
						rect: _pic.stack[j].rect
					});
				}
				_pic.stack[j] = null;
			}

			for (var _j = 1; _j < _pic.stack.length - 1; _j++) {
				_pic.stack[_j] = newStak[_j - 1];
			}

			// console.log(stack)
			_pic.stack.length = _pic.stack.length - 1;
			// console.log(stack)
			// stack = stack.slice(0,1).concat(newStak)   // stack undefined ??? 不能改变stack地址
		}

		if (_pic.stack.length == 1) {
			var selectColorArea = d3.select('#map-view').select('#select-area').select('.color-area-select');
			selectColorArea.attr('class', 'color-area-select disable');
			selectColorArea.select('input').attr('disabled', true);
		}

		(0, _pic.draw)();
	}

	function addSelection() {

		var id = _pic.stack.length;
		var s = d3.select(".rect-group:nth-child( " + id + ")").select('.selection');

		var s_top_right = (0, _util._p2l)(+s.attr('x') + +s.attr('width'), s.attr('y')),
		    s_bottom_left = (0, _util._p2l)(s.attr('x'), +s.attr('y') + +s.attr('height'));

		console.log(s_top_right, +s.attr('x') + +s.attr('width'), +s.attr('y'));
		console.log(s_bottom_left, +s.attr('x'), +s.attr('y') + +s.attr('height'));

		_pic.stack.push({
			rect: {
				top_right: s_top_right,
				bottom_left: s_bottom_left
			}
		});
		(0, _pic.draw)();

		var selectColorArea = d3.select('#map-view').select('#select-area').select('.color-area-select');
		selectColorArea.attr('class', 'color-area-select');
		selectColorArea.select('input').attr('disabled', null);
	}

/***/ }),
/* 345 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.appendWidget = appendWidget;

	var _d = __webpack_require__(328);

	var d3 = _interopRequireWildcard(_d);

	var _config = __webpack_require__(346);

	var Config = _interopRequireWildcard(_config);

	var _pic = __webpack_require__(339);

	var _index = __webpack_require__(341);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	function colorPickHander() {
		var color = d3.event.target.value;
		Config.picTrajColor = color;
		Config.PicUpdateFlag = true;
		(0, _pic.draw)();
	}

	// function selectColorPickHander(){
	// 	let color = d3.event.target.value
	// 	Config.picTrajColor = color
	// 	Config.PicUpdateFlag = true
	// 	draw()
	// }
	function opacityChange() {
		// console.log(d3.event.target.value)
		var i = d3.event.target.value;
		Config.picTrajOpacity = Config.picTrajOpacitys[i];
		Config.PicUpdateFlag = true;
		(0, _pic.draw)();
	}

	function bindColorPickEvent() {
		var colorInput = d3.select('#map-view').select('#select-area').select('.color-area').select('input');
		colorInput.on('change', colorPickHander);
		colorInput.attr('value', Config.picTrajColor);
	}
	// function bindSelectColorPickEvent(){
	// 	let selectColorInput = d3.select('#map-view').select('#select-area').select('.color-area-select').select('input')
	// 	selectColorInput.on('change',selectColorPickHander)
	// 	selectColorInput.attr('value',Config.picTrajColor)
	// 					.attr('disabled',true)
	// d3.select('#map-view').select('#select-area').select('.color-area-select')
	// 			.attr('class','color-area-select disable')
	// }


	function bindOpacityChangeEvent() {
		$("#trajslider").slider().on("slide", debounceOpacityChange(500)).slider("option", "min", 0).slider("option", "max", 10).slider("option", "step", 1).slider("value", 6);

		var opa = Config.picTrajOpacity;
		var opacityText = opa === Math.floor(opa) ? opa : opa.toFixed(2);
		d3.select('#opacity-num').text(opacityText);

		d3.select("#trajslider").select("span").style("width", "8px").style("margin-left", "-4px").style("height", "17px");
	}

	function bindIntensityChangeEvent() {
		$("#heatmapslider").slider().on("slide", function (event, ui) {
			var v = ui.value;
			d3.select('#intensity-num').text(v);
			// heatmap.setOptions({radius: 10,max: heatpoint*0.00035})

			var max = void 0,
			    maxtext = void 0;
			if (ui.value == 200) {
				_index.heatmap.setOptions({ radius: 12, max: _index.heatpoint * 5 * ui.value });
				max = Infinity;
				maxtext = Infinity;
			} else {
				max = _index.heatpoint * Config.heatMapIntensity * ui.value;
				max = max.toFixed(2);
				_index.heatmap.setOptions({ radius: 12, max: max });
				maxtext = Math.floor(_index.heatpoint / 200 * v);
			}
			d3.select('#heatmap-legend').select('#heatmap-legend-max').text(maxtext);
		}).slider("option", "min", 1).slider("option", "max", 200).slider("option", "step", 1).slider("value", 70);

		d3.select('#intensity-num').text(70);

		d3.select("#heatmapslider").select("span").style("width", "8px").style("margin-left", "-4px").style("height", "17px");
	}

	function initHeatMapLegend() {
		var rects = d3.select('#heatmap-legend').selectAll('.color_bar');

		rects.each(function (d, i) {
			d3.select(this).style('background-color', Config.heatMapColor[i]);
		});

		var min = d3.select('#heatmap-legend').select('#heatmap-legend-min').text(0);
	}
	// function bindOpacityChangeEvent(){
	// 	let colorInput = d3.select('#map-view').select('#select-area').select('.range-area').select('input')
	// 	colorInput.on('change',opacityChange)
	// }


	function appendWidget() {
		bindColorPickEvent();
		bindOpacityChangeEvent();
		// bindSelectColorPickEvent()
		bindIntensityChangeEvent();
		initHeatMapLegend();
	}

	// 防止短时内 频繁调用 耗时函数
	var debounceTimer = void 0;
	function debounceOpacityChange(delay) {

		var delaySecond = delay || 2000;
		return function (event, ui) {
			// console.log('debounce')
			var context = this;
			clearTimeout(debounceTimer);

			var i = ui.value;
			Config.picTrajOpacity = Config.picTrajOpacitys[i];

			var opa = Config.picTrajOpacity;
			var opacityText = opa === Math.floor(opa) ? opa : opa.toFixed(2);
			d3.select('#opacity-num').text(opacityText);
			Config.PicUpdateFlag = true;
			debounceTimer = setTimeout(function () {
				console.log('draw opacity');
				(0, _pic.draw)();
			}, delaySecond);
		};
	}

/***/ }),
/* 346 */
/***/ (function(module, exports) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});


	//  Hover 时绘制的 一条轨迹 
	var oneWholeTrajColor = exports.oneWholeTrajColor = 'red';

	var oneSectionTrajColor = exports.oneSectionTrajColor = 'black';

	// 轨迹动效时间  画整条的时间
	var duration = exports.duration = 3000;

	var PicUpdateFlag = exports.PicUpdateFlag = true;
	var picTrajColor = exports.picTrajColor = '#4575b4';

	// export let picTrajSelectColor = '#D73027' 

	var heatMapColor = exports.heatMapColor = ['rgb(253, 212, 158)', 'rgb(253, 187, 132)', 'rgb(252, 141, 89)', 'rgb(179, 0, 0)'];

	var heatMapIntensity = exports.heatMapIntensity = 0.00005;

	var oneWholeTrajOuterColor = exports.oneWholeTrajOuterColor = 'rgb(121, 157, 189)';
	var oneWholeTrajinnerColor = exports.oneWholeTrajinnerColor = 'rgb(46, 117, 182)';

	var oneWholeTrajOuterWidth = exports.oneWholeTrajOuterWidth = 6;
	var oneWholeTrajOuterOpacity = exports.oneWholeTrajOuterOpacity = 0.5;
	var oneWholeTrajinnerWidth = exports.oneWholeTrajinnerWidth = 1;

	// export let picTrajOpacitys = [0.004, 0.5 ,0.1 , 0.171,0.357,0.386,0.42, 0.461,0.569,0.643,0.735,0.843,0.945,0.994]
	var picTrajOpacitys = exports.picTrajOpacitys = [0.004, 0.02, 0.04, 0.1, 0.171, 0.26, 0.54, 0.7, 0.9, 1];
	var picTrajLineWidths = exports.picTrajLineWidths = [0.5, 1, 2];

	var picTrajOpacity = exports.picTrajOpacity = picTrajOpacitys[6];
	var picTrajLineWidth = exports.picTrajLineWidth = picTrajLineWidths[1];

/***/ }),
/* 347 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.bindTimeChangeEvent = bindTimeChangeEvent;

	var _d = __webpack_require__(328);

	var d3 = _interopRequireWildcard(_d);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	function bindTimeChangeEvent() {

		d3.selectAll('.timeconstraints').selectAll('input').on('input', function () {
			clearWarning(d3.event.target);
		});

		d3.selectAll('.timeconstraints').selectAll('input').on('change', function () {
			var nodeId = d3.event.target.parentNode.parentNode.parentNode.id,
			    //condition_node1
			timeType = d3.event.target.className.split(' ')[0],
			    // starttime textinput
			timeValue = d3.event.target.value;
			if (!testTime(timeValue)) {
				showWarning(d3.event.target);
			} else {

				//  修改时间
			}
		});
	}

	function showWarning(el) {
		console.log(el);
		d3.select(el).style('border-color', 'red');
	}
	function clearWarning(el) {
		d3.select(el).style('border-color', 'rgb(171, 171, 171)');
	}

	// 测试日期是否符合以下格式 
	function testTime(time) {
		var currentFormat = "YYYY.MM.dd HH:mm"; //输入常见日期格式

		currentFormat = currentFormat //优先替换特殊字符，因为后面替换的正则表达式中包含特殊字符
		.replace(/\s/ig, "\\s").replace(/\//ig, "\\/").replace(/\\/ig, "\\").replace(/\./ig, "\\.").replace(/\-/ig, "\\-").replace(/yyyy/ig, "[1-9]\\d{3}") //年份
		.replace(/yy/ig, "\\d{2}") //年份
		.replace(/HH/, "((0[1-9])|([1-9])|(1\\d)|(2[0-4]))") //小时
		.replace(/MM/, "((0[1-9])|(1[0-2])|\\d)") //月份
		.replace(/dd/, "((0[1-9])|([1-2]\\d)|(3[0-1]))").replace(/mm/, "([0-5]\\d)") //分钟
		.replace(/ss/, "([0-5]\\d|\\d)") //秒钟
		.replace(/SSS/, "\\d{1,3}"); //毫秒

		currentFormat = "^" + currentFormat + "$"; //"^{0}$".format(currentFormat);
		var res = new RegExp(currentFormat).test(time); //测试输入值
		console.log(res);
		return res;
	}

/***/ }),
/* 348 */
/***/ (function(module, exports, __webpack_require__) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	    value: true
	});

	var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function (obj) { return typeof obj; } : function (obj) { return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj; };

	exports.word_tab_start = word_tab_start;
	exports.word_tab_move = word_tab_move;
	exports.word_tab_end = word_tab_end;
	exports.change_time = change_time;
	exports.change_cur_time = change_cur_time;
	exports.merge_time_tab = merge_time_tab;
	exports.change_tab = change_tab;

	var _jquery = __webpack_require__(327);

	var _jquery2 = _interopRequireDefault(_jquery);

	var _searchbar = __webpack_require__(332);

	function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

	function word_tab_start() {
	    //d3.select(this.parentNode).style('position','absolute').style('z-index',10000)
	}
	function word_tab_move() {
	    var left = (0, _jquery2.default)(this.parentNode.parentNode)[0].getBoundingClientRect().left + (0, _jquery2.default)(this.parentNode.parentNode.parentNode)[0].getBoundingClientRect().left;
	    var top = (0, _jquery2.default)(this.parentNode.parentNode)[0].getBoundingClientRect().top + (0, _jquery2.default)(this.parentNode.parentNode.parentNode)[0].getBoundingClientRect().top;
	    if (d3.event.sourceEvent.pageX - left > 10 || d3.event.sourceEvent.pageY - top > 10) d3.select(this.parentNode).style('position', 'absolute').style('z-index', 10000).style('left', d3.event.sourceEvent.pageX - left + 'px').style('top', d3.event.sourceEvent.pageY - top + 'px');
	}

	function word_tab_end() {
	    var x = parseInt(d3.select(this.parentNode).style('left'));
	    var y = parseInt(d3.select(this.parentNode).style('top'));
	    var word = d3.select(this).select('.tab-text').attr('value');
	    var left_length = (0, _jquery2.default)('#Specification_view').scrollLeft();
	    d3.selectAll('.condition_node').each(function () {
	        var current_left = parseInt(d3.select(this).style('left')) - left_length;
	        var current_right = current_left + parseInt(d3.select(this).style('width'));
	        var current_top = (0, _jquery2.default)(this)[0].getBoundingClientRect().top;
	        if (x >= current_left && x <= current_right && y >= current_top) Add_word(d3.select(this).attr('id'), word);
	    });
	    d3.select(this.parentNode).style('position', 'static').style('z-index', 0).style('left', 'auto') //$(this.parentNode)[0].getBoundingClientRect().left)
	    .style('top', 'auto'); //$(this.parentNode)[0].getBoundingClientRect().top)
	}

	function Add_word(node_id, word) {
	    var nodelist = __webpack_require__(336);
	    var data_order = 0;
	    var node_index = parseInt(node_id.substr(node_id.length - 1, 1));
	    //如果有的话 把该搜索词的标签删掉

	    var _loop = function _loop(i) {
	        var titles = nodelist.data[i].name.split('_');
	        if (titles.indexOf(word) != -1) {
	            titles.splice(titles.indexOf(word), 1);
	            if (titles.length == 0) {
	                data_order = i;
	                var delete_node_id = nodelist.data[i].order; //搜索词原来的标签id的order部分
	                if (node_index == delete_node_id) return {
	                        v: void 0
	                    };
	                if (node_index > delete_node_id) node_index--;
	                nodelist.delete_node_byOrder(delete_node_id);
	            } else {
	                if (titles.length != 1) {
	                    (0, _searchbar.getMerge_data)(titles.join('_')).then(function (merge_data) {
	                        merge_data.order = nodelist.data[i].order;
	                        nodelist.data[i] = merge_data;
	                        nodelist.node_rendering(merge_data, nodelist.data[i].order);
	                    });
	                } else {
	                    var _data = (0, _searchbar.get_data)(titles[0]);
	                    _data.order = nodelist.data[i].order;
	                    nodelist.data[i] = _data;
	                    nodelist.node_rendering(_data, nodelist.data[i].order);
	                }
	            }
	        }
	    };

	    for (var i = 0; i < nodelist.data.length; i++) {
	        var _ret = _loop(i);

	        if ((typeof _ret === "undefined" ? "undefined" : _typeof(_ret)) === "object") return _ret.v;
	    }
	    //得到该搜索词的data覆盖原卡片数据
	    for (var i = 0; i < nodelist.data.length; i++) {
	        if (nodelist.data[i].order == node_index) {
	            if (nodelist.data[i].name == '') {
	                var data = (0, _searchbar.get_data)(word);
	                data.order = node_index;
	                nodelist.data[node_index - 1] = data;
	                nodelist.node_rendering(data, node_index);
	            } else {
	                (0, _searchbar.getMerge_data)(nodelist.data[i].name + "_" + word).then(function (merge_data) {
	                    merge_data.order = node_index;
	                    nodelist.data[node_index - 1] = merge_data;
	                    nodelist.node_rendering(merge_data, node_index);
	                });
	            }
	            break;
	        }
	    }
	}

	function change_time(operate, time) {
	    //operate:add,delete
	    if (operate == 'delete') delete_time(time);else add_time(time);
	    change_tag_time();
	}
	function delete_time() {

	    var nodelist = __webpack_require__(336);
	    if (nodelist.time[1]['y'] != '' || nodelist.time[1]['month'] != '' || nodelist.time[1]['d'] != '') nodelist.time[1] = { 'y': '', 'month': '', 'd': '', 'o': '', 'm': '' };else nodelist.time[0] = { 'y': '', 'month': '', 'd': '', 'o': '', 'm': '' };
	}
	function add_time(time) {
	    var nodelist = __webpack_require__(336);
	    var word_time_map = { '年': 'y', '月': 'month', '日': 'd', '时': 'o', '分': 'm' };
	    var old_time = nodelist.time;
	    var time_type = word_time_map[time[time.length - 1]];
	    var num = time.substr(0, time.length - 1);
	    if (num.length == 1) num = '0' + num;
	    if (old_time[0].hasOwnProperty(time_type) && old_time[0][time_type] == '') //起始时间不包含该时间信息
	        old_time[0][time_type] = num;else old_time[1][time_type] = num;
	}
	//为所有标签添加新的时间约束
	function change_tag_time() {
	    var str_time = get_time_number();
	    d3.selectAll('.starttime').property('value', str_time[0]).each(function () {
	        d3.select(this.parentNode).select('.textcon').select('.text').text(get_time_word());
	    });
	    d3.selectAll('.endtime').property('value', str_time[1]);
	}
	//为当前标签添加时间约束
	function change_cur_time() {
	    var str_time = get_time_number();
	    d3.select(this).select('.starttime').property('value', str_time[0]).each(function () {
	        d3.select(this.parentNode).select('.textcon').select('.text').text(get_time_word());
	    });
	    d3.select(this).select('.endtime').property('value', str_time[1]);
	}
	//将时间词合并并更新时间约束
	function merge_time_tab() {
	    var order = ['年', '月', '日'];
	    var key = ['y', 'month', 'd'];
	    var time_value = '';
	    var nodelist = __webpack_require__(336);
	    key.forEach(function (d, index) {
	        if (nodelist.time[0].hasOwnProperty(d) && nodelist.time[0][d] != '') time_value += nodelist.time[0][d] + order[index];
	    });
	    var merge = false;
	    d3.select('.search-container').selectAll('.word-tab').each(function () {
	        var word_type = d3.select(this).select('.tab-image-container').select('img').attr('word_type');
	        var v = d3.select(this).select('.tab-text-container').select('input').attr('value');
	        if (word_type == 't') {
	            if (!merge) {
	                d3.select(this).select('.tab-text-container').select('input').attr('value', time_value).style('width', time_value.length * 10 + "px");
	                _searchbar.textData.forEach(function (d) {
	                    if (d[0] == v) d[0] = time_value;
	                });
	                merge = true;
	            } else {
	                d3.select(this).remove();
	                _searchbar.textData.forEach(function (d, index) {
	                    if (d[0] == v) _searchbar.textData.splice(index, 1);
	                });
	            }
	        }
	    });
	}
	//用户修改搜索词后对tab进行修改，对应的标签更新（若新词无标签则原标签删除),若原搜索词无对应标签且新词为n，则建立新的标签
	function change_tab(old_name, new_name) {
	    var find = false;
	    _searchbar.textData.forEach(function (d, index) {
	        if (d[0] == old_name && !find) {
	            var nodelist = __webpack_require__(336);
	            if (new_name == '') //删除该搜索词并删除标签
	                {
	                    _searchbar.textData.splice(index, 1);
	                    (0, _searchbar.addParticle)(new_name);
	                    nodelist.delete_node_byName(old_name);
	                } else {
	                //更新标签
	                d[0] = new_name;
	                var se_find = false;
	                nodelist.data.forEach(function (d, index) {
	                    if (d.name == old_name && !se_find) {
	                        d.name = new_name;
	                        (0, _searchbar.addParticle)(new_name, [index, d.order]);
	                        se_find = true;
	                    }
	                });
	                if (!se_find) {
	                    nodelist.delete_node_byName(old_name);
	                    (0, _searchbar.addParticle)(new_name);
	                }
	            }
	            find = true;
	            //为新word创建标签卡
	        }
	    });
	}
	//得到数字形式的时间约束
	function get_time_number() {
	    var nodelist = __webpack_require__(336);
	    var year_order = ['y', 'month', 'd'];
	    var str_time = ['', ''];
	    nodelist.time.forEach(function (time, index) {
	        year_order.forEach(function (t) {
	            if (time.hasOwnProperty(t) && time[t] != '') {
	                str_time[index] += time[t];
	                if (t != 'd') str_time[index] += '.';
	            }
	        });
	        str_time[index] += ' ';
	        if (str_time[1] == '') str_time[1] = str_time[0].split(' ')[0] + ' ';
	        if (time.hasOwnProperty('o') && time['o'] != '') {
	            str_time[index] += time['o'] + ':';
	            if (time.hasOwnProperty('m') && time['m'] != '') str_time[index] += time['m'];else str_time[index] += '00';
	        } else {
	            if (str_time[0].split(' ')[0] != '') {
	                if (index == 0) str_time[index] += '00:00';else str_time[index] += '23:59';
	            }
	        }
	    });
	    return str_time;
	}
	//得到文字形式的时间约束
	function get_time_word() {
	    var nodelist = __webpack_require__(336);
	    var time_w = '';
	    if (nodelist.time[0]['y'] != '') time_w += nodelist.time[0]['y'] + "年";
	    if (nodelist.time[0]['month'] != '') time_w += nodelist.time[0]['month'] + "月";
	    if (nodelist.time[0]['d'] != '') time_w += nodelist.time[0]['d'] + "日";
	    return time_w;
	}

/***/ }),
/* 349 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.initial_siteScore = initial_siteScore;
	exports.max_value_POI = max_value_POI;

	//sitescore用于POI的val的更新，随参数调整而变化
	function initial_siteScore(initial_data) {
	  var alpha = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 1;
	  var param = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : false;

	  //若alpha=1则说明基于nodelist更新sitescore，反之则是仅对当前标签的数据进行poi中val的更新(simT和alpha)，然后更新sitescore.
	  //param为true表示alpha来自参数面板
	  var unify_data = {};
	  if (param) unify_data = {
	    data: [initial_data] //使各个部分的结构层数都一样
	  };else unify_data = initial_data;

	  unify_data.data.forEach(function (first_word) {
	    first_word.data.forEach(function (second_word) {
	      second_word.data.forEach(function (third_word) {
	        third_word.data.forEach(function (poi) {
	          if (param) poi.val = poi.max_val * alpha + poi.simT;
	          var nodelist = __webpack_require__(336);
	          var scores = nodelist.siteScore.get(poi.site_id);
	          if (!scores) {
	            scores = new Map();
	            scores.set(poi.id, poi.val);
	            nodelist.siteScore.set(poi.site_id, scores);
	          } else {
	            var bef_poi_val = scores.get(poi.id);
	            if (!bef_poi_val) {
	              scores.set(poi.id, poi.val);
	            } else {
	              scores.set(poi.id, poi.val > bef_poi_val ? poi.val : bef_poi_val);
	            }
	          }
	        });
	      });
	    });
	  });
	}

	//为每个POI找到他的max_val:单个搜索词时是该poi的max val，多个搜索词合并后的max_val为每个搜索词的max val相加
	function max_value_POI(d) {
	  var poi_map = {};
	  var pois = [];
	  for (var i = 0; i < d.data.length; i++) {
	    var cur_max_map = {}; //用于存储当前关键词的各个POI的val的分别最大值
	    for (var j = 0; j < d.data[i].data.length; j++) {
	      for (var m = 0; m < d.data[i].data[j].data.length; m++) {
	        var poi = d.data[i].data[j].data[m];
	        poi['max_val'] = poi.val;
	        if (cur_max_map.hasOwnProperty(poi.name)) {
	          if (cur_max_map[poi.name].val < poi.val) cur_max_map[poi.name] = { 'poi': poi, 'S': j };
	        } else {
	          cur_max_map[poi.name] = { 'poi': poi, 'S': j };
	        }
	      }
	    }
	    for (var k in cur_max_map) {
	      var poi_name = cur_max_map[k].poi.name;
	      if (poi_map.hasOwnProperty(poi_name)) {
	        var index = poi_map[poi_name];
	        pois[index].poi.val += cur_max_map[k].poi.val;
	        pois[index].poi.max_val = pois[index].poi.val;
	      } else {
	        if (cur_max_map[k].poi.latitude > 27.9248561995 && cur_max_map[k].poi.latitude < 28.0769120675 && cur_max_map[k].poi.longitude > 120.5833650410 && cur_max_map[k].poi.longitude < 120.7579719628) {
	          poi_map[poi_name] = pois.length;
	          var _j = parseInt(cur_max_map[k]['S']);
	          pois.push({
	            index: pois.length,
	            poi: cur_max_map[k].poi,
	            words: {
	              F: (i, d.data[i].name),
	              S: (_j, d.data[i].data[_j].name)
	            }
	          });
	        }
	      }
	    }
	  }
	}

	//对数据做归一化处理
	function normalization(initial_data) {
	  initial_data.data.forEach(function (second_word) {
	    var max_val = 0;
	    var min_val = 0;
	    second_word.data.forEach(function (third_word) {
	      third_word.data.forEach(function (poi) {
	        max_val = poi.val > max_val ? poi.val : max_val;
	        min_val = poi.val < min_val || min_val == 0 ? poi.val : min_val;
	      });
	    });
	    var dis = max_val - min_val;
	    if (dis != 0) {
	      second_word.data.forEach(function (third_word) {
	        third_word.data.forEach(function (poi) {
	          poi.val = (poi.val - min_val) / dis;
	        });
	      });
	    }
	  });
	}

/***/ }),
/* 350 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.draw = draw;
	exports.filterListGeo = filterListGeo;
	exports.filterListTime = filterListTime;
	exports.highLightOneItem = highLightOneItem;
	exports.unhighLightOneItem = unhighLightOneItem;
	exports.demo = demo;

	var _d = __webpack_require__(328);

	var d3 = _interopRequireWildcard(_d);

	var _app = __webpack_require__(326);

	var _index = __webpack_require__(351);

	var _index2 = __webpack_require__(356);

	var _config = __webpack_require__(338);

	var Config = _interopRequireWildcard(_config);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	var resultlist = {
		container: d3.select("#list-contain"),
		showNum: 50,
		addNum: 50,
		renderTrajs: [],
		orderTrasjs: [],
		filterPids: [],
		filterInGeoPids: [],
		filterInTimePids: [],
		checkPids: [],
		hide: false //  按钮属性 , 表示是否隐藏 被过滤的属性
	};

	var visBox = document.getElementById("resultlist");
	var width = visBox.offsetWidth; //宽度
	var rectWidth = width - 260;

	function draw(data) {
		var showNum = resultlist.showNum,
		    hide = resultlist.hide;

		resultlist.orderTrasjs = data;
		resultlist.filter();
	}
	function filterListGeo(pids) {
		resultlist.filterInGeoPids = pids;
		resultlist.filterPids = resultlist.filterInGeoPids.concat(resultlist.filterInTimePids);
		resultlist.filter();
	}
	function filterListTime(pids) {
		resultlist.filterInTimePids = pids;
		resultlist.filterPids = resultlist.filterInGeoPids.concat(resultlist.filterInTimePids);
		resultlist.filter();
	}
	//样式高亮
	function highLightOneItem(id) {
		var allItems = d3.selectAll('.list-item');
		// console.log('高亮',id)

		allItems.each(function () {
			var curItem = d3.select(this),
			    curId = curItem.attr('id');
			if (curId == id) {
				curItem.transition().duration(Config.duration).style('background-color', 'rgb(158, 158, 158)');
			}
		});
	}
	function unhighLightOneItem(id) {
		var allItems = d3.selectAll('.list-item');
		allItems.each(function () {
			var curItem = d3.select(this),
			    curId = curItem.attr('id');
			if (curId == id) {
				curItem.transition().duration(Config.duration).style('background-color', '#FFFFFF');
			}
		});
	}

	resultlist.draw = function () {
		var renderTrajs = this.renderTrajs;

		var alltraj = this.container.select("#resultlist").selectAll(".list-item").data(renderTrajs, function (d) {
			return d.pid;
		});

		var addtraj = alltraj.enter() // 添加
		.append("div").classed("list-item", true).attr('id', function (d) {
			return d.pid;
		});

		var mergetraj = addtraj.merge(alltraj); //更新
		mergetraj.attr('id', function (d) {
			return d.pid;
		}).attr('class', 'list-item');

		// checkbox
		var checkContain = addtraj.append('div').attr('class', "check-contain");
		checkContain.append('input').attr('type', 'checkbox').attr('id', function (d) {
			return 'input' + d.pid;
		});
		checkContain.append('label').attr('for', function (d) {
			return 'input' + d.pid;
		});

		// pid
		addtraj.append('div').attr('class', 'id-word');
		mergetraj.select(".id-word").text(function (d) {
			return d.pid;
		});

		// rect 
		addtraj.append('div').attr('class', 'percent-rect');
		mergetraj.select(".percent-rect").style("width", function (d) {
			return d.per * 0.01 * rectWidth + 'px';
		});

		// value
		addtraj.append('div').attr('class', 'percent-word');
		mergetraj.select('.percent-word').text(function (d) {
			var num = d.score;
			if (Math.round(num) === num) return num; // 整数
			else {
					return num.toFixed(2);
				} //小数
		});

		mergetraj.on('mouseenter', enterEventHandler).on('mouseleave', leaveEventHander);

		this.filterDisplay();
		alltraj.exit().remove();
	};
	resultlist.reflow = function () {
		var container = this.container,
		    renderTrajs = this.renderTrajs,
		    filterPids = this.filterPids;

		var flowIndex = 0,
		    itemHeight = 40,
		    topHeight = 0;

		var allItems = container.selectAll('.list-item');

		resultlist.renderTrajs.forEach(function (traj) {
			var pid = traj.pid;
			allItems.each(function () {
				var curItem = d3.select(this),
				    curId = curItem.attr('id');
				if (pid == curId && curItem.attr('class').indexOf('hide') == -1) {
					topHeight = flowIndex * itemHeight;
					curItem.style('top', topHeight + 'px');
					flowIndex += 1;
				}
			});
		});
	};
	resultlist.filter = function () {
		var isFiltered = d3.select('#filter-btn').attr('filtered');

		var showNum = resultlist.showNum,
		    hide = resultlist.hide,
		    filterPids = resultlist.filterPids,
		    orderTrasjs = resultlist.orderTrasjs;

		var i = 0,
		    count = showNum;
		var _renderTrajs = [];
		var _renderPids = [];

		if (isFiltered) {
			// 仅展示 符合条件的 
			for (i = 0; i < orderTrasjs.length; i++) {
				var pid = orderTrasjs[i].pid;
				if (filterPids.indexOf(pid) == -1) {
					_renderTrajs.push(orderTrasjs[i]);
					_renderPids.push(pid);
					count--;
				}
				if (count <= 0) break;
			}
		} else {
			_renderTrajs = resultlist.orderTrasjs.slice(0, showNum);
			_renderTrajs.forEach(function (traj) {
				if (filterPids.indexOf(traj.pid) == -1) {
					_renderPids.push(traj.pid);
				}
			});
		}

		resultlist.renderTrajs = _renderTrajs;

		var checkPids = [];
		this.checkPids.forEach(function (pid) {
			if (_renderPids.indexOf(pid) != -1) {
				checkPids.push(pid);
			}
		});
		this.checkPids = checkPids;
		(0, _app.topicAdd)(checkPids);

		this.draw();
	};
	resultlist.filterDisplay = function () {
		var container = this.container,
		    hide = this.hide,
		    filterPids = this.filterPids;

		var allItems = container.selectAll('.list-item');

		var filterClassName = hide == true ? 'hide' : 'filtered';
		allItems.each(function () {
			var curItem = d3.select(this),
			    curId = curItem.attr('id');

			if (filterPids.indexOf(curId) != -1) {
				curItem.attr('class', 'list-item ' + filterClassName);
			}
		});
		this.filterCheckPid();
		this.handleInput();
		this.reflow();
		updatePerNum();
	};
	resultlist.handleInput = function () {
		var container = this.container,
		    checkPids = this.checkPids;

		var allItems = container.selectAll('.list-item');
		var self = this;
		allItems.select('input').property('checked', function (d) {
			if (checkPids.indexOf(d.pid) != -1) return true;else return false;
		}).on('change', function (d) {
			var pid = d.pid;
			var add = d3.select(this).property('checked');

			if (add) {
				//add
				self.checkPids.push(pid);
			} else {
				//remove
				self.removeCheckPid(pid);
			}
			(0, _app.topicAdd)(self.checkPids);
		});
	};
	resultlist.filterCheckPid = function () {
		var self = this;
		this.filterPids.forEach(function (fpid) {
			if (self.checkPids.indexOf(fpid) != -1) self.removeCheckPid(fpid);
		});
	};
	resultlist.removeCheckPid = function (pid) {
		var newTopicPids = [];
		this.checkPids.forEach(function (_pid) {
			if (_pid != pid) {
				newTopicPids.push(_pid);
			}
		});
		this.checkPids = newTopicPids;
	};

	// 过滤按钮
	d3.select('#filter-btn').attr('filtered', null).on('mousedown', btnEventHandler);

	d3.select('#filter-btn').on('mousedown', btnEventHandler);
	function btnHandler() {
		var checked = d3.select('#semantic-view').select('.switch-btn').property('checked');
		ifSample = !ifSample;

		// 重新绘制
		var _iteratorNormalCompletion = true;
		var _didIteratorError = false;
		var _iteratorError = undefined;

		try {
			for (var _iterator = objsH.keys()[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
				var pid = _step.value;

				var traj = objsH.get(pid).data;
				var h = objsH.get(pid).obj;
				h.destroy();
				addHexa(traj);
			}
		} catch (err) {
			_didIteratorError = true;
			_iteratorError = err;
		} finally {
			try {
				if (!_iteratorNormalCompletion && _iterator.return) {
					_iterator.return();
				}
			} finally {
				if (_didIteratorError) {
					throw _iteratorError;
				}
			}
		}
	}

	function btnEventHandler() {
		// let isFiltered =  d3.select('#filter-btn').attr('filtered')
		resultlist.showNum = resultlist.addNum;

		var isFiltered = d3.select('#filter-btn').property('checked');
		if (isFiltered) {
			d3.select(this).text('hide');
			d3.select(this).attr('filtered', null);
			resultlist.hide = false;
		} else {
			d3.select(this).text('show');
			d3.select(this).attr('filtered', true);
			resultlist.hide = true;
		}
		resultlist.filter();
	}

	function enterEventHandler() {
		var disable = d3.select(this).select('input').property('disabled');
		if (disable) return;

		var pid = d3.select(this).attr('id');
		var checked = d3.select(this).select('input').property('checked');
		(0, _index.hl_listItem)(pid, checked);
	}
	function leaveEventHander() {
		var disable = d3.select(this).select('input').property('disabled');
		if (disable) return;
		var pid = d3.select(this).attr('id');
		var checked = d3.select(this).select('input').property('checked');
		(0, _index.uhl_listItem)(pid, checked);
	}

	function updatePerNum() {
		var orderTrasjs = resultlist.orderTrasjs,
		    filterPids = resultlist.filterPids;


		var allNum = orderTrasjs.length;
		var selectNum = allNum - filterPids.length;
		// let text =  avaNum + '/' + orderTrasjs.length
		var numContan = d3.select("#list-contain").select('.num-contain');
		numContan.select('.select-num').text(selectNum);
		numContan.select('.all-num').text(allNum);
	}

	bindCheckAllBtn();
	function bindCheckAllBtn() {
		resultlist.container.select('.btn-container').select('.check-contain').select('input').on('change', function () {
			var add = d3.select(this).property('checked');

			if (add) {
				checkAll();
			} else {
				unCheckAll();
			}
			(0, _app.topicAdd)(resultlist.checkPids);
		});
	}
	function checkAll() {
		var items = resultlist.container.selectAll('.list-item');
		var checkedPids = [];

		items.each(function (item) {
			var curItem = d3.select(this),
			    curId = curItem.attr('id');

			if (curItem.attr('class').indexOf('filtered') == -1) {
				curItem.select('input').property('checked', true);
				checkedPids.push(curId);
			}
		});
		resultlist.checkPids = checkedPids;
	}
	function unCheckAll() {
		var inputs = resultlist.container.selectAll('.list-item').selectAll('input');
		inputs.property('checked', false);

		resultlist.checkPids = [];
	}
	function updateCheckNum() {
		var checkNumItem = resultlist.container.select('.btn-container').select('.display-num');

		var checkNum = resultlist.checkPids.length;
		checkNumItem.text(checkNum);
		if (checkNum == 0) {
			checkNumItem.style('color', '#e8e8e8');
		} else {
			checkNumItem.style('color', '#595959');
		}
	}

	d3.select('#resultlist').on('scroll', scrollHander);

	var listLoadingFlag = false;
	function scrollHander() {
		// console.log('scroll')
		var scrollTop = d3.event.target.scrollTop;
		var scrollHeight = visBox.scrollHeight;
		var clientHeight = visBox.clientHeight;

		var distance = scrollHeight - clientHeight - scrollTop;
		if (distance < 30) {
			// console.log('come to bottom')
			if (listLoadingFlag != true) {
				loadMore();
			}
		}
	}

	function loadMore() {
		listLoadingFlag = true;
		console.log('loading');
		resultlist.showNum += resultlist.addNum;
		// if( resultlist.showNum >= )
		resultlist.filter();
		listLoadingFlag = false;
	}

	function demo() {
		resultlist.draw();
	}

/***/ }),
/* 351 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.hl_SemanticTraj = hl_SemanticTraj;
	exports.uhl_SemanticTraj = uhl_SemanticTraj;
	exports.hl_listItem = hl_listItem;
	exports.uhl_listItem = uhl_listItem;
	exports.hl_timeline = hl_timeline;
	exports.uhl_timeline = uhl_timeline;

	var _app = __webpack_require__(326);

	var _traj = __webpack_require__(352);

	var _index = __webpack_require__(353);

	var _index2 = __webpack_require__(350);

	var _index3 = __webpack_require__(356);

	var _poi = __webpack_require__(342);

	var _pic = __webpack_require__(339);

	// 在地图上高亮单条轨迹 
	function highLightTrajContorl(id) {
		_app.availableTrajsinLimitTime.forEach(function (traj) {
			if (traj.pid == id) {
				(0, _traj.highLightTrajInMap)(traj, true);
				(0, _traj.highLightTrajInMap)(traj, false);
			}
		});

		// availableTrajsinLimitTime.forEach( (traj) =>{
		// 	if(traj.pid == id){
		// 		highLightTrajInMap(traj , false )
		// 	}	
		// })

		(0, _pic.shadowCanvas)();
	} /**
	   * 视图间的联动高亮
	   */

	function unHighLightTrajContorl(id) {
		(0, _traj.unHighLightTrajInMap)();

		(0, _pic.unShadowCanvas)();
	}

	//在地图上标上 POI 点
	function highlightPoisInTrajs(pid) {
		var sites = _app.trajId2Points.get(pid);
		var nodelist = __webpack_require__(336);

		var pois = [];
		sites.forEach(function (site) {
			nodelist.data.forEach(function (a) {
				a.data.forEach(function (b) {
					b.data.forEach(function (c) {
						c.data.forEach(function (_site) {
							var mockId = (0, _app.mockTrans)(+site.siteId); //case1 打开此行 
							// let mockId = site.siteId
							// console.log( site.siteId , "  => " , mockId)
							if (_site.site_id == mockId) {
								if (mockId == 25655 && _site.name != '江心屿') return;
								pois.push(_site);
							}
						});
					});
				});
			});
		});

		// 有可能 site 有 stoppoint 但是 无 poi 
		// console.log(pois)
		(0, _poi.draw)(pois);
	}
	function unHighlightPoisInTrajs() {
		(0, _poi.remove)();
	}

	/*
		hl_xxx
		uhl_xxx
	*/

	// 当高亮 hexa 时的操作
	function hl_SemanticTraj(pid) {
		(0, _index2.highLightOneItem)(pid);
		highLightTrajContorl(pid);

		for (var i = 0; i < _app.topicLists.length; i++) {
			if (_app.topicLists[i].pid == pid) {
				(0, _index.highLightTopic)(i);
				break;
			}
		}
	}
	function uhl_SemanticTraj(pid) {
		unHighLightTrajContorl();
		(0, _index2.unhighLightOneItem)(pid);

		for (var i = 0; i < _app.topicLists.length; i++) {
			if (_app.topicLists[i].pid == pid) {
				(0, _index.unHighLightTopic)(i);
				break;
			}
		}
	}

	// 高亮 list item 对应的其他高亮操作
	function hl_listItem(pid, checked) {
		(0, _index2.highLightOneItem)(pid);
		highLightTrajContorl(pid);
		highlightPoisInTrajs(pid);

		if (!checked) return;

		(0, _index3.highlightHexa)(pid);
		for (var i = 0; i < _app.topicLists.length; i++) {
			if (_app.topicLists[i].pid == pid) {
				(0, _index.highLightTopic)(i);
				break;
			}
		}
	}
	function uhl_listItem(pid, checked) {
		(0, _index2.unhighLightOneItem)(pid);
		unHighLightTrajContorl();
		unHighlightPoisInTrajs();

		if (!checked) return;
		(0, _index3.unHighlightHexa)(pid);
		for (var i = 0; i < _app.topicLists.length; i++) {
			if (_app.topicLists[i].pid == pid) {
				(0, _index.unHighLightTopic)(i);
				break;
			}
		}
	}

	// 高亮 timeline 的操作
	function hl_timeline(i, t, fillColor) {
		var traj = _app.topicLists[i].traj;

		var pid = _app.topicLists[i].pid;

		(0, _index2.highLightOneItem)(pid);
		highLightTrajContorl(pid);
		highlightPoisInTrajs(pid);

		for (var j = 0; j < traj.length; j++) {
			var startTime = traj[j].startTime;

			var date = startTime.split(' ')[0],
			    time = startTime.split(' ')[1],
			    _t = new Date(date + 'T' + time);
			// console.log(_t)
			if (_t.getTime() == t.getTime()) {

				var siteId1 = +traj[j].site,
				    siteId2 = +traj[j + 1].site;

				(0, _index3.highlightHexaInOnePoint)(pid, j);
				(0, _traj.highLightTrajSectionInMap)(siteId1, siteId2, fillColor);

				break;
			}
		}
	}
	function uhl_timeline(i) {
		var id = _app.topicLists[i].pid;
		(0, _traj.unHighLightTrajSectionInMap)();
		(0, _index2.unhighLightOneItem)(id);
		(0, _traj.unHighLightTrajInMap)();
		(0, _index3.unHighlightHexaInOnePoint)(id);
		unHighlightPoisInTrajs();
		unHighLightTrajContorl();
	}

/***/ }),
/* 352 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.highLightTrajInMap = highLightTrajInMap;
	exports.unHighLightTrajInMap = unHighLightTrajInMap;
	exports.highLightTrajSectionInMap = highLightTrajSectionInMap;
	exports.unHighLightTrajSectionInMap = unHighLightTrajSectionInMap;

	var _d = __webpack_require__(328);

	var d3 = _interopRequireWildcard(_d);

	var _clip = __webpack_require__(340);

	var _index = __webpack_require__(341);

	var _util = __webpack_require__(343);

	var _datamanager = __webpack_require__(329);

	var DataManager = _interopRequireWildcard(_datamanager);

	var _config = __webpack_require__(346);

	var Config = _interopRequireWildcard(_config);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	//使用 site 中的 vertice 数据
	// 库函数 用于 判断直线是否经过矩形
	var trajData = void 0; //全局数据 ，保存


	/*
	// 那些用户勾选的轨迹
	export function draw(trajs){
		if(trajs){
			trajData = trajs
		}else{
			if(!trajData)  return
		}
		
		if(trajData){
			delTrajsInMap()
			drawTrajsInMap(trajData)
			console.log(trajData)
		}
	}


	//绘制高亮的 整条轨迹
	export function drawTrajsInMap(trajs){
		let trajsSvg = d3.select('#svg-poi')

		trajs.forEach( (traj)=> {
			trajsSvg.append('g').attr('class','trajs').attr('id','id'+traj.pid)

			let sites  = traj.traj,
				lat,lng 

			let box = [ boundry.bottom_left.lng , boundry.bottom_left.lat 
						, boundry.top_right.lng , boundry.top_right.lat ]

			for(let i = 0 ;i< sites.length - 1;i++){
				let lat1 =  sites[i].latitude,
					lng1 =  sites[i].longitude,
					lat2 =  sites[i + 1].latitude,
					lng2 =  sites[i + 1].longitude,
					da = [] , db = [] , res 

				res = clip( [lng1,lat1] , [lng2,lat2] , box , da , db)

				if( res ){
					let pa = _l2pb(da[1], da[0]) , 
						pb = _l2pb(db[1], db[0])

					drawOneLineInTrajs( pa, pb , traj.pid)
				}
			}

		})


	}
		// 轨迹中的一段
		function drawOneLineInTrajs( pa , pb , id ){
			let svg = d3.select('#svg-poi').select('#id'+id)
			
			svg.append('line')
				.attr('y1',pa[1])
				.attr('x1',pa[0])
				.attr('x2',pb[0])
				.attr('y2',pb[1])
				.style('stroke','pink')
				.style('stroke-width',2)
		}

	// 删除高亮整条轨迹
	export function delTrajsInMap(){
		// console.log(traj)
		d3.select('#svg-poi').selectAll('.trajs').remove()
	}

	*/

	/*
		动态的 绘制高亮的 整条轨迹
	*/

	/*
	let timeoutList = []
	export function highLightTrajInMapDynamic(traj){
		let poiSvg = d3.select('#svg-poi')
		poiSvg.append('g').attr('id','traj')

		let sites  = traj.traj,
			lat,lng 

		let box = [ boundry.bottom_left.lng , boundry.bottom_left.lat 
					, boundry.top_right.lng , boundry.top_right.lat ]

		let ts = setTimeScale(sites)

		clearTimeOuts()
		for(let i = 0 ;i< sites.length - 1;i++){
			let lat1 =  sites[i].latitude,
				lng1 =  sites[i].longitude,
				lat2 =  sites[i + 1].latitude,
				lng2 =  sites[i + 1].longitude,
				startTime = sites[i].startTime,
				endTime  =  sites[i].endTime,
				startDate = new Date(  startTime.split(' ')[0]  + 'T' + startTime.split(' ')[1] ),
				endDate = new Date(  endTime.split(' ')[0]  + 'T' + endTime.split(' ')[1] ),
				duration = ts(endDate) - ts(startDate),
				da = [] , db = [] , res 


			res = clip( [lng1,lat1] , [lng2,lat2] , box , da , db)

			if( res ){
				let pa = _l2pb(da[1], da[0]) , 
					pb = _l2pb(db[1], db[0])
				// console.log( duration )	
				let timeoutId = setTimeout( () => {
					// console.log(traj.pid +' 第 '+ ts(startDate) +'ms')
					drawOneLine( pa, pb , duration)  
				}	, ts(startDate) )
				timeoutList.push(timeoutId)
			}
		}
	}

	function clearTimeOuts(){
		timeoutList.forEach((id)=>{
			clearTimeout(id)

		})
		timeoutList = []
	}
	function setTimeScale(sites){
		let firstTime = sites[0].startTime,
			lastTime  = sites[sites.length - 1].endTime , 
			firstDate = new Date(  firstTime.split(' ')[0]  + 'T' + firstTime.split(' ')[1] ),
			lastDate = new Date(  lastTime.split(' ')[0]  + 'T' + lastTime.split(' ')[1] )
		
		var ts = d3.scaleTime()
		    .domain([firstDate, lastDate])
		    .range([0,Config.duration]);

		return ts
	}

	// 	// 轨迹中的一段
		function drawOneLine( pa , pb, duration){
			// console.log("经过 ",duration +'ms')
			let svg = d3.select('#svg-poi').select('#traj')
			svg.append('line')
				.attr('y1',pa[1])
				.attr('x1',pa[0])
				.attr('x2',pa[0])
				.attr('y2',pa[1])
				.style('stroke',Config.oneWholeTrajColor)
				.style('stroke-width',2)
				.transition()
				.duration(duration)
				.attr('x2',pb[0])
				.attr('y2',pb[1])

		}
	*/

	/*
		绘制高亮的 整条轨迹
	*/
	function highLightTrajInMap(traj, outer) {
		var poiSvg = d3.select('#svg-poi');
		poiSvg.append('g').attr('id', 'traj');

		var sites = traj.traj,
		    lat = void 0,
		    lng = void 0;

		var box = [_index.boundry.bottom_left.lng, _index.boundry.bottom_left.lat, _index.boundry.top_right.lng, _index.boundry.top_right.lat];

		var draw_points = [];
		for (var i = 0; i < sites.length - 1; i++) {
			var lat1 = sites[i].latitude,
			    lng1 = sites[i].longitude,
			    lat2 = sites[i + 1].latitude,
			    lng2 = sites[i + 1].longitude,
			    da = [],
			    db = [],
			    res = void 0;

			res = (0, _clip.clip)([lng1, lat1], [lng2, lat2], box, da, db);

			if (res) {
				var pa = (0, _util._l2pb)(da[1], da[0]),
				    pb = (0, _util._l2pb)(db[1], db[0]);

				// 会有重合点
				draw_points.push(pa);
				draw_points.push(pb);
			}
		}

		var curveLine = d3.line().x(function (d) {
			return d[0];
		}).y(function (d) {
			return d[1];
		}).curve(d3.curveNatural);

		d3.select('#svg-poi').select('#traj').append('path').attr('class', 'high-light-line').attr('d', function (d) {
			return curveLine(draw_points);
		}).style('stroke', Config.oneWholeTrajOuterColor).style('stroke-width', Config.oneWholeTrajOuterWidth).style('opacity', Config.oneWholeTrajOuterOpacity);

		d3.select('#svg-poi').select('#traj').append('path').attr('class', 'high-light-line').attr('d', function (d) {
			return curveLine(draw_points);
		}).style('stroke', Config.oneWholeTrajinnerColor).style('stroke-width', Config.oneWholeTrajinnerWidth);
	}

	// 轨迹中的一段
	// function drawTwoColorLine( pa , pb , outer){
	// 	let svg = d3.select('#svg-poi').select('#traj')

	// 	if( outer ){
	// 		svg.append('line')
	// 			.attr('y1',pa[1])
	// 			.attr('x1',pa[0])
	// 			.style('stroke',Config.oneWholeTrajOuterColor)
	// 			.style('stroke-width',2)
	// 			.style('opacity',0.7)
	// 			.attr('x2',pb[0])
	// 			.attr('y2',pb[1])
	// 	}else{
	// 		svg.append('line')
	// 			.attr('y1',pa[1])
	// 			.attr('x1',pa[0])
	// 			.style('stroke',Config.oneWholeTrajinnerColor)
	// 			.style('stroke-width',1)
	// 			.attr('x2',pb[0])
	// 			.attr('y2',pb[1])
	// 	}
	// }


	// 删除高亮整条轨迹
	function unHighLightTrajInMap() {
		d3.select('#svg-poi').select('#traj').remove();
	}

	/*
		 高亮 region
	*/
	function highLightTrajSectionInMap(siteId1, siteId2, fillColor) {

		var poiSvg = d3.select('#svg-poi');
		poiSvg.append('g').attr('id', 'traj-section');

		var sites = DataManager.sites;

		var site1 = sites.get(siteId1),
		    site2 = sites.get(siteId2);

		var box = [_index.boundry.bottom_left.lng, _index.boundry.bottom_left.lat, _index.boundry.top_right.lng, _index.boundry.top_right.lat];
		var lat1 = site1.latitude,
		    lng1 = site1.longitude,
		    lat2 = site2.latitude,
		    lng2 = site2.longitude,
		    da = [],
		    db = [],
		    res = void 0;

		var vertice1 = site1.vertice.split(';'),
		    vertice2 = site2.vertice.split(';');

		// res = clip( [lng1,lat1] , [lng2,lat2] , box , da , db)
		// if( res ){
		// 	let pa = _l2pb(da[1], da[0]) , 
		// 		pb = _l2pb(db[1], db[0])
		// 	drawOneSection( pa, pb)
		// }

		// 可能 vertice1 数据为空
		drawOneSectonPolygon(vertice1, fillColor);
		// drawOneSectonPolygon(vertice2)
	}
	function drawOneSectonPolygon(vertice, fillColor) {
		var vertices = [],
		    points = "";
		for (var i = 0; i < vertice.length; i++) {
			var v = vertice[i].split(',');
			v.forEach(function (_v) {
				_v = parseFloat(_v);
			});
			vertices.push([parseFloat(v[0]), parseFloat(v[1])]);
		}
		var box = [_index.boundry.bottom_left.lng, _index.boundry.bottom_left.lat, _index.boundry.top_right.lng, _index.boundry.top_right.lat];

		for (var _i = 1; _i < vertices.length; _i++) {
			var da = [],
			    db = [],
			    res = void 0;
			res = (0, _clip.clip)(vertices[_i - 1], vertices[_i], box, da, db);
			if (res) {
				var pa = (0, _util._l2pb)(da[1], da[0]),
				    pb = (0, _util._l2pb)(db[1], db[0]);

				points += pa[0] + "," + pa[1] + " ";
				points += pb[0] + "," + pb[1] + " ";

				// console.log(pa,pb,da,db)
			}
		}
		//points 可能为空
		var svg = d3.select('#svg-poi').select('#traj-section');

		// 有的点可能超出界外

		console.log(fillColor);
		//多边形
		svg.append('polygon').attr('points', points).style('stroke', 'black').style('stroke-width', 1).style('fill', fillColor).style('opacity', 0.6);
	}
	function unHighLightTrajSectionInMap() {
		d3.select('#svg-poi').select('#traj-section').remove();
	}

/***/ }),
/* 353 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.draw = draw;
	exports.addTopic = addTopic;
	exports.highLightTopic = highLightTopic;
	exports.unHighLightTopic = unHighLightTopic;

	var _d = __webpack_require__(328);

	var d3 = _interopRequireWildcard(_d);

	var _topic = __webpack_require__(354);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	var objsT = [];

	function draw(data) {
		d3.select('.rect-group-container').selectAll('.rect-container').remove();

		objsT = [];
		data.forEach(function (traj, i) {
			addTopic(traj, i);
		});
	}

	function addTopic(traj, i) {
		var t = new _topic.topicZoomRect();
		t._init(traj, i);
		t._render();
		objsT.push(t);
	}
	function highLightTopic(i) {
		var t = objsT[i];
		t.high_light_whole();
	}
	function unHighLightTopic(i) {
		var t = objsT[i];
		t.unhigh_light_whole();
	}

	function getTimeRange(data) {
		var timeRange = [];
		data.forEach(function (traj) {
			traj.traj.forEach(function (p) {

				var date = p.startTime.split(' ')[0],
				    time = p.startTime.split(' ')[1],
				    _t = new Date(date + 'T' + time);

				if (!timeRange[0]) {
					timeRange = [_t, _t];
				} else {
					if (_t.getTime() < timeRange[0].getTime()) timeRange[0] = _t;
					if (_t.getTime() > timeRange[1].getTime()) timeRange[1] = _t;
				}
			});
		});
		return timeRange;
	}

/***/ }),
/* 354 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.topicZoomRect = undefined;

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _d = __webpack_require__(328);

	var d3 = _interopRequireWildcard(_d);

	var _config = __webpack_require__(338);

	var Config = _interopRequireWildcard(_config);

	var _index = __webpack_require__(351);

	var _time = __webpack_require__(355);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	var visBox = document.getElementById("topic-container");
	var h = visBox.offsetHeight; //高度
	var w = visBox.offsetWidth - 100; //宽度


	// 数据格式转换
	function dataAdapter(_line_data) {
		// 将 topics 为空的项去掉 用后面的数据连起来
		var ps = _line_data.traj.map(function (line) {
			return {
				date: line.startTime.split(' ')[0],
				time: line.startTime.split(' ')[1],
				topics: line.topics,
				topicsHexa: Config.transTopicHexa(line.topics)
			};
		}).filter(function (p) {
			return p.topics;
		});

		return {
			id: _line_data.pid,
			ps: ps
		};
	}

	//  一个 instance 代表一条

	var topicZoomRect = exports.topicZoomRect = function () {
		function topicZoomRect() {
			_classCallCheck(this, topicZoomRect);
		}

		_createClass(topicZoomRect, [{
			key: '_init',
			value: function _init(_data, index) {
				var data = dataAdapter(_data);
				var _el = document.createElement('div');
				_el.className = 'rect-container th' + index;
				document.getElementsByClassName('rect-group-container')[0].appendChild(_el);
				this.rootEl = _el;
				this.vHeight = Config.vRectHeight;
				this.vWidth = w * 0.9;
				this.data = data;
				this.index = index; //代表第几个实例
			}
		}, {
			key: '_render',
			value: function _render() {
				//注意顺序
				this._appenTrajId();
				this._appenSVG();
				this._appendShowRect();
				this.update_rect();
				this._appendTopicRect();
				this._syncTopicRect();
			}
		}, {
			key: '_appenTrajId',
			value: function _appenTrajId() {
				var vHeight = this.vHeight,
				    vWidth = this.vWidth,
				    tLeft = this.tLeft,
				    tTop = this.tTop,
				    index = this.index,
				    data = this.data;

				console.log(data.id);
				var idDiv = d3.select('#topic-container').select('.th' + index) //选择有隐患
				.append('div').attr('class', 'traj-id');

				var circleR = 8;
				idDiv.append('svg').attr('width', circleR).attr('height', circleR).append('rect').attr('class', 'traj-circle').attr('width', circleR).attr('height', circleR).attr('rx', circleR).attr('ry', circleR);

				idDiv.append('div').attr('class', 'traj-text').text(data.id);
			}
			// 需要当 rootEl 挂载后再 append

		}, {
			key: '_appenSVG',
			value: function _appenSVG() {
				var vHeight = this.vHeight,
				    vWidth = this.vWidth,
				    tLeft = this.tLeft,
				    tTop = this.tTop,
				    index = this.index;

				var svg = d3.select('#topic-container').select('.th' + index) //选择有隐患
				.append('svg').attr("transform", "translate(" + ((w - vWidth) / 2 + 7) + ",0)").attr('width', vWidth).attr('height', vHeight);

				var chart = svg.append('g').attr('class', 'chart').attr("transform", "translate(12,0)");

				this.chart = chart;
			}
			/*
	  	- rect container 
	  		- svg 
	  			- rect show-rects
	  		- topic-rect-container
	  			- topic-rect
	  	两层重合的  ，外面一层与里面一层对齐
	  */

		}, {
			key: '_appendShowRect',
			value: function _appendShowRect() {
				var chart = this.chart;

				var _setAxis2 = this._setAxis(),
				    dates = _setAxis2.dates,
				    xAxis = _setAxis2.xAxis; // 时间轴  时间是隐藏的


				var gRects = chart.insert('g', '.listener-rect').attr('class', 'rects-group');
				var rects = gRects.selectAll('.show-rects').data(dates).enter().append('rect').attr('class', 'show-rects').attr('y', 0).attr('fill', 'none').attr('opcity', 0).attr('height', Config.vRectHeight);
				gRects.append("g").attr("class", "axis axis--x").attr("transform", "translate(0, " + (Config.vRectHeight + 1) + ")").call(xAxis);

				var self = this;
				this.update_rect = function () {
					chart.select(".axis--x").call(xAxis);

					rects.attr('x', function (d, i) {
						var x = (0, _time.timeScale)(dates[i]);
						return x;
					}).attr('width', function (d, i) {
						var width = (0, _time.timeScale)(dates[i + 1]) - (0, _time.timeScale)(dates[i]);
						width = width >= 0 ? width : 0;
						return width;
					});
					self.rects = rects;
				};

				_time.func_list.push({
					'func': this.update_rect,
					'whos': this
				});
				_time.func_list.push({
					'func': this._syncTopicRect,
					'whos': this
				});
			}
		}, {
			key: '_setAxis',
			value: function _setAxis() {
				var data = this.data;

				var ps = data.ps;
				var id = data.id;

				var dates = ps.map(function (el) {
					return new Date(el.date + 'T' + el.time);
				});

				var xAxis = d3.axisBottom(_time.timeScale);
				this.dates = dates;
				this.id = id;

				return { dates: dates, xAxis: xAxis };
			}
		}, {
			key: '_appendTopicRect',
			value: function _appendTopicRect() {
				var rects = this.rects,
				    vHeight = this.vHeight,
				    vWidth = this.vWidth,
				    index = this.index,
				    id = this.id;

				var self = this;
				var topicContainer = d3.select('.th' + index).insert('div').attr('class', 'topic-rect-container').style('left', 100 + (w - vWidth) / 2 + 7 + 'px').style('height', vHeight + 'px').style('width', vWidth + 'px');

				rects.each(function (t) {
					//对每一个 rect
					topicContainer.append('div').attr('class', 'topic-rect').on('mouseenter', function () {
						//其他的包括其他行的变暗
						d3.selectAll('.topic-rect').style('opacity', 0.2);
						//选中的这块变亮
						d3.select(this).style('opacity', 1);
						// 联动

						self.high_light_rect(t);
					}).on('mouseleave', function () {
						self.unhigh_light_rect();
					});
				});

				this.topicContainer = topicContainer;
			}
		}, {
			key: '_syncTopicRect',
			value: function _syncTopicRect() {
				var rects = this.rects,
				    topicContainer = this.topicContainer,
				    data = this.data,
				    selectedIndex = this.selectedIndex;


				var _rects = d3.selectAll('.rect-container:last-child .topic-rect');
				var _rect = void 0;

				// 根据不同的长度设置为不同的样式
				rects.each(function (d, i) {
					var rect = d3.select(this);
					var width = rect.attr('width'),
					    height = rect.attr('height'),
					    x = rect.attr('x'),
					    y = rect.attr('y');
					_rect = topicContainer.select('.topic-rect:nth-child(' + (i + 1) + ')');

					//移除 child elements 
					_rect.selectAll('div').remove();

					if (!data.ps[i].topics) {
						console.log('have empty topic!!');
						return;
					}

					var topic_top = data.ps[i].topicsHexa[0];
					var topic_top_name = topic_top.name;
					var topic_top_val = Math.round(+topic_top.val * 100) + '%';
					var topic_top_icon = topic_top.icon;
					var topic_top_color = topic_top.color;

					var topic_2rd = data.ps[i].topicsHexa[1];
					var topic_2rd_name = topic_2rd.name;
					var topic_2rd_val = Math.round(+topic_2rd.val * 100) + '%';
					var topic_2rd_icon = topic_2rd.icon;

					var topic_3rd = data.ps[i].topicsHexa[2];
					var topic_3rd_name = topic_3rd.name;
					var topic_3rd_val = Math.round(+topic_3rd.val * 100) + '%';

					_rect.style('width', width + 'px').style('height', height + 'px').style('left', x + 'px').style('top', y + 'px').style('background-color', topic_top_color);

					if (+width < Config.topicThemesConfig.min_width) {

						_rect.style('background-color', '#bfbfbf');
					} else {

						for (var j = 0; j < 6; j++) {
							var box = _rect.append('div').attr('class', 'left-box');

							if ((j + 1) * Config.topicThemesConfig.oneBoxWidth > +width) break;

							box.append('img').attr('src', Config.iconSrcUrl + data.ps[i].topicsHexa[j].icon);
							box.append('div').attr('class', 'precent-text').html(Math.round(+data.ps[i].topicsHexa[j].val * 100) + '%');
						}

						// let left_box = _rect.append('div')
						// 			.attr('class','left-box')
						// let right_box = _rect.append('div')
						// 			.attr('class','right-box')

						// 	left_box.append('img')		
						// 		.attr('src',Config.iconSrcUrl + topic_top_icon)
						// 	left_box.append('div')
						// 		.attr('class','precent-text')
						// 		.html(topic_top_val)

						// 	right_box.append('div')
						// 		.attr('class','precent-text')
						// 		.html(topic_2rd_name + ' : ' + topic_2rd_val)
						// 	right_box.append('div')
						// 		.attr('class','precent-text')
						// 		.html(topic_3rd_name + ' : ' + topic_3rd_val)
					}
				});

				if (selectedIndex) {
					_rect = topicContainer.select('.topic-rect:nth-child(' + (selectedIndex + 1) + ')');
					_rect.style('opacity', 1);
				}

				this.topicContainer = topicContainer;
			}

			// hover to select

		}, {
			key: 'select',
			value: function select() {
				var fromOuter = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : true;
				var index = this.index;


				if (fromOuter) {
					d3.selectAll('.topic-rect').transition().duration(Config.duration).style('opacity', 0.2);
					d3.select('.rect-group-container').select('.th' + index).select('.topic-rect-container').selectAll('.topic-rect').style('opacity', 1);
				}
			}
		}, {
			key: 'un_select',
			value: function un_select() {
				var fromOuter = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : true;
				var index = this.index;

				d3.selectAll('.topic-rect').style('opacity', 1);
			}
		}, {
			key: 'high_light_rect',
			value: function high_light_rect(t) {
				//t 如 Tue Jan 14 2014 05:37:06 GMT+0800 (China Standard Time)
				var index = this.index,
				    data = this.data,
				    dates = this.dates;

				var topicIndex = dates.indexOf(t),
				    fillColor = data.ps[topicIndex].topicsHexa[0].color;
				(0, _index.hl_timeline)(index, t, fillColor);
			}
		}, {
			key: 'unhigh_light_rect',
			value: function unhigh_light_rect() {
				//所有亮的变亮
				var index = this.index;

				d3.selectAll('.topic-rect').style('opacity', 1);
				(0, _index.uhl_timeline)(index);
			}
		}, {
			key: 'high_light_whole',
			value: function high_light_whole() {
				// 整个高亮 => 当整条轨迹被选中
				var index = this.index;

				d3.select('#topic-container').select('.th' + index).select('.topic-rect-container').style('box-shadow', '0px 1px 5.5px #333333');
			}
		}, {
			key: 'unhigh_light_whole',
			value: function unhigh_light_whole() {
				//取消选择
				var index = this.index;

				d3.select('#topic-container').select('.th' + index).select('.topic-rect-container').style('box-shadow', '0px 0px 0px');
			}
		}]);

		return topicZoomRect;
	}();

/***/ }),
/* 355 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.func_list = exports.timeScale2 = exports.timeScale = undefined;
	exports.init = init;

	var _d = __webpack_require__(328);

	var d3 = _interopRequireWildcard(_d);

	var _config = __webpack_require__(338);

	var Config = _interopRequireWildcard(_config);

	var _app = __webpack_require__(326);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	var visBox = document.getElementById("topic-container");
	var h = visBox.offsetHeight; //高度
	var w = visBox.offsetWidth - 100; //宽度

	var syncAsixFunc = void 0; // 时间轴同步方法
	var timeRange = void 0; // 时间范围 [start,end]\
	var timeScale = exports.timeScale = void 0,
	    timeScale2 = exports.timeScale2 = void 0; //两个 timeScale 
	var func_list = exports.func_list = void 0;
	var brushFunc;

	// for all instances 
	function init(_timeRange) {
		d3.select('#topic-container').selectAll('*').remove();

		exports.func_list = func_list = [];
		timeRange = _timeRange;
		// console.log(timeRange)

		// timerange 为多有查询出来轨迹的最大时间范围
		appendWidgets();
	}

	addLegend(); // 添加一次即可
	function appendWidgets() {
		setTimeScale();
		setTopAxis();
		addTopicContains();
		setZoom();
		setBrush();
	}
	function setTimeScale() {
		// scales 
		exports.timeScale = timeScale = d3.scaleTime().range([0, w * 0.9]);
		exports.timeScale2 = timeScale2 = d3.scaleTime().range([0, w * 0.9]);

		// set domain
		timeScale.domain(timeRange);
		timeScale2.domain(timeRange);
	}

	function setTopAxis() {
		// 顶部时间轴 
		var axisFunc = d3.axisBottom(timeScale);

		axisFunc.ticks(5) //显示的个数
		.tickFormat(d3.timeFormat("%x %H:%M"));

		var TrajLabel = d3.select('#topic-container').append('div').attr('id', 'label-trajectory').text('Trajectories');

		var topAxis = d3.select('#topic-container').append('div').attr('id', 'top-axis-container').style('left', 100 + w * 0.05 + 'px').append('svg').attr('width', w * 0.9 + 14).attr('height', 40).append("g").attr("transform", "translate(7 , -2 )").attr("class", "axis axis--top").call(axisFunc);

		syncAsixFunc = function syncAsixFunc() {
			topAxis.call(axisFunc);
		};
	}
	function addTopicContains() {
		// 中间的 toipcs
		d3.select('#topic-container').append('div').attr('class', 'rect-group-container');
	}
	function setZoom() {
		var zoomFunc = d3.zoom().scaleExtent([0.5, 100]) //缩放比例
		// .translateExtent([[-w * 0.9, 0], [2 *w * 0.9,10] ])
		.on('zoom', function () {
			// console.log('zoom')
			func_zoomed();
		});
		// .on('end', debounceSelect() )
		// 底部的 zoom 监听svg
		var listenerRect = d3.select('#topic-container').append('svg').attr("transform", "translate(100,0)").attr('width', w - 10).attr('height', 40).attr('class', 'listener-svg').append('rect').attr('class', 'listener-rect').attr('x', 0).attr('y', 0).attr('width', w).attr('height', 40);

		d3.select('#top-axis-container').select('svg').call(zoomFunc);
	}
	var s = [];
	function func_zoomed() {
		var t = d3.event.transform;

		// console.log(timeScale2.invert(t))

		// d3.select('#top-axis-container').select('svg')
		// 	.call( brushFunc.move , timeScale.range().map(t.invertX, t) )
		// brush.move, timeScale.range().map(t.invertX, t)


		// console.log(s)

		var s_t = [timeScale.invert(s[0]), timeScale.invert(s[1])];
		// console.log(s_t)
		//更新 timeScale
		timeScale.domain(t.rescaleX(timeScale2).domain()); //  timeScal2 不变


		var s_ = [timeScale(s_t[0]), timeScale(s_t[1])];

		// console.log(s_)
		//更新时间轴
		syncAsixFunc();

		d3.select('#top-axis-container').select('svg').select('.brush').call(brushFunc.move, s_);

		// console.log("transform ", t.invertX )
		// console.log("range ", timeScale.range() )
		// console.log(  timeScale.range().map((t.applyX), t)   )

		func_list.forEach(function (f) {
			f.func.call(f.whos);
		});
	}
	function syncAllInstance() {
		//将注册的函数一一调用
		func_list, forEach(function (f) {
			f.func.call(f.whos);
		});
	}
	function func_tickmove(x) {
		d3.select('#topic-container').selectAll('.tick-line').remove();

		var tickLine = d3.select('#topic-container').insert('svg', '.listener-svg').attr('class', 'tick-line').attr('width', w - 10).attr('height', h).append('line').attr('y1', 15).attr('y2', h - 15);

		tickLine.attr('x1', x).attr('x2', x);
	}
	function setBrush() {
		brushFunc = d3.brushX().extent([[0, 0], [w * 0.9, 10]])
		// .extent([[0, 0], [w * 0.9, Config.vRectHeight]])
		.on("start", function () {
			// console.log('start')
			brushStyle();
		}).on("brush", function () {
			s = d3.event.selection;
			brushStyle();
		}).on("end", debounceSelect());

		d3.select('#top-axis-container').select('svg').append('g').attr("class", "brush").attr("transform", "translate(7 , 20 )").call(brushFunc).call(brushFunc.move, [0, w * 0.9]); //拖动框

	}

	function brushStyle() {

		var brushG = d3.select('.brush');

		brushG.select('.selection').attr('height', function (a) {
			return 10;
		});

		brushG.select('.handle--e').attr('width', 10).attr('height', 10).attr('rx', 10).attr('ry', 10).attr('y', 0);

		brushG.select('.handle--w').attr('width', 10).attr('height', 10).attr('rx', 10).attr('ry', 10).attr('y', 0);
	}

	function brushed() {
		console.log('start select');
		timeSelect();
	}

	//添加图例
	function addLegend() {
		var container = d3.select('#topic-legend');

		Config.topicNames.forEach(function (topicType, i) {
			var item = container.append('div').attr('class', 'legend-item').style("left", i % 2 * 50 + '%').style("top", 29 + Math.floor(i / 2) * 29 + 'px');
			item.append('div').attr('class', 'legend-rect').style('background-image', function () {
				switch (topicType.name) {
					case "住宅":
						return 'url("' + Config.iconSrcUrl + 'Residential.png")';
						break;
					case "娱乐商业":
						return 'url("' + Config.iconSrcUrl + 'Entertainment.png")';
						break;
					case "办公":
						return 'url("' + Config.iconSrcUrl + 'Business.png")';
						break;
					case "医疗":
						return 'url("' + Config.iconSrcUrl + 'Medical.png")';
						break;
					case "交通":
						return 'url("' + Config.iconSrcUrl + 'Traffic.png")';
						break;
					case "教育":
						return 'url("' + Config.iconSrcUrl + 'Educational.png")';
						break;
				}
			});
			// .style('background-color',Config.colorList[i])
			item.append('div').attr('class', 'legend-name').text(function () {
				switch (topicType.name) {
					case "住宅":
						return 'Residential Related';
						break;
					case "娱乐商业":
						return 'Entertainment Related';
						break;
					case "办公":
						return 'Business Related';
						break;
					case "医疗":
						return 'Medical Related';
						break;
					case "交通":
						return 'Traffic Related';
						break;
					case "教育":
						return 'Educational Related';
						break;
				}
			});
		});
		// Config.topicNameList.forEach((name,i)=>{
		// 	let item = container.append('div').attr('class','legend-item')
		//  	item.append('div').attr('class','legend-rect')
		//  		.style('background-color',Config.colorList[i])
		//  item.append('div').attr('class','legend-name')
		//  	.text(name)
		// })
	}
	function timeSelect() {
		console.log('start select');

		var brushG = d3.select('.brush'),
		    brushRectWidth = 3,
		    rectStart = +brushG.select('.handle--w').attr('x') + brushRectWidth,
		    rectEnd = +brushG.select('.handle--e').attr('x') + brushRectWidth,
		    timeStart = timeScale.invert(rectStart),
		    timeEnd = timeScale.invert(rectEnd);

		console.log('brush', rectStart, rectEnd);
		console.log("full time:", timeRange);
		console.log("start time:", timeStart);
		console.log("end time:", timeEnd);
		(0, _app.filterDataInTime)(timeStart, timeEnd);
	}

	function getTimeInterval() {
		var time1 = timeRange[0];

		var oneMin = 1 * 60 * 1000;
		var onehour = oneMin * 60;
		var halfDay = onehour * 12;
		var oneDay = onehour * 24;
		var addTime = void 0,
		    times = [oneMin, onehour, halfDay, oneDay];

		for (var i = 0; i < 4; i++) {
			addTime = times[i];
			var time2 = new Date(time1.getTime() + addTime);
			var gap = timeScale(time2) - timeScale(time1);
			console.log(i + ' => ' + gap);
		}
	}

	// 防止短时内 频繁调用 耗时函数
	var debounceTimer = void 0;
	function debounceSelect(delay) {

		var delaySecond = delay || 2000;
		return function () {
			// console.log('debounce')
			var context = this;
			clearTimeout(debounceTimer);
			debounceTimer = setTimeout(function () {
				timeSelect();
			}, delaySecond);
		};
	}

/***/ }),
/* 356 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.ifSampleLine = exports.ifSamplePoint = undefined;
	exports.draw = draw;
	exports.addHexa = addHexa;
	exports.highlightHexa = highlightHexa;
	exports.unHighlightHexa = unHighlightHexa;
	exports.highlightHexaInOnePoint = highlightHexaInOnePoint;
	exports.unHighlightHexaInOnePoint = unHighlightHexaInOnePoint;

	var _hexagon = __webpack_require__(357);

	var _hexa = __webpack_require__(359);

	var ifSamplePoint = exports.ifSamplePoint = false;
	var ifSampleLine = exports.ifSampleLine = false;

	var objsH = new Map();
	(0, _hexagon.drawHexagon)();

	function draw(data) {
		var needPids = [];
		data.forEach(function (traj, i) {
			var pid = traj.pid;
			if (!objsH.get(pid)) {
				addHexa(traj);
			}
			needPids.push(pid);
		});

		//destroy
		var destroyPids = [];
		var _iteratorNormalCompletion = true;
		var _didIteratorError = false;
		var _iteratorError = undefined;

		try {
			for (var _iterator = objsH.keys()[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
				var pid = _step.value;

				if (needPids.indexOf(pid) == -1) destroyPids.push(pid);
			}
		} catch (err) {
			_didIteratorError = true;
			_iteratorError = err;
		} finally {
			try {
				if (!_iteratorNormalCompletion && _iterator.return) {
					_iterator.return();
				}
			} finally {
				if (_didIteratorError) {
					throw _iteratorError;
				}
			}
		}

		destroyPids.forEach(function (pid) {
			var h = objsH.get(pid);
			h.obj.destroy();
			objsH.delete(pid);
		});
	}

	function addHexa(traj) {
		var h = new _hexa.Hexa();
		h.init(traj);
		h.start();
		objsH.set(traj.pid, { obj: h, data: traj });
	}

	d3.select('#semantic-view').select('#sample-line').select('.switch-btn').on('mousedown', btnLineHandler);
	function btnLineHandler() {
		var checked = d3.select('#semantic-view').select('.switch-btn').property('checked');
		exports.ifSampleLine = ifSampleLine = !ifSampleLine;

		var _iteratorNormalCompletion2 = true;
		var _didIteratorError2 = false;
		var _iteratorError2 = undefined;

		try {
			for (var _iterator2 = objsH.keys()[Symbol.iterator](), _step2; !(_iteratorNormalCompletion2 = (_step2 = _iterator2.next()).done); _iteratorNormalCompletion2 = true) {
				var pid = _step2.value;

				var traj = objsH.get(pid).data;
				var h = objsH.get(pid).obj;

				if (ifSampleLine) h.showLinks();else h.hideLinks();
			}
		} catch (err) {
			_didIteratorError2 = true;
			_iteratorError2 = err;
		} finally {
			try {
				if (!_iteratorNormalCompletion2 && _iterator2.return) {
					_iterator2.return();
				}
			} finally {
				if (_didIteratorError2) {
					throw _iteratorError2;
				}
			}
		}
	}

	d3.select('#semantic-view').select('#sample-point').select('.switch-btn').on('mousedown', btnPointHandler);
	function btnPointHandler() {
		var checked = d3.select('#semantic-view').select('.switch-btn').property('checked');
		exports.ifSamplePoint = ifSamplePoint = !ifSamplePoint;

		// 重新绘制
		var _iteratorNormalCompletion3 = true;
		var _didIteratorError3 = false;
		var _iteratorError3 = undefined;

		try {
			for (var _iterator3 = objsH.keys()[Symbol.iterator](), _step3; !(_iteratorNormalCompletion3 = (_step3 = _iterator3.next()).done); _iteratorNormalCompletion3 = true) {
				var pid = _step3.value;

				var traj = objsH.get(pid).data;
				var h = objsH.get(pid).obj;
				h.destroy();
				addHexa(traj);
			}
		} catch (err) {
			_didIteratorError3 = true;
			_iteratorError3 = err;
		} finally {
			try {
				if (!_iteratorNormalCompletion3 && _iterator3.return) {
					_iterator3.return();
				}
			} finally {
				if (_didIteratorError3) {
					throw _iteratorError3;
				}
			}
		}
	}

	function highlightHexa(pid) {

		objsH.forEach(function (obj, _pid) {
			if (_pid == pid) {
				if (obj) obj.obj.highlight();
			} else {
				if (obj) obj.obj.dark();
			}
		});
	}

	function unHighlightHexa(pid) {
		objsH.forEach(function (obj, _pid) {
			if (_pid == pid) {
				if (obj) obj.obj.unHighlight();
			} else {
				if (obj) obj.obj.undark();
			}
		});
	}

	// 高亮某条轨迹对应的某个时间段
	function highlightHexaInOnePoint(pid, i) {
		objsH.forEach(function (obj, _pid) {
			if (_pid == pid) {
				if (obj) obj.obj.highlight();
				if (obj) obj.obj.highlightOnePoint(i);
			} else {
				if (obj) obj.obj.dark();
			}
		});
	}
	function unHighlightHexaInOnePoint(pid) {
		objsH.forEach(function (obj, _pid) {
			if (_pid == pid) {
				if (obj) obj.obj.unHighlightOnePoint();
				if (obj) obj.obj.unHighlight();
			} else {
				if (obj) obj.obj.undark();
			}
		});
	}

/***/ }),
/* 357 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.drawHexagon = drawHexagon;

	var _config = __webpack_require__(358);

	var Config = _interopRequireWildcard(_config);

	var _config2 = __webpack_require__(338);

	function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

	var svg = void 0;

	function drawHexagon(argument) {
		var visBox = document.getElementById("semantic-view");
		var h = visBox.offsetHeight; //高度
		var w = visBox.offsetWidth; //宽度

		svg = d3.select('#semantic-view').append('svg').attr('class', 'hexagon').style('width', "100%").style('height', "calc(100% - 35px)").style('top', "35px").style('position', "absolute");
		addLinks();
		addNodes();
	}

	// function addNodes(){
	// 	let nodes = svg.append("g")
	// 		.selectAll("circle")
	//         .data(coorHexagonCopy)
	//         .enter()
	//         .append("circle")
	//         .attr("class", "node-nomal")
	//         .attr("id",function(d,i) { return  "th"+i  } )
	//           .attr("cx", function(d) { return d.x; })
	//           .attr("cy", function(d) { return d.y; })
	//           .attr('fill',function(d,i){ return  colorList[i] })

	// 	node.call(
	// 		d3.drag()
	// 			.on("start", dragstarted)
	// 	        .on("drag", dragged)
	// 	        .on("end", dragended)
	//     )
	// }

	function addNodes() {
		var container = d3.select('#semantic-view').append('div').attr('class', 'legend-nodes');

		_config.coorHexagonCopy.forEach(function (d, i) {
			var item = container.append('div').attr('class', 'node-legend-item').style("left", d.x - 25 + 'px').style("top", d.y + 15 + 'px');

			item.append('div').attr('class', 'node-legend-rect').style('background-image', function () {
				switch (_config2.topicNames[i].name) {
					case "住宅":
						return 'url("' + _config.iconSrcUrl + 'Residential.png")';
						break;
					case "娱乐商业":
						return 'url("' + _config.iconSrcUrl + 'Entertainment.png")';
						break;
					case "办公":
						return 'url("' + _config.iconSrcUrl + 'Business.png")';
						break;
					case "医疗":
						return 'url("' + _config.iconSrcUrl + 'Medical.png")';
						break;
					case "交通":
						return 'url("' + _config.iconSrcUrl + 'Traffic.png")';
						break;
					case "教育":
						return 'url("' + _config.iconSrcUrl + 'Educational.png")';
						break;
				}
			});
		});
	}

	function addLinks() {
		var hexagonLinks = _config.coorHexagonCopy.map(function (p, i) {
			var now = p,
			    len = _config.coorHexagonCopy.length,
			    former = i > 0 ? _config.coorHexagonCopy[i - 1] : _config.coorHexagonCopy[len - 1];
			return {
				s: {
					x: now.x,
					y: now.y
				},
				d: {
					x: former.x,
					y: former.y
				}
			};
		});

		var link = svg.append("g").attr("class", "links").selectAll("line").data(hexagonLinks).enter().append("line").attr('class', 'link-nomal').attr("x1", function (d) {
			return d.s.x;
		}).attr("y1", function (d) {
			return d.s.y;
		}).attr("x2", function (d) {
			return d.d.x;
		}).attr("y2", function (d) {
			return d.d.y;
		});
	}

/***/ }),
/* 358 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.namesHexagon = exports.coorHexagonStrength = exports.coorHexagonCopy = exports.coorHexagon = exports.coorCenter = exports.iconSrcUrl = exports.stepLen = exports.SVGlength = undefined;
	exports.transDatas = transDatas;

	var _config = __webpack_require__(338);

	var SVGlength = exports.SVGlength = 400;

	var stepLen = exports.stepLen = 100;
	var iconSrcUrl = exports.iconSrcUrl = '../semantictrajdemo/assets/icon/';

	var visBox = document.getElementById("semantic-view");
	var h = visBox.offsetHeight - 35; //高度
	var w = visBox.offsetWidth;
	var m = h >= w ? w : h;

	// let r = 60    //  半径/2
	var r = Math.floor(m / 5);
	var sqrt3 = Math.sqrt(3);

	//中心点 坐标位置
	var coorCenter = exports.coorCenter = {
	  x: Math.floor(w / 2),
	  y: Math.floor(h / 2)
	};

	var coorHexagon = exports.coorHexagon = [
	// 最左
	{
	  x: coorCenter.x - 2 * r,
	  y: coorCenter.y
	},
	// 左上
	{
	  x: coorCenter.x - r,
	  y: coorCenter.y - sqrt3 * r
	},
	// 右上
	{
	  x: coorCenter.x + r,
	  y: coorCenter.y - sqrt3 * r
	},
	// 最右
	{
	  x: coorCenter.x + 2 * r,
	  y: coorCenter.y
	},
	// 右下
	{
	  x: coorCenter.x + r,
	  y: coorCenter.y + sqrt3 * r
	},
	// 左下
	{
	  x: coorCenter.x - r,
	  y: coorCenter.y + sqrt3 * r
	}];

	// deep clone
	// 用于表示 拖拽点的坐标
	var coorHexagonCopy = exports.coorHexagonCopy = JSON.parse(JSON.stringify(coorHexagon));

	var _r = 0.7 * r;
	var coorHexagonStrength = exports.coorHexagonStrength = [
	// 最左
	{
	  x: coorCenter.x - 2 * _r,
	  y: coorCenter.y
	},
	// 左上
	{
	  x: coorCenter.x - _r,
	  y: coorCenter.y - sqrt3 * _r
	},
	// 右上
	{
	  x: coorCenter.x + _r,
	  y: coorCenter.y - sqrt3 * _r
	},
	// 最右
	{
	  x: coorCenter.x + 2 * _r,
	  y: coorCenter.y
	},
	// 右下
	{
	  x: coorCenter.x + _r,
	  y: coorCenter.y + sqrt3 * _r
	},
	// 左下
	{
	  x: coorCenter.x - _r,
	  y: coorCenter.y + sqrt3 * _r
	}];

	// 通过 index 与 coorHexagon 对应
	var namesHexagon = exports.namesHexagon = _config.topicNames.map(function (n) {
	  return n.name;
	});

	function transDatas(point) {
	  var topics = point.topics;

	  var res = {};

	  if (!topics) {
	    console.log('empty topics');
	    return null;
	  }
	  topics.forEach(function (t) {
	    var topic = t.topic,
	        val = t.val;

	    var name = void 0;

	    for (var i = 0; i < _config.topicNames.length; i++) {
	      if (_config.topicNames[i].contain.indexOf(topic) != -1) {
	        var per = _config.topicNames[i].percent[topic];
	        name = _config.topicNames[i].name;
	        res[name] = !res[name] ? val * per : val * per + res[name];
	      }
	    }
	  });

	  res['sum'] = 0;
	  for (var one in res) {
	    res['sum'] += res[one];
	  }

	  return res;
	}

/***/ }),
/* 359 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.Hexa = undefined;

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _config = __webpack_require__(358);

	var _config2 = __webpack_require__(338);

	var _particle = __webpack_require__(360);

	var _index = __webpack_require__(356);

	var _index2 = __webpack_require__(351);

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	var Hexa = function () {
		function Hexa() {
			_classCallCheck(this, Hexa);
		}

		_createClass(Hexa, [{
			key: 'init',
			value: function init(trajData) {
				var pid = trajData.pid,
				    traj = trajData.traj;

				var points = traj.map(function (p) {
					return (0, _config.transDatas)(p);
				}).filter(function (e) {
					return e;
				}); //filter null element

				var stopPointsIndex = [];
				traj.filter(function (p) {
					return p.topics;
				}).forEach(function (p, i) {
					if (p.stoppoint != undefined) stopPointsIndex.push(i);
				});
				this.stopPointsIndex = stopPointsIndex;

				this.data = {
					pid: pid, points: points
				};

				this.generateNodes();
				this.generatePaticle();
				this.alive = true;

				// console.log(pid , points , traj , stopPointsIndex)
			}
		}, {
			key: 'generateNodes',
			value: function generateNodes() {
				var _data = this.data,
				    pid = _data.pid,
				    points = _data.points;
				var stopPointsIndex = this.stopPointsIndex;


				var svg = d3.select("#semantic-view").select('svg');
				var g = svg.append("g").attr('class', 'nodes' + pid);
				var self = this;
				var generateNodeNum = _index.ifSamplePoint == true ? stopPointsIndex.length : points.length;

				for (var i = 0; i < generateNodeNum; i++) {
					g.append('circle').attr("class", "node-item").attr("cx", _config.coorCenter.x).attr("cy", _config.coorCenter.y).on('mouseenter', function () {
						self.enterHandler();
					}).on('mouseleave', function () {
						self.leaveHandler();
					});
				}
				this.g = g;
			}
		}, {
			key: 'generatePaticle',
			value: function generatePaticle() {
				var points = this.data.points;
				var stopPointsIndex = this.stopPointsIndex;

				var Ps = void 0;
				if (!_index.ifSamplePoint) {
					Ps = points.map(function (d, i) {
						var p = new _particle.Particle();
						p.init(d, i);
						return p;
					});
				} else {
					// console.log("Hexa stopPointsIndex : ",stopPointsIndex)
					Ps = stopPointsIndex.map(function (d, i) {
						var p = new _particle.Particle();
						// console.log(points[d])
						p.init(points[d], i);
						return p;
					});
				}
				this.Ps = Ps;
			}
		}, {
			key: 'start',
			value: function start() {
				var self = this;
				var iterations = 50,
				    //迭代次数
				interval = 20;

				for (var i = 0; i < iterations; i++) {
					setTimeout(function () {
						self.tick();
					}, i * interval);
				}
			}
		}, {
			key: 'tick',
			value: function tick() {
				var Ps = this.Ps,
				    g = this.g,
				    alive = this.alive;
				var pid = this.data.pid;

				if (!alive) return;
				// console.log(Ps)
				Ps.forEach(function (p, i) {
					// console.log(pid + ' ticks')
					p.tick();
					var coor = p.getXY();
					g.select('circle:nth-child(' + (i + 1) + ')').attr("cx", coor.x).attr("cy", coor.y);
				});
				this.updateNodeLinks();
			}
		}, {
			key: 'updateNodeLinks',
			value: function updateNodeLinks() {
				var g = this.g,
				    Ps = this.Ps;

				g.selectAll('path').remove();

				var cn = 0.5;
				var curveLine = d3.line().x(function (d) {
					return d.x;
				}).y(function (d) {
					return d.y;
				}).curve(d3.curveNatural);

				var cps = [];
				for (var i = 0; i < Ps.length; i++) {
					cps.push(Ps[i].getXY());
				}

				g.append('path').attr('class', 'link-item').attr('d', function (d) {
					return curveLine(cps);
				});

				// console.log(cps)
				if (!_index.ifSampleLine) this.hideLinks();else this.showLinks();
			}
		}, {
			key: 'showLinks',
			value: function showLinks() {
				var g = this.g;

				g.select('path').transition().duration(_config2.duration).style('opacity', 0.5);
			}
		}, {
			key: 'hideLinks',
			value: function hideLinks() {
				var g = this.g;

				g.select('path').transition().duration(_config2.duration).style('opacity', 0);
			}
		}, {
			key: 'enterHandler',
			value: function enterHandler() {
				var pid = this.data.pid;

				this.highlight();
				(0, _index2.hl_SemanticTraj)(pid);
			}
		}, {
			key: 'leaveHandler',
			value: function leaveHandler() {
				var pid = this.data.pid;

				this.unHighlight();
				(0, _index2.uhl_SemanticTraj)(pid);
			}
		}, {
			key: 'highlight',
			value: function highlight() {
				var g = this.g;

				g.selectAll('circle').style('opacity', 0.7);
				g.selectAll('path').style('opacity', 0.7);
			}
		}, {
			key: 'unHighlight',
			value: function unHighlight() {
				var g = this.g;

				g.selectAll('circle').style('opacity', 0.5);
				g.selectAll('path').style('opacity', 0.5);
			}
		}, {
			key: 'dark',
			value: function dark() {
				var g = this.g;

				g.selectAll('circle').style('opacity', 0.01);
				g.selectAll('path').style('opacity', 0.01);
			}
		}, {
			key: 'undark',
			value: function undark() {
				var g = this.g;

				g.selectAll('circle').style('opacity', 0.5);
				g.selectAll('path').style('opacity', 0.5);
			}
		}, {
			key: 'destroy',
			value: function destroy() {
				var g = this.g;

				g.remove();
				this.alive = false; // 死了
			}
		}, {
			key: 'highlightOnePoint',
			value: function highlightOnePoint(index) {
				var Ps = this.Ps,
				    g = this.g,
				    alive = this.alive,
				    stopPointsIndex = this.stopPointsIndex;
				var pid = this.data.pid;


				if (_index.ifSamplePoint) {
					stopPointsIndex.forEach(function (val, i) {
						// val 为 index
						if (val == index) {
							g.select('circle:nth-child(' + (i + 1) + ')').style('opacity', 1).style('fill', 'rgb(46, 117, 182)');
						}
					});
				} else {
					g.select('circle:nth-child(' + (index + 1) + ')').style('opacity', 1).style('fill', 'rgb(46, 117, 182)');
				}
			}
		}, {
			key: 'unHighlightOnePoint',
			value: function unHighlightOnePoint() {
				var g = this.g;

				g.selectAll('circle').style("fill", 'none');
			}
		}]);

		return Hexa;
	}();

	exports.Hexa = Hexa;

/***/ }),
/* 360 */
/***/ (function(module, exports, __webpack_require__) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
		value: true
	});
	exports.Particle = undefined;

	var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

	var _config = __webpack_require__(358);

	function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

	var Particle = function () {
		function Particle() {
			_classCallCheck(this, Particle);
		}

		_createClass(Particle, [{
			key: 'init',
			value: function init(data, i) {
				this.x = _config.coorCenter.x;
				this.y = _config.coorCenter.y;
				this.i = i; // 第几个点
				this.setForces(data);
			}
		}, {
			key: 'setForces',
			value: function setForces(data) {
				var forces = [];
				forces = _config.namesHexagon.map(function (name, i) {
					return data[name] / data['sum'];
				});
				this.forces = forces;
			}
		}, {
			key: 'tick',
			value: function tick() {
				this.getForceComp();
				this.moveItem();
			}
		}, {
			key: 'getForceComp',
			value: function getForceComp() {
				// 计算合力
				// 注意 力大小 需要和 力位置 对应
				var coor = _config.coorHexagonStrength; //此时按照 index 对应
				var forces = this.forces;

				var c = void 0,
				    x = void 0,
				    y = void 0,
				    l = void 0,
				    fx = void 0,
				    fy = void 0,
				    f = void 0,
				    forces_ = [];
				for (var i = 0; i < 6; i++) {
					c = coor[i];
					f = forces[i];
					x = c.x - this.x;
					y = c.y - this.y;
					l = Math.sqrt(x * x + y * y);
					fx = f * x / l;
					fy = f * y / l;
					forces_[i] = {
						fx: fx, fy: fy
					};
				}

				var _forcesComp = { fx: 0, fy: 0 };
				for (var _i = 0; _i < 6; _i++) {
					_forcesComp.fx += forces_[_i].fx;
					_forcesComp.fy += forces_[_i].fy;
				}

				this.forcesComp = _forcesComp;
			}
		}, {
			key: 'moveItem',
			value: function moveItem() {
				var forcesComp = this.forcesComp;

				var x = void 0,
				    y = void 0,
				    s = 1;

				// 自适应 k 值
				// 使单词位移长度限定在 s 内
				var k = Math.floor(s / Math.sqrt(forcesComp.fx * forcesComp.fx + forcesComp.fy * forcesComp.fy));

				// x,y 为 单词位移长度 
				x = forcesComp.fx * _config.stepLen;
				y = forcesComp.fy * _config.stepLen;

				this.x = this.x + x;
				this.y = this.y + y;
			}
		}, {
			key: 'getXY',
			value: function getXY() {
				var x = this.x,
				    y = this.y,
				    i = this.i;

				return {
					x: x, y: y
					// console.log(x,y,i)
				};
			}
		}]);

		return Particle;
	}();

	exports.Particle = Particle;

/***/ }),
/* 361 */
/***/ (function(module, exports, __webpack_require__) {

	"use strict";Object.defineProperty(exports,"__esModule",{value:true});exports.mock=mock;var _app=__webpack_require__(326);//import { filterListTime  } from '../app/list/index_.js'
	function mock(){// console.log(mockData)
	(0,_app.setGlobalTrajData)(mockData20);// topicAdd([mockData20[0].pid])
	// let filterPids = ['460000102807196','460000122835349','460000102839002','410060006060278']
	// setTimeout(()=>{
	//   console.log('filter')
	//   filterListTime(filterPids)
	// },4000)
	// let OrderedTrajs = mockData20.sort((t1,t2)=>{
	//  let s1 = t1.pid , s2 = t2.pid
	//  return s2 - s1  // 逆序 
	// })
	// setTimeout(()=>{
	//     console.log('reOrder')
	//     setGlobalTrajData(OrderedTrajs)
	// },8000)
	}//模拟数据进行调用
	var mockData5=[{"pid":"460000102807196","traj":[{"site":"4249","startTime":"2014-01-14 07:10:08.79","endTime":"2014-01-14 07:58:38.96","stopTime":2910,"isStop":true,"stoppoint":0,"latitude":28.0051,"longitude":120.6387,"topics":[{"topic":"Hospital","val":0.1849773153942428},{"topic":"Uptown","val":0.11219102002503129},{"topic":"Government","val":0.07738188360450564},{"topic":"Education","val":0.06979427409261577},{"topic":"Traffic","val":0.06244133291614518},{"topic":"Enterprise","val":0.056300844806007506},{"topic":"Hotel","val":0.05010168961201502},{"topic":"Food","val":0.040714956195244054},{"topic":"Scenicspot","val":0.03928739048811014},{"topic":"Beauty","val":0.03408557571964956},{"topic":"Life","val":0.032110450563204},{"topic":"Shop","val":0.03126955569461827},{"topic":"Finance","val":0.03125}]},{"site":"16738","startTime":"2014-01-14 07:58:38.96","endTime":"2014-01-14 07:59:12.06","stopTime":33,"isStop":false,"latitude":28.0092,"longitude":120.6403,"topics":[{"topic":"Finance","val":0.13312372172532783},{"topic":"Life","val":0.06755395463841901},{"topic":"Hotel","val":0.06318874202509138},{"topic":"Food","val":0.06257822277847212},{"topic":"Beauty","val":0.060380353490642816},{"topic":"Shop","val":0.055038310082724445},{"topic":"Uptown","val":0.05326780426752861},{"topic":"Education","val":0.051894135962635285},{"topic":"Hospital","val":0.05125309075368505},{"topic":"Traffic","val":0.048811013767208006},{"topic":"Scenicspot","val":0.048811013767208006},{"topic":"Enterprise","val":0.048780487804877044},{"topic":"Government","val":0.048780487804877044}]},{"site":"4249","startTime":"2014-01-14 07:59:12.06","endTime":"2014-01-14 08:06:14.81","stopTime":422,"isStop":false,"latitude":28.0051,"longitude":120.6387,"topics":[{"topic":"Hospital","val":0.1849773153942428},{"topic":"Uptown","val":0.11219102002503129},{"topic":"Government","val":0.07738188360450564},{"topic":"Education","val":0.06979427409261577},{"topic":"Traffic","val":0.06244133291614518},{"topic":"Enterprise","val":0.056300844806007506},{"topic":"Hotel","val":0.05010168961201502},{"topic":"Food","val":0.040714956195244054},{"topic":"Scenicspot","val":0.03928739048811014},{"topic":"Beauty","val":0.03408557571964956},{"topic":"Life","val":0.032110450563204},{"topic":"Shop","val":0.03126955569461827},{"topic":"Finance","val":0.03125}]},{"site":"19696","startTime":"2014-01-14 08:06:14.81","endTime":"2014-01-14 08:07:50.74","stopTime":95,"isStop":false,"latitude":28.0048,"longitude":120.6409,"topics":[{"topic":"Life","val":0.07431586780773256},{"topic":"Food","val":0.07042587017555732},{"topic":"Shop","val":0.061901701451138366},{"topic":"Beauty","val":0.06054865879646868},{"topic":"Hotel","val":0.05828231234989696},{"topic":"Education","val":0.05747048675709515},{"topic":"Hospital","val":0.057335182491628174},{"topic":"Finance","val":0.05679396542976032},{"topic":"Uptown","val":0.05412170618678763},{"topic":"Scenicspot","val":0.05408788012042089},{"topic":"Government","val":0.05408788012042089},{"topic":"Enterprise","val":0.05405405405405415},{"topic":"Traffic","val":0.05405405405405415}]},{"site":"3068","startTime":"2014-01-14 08:07:50.74","endTime":"2014-01-14 08:10:29.33","stopTime":158,"isStop":false,"latitude":28.011,"longitude":120.6436,"topics":[{"topic":"Shop","val":0.10040328188012879},{"topic":"Hospital","val":0.0930793121030932},{"topic":"Beauty","val":0.07374959440040883},{"topic":"Food","val":0.05771102767348134},{"topic":"Government","val":0.05518472164279517},{"topic":"Finance","val":0.0524034672970849},{"topic":"Life","val":0.05131414267834865},{"topic":"Hotel","val":0.048718305289019215},{"topic":"Education","val":0.041394335511983016},{"topic":"Uptown","val":0.04086126176238863},{"topic":"Scenicspot","val":0.037407870949798694},{"topic":"Enterprise","val":0.037037037037037354},{"topic":"Traffic","val":0.037037037037037354}]},{"site":"27088","startTime":"2014-01-14 08:10:29.33","endTime":"2014-01-14 08:11:16.17","stopTime":46,"isStop":false,"latitude":28.0114,"longitude":120.6458,"topics":[{"topic":"Finance","val":0.08701666556880411},{"topic":"Food","val":0.06788090376128131},{"topic":"Shop","val":0.06712337790659458},{"topic":"Beauty","val":0.058560042157961366},{"topic":"Hotel","val":0.057736644489823595},{"topic":"Life","val":0.05276332257427132},{"topic":"Uptown","val":0.05269745076082028},{"topic":"Education","val":0.05266451485409479},{"topic":"Scenicspot","val":0.052664514854094784},{"topic":"Hospital","val":0.052664514854094784},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Traffic","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265}]},{"site":"2001","startTime":"2014-01-14 08:11:16.17","endTime":"2014-01-14 08:11:42.23","stopTime":26,"isStop":false,"latitude":28.0112,"longitude":120.6501,"topics":[{"topic":"Uptown","val":0.08273161965277206},{"topic":"Shop","val":0.07352138891563212},{"topic":"Hotel","val":0.061166201341420645},{"topic":"Finance","val":0.061005744359937254},{"topic":"Beauty","val":0.05805333590064511},{"topic":"Education","val":0.052790346907993835},{"topic":"Life","val":0.05153878245242434},{"topic":"Food","val":0.05134623407464442},{"topic":"Enterprise","val":0.05131414267834777},{"topic":"Hospital","val":0.05131414267834777},{"topic":"Traffic","val":0.05128205128205112},{"topic":"Scenicspot","val":0.05128205128205112},{"topic":"Government","val":0.05128205128205112}]},{"site":"2669","startTime":"2014-01-14 08:11:42.23","endTime":"2014-01-14 08:15:55.58","stopTime":253,"isStop":false,"latitude":28.0182,"longitude":120.6471,"topics":[{"topic":"Life","val":0.12277847309136442},{"topic":"Traffic","val":0.09679599499374182},{"topic":"Food","val":0.0811013767209011},{"topic":"Education","val":0.07224030037546941},{"topic":"Uptown","val":0.06230287859824818},{"topic":"Finance","val":0.06027534418022565},{"topic":"Government","val":0.05912390488110169},{"topic":"Hospital","val":0.05349186483103842},{"topic":"Scenicspot","val":0.05281602002503085},{"topic":"Beauty","val":0.05111389236545636},{"topic":"Hotel","val":0.04778473091364154},{"topic":"Enterprise","val":0.04002503128911076},{"topic":"Shop","val":0.04002503128911076}]},{"site":"10321","startTime":"2014-01-14 08:15:55.58","endTime":"2014-01-14 08:21:42.11","stopTime":346,"isStop":false,"latitude":28.0233,"longitude":120.659,"topics":[{"topic":"Traffic","val":0.09681219171022658},{"topic":"Life","val":0.08810032148028239},{"topic":"Shop","val":0.07634543178973768},{"topic":"Uptown","val":0.07359689808338926},{"topic":"Hotel","val":0.05379273110996605},{"topic":"Finance","val":0.053252840560504736},{"topic":"Hospital","val":0.051117818842180245},{"topic":"Beauty","val":0.04888463520577197},{"topic":"Scenicspot","val":0.04873739232864611},{"topic":"Education","val":0.04616064197894427},{"topic":"Enterprise","val":0.04586615622469263},{"topic":"Food","val":0.03931384819259356},{"topic":"Government","val":0.039240226754030647}]},{"site":"24425","startTime":"2014-01-14 08:21:42.11","endTime":"2014-01-14 08:24:47.4","stopTime":185,"isStop":false,"latitude":28.0272,"longitude":120.6511,"topics":[{"topic":"Beauty","val":0.1196950733871887},{"topic":"Scenicspot","val":0.07432586187279619},{"topic":"Education","val":0.0732734099442491},{"topic":"Hotel","val":0.06488223916259026},{"topic":"Life","val":0.06115599044259911},{"topic":"Traffic","val":0.05762885424963063},{"topic":"Food","val":0.05603595403345132},{"topic":"Enterprise","val":0.05321993400842005},{"topic":"Uptown","val":0.05304926612811518},{"topic":"Hospital","val":0.050859028330868666},{"topic":"Finance","val":0.04809989759927239},{"topic":"Shop","val":0.04545454545454611},{"topic":"Government","val":0.04545454545454611}]},{"site":"10321","startTime":"2014-01-14 08:24:47.4","endTime":"2014-01-14 08:21:42.11","stopTime":86214,"isStop":true,"latitude":28.0233,"longitude":120.659,"topics":[{"topic":"Traffic","val":0.09681219171022658},{"topic":"Life","val":0.08810032148028239},{"topic":"Shop","val":0.07634543178973768},{"topic":"Uptown","val":0.07359689808338926},{"topic":"Hotel","val":0.05379273110996605},{"topic":"Finance","val":0.053252840560504736},{"topic":"Hospital","val":0.051117818842180245},{"topic":"Beauty","val":0.04888463520577197},{"topic":"Scenicspot","val":0.04873739232864611},{"topic":"Education","val":0.04616064197894427},{"topic":"Enterprise","val":0.04586615622469263},{"topic":"Food","val":0.03931384819259356},{"topic":"Government","val":0.039240226754030647}]},{"site":"24425","startTime":"2014-01-14 08:21:42.11","endTime":"2014-01-14 08:24:47.4","stopTime":185,"isStop":false,"latitude":28.0272,"longitude":120.6511,"topics":[{"topic":"Beauty","val":0.1196950733871887},{"topic":"Scenicspot","val":0.07432586187279619},{"topic":"Education","val":0.0732734099442491},{"topic":"Hotel","val":0.06488223916259026},{"topic":"Life","val":0.06115599044259911},{"topic":"Traffic","val":0.05762885424963063},{"topic":"Food","val":0.05603595403345132},{"topic":"Enterprise","val":0.05321993400842005},{"topic":"Uptown","val":0.05304926612811518},{"topic":"Hospital","val":0.050859028330868666},{"topic":"Finance","val":0.04809989759927239},{"topic":"Shop","val":0.04545454545454611},{"topic":"Government","val":0.04545454545454611}]},{"site":"10321","startTime":"2014-01-14 08:24:47.4","endTime":"2014-01-14 08:25:47.33","stopTime":59,"isStop":false,"latitude":28.0233,"longitude":120.659,"topics":[{"topic":"Traffic","val":0.09681219171022658},{"topic":"Life","val":0.08810032148028239},{"topic":"Shop","val":0.07634543178973768},{"topic":"Uptown","val":0.07359689808338926},{"topic":"Hotel","val":0.05379273110996605},{"topic":"Finance","val":0.053252840560504736},{"topic":"Hospital","val":0.051117818842180245},{"topic":"Beauty","val":0.04888463520577197},{"topic":"Scenicspot","val":0.04873739232864611},{"topic":"Education","val":0.04616064197894427},{"topic":"Enterprise","val":0.04586615622469263},{"topic":"Food","val":0.03931384819259356},{"topic":"Government","val":0.039240226754030647}]},{"site":"11050","startTime":"2014-01-14 08:25:47.33","endTime":"2014-01-14 08:28:08.82","stopTime":141,"isStop":false,"latitude":28.0214,"longitude":120.6556,"topics":[{"topic":"Government","val":0.23995511630917793},{"topic":"Scenicspot","val":0.07530965430926621},{"topic":"Education","val":0.0698718225367925},{"topic":"Finance","val":0.06426136118423875},{"topic":"Shop","val":0.0638513659315524},{"topic":"Traffic","val":0.04365370506236281},{"topic":"Hospital","val":0.03614431832894616},{"topic":"Uptown","val":0.03495748996590615},{"topic":"Life","val":0.03465538820076869},{"topic":"Food","val":0.034525916015709784},{"topic":"Beauty","val":0.034525916015709784},{"topic":"Hotel","val":0.03450433731819996},{"topic":"Enterprise","val":0.03448275862069015}]},{"site":"10321","startTime":"2014-01-14 08:28:08.82","endTime":"2014-01-14 08:29:03.1","stopTime":54,"isStop":false,"latitude":28.0233,"longitude":120.659,"topics":[{"topic":"Traffic","val":0.09681219171022658},{"topic":"Life","val":0.08810032148028239},{"topic":"Shop","val":0.07634543178973768},{"topic":"Uptown","val":0.07359689808338926},{"topic":"Hotel","val":0.05379273110996605},{"topic":"Finance","val":0.053252840560504736},{"topic":"Hospital","val":0.051117818842180245},{"topic":"Beauty","val":0.04888463520577197},{"topic":"Scenicspot","val":0.04873739232864611},{"topic":"Education","val":0.04616064197894427},{"topic":"Enterprise","val":0.04586615622469263},{"topic":"Food","val":0.03931384819259356},{"topic":"Government","val":0.039240226754030647}]},{"site":"11050","startTime":"2014-01-14 08:29:03.1","endTime":"2014-01-14 08:29:24.34","stopTime":21,"isStop":false,"latitude":28.0214,"longitude":120.6556,"topics":[{"topic":"Government","val":0.23995511630917793},{"topic":"Scenicspot","val":0.07530965430926621},{"topic":"Education","val":0.0698718225367925},{"topic":"Finance","val":0.06426136118423875},{"topic":"Shop","val":0.0638513659315524},{"topic":"Traffic","val":0.04365370506236281},{"topic":"Hospital","val":0.03614431832894616},{"topic":"Uptown","val":0.03495748996590615},{"topic":"Life","val":0.03465538820076869},{"topic":"Food","val":0.034525916015709784},{"topic":"Beauty","val":0.034525916015709784},{"topic":"Hotel","val":0.03450433731819996},{"topic":"Enterprise","val":0.03448275862069015}]},{"site":"12701","startTime":"2014-01-14 08:29:24.34","endTime":"2014-01-14 08:39:32.29","stopTime":607,"isStop":false,"latitude":28.0227,"longitude":120.6568,"topics":[{"topic":"Government","val":0.1808510638297896},{"topic":"Hospital","val":0.10868699510752099},{"topic":"Shop","val":0.052139037433155594},{"topic":"Education","val":0.04929457276140686},{"topic":"Finance","val":0.0477016725452276},{"topic":"Life","val":0.046649220616680566},{"topic":"Scenicspot","val":0.0463078848560707},{"topic":"Uptown","val":0.045482990101263596},{"topic":"Hotel","val":0.045482990101263596},{"topic":"Enterprise","val":0.04545454545454611},{"topic":"Traffic","val":0.04545454545454611},{"topic":"Food","val":0.04545454545454611},{"topic":"Beauty","val":0.04545454545454611}]},{"site":"10321","startTime":"2014-01-14 08:39:32.29","endTime":"2014-01-14 08:58:10.35","stopTime":1118,"isStop":false,"latitude":28.0233,"longitude":120.659,"topics":[{"topic":"Traffic","val":0.09681219171022658},{"topic":"Life","val":0.08810032148028239},{"topic":"Shop","val":0.07634543178973768},{"topic":"Uptown","val":0.07359689808338926},{"topic":"Hotel","val":0.05379273110996605},{"topic":"Finance","val":0.053252840560504736},{"topic":"Hospital","val":0.051117818842180245},{"topic":"Beauty","val":0.04888463520577197},{"topic":"Scenicspot","val":0.04873739232864611},{"topic":"Education","val":0.04616064197894427},{"topic":"Enterprise","val":0.04586615622469263},{"topic":"Food","val":0.03931384819259356},{"topic":"Government","val":0.039240226754030647}]},{"site":"12701","startTime":"2014-01-14 08:58:10.35","endTime":"2014-01-14 09:14:33.07","stopTime":982,"isStop":false,"latitude":28.0227,"longitude":120.6568,"topics":[{"topic":"Government","val":0.1808510638297896},{"topic":"Hospital","val":0.10868699510752099},{"topic":"Shop","val":0.052139037433155594},{"topic":"Education","val":0.04929457276140686},{"topic":"Finance","val":0.0477016725452276},{"topic":"Life","val":0.046649220616680566},{"topic":"Scenicspot","val":0.0463078848560707},{"topic":"Uptown","val":0.045482990101263596},{"topic":"Hotel","val":0.045482990101263596},{"topic":"Enterprise","val":0.04545454545454611},{"topic":"Traffic","val":0.04545454545454611},{"topic":"Food","val":0.04545454545454611},{"topic":"Beauty","val":0.04545454545454611}]},{"site":"10321","startTime":"2014-01-14 09:14:33.07","endTime":"2014-01-14 09:21:50.2","stopTime":437,"isStop":false,"latitude":28.0233,"longitude":120.659,"topics":[{"topic":"Traffic","val":0.09681219171022658},{"topic":"Life","val":0.08810032148028239},{"topic":"Shop","val":0.07634543178973768},{"topic":"Uptown","val":0.07359689808338926},{"topic":"Hotel","val":0.05379273110996605},{"topic":"Finance","val":0.053252840560504736},{"topic":"Hospital","val":0.051117818842180245},{"topic":"Beauty","val":0.04888463520577197},{"topic":"Scenicspot","val":0.04873739232864611},{"topic":"Education","val":0.04616064197894427},{"topic":"Enterprise","val":0.04586615622469263},{"topic":"Food","val":0.03931384819259356},{"topic":"Government","val":0.039240226754030647}]},{"site":"12701","startTime":"2014-01-14 09:21:50.2","endTime":"2014-01-14 09:23:58.03","stopTime":127,"isStop":false,"latitude":28.0227,"longitude":120.6568,"topics":[{"topic":"Government","val":0.1808510638297896},{"topic":"Hospital","val":0.10868699510752099},{"topic":"Shop","val":0.052139037433155594},{"topic":"Education","val":0.04929457276140686},{"topic":"Finance","val":0.0477016725452276},{"topic":"Life","val":0.046649220616680566},{"topic":"Scenicspot","val":0.0463078848560707},{"topic":"Uptown","val":0.045482990101263596},{"topic":"Hotel","val":0.045482990101263596},{"topic":"Enterprise","val":0.04545454545454611},{"topic":"Traffic","val":0.04545454545454611},{"topic":"Food","val":0.04545454545454611},{"topic":"Beauty","val":0.04545454545454611}]},{"site":"10321","startTime":"2014-01-14 09:23:58.03","endTime":"2014-01-14 09:43:03.38","stopTime":1145,"isStop":false,"latitude":28.0233,"longitude":120.659,"topics":[{"topic":"Traffic","val":0.09681219171022658},{"topic":"Life","val":0.08810032148028239},{"topic":"Shop","val":0.07634543178973768},{"topic":"Uptown","val":0.07359689808338926},{"topic":"Hotel","val":0.05379273110996605},{"topic":"Finance","val":0.053252840560504736},{"topic":"Hospital","val":0.051117818842180245},{"topic":"Beauty","val":0.04888463520577197},{"topic":"Scenicspot","val":0.04873739232864611},{"topic":"Education","val":0.04616064197894427},{"topic":"Enterprise","val":0.04586615622469263},{"topic":"Food","val":0.03931384819259356},{"topic":"Government","val":0.039240226754030647}]},{"site":"12701","startTime":"2014-01-14 09:43:03.38","endTime":"2014-01-14 09:46:38.54","stopTime":215,"isStop":false,"latitude":28.0227,"longitude":120.6568,"topics":[{"topic":"Government","val":0.1808510638297896},{"topic":"Hospital","val":0.10868699510752099},{"topic":"Shop","val":0.052139037433155594},{"topic":"Education","val":0.04929457276140686},{"topic":"Finance","val":0.0477016725452276},{"topic":"Life","val":0.046649220616680566},{"topic":"Scenicspot","val":0.0463078848560707},{"topic":"Uptown","val":0.045482990101263596},{"topic":"Hotel","val":0.045482990101263596},{"topic":"Enterprise","val":0.04545454545454611},{"topic":"Traffic","val":0.04545454545454611},{"topic":"Food","val":0.04545454545454611},{"topic":"Beauty","val":0.04545454545454611}]},{"site":"10321","startTime":"2014-01-14 09:46:38.54","endTime":"2014-01-14 09:48:30.89","stopTime":112,"isStop":false,"latitude":28.0233,"longitude":120.659,"topics":[{"topic":"Traffic","val":0.09681219171022658},{"topic":"Life","val":0.08810032148028239},{"topic":"Shop","val":0.07634543178973768},{"topic":"Uptown","val":0.07359689808338926},{"topic":"Hotel","val":0.05379273110996605},{"topic":"Finance","val":0.053252840560504736},{"topic":"Hospital","val":0.051117818842180245},{"topic":"Beauty","val":0.04888463520577197},{"topic":"Scenicspot","val":0.04873739232864611},{"topic":"Education","val":0.04616064197894427},{"topic":"Enterprise","val":0.04586615622469263},{"topic":"Food","val":0.03931384819259356},{"topic":"Government","val":0.039240226754030647}]},{"site":"24425","startTime":"2014-01-14 09:48:30.89","endTime":"2014-01-14 09:49:19.07","stopTime":48,"isStop":false,"latitude":28.0272,"longitude":120.6511,"topics":[{"topic":"Beauty","val":0.1196950733871887},{"topic":"Scenicspot","val":0.07432586187279619},{"topic":"Education","val":0.0732734099442491},{"topic":"Hotel","val":0.06488223916259026},{"topic":"Life","val":0.06115599044259911},{"topic":"Traffic","val":0.05762885424963063},{"topic":"Food","val":0.05603595403345132},{"topic":"Enterprise","val":0.05321993400842005},{"topic":"Uptown","val":0.05304926612811518},{"topic":"Hospital","val":0.050859028330868666},{"topic":"Finance","val":0.04809989759927239},{"topic":"Shop","val":0.04545454545454611},{"topic":"Government","val":0.04545454545454611}]},{"site":"10321","startTime":"2014-01-14 09:49:19.07","endTime":"2014-01-14 09:50:01.51","stopTime":42,"isStop":false,"latitude":28.0233,"longitude":120.659,"topics":[{"topic":"Traffic","val":0.09681219171022658},{"topic":"Life","val":0.08810032148028239},{"topic":"Shop","val":0.07634543178973768},{"topic":"Uptown","val":0.07359689808338926},{"topic":"Hotel","val":0.05379273110996605},{"topic":"Finance","val":0.053252840560504736},{"topic":"Hospital","val":0.051117818842180245},{"topic":"Beauty","val":0.04888463520577197},{"topic":"Scenicspot","val":0.04873739232864611},{"topic":"Education","val":0.04616064197894427},{"topic":"Enterprise","val":0.04586615622469263},{"topic":"Food","val":0.03931384819259356},{"topic":"Government","val":0.039240226754030647}]},{"site":"12701","startTime":"2014-01-14 09:50:01.51","endTime":"2014-01-14 10:09:54.57","stopTime":1193,"isStop":false,"latitude":28.0227,"longitude":120.6568,"topics":[{"topic":"Government","val":0.1808510638297896},{"topic":"Hospital","val":0.10868699510752099},{"topic":"Shop","val":0.052139037433155594},{"topic":"Education","val":0.04929457276140686},{"topic":"Finance","val":0.0477016725452276},{"topic":"Life","val":0.046649220616680566},{"topic":"Scenicspot","val":0.0463078848560707},{"topic":"Uptown","val":0.045482990101263596},{"topic":"Hotel","val":0.045482990101263596},{"topic":"Enterprise","val":0.04545454545454611},{"topic":"Traffic","val":0.04545454545454611},{"topic":"Food","val":0.04545454545454611},{"topic":"Beauty","val":0.04545454545454611}]},{"site":"10321","startTime":"2014-01-14 10:09:54.57","endTime":"2014-01-14 10:10:40.34","stopTime":45,"isStop":false,"latitude":28.0233,"longitude":120.659,"topics":[{"topic":"Traffic","val":0.09681219171022658},{"topic":"Life","val":0.08810032148028239},{"topic":"Shop","val":0.07634543178973768},{"topic":"Uptown","val":0.07359689808338926},{"topic":"Hotel","val":0.05379273110996605},{"topic":"Finance","val":0.053252840560504736},{"topic":"Hospital","val":0.051117818842180245},{"topic":"Beauty","val":0.04888463520577197},{"topic":"Scenicspot","val":0.04873739232864611},{"topic":"Education","val":0.04616064197894427},{"topic":"Enterprise","val":0.04586615622469263},{"topic":"Food","val":0.03931384819259356},{"topic":"Government","val":0.039240226754030647}]},{"site":"12701","startTime":"2014-01-14 10:10:40.34","endTime":"2014-01-14 10:10:45.03","stopTime":4,"isStop":false,"latitude":28.0227,"longitude":120.6568,"topics":[{"topic":"Government","val":0.1808510638297896},{"topic":"Hospital","val":0.10868699510752099},{"topic":"Shop","val":0.052139037433155594},{"topic":"Education","val":0.04929457276140686},{"topic":"Finance","val":0.0477016725452276},{"topic":"Life","val":0.046649220616680566},{"topic":"Scenicspot","val":0.0463078848560707},{"topic":"Uptown","val":0.045482990101263596},{"topic":"Hotel","val":0.045482990101263596},{"topic":"Enterprise","val":0.04545454545454611},{"topic":"Traffic","val":0.04545454545454611},{"topic":"Food","val":0.04545454545454611},{"topic":"Beauty","val":0.04545454545454611}]},{"site":"10321","startTime":"2014-01-14 10:10:45.03","endTime":"2014-01-14 10:19:43.14","stopTime":538,"isStop":false,"latitude":28.0233,"longitude":120.659,"topics":[{"topic":"Traffic","val":0.09681219171022658},{"topic":"Life","val":0.08810032148028239},{"topic":"Shop","val":0.07634543178973768},{"topic":"Uptown","val":0.07359689808338926},{"topic":"Hotel","val":0.05379273110996605},{"topic":"Finance","val":0.053252840560504736},{"topic":"Hospital","val":0.051117818842180245},{"topic":"Beauty","val":0.04888463520577197},{"topic":"Scenicspot","val":0.04873739232864611},{"topic":"Education","val":0.04616064197894427},{"topic":"Enterprise","val":0.04586615622469263},{"topic":"Food","val":0.03931384819259356},{"topic":"Government","val":0.039240226754030647}]},{"site":"12701","startTime":"2014-01-14 10:19:43.14","endTime":"2014-01-14 10:20:57.44","stopTime":74,"isStop":false,"latitude":28.0227,"longitude":120.6568,"topics":[{"topic":"Government","val":0.1808510638297896},{"topic":"Hospital","val":0.10868699510752099},{"topic":"Shop","val":0.052139037433155594},{"topic":"Education","val":0.04929457276140686},{"topic":"Finance","val":0.0477016725452276},{"topic":"Life","val":0.046649220616680566},{"topic":"Scenicspot","val":0.0463078848560707},{"topic":"Uptown","val":0.045482990101263596},{"topic":"Hotel","val":0.045482990101263596},{"topic":"Enterprise","val":0.04545454545454611},{"topic":"Traffic","val":0.04545454545454611},{"topic":"Food","val":0.04545454545454611},{"topic":"Beauty","val":0.04545454545454611}]},{"site":"10321","startTime":"2014-01-14 10:20:57.44","endTime":"2014-01-14 10:21:15.66","stopTime":18,"isStop":false,"latitude":28.0233,"longitude":120.659,"topics":[{"topic":"Traffic","val":0.09681219171022658},{"topic":"Life","val":0.08810032148028239},{"topic":"Shop","val":0.07634543178973768},{"topic":"Uptown","val":0.07359689808338926},{"topic":"Hotel","val":0.05379273110996605},{"topic":"Finance","val":0.053252840560504736},{"topic":"Hospital","val":0.051117818842180245},{"topic":"Beauty","val":0.04888463520577197},{"topic":"Scenicspot","val":0.04873739232864611},{"topic":"Education","val":0.04616064197894427},{"topic":"Enterprise","val":0.04586615622469263},{"topic":"Food","val":0.03931384819259356},{"topic":"Government","val":0.039240226754030647}]},{"site":"12701","startTime":"2014-01-14 10:21:15.66","endTime":"2014-01-14 10:21:15.66","stopTime":0,"isStop":true,"stoppoint":1,"latitude":28.0227,"longitude":120.6568,"topics":[{"topic":"Government","val":0.1808510638297896},{"topic":"Hospital","val":0.10868699510752099},{"topic":"Shop","val":0.052139037433155594},{"topic":"Education","val":0.04929457276140686},{"topic":"Finance","val":0.0477016725452276},{"topic":"Life","val":0.046649220616680566},{"topic":"Scenicspot","val":0.0463078848560707},{"topic":"Uptown","val":0.045482990101263596},{"topic":"Hotel","val":0.045482990101263596},{"topic":"Enterprise","val":0.04545454545454611},{"topic":"Traffic","val":0.04545454545454611},{"topic":"Food","val":0.04545454545454611},{"topic":"Beauty","val":0.04545454545454611}]}],"matching":true},{"pid":"460000102835571","traj":[{"site":"4249","startTime":"2014-01-14 04:34:14.38","endTime":"2014-01-14 06:10:41.24","stopTime":5786,"isStop":true,"stoppoint":0,"latitude":28.0051,"longitude":120.6387,"topics":[{"topic":"Hospital","val":0.1849773153942428},{"topic":"Uptown","val":0.11219102002503129},{"topic":"Government","val":0.07738188360450564},{"topic":"Education","val":0.06979427409261577},{"topic":"Traffic","val":0.06244133291614518},{"topic":"Enterprise","val":0.056300844806007506},{"topic":"Hotel","val":0.05010168961201502},{"topic":"Food","val":0.040714956195244054},{"topic":"Scenicspot","val":0.03928739048811014},{"topic":"Beauty","val":0.03408557571964956},{"topic":"Life","val":0.032110450563204},{"topic":"Shop","val":0.03126955569461827},{"topic":"Finance","val":0.03125}]},{"site":"19696","startTime":"2014-01-14 06:10:41.24","endTime":"2014-01-14 06:44:47.89","stopTime":2046,"isStop":true,"latitude":28.0048,"longitude":120.6409,"topics":[{"topic":"Life","val":0.07431586780773256},{"topic":"Food","val":0.07042587017555732},{"topic":"Shop","val":0.061901701451138366},{"topic":"Beauty","val":0.06054865879646868},{"topic":"Hotel","val":0.05828231234989696},{"topic":"Education","val":0.05747048675709515},{"topic":"Hospital","val":0.057335182491628174},{"topic":"Finance","val":0.05679396542976032},{"topic":"Uptown","val":0.05412170618678763},{"topic":"Scenicspot","val":0.05408788012042089},{"topic":"Government","val":0.05408788012042089},{"topic":"Enterprise","val":0.05405405405405415},{"topic":"Traffic","val":0.05405405405405415}]},{"site":"3063","startTime":"2014-01-14 06:44:47.89","endTime":"2014-01-14 07:31:05.6","stopTime":2777,"isStop":true,"latitude":28.0076,"longitude":120.6368,"topics":[{"topic":"Shop","val":0.0659038083318436},{"topic":"Finance","val":0.0603611657428928},{"topic":"Life","val":0.057357411049526005},{"topic":"Beauty","val":0.057178616127301796},{"topic":"Education","val":0.057178616127301796},{"topic":"Enterprise","val":0.05714285714285696},{"topic":"Traffic","val":0.05714285714285696},{"topic":"Scenicspot","val":0.05714285714285696},{"topic":"Uptown","val":0.05714285714285696},{"topic":"Food","val":0.05714285714285696},{"topic":"Hospital","val":0.05714285714285696},{"topic":"Hotel","val":0.05714285714285696},{"topic":"Government","val":0.05714285714285696}]},{"site":"4249","startTime":"2014-01-14 07:31:05.6","endTime":"2014-01-14 07:31:13.56","stopTime":7,"isStop":false,"latitude":28.0051,"longitude":120.6387,"topics":[{"topic":"Hospital","val":0.1849773153942428},{"topic":"Uptown","val":0.11219102002503129},{"topic":"Government","val":0.07738188360450564},{"topic":"Education","val":0.06979427409261577},{"topic":"Traffic","val":0.06244133291614518},{"topic":"Enterprise","val":0.056300844806007506},{"topic":"Hotel","val":0.05010168961201502},{"topic":"Food","val":0.040714956195244054},{"topic":"Scenicspot","val":0.03928739048811014},{"topic":"Beauty","val":0.03408557571964956},{"topic":"Life","val":0.032110450563204},{"topic":"Shop","val":0.03126955569461827},{"topic":"Finance","val":0.03125}]},{"site":"3063","startTime":"2014-01-14 07:31:13.56","endTime":"2014-01-14 07:31:31.85","stopTime":18,"isStop":false,"latitude":28.0076,"longitude":120.6368,"topics":[{"topic":"Shop","val":0.0659038083318436},{"topic":"Finance","val":0.0603611657428928},{"topic":"Life","val":0.057357411049526005},{"topic":"Beauty","val":0.057178616127301796},{"topic":"Education","val":0.057178616127301796},{"topic":"Enterprise","val":0.05714285714285696},{"topic":"Traffic","val":0.05714285714285696},{"topic":"Scenicspot","val":0.05714285714285696},{"topic":"Uptown","val":0.05714285714285696},{"topic":"Food","val":0.05714285714285696},{"topic":"Hospital","val":0.05714285714285696},{"topic":"Hotel","val":0.05714285714285696},{"topic":"Government","val":0.05714285714285696}]},{"site":"4249","startTime":"2014-01-14 07:31:31.85","endTime":"2014-01-14 07:31:31.89","stopTime":0,"isStop":true,"stoppoint":1,"latitude":28.0051,"longitude":120.6387,"topics":[{"topic":"Hospital","val":0.1849773153942428},{"topic":"Uptown","val":0.11219102002503129},{"topic":"Government","val":0.07738188360450564},{"topic":"Education","val":0.06979427409261577},{"topic":"Traffic","val":0.06244133291614518},{"topic":"Enterprise","val":0.056300844806007506},{"topic":"Hotel","val":0.05010168961201502},{"topic":"Food","val":0.040714956195244054},{"topic":"Scenicspot","val":0.03928739048811014},{"topic":"Beauty","val":0.03408557571964956},{"topic":"Life","val":0.032110450563204},{"topic":"Shop","val":0.03126955569461827},{"topic":"Finance","val":0.03125}]}],"matching":true},{"pid":"460000102840173","traj":[{"site":"20480","startTime":"2014-01-14 02:42:12.75","endTime":"2014-01-14 03:04:01.77","stopTime":1309,"isStop":true,"stoppoint":0,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 03:04:01.77","endTime":"2014-01-14 03:08:00.17","stopTime":238,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"28699","startTime":"2014-01-14 03:08:00.17","endTime":"2014-01-14 03:11:03.4","stopTime":183,"isStop":false,"latitude":27.9959,"longitude":120.6442,"topics":[{"topic":"Finance","val":0.05882352941176564},{"topic":"Enterprise","val":0.05882352941176564},{"topic":"Traffic","val":0.05882352941176564},{"topic":"Scenicspot","val":0.05882352941176564},{"topic":"Uptown","val":0.05882352941176564},{"topic":"Food","val":0.05882352941176564},{"topic":"Hospital","val":0.05882352941176564},{"topic":"Life","val":0.05882352941176564},{"topic":"Hotel","val":0.05882352941176564},{"topic":"Shop","val":0.05882352941176564},{"topic":"Government","val":0.05882352941176564},{"topic":"Beauty","val":0.05882352941176564},{"topic":"Education","val":0.05882352941176564}]},{"site":"25845","startTime":"2014-01-14 03:11:03.4","endTime":"2014-01-14 03:21:49.68","stopTime":646,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"8247","startTime":"2014-01-14 03:21:49.68","endTime":"2014-01-14 03:34:01.77","stopTime":732,"isStop":false,"latitude":27.9963,"longitude":120.6444,"topics":[{"topic":"Scenicspot","val":0.11418878861735185},{"topic":"Education","val":0.0798037019959168},{"topic":"Traffic","val":0.06541071075686784},{"topic":"Hospital","val":0.055332323298861244},{"topic":"Uptown","val":0.053652592055860135},{"topic":"Hotel","val":0.052664514854094784},{"topic":"Finance","val":0.052631578947369265},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Food","val":0.052631578947369265},{"topic":"Life","val":0.052631578947369265},{"topic":"Shop","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265},{"topic":"Beauty","val":0.052631578947369265}]},{"site":"10878","startTime":"2014-01-14 03:34:01.77","endTime":"2014-01-14 03:39:27.6","stopTime":325,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"28699","startTime":"2014-01-14 03:39:27.6","endTime":"2014-01-14 03:40:00.56","stopTime":32,"isStop":false,"latitude":27.9959,"longitude":120.6442,"topics":[{"topic":"Finance","val":0.05882352941176564},{"topic":"Enterprise","val":0.05882352941176564},{"topic":"Traffic","val":0.05882352941176564},{"topic":"Scenicspot","val":0.05882352941176564},{"topic":"Uptown","val":0.05882352941176564},{"topic":"Food","val":0.05882352941176564},{"topic":"Hospital","val":0.05882352941176564},{"topic":"Life","val":0.05882352941176564},{"topic":"Hotel","val":0.05882352941176564},{"topic":"Shop","val":0.05882352941176564},{"topic":"Government","val":0.05882352941176564},{"topic":"Beauty","val":0.05882352941176564},{"topic":"Education","val":0.05882352941176564}]},{"site":"25845","startTime":"2014-01-14 03:40:00.56","endTime":"2014-01-14 03:41:35.83","stopTime":95,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 03:41:35.83","endTime":"2014-01-14 03:43:23.73","stopTime":107,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 03:43:23.73","endTime":"2014-01-14 03:43:52.37","stopTime":28,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"382","startTime":"2014-01-14 03:43:52.37","endTime":"2014-01-14 03:44:37.01","stopTime":44,"isStop":false,"latitude":28.0029,"longitude":120.6492,"topics":[{"topic":"Education","val":0.2624340207868537},{"topic":"Beauty","val":0.08494313544104078},{"topic":"Hospital","val":0.06468683680687855},{"topic":"Government","val":0.06441475757740756},{"topic":"Food","val":0.05206236055939487},{"topic":"Hotel","val":0.04863416226805244},{"topic":"Shop","val":0.04647113239375306},{"topic":"Finance","val":0.04547804320618178},{"topic":"Uptown","val":0.04425368667355937},{"topic":"Enterprise","val":0.035356695869837515},{"topic":"Traffic","val":0.03162921042607622},{"topic":"Life","val":0.02609239810632856},{"topic":"Scenicspot","val":0.024908853458127024}]},{"site":"25845","startTime":"2014-01-14 03:44:37.01","endTime":"2014-01-14 03:45:17.05","stopTime":40,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"10878","startTime":"2014-01-14 03:45:17.05","endTime":"2014-01-14 03:51:40.12","stopTime":383,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"20480","startTime":"2014-01-14 03:51:40.12","endTime":"2014-01-14 03:51:50.97","stopTime":10,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"10878","startTime":"2014-01-14 03:51:50.97","endTime":"2014-01-14 03:53:01.82","stopTime":70,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"20480","startTime":"2014-01-14 03:53:01.82","endTime":"2014-01-14 03:53:21.07","stopTime":19,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 03:53:21.07","endTime":"2014-01-14 03:54:37.19","stopTime":76,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 03:54:37.19","endTime":"2014-01-14 04:00:14.39","stopTime":337,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"382","startTime":"2014-01-14 04:00:14.39","endTime":"2014-01-14 04:02:19.01","stopTime":124,"isStop":false,"latitude":28.0029,"longitude":120.6492,"topics":[{"topic":"Education","val":0.2624340207868537},{"topic":"Beauty","val":0.08494313544104078},{"topic":"Hospital","val":0.06468683680687855},{"topic":"Government","val":0.06441475757740756},{"topic":"Food","val":0.05206236055939487},{"topic":"Hotel","val":0.04863416226805244},{"topic":"Shop","val":0.04647113239375306},{"topic":"Finance","val":0.04547804320618178},{"topic":"Uptown","val":0.04425368667355937},{"topic":"Enterprise","val":0.035356695869837515},{"topic":"Traffic","val":0.03162921042607622},{"topic":"Life","val":0.02609239810632856},{"topic":"Scenicspot","val":0.024908853458127024}]},{"site":"20480","startTime":"2014-01-14 04:02:19.01","endTime":"2014-01-14 04:02:28.12","stopTime":9,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"10878","startTime":"2014-01-14 04:02:28.12","endTime":"2014-01-14 04:02:51.81","stopTime":23,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"25845","startTime":"2014-01-14 04:02:51.81","endTime":"2014-01-14 04:02:56.81","stopTime":5,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"8247","startTime":"2014-01-14 04:02:56.81","endTime":"2014-01-14 04:07:51.73","stopTime":294,"isStop":false,"latitude":27.9963,"longitude":120.6444,"topics":[{"topic":"Scenicspot","val":0.11418878861735185},{"topic":"Education","val":0.0798037019959168},{"topic":"Traffic","val":0.06541071075686784},{"topic":"Hospital","val":0.055332323298861244},{"topic":"Uptown","val":0.053652592055860135},{"topic":"Hotel","val":0.052664514854094784},{"topic":"Finance","val":0.052631578947369265},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Food","val":0.052631578947369265},{"topic":"Life","val":0.052631578947369265},{"topic":"Shop","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265},{"topic":"Beauty","val":0.052631578947369265}]},{"site":"10878","startTime":"2014-01-14 04:07:51.73","endTime":"2014-01-14 04:15:24.63","stopTime":452,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"25845","startTime":"2014-01-14 04:15:24.63","endTime":"2014-01-14 04:15:35.16","stopTime":10,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 04:15:35.16","endTime":"2014-01-14 04:15:41.3","stopTime":6,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"382","startTime":"2014-01-14 04:15:41.3","endTime":"2014-01-14 04:16:37.77","stopTime":56,"isStop":false,"latitude":28.0029,"longitude":120.6492,"topics":[{"topic":"Education","val":0.2624340207868537},{"topic":"Beauty","val":0.08494313544104078},{"topic":"Hospital","val":0.06468683680687855},{"topic":"Government","val":0.06441475757740756},{"topic":"Food","val":0.05206236055939487},{"topic":"Hotel","val":0.04863416226805244},{"topic":"Shop","val":0.04647113239375306},{"topic":"Finance","val":0.04547804320618178},{"topic":"Uptown","val":0.04425368667355937},{"topic":"Enterprise","val":0.035356695869837515},{"topic":"Traffic","val":0.03162921042607622},{"topic":"Life","val":0.02609239810632856},{"topic":"Scenicspot","val":0.024908853458127024}]},{"site":"20480","startTime":"2014-01-14 04:16:37.77","endTime":"2014-01-14 04:17:48.75","stopTime":70,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 04:17:48.75","endTime":"2014-01-14 04:18:00.94","stopTime":12,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 04:18:00.94","endTime":"2014-01-14 04:21:07.76","stopTime":186,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"382","startTime":"2014-01-14 04:21:07.76","endTime":"2014-01-14 04:21:12.6","stopTime":4,"isStop":false,"latitude":28.0029,"longitude":120.6492,"topics":[{"topic":"Education","val":0.2624340207868537},{"topic":"Beauty","val":0.08494313544104078},{"topic":"Hospital","val":0.06468683680687855},{"topic":"Government","val":0.06441475757740756},{"topic":"Food","val":0.05206236055939487},{"topic":"Hotel","val":0.04863416226805244},{"topic":"Shop","val":0.04647113239375306},{"topic":"Finance","val":0.04547804320618178},{"topic":"Uptown","val":0.04425368667355937},{"topic":"Enterprise","val":0.035356695869837515},{"topic":"Traffic","val":0.03162921042607622},{"topic":"Life","val":0.02609239810632856},{"topic":"Scenicspot","val":0.024908853458127024}]},{"site":"20480","startTime":"2014-01-14 04:21:12.6","endTime":"2014-01-14 04:21:40.61","stopTime":28,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 04:21:40.61","endTime":"2014-01-14 04:24:29.79","stopTime":169,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"10878","startTime":"2014-01-14 04:24:29.79","endTime":"2014-01-14 04:24:36.13","stopTime":6,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"20480","startTime":"2014-01-14 04:24:36.13","endTime":"2014-01-14 04:25:04.96","stopTime":28,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 04:25:04.96","endTime":"2014-01-14 04:30:51.47","stopTime":346,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 04:30:51.47","endTime":"2014-01-14 04:32:33.03","stopTime":101,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"14848","startTime":"2014-01-14 04:32:33.03","endTime":"2014-01-14 04:34:57.37","stopTime":144,"isStop":false,"latitude":28.0007,"longitude":120.6432,"topics":[{"topic":"Traffic","val":0.07272980114031524},{"topic":"Food","val":0.07196495619524494},{"topic":"Beauty","val":0.06219580030593891},{"topic":"Scenicspot","val":0.06125712696426177},{"topic":"Hotel","val":0.060214156584620586},{"topic":"Hospital","val":0.05878876373244426},{"topic":"Uptown","val":0.05649422889723358},{"topic":"Education","val":0.05628563482130533},{"topic":"Life","val":0.05559032123487788},{"topic":"Finance","val":0.05559032123487787},{"topic":"Enterprise","val":0.0555555555555565},{"topic":"Shop","val":0.0555555555555565},{"topic":"Government","val":0.0555555555555565}]},{"site":"25845","startTime":"2014-01-14 04:34:57.37","endTime":"2014-01-14 04:35:14.01","stopTime":16,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"8247","startTime":"2014-01-14 04:35:14.01","endTime":"2014-01-14 04:39:15.68","stopTime":241,"isStop":false,"latitude":27.9963,"longitude":120.6444,"topics":[{"topic":"Scenicspot","val":0.11418878861735185},{"topic":"Education","val":0.0798037019959168},{"topic":"Traffic","val":0.06541071075686784},{"topic":"Hospital","val":0.055332323298861244},{"topic":"Uptown","val":0.053652592055860135},{"topic":"Hotel","val":0.052664514854094784},{"topic":"Finance","val":0.052631578947369265},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Food","val":0.052631578947369265},{"topic":"Life","val":0.052631578947369265},{"topic":"Shop","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265},{"topic":"Beauty","val":0.052631578947369265}]},{"site":"25845","startTime":"2014-01-14 04:39:15.68","endTime":"2014-01-14 04:41:11.66","stopTime":115,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"10878","startTime":"2014-01-14 04:41:11.66","endTime":"2014-01-14 04:57:47.35","stopTime":995,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"20480","startTime":"2014-01-14 04:57:47.35","endTime":"2014-01-14 04:57:52.15","stopTime":4,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 04:57:52.15","endTime":"2014-01-14 04:58:13.53","stopTime":21,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"14565","startTime":"2014-01-14 04:58:13.53","endTime":"2014-01-14 04:58:25.21","stopTime":11,"isStop":false,"latitude":28.0031,"longitude":120.6539,"topics":[{"topic":"Shop","val":0.08099868425275235},{"topic":"Hospital","val":0.07451622220082918},{"topic":"Finance","val":0.06145502390809034},{"topic":"Education","val":0.05279034690799387},{"topic":"Life","val":0.05250152434132395},{"topic":"Beauty","val":0.05141041686723774},{"topic":"Scenicspot","val":0.05134623407464442},{"topic":"Food","val":0.05131414267834777},{"topic":"Enterprise","val":0.05128205128205112},{"topic":"Traffic","val":0.05128205128205112},{"topic":"Uptown","val":0.05128205128205112},{"topic":"Hotel","val":0.05128205128205112},{"topic":"Government","val":0.05128205128205112}]},{"site":"20480","startTime":"2014-01-14 04:58:25.21","endTime":"2014-01-14 05:00:58.56","stopTime":153,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 05:00:58.56","endTime":"2014-01-14 05:01:45.86","stopTime":47,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"28699","startTime":"2014-01-14 05:01:45.86","endTime":"2014-01-14 05:02:04.97","stopTime":19,"isStop":false,"latitude":27.9959,"longitude":120.6442,"topics":[{"topic":"Finance","val":0.05882352941176564},{"topic":"Enterprise","val":0.05882352941176564},{"topic":"Traffic","val":0.05882352941176564},{"topic":"Scenicspot","val":0.05882352941176564},{"topic":"Uptown","val":0.05882352941176564},{"topic":"Food","val":0.05882352941176564},{"topic":"Hospital","val":0.05882352941176564},{"topic":"Life","val":0.05882352941176564},{"topic":"Hotel","val":0.05882352941176564},{"topic":"Shop","val":0.05882352941176564},{"topic":"Government","val":0.05882352941176564},{"topic":"Beauty","val":0.05882352941176564},{"topic":"Education","val":0.05882352941176564}]},{"site":"25845","startTime":"2014-01-14 05:02:04.97","endTime":"2014-01-14 05:06:03.76","stopTime":238,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"8247","startTime":"2014-01-14 05:06:03.76","endTime":"2014-01-14 05:13:09.58","stopTime":425,"isStop":false,"latitude":27.9963,"longitude":120.6444,"topics":[{"topic":"Scenicspot","val":0.11418878861735185},{"topic":"Education","val":0.0798037019959168},{"topic":"Traffic","val":0.06541071075686784},{"topic":"Hospital","val":0.055332323298861244},{"topic":"Uptown","val":0.053652592055860135},{"topic":"Hotel","val":0.052664514854094784},{"topic":"Finance","val":0.052631578947369265},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Food","val":0.052631578947369265},{"topic":"Life","val":0.052631578947369265},{"topic":"Shop","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265},{"topic":"Beauty","val":0.052631578947369265}]},{"site":"25845","startTime":"2014-01-14 05:13:09.58","endTime":"2014-01-14 05:15:26.9","stopTime":137,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"8247","startTime":"2014-01-14 05:15:26.9","endTime":"2014-01-14 05:24:01.16","stopTime":514,"isStop":false,"latitude":27.9963,"longitude":120.6444,"topics":[{"topic":"Scenicspot","val":0.11418878861735185},{"topic":"Education","val":0.0798037019959168},{"topic":"Traffic","val":0.06541071075686784},{"topic":"Hospital","val":0.055332323298861244},{"topic":"Uptown","val":0.053652592055860135},{"topic":"Hotel","val":0.052664514854094784},{"topic":"Finance","val":0.052631578947369265},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Food","val":0.052631578947369265},{"topic":"Life","val":0.052631578947369265},{"topic":"Shop","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265},{"topic":"Beauty","val":0.052631578947369265}]},{"site":"382","startTime":"2014-01-14 05:24:01.16","endTime":"2014-01-14 05:25:00.22","stopTime":59,"isStop":false,"latitude":28.0029,"longitude":120.6492,"topics":[{"topic":"Education","val":0.2624340207868537},{"topic":"Beauty","val":0.08494313544104078},{"topic":"Hospital","val":0.06468683680687855},{"topic":"Government","val":0.06441475757740756},{"topic":"Food","val":0.05206236055939487},{"topic":"Hotel","val":0.04863416226805244},{"topic":"Shop","val":0.04647113239375306},{"topic":"Finance","val":0.04547804320618178},{"topic":"Uptown","val":0.04425368667355937},{"topic":"Enterprise","val":0.035356695869837515},{"topic":"Traffic","val":0.03162921042607622},{"topic":"Life","val":0.02609239810632856},{"topic":"Scenicspot","val":0.024908853458127024}]},{"site":"20480","startTime":"2014-01-14 05:25:00.22","endTime":"2014-01-14 05:26:02.29","stopTime":62,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 05:26:02.29","endTime":"2014-01-14 05:27:29.98","stopTime":87,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 05:27:29.98","endTime":"2014-01-14 05:29:05.67","stopTime":95,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 05:29:05.67","endTime":"2014-01-14 05:31:12.15","stopTime":126,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"8247","startTime":"2014-01-14 05:31:12.15","endTime":"2014-01-14 05:35:18.05","stopTime":245,"isStop":false,"latitude":27.9963,"longitude":120.6444,"topics":[{"topic":"Scenicspot","val":0.11418878861735185},{"topic":"Education","val":0.0798037019959168},{"topic":"Traffic","val":0.06541071075686784},{"topic":"Hospital","val":0.055332323298861244},{"topic":"Uptown","val":0.053652592055860135},{"topic":"Hotel","val":0.052664514854094784},{"topic":"Finance","val":0.052631578947369265},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Food","val":0.052631578947369265},{"topic":"Life","val":0.052631578947369265},{"topic":"Shop","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265},{"topic":"Beauty","val":0.052631578947369265}]},{"site":"25845","startTime":"2014-01-14 05:35:18.05","endTime":"2014-01-14 05:37:06.13","stopTime":108,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"8247","startTime":"2014-01-14 05:37:06.13","endTime":"2014-01-14 05:45:31.79","stopTime":505,"isStop":false,"latitude":27.9963,"longitude":120.6444,"topics":[{"topic":"Scenicspot","val":0.11418878861735185},{"topic":"Education","val":0.0798037019959168},{"topic":"Traffic","val":0.06541071075686784},{"topic":"Hospital","val":0.055332323298861244},{"topic":"Uptown","val":0.053652592055860135},{"topic":"Hotel","val":0.052664514854094784},{"topic":"Finance","val":0.052631578947369265},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Food","val":0.052631578947369265},{"topic":"Life","val":0.052631578947369265},{"topic":"Shop","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265},{"topic":"Beauty","val":0.052631578947369265}]},{"site":"10878","startTime":"2014-01-14 05:45:31.79","endTime":"2014-01-14 05:56:52.11","stopTime":680,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"20480","startTime":"2014-01-14 05:56:52.11","endTime":"2014-01-14 05:56:58.44","stopTime":6,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"10878","startTime":"2014-01-14 05:56:58.44","endTime":"2014-01-14 05:57:48.89","stopTime":50,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"20480","startTime":"2014-01-14 05:57:48.89","endTime":"2014-01-14 05:58:02.1","stopTime":13,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"10878","startTime":"2014-01-14 05:58:02.1","endTime":"2014-01-14 05:59:48.7","stopTime":106,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"20480","startTime":"2014-01-14 05:59:48.7","endTime":"2014-01-14 06:00:15.06","stopTime":26,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 06:00:15.06","endTime":"2014-01-14 06:02:18.79","stopTime":123,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 06:02:18.79","endTime":"2014-01-14 06:06:11.13","stopTime":232,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"10878","startTime":"2014-01-14 06:06:11.13","endTime":"2014-01-14 06:07:45.18","stopTime":94,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"20480","startTime":"2014-01-14 06:07:45.18","endTime":"2014-01-14 06:07:54.73","stopTime":9,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 06:07:54.73","endTime":"2014-01-14 06:08:40.46","stopTime":45,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 06:08:40.46","endTime":"2014-01-14 06:08:51.77","stopTime":11,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"10878","startTime":"2014-01-14 06:08:51.77","endTime":"2014-01-14 06:09:07.53","stopTime":15,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"28699","startTime":"2014-01-14 06:09:07.53","endTime":"2014-01-14 06:10:15.04","stopTime":67,"isStop":false,"latitude":27.9959,"longitude":120.6442,"topics":[{"topic":"Finance","val":0.05882352941176564},{"topic":"Enterprise","val":0.05882352941176564},{"topic":"Traffic","val":0.05882352941176564},{"topic":"Scenicspot","val":0.05882352941176564},{"topic":"Uptown","val":0.05882352941176564},{"topic":"Food","val":0.05882352941176564},{"topic":"Hospital","val":0.05882352941176564},{"topic":"Life","val":0.05882352941176564},{"topic":"Hotel","val":0.05882352941176564},{"topic":"Shop","val":0.05882352941176564},{"topic":"Government","val":0.05882352941176564},{"topic":"Beauty","val":0.05882352941176564},{"topic":"Education","val":0.05882352941176564}]},{"site":"25845","startTime":"2014-01-14 06:10:15.04","endTime":"2014-01-14 06:14:39.6","stopTime":264,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 06:14:39.6","endTime":"2014-01-14 06:17:01.31","stopTime":141,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 06:17:01.31","endTime":"2014-01-14 06:19:49.63","stopTime":168,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 06:19:49.63","endTime":"2014-01-14 06:20:28.96","stopTime":39,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 06:20:28.96","endTime":"2014-01-14 06:20:52.39","stopTime":23,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 06:20:52.39","endTime":"2014-01-14 06:24:43.86","stopTime":231,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 06:24:43.86","endTime":"2014-01-14 06:29:17.07","stopTime":273,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 06:29:17.07","endTime":"2014-01-14 06:36:41.6","stopTime":444,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 06:36:41.6","endTime":"2014-01-14 06:39:34.74","stopTime":173,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 06:39:34.74","endTime":"2014-01-14 06:40:53.87","stopTime":79,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"10878","startTime":"2014-01-14 06:40:53.87","endTime":"2014-01-14 06:41:33.9","stopTime":40,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"20480","startTime":"2014-01-14 06:41:33.9","endTime":"2014-01-14 06:41:39.96","stopTime":6,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 06:41:39.96","endTime":"2014-01-14 06:43:15.68","stopTime":95,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"10878","startTime":"2014-01-14 06:43:15.68","endTime":"2014-01-14 06:46:52.16","stopTime":216,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"20480","startTime":"2014-01-14 06:46:52.16","endTime":"2014-01-14 06:47:11.0","stopTime":18,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 06:47:11.0","endTime":"2014-01-14 06:48:08.24","stopTime":57,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 06:48:08.24","endTime":"2014-01-14 06:48:29.28","stopTime":21,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 06:48:29.28","endTime":"2014-01-14 06:49:20.89","stopTime":51,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"8247","startTime":"2014-01-14 06:49:20.89","endTime":"2014-01-14 06:59:32.08","stopTime":611,"isStop":false,"latitude":27.9963,"longitude":120.6444,"topics":[{"topic":"Scenicspot","val":0.11418878861735185},{"topic":"Education","val":0.0798037019959168},{"topic":"Traffic","val":0.06541071075686784},{"topic":"Hospital","val":0.055332323298861244},{"topic":"Uptown","val":0.053652592055860135},{"topic":"Hotel","val":0.052664514854094784},{"topic":"Finance","val":0.052631578947369265},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Food","val":0.052631578947369265},{"topic":"Life","val":0.052631578947369265},{"topic":"Shop","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265},{"topic":"Beauty","val":0.052631578947369265}]},{"site":"25845","startTime":"2014-01-14 06:59:32.08","endTime":"2014-01-14 07:00:52.55","stopTime":80,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 07:00:52.55","endTime":"2014-01-14 07:01:06.27","stopTime":13,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 07:01:06.27","endTime":"2014-01-14 07:06:10.51","stopTime":304,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"382","startTime":"2014-01-14 07:06:10.51","endTime":"2014-01-14 07:16:02.65","stopTime":592,"isStop":false,"latitude":28.0029,"longitude":120.6492,"topics":[{"topic":"Education","val":0.2624340207868537},{"topic":"Beauty","val":0.08494313544104078},{"topic":"Hospital","val":0.06468683680687855},{"topic":"Government","val":0.06441475757740756},{"topic":"Food","val":0.05206236055939487},{"topic":"Hotel","val":0.04863416226805244},{"topic":"Shop","val":0.04647113239375306},{"topic":"Finance","val":0.04547804320618178},{"topic":"Uptown","val":0.04425368667355937},{"topic":"Enterprise","val":0.035356695869837515},{"topic":"Traffic","val":0.03162921042607622},{"topic":"Life","val":0.02609239810632856},{"topic":"Scenicspot","val":0.024908853458127024}]},{"site":"25845","startTime":"2014-01-14 07:16:02.65","endTime":"2014-01-14 07:16:19.39","stopTime":16,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"8247","startTime":"2014-01-14 07:16:19.39","endTime":"2014-01-14 07:19:49.12","stopTime":209,"isStop":false,"latitude":27.9963,"longitude":120.6444,"topics":[{"topic":"Scenicspot","val":0.11418878861735185},{"topic":"Education","val":0.0798037019959168},{"topic":"Traffic","val":0.06541071075686784},{"topic":"Hospital","val":0.055332323298861244},{"topic":"Uptown","val":0.053652592055860135},{"topic":"Hotel","val":0.052664514854094784},{"topic":"Finance","val":0.052631578947369265},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Food","val":0.052631578947369265},{"topic":"Life","val":0.052631578947369265},{"topic":"Shop","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265},{"topic":"Beauty","val":0.052631578947369265}]},{"site":"382","startTime":"2014-01-14 07:19:49.12","endTime":"2014-01-14 07:19:53.73","stopTime":4,"isStop":false,"latitude":28.0029,"longitude":120.6492,"topics":[{"topic":"Education","val":0.2624340207868537},{"topic":"Beauty","val":0.08494313544104078},{"topic":"Hospital","val":0.06468683680687855},{"topic":"Government","val":0.06441475757740756},{"topic":"Food","val":0.05206236055939487},{"topic":"Hotel","val":0.04863416226805244},{"topic":"Shop","val":0.04647113239375306},{"topic":"Finance","val":0.04547804320618178},{"topic":"Uptown","val":0.04425368667355937},{"topic":"Enterprise","val":0.035356695869837515},{"topic":"Traffic","val":0.03162921042607622},{"topic":"Life","val":0.02609239810632856},{"topic":"Scenicspot","val":0.024908853458127024}]},{"site":"20480","startTime":"2014-01-14 07:19:53.73","endTime":"2014-01-14 07:19:58.55","stopTime":4,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 07:19:58.55","endTime":"2014-01-14 07:27:22.13","stopTime":443,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 07:27:22.13","endTime":"2014-01-14 07:28:11.39","stopTime":49,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 07:28:11.39","endTime":"2014-01-14 07:28:21.27","stopTime":9,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"8247","startTime":"2014-01-14 07:28:21.27","endTime":"2014-01-14 07:36:05.24","stopTime":463,"isStop":false,"latitude":27.9963,"longitude":120.6444,"topics":[{"topic":"Scenicspot","val":0.11418878861735185},{"topic":"Education","val":0.0798037019959168},{"topic":"Traffic","val":0.06541071075686784},{"topic":"Hospital","val":0.055332323298861244},{"topic":"Uptown","val":0.053652592055860135},{"topic":"Hotel","val":0.052664514854094784},{"topic":"Finance","val":0.052631578947369265},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Food","val":0.052631578947369265},{"topic":"Life","val":0.052631578947369265},{"topic":"Shop","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265},{"topic":"Beauty","val":0.052631578947369265}]},{"site":"10878","startTime":"2014-01-14 07:36:05.24","endTime":"2014-01-14 07:37:02.99","stopTime":57,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"20480","startTime":"2014-01-14 07:37:02.99","endTime":"2014-01-14 07:37:22.67","stopTime":19,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 07:37:22.67","endTime":"2014-01-14 07:41:52.92","stopTime":270,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 07:41:52.92","endTime":"2014-01-14 07:52:58.49","stopTime":665,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"10878","startTime":"2014-01-14 07:52:58.49","endTime":"2014-01-14 07:54:20.11","stopTime":81,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"20480","startTime":"2014-01-14 07:54:20.11","endTime":"2014-01-14 07:54:32.04","stopTime":11,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 07:54:32.04","endTime":"2014-01-14 07:54:36.75","stopTime":4,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"8247","startTime":"2014-01-14 07:54:36.75","endTime":"2014-01-14 08:00:41.5","stopTime":364,"isStop":false,"latitude":27.9963,"longitude":120.6444,"topics":[{"topic":"Scenicspot","val":0.11418878861735185},{"topic":"Education","val":0.0798037019959168},{"topic":"Traffic","val":0.06541071075686784},{"topic":"Hospital","val":0.055332323298861244},{"topic":"Uptown","val":0.053652592055860135},{"topic":"Hotel","val":0.052664514854094784},{"topic":"Finance","val":0.052631578947369265},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Food","val":0.052631578947369265},{"topic":"Life","val":0.052631578947369265},{"topic":"Shop","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265},{"topic":"Beauty","val":0.052631578947369265}]},{"site":"382","startTime":"2014-01-14 08:00:41.5","endTime":"2014-01-14 08:01:24.09","stopTime":42,"isStop":false,"latitude":28.0029,"longitude":120.6492,"topics":[{"topic":"Education","val":0.2624340207868537},{"topic":"Beauty","val":0.08494313544104078},{"topic":"Hospital","val":0.06468683680687855},{"topic":"Government","val":0.06441475757740756},{"topic":"Food","val":0.05206236055939487},{"topic":"Hotel","val":0.04863416226805244},{"topic":"Shop","val":0.04647113239375306},{"topic":"Finance","val":0.04547804320618178},{"topic":"Uptown","val":0.04425368667355937},{"topic":"Enterprise","val":0.035356695869837515},{"topic":"Traffic","val":0.03162921042607622},{"topic":"Life","val":0.02609239810632856},{"topic":"Scenicspot","val":0.024908853458127024}]},{"site":"8247","startTime":"2014-01-14 08:01:24.09","endTime":"2014-01-14 08:01:30.23","stopTime":6,"isStop":false,"latitude":27.9963,"longitude":120.6444,"topics":[{"topic":"Scenicspot","val":0.11418878861735185},{"topic":"Education","val":0.0798037019959168},{"topic":"Traffic","val":0.06541071075686784},{"topic":"Hospital","val":0.055332323298861244},{"topic":"Uptown","val":0.053652592055860135},{"topic":"Hotel","val":0.052664514854094784},{"topic":"Finance","val":0.052631578947369265},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Food","val":0.052631578947369265},{"topic":"Life","val":0.052631578947369265},{"topic":"Shop","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265},{"topic":"Beauty","val":0.052631578947369265}]},{"site":"25845","startTime":"2014-01-14 08:01:30.23","endTime":"2014-01-14 08:01:42.96","stopTime":12,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"10878","startTime":"2014-01-14 08:01:42.96","endTime":"2014-01-14 08:10:01.53","stopTime":498,"isStop":false,"latitude":28.0003,"longitude":120.6472,"topics":[{"topic":"Food","val":0.11189389963260434},{"topic":"Education","val":0.09796519843352477},{"topic":"Uptown","val":0.08924462029149229},{"topic":"Life","val":0.06669627356776565},{"topic":"Traffic","val":0.06257822277847265},{"topic":"Hospital","val":0.05553312608502523},{"topic":"Shop","val":0.0515967540070246},{"topic":"Enterprise","val":0.047963179781177805},{"topic":"Government","val":0.04776131454640816},{"topic":"Beauty","val":0.04640881747345444},{"topic":"Hotel","val":0.04344139852234627},{"topic":"Scenicspot","val":0.039747264726068626},{"topic":"Finance","val":0.03873793855222234}]},{"site":"25845","startTime":"2014-01-14 08:10:01.53","endTime":"2014-01-14 08:10:11.88","stopTime":10,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 08:10:11.88","endTime":"2014-01-14 08:15:45.05","stopTime":333,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"25845","startTime":"2014-01-14 08:15:45.05","endTime":"2014-01-14 08:15:48.94","stopTime":3,"isStop":false,"latitude":27.9986,"longitude":120.6404,"topics":[{"topic":"Shop","val":0.13934648039779468},{"topic":"Beauty","val":0.08742346852484538},{"topic":"Enterprise","val":0.0585529208808306},{"topic":"Uptown","val":0.04236714812434442},{"topic":"Finance","val":0.04114940973514191},{"topic":"Life","val":0.03790210736393458},{"topic":"Hotel","val":0.03057876399553506},{"topic":"Education","val":0.029344112573148937},{"topic":"Hospital","val":0.028413895748063522},{"topic":"Food","val":0.027111592192943932},{"topic":"Scenicspot","val":0.027077766126577193},{"topic":"Traffic","val":0.027043940060210447},{"topic":"Government","val":0.027027027027027074}]},{"site":"20480","startTime":"2014-01-14 08:15:48.94","endTime":"2014-01-14 08:26:45.01","stopTime":656,"isStop":false,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]},{"site":"382","startTime":"2014-01-14 08:26:45.01","endTime":"2014-01-14 08:28:16.17","stopTime":91,"isStop":false,"latitude":28.0029,"longitude":120.6492,"topics":[{"topic":"Education","val":0.2624340207868537},{"topic":"Beauty","val":0.08494313544104078},{"topic":"Hospital","val":0.06468683680687855},{"topic":"Government","val":0.06441475757740756},{"topic":"Food","val":0.05206236055939487},{"topic":"Hotel","val":0.04863416226805244},{"topic":"Shop","val":0.04647113239375306},{"topic":"Finance","val":0.04547804320618178},{"topic":"Uptown","val":0.04425368667355937},{"topic":"Enterprise","val":0.035356695869837515},{"topic":"Traffic","val":0.03162921042607622},{"topic":"Life","val":0.02609239810632856},{"topic":"Scenicspot","val":0.024908853458127024}]},{"site":"8247","startTime":"2014-01-14 08:28:16.17","endTime":"2014-01-14 08:28:43.58","stopTime":27,"isStop":false,"latitude":27.9963,"longitude":120.6444,"topics":[{"topic":"Scenicspot","val":0.11418878861735185},{"topic":"Education","val":0.0798037019959168},{"topic":"Traffic","val":0.06541071075686784},{"topic":"Hospital","val":0.055332323298861244},{"topic":"Uptown","val":0.053652592055860135},{"topic":"Hotel","val":0.052664514854094784},{"topic":"Finance","val":0.052631578947369265},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Food","val":0.052631578947369265},{"topic":"Life","val":0.052631578947369265},{"topic":"Shop","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265},{"topic":"Beauty","val":0.052631578947369265}]},{"site":"20480","startTime":"2014-01-14 08:28:43.58","endTime":"2014-01-14 08:26:45.01","stopTime":86281,"isStop":true,"stoppoint":1,"latitude":27.9961,"longitude":120.6477,"topics":[{"topic":"Hospital","val":0.14548923859742754},{"topic":"Uptown","val":0.13246065778944938},{"topic":"Beauty","val":0.07866390365005371},{"topic":"Education","val":0.07191366257001564},{"topic":"Enterprise","val":0.060136646217608204},{"topic":"Hotel","val":0.05989043681651244},{"topic":"Government","val":0.04883153121730088},{"topic":"Life","val":0.04281991834054879},{"topic":"Food","val":0.041650423685344475},{"topic":"Finance","val":0.03356654834937081},{"topic":"Scenicspot","val":0.03317671679763606},{"topic":"Traffic","val":0.0328074026959926},{"topic":"Shop","val":0.0327868852459013}]}],"matching":true},{"pid":"460000112823199","traj":[{"site":"22279","startTime":"2014-01-14 06:09:59.79","endTime":"2014-01-14 07:58:20.13","stopTime":6500,"isStop":true,"stoppoint":0,"latitude":28.0158,"longitude":120.5944,"topics":[{"topic":"Uptown","val":0.08033067650352499},{"topic":"Food","val":0.06929714775047831},{"topic":"Traffic","val":0.06844081417561503},{"topic":"Shop","val":0.05961399117317777},{"topic":"Scenicspot","val":0.058066003557078714},{"topic":"Beauty","val":0.05776958039654912},{"topic":"Hotel","val":0.056979118635136806},{"topic":"Finance","val":0.056023977340097},{"topic":"Hospital","val":0.05539819511231224},{"topic":"Education","val":0.05358672024240912},{"topic":"Life","val":0.052796258480996824},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265}]},{"site":"5291","startTime":"2014-01-14 07:58:20.13","endTime":"2014-01-14 08:09:24.95","stopTime":664,"isStop":false,"latitude":28.011,"longitude":120.6061},{"site":"20403","startTime":"2014-01-14 08:09:24.95","endTime":"2014-01-14 08:10:06.92","stopTime":41,"isStop":false,"latitude":28.0174,"longitude":120.6061,"topics":[{"topic":"Uptown","val":0.17861612730198448},{"topic":"Life","val":0.0719777272611178},{"topic":"Finance","val":0.06891267145155973},{"topic":"Education","val":0.06783990191821443},{"topic":"Traffic","val":0.06584761564200177},{"topic":"Shop","val":0.05269341779264905},{"topic":"Hospital","val":0.04901535082117966},{"topic":"Scenicspot","val":0.047304027994176524},{"topic":"Hotel","val":0.04577150008939768},{"topic":"Beauty","val":0.04367704528619991},{"topic":"Food","val":0.04091849505759799},{"topic":"Enterprise","val":0.040816326530612734},{"topic":"Government","val":0.040816326530612734}]},{"site":"22279","startTime":"2014-01-14 08:10:06.92","endTime":"2014-01-14 08:10:16.17","stopTime":9,"isStop":false,"latitude":28.0158,"longitude":120.5944,"topics":[{"topic":"Uptown","val":0.08033067650352499},{"topic":"Food","val":0.06929714775047831},{"topic":"Traffic","val":0.06844081417561503},{"topic":"Shop","val":0.05961399117317777},{"topic":"Scenicspot","val":0.058066003557078714},{"topic":"Beauty","val":0.05776958039654912},{"topic":"Hotel","val":0.056979118635136806},{"topic":"Finance","val":0.056023977340097},{"topic":"Hospital","val":0.05539819511231224},{"topic":"Education","val":0.05358672024240912},{"topic":"Life","val":0.052796258480996824},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265}]},{"site":"25872","startTime":"2014-01-14 08:10:16.17","endTime":"2014-01-14 08:10:28.18","stopTime":12,"isStop":false,"latitude":28.0227,"longitude":120.6045,"topics":[{"topic":"Beauty","val":0.07381449033514198},{"topic":"Enterprise","val":0.07348073981365641},{"topic":"Hospital","val":0.07125573633708882},{"topic":"Life","val":0.060742594910305134},{"topic":"Traffic","val":0.0575163398692815},{"topic":"Shop","val":0.05696008900013925},{"topic":"Hotel","val":0.052649144764288856},{"topic":"Finance","val":0.05189820609094704},{"topic":"Uptown","val":0.0509803921568628},{"topic":"Education","val":0.05009039076623556},{"topic":"Scenicspot","val":0.04856070087609508},{"topic":"Food","val":0.0445000695313585},{"topic":"Government","val":0.04444444444444431}]},{"site":"4909","startTime":"2014-01-14 08:10:28.18","endTime":"2014-01-14 10:10:30.42","stopTime":7202,"isStop":true,"latitude":28.0223,"longitude":120.6044,"topics":[{"topic":"Finance","val":0.1441906549853978},{"topic":"Hospital","val":0.07867473230426918},{"topic":"Hotel","val":0.06478584341538034},{"topic":"Enterprise","val":0.06342998192184673},{"topic":"Food","val":0.06217841746627724},{"topic":"Life","val":0.058058684466694474},{"topic":"Education","val":0.05133152551800871},{"topic":"Shop","val":0.04973230426922564},{"topic":"Uptown","val":0.04653386177165927},{"topic":"Beauty","val":0.04403073286052045},{"topic":"Traffic","val":0.04283131692393322},{"topic":"Scenicspot","val":0.032418995967181674},{"topic":"Government","val":0.02781254345709962}]},{"site":"58","startTime":"2014-01-14 10:10:30.42","endTime":"2014-01-14 10:14:16.82","stopTime":226,"isStop":false,"latitude":28.0271,"longitude":120.6009,"topics":[{"topic":"Enterprise","val":0.06455986649979234},{"topic":"Shop","val":0.06382978723404346},{"topic":"Finance","val":0.05899735780837252},{"topic":"Life","val":0.05565985259352062},{"topic":"Scenicspot","val":0.05559032123487788},{"topic":"Education","val":0.05559032123487787},{"topic":"Traffic","val":0.0555555555555565},{"topic":"Uptown","val":0.0555555555555565},{"topic":"Food","val":0.0555555555555565},{"topic":"Hospital","val":0.0555555555555565},{"topic":"Hotel","val":0.0555555555555565},{"topic":"Government","val":0.0555555555555565},{"topic":"Beauty","val":0.0555555555555565}]},{"site":"1654","startTime":"2014-01-14 10:14:16.82","endTime":"2014-01-14 10:23:55.72","stopTime":578,"isStop":false,"latitude":28.0246,"longitude":120.5916,"topics":[{"topic":"Traffic","val":0.07750967779491742},{"topic":"Hotel","val":0.07500654888377827},{"topic":"Shop","val":0.07468638123235354},{"topic":"Life","val":0.06449922868702042},{"topic":"Food","val":0.059754926215908395},{"topic":"Finance","val":0.05652414355153155},{"topic":"Hospital","val":0.05445760689233548},{"topic":"Scenicspot","val":0.05425386384142882},{"topic":"Beauty","val":0.05131414267834719},{"topic":"Education","val":0.05049917047472064},{"topic":"Uptown","val":0.047646767762027586},{"topic":"Government","val":0.04654073405710585},{"topic":"Enterprise","val":0.04651162790697633}]},{"site":"28685","startTime":"2014-01-14 10:23:55.72","endTime":"2014-01-14 10:24:48.85","stopTime":53,"isStop":false,"latitude":28.0234,"longitude":120.5928,"topics":[{"topic":"Hotel","val":0.12768678239103254},{"topic":"Traffic","val":0.08962289818795251},{"topic":"Government","val":0.08592262066713872},{"topic":"Food","val":0.0794471350057143},{"topic":"Scenicspot","val":0.05525929150568642},{"topic":"Beauty","val":0.05376285574359256},{"topic":"Shop","val":0.050171409914567124},{"topic":"Hospital","val":0.04905588507373346},{"topic":"Finance","val":0.04598138978070427},{"topic":"Uptown","val":0.045029112477553535},{"topic":"Education","val":0.04467540947924047},{"topic":"Life","val":0.04355988463840687},{"topic":"Enterprise","val":0.04347826086956539}]},{"site":"19703","startTime":"2014-01-14 10:24:48.85","endTime":"2014-01-14 10:29:40.01","stopTime":291,"isStop":false,"latitude":28.0238,"longitude":120.5972,"topics":[{"topic":"Shop","val":0.11757879167140732},{"topic":"Beauty","val":0.09059051086585632},{"topic":"Hospital","val":0.08619865741267581},{"topic":"Education","val":0.058459437933780965},{"topic":"Finance","val":0.050267379679144526},{"topic":"Life","val":0.03750142223233553},{"topic":"Uptown","val":0.036431903515757987},{"topic":"Hotel","val":0.036431903515757987},{"topic":"Food","val":0.036409147798383994},{"topic":"Enterprise","val":0.036363636363636015},{"topic":"Traffic","val":0.036363636363636015},{"topic":"Scenicspot","val":0.036363636363636015},{"topic":"Government","val":0.036363636363636015}]},{"site":"2795","startTime":"2014-01-14 10:29:40.01","endTime":"2014-01-14 10:36:47.0","stopTime":426,"isStop":false,"latitude":28.0244,"longitude":120.603,"topics":[{"topic":"Shop","val":0.12251425392852172},{"topic":"Finance","val":0.07731887081073538},{"topic":"Enterprise","val":0.06602388788454677},{"topic":"Hotel","val":0.055934115174832384},{"topic":"Beauty","val":0.05417265408921638},{"topic":"Food","val":0.04423739550981929},{"topic":"Hospital","val":0.03773235062346474},{"topic":"Traffic","val":0.03229345323629835},{"topic":"Uptown","val":0.02863146834778015},{"topic":"Scenicspot","val":0.026900910088227252},{"topic":"Education","val":0.025881116828133675},{"topic":"Life","val":0.025402123024150267},{"topic":"Government","val":0.024691358024691124}]},{"site":"5694","startTime":"2014-01-14 10:36:47.0","endTime":"2014-01-14 10:46:18.05","stopTime":571,"isStop":false,"latitude":28.0189,"longitude":120.5962,"topics":[{"topic":"Beauty","val":0.10421881518564878},{"topic":"Uptown","val":0.09407592824363795},{"topic":"Hotel","val":0.0828379224030039},{"topic":"Hospital","val":0.07994367959950013},{"topic":"Shop","val":0.06664580725907367},{"topic":"Finance","val":0.051731330830204286},{"topic":"Food","val":0.05058406341259901},{"topic":"Education","val":0.044169795577805766},{"topic":"Life","val":0.04278785982478127},{"topic":"Scenicspot","val":0.04169274092615806},{"topic":"Enterprise","val":0.04169274092615805},{"topic":"Government","val":0.04169274092615805},{"topic":"Traffic","val":0.041666666666667025}]},{"site":"8821","startTime":"2014-01-14 10:46:18.05","endTime":"2014-01-14 10:46:37.84","stopTime":19,"isStop":false,"latitude":28.0211,"longitude":120.601,"topics":[{"topic":"Hotel","val":0.11627945527936287},{"topic":"Shop","val":0.11145554459630719},{"topic":"Hospital","val":0.059577783487636975},{"topic":"Traffic","val":0.056668517766412733},{"topic":"Life","val":0.056030302779135566},{"topic":"Government","val":0.05252426460227516},{"topic":"Beauty","val":0.049150842526667675},{"topic":"Finance","val":0.043580966274067275},{"topic":"Enterprise","val":0.04223822824888691},{"topic":"Education","val":0.034405589768667436},{"topic":"Food","val":0.029822045769131677},{"topic":"Uptown","val":0.02968942966787925},{"topic":"Scenicspot","val":0.01847508060572383}]},{"site":"22279","startTime":"2014-01-14 10:46:37.84","endTime":"2014-01-14 10:52:49.99","stopTime":372,"isStop":false,"latitude":28.0158,"longitude":120.5944,"topics":[{"topic":"Uptown","val":0.08033067650352499},{"topic":"Food","val":0.06929714775047831},{"topic":"Traffic","val":0.06844081417561503},{"topic":"Shop","val":0.05961399117317777},{"topic":"Scenicspot","val":0.058066003557078714},{"topic":"Beauty","val":0.05776958039654912},{"topic":"Hotel","val":0.056979118635136806},{"topic":"Finance","val":0.056023977340097},{"topic":"Hospital","val":0.05539819511231224},{"topic":"Education","val":0.05358672024240912},{"topic":"Life","val":0.052796258480996824},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265}]},{"site":"6225","startTime":"2014-01-14 10:52:49.99","endTime":"2014-01-14 11:08:47.94","stopTime":957,"isStop":false,"latitude":28.0252,"longitude":120.6105,"topics":[{"topic":"Traffic","val":0.11666097773732463},{"topic":"Shop","val":0.09530852960139662},{"topic":"Enterprise","val":0.0667501042970388},{"topic":"Food","val":0.05180718322145188},{"topic":"Finance","val":0.04308415822808874},{"topic":"Life","val":0.04164296279440271},{"topic":"Scenicspot","val":0.04128266393598119},{"topic":"Education","val":0.04061895551257315},{"topic":"Hospital","val":0.03764174915614274},{"topic":"Beauty","val":0.037471081275837814},{"topic":"Hotel","val":0.037091819319604646},{"topic":"Uptown","val":0.03513862024500379},{"topic":"Government","val":0.03032199340084253}]},{"site":"22279","startTime":"2014-01-14 11:08:47.94","endTime":"2014-01-14 11:10:31.38","stopTime":103,"isStop":false,"latitude":28.0158,"longitude":120.5944,"topics":[{"topic":"Uptown","val":0.08033067650352499},{"topic":"Food","val":0.06929714775047831},{"topic":"Traffic","val":0.06844081417561503},{"topic":"Shop","val":0.05961399117317777},{"topic":"Scenicspot","val":0.058066003557078714},{"topic":"Beauty","val":0.05776958039654912},{"topic":"Hotel","val":0.056979118635136806},{"topic":"Finance","val":0.056023977340097},{"topic":"Hospital","val":0.05539819511231224},{"topic":"Education","val":0.05358672024240912},{"topic":"Life","val":0.052796258480996824},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265}]},{"site":"24824","startTime":"2014-01-14 11:10:31.38","endTime":"2014-01-14 11:10:37.2","stopTime":5,"isStop":false,"latitude":28.0142,"longitude":120.5975,"topics":[{"topic":"Shop","val":0.12987308515809998},{"topic":"Beauty","val":0.09217099887255777},{"topic":"Hospital","val":0.07489734068411938},{"topic":"Finance","val":0.06812234301140871},{"topic":"Education","val":0.06657081682681823},{"topic":"Uptown","val":0.054386164523836336},{"topic":"Hotel","val":0.045790709461206607},{"topic":"Food","val":0.033399187000279204},{"topic":"Traffic","val":0.033088881763361194},{"topic":"Life","val":0.03084434054965407},{"topic":"Scenicspot","val":0.03023407358371527},{"topic":"Government","val":0.024348617590169434},{"topic":"Enterprise","val":0.016539269127731883}]},{"site":"22279","startTime":"2014-01-14 11:10:37.2","endTime":"2014-01-14 11:10:55.83","stopTime":18,"isStop":false,"latitude":28.0158,"longitude":120.5944,"topics":[{"topic":"Uptown","val":0.08033067650352499},{"topic":"Food","val":0.06929714775047831},{"topic":"Traffic","val":0.06844081417561503},{"topic":"Shop","val":0.05961399117317777},{"topic":"Scenicspot","val":0.058066003557078714},{"topic":"Beauty","val":0.05776958039654912},{"topic":"Hotel","val":0.056979118635136806},{"topic":"Finance","val":0.056023977340097},{"topic":"Hospital","val":0.05539819511231224},{"topic":"Education","val":0.05358672024240912},{"topic":"Life","val":0.052796258480996824},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265}]},{"site":"20403","startTime":"2014-01-14 11:10:55.83","endTime":"2014-01-14 11:23:53.6","stopTime":777,"isStop":false,"latitude":28.0174,"longitude":120.6061,"topics":[{"topic":"Uptown","val":0.17861612730198448},{"topic":"Life","val":0.0719777272611178},{"topic":"Finance","val":0.06891267145155973},{"topic":"Education","val":0.06783990191821443},{"topic":"Traffic","val":0.06584761564200177},{"topic":"Shop","val":0.05269341779264905},{"topic":"Hospital","val":0.04901535082117966},{"topic":"Scenicspot","val":0.047304027994176524},{"topic":"Hotel","val":0.04577150008939768},{"topic":"Beauty","val":0.04367704528619991},{"topic":"Food","val":0.04091849505759799},{"topic":"Enterprise","val":0.040816326530612734},{"topic":"Government","val":0.040816326530612734}]},{"site":"22279","startTime":"2014-01-14 11:23:53.6","endTime":"2014-01-14 11:24:36.94","stopTime":43,"isStop":false,"latitude":28.0158,"longitude":120.5944,"topics":[{"topic":"Uptown","val":0.08033067650352499},{"topic":"Food","val":0.06929714775047831},{"topic":"Traffic","val":0.06844081417561503},{"topic":"Shop","val":0.05961399117317777},{"topic":"Scenicspot","val":0.058066003557078714},{"topic":"Beauty","val":0.05776958039654912},{"topic":"Hotel","val":0.056979118635136806},{"topic":"Finance","val":0.056023977340097},{"topic":"Hospital","val":0.05539819511231224},{"topic":"Education","val":0.05358672024240912},{"topic":"Life","val":0.052796258480996824},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265}]},{"site":"5291","startTime":"2014-01-14 11:24:36.94","endTime":"2014-01-14 11:26:43.12","stopTime":126,"isStop":false,"latitude":28.011,"longitude":120.6061},{"site":"20403","startTime":"2014-01-14 11:26:43.12","endTime":"2014-01-14 11:28:47.32","stopTime":124,"isStop":false,"latitude":28.0174,"longitude":120.6061,"topics":[{"topic":"Uptown","val":0.17861612730198448},{"topic":"Life","val":0.0719777272611178},{"topic":"Finance","val":0.06891267145155973},{"topic":"Education","val":0.06783990191821443},{"topic":"Traffic","val":0.06584761564200177},{"topic":"Shop","val":0.05269341779264905},{"topic":"Hospital","val":0.04901535082117966},{"topic":"Scenicspot","val":0.047304027994176524},{"topic":"Hotel","val":0.04577150008939768},{"topic":"Beauty","val":0.04367704528619991},{"topic":"Food","val":0.04091849505759799},{"topic":"Enterprise","val":0.040816326530612734},{"topic":"Government","val":0.040816326530612734}]},{"site":"28682","startTime":"2014-01-14 11:28:47.32","endTime":"2014-01-14 11:34:37.34","stopTime":350,"isStop":false,"latitude":28.019,"longitude":120.6119,"topics":[{"topic":"Life","val":0.10001021685269881},{"topic":"Shop","val":0.09371408137723194},{"topic":"Uptown","val":0.08204132716916603},{"topic":"Government","val":0.08110903936042595},{"topic":"Education","val":0.06597532630073337},{"topic":"Beauty","val":0.06506858062373914},{"topic":"Finance","val":0.06343388419197486},{"topic":"Hospital","val":0.051901611708513345},{"topic":"Hotel","val":0.03856861893693669},{"topic":"Food","val":0.03473729917498923},{"topic":"Enterprise","val":0.030969834742407613},{"topic":"Scenicspot","val":0.0208423795049937},{"topic":"Traffic","val":0.020433705397052682}]},{"site":"4909","startTime":"2014-01-14 11:34:37.34","endTime":"2014-01-14 11:34:45.39","stopTime":8,"isStop":false,"latitude":28.0223,"longitude":120.6044,"topics":[{"topic":"Finance","val":0.1441906549853978},{"topic":"Hospital","val":0.07867473230426918},{"topic":"Hotel","val":0.06478584341538034},{"topic":"Enterprise","val":0.06342998192184673},{"topic":"Food","val":0.06217841746627724},{"topic":"Life","val":0.058058684466694474},{"topic":"Education","val":0.05133152551800871},{"topic":"Shop","val":0.04973230426922564},{"topic":"Uptown","val":0.04653386177165927},{"topic":"Beauty","val":0.04403073286052045},{"topic":"Traffic","val":0.04283131692393322},{"topic":"Scenicspot","val":0.032418995967181674},{"topic":"Government","val":0.02781254345709962}]},{"site":"22279","startTime":"2014-01-14 11:34:45.39","endTime":"2014-01-14 11:37:09.16","stopTime":143,"isStop":false,"latitude":28.0158,"longitude":120.5944,"topics":[{"topic":"Uptown","val":0.08033067650352499},{"topic":"Food","val":0.06929714775047831},{"topic":"Traffic","val":0.06844081417561503},{"topic":"Shop","val":0.05961399117317777},{"topic":"Scenicspot","val":0.058066003557078714},{"topic":"Beauty","val":0.05776958039654912},{"topic":"Hotel","val":0.056979118635136806},{"topic":"Finance","val":0.056023977340097},{"topic":"Hospital","val":0.05539819511231224},{"topic":"Education","val":0.05358672024240912},{"topic":"Life","val":0.052796258480996824},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265}]},{"site":"10451","startTime":"2014-01-14 11:37:09.16","endTime":"2014-01-14 12:00:18.0","stopTime":1388,"isStop":true,"latitude":28.0147,"longitude":120.6101,"topics":[{"topic":"Uptown","val":0.11825828797625022},{"topic":"Life","val":0.08333090782082202},{"topic":"Enterprise","val":0.06091917222108938},{"topic":"Traffic","val":0.06027883691823973},{"topic":"Hospital","val":0.053788165439356474},{"topic":"Shop","val":0.05349710393806126},{"topic":"Education","val":0.05189626568093761},{"topic":"Scenicspot","val":0.05076112582588634},{"topic":"Finance","val":0.04924760601915125},{"topic":"Hotel","val":0.04654073405710585},{"topic":"Food","val":0.04651162790697633},{"topic":"Government","val":0.04651162790697633},{"topic":"Beauty","val":0.04651162790697633}]},{"site":"22279","startTime":"2014-01-14 12:00:18.0","endTime":"2014-01-14 12:04:02.0","stopTime":224,"isStop":false,"latitude":28.0158,"longitude":120.5944,"topics":[{"topic":"Uptown","val":0.08033067650352499},{"topic":"Food","val":0.06929714775047831},{"topic":"Traffic","val":0.06844081417561503},{"topic":"Shop","val":0.05961399117317777},{"topic":"Scenicspot","val":0.058066003557078714},{"topic":"Beauty","val":0.05776958039654912},{"topic":"Hotel","val":0.056979118635136806},{"topic":"Finance","val":0.056023977340097},{"topic":"Hospital","val":0.05539819511231224},{"topic":"Education","val":0.05358672024240912},{"topic":"Life","val":0.052796258480996824},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265}]},{"site":"6019","startTime":"2014-01-14 12:04:02.0","endTime":"2014-01-14 12:16:33.79","stopTime":751,"isStop":false,"latitude":28.0259,"longitude":120.6042,"topics":[{"topic":"Enterprise","val":0.0802670004171887},{"topic":"Shop","val":0.07028229731609004},{"topic":"Food","val":0.057627590043109876},{"topic":"Hotel","val":0.05740508969545288},{"topic":"Beauty","val":0.055819774718398156},{"topic":"Finance","val":0.05529133639271321},{"topic":"Uptown","val":0.04933945209289388},{"topic":"Life","val":0.04475038242247239},{"topic":"Hospital","val":0.044527882074815596},{"topic":"Education","val":0.044527882074815596},{"topic":"Traffic","val":0.04444444444444431},{"topic":"Scenicspot","val":0.04444444444444431},{"topic":"Government","val":0.04444444444444431}]},{"site":"2795","startTime":"2014-01-14 12:16:33.79","endTime":"2014-01-14 13:06:10.68","stopTime":2976,"isStop":true,"latitude":28.0244,"longitude":120.603,"topics":[{"topic":"Shop","val":0.12251425392852172},{"topic":"Finance","val":0.07731887081073538},{"topic":"Enterprise","val":0.06602388788454677},{"topic":"Hotel","val":0.055934115174832384},{"topic":"Beauty","val":0.05417265408921638},{"topic":"Food","val":0.04423739550981929},{"topic":"Hospital","val":0.03773235062346474},{"topic":"Traffic","val":0.03229345323629835},{"topic":"Uptown","val":0.02863146834778015},{"topic":"Scenicspot","val":0.026900910088227252},{"topic":"Education","val":0.025881116828133675},{"topic":"Life","val":0.025402123024150267},{"topic":"Government","val":0.024691358024691124}]},{"site":"20840","startTime":"2014-01-14 13:06:10.68","endTime":"2014-01-14 13:06:18.25","stopTime":7,"isStop":false,"latitude":28.0246,"longitude":120.5996,"topics":[{"topic":"Shop","val":0.13852913968547623},{"topic":"Finance","val":0.06597921314686858},{"topic":"Government","val":0.06468683680687923},{"topic":"Hospital","val":0.06414267834793513},{"topic":"Enterprise","val":0.050620340643195275},{"topic":"Life","val":0.04657996408554174},{"topic":"Traffic","val":0.04217228056810169},{"topic":"Beauty","val":0.03553354736899442},{"topic":"Food","val":0.027575229906949026},{"topic":"Education","val":0.026813408064428376},{"topic":"Scenicspot","val":0.02660934864232465},{"topic":"Hotel","val":0.023725308809925485},{"topic":"Uptown","val":0.022568972084671082}]},{"site":"4909","startTime":"2014-01-14 13:06:18.25","endTime":"2014-01-14 13:11:04.66","stopTime":286,"isStop":false,"latitude":28.0223,"longitude":120.6044,"topics":[{"topic":"Finance","val":0.1441906549853978},{"topic":"Hospital","val":0.07867473230426918},{"topic":"Hotel","val":0.06478584341538034},{"topic":"Enterprise","val":0.06342998192184673},{"topic":"Food","val":0.06217841746627724},{"topic":"Life","val":0.058058684466694474},{"topic":"Education","val":0.05133152551800871},{"topic":"Shop","val":0.04973230426922564},{"topic":"Uptown","val":0.04653386177165927},{"topic":"Beauty","val":0.04403073286052045},{"topic":"Traffic","val":0.04283131692393322},{"topic":"Scenicspot","val":0.032418995967181674},{"topic":"Government","val":0.02781254345709962}]},{"site":"58","startTime":"2014-01-14 13:11:04.66","endTime":"2014-01-14 14:03:49.87","stopTime":3165,"isStop":true,"latitude":28.0271,"longitude":120.6009,"topics":[{"topic":"Enterprise","val":0.06455986649979234},{"topic":"Shop","val":0.06382978723404346},{"topic":"Finance","val":0.05899735780837252},{"topic":"Life","val":0.05565985259352062},{"topic":"Scenicspot","val":0.05559032123487788},{"topic":"Education","val":0.05559032123487787},{"topic":"Traffic","val":0.0555555555555565},{"topic":"Uptown","val":0.0555555555555565},{"topic":"Food","val":0.0555555555555565},{"topic":"Hospital","val":0.0555555555555565},{"topic":"Hotel","val":0.0555555555555565},{"topic":"Government","val":0.0555555555555565},{"topic":"Beauty","val":0.0555555555555565}]},{"site":"2795","startTime":"2014-01-14 14:03:49.87","endTime":"2014-01-14 14:06:46.03","stopTime":176,"isStop":false,"latitude":28.0244,"longitude":120.603,"topics":[{"topic":"Shop","val":0.12251425392852172},{"topic":"Finance","val":0.07731887081073538},{"topic":"Enterprise","val":0.06602388788454677},{"topic":"Hotel","val":0.055934115174832384},{"topic":"Beauty","val":0.05417265408921638},{"topic":"Food","val":0.04423739550981929},{"topic":"Hospital","val":0.03773235062346474},{"topic":"Traffic","val":0.03229345323629835},{"topic":"Uptown","val":0.02863146834778015},{"topic":"Scenicspot","val":0.026900910088227252},{"topic":"Education","val":0.025881116828133675},{"topic":"Life","val":0.025402123024150267},{"topic":"Government","val":0.024691358024691124}]},{"site":"4909","startTime":"2014-01-14 14:06:46.03","endTime":"2014-01-14 16:06:47.91","stopTime":7201,"isStop":true,"latitude":28.0223,"longitude":120.6044,"topics":[{"topic":"Finance","val":0.1441906549853978},{"topic":"Hospital","val":0.07867473230426918},{"topic":"Hotel","val":0.06478584341538034},{"topic":"Enterprise","val":0.06342998192184673},{"topic":"Food","val":0.06217841746627724},{"topic":"Life","val":0.058058684466694474},{"topic":"Education","val":0.05133152551800871},{"topic":"Shop","val":0.04973230426922564},{"topic":"Uptown","val":0.04653386177165927},{"topic":"Beauty","val":0.04403073286052045},{"topic":"Traffic","val":0.04283131692393322},{"topic":"Scenicspot","val":0.032418995967181674},{"topic":"Government","val":0.02781254345709962}]},{"site":"22279","startTime":"2014-01-14 16:06:47.91","endTime":"2014-01-14 17:14:24.45","stopTime":4056,"isStop":true,"stoppoint":1,"latitude":28.0158,"longitude":120.5944,"topics":[{"topic":"Uptown","val":0.08033067650352499},{"topic":"Food","val":0.06929714775047831},{"topic":"Traffic","val":0.06844081417561503},{"topic":"Shop","val":0.05961399117317777},{"topic":"Scenicspot","val":0.058066003557078714},{"topic":"Beauty","val":0.05776958039654912},{"topic":"Hotel","val":0.056979118635136806},{"topic":"Finance","val":0.056023977340097},{"topic":"Hospital","val":0.05539819511231224},{"topic":"Education","val":0.05358672024240912},{"topic":"Life","val":0.052796258480996824},{"topic":"Enterprise","val":0.052631578947369265},{"topic":"Government","val":0.052631578947369265}]}],"matching":true},{"pid":"460000112828695","traj":[{"site":"25950","isStop":true,"startTime":"2014-01-14 01:48:49.41","endTime":"2014-01-14 03:48:51.03","stopTime":7201,"stoppoint":0,"latitude":27.9561,"longitude":120.6516,"topics":[{"topic":"Shop","val":0.14022651264900798},{"topic":"Beauty","val":0.09580110383881471},{"topic":"Hotel","val":0.06422987751082282},{"topic":"Hospital","val":0.053052996573585816},{"topic":"Enterprise","val":0.04390221383286483},{"topic":"Food","val":0.04095283038224008},{"topic":"Finance","val":0.03994234596524333},{"topic":"Education","val":0.03696731570200442},{"topic":"Government","val":0.028396150926362478},{"topic":"Life","val":0.023630973142657805},{"topic":"Uptown","val":0.016075422146535526},{"topic":"Traffic","val":0.016034387246353022},{"topic":"Scenicspot","val":0.010120232257535009}]},{"site":"14253","startTime":"2014-01-14 03:48:51.03","endTime":"2014-01-14 07:48:54.26","stopTime":14403,"isStop":true,"latitude":27.9524,"longitude":120.6491,"topics":[{"topic":"Shop","val":0.1244530870199317},{"topic":"Beauty","val":0.09450863165734046},{"topic":"Life","val":0.0711012732858219},{"topic":"Hospital","val":0.06783272479028508},{"topic":"Food","val":0.058337384540592806},{"topic":"Education","val":0.046566472553501694},{"topic":"Enterprise","val":0.041580901747018444},{"topic":"Hotel","val":0.0325303323369088},{"topic":"Government","val":0.03243724076583319},{"topic":"Scenicspot","val":0.030492661281146936},{"topic":"Traffic","val":0.02827915059113151},{"topic":"Finance","val":0.024741670890265584},{"topic":"Uptown","val":0.017852894630684908}]},{"site":"1180","startTime":"2014-01-14 07:48:54.26","endTime":"2014-01-14 07:53:49.77","stopTime":295,"isStop":false,"latitude":27.951,"longitude":120.6569,"topics":[{"topic":"Uptown","val":0.11924741993064933},{"topic":"Traffic","val":0.1069985022261426},{"topic":"Finance","val":0.09639098052893957},{"topic":"Enterprise","val":0.07058002831408126},{"topic":"Education","val":0.05457641724286526},{"topic":"Shop","val":0.04905722316830481},{"topic":"Life","val":0.04454338414821841},{"topic":"Scenicspot","val":0.04400993044584429},{"topic":"Hospital","val":0.04031678942940961},{"topic":"Beauty","val":0.032827920146083905},{"topic":"Hotel","val":0.0328074026959926},{"topic":"Food","val":0.0327868852459013},{"topic":"Government","val":0.0327868852459013}]},{"site":"25950","startTime":"2014-01-14 07:53:49.77","endTime":"2014-01-14 07:55:46.74","stopTime":116,"isStop":false,"latitude":27.9561,"longitude":120.6516,"topics":[{"topic":"Shop","val":0.14022651264900798},{"topic":"Beauty","val":0.09580110383881471},{"topic":"Hotel","val":0.06422987751082282},{"topic":"Hospital","val":0.053052996573585816},{"topic":"Enterprise","val":0.04390221383286483},{"topic":"Food","val":0.04095283038224008},{"topic":"Finance","val":0.03994234596524333},{"topic":"Education","val":0.03696731570200442},{"topic":"Government","val":0.028396150926362478},{"topic":"Life","val":0.023630973142657805},{"topic":"Uptown","val":0.016075422146535526},{"topic":"Traffic","val":0.016034387246353022},{"topic":"Scenicspot","val":0.010120232257535009}]},{"site":"1180","startTime":"2014-01-14 07:55:46.74","endTime":"2014-01-14 08:40:37.46","stopTime":2690,"isStop":true,"latitude":27.951,"longitude":120.6569,"topics":[{"topic":"Uptown","val":0.11924741993064933},{"topic":"Traffic","val":0.1069985022261426},{"topic":"Finance","val":0.09639098052893957},{"topic":"Enterprise","val":0.07058002831408126},{"topic":"Education","val":0.05457641724286526},{"topic":"Shop","val":0.04905722316830481},{"topic":"Life","val":0.04454338414821841},{"topic":"Scenicspot","val":0.04400993044584429},{"topic":"Hospital","val":0.04031678942940961},{"topic":"Beauty","val":0.032827920146083905},{"topic":"Hotel","val":0.0328074026959926},{"topic":"Food","val":0.0327868852459013},{"topic":"Government","val":0.0327868852459013}]},{"site":"14253","startTime":"2014-01-14 08:40:37.46","endTime":"2014-01-14 09:03:27.39","stopTime":1369,"isStop":true,"latitude":27.9524,"longitude":120.6491,"topics":[{"topic":"Shop","val":0.1244530870199317},{"topic":"Beauty","val":0.09450863165734046},{"topic":"Life","val":0.0711012732858219},{"topic":"Hospital","val":0.06783272479028508},{"topic":"Food","val":0.058337384540592806},{"topic":"Education","val":0.046566472553501694},{"topic":"Enterprise","val":0.041580901747018444},{"topic":"Hotel","val":0.0325303323369088},{"topic":"Government","val":0.03243724076583319},{"topic":"Scenicspot","val":0.030492661281146936},{"topic":"Traffic","val":0.02827915059113151},{"topic":"Finance","val":0.024741670890265584},{"topic":"Uptown","val":0.017852894630684908}]},{"site":"1180","startTime":"2014-01-14 09:03:27.39","endTime":"2014-01-14 10:19:55.49","stopTime":4588,"isStop":true,"latitude":27.951,"longitude":120.6569,"topics":[{"topic":"Uptown","val":0.11924741993064933},{"topic":"Traffic","val":0.1069985022261426},{"topic":"Finance","val":0.09639098052893957},{"topic":"Enterprise","val":0.07058002831408126},{"topic":"Education","val":0.05457641724286526},{"topic":"Shop","val":0.04905722316830481},{"topic":"Life","val":0.04454338414821841},{"topic":"Scenicspot","val":0.04400993044584429},{"topic":"Hospital","val":0.04031678942940961},{"topic":"Beauty","val":0.032827920146083905},{"topic":"Hotel","val":0.0328074026959926},{"topic":"Food","val":0.0327868852459013},{"topic":"Government","val":0.0327868852459013}]},{"site":"25950","startTime":"2014-01-14 10:19:55.49","endTime":"2014-01-14 16:58:16.06","stopTime":23900,"isStop":true,"stoppoint":1,"latitude":27.9561,"longitude":120.6516,"topics":[{"topic":"Shop","val":0.14022651264900798},{"topic":"Beauty","val":0.09580110383881471},{"topic":"Hotel","val":0.06422987751082282},{"topic":"Hospital","val":0.053052996573585816},{"topic":"Enterprise","val":0.04390221383286483},{"topic":"Food","val":0.04095283038224008},{"topic":"Finance","val":0.03994234596524333},{"topic":"Education","val":0.03696731570200442},{"topic":"Government","val":0.028396150926362478},{"topic":"Life","val":0.023630973142657805},{"topic":"Uptown","val":0.016075422146535526},{"topic":"Traffic","val":0.016034387246353022},{"topic":"Scenicspot","val":0.010120232257535009}]}],"matching":true}];var mockData20=[{"pid":"460000102807196","traj":[{"site":"8176","startTime":"2014-01-14 08:11:27.72","endTime":"2014-01-14 09:32:31.47","stopTime":4863,"isStop":true,"stoppoint":0,"latitude":28.0661,"longitude":120.5849,"topics":[{"topic":"Finance","val":0.11324407307695748},{"topic":"Beauty","val":0.09246270067800577},{"topic":"Shop","val":0.09086898191084185},{"topic":"Life","val":0.07157327954907657},{"topic":"Government","val":0.07143821863660578},{"topic":"Education","val":0.06587370904277809},{"topic":"Hospital","val":0.057193794401274915},{"topic":"Uptown","val":0.045488515320409664},{"topic":"Enterprise","val":0.03375622405704983},{"topic":"Food","val":0.03163126570083121},{"topic":"Hotel","val":0.029524315466275285},{"topic":"Scenicspot","val":0.025859662707881276},{"topic":"Traffic","val":0.024689134799794756}]},{"site":"293","startTime":"2014-01-14 09:32:31.47","endTime":"2014-01-14 09:39:05.54","stopTime":394,"isStop":false,"latitude":28.0735,"longitude":120.5847,"topics":[{"topic":"Uptown","val":0.08999886221413254},{"topic":"Shop","val":0.08118102173171084},{"topic":"Enterprise","val":0.06875071111616862},{"topic":"Finance","val":0.0548697235180345},{"topic":"Education","val":0.04633632950278818},{"topic":"Life","val":0.04568210262828601},{"topic":"Beauty","val":0.04559676868813354},{"topic":"Hospital","val":0.04551143474798108},{"topic":"Hotel","val":0.045511434747981076},{"topic":"Traffic","val":0.04545454545454611},{"topic":"Scenicspot","val":0.04545454545454611},{"topic":"Food","val":0.04545454545454611},{"topic":"Government","val":0.04545454545454611}]},{"site":"8176","startTime":"2014-01-14 09:39:05.54","endTime":"2014-01-14 10:43:22.4","stopTime":3856,"isStop":true,"stoppoint":1,"latitude":28.0661,"longitude":120.5849,"topics":[{"topic":"Finance","val":0.11324407307695748},{"topic":"Beauty","val":0.09246270067800577},{"topic":"Shop","val":0.09086898191084185},{"topic":"Life","val":0.07157327954907657},{"topic":"Government","val":0.07143821863660578},{"topic":"Education","val":0.06587370904277809},{"topic":"Hospital","val":0.057193794401274915},{"topic":"Uptown","val":0.045488515320409664},{"topic":"Enterprise","val":0.03375622405704983},{"topic":"Food","val":0.03163126570083121},{"topic":"Hotel","val":0.029524315466275285},{"topic":"Scenicspot","val":0.025859662707881276},{"topic":"Traffic","val":0.024689134799794756}]}],"matching":true},{"pid":"410060006060278","traj":[{"site":"8176","isStop":true,"startTime":"2014-01-13 23:54:27.51","endTime":"2014-01-14 09:59:39.66","stopTime":36312,"stoppoint":0,"latitude":28.0661,"longitude":120.5849,"topics":[{"topic":"Finance","val":0.11324407307695748},{"topic":"Beauty","val":0.09246270067800577},{"topic":"Shop","val":0.09086898191084185},{"topic":"Life","val":0.07157327954907657},{"topic":"Government","val":0.07143821863660578},{"topic":"Education","val":0.06587370904277809},{"topic":"Hospital","val":0.057193794401274915},{"topic":"Uptown","val":0.045488515320409664},{"topic":"Enterprise","val":0.03375622405704983},{"topic":"Food","val":0.03163126570083121},{"topic":"Hotel","val":0.029524315466275285},{"topic":"Scenicspot","val":0.025859662707881276},{"topic":"Traffic","val":0.024689134799794756}]},{"site":"293","startTime":"2014-01-14 09:59:39.66","endTime":"2014-01-14 10:06:29.88","stopTime":410,"isStop":false,"latitude":28.0735,"longitude":120.5847,"topics":[{"topic":"Uptown","val":0.08999886221413254},{"topic":"Shop","val":0.08118102173171084},{"topic":"Enterprise","val":0.06875071111616862},{"topic":"Finance","val":0.0548697235180345},{"topic":"Education","val":0.04633632950278818},{"topic":"Life","val":0.04568210262828601},{"topic":"Beauty","val":0.04559676868813354},{"topic":"Hospital","val":0.04551143474798108},{"topic":"Hotel","val":0.045511434747981076},{"topic":"Traffic","val":0.04545454545454611},{"topic":"Scenicspot","val":0.04545454545454611},{"topic":"Food","val":0.04545454545454611},{"topic":"Government","val":0.04545454545454611}]},{"site":"7883","startTime":"2014-01-14 10:06:29.88","endTime":"2014-01-14 12:07:04.99","stopTime":7235,"isStop":true,"latitude":28.0858,"longitude":120.5907,"topics":[{"topic":"Enterprise","val":0.13559502569701362},{"topic":"Finance","val":0.04257982052033083},{"topic":"Traffic","val":0.04257982052033083},{"topic":"Scenicspot","val":0.04257982052033083},{"topic":"Uptown","val":0.042553191489361264},{"topic":"Food","val":0.042553191489361264},{"topic":"Hospital","val":0.042553191489361264},{"topic":"Life","val":0.042553191489361264},{"topic":"Hotel","val":0.042553191489361264},{"topic":"Shop","val":0.042553191489361264},{"topic":"Government","val":0.042553191489361264},{"topic":"Beauty","val":0.042553191489361264},{"topic":"Education","val":0.042553191489361264}]},{"site":"15365","startTime":"2014-01-14 12:07:04.99","endTime":"2014-01-14 12:07:54.47","stopTime":49,"isStop":false,"latitude":28.0858,"longitude":120.5908},{"site":"7883","startTime":"2014-01-14 12:07:54.47","endTime":"2014-01-14 12:07:38.03","stopTime":86383,"isStop":true,"latitude":28.0858,"longitude":120.5907,"topics":[{"topic":"Enterprise","val":0.13559502569701362},{"topic":"Finance","val":0.04257982052033083},{"topic":"Traffic","val":0.04257982052033083},{"topic":"Scenicspot","val":0.04257982052033083},{"topic":"Uptown","val":0.042553191489361264},{"topic":"Food","val":0.042553191489361264},{"topic":"Hospital","val":0.042553191489361264},{"topic":"Life","val":0.042553191489361264},{"topic":"Hotel","val":0.042553191489361264},{"topic":"Shop","val":0.042553191489361264},{"topic":"Government","val":0.042553191489361264},{"topic":"Beauty","val":0.042553191489361264},{"topic":"Education","val":0.042553191489361264}]},{"site":"15365","startTime":"2014-01-14 12:07:38.03","endTime":"2014-01-14 12:07:21.55","stopTime":86383,"isStop":true,"latitude":28.0858,"longitude":120.5908},{"site":"7883","startTime":"2014-01-14 12:07:21.55","endTime":"2014-01-14 12:07:04.99","stopTime":86383,"isStop":true,"latitude":28.0858,"longitude":120.5907,"topics":[{"topic":"Enterprise","val":0.13559502569701362},{"topic":"Finance","val":0.04257982052033083},{"topic":"Traffic","val":0.04257982052033083},{"topic":"Scenicspot","val":0.04257982052033083},{"topic":"Uptown","val":0.042553191489361264},{"topic":"Food","val":0.042553191489361264},{"topic":"Hospital","val":0.042553191489361264},{"topic":"Life","val":0.042553191489361264},{"topic":"Hotel","val":0.042553191489361264},{"topic":"Shop","val":0.042553191489361264},{"topic":"Government","val":0.042553191489361264},{"topic":"Beauty","val":0.042553191489361264},{"topic":"Education","val":0.042553191489361264}]},{"site":"15365","startTime":"2014-01-14 12:07:04.99","endTime":"2014-01-14 14:08:13.34","stopTime":7268,"isStop":true,"latitude":28.0858,"longitude":120.5908},{"site":"8176","startTime":"2014-01-14 14:08:13.34","endTime":"2014-01-14 14:20:44.15","stopTime":750,"isStop":false,"latitude":28.0661,"longitude":120.5849,"topics":[{"topic":"Finance","val":0.11324407307695748},{"topic":"Beauty","val":0.09246270067800577},{"topic":"Shop","val":0.09086898191084185},{"topic":"Life","val":0.07157327954907657},{"topic":"Government","val":0.07143821863660578},{"topic":"Education","val":0.06587370904277809},{"topic":"Hospital","val":0.057193794401274915},{"topic":"Uptown","val":0.045488515320409664},{"topic":"Enterprise","val":0.03375622405704983},{"topic":"Food","val":0.03163126570083121},{"topic":"Hotel","val":0.029524315466275285},{"topic":"Scenicspot","val":0.025859662707881276},{"topic":"Traffic","val":0.024689134799794756}]},{"site":"26263","startTime":"2014-01-14 14:20:44.15","endTime":"2014-01-14 14:30:57.02","stopTime":612,"isStop":false,"latitude":28.0604,"longitude":120.5739,"topics":[{"topic":"Finance","val":0.05882352941176564},{"topic":"Enterprise","val":0.05882352941176564},{"topic":"Traffic","val":0.05882352941176564},{"topic":"Scenicspot","val":0.05882352941176564},{"topic":"Uptown","val":0.05882352941176564},{"topic":"Food","val":0.05882352941176564},{"topic":"Hospital","val":0.05882352941176564},{"topic":"Life","val":0.05882352941176564},{"topic":"Hotel","val":0.05882352941176564},{"topic":"Shop","val":0.05882352941176564},{"topic":"Government","val":0.05882352941176564},{"topic":"Beauty","val":0.05882352941176564},{"topic":"Education","val":0.05882352941176564}]},{"site":"5341","startTime":"2014-01-14 14:30:57.02","endTime":"2014-01-14 15:09:24.13","stopTime":2307,"isStop":true,"stoppoint":1,"latitude":28.0645,"longitude":120.5822,"topics":[{"topic":"Shop","val":0.16994190499318182},{"topic":"Beauty","val":0.06976070834812188},{"topic":"Food","val":0.05689014252890741},{"topic":"Government","val":0.044318457773709886},{"topic":"Life","val":0.043842116077933146},{"topic":"Hospital","val":0.04238507089085231},{"topic":"Education","val":0.03679973100704248},{"topic":"Finance","val":0.035520146451721364},{"topic":"Enterprise","val":0.029196943941120507},{"topic":"Scenicspot","val":0.025815851904432804},{"topic":"Uptown","val":0.02431210655109936},{"topic":"Traffic","val":0.02277100106476395},{"topic":"Hotel","val":0.020762893915902542}]}],"matching":true},{"pid":"460000050641956","traj":[{"site":"8176","isStop":true,"startTime":"2014-01-13 23:59:19.45","endTime":"2014-01-14 10:40:47.22","stopTime":38487,"stoppoint":0,"latitude":28.0661,"longitude":120.5849,"topics":[{"topic":"Finance","val":0.11324407307695748},{"topic":"Beauty","val":0.09246270067800577},{"topic":"Shop","val":0.09086898191084185},{"topic":"Life","val":0.07157327954907657},{"topic":"Government","val":0.07143821863660578},{"topic":"Education","val":0.06587370904277809},{"topic":"Hospital","val":0.057193794401274915},{"topic":"Uptown","val":0.045488515320409664},{"topic":"Enterprise","val":0.03375622405704983},{"topic":"Food","val":0.03163126570083121},{"topic":"Hotel","val":0.029524315466275285},{"topic":"Scenicspot","val":0.025859662707881276},{"topic":"Traffic","val":0.024689134799794756}]},{"site":"14217","startTime":"2014-01-14 10:40:47.22","endTime":"2014-01-14 20:41:04.83","stopTime":36017,"isStop":true,"latitude":28.0695,"longitude":120.5852,"topics":[{"topic":"Traffic","val":0.07794743429286612},{"topic":"Scenicspot","val":0.07511889862327917},{"topic":"Enterprise","val":0.06655819774718384},{"topic":"Shop","val":0.06300375469336647},{"topic":"Government","val":0.05892365456821056},{"topic":"Life","val":0.05594493116395495},{"topic":"Hotel","val":0.05188986232790948},{"topic":"Food","val":0.05136420525657046},{"topic":"Beauty","val":0.05041301627033746},{"topic":"Finance","val":0.05011264080100076},{"topic":"Hospital","val":0.04760951188986181},{"topic":"Uptown","val":0.04608260325406702},{"topic":"Education","val":0.043629536921150854}]},{"site":"8176","startTime":"2014-01-14 20:41:04.83","endTime":"2014-01-14 23:41:10.23","stopTime":10805,"isStop":true,"stoppoint":1,"latitude":28.0661,"longitude":120.5849,"topics":[{"topic":"Finance","val":0.11324407307695748},{"topic":"Beauty","val":0.09246270067800577},{"topic":"Shop","val":0.09086898191084185},{"topic":"Life","val":0.07157327954907657},{"topic":"Government","val":0.07143821863660578},{"topic":"Education","val":0.06587370904277809},{"topic":"Hospital","val":0.057193794401274915},{"topic":"Uptown","val":0.045488515320409664},{"topic":"Enterprise","val":0.03375622405704983},{"topic":"Food","val":0.03163126570083121},{"topic":"Hotel","val":0.029524315466275285},{"topic":"Scenicspot","val":0.025859662707881276},{"topic":"Traffic","val":0.024689134799794756}]}],"matching":true},{"pid":"460000102243499","traj":[{"site":"8176","isStop":true,"startTime":"2014-01-13 23:50:15.58","endTime":"2014-01-14 07:50:30.22","stopTime":28814,"stoppoint":0,"latitude":28.0661,"longitude":120.5849,"topics":[{"topic":"Finance","val":0.11324407307695748},{"topic":"Beauty","val":0.09246270067800577},{"topic":"Shop","val":0.09086898191084185},{"topic":"Life","val":0.07157327954907657},{"topic":"Government","val":0.07143821863660578},{"topic":"Education","val":0.06587370904277809},{"topic":"Hospital","val":0.057193794401274915},{"topic":"Uptown","val":0.045488515320409664},{"topic":"Enterprise","val":0.03375622405704983},{"topic":"Food","val":0.03163126570083121},{"topic":"Hotel","val":0.029524315466275285},{"topic":"Scenicspot","val":0.025859662707881276},{"topic":"Traffic","val":0.024689134799794756}]},{"site":"12722","startTime":"2014-01-14 07:50:30.22","endTime":"2014-01-14 10:43:37.31","stopTime":10387,"isStop":true,"latitude":28.0691,"longitude":120.5728,"topics":[{"topic":"Traffic","val":0.10678241418794678},{"topic":"Shop","val":0.07514712539610616},{"topic":"Enterprise","val":0.06367001304822459},{"topic":"Life","val":0.05834420685431212},{"topic":"Scenicspot","val":0.05424333608499939},{"topic":"Finance","val":0.052459191010038694},{"topic":"Hospital","val":0.05150054589513441},{"topic":"Education","val":0.04689372353740003},{"topic":"Uptown","val":0.04391127206880899},{"topic":"Beauty","val":0.04268633664420908},{"topic":"Food","val":0.04260644955130039},{"topic":"Hotel","val":0.04260644955130039},{"topic":"Government","val":0.04260644955130039}]},{"site":"5341","startTime":"2014-01-14 10:43:37.31","endTime":"2014-01-14 10:43:48.6","stopTime":11,"isStop":false,"latitude":28.0645,"longitude":120.5822,"topics":[{"topic":"Shop","val":0.16994190499318182},{"topic":"Beauty","val":0.06976070834812188},{"topic":"Food","val":0.05689014252890741},{"topic":"Government","val":0.044318457773709886},{"topic":"Life","val":0.043842116077933146},{"topic":"Hospital","val":0.04238507089085231},{"topic":"Education","val":0.03679973100704248},{"topic":"Finance","val":0.035520146451721364},{"topic":"Enterprise","val":0.029196943941120507},{"topic":"Scenicspot","val":0.025815851904432804},{"topic":"Uptown","val":0.02431210655109936},{"topic":"Traffic","val":0.02277100106476395},{"topic":"Hotel","val":0.020762893915902542}]},{"site":"12722","startTime":"2014-01-14 10:43:48.6","endTime":"2014-01-14 10:44:33.78","stopTime":45,"isStop":false,"latitude":28.0691,"longitude":120.5728,"topics":[{"topic":"Traffic","val":0.10678241418794678},{"topic":"Shop","val":0.07514712539610616},{"topic":"Enterprise","val":0.06367001304822459},{"topic":"Life","val":0.05834420685431212},{"topic":"Scenicspot","val":0.05424333608499939},{"topic":"Finance","val":0.052459191010038694},{"topic":"Hospital","val":0.05150054589513441},{"topic":"Education","val":0.04689372353740003},{"topic":"Uptown","val":0.04391127206880899},{"topic":"Beauty","val":0.04268633664420908},{"topic":"Food","val":0.04260644955130039},{"topic":"Hotel","val":0.04260644955130039},{"topic":"Government","val":0.04260644955130039}]},{"site":"5341","startTime":"2014-01-14 10:44:33.78","endTime":"2014-01-14 10:45:10.18","stopTime":36,"isStop":false,"latitude":28.0645,"longitude":120.5822,"topics":[{"topic":"Shop","val":0.16994190499318182},{"topic":"Beauty","val":0.06976070834812188},{"topic":"Food","val":0.05689014252890741},{"topic":"Government","val":0.044318457773709886},{"topic":"Life","val":0.043842116077933146},{"topic":"Hospital","val":0.04238507089085231},{"topic":"Education","val":0.03679973100704248},{"topic":"Finance","val":0.035520146451721364},{"topic":"Enterprise","val":0.029196943941120507},{"topic":"Scenicspot","val":0.025815851904432804},{"topic":"Uptown","val":0.02431210655109936},{"topic":"Traffic","val":0.02277100106476395},{"topic":"Hotel","val":0.020762893915902542}]},{"site":"12722","startTime":"2014-01-14 10:45:10.18","endTime":"2014-01-14 10:46:23.48","stopTime":73,"isStop":false,"latitude":28.0691,"longitude":120.5728,"topics":[{"topic":"Traffic","val":0.10678241418794678},{"topic":"Shop","val":0.07514712539610616},{"topic":"Enterprise","val":0.06367001304822459},{"topic":"Life","val":0.05834420685431212},{"topic":"Scenicspot","val":0.05424333608499939},{"topic":"Finance","val":0.052459191010038694},{"topic":"Hospital","val":0.05150054589513441},{"topic":"Education","val":0.04689372353740003},{"topic":"Uptown","val":0.04391127206880899},{"topic":"Beauty","val":0.04268633664420908},{"topic":"Food","val":0.04260644955130039},{"topic":"Hotel","val":0.04260644955130039},{"topic":"Government","val":0.04260644955130039}]},{"site":"5341","startTime":"2014-01-14 10:46:23.48","endTime":"2014-01-14 11:51:46.07","stopTime":3922,"isStop":true,"stoppoint":1,"latitude":28.0645,"longitude":120.5822,"topics":[{"topic":"Shop","val":0.16994190499318182},{"topic":"Beauty","val":0.06976070834812188},{"topic":"Food","val":0.05689014252890741},{"topic":"Government","val":0.044318457773709886},{"topic":"Life","val":0.043842116077933146},{"topic":"Hospital","val":0.04238507089085231},{"topic":"Education","val":0.03679973100704248},{"topic":"Finance","val":0.035520146451721364},{"topic":"Enterprise","val":0.029196943941120507},{"topic":"Scenicspot","val":0.025815851904432804},{"topic":"Uptown","val":0.02431210655109936},{"topic":"Traffic","val":0.02277100106476395},{"topic":"Hotel","val":0.020762893915902542}]}],"matching":true},{"pid":"460000102244539","traj":[{"site":"8176","isStop":true,"startTime":"2014-01-13 23:51:42.48","endTime":"2014-01-14 09:51:52.68","stopTime":36010,"stoppoint":0,"latitude":28.0661,"longitude":120.5849,"topics":[{"topic":"Finance","val":0.11324407307695748},{"topic":"Beauty","val":0.09246270067800577},{"topic":"Shop","val":0.09086898191084185},{"topic":"Life","val":0.07157327954907657},{"topic":"Government","val":0.07143821863660578},{"topic":"Education","val":0.06587370904277809},{"topic":"Hospital","val":0.057193794401274915},{"topic":"Uptown","val":0.045488515320409664},{"topic":"Enterprise","val":0.03375622405704983},{"topic":"Food","val":0.03163126570083121},{"topic":"Hotel","val":0.029524315466275285},{"topic":"Scenicspot","val":0.025859662707881276},{"topic":"Traffic","val":0.024689134799794756}]},{"site":"19502","startTime":"2014-01-14 09:51:52.68","endTime":"2014-01-14 10:43:34.18","stopTime":3101,"isStop":true,"latitude":28.0651,"longitude":120.5755,"topics":[{"topic":"Education","val":0.0988125400653238},{"topic":"Government","val":0.09676730058914913},{"topic":"Traffic","val":0.06309716413809852},{"topic":"Shop","val":0.0627003266277959},{"topic":"Scenicspot","val":0.054641472572421926},{"topic":"Finance","val":0.0542141090997884},{"topic":"Hospital","val":0.05164992826398753},{"topic":"Uptown","val":0.04960468878781303},{"topic":"Life","val":0.048872065691869936},{"topic":"Hotel","val":0.048872065691869936},{"topic":"Beauty","val":0.048811013767208006},{"topic":"Enterprise","val":0.048780487804877044},{"topic":"Food","val":0.048780487804877044}]},{"site":"5341","startTime":"2014-01-14 10:43:34.18","endTime":"2014-01-14 10:44:12.71","stopTime":38,"isStop":false,"latitude":28.0645,"longitude":120.5822,"topics":[{"topic":"Shop","val":0.16994190499318182},{"topic":"Beauty","val":0.06976070834812188},{"topic":"Food","val":0.05689014252890741},{"topic":"Government","val":0.044318457773709886},{"topic":"Life","val":0.043842116077933146},{"topic":"Hospital","val":0.04238507089085231},{"topic":"Education","val":0.03679973100704248},{"topic":"Finance","val":0.035520146451721364},{"topic":"Enterprise","val":0.029196943941120507},{"topic":"Scenicspot","val":0.025815851904432804},{"topic":"Uptown","val":0.02431210655109936},{"topic":"Traffic","val":0.02277100106476395},{"topic":"Hotel","val":0.020762893915902542}]},{"site":"19502","startTime":"2014-01-14 10:44:12.71","endTime":"2014-01-14 10:46:56.61","stopTime":163,"isStop":false,"latitude":28.0651,"longitude":120.5755,"topics":[{"topic":"Education","val":0.0988125400653238},{"topic":"Government","val":0.09676730058914913},{"topic":"Traffic","val":0.06309716413809852},{"topic":"Shop","val":0.0627003266277959},{"topic":"Scenicspot","val":0.054641472572421926},{"topic":"Finance","val":0.0542141090997884},{"topic":"Hospital","val":0.05164992826398753},{"topic":"Uptown","val":0.04960468878781303},{"topic":"Life","val":0.048872065691869936},{"topic":"Hotel","val":0.048872065691869936},{"topic":"Beauty","val":0.048811013767208006},{"topic":"Enterprise","val":0.048780487804877044},{"topic":"Food","val":0.048780487804877044}]},{"site":"8176","startTime":"2014-01-14 10:46:56.61","endTime":"2014-01-14 11:14:18.2","stopTime":1641,"isStop":true,"stoppoint":1,"latitude":28.0661,"longitude":120.5849,"topics":[{"topic":"Finance","val":0.11324407307695748},{"topic":"Beauty","val":0.09246270067800577},{"topic":"Shop","val":0.09086898191084185},{"topic":"Life","val":0.07157327954907657},{"topic":"Government","val":0.07143821863660578},{"topic":"Education","val":0.06587370904277809},{"topic":"Hospital","val":0.057193794401274915},{"topic":"Uptown","val":0.045488515320409664},{"topic":"Enterprise","val":0.03375622405704983},{"topic":"Food","val":0.03163126570083121},{"topic":"Hotel","val":0.029524315466275285},{"topic":"Scenicspot","val":0.025859662707881276},{"topic":"Traffic","val":0.024689134799794756}]}],"matching":true},{"pid":"460000102824741","traj":[{"site":"1764","isStop":true,"startTime":"2014-01-14 01:10:08.81","endTime":"2014-01-14 07:17:04.98","stopTime":22016,"stoppoint":0,"latitude":28.0485,"longitude":120.6535,"topics":[{"topic":"Traffic","val":0.11944876150132806},{"topic":"Shop","val":0.09011264080100187},{"topic":"Uptown","val":0.06915237453410174},{"topic":"Enterprise","val":0.0622894002118036},{"topic":"Hospital","val":0.05577026227839777},{"topic":"Life","val":0.05545393280061638},{"topic":"Education","val":0.054339902900604},{"topic":"Food","val":0.04478125128938655},{"topic":"Finance","val":0.03222434636702461},{"topic":"Scenicspot","val":0.03076648007811953},{"topic":"Beauty","val":0.027933268233643612},{"topic":"Hotel","val":0.026351620844737154},{"topic":"Government","val":0.022019282344689035}]},{"site":"23399","startTime":"2014-01-14 07:17:04.98","endTime":"2014-01-14 07:42:43.75","stopTime":1538,"isStop":true,"stoppoint":1,"latitude":28.0498,"longitude":120.6511,"topics":[{"topic":"Enterprise","val":0.12873234400143027},{"topic":"Finance","val":0.06730351715154174},{"topic":"Traffic","val":0.06660110852851801},{"topic":"Life","val":0.05310209190059024},{"topic":"Food","val":0.04969221731245708},{"topic":"Shop","val":0.047546678245766534},{"topic":"Uptown","val":0.04233608336951831},{"topic":"Hospital","val":0.03860693213455612},{"topic":"Education","val":0.037406451942479324},{"topic":"Scenicspot","val":0.027151286046333455},{"topic":"Beauty","val":0.026819238333631354},{"topic":"Hotel","val":0.025567673878061883},{"topic":"Government","val":0.020420934331179523}]}],"matching":true},{"pid":"460000102839002","traj":[{"site":"8176","startTime":"2014-01-14 06:38:25.04","endTime":"2014-01-14 07:50:37.72","stopTime":4332,"isStop":true,"stoppoint":0,"latitude":28.0661,"longitude":120.5849,"topics":[{"topic":"Finance","val":0.11324407307695748},{"topic":"Beauty","val":0.09246270067800577},{"topic":"Shop","val":0.09086898191084185},{"topic":"Life","val":0.07157327954907657},{"topic":"Government","val":0.07143821863660578},{"topic":"Education","val":0.06587370904277809},{"topic":"Hospital","val":0.057193794401274915},{"topic":"Uptown","val":0.045488515320409664},{"topic":"Enterprise","val":0.03375622405704983},{"topic":"Food","val":0.03163126570083121},{"topic":"Hotel","val":0.029524315466275285},{"topic":"Scenicspot","val":0.025859662707881276},{"topic":"Traffic","val":0.024689134799794756}]},{"site":"15553","startTime":"2014-01-14 07:50:37.72","endTime":"2014-01-14 08:06:12.02","stopTime":934,"isStop":false,"latitude":28.0393,"longitude":120.5877,"topics":[{"topic":"Enterprise","val":0.0665474700518508},{"topic":"Finance","val":0.05714285714285696},{"topic":"Traffic","val":0.05714285714285696},{"topic":"Scenicspot","val":0.05714285714285696},{"topic":"Uptown","val":0.05714285714285696},{"topic":"Food","val":0.05714285714285696},{"topic":"Hospital","val":0.05714285714285696},{"topic":"Life","val":0.05714285714285696},{"topic":"Hotel","val":0.05714285714285696},{"topic":"Shop","val":0.05714285714285696},{"topic":"Government","val":0.05714285714285696},{"topic":"Beauty","val":0.05714285714285696},{"topic":"Education","val":0.05714285714285696}]},{"site":"3156","startTime":"2014-01-14 08:06:12.02","endTime":"2014-01-14 08:08:43.28","stopTime":151,"isStop":false,"latitude":28.0279,"longitude":120.5993,"topics":[{"topic":"Education","val":0.11141119381683424},{"topic":"Shop","val":0.09779768570362174},{"topic":"Finance","val":0.07166853303471526},{"topic":"Traffic","val":0.05669367411018206},{"topic":"Hospital","val":0.05289506620117318},{"topic":"Enterprise","val":0.04731791932898619},{"topic":"Food","val":0.04558329490810943},{"topic":"Scenicspot","val":0.040247678018576184},{"topic":"Beauty","val":0.0391717717322095},{"topic":"Hotel","val":0.038205651801594444},{"topic":"Uptown","val":0.03627341194036433},{"topic":"Life","val":0.03583426651735748},{"topic":"Government","val":0.03510967656939619}]},{"site":"2795","startTime":"2014-01-14 08:08:43.28","endTime":"2014-01-14 08:09:47.65","stopTime":64,"isStop":false,"latitude":28.0244,"longitude":120.603,"topics":[{"topic":"Shop","val":0.12251425392852172},{"topic":"Finance","val":0.07731887081073538},{"topic":"Enterprise","val":0.06602388788454677},{"topic":"Hotel","val":0.055934115174832384},{"topic":"Beauty","val":0.05417265408921638},{"topic":"Food","val":0.04423739550981929},{"topic":"Hospital","val":0.03773235062346474},{"topic":"Traffic","val":0.03229345323629835},{"topic":"Uptown","val":0.02863146834778015},{"topic":"Scenicspot","val":0.026900910088227252},{"topic":"Education","val":0.025881116828133675},{"topic":"Life","val":0.025402123024150267},{"topic":"Government","val":0.024691358024691124}]},{"site":"8176","startTime":"2014-01-14 08:09:47.65","endTime":"2014-01-14 08:06:11.97","stopTime":86184,"isStop":true,"stoppoint":1,"latitude":28.0661,"longitude":120.5849,"topics":[{"topic":"Finance","val":0.11324407307695748},{"topic":"Beauty","val":0.09246270067800577},{"topic":"Shop","val":0.09086898191084185},{"topic":"Life","val":0.07157327954907657},{"topic":"Government","val":0.07143821863660578},{"topic":"Education","val":0.06587370904277809},{"topic":"Hospital","val":0.057193794401274915},{"topic":"Uptown","val":0.045488515320409664},{"topic":"Enterprise","val":0.03375622405704983},{"topic":"Food","val":0.03163126570083121},{"topic":"Hotel","val":0.029524315466275285},{"topic":"Scenicspot","val":0.025859662707881276},{"topic":"Traffic","val":0.024689134799794756}]}],"matching":true},{"pid":"460000102855683","traj":[{"site":"2305","isStop":true,"startTime":"2014-01-14 01:06:28.87","endTime":"2014-01-14 07:06:32.88","stopTime":21604,"stoppoint":0,"latitude":27.7862,"longitude":120.689,"topics":[{"topic":"Hospital","val":0.14748346146969452},{"topic":"Education","val":0.09898533881637712},{"topic":"Scenicspot","val":0.09181119256213081},{"topic":"Traffic","val":0.08494993742177692},{"topic":"Life","val":0.07010995887716794},{"topic":"Beauty","val":0.05491239048811022},{"topic":"Government","val":0.05261040586447339},{"topic":"Shop","val":0.046173788664401624},{"topic":"Finance","val":0.0399606651171103},{"topic":"Uptown","val":0.03991596638655423},{"topic":"Hotel","val":0.0368988020740208},{"topic":"Food","val":0.036697657786518574},{"topic":"Enterprise","val":0.035714285714285456}]},{"site":"19912","startTime":"2014-01-14 07:06:32.88","endTime":"2014-01-14 07:55:28.56","stopTime":2935,"isStop":true,"latitude":27.7898,"longitude":120.6973,"topics":[{"topic":"Shop","val":0.0906587780179772},{"topic":"Enterprise","val":0.07834793491864862},{"topic":"Education","val":0.0575492092388213},{"topic":"Uptown","val":0.0546819888496986},{"topic":"Life","val":0.05006257822277862},{"topic":"Finance","val":0.048196609398111344},{"topic":"Food","val":0.047513937876891725},{"topic":"Beauty","val":0.040300375469336375},{"topic":"Hotel","val":0.039663215382864624},{"topic":"Hospital","val":0.03813858231880727},{"topic":"Traffic","val":0.03638639208101},{"topic":"Scenicspot","val":0.036363636363636015},{"topic":"Government","val":0.036363636363636015}]},{"site":"2305","startTime":"2014-01-14 07:55:28.56","endTime":"2014-01-14 07:56:44.81","stopTime":76,"isStop":false,"latitude":27.7862,"longitude":120.689,"topics":[{"topic":"Hospital","val":0.14748346146969452},{"topic":"Education","val":0.09898533881637712},{"topic":"Scenicspot","val":0.09181119256213081},{"topic":"Traffic","val":0.08494993742177692},{"topic":"Life","val":0.07010995887716794},{"topic":"Beauty","val":0.05491239048811022},{"topic":"Government","val":0.05261040586447339},{"topic":"Shop","val":0.046173788664401624},{"topic":"Finance","val":0.0399606651171103},{"topic":"Uptown","val":0.03991596638655423},{"topic":"Hotel","val":0.0368988020740208},{"topic":"Food","val":0.036697657786518574},{"topic":"Enterprise","val":0.035714285714285456}]},{"site":"19912","startTime":"2014-01-14 07:56:44.81","endTime":"2014-01-14 08:00:23.98","stopTime":219,"isStop":false,"latitude":27.7898,"longitude":120.6973,"topics":[{"topic":"Shop","val":0.0906587780179772},{"topic":"Enterprise","val":0.07834793491864862},{"topic":"Education","val":0.0575492092388213},{"topic":"Uptown","val":0.0546819888496986},{"topic":"Life","val":0.05006257822277862},{"topic":"Finance","val":0.048196609398111344},{"topic":"Food","val":0.047513937876891725},{"topic":"Beauty","val":0.040300375469336375},{"topic":"Hotel","val":0.039663215382864624},{"topic":"Hospital","val":0.03813858231880727},{"topic":"Traffic","val":0.03638639208101},{"topic":"Scenicspot","val":0.036363636363636015},{"topic":"Government","val":0.036363636363636015}]},{"site":"6362","startTime":"2014-01-14 08:00:23.98","endTime":"2014-01-14 08:02:41.84","stopTime":137,"isStop":false,"latitude":27.7817,"longitude":120.7001,"topics":[{"topic":"Enterprise","val":0.0743917702005545},{"topic":"Government","val":0.07237705668671104},{"topic":"Shop","val":0.0720717970634014},{"topic":"Finance","val":0.055221465856710124},{"topic":"Scenicspot","val":0.04957416282548208},{"topic":"Life","val":0.0489025916542009},{"topic":"Beauty","val":0.04884153972953897},{"topic":"Hotel","val":0.048811013767208006},{"topic":"Education","val":0.048811013767208006},{"topic":"Traffic","val":0.048780487804877044},{"topic":"Uptown","val":0.048780487804877044},{"topic":"Food","val":0.048780487804877044},{"topic":"Hospital","val":0.048780487804877044}]},{"site":"19912","startTime":"2014-01-14 08:02:41.84","endTime":"2014-01-14 08:40:47.45","stopTime":2285,"isStop":true,"latitude":27.7898,"longitude":120.6973,"topics":[{"topic":"Shop","val":0.0906587780179772},{"topic":"Enterprise","val":0.07834793491864862},{"topic":"Education","val":0.0575492092388213},{"topic":"Uptown","val":0.0546819888496986},{"topic":"Life","val":0.05006257822277862},{"topic":"Finance","val":0.048196609398111344},{"topic":"Food","val":0.047513937876891725},{"topic":"Beauty","val":0.040300375469336375},{"topic":"Hotel","val":0.039663215382864624},{"topic":"Hospital","val":0.03813858231880727},{"topic":"Traffic","val":0.03638639208101},{"topic":"Scenicspot","val":0.036363636363636015},{"topic":"Government","val":0.036363636363636015}]},{"site":"2305","startTime":"2014-01-14 08:40:47.45","endTime":"2014-01-14 08:44:17.24","stopTime":209,"isStop":false,"latitude":27.7862,"longitude":120.689,"topics":[{"topic":"Hospital","val":0.14748346146969452},{"topic":"Education","val":0.09898533881637712},{"topic":"Scenicspot","val":0.09181119256213081},{"topic":"Traffic","val":0.08494993742177692},{"topic":"Life","val":0.07010995887716794},{"topic":"Beauty","val":0.05491239048811022},{"topic":"Government","val":0.05261040586447339},{"topic":"Shop","val":0.046173788664401624},{"topic":"Finance","val":0.0399606651171103},{"topic":"Uptown","val":0.03991596638655423},{"topic":"Hotel","val":0.0368988020740208},{"topic":"Food","val":0.036697657786518574},{"topic":"Enterprise","val":0.035714285714285456}]},{"site":"19912","startTime":"2014-01-14 08:44:17.24","endTime":"2014-01-14 08:44:59.59","stopTime":42,"isStop":false,"latitude":27.7898,"longitude":120.6973,"topics":[{"topic":"Shop","val":0.0906587780179772},{"topic":"Enterprise","val":0.07834793491864862},{"topic":"Education","val":0.0575492092388213},{"topic":"Uptown","val":0.0546819888496986},{"topic":"Life","val":0.05006257822277862},{"topic":"Finance","val":0.048196609398111344},{"topic":"Food","val":0.047513937876891725},{"topic":"Beauty","val":0.040300375469336375},{"topic":"Hotel","val":0.039663215382864624},{"topic":"Hospital","val":0.03813858231880727},{"topic":"Traffic","val":0.03638639208101},{"topic":"Scenicspot","val":0.036363636363636015},{"topic":"Government","val":0.036363636363636015}]},{"site":"2305","startTime":"2014-01-14 08:44:59.59","endTime":"2014-01-14 15:36:27.24","stopTime":24687,"isStop":true,"stoppoint":1,"latitude":27.7862,"longitude":120.689,"topics":[{"topic":"Hospital","val":0.14748346146969452},{"topic":"Education","val":0.09898533881637712},{"topic":"Scenicspot","val":0.09181119256213081},{"topic":"Traffic","val":0.08494993742177692},{"topic":"Life","val":0.07010995887716794},{"topic":"Beauty","val":0.05491239048811022},{"topic":"Government","val":0.05261040586447339},{"topic":"Shop","val":0.046173788664401624},{"topic":"Finance","val":0.0399606651171103},{"topic":"Uptown","val":0.03991596638655423},{"topic":"Hotel","val":0.0368988020740208},{"topic":"Food","val":0.036697657786518574},{"topic":"Enterprise","val":0.035714285714285456}]}],"matching":true},{"pid":"460000112821423","traj":[{"site":"2305","startTime":"2014-01-14 07:55:23.7","endTime":"2014-01-14 09:57:43.46","stopTime":7339,"isStop":true,"stoppoint":0,"latitude":27.7862,"longitude":120.689,"topics":[{"topic":"Hospital","val":0.14748346146969452},{"topic":"Education","val":0.09898533881637712},{"topic":"Scenicspot","val":0.09181119256213081},{"topic":"Traffic","val":0.08494993742177692},{"topic":"Life","val":0.07010995887716794},{"topic":"Beauty","val":0.05491239048811022},{"topic":"Government","val":0.05261040586447339},{"topic":"Shop","val":0.046173788664401624},{"topic":"Finance","val":0.0399606651171103},{"topic":"Uptown","val":0.03991596638655423},{"topic":"Hotel","val":0.0368988020740208},{"topic":"Food","val":0.036697657786518574},{"topic":"Enterprise","val":0.035714285714285456}]},{"site":"28422","startTime":"2014-01-14 09:57:43.46","endTime":"2014-01-14 11:41:14.62","stopTime":6211,"isStop":true,"latitude":27.7932,"longitude":120.6912,"topics":[{"topic":"Beauty","val":0.14197491389784678},{"topic":"Government","val":0.09408658633510862},{"topic":"Shop","val":0.09003042123822663},{"topic":"Hospital","val":0.06465198286178882},{"topic":"Life","val":0.054676009245133037},{"topic":"Food","val":0.04945049925545602},{"topic":"Uptown","val":0.04535779213067365},{"topic":"Traffic","val":0.044014872605355096},{"topic":"Scenicspot","val":0.0394288481039255},{"topic":"Education","val":0.03758347569498372},{"topic":"Finance","val":0.032705114970355144},{"topic":"Enterprise","val":0.03159971862638515},{"topic":"Hotel","val":0.027972922357326027}]},{"site":"6053","startTime":"2014-01-14 11:41:14.62","endTime":"2014-01-14 11:42:10.81","stopTime":56,"isStop":false,"latitude":27.7886,"longitude":120.6826,"topics":[{"topic":"Hospital","val":0.14193668010939567},{"topic":"Government","val":0.13465906457145355},{"topic":"Finance","val":0.08726958080316445},{"topic":"Shop","val":0.06543673418934132},{"topic":"Life","val":0.05886988365085956},{"topic":"Scenicspot","val":0.05581050387057837},{"topic":"Enterprise","val":0.04196603779415612},{"topic":"Beauty","val":0.040729924751618816},{"topic":"Uptown","val":0.04008096540428649},{"topic":"Traffic","val":0.03464206801712006},{"topic":"Education","val":0.03096463171557008},{"topic":"Hotel","val":0.028446051391399496},{"topic":"Food","val":0.024737712263786286}]},{"site":"28422","startTime":"2014-01-14 11:42:10.81","endTime":"2014-01-14 12:22:26.37","stopTime":2415,"isStop":true,"latitude":27.7932,"longitude":120.6912,"topics":[{"topic":"Beauty","val":0.14197491389784678},{"topic":"Government","val":0.09408658633510862},{"topic":"Shop","val":0.09003042123822663},{"topic":"Hospital","val":0.06465198286178882},{"topic":"Life","val":0.054676009245133037},{"topic":"Food","val":0.04945049925545602},{"topic":"Uptown","val":0.04535779213067365},{"topic":"Traffic","val":0.044014872605355096},{"topic":"Scenicspot","val":0.0394288481039255},{"topic":"Education","val":0.03758347569498372},{"topic":"Finance","val":0.032705114970355144},{"topic":"Enterprise","val":0.03159971862638515},{"topic":"Hotel","val":0.027972922357326027}]},{"site":"26545","startTime":"2014-01-14 12:22:26.37","endTime":"2014-01-14 12:24:58.99","stopTime":152,"isStop":false,"latitude":27.7819,"longitude":120.6847,"topics":[{"topic":"Hospital","val":0.09505185052744469},{"topic":"Shop","val":0.07891560879670997},{"topic":"Beauty","val":0.07784283926336485},{"topic":"Uptown","val":0.06253352404791726},{"topic":"Education","val":0.06208653674235673},{"topic":"Life","val":0.06184069372429817},{"topic":"Scenicspot","val":0.05754961559091722},{"topic":"Hotel","val":0.054331306990881324},{"topic":"Food","val":0.053884319685320786},{"topic":"Finance","val":0.049436795994993454},{"topic":"Traffic","val":0.04429644198104733},{"topic":"Government","val":0.035736635079563484},{"topic":"Enterprise","val":0.035714285714285456}]},{"site":"28422","startTime":"2014-01-14 12:24:58.99","endTime":"2014-01-14 12:25:33.63","stopTime":34,"isStop":false,"latitude":27.7932,"longitude":120.6912,"topics":[{"topic":"Beauty","val":0.14197491389784678},{"topic":"Government","val":0.09408658633510862},{"topic":"Shop","val":0.09003042123822663},{"topic":"Hospital","val":0.06465198286178882},{"topic":"Life","val":0.054676009245133037},{"topic":"Food","val":0.04945049925545602},{"topic":"Uptown","val":0.04535779213067365},{"topic":"Traffic","val":0.044014872605355096},{"topic":"Scenicspot","val":0.0394288481039255},{"topic":"Education","val":0.03758347569498372},{"topic":"Finance","val":0.032705114970355144},{"topic":"Enterprise","val":0.03159971862638515},{"topic":"Hotel","val":0.027972922357326027}]},{"site":"6053","startTime":"2014-01-14 12:25:33.63","endTime":"2014-01-14 12:25:40.52","stopTime":6,"isStop":false,"latitude":27.7886,"longitude":120.6826,"topics":[{"topic":"Hospital","val":0.14193668010939567},{"topic":"Government","val":0.13465906457145355},{"topic":"Finance","val":0.08726958080316445},{"topic":"Shop","val":0.06543673418934132},{"topic":"Life","val":0.05886988365085956},{"topic":"Scenicspot","val":0.05581050387057837},{"topic":"Enterprise","val":0.04196603779415612},{"topic":"Beauty","val":0.040729924751618816},{"topic":"Uptown","val":0.04008096540428649},{"topic":"Traffic","val":0.03464206801712006},{"topic":"Education","val":0.03096463171557008},{"topic":"Hotel","val":0.028446051391399496},{"topic":"Food","val":0.024737712263786286}]},{"site":"28422","startTime":"2014-01-14 12:25:40.52","endTime":"2014-01-14 12:26:41.35","stopTime":60,"isStop":false,"latitude":27.7932,"longitude":120.6912,"topics":[{"topic":"Beauty","val":0.14197491389784678},{"topic":"Government","val":0.09408658633510862},{"topic":"Shop","val":0.09003042123822663},{"topic":"Hospital","val":0.06465198286178882},{"topic":"Life","val":0.054676009245133037},{"topic":"Food","val":0.04945049925545602},{"topic":"Uptown","val":0.04535779213067365},{"topic":"Traffic","val":0.044014872605355096},{"topic":"Scenicspot","val":0.0394288481039255},{"topic":"Education","val":0.03758347569498372},{"topic":"Finance","val":0.032705114970355144},{"topic":"Enterprise","val":0.03159971862638515},{"topic":"Hotel","val":0.027972922357326027}]},{"site":"26545","startTime":"2014-01-14 12:26:41.35","endTime":"2014-01-14 12:28:45.21","stopTime":123,"isStop":false,"latitude":27.7819,"longitude":120.6847,"topics":[{"topic":"Hospital","val":0.09505185052744469},{"topic":"Shop","val":0.07891560879670997},{"topic":"Beauty","val":0.07784283926336485},{"topic":"Uptown","val":0.06253352404791726},{"topic":"Education","val":0.06208653674235673},{"topic":"Life","val":0.06184069372429817},{"topic":"Scenicspot","val":0.05754961559091722},{"topic":"Hotel","val":0.054331306990881324},{"topic":"Food","val":0.053884319685320786},{"topic":"Finance","val":0.049436795994993454},{"topic":"Traffic","val":0.04429644198104733},{"topic":"Government","val":0.035736635079563484},{"topic":"Enterprise","val":0.035714285714285456}]},{"site":"28422","startTime":"2014-01-14 12:28:45.21","endTime":"2014-01-14 12:35:28.66","stopTime":403,"isStop":false,"latitude":27.7932,"longitude":120.6912,"topics":[{"topic":"Beauty","val":0.14197491389784678},{"topic":"Government","val":0.09408658633510862},{"topic":"Shop","val":0.09003042123822663},{"topic":"Hospital","val":0.06465198286178882},{"topic":"Life","val":0.054676009245133037},{"topic":"Food","val":0.04945049925545602},{"topic":"Uptown","val":0.04535779213067365},{"topic":"Traffic","val":0.044014872605355096},{"topic":"Scenicspot","val":0.0394288481039255},{"topic":"Education","val":0.03758347569498372},{"topic":"Finance","val":0.032705114970355144},{"topic":"Enterprise","val":0.03159971862638515},{"topic":"Hotel","val":0.027972922357326027}]},{"site":"26545","startTime":"2014-01-14 12:35:28.66","endTime":"2014-01-14 12:36:08.42","stopTime":39,"isStop":false,"latitude":27.7819,"longitude":120.6847,"topics":[{"topic":"Hospital","val":0.09505185052744469},{"topic":"Shop","val":0.07891560879670997},{"topic":"Beauty","val":0.07784283926336485},{"topic":"Uptown","val":0.06253352404791726},{"topic":"Education","val":0.06208653674235673},{"topic":"Life","val":0.06184069372429817},{"topic":"Scenicspot","val":0.05754961559091722},{"topic":"Hotel","val":0.054331306990881324},{"topic":"Food","val":0.053884319685320786},{"topic":"Finance","val":0.049436795994993454},{"topic":"Traffic","val":0.04429644198104733},{"topic":"Government","val":0.035736635079563484},{"topic":"Enterprise","val":0.035714285714285456}]},{"site":"28422","startTime":"2014-01-14 12:36:08.42","endTime":"2014-01-14 12:36:55.3","stopTime":46,"isStop":false,"latitude":27.7932,"longitude":120.6912,"topics":[{"topic":"Beauty","val":0.14197491389784678},{"topic":"Government","val":0.09408658633510862},{"topic":"Shop","val":0.09003042123822663},{"topic":"Hospital","val":0.06465198286178882},{"topic":"Life","val":0.054676009245133037},{"topic":"Food","val":0.04945049925545602},{"topic":"Uptown","val":0.04535779213067365},{"topic":"Traffic","val":0.044014872605355096},{"topic":"Scenicspot","val":0.0394288481039255},{"topic":"Education","val":0.03758347569498372},{"topic":"Finance","val":0.032705114970355144},{"topic":"Enterprise","val":0.03159971862638515},{"topic":"Hotel","val":0.027972922357326027}]},{"site":"26545","startTime":"2014-01-14 12:36:55.3","endTime":"2014-01-14 12:39:33.5","stopTime":158,"isStop":false,"latitude":27.7819,"longitude":120.6847,"topics":[{"topic":"Hospital","val":0.09505185052744469},{"topic":"Shop","val":0.07891560879670997},{"topic":"Beauty","val":0.07784283926336485},{"topic":"Uptown","val":0.06253352404791726},{"topic":"Education","val":0.06208653674235673},{"topic":"Life","val":0.06184069372429817},{"topic":"Scenicspot","val":0.05754961559091722},{"topic":"Hotel","val":0.054331306990881324},{"topic":"Food","val":0.053884319685320786},{"topic":"Finance","val":0.049436795994993454},{"topic":"Traffic","val":0.04429644198104733},{"topic":"Government","val":0.035736635079563484},{"topic":"Enterprise","val":0.035714285714285456}]},{"site":"28422","startTime":"2014-01-14 12:39:33.5","endTime":"2014-01-14 12:44:19.62","stopTime":286,"isStop":false,"latitude":27.7932,"longitude":120.6912,"topics":[{"topic":"Beauty","val":0.14197491389784678},{"topic":"Government","val":0.09408658633510862},{"topic":"Shop","val":0.09003042123822663},{"topic":"Hospital","val":0.06465198286178882},{"topic":"Life","val":0.054676009245133037},{"topic":"Food","val":0.04945049925545602},{"topic":"Uptown","val":0.04535779213067365},{"topic":"Traffic","val":0.044014872605355096},{"topic":"Scenicspot","val":0.0394288481039255},{"topic":"Education","val":0.03758347569498372},{"topic":"Finance","val":0.032705114970355144},{"topic":"Enterprise","val":0.03159971862638515},{"topic":"Hotel","val":0.027972922357326027}]},{"site":"18523","startTime":"2014-01-14 12:44:19.62","endTime":"2014-01-14 12:44:47.51","stopTime":27,"isStop":false,"latitude":27.7956,"longitude":120.6825,"topics":[{"topic":"Uptown","val":0.15101538213088805},{"topic":"Hospital","val":0.09316080584601613},{"topic":"Traffic","val":0.08094795914247553},{"topic":"Education","val":0.07682990835318272},{"topic":"Scenicspot","val":0.060438451289918385},{"topic":"Shop","val":0.056643384876256225},{"topic":"Life","val":0.053918204206871184},{"topic":"Beauty","val":0.048387096774192874},{"topic":"Enterprise","val":0.042997295005853815},{"topic":"Finance","val":0.04023174128951487},{"topic":"Hotel","val":0.03231862408655972},{"topic":"Government","val":0.03227825103960587},{"topic":"Food","val":0.03225806451612894}]},{"site":"28422","startTime":"2014-01-14 12:44:47.51","endTime":"2014-01-14 12:44:53.55","stopTime":6,"isStop":false,"latitude":27.7932,"longitude":120.6912,"topics":[{"topic":"Beauty","val":0.14197491389784678},{"topic":"Government","val":0.09408658633510862},{"topic":"Shop","val":0.09003042123822663},{"topic":"Hospital","val":0.06465198286178882},{"topic":"Life","val":0.054676009245133037},{"topic":"Food","val":0.04945049925545602},{"topic":"Uptown","val":0.04535779213067365},{"topic":"Traffic","val":0.044014872605355096},{"topic":"Scenicspot","val":0.0394288481039255},{"topic":"Education","val":0.03758347569498372},{"topic":"Finance","val":0.032705114970355144},{"topic":"Enterprise","val":0.03159971862638515},{"topic":"Hotel","val":0.027972922357326027}]},{"site":"6053","startTime":"2014-01-14 12:44:53.55","endTime":"2014-01-14 12:45:00.7","stopTime":7,"isStop":false,"latitude":27.7886,"longitude":120.6826,"topics":[{"topic":"Hospital","val":0.14193668010939567},{"topic":"Government","val":0.13465906457145355},{"topic":"Finance","val":0.08726958080316445},{"topic":"Shop","val":0.06543673418934132},{"topic":"Life","val":0.05886988365085956},{"topic":"Scenicspot","val":0.05581050387057837},{"topic":"Enterprise","val":0.04196603779415612},{"topic":"Beauty","val":0.040729924751618816},{"topic":"Uptown","val":0.04008096540428649},{"topic":"Traffic","val":0.03464206801712006},{"topic":"Education","val":0.03096463171557008},{"topic":"Hotel","val":0.028446051391399496},{"topic":"Food","val":0.024737712263786286}]},{"site":"28422","startTime":"2014-01-14 12:45:00.7","endTime":"2014-01-14 12:58:34.44","stopTime":813,"isStop":false,"latitude":27.7932,"longitude":120.6912,"topics":[{"topic":"Beauty","val":0.14197491389784678},{"topic":"Government","val":0.09408658633510862},{"topic":"Shop","val":0.09003042123822663},{"topic":"Hospital","val":0.06465198286178882},{"topic":"Life","val":0.054676009245133037},{"topic":"Food","val":0.04945049925545602},{"topic":"Uptown","val":0.04535779213067365},{"topic":"Traffic","val":0.044014872605355096},{"topic":"Scenicspot","val":0.0394288481039255},{"topic":"Education","val":0.03758347569498372},{"topic":"Finance","val":0.032705114970355144},{"topic":"Enterprise","val":0.03159971862638515},{"topic":"Hotel","val":0.027972922357326027}]},{"site":"6053","startTime":"2014-01-14 12:58:34.44","endTime":"2014-01-14 12:58:43.84","stopTime":9,"isStop":false,"latitude":27.7886,"longitude":120.6826,"topics":[{"topic":"Hospital","val":0.14193668010939567},{"topic":"Government","val":0.13465906457145355},{"topic":"Finance","val":0.08726958080316445},{"topic":"Shop","val":0.06543673418934132},{"topic":"Life","val":0.05886988365085956},{"topic":"Scenicspot","val":0.05581050387057837},{"topic":"Enterprise","val":0.04196603779415612},{"topic":"Beauty","val":0.040729924751618816},{"topic":"Uptown","val":0.04008096540428649},{"topic":"Traffic","val":0.03464206801712006},{"topic":"Education","val":0.03096463171557008},{"topic":"Hotel","val":0.028446051391399496},{"topic":"Food","val":0.024737712263786286}]},{"site":"28422","startTime":"2014-01-14 12:58:43.84","endTime":"2014-01-14 12:59:15.87","stopTime":32,"isStop":false,"latitude":27.7932,"longitude":120.6912,"topics":[{"topic":"Beauty","val":0.14197491389784678},{"topic":"Government","val":0.09408658633510862},{"topic":"Shop","val":0.09003042123822663},{"topic":"Hospital","val":0.06465198286178882},{"topic":"Life","val":0.054676009245133037},{"topic":"Food","val":0.04945049925545602},{"topic":"Uptown","val":0.04535779213067365},{"topic":"Traffic","val":0.044014872605355096},{"topic":"Scenicspot","val":0.0394288481039255},{"topic":"Education","val":0.03758347569498372},{"topic":"Finance","val":0.032705114970355144},{"topic":"Enterprise","val":0.03159971862638515},{"topic":"Hotel","val":0.027972922357326027}]},{"site":"6053","startTime":"2014-01-14 12:59:15.87","endTime":"2014-01-14 12:59:51.64","stopTime":35,"isStop":false,"latitude":27.7886,"longitude":120.6826,"topics":[{"topic":"Hospital","val":0.14193668010939567},{"topic":"Government","val":0.13465906457145355},{"topic":"Finance","val":0.08726958080316445},{"topic":"Shop","val":0.06543673418934132},{"topic":"Life","val":0.05886988365085956},{"topic":"Scenicspot","val":0.05581050387057837},{"topic":"Enterprise","val":0.04196603779415612},{"topic":"Beauty","val":0.040729924751618816},{"topic":"Uptown","val":0.04008096540428649},{"topic":"Traffic","val":0.03464206801712006},{"topic":"Education","val":0.03096463171557008},{"topic":"Hotel","val":0.028446051391399496},{"topic":"Food","val":0.024737712263786286}]},{"site":"28422","startTime":"2014-01-14 12:59:51.64","endTime":"2014-01-14 13:00:04.88","stopTime":13,"isStop":false,"latitude":27.7932,"longitude":120.6912,"topics":[{"topic":"Beauty","val":0.14197491389784678},{"topic":"Government","val":0.09408658633510862},{"topic":"Shop","val":0.09003042123822663},{"topic":"Hospital","val":0.06465198286178882},{"topic":"Life","val":0.054676009245133037},{"topic":"Food","val":0.04945049925545602},{"topic":"Uptown","val":0.04535779213067365},{"topic":"Traffic","val":0.044014872605355096},{"topic":"Scenicspot","val":0.0394288481039255},{"topic":"Education","val":0.03758347569498372},{"topic":"Finance","val":0.032705114970355144},{"topic":"Enterprise","val":0.03159971862638515},{"topic":"Hotel","val":0.027972922357326027}]},{"site":"6053","startTime":"2014-01-14 13:00:04.88","endTime":"2014-01-14 13:01:10.82","stopTime":65,"isStop":false,"latitude":27.7886,"longitude":120.6826,"topics":[{"topic":"Hospital","val":0.14193668010939567},{"topic":"Government","val":0.13465906457145355},{"topic":"Finance","val":0.08726958080316445},{"topic":"Shop","val":0.06543673418934132},{"topic":"Life","val":0.05886988365085956},{"topic":"Scenicspot","val":0.05581050387057837},{"topic":"Enterprise","val":0.04196603779415612},{"topic":"Beauty","val":0.040729924751618816},{"topic":"Uptown","val":0.04008096540428649},{"topic":"Traffic","val":0.03464206801712006},{"topic":"Education","val":0.03096463171557008},{"topic":"Hotel","val":0.028446051391399496},{"topic":"Food","val":0.024737712263786286}]},{"site":"28422","startTime":"2014-01-14 13:01:10.82","endTime":"2014-01-14 13:10:45.28","stopTime":574,"isStop":false,"latitude":27.7932,"longitude":120.6912,"topics":[{"topic":"Beauty","val":0.14197491389784678},{"topic":"Government","val":0.09408658633510862},{"topic":"Shop","val":0.09003042123822663},{"topic":"Hospital","val":0.06465198286178882},{"topic":"Life","val":0.054676009245133037},{"topic":"Food","val":0.04945049925545602},{"topic":"Uptown","val":0.04535779213067365},{"topic":"Traffic","val":0.044014872605355096},{"topic":"Scenicspot","val":0.0394288481039255},{"topic":"Education","val":0.03758347569498372},{"topic":"Finance","val":0.032705114970355144},{"topic":"Enterprise","val":0.03159971862638515},{"topic":"Hotel","val":0.027972922357326027}]},{"site":"18523","startTime":"2014-01-14 13:10:45.28","endTime":"2014-01-14 13:12:42.21","stopTime":116,"isStop":false,"latitude":27.7956,"longitude":120.6825,"topics":[{"topic":"Uptown","val":0.15101538213088805},{"topic":"Hospital","val":0.09316080584601613},{"topic":"Traffic","val":0.08094795914247553},{"topic":"Education","val":0.07682990835318272},{"topic":"Scenicspot","val":0.060438451289918385},{"topic":"Shop","val":0.056643384876256225},{"topic":"Life","val":0.053918204206871184},{"topic":"Beauty","val":0.048387096774192874},{"topic":"Enterprise","val":0.042997295005853815},{"topic":"Finance","val":0.04023174128951487},{"topic":"Hotel","val":0.03231862408655972},{"topic":"Government","val":0.03227825103960587},{"topic":"Food","val":0.03225806451612894}]},{"site":"28422","startTime":"2014-01-14 13:12:42.21","endTime":"2014-01-14 13:12:53.07","stopTime":10,"isStop":false,"latitude":27.7932,"longitude":120.6912,"topics":[{"topic":"Beauty","val":0.14197491389784678},{"topic":"Government","val":0.09408658633510862},{"topic":"Shop","val":0.09003042123822663},{"topic":"Hospital","val":0.06465198286178882},{"topic":"Life","val":0.054676009245133037},{"topic":"Food","val":0.04945049925545602},{"topic":"Uptown","val":0.04535779213067365},{"topic":"Traffic","val":0.044014872605355096},{"topic":"Scenicspot","val":0.0394288481039255},{"topic":"Education","val":0.03758347569498372},{"topic":"Finance","val":0.032705114970355144},{"topic":"Enterprise","val":0.03159971862638515},{"topic":"Hotel","val":0.027972922357326027}]},{"site":"6053","startTime":"2014-01-14 13:12:53.07","endTime":"2014-01-14 13:15:03.43","stopTime":130,"isStop":false,"latitude":27.7886,"longitude":120.6826,"topics":[{"topic":"Hospital","val":0.14193668010939567},{"topic":"Government","val":0.13465906457145355},{"topic":"Finance","val":0.08726958080316445},{"topic":"Shop","val":0.06543673418934132},{"topic":"Life","val":0.05886988365085956},{"topic":"Scenicspot","val":0.05581050387057837},{"topic":"Enterprise","val":0.04196603779415612},{"topic":"Beauty","val":0.040729924751618816},{"topic":"Uptown","val":0.04008096540428649},{"topic":"Traffic","val":0.03464206801712006},{"topic":"Education","val":0.03096463171557008},{"topic":"Hotel","val":0.028446051391399496},{"topic":"Food","val":0.024737712263786286}]},{"site":"2305","startTime":"2014-01-14 13:15:03.43","endTime":"2014-01-14 22:27:12.18","stopTime":33128,"isStop":true,"stoppoint":1,"latitude":27.7862,"longitude":120.689,"topics":[{"topic":"Hospital","val":0.14748346146969452},{"topic":"Education","val":0.09898533881637712},{"topic":"Scenicspot","val":0.09181119256213081},{"topic":"Traffic","val":0.08494993742177692},{"topic":"Life","val":0.07010995887716794},{"topic":"Beauty","val":0.05491239048811022},{"topic":"Government","val":0.05261040586447339},{"topic":"Shop","val":0.046173788664401624},{"topic":"Finance","val":0.0399606651171103},{"topic":"Uptown","val":0.03991596638655423},{"topic":"Hotel","val":0.0368988020740208},{"topic":"Food","val":0.036697657786518574},{"topic":"Enterprise","val":0.035714285714285456}]}],"matching":true},{"pid":"460000112826910","traj":[{"site":"584","startTime":"2014-01-14 05:50:54.91","endTime":"2014-01-14 07:38:13.42","stopTime":6438,"isStop":true,"stoppoint":0,"latitude":27.785,"longitude":120.6761,"topics":[{"topic":"Beauty","val":0.15814175614254664},{"topic":"Uptown","val":0.08891048020552089},{"topic":"Food","val":0.0810717344048491},{"topic":"Scenicspot","val":0.0788485607008769},{"topic":"Shop","val":0.07734997694486608},{"topic":"Education","val":0.06394506290758244},{"topic":"Hospital","val":0.061129042882550895},{"topic":"Hotel","val":0.0450398524471383},{"topic":"Finance","val":0.03795863250115318},{"topic":"Life","val":0.027353270535538262},{"topic":"Government","val":0.026365193333772904},{"topic":"Traffic","val":0.026332257427047392},{"topic":"Enterprise","val":0.026315789473684632}]},{"site":"19912","startTime":"2014-01-14 07:38:13.42","endTime":"2014-01-14 07:47:00.28","stopTime":526,"isStop":false,"latitude":27.7898,"longitude":120.6973,"topics":[{"topic":"Shop","val":0.0906587780179772},{"topic":"Enterprise","val":0.07834793491864862},{"topic":"Education","val":0.0575492092388213},{"topic":"Uptown","val":0.0546819888496986},{"topic":"Life","val":0.05006257822277862},{"topic":"Finance","val":0.048196609398111344},{"topic":"Food","val":0.047513937876891725},{"topic":"Beauty","val":0.040300375469336375},{"topic":"Hotel","val":0.039663215382864624},{"topic":"Hospital","val":0.03813858231880727},{"topic":"Traffic","val":0.03638639208101},{"topic":"Scenicspot","val":0.036363636363636015},{"topic":"Government","val":0.036363636363636015}]},{"site":"6362","startTime":"2014-01-14 07:47:00.28","endTime":"2014-01-14 07:49:56.13","stopTime":175,"isStop":false,"latitude":27.7817,"longitude":120.7001,"topics":[{"topic":"Enterprise","val":0.0743917702005545},{"topic":"Government","val":0.07237705668671104},{"topic":"Shop","val":0.0720717970634014},{"topic":"Finance","val":0.055221465856710124},{"topic":"Scenicspot","val":0.04957416282548208},{"topic":"Life","val":0.0489025916542009},{"topic":"Beauty","val":0.04884153972953897},{"topic":"Hotel","val":0.048811013767208006},{"topic":"Education","val":0.048811013767208006},{"topic":"Traffic","val":0.048780487804877044},{"topic":"Uptown","val":0.048780487804877044},{"topic":"Food","val":0.048780487804877044},{"topic":"Hospital","val":0.048780487804877044}]},{"site":"19912","startTime":"2014-01-14 07:49:56.13","endTime":"2014-01-14 07:50:08.84","stopTime":12,"isStop":false,"latitude":27.7898,"longitude":120.6973,"topics":[{"topic":"Shop","val":0.0906587780179772},{"topic":"Enterprise","val":0.07834793491864862},{"topic":"Education","val":0.0575492092388213},{"topic":"Uptown","val":0.0546819888496986},{"topic":"Life","val":0.05006257822277862},{"topic":"Finance","val":0.048196609398111344},{"topic":"Food","val":0.047513937876891725},{"topic":"Beauty","val":0.040300375469336375},{"topic":"Hotel","val":0.039663215382864624},{"topic":"Hospital","val":0.03813858231880727},{"topic":"Traffic","val":0.03638639208101},{"topic":"Scenicspot","val":0.036363636363636015},{"topic":"Government","val":0.036363636363636015}]},{"site":"2305","startTime":"2014-01-14 07:50:08.84","endTime":"2014-01-14 07:53:28.26","stopTime":199,"isStop":false,"latitude":27.7862,"longitude":120.689,"topics":[{"topic":"Hospital","val":0.14748346146969452},{"topic":"Education","val":0.09898533881637712},{"topic":"Scenicspot","val":0.09181119256213081},{"topic":"Traffic","val":0.08494993742177692},{"topic":"Life","val":0.07010995887716794},{"topic":"Beauty","val":0.05491239048811022},{"topic":"Government","val":0.05261040586447339},{"topic":"Shop","val":0.046173788664401624},{"topic":"Finance","val":0.0399606651171103},{"topic":"Uptown","val":0.03991596638655423},{"topic":"Hotel","val":0.0368988020740208},{"topic":"Food","val":0.036697657786518574},{"topic":"Enterprise","val":0.035714285714285456}]},{"site":"19912","startTime":"2014-01-14 07:53:28.26","endTime":"2014-01-14 10:28:42.09","stopTime":9313,"isStop":true,"latitude":27.7898,"longitude":120.6973,"topics":[{"topic":"Shop","val":0.0906587780179772},{"topic":"Enterprise","val":0.07834793491864862},{"topic":"Education","val":0.0575492092388213},{"topic":"Uptown","val":0.0546819888496986},{"topic":"Life","val":0.05006257822277862},{"topic":"Finance","val":0.048196609398111344},{"topic":"Food","val":0.047513937876891725},{"topic":"Beauty","val":0.040300375469336375},{"topic":"Hotel","val":0.039663215382864624},{"topic":"Hospital","val":0.03813858231880727},{"topic":"Traffic","val":0.03638639208101},{"topic":"Scenicspot","val":0.036363636363636015},{"topic":"Government","val":0.036363636363636015}]},{"site":"2305","startTime":"2014-01-14 10:28:42.09","endTime":"2014-01-14 10:46:23.64","stopTime":1061,"isStop":false,"latitude":27.7862,"longitude":120.689,"topics":[{"topic":"Hospital","val":0.14748346146969452},{"topic":"Education","val":0.09898533881637712},{"topic":"Scenicspot","val":0.09181119256213081},{"topic":"Traffic","val":0.08494993742177692},{"topic":"Life","val":0.07010995887716794},{"topic":"Beauty","val":0.05491239048811022},{"topic":"Government","val":0.05261040586447339},{"topic":"Shop","val":0.046173788664401624},{"topic":"Finance","val":0.0399606651171103},{"topic":"Uptown","val":0.03991596638655423},{"topic":"Hotel","val":0.0368988020740208},{"topic":"Food","val":0.036697657786518574},{"topic":"Enterprise","val":0.035714285714285456}]},{"site":"584","startTime":"2014-01-14 10:46:23.64","endTime":"2014-01-14 12:16:58.92","stopTime":5435,"isStop":true,"latitude":27.785,"longitude":120.6761,"topics":[{"topic":"Beauty","val":0.15814175614254664},{"topic":"Uptown","val":0.08891048020552089},{"topic":"Food","val":0.0810717344048491},{"topic":"Scenicspot","val":0.0788485607008769},{"topic":"Shop","val":0.07734997694486608},{"topic":"Education","val":0.06394506290758244},{"topic":"Hospital","val":0.061129042882550895},{"topic":"Hotel","val":0.0450398524471383},{"topic":"Finance","val":0.03795863250115318},{"topic":"Life","val":0.027353270535538262},{"topic":"Government","val":0.026365193333772904},{"topic":"Traffic","val":0.026332257427047392},{"topic":"Enterprise","val":0.026315789473684632}]},{"site":"19912","startTime":"2014-01-14 12:16:58.92","endTime":"2014-01-14 14:25:31.84","stopTime":7712,"isStop":true,"latitude":27.7898,"longitude":120.6973,"topics":[{"topic":"Shop","val":0.0906587780179772},{"topic":"Enterprise","val":0.07834793491864862},{"topic":"Education","val":0.0575492092388213},{"topic":"Uptown","val":0.0546819888496986},{"topic":"Life","val":0.05006257822277862},{"topic":"Finance","val":0.048196609398111344},{"topic":"Food","val":0.047513937876891725},{"topic":"Beauty","val":0.040300375469336375},{"topic":"Hotel","val":0.039663215382864624},{"topic":"Hospital","val":0.03813858231880727},{"topic":"Traffic","val":0.03638639208101},{"topic":"Scenicspot","val":0.036363636363636015},{"topic":"Government","val":0.036363636363636015}]},{"site":"2305","startTime":"2014-01-14 14:25:31.84","endTime":"2014-01-14 14:38:43.91","stopTime":792,"isStop":false,"latitude":27.7862,"longitude":120.689,"topics":[{"topic":"Hospital","val":0.14748346146969452},{"topic":"Education","val":0.09898533881637712},{"topic":"Scenicspot","val":0.09181119256213081},{"topic":"Traffic","val":0.08494993742177692},{"topic":"Life","val":0.07010995887716794},{"topic":"Beauty","val":0.05491239048811022},{"topic":"Government","val":0.05261040586447339},{"topic":"Shop","val":0.046173788664401624},{"topic":"Finance","val":0.0399606651171103},{"topic":"Uptown","val":0.03991596638655423},{"topic":"Hotel","val":0.0368988020740208},{"topic":"Food","val":0.036697657786518574},{"topic":"Enterprise","val":0.035714285714285456}]},{"site":"19912","startTime":"2014-01-14 14:38:43.91","endTime":"2014-01-14 14:39:24.48","stopTime":40,"isStop":false,"latitude":27.7898,"longitude":120.6973,"topics":[{"topic":"Shop","val":0.0906587780179772},{"topic":"Enterprise","val":0.07834793491864862},{"topic":"Education","val":0.0575492092388213},{"topic":"Uptown","val":0.0546819888496986},{"topic":"Life","val":0.05006257822277862},{"topic":"Finance","val":0.048196609398111344},{"topic":"Food","val":0.047513937876891725},{"topic":"Beauty","val":0.040300375469336375},{"topic":"Hotel","val":0.039663215382864624},{"topic":"Hospital","val":0.03813858231880727},{"topic":"Traffic","val":0.03638639208101},{"topic":"Scenicspot","val":0.036363636363636015},{"topic":"Government","val":0.036363636363636015}]},{"site":"2305","startTime":"2014-01-14 14:39:24.48","endTime":"2014-01-14 15:09:01.2","stopTime":1776,"isStop":true,"stoppoint":1,"latitude":27.7862,"longitude":120.689,"topics":[{"topic":"Hospital","val":0.14748346146969452},{"topic":"Education","val":0.09898533881637712},{"topic":"Scenicspot","val":0.09181119256213081},{"topic":"Traffic","val":0.08494993742177692},{"topic":"Life","val":0.07010995887716794},{"topic":"Beauty","val":0.05491239048811022},{"topic":"Government","val":0.05261040586447339},{"topic":"Shop","val":0.046173788664401624},{"topic":"Finance","val":0.0399606651171103},{"topic":"Uptown","val":0.03991596638655423},{"topic":"Hotel","val":0.0368988020740208},{"topic":"Food","val":0.036697657786518574},{"topic":"Enterprise","val":0.035714285714285456}]}],"matching":true},{"pid":"460000112828695","traj":[{"site":"25950","isStop":true,"startTime":"2014-01-14 01:48:49.41","endTime":"2014-01-14 03:48:51.03","stopTime":7201,"stoppoint":0,"latitude":27.9561,"longitude":120.6516,"topics":[{"topic":"Shop","val":0.14022651264900798},{"topic":"Beauty","val":0.09580110383881471},{"topic":"Hotel","val":0.06422987751082282},{"topic":"Hospital","val":0.053052996573585816},{"topic":"Enterprise","val":0.04390221383286483},{"topic":"Food","val":0.04095283038224008},{"topic":"Finance","val":0.03994234596524333},{"topic":"Education","val":0.03696731570200442},{"topic":"Government","val":0.028396150926362478},{"topic":"Life","val":0.023630973142657805},{"topic":"Uptown","val":0.016075422146535526},{"topic":"Traffic","val":0.016034387246353022},{"topic":"Scenicspot","val":0.010120232257535009}]},{"site":"14253","startTime":"2014-01-14 03:48:51.03","endTime":"2014-01-14 07:48:54.26","stopTime":14403,"isStop":true,"latitude":27.9524,"longitude":120.6491,"topics":[{"topic":"Shop","val":0.1244530870199317},{"topic":"Beauty","val":0.09450863165734046},{"topic":"Life","val":0.0711012732858219},{"topic":"Hospital","val":0.06783272479028508},{"topic":"Food","val":0.058337384540592806},{"topic":"Education","val":0.046566472553501694},{"topic":"Enterprise","val":0.041580901747018444},{"topic":"Hotel","val":0.0325303323369088},{"topic":"Government","val":0.03243724076583319},{"topic":"Scenicspot","val":0.030492661281146936},{"topic":"Traffic","val":0.02827915059113151},{"topic":"Finance","val":0.024741670890265584},{"topic":"Uptown","val":0.017852894630684908}]},{"site":"1180","startTime":"2014-01-14 07:48:54.26","endTime":"2014-01-14 07:53:49.77","stopTime":295,"isStop":false,"latitude":27.951,"longitude":120.6569,"topics":[{"topic":"Uptown","val":0.11924741993064933},{"topic":"Traffic","val":0.1069985022261426},{"topic":"Finance","val":0.09639098052893957},{"topic":"Enterprise","val":0.07058002831408126},{"topic":"Education","val":0.05457641724286526},{"topic":"Shop","val":0.04905722316830481},{"topic":"Life","val":0.04454338414821841},{"topic":"Scenicspot","val":0.04400993044584429},{"topic":"Hospital","val":0.04031678942940961},{"topic":"Beauty","val":0.032827920146083905},{"topic":"Hotel","val":0.0328074026959926},{"topic":"Food","val":0.0327868852459013},{"topic":"Government","val":0.0327868852459013}]},{"site":"25950","startTime":"2014-01-14 07:53:49.77","endTime":"2014-01-14 07:55:46.74","stopTime":116,"isStop":false,"latitude":27.9561,"longitude":120.6516,"topics":[{"topic":"Shop","val":0.14022651264900798},{"topic":"Beauty","val":0.09580110383881471},{"topic":"Hotel","val":0.06422987751082282},{"topic":"Hospital","val":0.053052996573585816},{"topic":"Enterprise","val":0.04390221383286483},{"topic":"Food","val":0.04095283038224008},{"topic":"Finance","val":0.03994234596524333},{"topic":"Education","val":0.03696731570200442},{"topic":"Government","val":0.028396150926362478},{"topic":"Life","val":0.023630973142657805},{"topic":"Uptown","val":0.016075422146535526},{"topic":"Traffic","val":0.016034387246353022},{"topic":"Scenicspot","val":0.010120232257535009}]},{"site":"1180","startTime":"2014-01-14 07:55:46.74","endTime":"2014-01-14 08:40:37.46","stopTime":2690,"isStop":true,"latitude":27.951,"longitude":120.6569,"topics":[{"topic":"Uptown","val":0.11924741993064933},{"topic":"Traffic","val":0.1069985022261426},{"topic":"Finance","val":0.09639098052893957},{"topic":"Enterprise","val":0.07058002831408126},{"topic":"Education","val":0.05457641724286526},{"topic":"Shop","val":0.04905722316830481},{"topic":"Life","val":0.04454338414821841},{"topic":"Scenicspot","val":0.04400993044584429},{"topic":"Hospital","val":0.04031678942940961},{"topic":"Beauty","val":0.032827920146083905},{"topic":"Hotel","val":0.0328074026959926},{"topic":"Food","val":0.0327868852459013},{"topic":"Government","val":0.0327868852459013}]},{"site":"14253","startTime":"2014-01-14 08:40:37.46","endTime":"2014-01-14 09:03:27.39","stopTime":1369,"isStop":true,"latitude":27.9524,"longitude":120.6491,"topics":[{"topic":"Shop","val":0.1244530870199317},{"topic":"Beauty","val":0.09450863165734046},{"topic":"Life","val":0.0711012732858219},{"topic":"Hospital","val":0.06783272479028508},{"topic":"Food","val":0.058337384540592806},{"topic":"Education","val":0.046566472553501694},{"topic":"Enterprise","val":0.041580901747018444},{"topic":"Hotel","val":0.0325303323369088},{"topic":"Government","val":0.03243724076583319},{"topic":"Scenicspot","val":0.030492661281146936},{"topic":"Traffic","val":0.02827915059113151},{"topic":"Finance","val":0.024741670890265584},{"topic":"Uptown","val":0.017852894630684908}]},{"site":"1180","startTime":"2014-01-14 09:03:27.39","endTime":"2014-01-14 10:19:55.49","stopTime":4588,"isStop":true,"latitude":27.951,"longitude":120.6569,"topics":[{"topic":"Uptown","val":0.11924741993064933},{"topic":"Traffic","val":0.1069985022261426},{"topic":"Finance","val":0.09639098052893957},{"topic":"Enterprise","val":0.07058002831408126},{"topic":"Education","val":0.05457641724286526},{"topic":"Shop","val":0.04905722316830481},{"topic":"Life","val":0.04454338414821841},{"topic":"Scenicspot","val":0.04400993044584429},{"topic":"Hospital","val":0.04031678942940961},{"topic":"Beauty","val":0.032827920146083905},{"topic":"Hotel","val":0.0328074026959926},{"topic":"Food","val":0.0327868852459013},{"topic":"Government","val":0.0327868852459013}]},{"site":"25950","startTime":"2014-01-14 10:19:55.49","endTime":"2014-01-14 16:58:16.06","stopTime":23900,"isStop":true,"stoppoint":1,"latitude":27.9561,"longitude":120.6516,"topics":[{"topic":"Shop","val":0.14022651264900798},{"topic":"Beauty","val":0.09580110383881471},{"topic":"Hotel","val":0.06422987751082282},{"topic":"Hospital","val":0.053052996573585816},{"topic":"Enterprise","val":0.04390221383286483},{"topic":"Food","val":0.04095283038224008},{"topic":"Finance","val":0.03994234596524333},{"topic":"Education","val":0.03696731570200442},{"topic":"Government","val":0.028396150926362478},{"topic":"Life","val":0.023630973142657805},{"topic":"Uptown","val":0.016075422146535526},{"topic":"Traffic","val":0.016034387246353022},{"topic":"Scenicspot","val":0.010120232257535009}]}],"matching":true},{"pid":"460000112841126","traj":[{"site":"15870","isStop":true,"startTime":"2014-01-14 01:19:34.47","endTime":"2014-01-14 13:36:52.6","stopTime":44238,"stoppoint":0,"latitude":28.153,"longitude":120.392,"topics":[{"topic":"Beauty","val":0.12420683364770695},{"topic":"Finance","val":0.08929909986407338},{"topic":"Government","val":0.08090453058639831},{"topic":"Hospital","val":0.07605405719885158},{"topic":"Life","val":0.07376873800664209},{"topic":"Hotel","val":0.06642282876646138},{"topic":"Shop","val":0.0653155091578649},{"topic":"Food","val":0.05267196750452089},{"topic":"Uptown","val":0.045978525788815},{"topic":"Traffic","val":0.044312016990119704},{"topic":"Education","val":0.0392509461259921},{"topic":"Enterprise","val":0.03241422119562631},{"topic":"Scenicspot","val":0.020070228007829632}]},{"site":"22903","startTime":"2014-01-14 13:36:52.6","endTime":"2014-01-14 13:39:40.32","stopTime":167,"isStop":false,"latitude":28.1555,"longitude":120.3983,"topics":[{"topic":"Beauty","val":0.1297569450397475},{"topic":"Shop","val":0.11057200496053864},{"topic":"Life","val":0.06578428515095953},{"topic":"Enterprise","val":0.06304113017984818},{"topic":"Food","val":0.06216103462661673},{"topic":"Hospital","val":0.04055868922911634},{"topic":"Traffic","val":0.03494093644452829},{"topic":"Scenicspot","val":0.031186243077819937},{"topic":"Hotel","val":0.027505843491579123},{"topic":"Education","val":0.025854235602722715},{"topic":"Finance","val":0.02372829050011148},{"topic":"Government","val":0.017681919751287432},{"topic":"Uptown","val":0.011869860156245568}]},{"site":"26978","startTime":"2014-01-14 13:39:40.32","endTime":"2014-01-14 13:39:50.54","stopTime":10,"isStop":false,"latitude":28.1553,"longitude":120.419,"topics":[{"topic":"Scenicspot","val":0.10711759264297713},{"topic":"Government","val":0.10698155302824079},{"topic":"Life","val":0.09773085922620618},{"topic":"Traffic","val":0.082412798606955},{"topic":"Hospital","val":0.05403493497306421},{"topic":"Hotel","val":0.052701746748653425},{"topic":"Education","val":0.050198617837514385},{"topic":"Uptown","val":0.05006257822277843},{"topic":"Beauty","val":0.04919192468846929},{"topic":"Finance","val":0.04502911247755359},{"topic":"Food","val":0.0436143004843012},{"topic":"Shop","val":0.04350546879251255},{"topic":"Enterprise","val":0.04347826086956539}]},{"site":"15870","startTime":"2014-01-14 13:39:50.54","endTime":"2014-01-14 13:36:52.6","stopTime":86222,"isStop":true,"stoppoint":1,"latitude":28.153,"longitude":120.392,"topics":[{"topic":"Beauty","val":0.12420683364770695},{"topic":"Finance","val":0.08929909986407338},{"topic":"Government","val":0.08090453058639831},{"topic":"Hospital","val":0.07605405719885158},{"topic":"Life","val":0.07376873800664209},{"topic":"Hotel","val":0.06642282876646138},{"topic":"Shop","val":0.0653155091578649},{"topic":"Food","val":0.05267196750452089},{"topic":"Uptown","val":0.045978525788815},{"topic":"Traffic","val":0.044312016990119704},{"topic":"Education","val":0.0392509461259921},{"topic":"Enterprise","val":0.03241422119562631},{"topic":"Scenicspot","val":0.020070228007829632}]}],"matching":true},{"pid":"460000122810648","traj":[{"site":"8176","isStop":true,"startTime":"2014-01-13 23:54:57.28","endTime":"2014-01-14 08:10:38.79","stopTime":29741,"stoppoint":0,"latitude":28.0661,"longitude":120.5849,"topics":[{"topic":"Finance","val":0.11324407307695748},{"topic":"Beauty","val":0.09246270067800577},{"topic":"Shop","val":0.09086898191084185},{"topic":"Life","val":0.07157327954907657},{"topic":"Government","val":0.07143821863660578},{"topic":"Education","val":0.06587370904277809},{"topic":"Hospital","val":0.057193794401274915},{"topic":"Uptown","val":0.045488515320409664},{"topic":"Enterprise","val":0.03375622405704983},{"topic":"Food","val":0.03163126570083121},{"topic":"Hotel","val":0.029524315466275285},{"topic":"Scenicspot","val":0.025859662707881276},{"topic":"Traffic","val":0.024689134799794756}]},{"site":"5341","startTime":"2014-01-14 08:10:38.79","endTime":"2014-01-14 08:45:34.9","stopTime":2096,"isStop":true,"stoppoint":1,"latitude":28.0645,"longitude":120.5822,"topics":[{"topic":"Shop","val":0.16994190499318182},{"topic":"Beauty","val":0.06976070834812188},{"topic":"Food","val":0.05689014252890741},{"topic":"Government","val":0.044318457773709886},{"topic":"Life","val":0.043842116077933146},{"topic":"Hospital","val":0.04238507089085231},{"topic":"Education","val":0.03679973100704248},{"topic":"Finance","val":0.035520146451721364},{"topic":"Enterprise","val":0.029196943941120507},{"topic":"Scenicspot","val":0.025815851904432804},{"topic":"Uptown","val":0.02431210655109936},{"topic":"Traffic","val":0.02277100106476395},{"topic":"Hotel","val":0.020762893915902542}]}],"matching":true},{"pid":"460000122810982","traj":[{"site":"8176","isStop":true,"startTime":"2014-01-14 01:32:21.54","endTime":"2014-01-14 07:32:09.68","stopTime":21588,"stoppoint":0,"latitude":28.0661,"longitude":120.5849,"topics":[{"topic":"Finance","val":0.11324407307695748},{"topic":"Beauty","val":0.09246270067800577},{"topic":"Shop","val":0.09086898191084185},{"topic":"Life","val":0.07157327954907657},{"topic":"Government","val":0.07143821863660578},{"topic":"Education","val":0.06587370904277809},{"topic":"Hospital","val":0.057193794401274915},{"topic":"Uptown","val":0.045488515320409664},{"topic":"Enterprise","val":0.03375622405704983},{"topic":"Food","val":0.03163126570083121},{"topic":"Hotel","val":0.029524315466275285},{"topic":"Scenicspot","val":0.025859662707881276},{"topic":"Traffic","val":0.024689134799794756}]},{"site":"12722","startTime":"2014-01-14 07:32:09.68","endTime":"2014-01-14 07:50:35.91","stopTime":1106,"isStop":false,"latitude":28.0691,"longitude":120.5728,"topics":[{"topic":"Traffic","val":0.10678241418794678},{"topic":"Shop","val":0.07514712539610616},{"topic":"Enterprise","val":0.06367001304822459},{"topic":"Life","val":0.05834420685431212},{"topic":"Scenicspot","val":0.05424333608499939},{"topic":"Finance","val":0.052459191010038694},{"topic":"Hospital","val":0.05150054589513441},{"topic":"Education","val":0.04689372353740003},{"topic":"Uptown","val":0.04391127206880899},{"topic":"Beauty","val":0.04268633664420908},{"topic":"Food","val":0.04260644955130039},{"topic":"Hotel","val":0.04260644955130039},{"topic":"Government","val":0.04260644955130039}]},{"site":"8176","startTime":"2014-01-14 07:50:35.91","endTime":"2014-01-14 07:51:23.7","stopTime":47,"isStop":false,"latitude":28.0661,"longitude":120.5849,"topics":[{"topic":"Finance","val":0.11324407307695748},{"topic":"Beauty","val":0.09246270067800577},{"topic":"Shop","val":0.09086898191084185},{"topic":"Life","val":0.07157327954907657},{"topic":"Government","val":0.07143821863660578},{"topic":"Education","val":0.06587370904277809},{"topic":"Hospital","val":0.057193794401274915},{"topic":"Uptown","val":0.045488515320409664},{"topic":"Enterprise","val":0.03375622405704983},{"topic":"Food","val":0.03163126570083121},{"topic":"Hotel","val":0.029524315466275285},{"topic":"Scenicspot","val":0.025859662707881276},{"topic":"Traffic","val":0.024689134799794756}]},{"site":"12722","startTime":"2014-01-14 07:51:23.7","endTime":"2014-01-14 07:52:38.99","stopTime":75,"isStop":false,"latitude":28.0691,"longitude":120.5728,"topics":[{"topic":"Traffic","val":0.10678241418794678},{"topic":"Shop","val":0.07514712539610616},{"topic":"Enterprise","val":0.06367001304822459},{"topic":"Life","val":0.05834420685431212},{"topic":"Scenicspot","val":0.05424333608499939},{"topic":"Finance","val":0.052459191010038694},{"topic":"Hospital","val":0.05150054589513441},{"topic":"Education","val":0.04689372353740003},{"topic":"Uptown","val":0.04391127206880899},{"topic":"Beauty","val":0.04268633664420908},{"topic":"Food","val":0.04260644955130039},{"topic":"Hotel","val":0.04260644955130039},{"topic":"Government","val":0.04260644955130039}]},{"site":"8176","startTime":"2014-01-14 07:52:38.99","endTime":"2014-01-14 07:52:52.17","stopTime":13,"isStop":false,"latitude":28.0661,"longitude":120.5849,"topics":[{"topic":"Finance","val":0.11324407307695748},{"topic":"Beauty","val":0.09246270067800577},{"topic":"Shop","val":0.09086898191084185},{"topic":"Life","val":0.07157327954907657},{"topic":"Government","val":0.07143821863660578},{"topic":"Education","val":0.06587370904277809},{"topic":"Hospital","val":0.057193794401274915},{"topic":"Uptown","val":0.045488515320409664},{"topic":"Enterprise","val":0.03375622405704983},{"topic":"Food","val":0.03163126570083121},{"topic":"Hotel","val":0.029524315466275285},{"topic":"Scenicspot","val":0.025859662707881276},{"topic":"Traffic","val":0.024689134799794756}]},{"site":"8742","startTime":"2014-01-14 07:52:52.17","endTime":"2014-01-14 08:13:37.71","stopTime":1245,"isStop":true,"latitude":28.0363,"longitude":120.5857,"topics":[{"topic":"Finance","val":0.05882352941176564},{"topic":"Enterprise","val":0.05882352941176564},{"topic":"Traffic","val":0.05882352941176564},{"topic":"Scenicspot","val":0.05882352941176564},{"topic":"Uptown","val":0.05882352941176564},{"topic":"Food","val":0.05882352941176564},{"topic":"Hospital","val":0.05882352941176564},{"topic":"Life","val":0.05882352941176564},{"topic":"Hotel","val":0.05882352941176564},{"topic":"Shop","val":0.05882352941176564},{"topic":"Government","val":0.05882352941176564},{"topic":"Beauty","val":0.05882352941176564},{"topic":"Education","val":0.05882352941176564}]},{"site":"8176","startTime":"2014-01-14 08:13:37.71","endTime":"2014-01-14 08:13:37.66","stopTime":86399,"isStop":true,"stoppoint":1,"latitude":28.0661,"longitude":120.5849,"topics":[{"topic":"Finance","val":0.11324407307695748},{"topic":"Beauty","val":0.09246270067800577},{"topic":"Shop","val":0.09086898191084185},{"topic":"Life","val":0.07157327954907657},{"topic":"Government","val":0.07143821863660578},{"topic":"Education","val":0.06587370904277809},{"topic":"Hospital","val":0.057193794401274915},{"topic":"Uptown","val":0.045488515320409664},{"topic":"Enterprise","val":0.03375622405704983},{"topic":"Food","val":0.03163126570083121},{"topic":"Hotel","val":0.029524315466275285},{"topic":"Scenicspot","val":0.025859662707881276},{"topic":"Traffic","val":0.024689134799794756}]}],"matching":true},{"pid":"460000122834481","traj":[{"site":"6163","isStop":true,"startTime":"2014-01-14 00:10:02.67","endTime":"2014-01-14 18:42:19.28","stopTime":66736,"stoppoint":0,"latitude":28.0434,"longitude":120.6273,"topics":[{"topic":"Shop","val":0.12255961663309084},{"topic":"Beauty","val":0.09572125584788485},{"topic":"Uptown","val":0.07663322468593796},{"topic":"Education","val":0.07315293849932764},{"topic":"Food","val":0.05943927235247266},{"topic":"Hotel","val":0.05065824258933282},{"topic":"Government","val":0.04777362076927659},{"topic":"Life","val":0.044996084678040046},{"topic":"Hospital","val":0.03728591220308817},{"topic":"Enterprise","val":0.033926097461398866},{"topic":"Finance","val":0.01938251691619856},{"topic":"Scenicspot","val":0.01624356649019808},{"topic":"Traffic","val":0.010715265739928903}]},{"site":"12012","startTime":"2014-01-14 18:42:19.28","endTime":"2014-01-14 18:45:07.28","stopTime":168,"isStop":false,"latitude":28.0412,"longitude":120.6286,"topics":[{"topic":"Government","val":0.17578345480870652},{"topic":"Education","val":0.10046872315885087},{"topic":"Enterprise","val":0.06532675648482214},{"topic":"Hotel","val":0.05879898893224467},{"topic":"Uptown","val":0.05619769810302165},{"topic":"Shop","val":0.04505632040050059},{"topic":"Finance","val":0.04149795086995988},{"topic":"Scenicspot","val":0.03980465778301295},{"topic":"Life","val":0.039436550590198405},{"topic":"Food","val":0.039240226754030647},{"topic":"Hospital","val":0.039240226754030647},{"topic":"Beauty","val":0.039240226754030647},{"topic":"Traffic","val":0.03921568627450968}]},{"site":"6163","startTime":"2014-01-14 18:45:07.28","endTime":"2014-01-14 20:45:10.16","stopTime":7202,"isStop":true,"stoppoint":1,"latitude":28.0434,"longitude":120.6273,"topics":[{"topic":"Shop","val":0.12255961663309084},{"topic":"Beauty","val":0.09572125584788485},{"topic":"Uptown","val":0.07663322468593796},{"topic":"Education","val":0.07315293849932764},{"topic":"Food","val":0.05943927235247266},{"topic":"Hotel","val":0.05065824258933282},{"topic":"Government","val":0.04777362076927659},{"topic":"Life","val":0.044996084678040046},{"topic":"Hospital","val":0.03728591220308817},{"topic":"Enterprise","val":0.033926097461398866},{"topic":"Finance","val":0.01938251691619856},{"topic":"Scenicspot","val":0.01624356649019808},{"topic":"Traffic","val":0.010715265739928903}]}],"matching":true},{"pid":"460000122835349","traj":[{"site":"2305","startTime":"2014-01-14 07:04:00.55","endTime":"2014-01-14 07:55:18.19","stopTime":3077,"isStop":true,"stoppoint":0,"latitude":27.7862,"longitude":120.689,"topics":[{"topic":"Hospital","val":0.14748346146969452},{"topic":"Education","val":0.09898533881637712},{"topic":"Scenicspot","val":0.09181119256213081},{"topic":"Traffic","val":0.08494993742177692},{"topic":"Life","val":0.07010995887716794},{"topic":"Beauty","val":0.05491239048811022},{"topic":"Government","val":0.05261040586447339},{"topic":"Shop","val":0.046173788664401624},{"topic":"Finance","val":0.0399606651171103},{"topic":"Uptown","val":0.03991596638655423},{"topic":"Hotel","val":0.0368988020740208},{"topic":"Food","val":0.036697657786518574},{"topic":"Enterprise","val":0.035714285714285456}]},{"site":"19912","startTime":"2014-01-14 07:55:18.19","endTime":"2014-01-14 08:39:03.61","stopTime":2625,"isStop":true,"latitude":27.7898,"longitude":120.6973,"topics":[{"topic":"Shop","val":0.0906587780179772},{"topic":"Enterprise","val":0.07834793491864862},{"topic":"Education","val":0.0575492092388213},{"topic":"Uptown","val":0.0546819888496986},{"topic":"Life","val":0.05006257822277862},{"topic":"Finance","val":0.048196609398111344},{"topic":"Food","val":0.047513937876891725},{"topic":"Beauty","val":0.040300375469336375},{"topic":"Hotel","val":0.039663215382864624},{"topic":"Hospital","val":0.03813858231880727},{"topic":"Traffic","val":0.03638639208101},{"topic":"Scenicspot","val":0.036363636363636015},{"topic":"Government","val":0.036363636363636015}]},{"site":"2305","startTime":"2014-01-14 08:39:03.61","endTime":"2014-01-14 08:45:59.31","stopTime":415,"isStop":false,"latitude":27.7862,"longitude":120.689,"topics":[{"topic":"Hospital","val":0.14748346146969452},{"topic":"Education","val":0.09898533881637712},{"topic":"Scenicspot","val":0.09181119256213081},{"topic":"Traffic","val":0.08494993742177692},{"topic":"Life","val":0.07010995887716794},{"topic":"Beauty","val":0.05491239048811022},{"topic":"Government","val":0.05261040586447339},{"topic":"Shop","val":0.046173788664401624},{"topic":"Finance","val":0.0399606651171103},{"topic":"Uptown","val":0.03991596638655423},{"topic":"Hotel","val":0.0368988020740208},{"topic":"Food","val":0.036697657786518574},{"topic":"Enterprise","val":0.035714285714285456}]},{"site":"19912","startTime":"2014-01-14 08:45:59.31","endTime":"2014-01-14 09:13:53.87","stopTime":1674,"isStop":true,"latitude":27.7898,"longitude":120.6973,"topics":[{"topic":"Shop","val":0.0906587780179772},{"topic":"Enterprise","val":0.07834793491864862},{"topic":"Education","val":0.0575492092388213},{"topic":"Uptown","val":0.0546819888496986},{"topic":"Life","val":0.05006257822277862},{"topic":"Finance","val":0.048196609398111344},{"topic":"Food","val":0.047513937876891725},{"topic":"Beauty","val":0.040300375469336375},{"topic":"Hotel","val":0.039663215382864624},{"topic":"Hospital","val":0.03813858231880727},{"topic":"Traffic","val":0.03638639208101},{"topic":"Scenicspot","val":0.036363636363636015},{"topic":"Government","val":0.036363636363636015}]},{"site":"2305","startTime":"2014-01-14 09:13:53.87","endTime":"2014-01-14 09:15:31.34","stopTime":97,"isStop":false,"latitude":27.7862,"longitude":120.689,"topics":[{"topic":"Hospital","val":0.14748346146969452},{"topic":"Education","val":0.09898533881637712},{"topic":"Scenicspot","val":0.09181119256213081},{"topic":"Traffic","val":0.08494993742177692},{"topic":"Life","val":0.07010995887716794},{"topic":"Beauty","val":0.05491239048811022},{"topic":"Government","val":0.05261040586447339},{"topic":"Shop","val":0.046173788664401624},{"topic":"Finance","val":0.0399606651171103},{"topic":"Uptown","val":0.03991596638655423},{"topic":"Hotel","val":0.0368988020740208},{"topic":"Food","val":0.036697657786518574},{"topic":"Enterprise","val":0.035714285714285456}]},{"site":"19912","startTime":"2014-01-14 09:15:31.34","endTime":"2014-01-14 10:41:01.45","stopTime":5130,"isStop":true,"latitude":27.7898,"longitude":120.6973,"topics":[{"topic":"Shop","val":0.0906587780179772},{"topic":"Enterprise","val":0.07834793491864862},{"topic":"Education","val":0.0575492092388213},{"topic":"Uptown","val":0.0546819888496986},{"topic":"Life","val":0.05006257822277862},{"topic":"Finance","val":0.048196609398111344},{"topic":"Food","val":0.047513937876891725},{"topic":"Beauty","val":0.040300375469336375},{"topic":"Hotel","val":0.039663215382864624},{"topic":"Hospital","val":0.03813858231880727},{"topic":"Traffic","val":0.03638639208101},{"topic":"Scenicspot","val":0.036363636363636015},{"topic":"Government","val":0.036363636363636015}]},{"site":"2305","startTime":"2014-01-14 10:41:01.45","endTime":"2014-01-14 12:31:36.98","stopTime":6635,"isStop":true,"stoppoint":1,"latitude":27.7862,"longitude":120.689,"topics":[{"topic":"Hospital","val":0.14748346146969452},{"topic":"Education","val":0.09898533881637712},{"topic":"Scenicspot","val":0.09181119256213081},{"topic":"Traffic","val":0.08494993742177692},{"topic":"Life","val":0.07010995887716794},{"topic":"Beauty","val":0.05491239048811022},{"topic":"Government","val":0.05261040586447339},{"topic":"Shop","val":0.046173788664401624},{"topic":"Finance","val":0.0399606651171103},{"topic":"Uptown","val":0.03991596638655423},{"topic":"Hotel","val":0.0368988020740208},{"topic":"Food","val":0.036697657786518574},{"topic":"Enterprise","val":0.035714285714285456}]}],"matching":true},{"pid":"460000122851234","traj":[{"site":"25950","isStop":true,"startTime":"2014-01-13 23:56:00.14","endTime":"2014-01-14 07:56:09.6","stopTime":28809,"stoppoint":0,"latitude":27.9561,"longitude":120.6516,"topics":[{"topic":"Shop","val":0.14022651264900798},{"topic":"Beauty","val":0.09580110383881471},{"topic":"Hotel","val":0.06422987751082282},{"topic":"Hospital","val":0.053052996573585816},{"topic":"Enterprise","val":0.04390221383286483},{"topic":"Food","val":0.04095283038224008},{"topic":"Finance","val":0.03994234596524333},{"topic":"Education","val":0.03696731570200442},{"topic":"Government","val":0.028396150926362478},{"topic":"Life","val":0.023630973142657805},{"topic":"Uptown","val":0.016075422146535526},{"topic":"Traffic","val":0.016034387246353022},{"topic":"Scenicspot","val":0.010120232257535009}]},{"site":"1180","startTime":"2014-01-14 07:56:09.6","endTime":"2014-01-14 09:08:26.29","stopTime":4336,"isStop":true,"latitude":27.951,"longitude":120.6569,"topics":[{"topic":"Uptown","val":0.11924741993064933},{"topic":"Traffic","val":0.1069985022261426},{"topic":"Finance","val":0.09639098052893957},{"topic":"Enterprise","val":0.07058002831408126},{"topic":"Education","val":0.05457641724286526},{"topic":"Shop","val":0.04905722316830481},{"topic":"Life","val":0.04454338414821841},{"topic":"Scenicspot","val":0.04400993044584429},{"topic":"Hospital","val":0.04031678942940961},{"topic":"Beauty","val":0.032827920146083905},{"topic":"Hotel","val":0.0328074026959926},{"topic":"Food","val":0.0327868852459013},{"topic":"Government","val":0.0327868852459013}]},{"site":"14253","startTime":"2014-01-14 09:08:26.29","endTime":"2014-01-14 09:14:40.11","stopTime":373,"isStop":false,"latitude":27.9524,"longitude":120.6491,"topics":[{"topic":"Shop","val":0.1244530870199317},{"topic":"Beauty","val":0.09450863165734046},{"topic":"Life","val":0.0711012732858219},{"topic":"Hospital","val":0.06783272479028508},{"topic":"Food","val":0.058337384540592806},{"topic":"Education","val":0.046566472553501694},{"topic":"Enterprise","val":0.041580901747018444},{"topic":"Hotel","val":0.0325303323369088},{"topic":"Government","val":0.03243724076583319},{"topic":"Scenicspot","val":0.030492661281146936},{"topic":"Traffic","val":0.02827915059113151},{"topic":"Finance","val":0.024741670890265584},{"topic":"Uptown","val":0.017852894630684908}]},{"site":"1180","startTime":"2014-01-14 09:14:40.11","endTime":"2014-01-14 09:17:17.28","stopTime":157,"isStop":false,"latitude":27.951,"longitude":120.6569,"topics":[{"topic":"Uptown","val":0.11924741993064933},{"topic":"Traffic","val":0.1069985022261426},{"topic":"Finance","val":0.09639098052893957},{"topic":"Enterprise","val":0.07058002831408126},{"topic":"Education","val":0.05457641724286526},{"topic":"Shop","val":0.04905722316830481},{"topic":"Life","val":0.04454338414821841},{"topic":"Scenicspot","val":0.04400993044584429},{"topic":"Hospital","val":0.04031678942940961},{"topic":"Beauty","val":0.032827920146083905},{"topic":"Hotel","val":0.0328074026959926},{"topic":"Food","val":0.0327868852459013},{"topic":"Government","val":0.0327868852459013}]},{"site":"25950","startTime":"2014-01-14 09:17:17.28","endTime":"2014-01-14 09:18:15.66","stopTime":58,"isStop":false,"latitude":27.9561,"longitude":120.6516,"topics":[{"topic":"Shop","val":0.14022651264900798},{"topic":"Beauty","val":0.09580110383881471},{"topic":"Hotel","val":0.06422987751082282},{"topic":"Hospital","val":0.053052996573585816},{"topic":"Enterprise","val":0.04390221383286483},{"topic":"Food","val":0.04095283038224008},{"topic":"Finance","val":0.03994234596524333},{"topic":"Education","val":0.03696731570200442},{"topic":"Government","val":0.028396150926362478},{"topic":"Life","val":0.023630973142657805},{"topic":"Uptown","val":0.016075422146535526},{"topic":"Traffic","val":0.016034387246353022},{"topic":"Scenicspot","val":0.010120232257535009}]},{"site":"1180","startTime":"2014-01-14 09:18:15.66","endTime":"2014-01-14 09:17:17.28","stopTime":86341,"isStop":true,"latitude":27.951,"longitude":120.6569,"topics":[{"topic":"Uptown","val":0.11924741993064933},{"topic":"Traffic","val":0.1069985022261426},{"topic":"Finance","val":0.09639098052893957},{"topic":"Enterprise","val":0.07058002831408126},{"topic":"Education","val":0.05457641724286526},{"topic":"Shop","val":0.04905722316830481},{"topic":"Life","val":0.04454338414821841},{"topic":"Scenicspot","val":0.04400993044584429},{"topic":"Hospital","val":0.04031678942940961},{"topic":"Beauty","val":0.032827920146083905},{"topic":"Hotel","val":0.0328074026959926},{"topic":"Food","val":0.0327868852459013},{"topic":"Government","val":0.0327868852459013}]},{"site":"25950","startTime":"2014-01-14 09:17:17.28","endTime":"2014-01-14 09:18:15.66","stopTime":58,"isStop":false,"latitude":27.9561,"longitude":120.6516,"topics":[{"topic":"Shop","val":0.14022651264900798},{"topic":"Beauty","val":0.09580110383881471},{"topic":"Hotel","val":0.06422987751082282},{"topic":"Hospital","val":0.053052996573585816},{"topic":"Enterprise","val":0.04390221383286483},{"topic":"Food","val":0.04095283038224008},{"topic":"Finance","val":0.03994234596524333},{"topic":"Education","val":0.03696731570200442},{"topic":"Government","val":0.028396150926362478},{"topic":"Life","val":0.023630973142657805},{"topic":"Uptown","val":0.016075422146535526},{"topic":"Traffic","val":0.016034387246353022},{"topic":"Scenicspot","val":0.010120232257535009}]},{"site":"1180","startTime":"2014-01-14 09:18:15.66","endTime":"2014-01-14 09:17:17.28","stopTime":86341,"isStop":true,"latitude":27.951,"longitude":120.6569,"topics":[{"topic":"Uptown","val":0.11924741993064933},{"topic":"Traffic","val":0.1069985022261426},{"topic":"Finance","val":0.09639098052893957},{"topic":"Enterprise","val":0.07058002831408126},{"topic":"Education","val":0.05457641724286526},{"topic":"Shop","val":0.04905722316830481},{"topic":"Life","val":0.04454338414821841},{"topic":"Scenicspot","val":0.04400993044584429},{"topic":"Hospital","val":0.04031678942940961},{"topic":"Beauty","val":0.032827920146083905},{"topic":"Hotel","val":0.0328074026959926},{"topic":"Food","val":0.0327868852459013},{"topic":"Government","val":0.0327868852459013}]},{"site":"25950","startTime":"2014-01-14 09:17:17.28","endTime":"2014-01-14 10:23:08.85","stopTime":3951,"isStop":true,"stoppoint":1,"latitude":27.9561,"longitude":120.6516,"topics":[{"topic":"Shop","val":0.14022651264900798},{"topic":"Beauty","val":0.09580110383881471},{"topic":"Hotel","val":0.06422987751082282},{"topic":"Hospital","val":0.053052996573585816},{"topic":"Enterprise","val":0.04390221383286483},{"topic":"Food","val":0.04095283038224008},{"topic":"Finance","val":0.03994234596524333},{"topic":"Education","val":0.03696731570200442},{"topic":"Government","val":0.028396150926362478},{"topic":"Life","val":0.023630973142657805},{"topic":"Uptown","val":0.016075422146535526},{"topic":"Traffic","val":0.016034387246353022},{"topic":"Scenicspot","val":0.010120232257535009}]}],"matching":true},{"pid":"460000122856246","traj":[{"site":"23399","isStop":true,"startTime":"2014-01-14 01:34:27.74","endTime":"2014-01-14 05:34:19.29","stopTime":14391,"stoppoint":0,"latitude":28.0498,"longitude":120.6511,"topics":[{"topic":"Enterprise","val":0.12873234400143027},{"topic":"Finance","val":0.06730351715154174},{"topic":"Traffic","val":0.06660110852851801},{"topic":"Life","val":0.05310209190059024},{"topic":"Food","val":0.04969221731245708},{"topic":"Shop","val":0.047546678245766534},{"topic":"Uptown","val":0.04233608336951831},{"topic":"Hospital","val":0.03860693213455612},{"topic":"Education","val":0.037406451942479324},{"topic":"Scenicspot","val":0.027151286046333455},{"topic":"Beauty","val":0.026819238333631354},{"topic":"Hotel","val":0.025567673878061883},{"topic":"Government","val":0.020420934331179523}]},{"site":"1764","startTime":"2014-01-14 05:34:19.29","endTime":"2014-01-14 07:08:39.76","stopTime":5660,"isStop":true,"stoppoint":1,"latitude":28.0485,"longitude":120.6535,"topics":[{"topic":"Traffic","val":0.11944876150132806},{"topic":"Shop","val":0.09011264080100187},{"topic":"Uptown","val":0.06915237453410174},{"topic":"Enterprise","val":0.0622894002118036},{"topic":"Hospital","val":0.05577026227839777},{"topic":"Life","val":0.05545393280061638},{"topic":"Education","val":0.054339902900604},{"topic":"Food","val":0.04478125128938655},{"topic":"Finance","val":0.03222434636702461},{"topic":"Scenicspot","val":0.03076648007811953},{"topic":"Beauty","val":0.027933268233643612},{"topic":"Hotel","val":0.026351620844737154},{"topic":"Government","val":0.022019282344689035}]}],"matching":true},{"pid":"460000132842232","traj":[{"site":"27464","startTime":"2014-01-14 01:22:17.59","endTime":"2014-01-14 07:22:22.94","stopTime":21605,"isStop":true,"stoppoint":0,"latitude":27.5797,"longitude":120.5455,"topics":[{"topic":"Beauty","val":0.18922011099733435},{"topic":"Food","val":0.1255546706109922},{"topic":"Life","val":0.09511889862328046},{"topic":"Finance","val":0.08403812846866761},{"topic":"Hospital","val":0.07847561977724767},{"topic":"Hotel","val":0.05870975082489547},{"topic":"Education","val":0.05277430120984632},{"topic":"Shop","val":0.048457035941391856},{"topic":"Enterprise","val":0.03519550953843858},{"topic":"Government","val":0.019848042376202805},{"topic":"Traffic","val":0.018489020366367306},{"topic":"Scenicspot","val":0.018014942921075847},{"topic":"Uptown","val":0.010777360589626053}]},{"site":"25996","startTime":"2014-01-14 07:22:22.94","endTime":"2014-01-14 07:54:33.56","stopTime":1930,"isStop":true,"latitude":27.6062,"longitude":120.5431,"topics":[{"topic":"Hospital","val":0.15531609633993732},{"topic":"Traffic","val":0.09882780304649057},{"topic":"Food","val":0.09844622851735374},{"topic":"Beauty","val":0.09469153515064516},{"topic":"Life","val":0.05502304710155979},{"topic":"Hotel","val":0.0534356970603496},{"topic":"Shop","val":0.05313043743703995},{"topic":"Education","val":0.0523825513599313},{"topic":"Finance","val":0.048597332030891935},{"topic":"Uptown","val":0.04151530877010844},{"topic":"Scenicspot","val":0.03260172776946743},{"topic":"Enterprise","val":0.0282670411184708},{"topic":"Government","val":0.024405506883604003}]},{"site":"27464","startTime":"2014-01-14 07:54:33.56","endTime":"2014-01-14 07:54:33.56","stopTime":0,"isStop":true,"latitude":27.5797,"longitude":120.5455,"topics":[{"topic":"Beauty","val":0.18922011099733435},{"topic":"Food","val":0.1255546706109922},{"topic":"Life","val":0.09511889862328046},{"topic":"Finance","val":0.08403812846866761},{"topic":"Hospital","val":0.07847561977724767},{"topic":"Hotel","val":0.05870975082489547},{"topic":"Education","val":0.05277430120984632},{"topic":"Shop","val":0.048457035941391856},{"topic":"Enterprise","val":0.03519550953843858},{"topic":"Government","val":0.019848042376202805},{"topic":"Traffic","val":0.018489020366367306},{"topic":"Scenicspot","val":0.018014942921075847},{"topic":"Uptown","val":0.010777360589626053}]},{"site":"15473","startTime":"2014-01-14 07:54:33.56","endTime":"2014-01-14 07:55:05.52","stopTime":31,"isStop":false,"latitude":27.6067,"longitude":120.5362,"topics":[{"topic":"Education","val":0.08402394885498723},{"topic":"Hospital","val":0.07688664885160484},{"topic":"Enterprise","val":0.06278117917667368},{"topic":"Life","val":0.05520414031052344},{"topic":"Beauty","val":0.05408788012042089},{"topic":"Uptown","val":0.054087880120420886},{"topic":"Finance","val":0.05405405405405415},{"topic":"Traffic","val":0.05405405405405415},{"topic":"Scenicspot","val":0.05405405405405415},{"topic":"Food","val":0.05405405405405415},{"topic":"Hotel","val":0.05405405405405415},{"topic":"Shop","val":0.05405405405405415},{"topic":"Government","val":0.05405405405405415}]},{"site":"22475","startTime":"2014-01-14 07:55:05.52","endTime":"2014-01-14 07:55:52.97","stopTime":47,"isStop":false,"latitude":27.5982,"longitude":120.5508,"topics":[{"topic":"Hotel","val":0.1724030037546929},{"topic":"Shop","val":0.10022471270906813},{"topic":"Finance","val":0.06802537262487215},{"topic":"Uptown","val":0.05994709295710541},{"topic":"Food","val":0.0585390829445899},{"topic":"Life","val":0.051129252474684556},{"topic":"Beauty","val":0.0493656843782001},{"topic":"Traffic","val":0.037746046194106694},{"topic":"Enterprise","val":0.030549550574582137},{"topic":"Hospital","val":0.0277193082261921},{"topic":"Education","val":0.02720730458527733},{"topic":"Scenicspot","val":0.02643929912390515},{"topic":"Government","val":0.022769939697349285}]},{"site":"24048","startTime":"2014-01-14 07:55:52.97","endTime":"2014-01-14 07:58:09.87","stopTime":136,"isStop":false,"latitude":27.5976,"longitude":120.5419,"topics":[{"topic":"Traffic","val":0.11969229829970356},{"topic":"Food","val":0.08040538477975374},{"topic":"Education","val":0.07588754235477159},{"topic":"Scenicspot","val":0.06370768338471768},{"topic":"Beauty","val":0.05897615922341853},{"topic":"Hotel","val":0.055709881254005535},{"topic":"Hospital","val":0.055465673555357815},{"topic":"Uptown","val":0.05100888305503733},{"topic":"Life","val":0.04887206569186993},{"topic":"Government","val":0.048811013767208006},{"topic":"Finance","val":0.048780487804877044},{"topic":"Enterprise","val":0.048780487804877044},{"topic":"Shop","val":0.048780487804877044}]},{"site":"26573","startTime":"2014-01-14 07:58:09.87","endTime":"2014-01-14 07:57:32.45","stopTime":86362,"isStop":true,"latitude":27.5945,"longitude":120.5451,"topics":[{"topic":"Uptown","val":0.08166458072590822},{"topic":"Shop","val":0.06299541093033047},{"topic":"Finance","val":0.05864970101515879},{"topic":"Education","val":0.05712001112501832},{"topic":"Beauty","val":0.05565985259352062},{"topic":"Life","val":0.05562508691419924},{"topic":"Hospital","val":0.05559032123487788},{"topic":"Food","val":0.05559032123487787},{"topic":"Enterprise","val":0.0555555555555565},{"topic":"Traffic","val":0.0555555555555565},{"topic":"Scenicspot","val":0.0555555555555565},{"topic":"Hotel","val":0.0555555555555565},{"topic":"Government","val":0.0555555555555565}]},{"site":"9190","startTime":"2014-01-14 07:57:32.45","endTime":"2014-01-14 07:58:09.86","stopTime":37,"isStop":false,"latitude":27.5897,"longitude":120.5484,"topics":[{"topic":"Shop","val":0.10201776611007612},{"topic":"Finance","val":0.06190665160719091},{"topic":"Life","val":0.04908574742818668},{"topic":"Beauty","val":0.04887206569186993},{"topic":"Hotel","val":0.04884153972953897},{"topic":"Education","val":0.048811013767208006},{"topic":"Enterprise","val":0.048780487804877044},{"topic":"Traffic","val":0.048780487804877044},{"topic":"Scenicspot","val":0.048780487804877044},{"topic":"Uptown","val":0.048780487804877044},{"topic":"Food","val":0.048780487804877044},{"topic":"Hospital","val":0.048780487804877044},{"topic":"Government","val":0.048780487804877044}]},{"site":"19549","startTime":"2014-01-14 07:58:09.86","endTime":"2014-01-14 09:43:27.61","stopTime":6317,"isStop":true,"stoppoint":1,"latitude":27.5928,"longitude":120.555,"topics":[{"topic":"Traffic","val":0.13624789607699314},{"topic":"Shop","val":0.09521959921165822},{"topic":"Beauty","val":0.08010012515644527},{"topic":"Hospital","val":0.07523772531756624},{"topic":"Hotel","val":0.061412973112942415},{"topic":"Life","val":0.05919756016860131},{"topic":"Finance","val":0.05219167637708043},{"topic":"Food","val":0.051788874023563816},{"topic":"Government","val":0.03413749946053302},{"topic":"Scenicspot","val":0.032814006013263584},{"topic":"Education","val":0.02903054104987541},{"topic":"Enterprise","val":0.026771970710514455},{"topic":"Uptown","val":0.02575057902838294}]}],"matching":true},{"pid":"460000132848899","traj":[{"site":"6498","startTime":"2014-01-14 05:51:39.3","endTime":"2014-01-14 07:51:42.65","stopTime":7203,"isStop":true,"stoppoint":0,"latitude":27.7834,"longitude":120.6262,"topics":[{"topic":"Hospital","val":0.122943534490577},{"topic":"Beauty","val":0.1227621483375958},{"topic":"Life","val":0.10825125609910906},{"topic":"Food","val":0.09627977000235773},{"topic":"Education","val":0.08026337269412888},{"topic":"Hotel","val":0.07246376811594203},{"topic":"Government","val":0.05693711342076065},{"topic":"Finance","val":0.04485679563222148},{"topic":"Scenicspot","val":0.0443489144038745},{"topic":"Enterprise","val":0.03371968583918271},{"topic":"Uptown","val":0.03342946799441294},{"topic":"Traffic","val":0.02900364586167448},{"topic":"Shop","val":0.028985507246376375}]},{"site":"16403","startTime":"2014-01-14 07:51:42.65","endTime":"2014-01-14 09:07:30.5","stopTime":4547,"isStop":true,"latitude":27.7902,"longitude":120.633,"topics":[{"topic":"Government","val":0.12449937421777217},{"topic":"Education","val":0.08757822277847341},{"topic":"Uptown","val":0.06714643304130169},{"topic":"Hotel","val":0.05779098873591978},{"topic":"Food","val":0.05644555694618241},{"topic":"Beauty","val":0.05553817271589457},{"topic":"Scenicspot","val":0.05050062578222764},{"topic":"Finance","val":0.050156445556946},{"topic":"Life","val":0.05009386733416753},{"topic":"Shop","val":0.05006257822277831},{"topic":"Hospital","val":0.05003128911138907},{"topic":"Enterprise","val":0.049999999999999836},{"topic":"Traffic","val":0.049999999999999836}]},{"site":"21489","startTime":"2014-01-14 09:07:30.5","endTime":"2014-01-14 09:08:00.66","stopTime":30,"isStop":false,"latitude":27.7901,"longitude":120.63,"topics":[{"topic":"Enterprise","val":0.06490752329300606},{"topic":"Shop","val":0.06417744402725718},{"topic":"Finance","val":0.058893060770408366},{"topic":"Life","val":0.05565985259352062},{"topic":"Beauty","val":0.05562508691419924},{"topic":"Traffic","val":0.0555555555555565},{"topic":"Scenicspot","val":0.0555555555555565},{"topic":"Uptown","val":0.0555555555555565},{"topic":"Food","val":0.0555555555555565},{"topic":"Hospital","val":0.0555555555555565},{"topic":"Hotel","val":0.0555555555555565},{"topic":"Government","val":0.0555555555555565},{"topic":"Education","val":0.0555555555555565}]},{"site":"15540","startTime":"2014-01-14 09:08:00.66","endTime":"2014-01-14 09:10:32.95","stopTime":152,"isStop":false,"latitude":27.7836,"longitude":120.6383,"topics":[{"topic":"Government","val":0.09633569739952773},{"topic":"Food","val":0.08813099707968325},{"topic":"Shop","val":0.08178626060353227},{"topic":"Education","val":0.07544152412738149},{"topic":"Life","val":0.07126964260881663},{"topic":"Traffic","val":0.0555207898762342},{"topic":"Beauty","val":0.051748713669865276},{"topic":"Uptown","val":0.050618829091920656},{"topic":"Hotel","val":0.04997566402447528},{"topic":"Finance","val":0.040693227645668656},{"topic":"Enterprise","val":0.03702544847726373},{"topic":"Hospital","val":0.035652204144069444},{"topic":"Scenicspot","val":0.03511333611458817}]},{"site":"23850","startTime":"2014-01-14 09:10:32.95","endTime":"2014-01-14 09:22:22.38","stopTime":709,"isStop":false,"latitude":27.7722,"longitude":120.6565,"topics":[{"topic":"Food","val":0.07394957983193347},{"topic":"Beauty","val":0.06354371535848394},{"topic":"Hotel","val":0.06250670480958344},{"topic":"Finance","val":0.05714285714285696},{"topic":"Enterprise","val":0.05714285714285696},{"topic":"Traffic","val":0.05714285714285696},{"topic":"Scenicspot","val":0.05714285714285696},{"topic":"Uptown","val":0.05714285714285696},{"topic":"Hospital","val":0.05714285714285696},{"topic":"Life","val":0.05714285714285696},{"topic":"Shop","val":0.05714285714285696},{"topic":"Government","val":0.05714285714285696},{"topic":"Education","val":0.05714285714285696}]},{"site":"556","startTime":"2014-01-14 09:22:22.38","endTime":"2014-01-14 09:40:31.19","stopTime":1088,"isStop":false,"latitude":27.7721,"longitude":120.6613,"topics":[{"topic":"Food","val":0.18556241278609767},{"topic":"Uptown","val":0.1226389308474671},{"topic":"Hospital","val":0.09428452231956555},{"topic":"Finance","val":0.09140736265159005},{"topic":"Shop","val":0.050076964021118296},{"topic":"Life","val":0.04753067771495952},{"topic":"Scenicspot","val":0.04029462115000081},{"topic":"Traffic","val":0.03956094543466685},{"topic":"Beauty","val":0.03813675139901884},{"topic":"Enterprise","val":0.034554687612389015},{"topic":"Hotel","val":0.03426697164559137},{"topic":"Education","val":0.02877159667975758},{"topic":"Government","val":0.023002891545465982}]},{"site":"20550","startTime":"2014-01-14 09:40:31.19","endTime":"2014-01-14 09:41:04.96","stopTime":33,"isStop":false,"latitude":27.7718,"longitude":120.6569,"topics":[{"topic":"Government","val":0.08050603795284614},{"topic":"Uptown","val":0.07962656022731088},{"topic":"Food","val":0.07154213036565972},{"topic":"Beauty","val":0.05963535500456672},{"topic":"Hotel","val":0.05791022561986279},{"topic":"Education","val":0.05537327064235709},{"topic":"Scenicspot","val":0.05449379291682181},{"topic":"Finance","val":0.05418935831952111},{"topic":"Life","val":0.05412170618678763},{"topic":"Enterprise","val":0.05408788012042089},{"topic":"Traffic","val":0.05408788012042089},{"topic":"Hospital","val":0.054087880120420886},{"topic":"Shop","val":0.05405405405405415}]},{"site":"4855","startTime":"2014-01-14 09:41:04.96","endTime":"2014-01-14 09:53:11.05","stopTime":726,"isStop":false,"latitude":27.7809,"longitude":120.6571,"topics":[{"topic":"Finance","val":0.05882352941176564},{"topic":"Enterprise","val":0.05882352941176564},{"topic":"Traffic","val":0.05882352941176564},{"topic":"Scenicspot","val":0.05882352941176564},{"topic":"Uptown","val":0.05882352941176564},{"topic":"Food","val":0.05882352941176564},{"topic":"Hospital","val":0.05882352941176564},{"topic":"Life","val":0.05882352941176564},{"topic":"Hotel","val":0.05882352941176564},{"topic":"Shop","val":0.05882352941176564},{"topic":"Government","val":0.05882352941176564},{"topic":"Beauty","val":0.05882352941176564},{"topic":"Education","val":0.05882352941176564}]},{"site":"24296","startTime":"2014-01-14 09:53:11.05","endTime":"2014-01-14 09:56:08.17","stopTime":177,"isStop":false,"latitude":27.7856,"longitude":120.6578,"topics":[{"topic":"Education","val":0.09095553114863018},{"topic":"Uptown","val":0.08296084391203372},{"topic":"Life","val":0.06975556179918817},{"topic":"Enterprise","val":0.06909146637378362},{"topic":"Shop","val":0.05969196189113947},{"topic":"Food","val":0.053766187325994384},{"topic":"Traffic","val":0.05233582794820067},{"topic":"Finance","val":0.04796812341958071},{"topic":"Hospital","val":0.04630788485607026},{"topic":"Scenicspot","val":0.04531174171796401},{"topic":"Beauty","val":0.04531174171796399},{"topic":"Hotel","val":0.043958008735409344},{"topic":"Government","val":0.04084186866235905}]},{"site":"11593","startTime":"2014-01-14 09:56:08.17","endTime":"2014-01-14 09:58:18.33","stopTime":130,"isStop":false,"latitude":27.7849,"longitude":120.6513,"topics":[{"topic":"Uptown","val":0.19069889994071546},{"topic":"Government","val":0.10455503590013991},{"topic":"Shop","val":0.06326987681970948},{"topic":"Traffic","val":0.05918582438574583},{"topic":"Education","val":0.058872933271853584},{"topic":"Finance","val":0.052269283973388245},{"topic":"Hospital","val":0.044479942032804635},{"topic":"Enterprise","val":0.044002371385284636},{"topic":"Food","val":0.043574204597853175},{"topic":"Life","val":0.035554311310190774},{"topic":"Scenicspot","val":0.03339700941966975},{"topic":"Beauty","val":0.03161847045649208},{"topic":"Hotel","val":0.030103418747118523}]},{"site":"3333","startTime":"2014-01-14 09:58:18.33","endTime":"2014-01-14 10:06:22.71","stopTime":484,"isStop":false,"latitude":27.7839,"longitude":120.6593,"topics":[{"topic":"Enterprise","val":0.08931896578039557},{"topic":"Uptown","val":0.0731402057449851},{"topic":"Government","val":0.07262126438535874},{"topic":"Scenicspot","val":0.04923837723984147},{"topic":"Education","val":0.04887206569186993},{"topic":"Life","val":0.04884153972953897},{"topic":"Finance","val":0.048811013767208006},{"topic":"Hospital","val":0.048811013767208006},{"topic":"Traffic","val":0.048780487804877044},{"topic":"Food","val":0.048780487804877044},{"topic":"Hotel","val":0.048780487804877044},{"topic":"Shop","val":0.048780487804877044},{"topic":"Beauty","val":0.048780487804877044}]},{"site":"11593","startTime":"2014-01-14 10:06:22.71","endTime":"2014-01-14 10:09:15.47","stopTime":172,"isStop":false,"latitude":27.7849,"longitude":120.6513,"topics":[{"topic":"Uptown","val":0.19069889994071546},{"topic":"Government","val":0.10455503590013991},{"topic":"Shop","val":0.06326987681970948},{"topic":"Traffic","val":0.05918582438574583},{"topic":"Education","val":0.058872933271853584},{"topic":"Finance","val":0.052269283973388245},{"topic":"Hospital","val":0.044479942032804635},{"topic":"Enterprise","val":0.044002371385284636},{"topic":"Food","val":0.043574204597853175},{"topic":"Life","val":0.035554311310190774},{"topic":"Scenicspot","val":0.03339700941966975},{"topic":"Beauty","val":0.03161847045649208},{"topic":"Hotel","val":0.030103418747118523}]},{"site":"20979","startTime":"2014-01-14 10:09:15.47","endTime":"2014-01-14 10:29:53.39","stopTime":1237,"isStop":true,"latitude":27.785,"longitude":120.6618,"topics":[{"topic":"Government","val":0.08284661382283486},{"topic":"Food","val":0.07321652065081441},{"topic":"Beauty","val":0.06090947017104808},{"topic":"Hotel","val":0.06024892226394188},{"topic":"Scenicspot","val":0.056042275066055705},{"topic":"Life","val":0.05559032123487788},{"topic":"Finance","val":0.0555555555555565},{"topic":"Enterprise","val":0.0555555555555565},{"topic":"Traffic","val":0.0555555555555565},{"topic":"Uptown","val":0.0555555555555565},{"topic":"Hospital","val":0.0555555555555565},{"topic":"Shop","val":0.0555555555555565},{"topic":"Education","val":0.0555555555555565}]},{"site":"11593","startTime":"2014-01-14 10:29:53.39","endTime":"2014-01-14 10:30:15.07","stopTime":21,"isStop":false,"latitude":27.7849,"longitude":120.6513,"topics":[{"topic":"Uptown","val":0.19069889994071546},{"topic":"Government","val":0.10455503590013991},{"topic":"Shop","val":0.06326987681970948},{"topic":"Traffic","val":0.05918582438574583},{"topic":"Education","val":0.058872933271853584},{"topic":"Finance","val":0.052269283973388245},{"topic":"Hospital","val":0.044479942032804635},{"topic":"Enterprise","val":0.044002371385284636},{"topic":"Food","val":0.043574204597853175},{"topic":"Life","val":0.035554311310190774},{"topic":"Scenicspot","val":0.03339700941966975},{"topic":"Beauty","val":0.03161847045649208},{"topic":"Hotel","val":0.030103418747118523}]},{"site":"3333","startTime":"2014-01-14 10:30:15.07","endTime":"2014-01-14 10:31:14.45","stopTime":59,"isStop":false,"latitude":27.7839,"longitude":120.6593,"topics":[{"topic":"Enterprise","val":0.08931896578039557},{"topic":"Uptown","val":0.0731402057449851},{"topic":"Government","val":0.07262126438535874},{"topic":"Scenicspot","val":0.04923837723984147},{"topic":"Education","val":0.04887206569186993},{"topic":"Life","val":0.04884153972953897},{"topic":"Finance","val":0.048811013767208006},{"topic":"Hospital","val":0.048811013767208006},{"topic":"Traffic","val":0.048780487804877044},{"topic":"Food","val":0.048780487804877044},{"topic":"Hotel","val":0.048780487804877044},{"topic":"Shop","val":0.048780487804877044},{"topic":"Beauty","val":0.048780487804877044}]},{"site":"11593","startTime":"2014-01-14 10:31:14.45","endTime":"2014-01-14 10:31:50.24","stopTime":35,"isStop":false,"latitude":27.7849,"longitude":120.6513,"topics":[{"topic":"Uptown","val":0.19069889994071546},{"topic":"Government","val":0.10455503590013991},{"topic":"Shop","val":0.06326987681970948},{"topic":"Traffic","val":0.05918582438574583},{"topic":"Education","val":0.058872933271853584},{"topic":"Finance","val":0.052269283973388245},{"topic":"Hospital","val":0.044479942032804635},{"topic":"Enterprise","val":0.044002371385284636},{"topic":"Food","val":0.043574204597853175},{"topic":"Life","val":0.035554311310190774},{"topic":"Scenicspot","val":0.03339700941966975},{"topic":"Beauty","val":0.03161847045649208},{"topic":"Hotel","val":0.030103418747118523}]},{"site":"7533","startTime":"2014-01-14 10:31:50.24","endTime":"2014-01-14 10:38:07.64","stopTime":377,"isStop":false,"latitude":27.7889,"longitude":120.6339,"topics":[{"topic":"Uptown","val":0.07969200631223927},{"topic":"Scenicspot","val":0.07163846111987929},{"topic":"Hospital","val":0.069108124285793},{"topic":"Government","val":0.06431952984709258},{"topic":"Shop","val":0.062850302007945},{"topic":"Hotel","val":0.060401588942700264},{"topic":"Life","val":0.059721390869021534},{"topic":"Traffic","val":0.055150459813898024},{"topic":"Beauty","val":0.05381727158948688},{"topic":"Finance","val":0.0519399249061327},{"topic":"Enterprise","val":0.050906023834140536},{"topic":"Education","val":0.04891984545899769},{"topic":"Food","val":0.043695924253142664}]},{"site":"8793","startTime":"2014-01-14 10:38:07.64","endTime":"2014-01-14 12:58:45.06","stopTime":8437,"isStop":true,"stoppoint":1,"latitude":27.7964,"longitude":120.6275,"topics":[{"topic":"Government","val":0.11349901662792741},{"topic":"Education","val":0.10135884140890446},{"topic":"Beauty","val":0.08789558376542138},{"topic":"Uptown","val":0.062345789379581656},{"topic":"Food","val":0.05959234757732921},{"topic":"Shop","val":0.058036831753978634},{"topic":"Traffic","val":0.05621312354729149},{"topic":"Hospital","val":0.047541569819417595},{"topic":"Life","val":0.04716610048274666},{"topic":"Hotel","val":0.04496692293938895},{"topic":"Enterprise","val":0.04303593777936751},{"topic":"Finance","val":0.03929912390488128},{"topic":"Scenicspot","val":0.035401394600393446}]}],"matching":true}];

/***/ }),
/* 362 */
/***/ (function(module, exports, __webpack_require__) {

	"use strict";

	Object.defineProperty(exports, "__esModule", {
	  value: true
	});
	exports.mock = mock;


	var listData = [{
	  "name": "学校",
	  "data": [{
	    "name": "学校",
	    "data": [{
	      "name": "学校",
	      "val": 0.9999999403953552,
	      "data": [{
	        "site_id": 5077,
	        "id": "e93761f6906c800f223bf204",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "温州外国语学校",
	        "type": "教育培训/中学",
	        "longitude": 120.67294311523438,
	        "latitude": 28.009475708007812
	      }, {
	        "site_id": 9676,
	        "id": "e96b44206298c02482288ac2",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "瓯海文武学校",
	        "type": "教育培训/中学",
	        "longitude": 120.56774139404297,
	        "latitude": 28.00612449645996
	      }, {
	        "site_id": 25241,
	        "id": "e9878e97809ce51e3a13fd71",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "飞腾艺术培训学校",
	        "type": "教育培训",
	        "longitude": 120.65231323242188,
	        "latitude": 27.780376434326172
	      }, {
	        "site_id": 25095,
	        "id": "ea847e775fc19176471a3213",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "状元民工子弟学校",
	        "type": "教育培训",
	        "longitude": 120.75593566894531,
	        "latitude": 27.969024658203125
	      }, {
	        "site_id": 5439,
	        "id": "e96b4420ecdf5e2782288a06",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "锦园社区市民学校",
	        "type": "教育培训",
	        "longitude": 120.6865005493164,
	        "latitude": 28.006492614746094
	      }, {
	        "site_id": 28149,
	        "id": "ea4a3dc94975b82d4e554715",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "温州越秀学校",
	        "type": "教育培训/中学",
	        "longitude": 120.81077575683594,
	        "latitude": 27.934097290039062
	      }, {
	        "site_id": 2528,
	        "id": "02ac65c44e9eea7a276e9875",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "林墨培训学校",
	        "type": "教育培训",
	        "longitude": 120.56018829345703,
	        "latitude": 27.665699005126953
	      }, {
	        "site_id": 5421,
	        "id": "eb3675035d02e91801ec01ab",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "泰顺县行政学校",
	        "type": "教育培训/高等院校",
	        "longitude": 119.7101821899414,
	        "latitude": 27.55588722229004
	      }, {
	        "site_id": 6352,
	        "id": "f244dc9a36e277fd16c905ae",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "瑞安市马屿社区学校",
	        "type": "教育培训",
	        "longitude": 120.4494400024414,
	        "latitude": 27.780773162841797
	      }, {
	        "site_id": 4770,
	        "id": "ea4a3dc94959b82d4e554739",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "永嘉县青年培训学校",
	        "type": "教育培训",
	        "longitude": 120.68338012695312,
	        "latitude": 28.15976905822754
	      }, {
	        "site_id": 6195,
	        "id": "ea4a3dc94959b82d4e554739",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "永嘉县青年培训学校",
	        "type": "教育培训",
	        "longitude": 120.68338012695312,
	        "latitude": 28.15976905822754
	      }, {
	        "site_id": 28331,
	        "id": "f44609183464e769b01e36ac",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "镇希望学校",
	        "type": "教育培训/小学",
	        "longitude": 120.89002227783203,
	        "latitude": 28.056446075439453
	      }, {
	        "site_id": 18296,
	        "id": "edd64ce1b138eeec3fe231fd",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "瑞安市万松实验学校",
	        "type": "教育培训",
	        "longitude": 120.6462631225586,
	        "latitude": 27.786102294921875
	      }, {
	        "site_id": 12595,
	        "id": "3b5beafd6af158eb94031919",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "雁荡山旅游学校",
	        "type": "教育培训",
	        "longitude": 121.15005493164062,
	        "latitude": 28.366249084472656
	      }, {
	        "site_id": 20337,
	        "id": "0dc3d82d8ef17f6f75e5b678",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "宁益乡学校",
	        "type": "教育培训/小学",
	        "longitude": 120.281005859375,
	        "latitude": 27.83042335510254
	      }, {
	        "site_id": 9321,
	        "id": "eda7491b9de5483bad31e52c",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "温州市总工会干部学校",
	        "type": "教育培训",
	        "longitude": 120.65986633300781,
	        "latitude": 28.013120651245117
	      }, {
	        "site_id": 17459,
	        "id": "f2e3d1f94b56e9d30f0e6b8b",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "福鼎市金桥学校",
	        "type": "教育培训/中学",
	        "longitude": 120.20411682128906,
	        "latitude": 27.33107566833496
	      }, {
	        "site_id": 17459,
	        "id": "f60a15ed84fe77ecba17e7b0",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "福鼎市职工业余学校",
	        "type": "教育培训",
	        "longitude": 120.21033477783203,
	        "latitude": 27.328346252441406
	      }, {
	        "site_id": 8641,
	        "id": "f300cc15e7877cf9bfa822f8",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "浙南职业学校",
	        "type": "教育培训/中学",
	        "longitude": 120.60963439941406,
	        "latitude": 27.961315155029297
	      }, {
	        "site_id": 20906,
	        "id": "eb367503e5e2516600ec01c4",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "霓南义务教育学校",
	        "type": "教育培训/小学",
	        "longitude": 121.0231704711914,
	        "latitude": 27.848573684692383
	      }, {
	        "site_id": 16484,
	        "id": "f60a15edc46eb7efbb17e702",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "乐清英华学校",
	        "type": "教育培训/中学",
	        "longitude": 120.8907241821289,
	        "latitude": 28.0297908782959
	      }, {
	        "site_id": 21638,
	        "id": "0ce65c2da152b35a2d473353",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "飞林文武学校",
	        "type": "教育培训",
	        "longitude": 120.29820251464844,
	        "latitude": 27.462244033813477
	      }, {
	        "site_id": 1811,
	        "id": "2fd2beab5f58dc517adbd236",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "晓雯音乐学校",
	        "type": "教育培训",
	        "longitude": 121.03012084960938,
	        "latitude": 28.219581604003906
	      }, {
	        "site_id": 2447,
	        "id": "feb76b3bd30ad6efcf06a4f1",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "天天艺术培训学校",
	        "type": "教育培训",
	        "longitude": 120.39923095703125,
	        "latitude": 27.511789321899414
	      }, {
	        "site_id": 28134,
	        "id": "299d701c621aaec3dbd8917b",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "蒸蒸电脑学校",
	        "type": "教育培训",
	        "longitude": 120.60445404052734,
	        "latitude": 28.02056884765625
	      }, {
	        "site_id": 6351,
	        "id": "ffd6d403b59abf8c16b54bb4",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "裕靠体育学校",
	        "type": "教育培训/其他",
	        "longitude": 120.53307342529297,
	        "latitude": 27.56888198852539
	      }, {
	        "site_id": 1636,
	        "id": "2ecbb223aeed4af2fb2d6113",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "永嘉县培智学校",
	        "type": "教育培训",
	        "longitude": 120.6803207397461,
	        "latitude": 28.153865814208984
	      }, {
	        "site_id": 20437,
	        "id": "50c04c7cbfb4bd6688f15dea",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "综艺培训学校",
	        "type": "教育培训",
	        "longitude": 120.65727996826172,
	        "latitude": 28.01656150817871
	      }, {
	        "site_id": 28251,
	        "id": "490d0cfdcb10faffddee8b5b",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "丽岙镇学校",
	        "type": "教育培训/中学",
	        "longitude": 120.63248443603516,
	        "latitude": 27.89122200012207
	      }, {
	        "site_id": 14916,
	        "id": "435d7aeab1e7e2365abbbc52",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "郊外乡中心学校",
	        "type": "教育培训",
	        "longitude": 120.59750366210938,
	        "latitude": 27.466915130615234
	      }, {
	        "site_id": 22930,
	        "id": "0ffd0cf34eee2211e91e9730",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "大山街育英学校",
	        "type": "教育培训",
	        "longitude": 120.08321380615234,
	        "latitude": 27.795629501342773
	      }, {
	        "site_id": 18085,
	        "id": "fa08d0d4eeed861c3fe2310c",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "阳光绿洲培训学校",
	        "type": "教育培训",
	        "longitude": 120.65784454345703,
	        "latitude": 28.010976791381836
	      }, {
	        "site_id": 5710,
	        "id": "08fc93dfe74ed2929b68b411",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "群英文武学校",
	        "type": "教育培训/小学",
	        "longitude": 120.61344909667969,
	        "latitude": 28.054834365844727
	      }, {
	        "site_id": 24080,
	        "id": "f7fff224309bb931cae33cb0",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "杨府山文化补习学校",
	        "type": "教育培训",
	        "longitude": 120.74186706542969,
	        "latitude": 27.974515914916992
	      }, {
	        "site_id": 9730,
	        "id": "1a6285c2c164a62d0e3dda31",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "育才电脑学校",
	        "type": "教育培训",
	        "longitude": 120.58975982666016,
	        "latitude": 28.081308364868164
	      }, {
	        "site_id": 15870,
	        "id": "f633606a3047414f0b69d909",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "青田县行政学校",
	        "type": "教育培训/高等院校",
	        "longitude": 120.2968978881836,
	        "latitude": 28.123899459838867
	      }, {
	        "site_id": 8793,
	        "id": "2bfa068bdbe9da01d4b74ee5",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "震旦培训学校",
	        "type": "教育培训",
	        "longitude": 120.62889862060547,
	        "latitude": 27.796260833740234
	      }, {
	        "site_id": 113,
	        "id": "f4a04e6ef7f7637688f15da4",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "奇奇英语培训学校",
	        "type": "教育培训",
	        "longitude": 120.83393859863281,
	        "latitude": 27.92102813720703
	      }, {
	        "site_id": 11839,
	        "id": "631c41577f75cf6231f71d69",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "英才学校",
	        "type": "教育培训/中学",
	        "longitude": 120.62985229492188,
	        "latitude": 27.907495498657227
	      }, {
	        "site_id": 23039,
	        "id": "3a0a78bd7a62cc846318090a",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "创达电脑学校",
	        "type": "教育培训",
	        "longitude": 120.55052185058594,
	        "latitude": 27.581506729125977
	      }, {
	        "site_id": 5786,
	        "id": "f0a391fd2adcfea06240a911",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "温州瓯越培训学校",
	        "type": "教育培训",
	        "longitude": 120.67491912841797,
	        "latitude": 27.99611473083496
	      }, {
	        "site_id": 15950,
	        "id": "14076dee9898a68902294f0f",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "杨府山高复学校",
	        "type": "教育培训",
	        "longitude": 120.68412017822266,
	        "latitude": 27.94593620300293
	      }, {
	        "site_id": 10395,
	        "id": "f633606a3047414f0b69d909",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "青田县行政学校",
	        "type": "教育培训/高等院校",
	        "longitude": 120.2968978881836,
	        "latitude": 28.123899459838867
	      }, {
	        "site_id": 8869,
	        "id": "53cebd50ab02dd1c2a02e1f1",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "乐舟培训学校",
	        "type": "教育培训",
	        "longitude": 120.68061065673828,
	        "latitude": 27.993757247924805
	      }, {
	        "site_id": 13355,
	        "id": "17cbb44e2f45301cee74f816",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "温州中专学校",
	        "type": "教育培训",
	        "longitude": 120.66812133789062,
	        "latitude": 27.976076126098633
	      }, {
	        "site_id": 24232,
	        "id": "3a0fbba8de6e6d3c9ee321f5",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "瑞安市社会主义学校",
	        "type": "教育培训",
	        "longitude": 120.65277862548828,
	        "latitude": 27.769163131713867
	      }, {
	        "site_id": 23399,
	        "id": "55fa2c0b1b5fd32e1ce99904",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "群英文武学校",
	        "type": "教育培训/小学",
	        "longitude": 120.65174102783203,
	        "latitude": 28.050403594970703
	      }, {
	        "site_id": 28434,
	        "id": "5ce2637d2f3adf51af31e5b0",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "福鼎电脑学校",
	        "type": "教育培训",
	        "longitude": 120.22085571289062,
	        "latitude": 27.329936981201172
	      }, {
	        "site_id": 10694,
	        "id": "803708f18d107804018972fa",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "洞头县行政学校",
	        "type": "教育培训",
	        "longitude": 121.1480941772461,
	        "latitude": 27.839115142822266
	      }, {
	        "site_id": 18320,
	        "id": "5211d219936c325b7dc8766d",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "邹鲁辅导学校",
	        "type": "教育培训",
	        "longitude": 120.64908599853516,
	        "latitude": 27.778270721435547
	      }, {
	        "site_id": 19725,
	        "id": "7725193086382c5dcae33c12",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "水亭中心学校",
	        "type": "教育培训",
	        "longitude": 120.52151489257812,
	        "latitude": 27.686866760253906
	      }, {
	        "site_id": 7759,
	        "id": "53cebd50d34865622a02e102",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "春晖学校",
	        "type": "教育培训/中学",
	        "longitude": 120.87651062011719,
	        "latitude": 28.04810905456543
	      }, {
	        "site_id": 23518,
	        "id": "b14386027142bb9b3310af36",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "艺缘艺术学校",
	        "type": "教育培训",
	        "longitude": 120.67716217041016,
	        "latitude": 27.784027099609375
	      }, {
	        "site_id": 6971,
	        "id": "8b60775745654b135cbe86f9",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "埔坪学校",
	        "type": "教育培训/小学",
	        "longitude": 120.36520385742188,
	        "latitude": 27.365192413330078
	      }, {
	        "site_id": 14531,
	        "id": "a74481d642d56a3bf503b8c0",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "潜龙学校",
	        "type": "教育培训/小学",
	        "longitude": 120.56941223144531,
	        "latitude": 27.570863723754883
	      }, {
	        "site_id": 27240,
	        "id": "9f2a070e9f36ebce6f476047",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "育才培训学校",
	        "type": "教育培训",
	        "longitude": 120.69221496582031,
	        "latitude": 28.008363723754883
	      }, {
	        "site_id": 3335,
	        "id": "9f2a070e9f36ebce6f476047",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "育才培训学校",
	        "type": "教育培训",
	        "longitude": 120.69221496582031,
	        "latitude": 28.008363723754883
	      }, {
	        "site_id": 21742,
	        "id": "6deee308d630f56929fc4865",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "林溪乡学校",
	        "type": "教育培训/中学",
	        "longitude": 120.43092346191406,
	        "latitude": 27.92296028137207
	      }, {
	        "site_id": 17476,
	        "id": "654ddaf625cc05fc246e98d9",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "龙湾区外国语学校",
	        "type": "教育培训",
	        "longitude": 120.75704956054688,
	        "latitude": 27.932889938354492
	      }, {
	        "site_id": 10185,
	        "id": "33fbfa83c78369c20bf5eb7d",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "科兴计算机学校",
	        "type": "教育培训",
	        "longitude": 120.54747009277344,
	        "latitude": 27.65964698791504
	      }, {
	        "site_id": 12179,
	        "id": "bccd0e61882ad8fcb54f5455",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "振华民工子弟学校",
	        "type": "教育培训",
	        "longitude": 120.78616333007812,
	        "latitude": 27.85947036743164
	      }, {
	        "site_id": 4824,
	        "id": "fe69503bf95172c47ab55cb0",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "越英语培训学校",
	        "type": "教育培训",
	        "longitude": 120.56089782714844,
	        "latitude": 27.671255111694336
	      }, {
	        "site_id": 23930,
	        "id": "1aaeeab3c03e7485ece4a5b9",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "振清外国语学校",
	        "type": "教育培训",
	        "longitude": 120.52786254882812,
	        "latitude": 27.573579788208008
	      }, {
	        "site_id": 10395,
	        "id": "59948b39792081d9128824f3",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "青田县江南学校",
	        "type": "教育培训",
	        "longitude": 120.28135681152344,
	        "latitude": 28.134443283081055
	      }, {
	        "site_id": 25524,
	        "id": "14076dee9892a68902294f01",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "凤凰学校",
	        "type": "教育培训/小学",
	        "longitude": 120.84818267822266,
	        "latitude": 28.082162857055664
	      }, {
	        "site_id": 28108,
	        "id": "8ab0dfc437d77815d8eced7d",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "平阳文武学校",
	        "type": "教育培训",
	        "longitude": 120.57469177246094,
	        "latitude": 27.653329849243164
	      }, {
	        "site_id": 7867,
	        "id": "1bf85b253698bbd9a1a934fd",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "东都学校",
	        "type": "教育培训/小学",
	        "longitude": 120.65542602539062,
	        "latitude": 27.748828887939453
	      }, {
	        "site_id": 2124,
	        "id": "86c36027f16b72d172455b02",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "俊雅音乐学校",
	        "type": "教育培训",
	        "longitude": 120.82597351074219,
	        "latitude": 27.924484252929688
	      }, {
	        "site_id": 6592,
	        "id": "d29ba7bb993f94c842d63e2e",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "林墨培训学校",
	        "type": "教育培训",
	        "longitude": 120.55616760253906,
	        "latitude": 27.678592681884766
	      }, {
	        "site_id": 16096,
	        "id": "bc547bdac9a71d5541024506",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "天宇职业学校",
	        "type": "教育培训",
	        "longitude": 120.74246978759766,
	        "latitude": 27.97766876220703
	      }, {
	        "site_id": 24571,
	        "id": "4d4aaadad0997538cd04efb4",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "温州侨商学校",
	        "type": "教育培训",
	        "longitude": 119.75237274169922,
	        "latitude": 27.585044860839844
	      }, {
	        "site_id": 8773,
	        "id": "3c5e3e2c384e6a71b6147f4c",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "周山中心学校",
	        "type": "教育培训",
	        "longitude": 120.16650390625,
	        "latitude": 27.72138214111328
	      }, {
	        "site_id": 3710,
	        "id": "52f3bc0c9cd75d14d5851f05",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "番垟新村学校",
	        "type": "教育培训",
	        "longitude": 119.92488098144531,
	        "latitude": 27.618379592895508
	      }, {
	        "site_id": 1764,
	        "id": "55fa2c0b1b5fd32e1ce99904",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "群英文武学校",
	        "type": "教育培训/小学",
	        "longitude": 120.65174102783203,
	        "latitude": 28.050403594970703
	      }, {
	        "site_id": 25304,
	        "id": "9382719312fa6ddbaecb660e",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "叮叮当才艺学校",
	        "type": "教育培训",
	        "longitude": 120.57105255126953,
	        "latitude": 27.96390151977539
	      }, {
	        "site_id": 17621,
	        "id": "af0caf666115955f78dbd263",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "雅阳镇社区学校",
	        "type": "教育培训",
	        "longitude": 120.0398941040039,
	        "latitude": 27.390138626098633
	      }, {
	        "site_id": 23862,
	        "id": "70bf8c4e57c86b2540d63e65",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "红星电脑学校",
	        "type": "教育培训",
	        "longitude": 120.47701263427734,
	        "latitude": 28.16754150390625
	      }, {
	        "site_id": 20242,
	        "id": "61f634f629d1d57b6b49f37d",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "晓雯音乐学校",
	        "type": "教育培训",
	        "longitude": 120.5465087890625,
	        "latitude": 27.60576629638672
	      }, {
	        "site_id": 27740,
	        "id": "6deee308f4dfd31429fc4807",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "育人培训学校",
	        "type": "教育培训",
	        "longitude": 120.55687713623047,
	        "latitude": 27.665815353393555
	      }, {
	        "site_id": 18818,
	        "id": "2ea60375222cf02ead080f02",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "温州行知学校",
	        "type": "教育培训",
	        "longitude": 120.69642639160156,
	        "latitude": 27.984859466552734
	      }, {
	        "site_id": 7786,
	        "id": "f961801580cb357603ff2d74",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "管阳镇金钗溪村普法学校",
	        "type": "教育培训",
	        "longitude": 120.06582641601562,
	        "latitude": 27.273635864257812
	      }, {
	        "site_id": 8293,
	        "id": "6deee308d630f56929fc4865",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "林溪乡学校",
	        "type": "教育培训/中学",
	        "longitude": 120.43092346191406,
	        "latitude": 27.92296028137207
	      }, {
	        "site_id": 23296,
	        "id": "59ca790d21851a2a715e8037",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "金雅音乐学校",
	        "type": "教育培训",
	        "longitude": 120.82555389404297,
	        "latitude": 27.924192428588867
	      }, {
	        "site_id": 19283,
	        "id": "9a9341dc5301b7a302294fe1",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "音缘音乐学校",
	        "type": "教育培训",
	        "longitude": 120.71549987792969,
	        "latitude": 27.995616912841797
	      }, {
	        "site_id": 7553,
	        "id": "3f9337d64c10d6b0f7231048",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "云岩学校",
	        "type": "教育培训/中学",
	        "longitude": 120.49345397949219,
	        "latitude": 27.514501571655273
	      }, {
	        "site_id": 7521,
	        "id": "7725193086382c5dcae33c12",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "水亭中心学校",
	        "type": "教育培训",
	        "longitude": 120.52151489257812,
	        "latitude": 27.686866760253906
	      }, {
	        "site_id": 13992,
	        "id": "61f634f61c79c8796b49f3db",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "木栗学校",
	        "type": "教育培训/小学",
	        "longitude": 120.34174346923828,
	        "latitude": 28.44843101501465
	      }, {
	        "site_id": 27464,
	        "id": "72665f15a198e7894afc8d17",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "创雅电脑学校",
	        "type": "教育培训",
	        "longitude": 120.54637145996094,
	        "latitude": 27.57975196838379
	      }, {
	        "site_id": 24650,
	        "id": "3c5e3e2c384e6a71b6147f4c",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "周山中心学校",
	        "type": "教育培训",
	        "longitude": 120.16650390625,
	        "latitude": 27.72138214111328
	      }, {
	        "site_id": 15870,
	        "id": "59948b39792081d9128824f3",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "青田县江南学校",
	        "type": "教育培训",
	        "longitude": 120.28135681152344,
	        "latitude": 28.134443283081055
	      }, {
	        "site_id": 28384,
	        "id": "832c6e680c457301343a718c",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "震撼音乐学校",
	        "type": "教育培训",
	        "longitude": 120.65421295166016,
	        "latitude": 27.777435302734375
	      }, {
	        "site_id": 23416,
	        "id": "b388f9fd6c9c933fb35c6a9d",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "林立电脑学校",
	        "type": "教育培训",
	        "longitude": 120.89530944824219,
	        "latitude": 28.049482345581055
	      }, {
	        "site_id": 1326,
	        "id": "859b7bcca0d99f9095ca0795",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "福鼎市三田学校",
	        "type": "教育培训",
	        "longitude": 120.31526184082031,
	        "latitude": 27.255653381347656
	      }, {
	        "site_id": 2387,
	        "id": "1eca06235ca93774e81e978f",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "吉田造型艺术学校",
	        "type": "教育培训",
	        "longitude": 120.65492248535156,
	        "latitude": 28.00705909729004
	      }, {
	        "site_id": 22321,
	        "id": "33fbfa83c76069c20bf5eb1c",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "新纪元学校",
	        "type": "教育培训/中学",
	        "longitude": 120.64534759521484,
	        "latitude": 27.987873077392578
	      }, {
	        "site_id": 5113,
	        "id": "6deee308f4dfd31429fc4807",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "育人培训学校",
	        "type": "教育培训",
	        "longitude": 120.55687713623047,
	        "latitude": 27.665815353393555
	      }, {
	        "site_id": 21128,
	        "id": "865a235d62ebd98f95f08c89",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "瑞安天宇学校",
	        "type": "教育培训",
	        "longitude": 120.65966796875,
	        "latitude": 27.774991989135742
	      }, {
	        "site_id": 28634,
	        "id": "774a5957ff3c8016c8213de1",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "浩铭电脑学校",
	        "type": "教育培训",
	        "longitude": 120.65603637695312,
	        "latitude": 28.043373107910156
	      }, {
	        "site_id": 18659,
	        "id": "be1ea5efa895bdbc14948e04",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "晓雯音乐学校",
	        "type": "教育培训",
	        "longitude": 120.66410827636719,
	        "latitude": 28.02324867248535
	      }, {
	        "site_id": 21148,
	        "id": "8b96aeff7bc12b52f219375e",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "七彩虹培训学校",
	        "type": "教育培训",
	        "longitude": 120.67361450195312,
	        "latitude": 27.979589462280273
	      }, {
	        "site_id": 18707,
	        "id": "d6eef9d41eaa12f3a2a934bd",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "平阳县育才学校",
	        "type": "教育培训",
	        "longitude": 120.57525634765625,
	        "latitude": 27.65570640563965
	      }, {
	        "site_id": 22679,
	        "id": "9de9a3e99a47e9da7ec876df",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "晓雯音乐学校",
	        "type": "教育培训",
	        "longitude": 120.62452697753906,
	        "latitude": 27.777353286743164
	      }, {
	        "site_id": 22258,
	        "id": "ae8cc7d9ec13f0b06cc02c39",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "莘莘培训学校",
	        "type": "教育培训",
	        "longitude": 120.4031982421875,
	        "latitude": 27.511272430419922
	      }, {
	        "site_id": 10391,
	        "id": "d5f93c0edba3645feddfca78",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "曙光学校",
	        "type": "教育培训/中学",
	        "longitude": 120.07721710205078,
	        "latitude": 27.791643142700195
	      }, {
	        "site_id": 6163,
	        "id": "72e57fd791f078d2f8dc85ff",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "东瓯电脑学校",
	        "type": "教育培训",
	        "longitude": 120.62833404541016,
	        "latitude": 28.043121337890625
	      }, {
	        "site_id": 7662,
	        "id": "a8ef389f2627797e95ca074e",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "温州市华夏学校",
	        "type": "教育培训",
	        "longitude": 120.62673950195312,
	        "latitude": 27.96579360961914
	      }, {
	        "site_id": 14607,
	        "id": "fdc9ab5bed3bb6bb08f5ebf6",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "教师进修学校",
	        "type": "教育培训",
	        "longitude": 120.57093811035156,
	        "latitude": 27.659202575683594
	      }, {
	        "site_id": 15757,
	        "id": "d6c5d991c2c10c9530169bc5",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "藤桥镇社区学校",
	        "type": "教育培训",
	        "longitude": 120.49894714355469,
	        "latitude": 28.072429656982422
	      }, {
	        "site_id": 11876,
	        "id": "d6c5d991c2c10c9530169bc5",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "藤桥镇社区学校",
	        "type": "教育培训",
	        "longitude": 120.49894714355469,
	        "latitude": 28.072429656982422
	      }, {
	        "site_id": 25289,
	        "id": "9fbfeef2628b1d3bede4a538",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "育林培训学校",
	        "type": "教育培训",
	        "longitude": 120.63970947265625,
	        "latitude": 28.00747299194336
	      }, {
	        "site_id": 1622,
	        "id": "dffd631cefa880bd38221ce0",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "树人电脑学校",
	        "type": "教育培训",
	        "longitude": 120.54901885986328,
	        "latitude": 27.58068084716797
	      }, {
	        "site_id": 16781,
	        "id": "befdb292c20acd9c8715413f",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "慧谷学校",
	        "type": "教育培训",
	        "longitude": 120.96358489990234,
	        "latitude": 28.128198623657227
	      }, {
	        "site_id": 28434,
	        "id": "d7fd2ad3fdf302540accddb0",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "茂华学校",
	        "type": "教育培训/中学",
	        "longitude": 120.2318115234375,
	        "latitude": 27.308818817138672
	      }, {
	        "site_id": 3681,
	        "id": "500a55a866fbbd9142d63eb2",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "民族学校",
	        "type": "教育培训",
	        "longitude": 120.64151000976562,
	        "latitude": 27.69548225402832
	      }, {
	        "site_id": 24146,
	        "id": "e93761f69097800f223bf27d",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "育人培训学校",
	        "type": "教育培训",
	        "longitude": 120.55003356933594,
	        "latitude": 27.600677490234375
	      }, {
	        "site_id": 6454,
	        "id": "f5ca6b30d7e4c9b703ff2dcc",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "永兴利民学校",
	        "type": "教育培训",
	        "longitude": 120.81444549560547,
	        "latitude": 27.896623611450195
	      }, {
	        "site_id": 1515,
	        "id": "a3e8341c787d93eb4afd3f38",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "苏步青学校",
	        "type": "教育培训/中学",
	        "longitude": 120.31964874267578,
	        "latitude": 27.681848526000977
	      }, {
	        "site_id": 7211,
	        "id": "826eda93be6c6533c7f50de5",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "叮叮当才艺学校",
	        "type": "教育培训",
	        "longitude": 120.68252563476562,
	        "latitude": 27.9879207611084
	      }, {
	        "site_id": 2776,
	        "id": "c3269b3648cabb9a4bfc8db8",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "曙光培训学校",
	        "type": "教育培训",
	        "longitude": 120.65300750732422,
	        "latitude": 28.015090942382812
	      }, {
	        "site_id": 14145,
	        "id": "d7fd2ad3fdf302540accddb0",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "茂华学校",
	        "type": "教育培训/中学",
	        "longitude": 120.2318115234375,
	        "latitude": 27.308818817138672
	      }, {
	        "site_id": 2324,
	        "id": "63445fe1222807dee5e965a6",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "龙湖镇中心学校",
	        "type": "教育培训",
	        "longitude": 120.28028106689453,
	        "latitude": 27.765228271484375
	      }, {
	        "site_id": 26364,
	        "id": "ee9ca423d5571e346ca3cc16",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "文成县敬业学校",
	        "type": "教育培训",
	        "longitude": 120.03914642333984,
	        "latitude": 27.790822982788086
	      }, {
	        "site_id": 8091,
	        "id": "d24e48eb2c0a6bb7afee7a4c",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "炎亭镇学校",
	        "type": "教育培训/中学",
	        "longitude": 120.64669799804688,
	        "latitude": 27.452342987060547
	      }, {
	        "site_id": 17768,
	        "id": "14076dee9898a68902294f0f",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "杨府山高复学校",
	        "type": "教育培训",
	        "longitude": 120.68412017822266,
	        "latitude": 27.94593620300293
	      }, {
	        "site_id": 584,
	        "id": "b14386027142bb9b3310af36",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "艺缘艺术学校",
	        "type": "教育培训",
	        "longitude": 120.67716217041016,
	        "latitude": 27.784027099609375
	      }, {
	        "site_id": 5081,
	        "id": "78bd50f7ede3945831870a39",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "赤水垟学校",
	        "type": "教育培训/小学",
	        "longitude": 120.85114288330078,
	        "latitude": 28.151012420654297
	      }, {
	        "site_id": 8562,
	        "id": "3947346e337cca98bfa82281",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "南宋学校",
	        "type": "教育培训",
	        "longitude": 120.38946533203125,
	        "latitude": 27.3907470703125
	      }, {
	        "site_id": 9451,
	        "id": "d7fd2ad34bc2fc560accdde2",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "晓雯音乐学校",
	        "type": "教育培训",
	        "longitude": 120.65567016601562,
	        "latitude": 28.04619789123535
	      }, {
	        "site_id": 20734,
	        "id": "307d818dc9957259213bf249",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "群星学校",
	        "type": "教育培训",
	        "longitude": 120.73416137695312,
	        "latitude": 28.246627807617188
	      }, {
	        "site_id": 21665,
	        "id": "62d462da8b1ced83b6147fd4",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "海韵音乐学校",
	        "type": "教育培训",
	        "longitude": 120.65937042236328,
	        "latitude": 28.0098934173584
	      }, {
	        "site_id": 11001,
	        "id": "a5097bfa55960a62f03cba7a",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "温州市国际象棋学校",
	        "type": "教育培训",
	        "longitude": 120.65290832519531,
	        "latitude": 28.01358413696289
	      }, {
	        "site_id": 14327,
	        "id": "87741375f1e4d231581093e9",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "春华教育学校",
	        "type": "教育培训",
	        "longitude": 120.69409942626953,
	        "latitude": 27.831104278564453
	      }, {
	        "site_id": 6878,
	        "id": "e804b71ea71d670c3686f408",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "胡克聪学校",
	        "type": "教育培训",
	        "longitude": 120.12086486816406,
	        "latitude": 27.926740646362305
	      }, {
	        "site_id": 22951,
	        "id": "dffd631c130124bf38221c48",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "风铃轩培训学校",
	        "type": "教育培训",
	        "longitude": 120.40072631835938,
	        "latitude": 27.510913848876953
	      }, {
	        "site_id": 16082,
	        "id": "4b98d76f2f1284be14c8f681",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "棠溪学校",
	        "type": "教育培训",
	        "longitude": 119.67547607421875,
	        "latitude": 27.234865188598633
	      }, {
	        "site_id": 18501,
	        "id": "eba0d9bc5f4f1c1784420e91",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "东方江南学校",
	        "type": "教育培训",
	        "longitude": 120.57784271240234,
	        "latitude": 27.485305786132812
	      }, {
	        "site_id": 7373,
	        "id": "b7021bf83077fef9f4e73b4c",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "浦亭乡学校",
	        "type": "教育培训/小学",
	        "longitude": 120.35459899902344,
	        "latitude": 27.5262451171875
	      }, {
	        "site_id": 4986,
	        "id": "e2c734e443461f5e15a19050",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "石砰学校",
	        "type": "教育培训/小学",
	        "longitude": 120.63387298583984,
	        "latitude": 27.40381622314453
	      }, {
	        "site_id": 24232,
	        "id": "d4034eb3e7ec588bc03644fc",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "瑞安市行政学校",
	        "type": "教育培训",
	        "longitude": 120.65277862548828,
	        "latitude": 27.769163131713867
	      }, {
	        "site_id": 20449,
	        "id": "67b6c722ff656c4e9703195e",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "沙城艺术学校",
	        "type": "教育培训",
	        "longitude": 120.81207275390625,
	        "latitude": 27.888349533081055
	      }, {
	        "site_id": 12772,
	        "id": "605fb65bc8e45189e81e9788",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "温州美发学校",
	        "type": "教育培训",
	        "longitude": 120.66282653808594,
	        "latitude": 28.008020401000977
	      }, {
	        "site_id": 17459,
	        "id": "5ce2637d2f3adf51af31e5b0",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "福鼎电脑学校",
	        "type": "教育培训",
	        "longitude": 120.22085571289062,
	        "latitude": 27.329936981201172
	      }, {
	        "site_id": 10632,
	        "id": "62a7691533c745daaeb22a5d",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "顺泰学校",
	        "type": "教育培训",
	        "longitude": 120.39399719238281,
	        "latitude": 27.78605079650879
	      }, {
	        "site_id": 11904,
	        "id": "807c1c092fa934d6871541f1",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "成才学校",
	        "type": "教育培训",
	        "longitude": 120.81742858886719,
	        "latitude": 27.903430938720703
	      }, {
	        "site_id": 8434,
	        "id": "e2c734e49bd5a72015a190c3",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "天音音乐学校",
	        "type": "教育培训",
	        "longitude": 120.6528091430664,
	        "latitude": 27.77284049987793
	      }, {
	        "site_id": 9733,
	        "id": "fa01e937c4853e49774ff16f",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "桂峰乡学校",
	        "type": "教育培训/小学",
	        "longitude": 120.26981353759766,
	        "latitude": 27.917591094970703
	      }, {
	        "site_id": 8176,
	        "id": "c2e50ca2692cda0831169bb7",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "晓文音乐学校",
	        "type": "教育培训",
	        "longitude": 120.58357238769531,
	        "latitude": 28.066686630249023
	      }, {
	        "site_id": 17344,
	        "id": "fa7da3e8446b3f6142d63e6b",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "新星学校",
	        "type": "教育培训",
	        "longitude": 120.75341033935547,
	        "latitude": 27.83724021911621
	      }, {
	        "site_id": 6025,
	        "id": "d3010852fc7c20a4ab059dae",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "芭比娃艺术学校",
	        "type": "教育培训",
	        "longitude": 120.79777526855469,
	        "latitude": 27.873674392700195
	      }, {
	        "site_id": 25117,
	        "id": "8eb48fa1fc411ca5f32d831c",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "晓雯音乐学校",
	        "type": "教育培训",
	        "longitude": 120.6717758178711,
	        "latitude": 27.974124908447266
	      }, {
	        "site_id": 11776,
	        "id": "797c3aecfc2a5f8a2fb17e32",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "云头垟学校",
	        "type": "教育培训",
	        "longitude": 120.50062561035156,
	        "latitude": 27.50981330871582
	      }, {
	        "site_id": 16572,
	        "id": "d2b3c0e2defc6eb92e47333e",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "上金学校",
	        "type": "教育培训/小学",
	        "longitude": 120.68104553222656,
	        "latitude": 27.820331573486328
	      }, {
	        "site_id": 4084,
	        "id": "90065b64006fddf6c7f02568",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "培林学校",
	        "type": "教育培训",
	        "longitude": 120.55912017822266,
	        "latitude": 27.56557846069336
	      }, {
	        "site_id": 6959,
	        "id": "aa49bf0d258ad400cc04ef54",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "瑞安市高楼乡学校",
	        "type": "教育培训",
	        "longitude": 120.28516387939453,
	        "latitude": 27.780380249023438
	      }, {
	        "site_id": 10554,
	        "id": "b48c001583b092b771455bbc",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "环球雅思学校",
	        "type": "教育培训",
	        "longitude": 120.6554183959961,
	        "latitude": 28.0073299407959
	      }, {
	        "site_id": 11169,
	        "id": "bb1d38e4a646024e0c0a706d",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "星苑艺术学校",
	        "type": "教育培训",
	        "longitude": 120.64607238769531,
	        "latitude": 27.781328201293945
	      }, {
	        "site_id": 15067,
	        "id": "9af4e347d2bb8de3ecdfca84",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "晓雯音乐学校",
	        "type": "教育培训",
	        "longitude": 121.15058135986328,
	        "latitude": 27.84469223022461
	      }, {
	        "site_id": 17573,
	        "id": "fd246144e2f4d78978f796e3",
	        "val": 1.4258589401543402,
	        "relation_val": 1.4258589401543402,
	        "name": "小名堂艺术学校",
	        "type": "教育培训",
	        "longitude": 120.65240478515625,
	        "latitude": 28.045852661132812
	      }, {
	        "site_id": 10679,
	        "id": "9dee307ba7bec89d8936eec6",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "墨城学校",
	        "type": "教育培训",
	        "longitude": 120.61683654785156,
	        "latitude": 27.60588264465332
	      }, {
	        "site_id": 15944,
	        "id": "cde9e2c7f3828fb7e81e9778",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "东岩乡学校",
	        "type": "教育培训",
	        "longitude": 120.20197296142578,
	        "latitude": 27.813932418823242
	      }, {
	        "site_id": 7469,
	        "id": "6113ef776d5db1178e77274c",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "腾垟学校",
	        "type": "教育培训/小学",
	        "longitude": 120.24056243896484,
	        "latitude": 27.521072387695312
	      }, {
	        "site_id": 17459,
	        "id": "637df60a6860760c40afe612",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "少儿美术学校",
	        "type": "教育培训",
	        "longitude": 120.21199798583984,
	        "latitude": 27.33484649658203
	      }, {
	        "site_id": 24063,
	        "id": "c484913a45388cc2b13c814f",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "温州新星学校",
	        "type": "教育培训",
	        "longitude": 120.41036987304688,
	        "latitude": 27.508506774902344
	      }, {
	        "site_id": 227,
	        "id": "9de9a3e99a47e9da7ec876df",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "晓雯音乐学校",
	        "type": "教育培训",
	        "longitude": 120.62452697753906,
	        "latitude": 27.777353286743164
	      }, {
	        "site_id": 21587,
	        "id": "abe1bcc11e2801a549fd3f34",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "文武学校",
	        "type": "教育培训",
	        "longitude": 120.5713119506836,
	        "latitude": 27.658395767211914
	      }, {
	        "site_id": 17424,
	        "id": "d219bb1dabc81ef6f40e77b3",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "雅西艺术学校",
	        "type": "教育培训",
	        "longitude": 120.66986846923828,
	        "latitude": 27.969768524169922
	      }, {
	        "site_id": 12371,
	        "id": "d24e48eb7d11d8b5afee7a5a",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "大南乡学校",
	        "type": "教育培训/中学",
	        "longitude": 120.41109466552734,
	        "latitude": 27.717580795288086
	      }, {
	        "site_id": 16818,
	        "id": "8ab0dfc437d77815d8eced7d",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "平阳文武学校",
	        "type": "教育培训",
	        "longitude": 120.57469177246094,
	        "latitude": 27.653329849243164
	      }, {
	        "site_id": 14978,
	        "id": "de936bb4a07e01d4e3110313",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "公阳乡中心学校",
	        "type": "教育培训",
	        "longitude": 120.19534301757812,
	        "latitude": 27.719985961914062
	      }, {
	        "site_id": 19576,
	        "id": "f4a33da9d880c20a3010af6f",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "溪坦学校",
	        "type": "教育培训/小学",
	        "longitude": 120.43192291259766,
	        "latitude": 27.91067123413086
	      }, {
	        "site_id": 5950,
	        "id": "bc54883de8e7cd6f5bf6bea9",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "爱华民工子弟学校",
	        "type": "教育培训",
	        "longitude": 120.75463104248047,
	        "latitude": 27.966402053833008
	      }, {
	        "site_id": 15225,
	        "id": "83e200d91aabcb018d7727b3",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "温州市开元学校",
	        "type": "教育培训",
	        "longitude": 120.66608428955078,
	        "latitude": 28.015108108520508
	      }, {
	        "site_id": 22509,
	        "id": "cf8a3ef712e914af44e274dc",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "学校路",
	        "type": "交通设施/道路",
	        "longitude": 120.78038787841797,
	        "latitude": 27.964519500732422
	      }, {
	        "site_id": 18209,
	        "id": "26db36b41451b825eedfca6a",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "曙光学校",
	        "type": "教育培训",
	        "longitude": 120.62642669677734,
	        "latitude": 27.9628849029541
	      }, {
	        "site_id": 25950,
	        "id": "9412036ad6d8680146e274d2",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "温州市勤俭学校",
	        "type": "教育培训",
	        "longitude": 120.64974975585938,
	        "latitude": 27.9570369720459
	      }, {
	        "site_id": 2387,
	        "id": "f8faddb5a59bffa6220262e0",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "华音艺术学校",
	        "type": "教育培训",
	        "longitude": 120.65493774414062,
	        "latitude": 28.007131576538086
	      }, {
	        "site_id": 7532,
	        "id": "3947346e337cca98bfa82281",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "南宋学校",
	        "type": "教育培训",
	        "longitude": 120.38946533203125,
	        "latitude": 27.3907470703125
	      }, {
	        "site_id": 19412,
	        "id": "f7d91e87f1e66c2bd4b74eb8",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "鲍田社区学校",
	        "type": "医疗",
	        "longitude": 120.72254180908203,
	        "latitude": 27.823612213134766
	      }, {
	        "site_id": 11050,
	        "id": "8a2249b51cd41092d7851fc7",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "温州市工艺美术学校",
	        "type": "教育培训",
	        "longitude": 120.65557098388672,
	        "latitude": 28.022165298461914
	      }, {
	        "site_id": 6194,
	        "id": "dffd631c8fc6a0c138221c0e",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "惠诚培训学校",
	        "type": "教育培训",
	        "longitude": 120.82264709472656,
	        "latitude": 27.92180824279785
	      }, {
	        "site_id": 12776,
	        "id": "50fb64ff384b285ee9f85334",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "金盾学校",
	        "type": "教育培训/中学",
	        "longitude": 120.45575714111328,
	        "latitude": 28.559890747070312
	      }, {
	        "site_id": 21535,
	        "id": "f5ca6b30d7e4c9b703ff2dcc",
	        "val": 2.547092971640768,
	        "relation_val": 2.547092971640768,
	        "name": "永兴利民学校",
	        "type": "教育培训",
	        "longitude": 120.81444549560547,
	        "latitude": 27.896623611450195
	      }, {
	        "site_id": 5009,
	        "id": "7a9932aca6006dfaabf0cd60",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "惠民学校",
	        "type": "教育培训/小学",
	        "longitude": 120.72638702392578,
	        "latitude": 27.985170364379883
	      }, {
	        "site_id": 12164,
	        "id": "cd9d05dd9dc150b785420e01",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "里仁电脑学校",
	        "type": "教育培训",
	        "longitude": 120.54914855957031,
	        "latitude": 27.580760955810547
	      }, {
	        "site_id": 12246,
	        "id": "d1469ee7f635856184330451",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "艺声艺术学校",
	        "type": "教育培训",
	        "longitude": 119.71034240722656,
	        "latitude": 27.556438446044922
	      }, {
	        "site_id": 26871,
	        "id": "dec69804864bd8d131f71ddb",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "上望社区学校",
	        "type": "教育培训",
	        "longitude": 120.6766357421875,
	        "latitude": 27.76443099975586
	      }, {
	        "site_id": 4821,
	        "id": "e158b4a2754fbe3b5abbbca5",
	        "val": 1.8724080782877626,
	        "relation_val": 1.8724080782877626,
	        "name": "金岙学校",
	        "type": "教育培训/中学",
	        "longitude": 120.80088806152344,
	        "latitude": 27.962543487548828
	      }]
	    }, {
	      "name": "学堂",
	      "val": 0.5783611536026001,
	      "data": [{
	        "site_id": 7387,
	        "id": "798ef118257dcb6a8605bf2f",
	        "val": 1.6133377128445396,
	        "relation_val": 1.6133377128445396,
	        "name": "好学堂培训学校",
	        "type": "教育培训",
	        "longitude": 120.60875701904297,
	        "latitude": 28.012117385864258
	      }, {
	        "site_id": 6498,
	        "id": "7bf7797785c8132359bbbc2c",
	        "val": 2.118601273565977,
	        "relation_val": 2.118601273565977,
	        "name": "三正学堂",
	        "type": "教育培训",
	        "longitude": 120.62599182128906,
	        "latitude": 27.783655166625977
	      }, {
	        "site_id": 16539,
	        "id": "e96b4420ca9a785a82288ac6",
	        "val": 2.881996972873425,
	        "relation_val": 2.881996972873425,
	        "name": "学堂山",
	        "type": "行政区划/村庄",
	        "longitude": 120.42436218261719,
	        "latitude": 27.885438919067383
	      }, {
	        "site_id": 13553,
	        "id": "3a0fbba82639d5429ee3212d",
	        "val": 2.881996972873425,
	        "relation_val": 2.881996972873425,
	        "name": "学堂段",
	        "type": "行政区划/村庄",
	        "longitude": 120.90694427490234,
	        "latitude": 27.965564727783203
	      }]
	    }, {
	      "name": "院校",
	      "val": 0.6070029735565186,
	      "data": [{
	        "site_id": 2305,
	        "id": "760f01dc81c7b900d7851fdc",
	        "val": 1.4919975110830586,
	        "relation_val": 1.4919975110830586,
	        "name": "董田卫生院(校前路)",
	        "type": "医疗/诊所",
	        "longitude": 120.68879699707031,
	        "latitude": 27.786149978637695
	      }]
	    }, {
	      "name": "书院",
	      "val": 0.5713227987289429,
	      "data": [{
	        "site_id": 1978,
	        "id": "99fc98990876fe14a2d230d9",
	        "val": 2.566078222201472,
	        "relation_val": 2.566078222201472,
	        "name": "君子书院",
	        "type": "教育培训",
	        "longitude": 120.64877319335938,
	        "latitude": 27.768709182739258
	      }, {
	        "site_id": 10395,
	        "id": "d4972515034f2e3f58bbbc25",
	        "val": 1.8863644343823267,
	        "relation_val": 1.8863644343823267,
	        "name": "书院路",
	        "type": "交通设施/道路",
	        "longitude": 120.28117370605469,
	        "latitude": 28.136173248291016
	      }, {
	        "site_id": 14241,
	        "id": "019763421c370e83343a717b",
	        "val": 1.8863644343823267,
	        "relation_val": 1.8863644343823267,
	        "name": "王瓒书院",
	        "type": "旅游景点/风景区",
	        "longitude": 120.78285217285156,
	        "latitude": 27.912189483642578
	      }, {
	        "site_id": 15870,
	        "id": "d4972515034f2e3f58bbbc25",
	        "val": 1.8863644343823267,
	        "relation_val": 1.8863644343823267,
	        "name": "书院路",
	        "type": "交通设施/道路",
	        "longitude": 120.28117370605469,
	        "latitude": 28.136173248291016
	      }, {
	        "site_id": 15403,
	        "id": "40129e95c10381f4f423105b",
	        "val": 2.566078222201472,
	        "relation_val": 2.566078222201472,
	        "name": "桃园书院",
	        "type": "购物/商铺",
	        "longitude": 120.96302795410156,
	        "latitude": 28.127120971679688
	      }, {
	        "site_id": 15403,
	        "id": "31f9101b94845dc426e0a03d",
	        "val": 2.566078222201472,
	        "relation_val": 2.566078222201472,
	        "name": "框园书院",
	        "type": "购物/商铺",
	        "longitude": 120.96326446533203,
	        "latitude": 28.12750244140625
	      }, {
	        "site_id": 15379,
	        "id": "038ea9e8cc2781e11a94c658",
	        "val": 1.8863644343823267,
	        "relation_val": 1.8863644343823267,
	        "name": "书院路",
	        "type": "交通设施/道路",
	        "longitude": 119.70591735839844,
	        "latitude": 27.559555053710938
	      }, {
	        "site_id": 7309,
	        "id": "92b514e27ce300c79d95ded5",
	        "val": 2.566078222201472,
	        "relation_val": 2.566078222201472,
	        "name": "凤凰书院",
	        "type": "购物/商铺",
	        "longitude": 120.90116119384766,
	        "latitude": 28.0313720703125
	      }]
	    }]
	  }]
	  // {
	  //   "name": "医院",
	  //   "data": [
	  //     {
	  //       "name": "医院",
	  //       "data": [
	  //         {
	  //           "name": "医院",
	  //           "val": 1.0000001192092896,
	  //           "data": [
	  //             {
	  //               "site_id": 15870,
	  //               "id": "aa40108b827d0214c5effb09",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "青田县第二人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.3841552734375,
	  //               "latitude": 28.157573699951172
	  //             },
	  //             {
	  //               "site_id": 24163,
	  //               "id": "b0787b9a2de915deab175a0a",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "泸州地区牙科医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.61400604248047,
	  //               "latitude": 27.772655487060547
	  //             },
	  //             {
	  //               "site_id": 14330,
	  //               "id": "ae5b024a91a3e7678a36ee47",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "乐清市第五人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 121.15711975097656,
	  //               "latitude": 28.409767150878906
	  //             },
	  //             {
	  //               "site_id": 20480,
	  //               "id": "cc788fde89d1b99389fc069e",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "温州东瓯皮肤病医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.64643096923828,
	  //               "latitude": 27.995140075683594
	  //             },
	  //             {
	  //               "site_id": 7808,
	  //               "id": "c5ddcc08468763dfd9131891",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "艺星医疗美容医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.66060638427734,
	  //               "latitude": 27.985490798950195
	  //             },
	  //             {
	  //               "site_id": 9739,
	  //               "id": "c87c85c76597535d9122e045",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "浙江省乐清市凤凰医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.98042297363281,
	  //               "latitude": 28.12578582763672
	  //             },
	  //             {
	  //               "site_id": 1168,
	  //               "id": "e889a7446e97afb68605bf5d",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "瓯海区第三人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.67845916748047,
	  //               "latitude": 27.97702980041504
	  //             },
	  //             {
	  //               "site_id": 618,
	  //               "id": "d40fc9e07d9375c34e2fdf43",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "永嘉县第二人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.734130859375,
	  //               "latitude": 28.3450927734375
	  //             },
	  //             {
	  //               "site_id": 10261,
	  //               "id": "c7eca12b077e1a34e015f9a0",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "瑞生微创外科医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.67627716064453,
	  //               "latitude": 27.796607971191406
	  //             },
	  //             {
	  //               "site_id": 27307,
	  //               "id": "c882ba2f991eb75d79dbd22f",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "乐清市第六人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.85453033447266,
	  //               "latitude": 28.033906936645508
	  //             },
	  //             {
	  //               "site_id": 8156,
	  //               "id": "f4460918341fe769b11e3655",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "华山中西医结合医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.9590835571289,
	  //               "latitude": 28.105178833007812
	  //             },
	  //             {
	  //               "site_id": 2548,
	  //               "id": "fde11409b74d51c184154166",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "温州东华整形医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.64888763427734,
	  //               "latitude": 27.996328353881836
	  //             },
	  //             {
	  //               "site_id": 2548,
	  //               "id": "fa7da3e85df5241f42d63ef7",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "温州手足外科医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.64908599853516,
	  //               "latitude": 27.996353149414062
	  //             },
	  //             {
	  //               "site_id": 24071,
	  //               "id": "d6bbcfbe328a315d213bf263",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "瑞安市第三人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.45942687988281,
	  //               "latitude": 27.779150009155273
	  //             },
	  //             {
	  //               "site_id": 11544,
	  //               "id": "dbf4d6eecf3ec9336ea3cce2",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "温州东方妇产医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.6754379272461,
	  //               "latitude": 27.982213973999023
	  //             },
	  //             {
	  //               "site_id": 24163,
	  //               "id": "f59bc3591d2c79728605bff8",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "瑞安市人民第二医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.6148452758789,
	  //               "latitude": 27.77349090576172
	  //             },
	  //             {
	  //               "site_id": 27720,
	  //               "id": "ea4a3dc905b7f4374f5547ad",
	  //               "val": 2.108582877125619,
	  //               "relation_val": 2.108582877125619,
	  //               "name": "温州加美美容医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.67718505859375,
	  //               "latitude": 28.013954162597656
	  //             },
	  //             {
	  //               "site_id": 9399,
	  //               "id": "7aea43b719c8adfb7c29bd5d",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "温州万福医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.64743041992188,
	  //               "latitude": 27.969738006591797
	  //             },
	  //             {
	  //               "site_id": 2703,
	  //               "id": "37d60a46e2e31b1d1e1d11f0",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "瑞安隆山医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.6379623413086,
	  //               "latitude": 27.78217315673828
	  //             },
	  //             {
	  //               "site_id": 14775,
	  //               "id": "1cd8cca97f8e92d1b52655ab",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "星范整形医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.66585540771484,
	  //               "latitude": 28.022289276123047
	  //             },
	  //             {
	  //               "site_id": 17459,
	  //               "id": "50f7d1469c838fdd72210b54",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "福鼎同济医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.2135009765625,
	  //               "latitude": 27.309526443481445
	  //             },
	  //             {
	  //               "site_id": 5888,
	  //               "id": "2d99c8d38226827d6b49f3b9",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "温州市肿瘤医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.61476135253906,
	  //               "latitude": 28.018224716186523
	  //             },
	  //             {
	  //               "site_id": 23665,
	  //               "id": "0b214afe881a9b2cc2f1cf18",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "新华医院(双塔路)",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.64007568359375,
	  //               "latitude": 28.044166564941406
	  //             },
	  //             {
	  //               "site_id": 7332,
	  //               "id": "161a0ed2ba50cd535cd4fe59",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "温州市中心医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.65352630615234,
	  //               "latitude": 28.02382469177246
	  //             },
	  //             {
	  //               "site_id": 3039,
	  //               "id": "1a32ed1545e3d13f2fb96dcf",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "温州市儿童医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.69081115722656,
	  //               "latitude": 28.00217056274414
	  //             },
	  //             {
	  //               "site_id": 22129,
	  //               "id": "a90bd2e70d84816b774ff1b7",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "永嘉县乌牛医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.78790283203125,
	  //               "latitude": 28.027681350708008
	  //             },
	  //             {
	  //               "site_id": 9869,
	  //               "id": "0bc5d9068c332d831329357c",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "博爱医院分院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.57003021240234,
	  //               "latitude": 27.565073013305664
	  //             },
	  //             {
	  //               "site_id": 15870,
	  //               "id": "0381850894c8ac6688db73d6",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "青田康宁医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.29595184326172,
	  //               "latitude": 28.124116897583008
	  //             },
	  //             {
	  //               "site_id": 5235,
	  //               "id": "6efde5b5692b492f5e17c33d",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "县第二医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.31541442871094,
	  //               "latitude": 27.636829376220703
	  //             },
	  //             {
	  //               "site_id": 22039,
	  //               "id": "34e48508d0a4e2e1d8d89159",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "永嘉金瓯医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.63728332519531,
	  //               "latitude": 28.04898452758789
	  //             },
	  //             {
	  //               "site_id": 2324,
	  //               "id": "679d7cde259ea1ad29d7aba9",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "高楼中心医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.28058624267578,
	  //               "latitude": 27.76696014404297
	  //             },
	  //             {
	  //               "site_id": 22083,
	  //               "id": "c37ab9455f52a9fed6b74ecc",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "罗阳镇医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 119.71508026123047,
	  //               "latitude": 27.566194534301758
	  //             },
	  //             {
	  //               "site_id": 13076,
	  //               "id": "e93905a841922195f42310c5",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "黎明医院门诊楼",
	  //               "type": "医疗",
	  //               "longitude": 120.70173645019531,
	  //               "latitude": 28.00348472595215
	  //             },
	  //             {
	  //               "site_id": 2390,
	  //               "id": "d52831f96533aaa62ffaa7b8",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "温州广化医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.6335220336914,
	  //               "latitude": 28.02153778076172
	  //             },
	  //             {
	  //               "site_id": 24785,
	  //               "id": "beabdbf455d78d9b7169d3a5",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "平阳县萧江医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.46863555908203,
	  //               "latitude": 27.5704288482666
	  //             },
	  //             {
	  //               "site_id": 7310,
	  //               "id": "2d99c8d38226827d6b49f3b9",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "温州市肿瘤医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.61476135253906,
	  //               "latitude": 28.018224716186523
	  //             },
	  //             {
	  //               "site_id": 23547,
	  //               "id": "5002435147029634b45f1e9f",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "温州市妇产科医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.65049743652344,
	  //               "latitude": 28.01542854309082
	  //             },
	  //             {
	  //               "site_id": 1526,
	  //               "id": "08fc93dfe7ead2929a68b4b5",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "苍南县人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.40156555175781,
	  //               "latitude": 27.507328033447266
	  //             },
	  //             {
	  //               "site_id": 10395,
	  //               "id": "0381850894c8ac6688db73d6",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "青田康宁医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.29595184326172,
	  //               "latitude": 28.124116897583008
	  //             },
	  //             {
	  //               "site_id": 8728,
	  //               "id": "3a0a78bd6dbfdbf96318095a",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "永嘉第三医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.478271484375,
	  //               "latitude": 28.167970657348633
	  //             },
	  //             {
	  //               "site_id": 4249,
	  //               "id": "50f7d1466dfa9edd73210b9e",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "温州广慈医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.63774871826172,
	  //               "latitude": 28.005754470825195
	  //             },
	  //             {
	  //               "site_id": 16055,
	  //               "id": "c21db9e599a4b0fd01ff2d83",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "鹿城人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.59928894042969,
	  //               "latitude": 28.022830963134766
	  //             },
	  //             {
	  //               "site_id": 23547,
	  //               "id": "eb661c5417363e8e09a8d4a3",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "温州市人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.65050506591797,
	  //               "latitude": 28.01543617248535
	  //             },
	  //             {
	  //               "site_id": 2682,
	  //               "id": "4cf8d24e437e2c9a9cf7790a",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "浙江省眼科医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.66785430908203,
	  //               "latitude": 28.010517120361328
	  //             },
	  //             {
	  //               "site_id": 20625,
	  //               "id": "c34566f99b062b185cd4fece",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "南城新区医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 119.85713195800781,
	  //               "latitude": 28.41804313659668
	  //             },
	  //             {
	  //               "site_id": 8287,
	  //               "id": "33fbfa83c77e69c20bf5eb06",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "湖头医院",
	  //               "type": "医疗/诊所",
	  //               "longitude": 120.87013244628906,
	  //               "latitude": 28.066343307495117
	  //             },
	  //             {
	  //               "site_id": 2776,
	  //               "id": "707af89e44c2c558a0acc425",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "乐清骨科医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.65335083007812,
	  //               "latitude": 28.015153884887695
	  //             },
	  //             {
	  //               "site_id": 22247,
	  //               "id": "d5f93c0edbf3645fecdfcaa8",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "慈爱康复医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.44426727294922,
	  //               "latitude": 27.23455047607422
	  //             },
	  //             {
	  //               "site_id": 17001,
	  //               "id": "55a0bff690239465e31103f3",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "乐清市人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.96678924560547,
	  //               "latitude": 28.10765838623047
	  //             },
	  //             {
	  //               "site_id": 22860,
	  //               "id": "04bdf4a3dd920d4c344aaea7",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "腾蛟中心医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.32463073730469,
	  //               "latitude": 27.6862735748291
	  //             },
	  //             {
	  //               "site_id": 15071,
	  //               "id": "509beee006f60cad88b988ff",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "温州市友好医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.66303253173828,
	  //               "latitude": 28.010570526123047
	  //             },
	  //             {
	  //               "site_id": 11227,
	  //               "id": "368a280011ea31582a3a9fab",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "薪堂中医医院",
	  //               "type": "医疗",
	  //               "longitude": 120.70347595214844,
	  //               "latitude": 27.834856033325195
	  //             },
	  //             {
	  //               "site_id": 17189,
	  //               "id": "e911e6aba03186dc9d82ea47",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "福鼎龙安医院",
	  //               "type": "医疗",
	  //               "longitude": 120.36662292480469,
	  //               "latitude": 27.19026756286621
	  //             },
	  //             {
	  //               "site_id": 4407,
	  //               "id": "aaed96431ac41f739e82eaeb",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "温州同济医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.67980194091797,
	  //               "latitude": 28.008625030517578
	  //             },
	  //             {
	  //               "site_id": 10613,
	  //               "id": "255fb2285d811e3c6e20b9b8",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "贵州省肿瘤医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.58639526367188,
	  //               "latitude": 28.029821395874023
	  //             },
	  //             {
	  //               "site_id": 2305,
	  //               "id": "fca1e5e911d5d3a331870a4e",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "王华骨伤医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.69271850585938,
	  //               "latitude": 27.78826141357422
	  //             },
	  //             {
	  //               "site_id": 13317,
	  //               "id": "4d0e878b31507c79e215f9d8",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "湖岭医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.38519287109375,
	  //               "latitude": 27.90509605407715
	  //             },
	  //             {
	  //               "site_id": 19549,
	  //               "id": "bff8fa7dd2d33ea3c9213da7",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "鳌江镇医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.55533599853516,
	  //               "latitude": 27.593122482299805
	  //             },
	  //             {
	  //               "site_id": 7324,
	  //               "id": "eeef6edc0c55984ff8f0cb7d",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "平阳县人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.56208801269531,
	  //               "latitude": 27.670923233032227
	  //             },
	  //             {
	  //               "site_id": 10395,
	  //               "id": "8e675f56de37ae132bb97c94",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "青田县人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.28314971923828,
	  //               "latitude": 28.13365936279297
	  //             },
	  //             {
	  //               "site_id": 24887,
	  //               "id": "7480c77904b1f838c9213da6",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "瓯海区新桥医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.6196517944336,
	  //               "latitude": 27.986961364746094
	  //             },
	  //             {
	  //               "site_id": 8194,
	  //               "id": "cde9e2c7f3a18fb7e81e9759",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "瓯北医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.6339111328125,
	  //               "latitude": 28.051387786865234
	  //             },
	  //             {
	  //               "site_id": 26405,
	  //               "id": "435d7aeafae3ab2c5bbbbca4",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "温州友好医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.67591857910156,
	  //               "latitude": 27.999662399291992
	  //             },
	  //             {
	  //               "site_id": 11544,
	  //               "id": "81d6a50996bb02436e20b95d",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "东方女子医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.6754379272461,
	  //               "latitude": 27.982206344604492
	  //             },
	  //             {
	  //               "site_id": 2565,
	  //               "id": "07f3c8b510d544606340a9a4",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "平阳县麻步医院",
	  //               "type": "医疗",
	  //               "longitude": 120.41458129882812,
	  //               "latitude": 27.5919246673584
	  //             },
	  //             {
	  //               "site_id": 10593,
	  //               "id": "37d60a467810a1611f1d1180",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "洞头县人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 121.15225982666016,
	  //               "latitude": 27.843074798583984
	  //             },
	  //             {
	  //               "site_id": 5930,
	  //               "id": "b292b3aa0f82c82bbe4842fc",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "丽岙医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.63330841064453,
	  //               "latitude": 27.901382446289062
	  //             },
	  //             {
	  //               "site_id": 17459,
	  //               "id": "e3a7fd135a915d4b28b97c54",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "福鼎华山医院",
	  //               "type": "医疗",
	  //               "longitude": 120.21125793457031,
	  //               "latitude": 27.329757690429688
	  //             },
	  //             {
	  //               "site_id": 10613,
	  //               "id": "70ce326c0f2e7548d8131864",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "漳州市医院(妇产科)",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.58639526367188,
	  //               "latitude": 28.029821395874023
	  //             },
	  //             {
	  //               "site_id": 28434,
	  //               "id": "50f7d1469c838fdd72210b54",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "福鼎同济医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.2135009765625,
	  //               "latitude": 27.309526443481445
	  //             },
	  //             {
	  //               "site_id": 15870,
	  //               "id": "d0fc41951fda6bf9f6b916ad",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "青田维康骨科医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.37722778320312,
	  //               "latitude": 28.152746200561523
	  //             },
	  //             {
	  //               "site_id": 17103,
	  //               "id": "1a32ed1545e3d13f2fb96dcf",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "温州市儿童医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.69081115722656,
	  //               "latitude": 28.00217056274414
	  //             },
	  //             {
	  //               "site_id": 9235,
	  //               "id": "81d6a50996bd02436e20b95b",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "乐清口腔医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.89391326904297,
	  //               "latitude": 28.044857025146484
	  //             },
	  //             {
	  //               "site_id": 23585,
	  //               "id": "f28bb0449001b9445cd4feb8",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "乐清市红十字医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.88587188720703,
	  //               "latitude": 28.040538787841797
	  //             },
	  //             {
	  //               "site_id": 17365,
	  //               "id": "e96b44200aff384082288a03",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "温州建国医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.66188049316406,
	  //               "latitude": 27.985353469848633
	  //             },
	  //             {
	  //               "site_id": 18070,
	  //               "id": "2e420f5f4b3bdb6638221c8b",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "乐清虹丰医院",
	  //               "type": "医疗",
	  //               "longitude": 121.02703857421875,
	  //               "latitude": 28.21088218688965
	  //             },
	  //             {
	  //               "site_id": 14061,
	  //               "id": "13d2ab1abdd4d4d6fa42b245",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "瓯海区人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.63128662109375,
	  //               "latitude": 28.012584686279297
	  //             },
	  //             {
	  //               "site_id": 24275,
	  //               "id": "3a0a78bd6dbfdbf96318095a",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "永嘉第三医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.478271484375,
	  //               "latitude": 28.167970657348633
	  //             },
	  //             {
	  //               "site_id": 28127,
	  //               "id": "1bf85b25363dbbd9a1a93460",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "南山法敏骨伤医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 119.79304504394531,
	  //               "latitude": 28.335147857666016
	  //             },
	  //             {
	  //               "site_id": 7332,
	  //               "id": "419d2a309338385f9e95de23",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "温州市肿瘤医院",
	  //               "type": "医疗",
	  //               "longitude": 120.65352630615234,
	  //               "latitude": 28.023664474487305
	  //             },
	  //             {
	  //               "site_id": 17569,
	  //               "id": "e889a7443782e6ac8705bfa4",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "光明医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.70172119140625,
	  //               "latitude": 28.00358772277832
	  //             },
	  //             {
	  //               "site_id": 26039,
	  //               "id": "480b24d0776c13e89cec2b54",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "柘荣县医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 119.89680480957031,
	  //               "latitude": 27.241912841796875
	  //             },
	  //             {
	  //               "site_id": 27962,
	  //               "id": "68ee0f49f1b7aa7681a12ea1",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "锦隆电脑医院",
	  //               "type": "医疗",
	  //               "longitude": 120.50183868408203,
	  //               "latitude": 28.067272186279297
	  //             },
	  //             {
	  //               "site_id": 15338,
	  //               "id": "d24e48eb2cd56bb7afee7a11",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "上戍医院",
	  //               "type": "医疗/诊所",
	  //               "longitude": 120.54224395751953,
	  //               "latitude": 28.0863094329834
	  //             },
	  //             {
	  //               "site_id": 20189,
	  //               "id": "b292b3aa0f82c82bbe4842fc",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "丽岙医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.63330841064453,
	  //               "latitude": 27.901382446289062
	  //             },
	  //             {
	  //               "site_id": 12586,
	  //               "id": "93df0dc331166c28892fb5a7",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "曹村医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.457275390625,
	  //               "latitude": 27.71126937866211
	  //             },
	  //             {
	  //               "site_id": 25629,
	  //               "id": "befdb2920ffe00f8861541a7",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "滨海医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.80976867675781,
	  //               "latitude": 27.871437072753906
	  //             },
	  //             {
	  //               "site_id": 10395,
	  //               "id": "f4c8f78e9c5ebfe8b75f1ecf",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "青田县惠民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.2947006225586,
	  //               "latitude": 28.134641647338867
	  //             },
	  //             {
	  //               "site_id": 25950,
	  //               "id": "2527be1e2db62baa4bfc8da3",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "中山医院",
	  //               "type": "医疗",
	  //               "longitude": 120.65184783935547,
	  //               "latitude": 27.95677375793457
	  //             },
	  //             {
	  //               "site_id": 17459,
	  //               "id": "79cceb3688602b901e580054",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "福鼎市医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.21210479736328,
	  //               "latitude": 27.32599639892578
	  //             },
	  //             {
	  //               "site_id": 27327,
	  //               "id": "ffd6d4038ce7f69616b54ba7",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "金乡医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.61022186279297,
	  //               "latitude": 27.434036254882812
	  //             },
	  //             {
	  //               "site_id": 762,
	  //               "id": "edd64ce169a556923fe2315f",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "海滨医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.83926391601562,
	  //               "latitude": 27.921255111694336
	  //             },
	  //             {
	  //               "site_id": 4886,
	  //               "id": "87c3274e7850bb7c2811d110",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "苍南金城医院",
	  //               "type": "医疗",
	  //               "longitude": 120.60227966308594,
	  //               "latitude": 27.440889358520508
	  //             },
	  //             {
	  //               "site_id": 18309,
	  //               "id": "6efde5b5692b492f5e17c33d",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "县第二医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.31541442871094,
	  //               "latitude": 27.636829376220703
	  //             },
	  //             {
	  //               "site_id": 9138,
	  //               "id": "8b79beda5e6ebdbbb2077d4d",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "郭溪医院",
	  //               "type": "医疗",
	  //               "longitude": 120.56372833251953,
	  //               "latitude": 27.994869232177734
	  //             },
	  //             {
	  //               "site_id": 17351,
	  //               "id": "dcc4b0e33859e86fc9e33c6c",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "瑞安同济医院",
	  //               "type": "医疗",
	  //               "longitude": 120.6333999633789,
	  //               "latitude": 27.777132034301758
	  //             },
	  //             {
	  //               "site_id": 15627,
	  //               "id": "b3d83bbe0e2e9e8e2902e142",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "乐清仁济医院",
	  //               "type": "医疗",
	  //               "longitude": 120.96439361572266,
	  //               "latitude": 28.11569595336914
	  //             },
	  //             {
	  //               "site_id": 28692,
	  //               "id": "dc7b5c1416cbbb889cf77992",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "利济医院",
	  //               "type": "医疗",
	  //               "longitude": 120.62976837158203,
	  //               "latitude": 27.78575897216797
	  //             },
	  //             {
	  //               "site_id": 28127,
	  //               "id": "ed649ae16b4aa0f149fd3fab",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "丽水碧湖医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 119.78301239013672,
	  //               "latitude": 28.34029769897461
	  //             },
	  //             {
	  //               "site_id": 28068,
	  //               "id": "864cb6d715e6eb76d518b719",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "温州市红旗医院",
	  //               "type": "医疗",
	  //               "longitude": 120.68377685546875,
	  //               "latitude": 27.985286712646484
	  //             },
	  //             {
	  //               "site_id": 9448,
	  //               "id": "6ed3083a976d3bb62b3a9f09",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "山口中心医院",
	  //               "type": "医疗",
	  //               "longitude": 120.31419372558594,
	  //               "latitude": 28.07124900817871
	  //             },
	  //             {
	  //               "site_id": 16226,
	  //               "id": "e911e6aba03186dc9d82ea47",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "福鼎龙安医院",
	  //               "type": "医疗",
	  //               "longitude": 120.36662292480469,
	  //               "latitude": 27.19026756286621
	  //             },
	  //             {
	  //               "site_id": 15870,
	  //               "id": "f4c8f78e9c5ebfe8b75f1ecf",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "青田县惠民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.2947006225586,
	  //               "latitude": 28.134641647338867
	  //             },
	  //             {
	  //               "site_id": 516,
	  //               "id": "ffb479ef9e3a12b576e5b610",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "爱尔五官科医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.72454071044922,
	  //               "latitude": 27.992204666137695
	  //             },
	  //             {
	  //               "site_id": 2371,
	  //               "id": "c59d23ff7d47e91c8936eec2",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "瑞安华康医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.6900634765625,
	  //               "latitude": 27.783214569091797
	  //             },
	  //             {
	  //               "site_id": 14244,
	  //               "id": "5b25f446605b56e91029355f",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "曙光医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.95657348632812,
	  //               "latitude": 28.116939544677734
	  //             },
	  //             {
	  //               "site_id": 20625,
	  //               "id": "b1c556e880dd4c0fb426557b",
	  //               "val": 5.51836520996028,
	  //               "relation_val": 5.51836520996028,
	  //               "name": "医院",
	  //               "type": "医疗",
	  //               "longitude": 119.94491577148438,
	  //               "latitude": 28.350528717041016
	  //             },
	  //             {
	  //               "site_id": 28390,
	  //               "id": "3dc9befd0fc6f00fbfa240a7",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "永嘉江南医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.68549346923828,
	  //               "latitude": 28.067672729492188
	  //             },
	  //             {
	  //               "site_id": 12323,
	  //               "id": "a5097bfae4bd9960f03cba5d",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "糖尿病专科医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.72216796875,
	  //               "latitude": 27.991416931152344
	  //             },
	  //             {
	  //               "site_id": 16399,
	  //               "id": "e96b4420d9ec655882288a3a",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "景宁县人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 119.63217163085938,
	  //               "latitude": 27.974245071411133
	  //             },
	  //             {
	  //               "site_id": 18644,
	  //               "id": "060f5e53d85aefd4091cc7a2",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "娄桥医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.61233520507812,
	  //               "latitude": 27.962705612182617
	  //             },
	  //             {
	  //               "site_id": 7863,
	  //               "id": "1eeed5466375d8f1b5147f11",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "藤桥医院",
	  //               "type": "医疗",
	  //               "longitude": 120.5014877319336,
	  //               "latitude": 28.072603225708008
	  //             },
	  //             {
	  //               "site_id": 14207,
	  //               "id": "130986dc2fdbb364a20464c7",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "温州市惠民医院",
	  //               "type": "医疗",
	  //               "longitude": 120.65149688720703,
	  //               "latitude": 28.01555061340332
	  //             },
	  //             {
	  //               "site_id": 20183,
	  //               "id": "54bdc685f0f3c6dbd5b74e8d",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "潘桥医院",
	  //               "type": "医疗",
	  //               "longitude": 120.56825256347656,
	  //               "latitude": 27.959253311157227
	  //             },
	  //             {
	  //               "site_id": 2217,
	  //               "id": "d67bb524dafadc052fb96d64",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "永嘉金瓯医院",
	  //               "type": "医疗",
	  //               "longitude": 120.61502838134766,
	  //               "latitude": 28.05031394958496
	  //             },
	  //             {
	  //               "site_id": 12961,
	  //               "id": "75d0881acdae0a4e3a04b155",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "碧莲医院",
	  //               "type": "医疗",
	  //               "longitude": 120.55451202392578,
	  //               "latitude": 28.312305450439453
	  //             },
	  //             {
	  //               "site_id": 20625,
	  //               "id": "a243f46b84de7910e8e5826e",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "丽水协和医院",
	  //               "type": "医疗",
	  //               "longitude": 119.85617065429688,
	  //               "latitude": 28.42442512512207
	  //             },
	  //             {
	  //               "site_id": 419,
	  //               "id": "ed4df1efb5b9d61b902ca362",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "马站医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.4554214477539,
	  //               "latitude": 27.2667293548584
	  //             },
	  //             {
	  //               "site_id": 1764,
	  //               "id": "373cf75761df256d4254d7ad",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "瑞金医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.65249633789062,
	  //               "latitude": 28.046825408935547
	  //             },
	  //             {
	  //               "site_id": 14494,
	  //               "id": "38e480372dcbfb4a363a71a4",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "东方医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.54737854003906,
	  //               "latitude": 27.581241607666016
	  //             },
	  //             {
	  //               "site_id": 4480,
	  //               "id": "4140e7ccf6c964254f3a592a",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "乐清牙科医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.96524047851562,
	  //               "latitude": 28.112960815429688
	  //             },
	  //             {
	  //               "site_id": 26457,
	  //               "id": "f9fd6062fa1afc620f0e6bf4",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "上望医院",
	  //               "type": "医疗",
	  //               "longitude": 120.6740951538086,
	  //               "latitude": 27.7739315032959
	  //             },
	  //             {
	  //               "site_id": 13478,
	  //               "id": "0622f3d3257301c4b320a8c3",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "双潮医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.44523620605469,
	  //               "latitude": 28.138877868652344
	  //             },
	  //             {
	  //               "site_id": 16148,
	  //               "id": "af2656b2605c27c53913fdb1",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "上塘镇医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.6937484741211,
	  //               "latitude": 28.15618896484375
	  //             },
	  //             {
	  //               "site_id": 16816,
	  //               "id": "2527be1e2db62baa4bfc8da3",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "中山医院",
	  //               "type": "医疗",
	  //               "longitude": 120.65184783935547,
	  //               "latitude": 27.95677375793457
	  //             },
	  //             {
	  //               "site_id": 28434,
	  //               "id": "d1469ee7cd1ccc7b84330454",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "海桐医院",
	  //               "type": "医疗",
	  //               "longitude": 120.21663665771484,
	  //               "latitude": 27.312992095947266
	  //             },
	  //             {
	  //               "site_id": 3786,
	  //               "id": "70292957926eae2272455b3e",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "庄稼医院",
	  //               "type": "医疗",
	  //               "longitude": 120.9304428100586,
	  //               "latitude": 28.033203125
	  //             },
	  //             {
	  //               "site_id": 11708,
	  //               "id": "e308284802a0debcb34c496b",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "天河医院",
	  //               "type": "医疗/诊所",
	  //               "longitude": 120.78617858886719,
	  //               "latitude": 27.86334991455078
	  //             },
	  //             {
	  //               "site_id": 13773,
	  //               "id": "4777dfe01e192e794054d7f8",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "阁巷医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.6512451171875,
	  //               "latitude": 27.702816009521484
	  //             },
	  //             {
	  //               "site_id": 25157,
	  //               "id": "e371b43ab1ef955183a00cd8",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "温州华康医院",
	  //               "type": "医疗",
	  //               "longitude": 120.69086456298828,
	  //               "latitude": 28.01023292541504
	  //             },
	  //             {
	  //               "site_id": 770,
	  //               "id": "e2c734e4434d1f5e15a19059",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "利民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.62657928466797,
	  //               "latitude": 28.04705238342285
	  //             },
	  //             {
	  //               "site_id": 219,
	  //               "id": "b292b3aaf809394fbf4842a7",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "临江医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.48375701904297,
	  //               "latitude": 28.13569450378418
	  //             },
	  //             {
	  //               "site_id": 18342,
	  //               "id": "dc7b5c1416cbbb889cf77992",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "利济医院",
	  //               "type": "医疗",
	  //               "longitude": 120.62976837158203,
	  //               "latitude": 27.78575897216797
	  //             },
	  //             {
	  //               "site_id": 11681,
	  //               "id": "70292957926eae2272455b3e",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "庄稼医院",
	  //               "type": "医疗",
	  //               "longitude": 120.9304428100586,
	  //               "latitude": 28.033203125
	  //             },
	  //             {
	  //               "site_id": 26124,
	  //               "id": "f97d15ceda68a4099cec2b67",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "温州梧田医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.65560913085938,
	  //               "latitude": 27.95902442932129
	  //             },
	  //             {
	  //               "site_id": 9651,
	  //               "id": "bba19c1d7462bd38d2f49e50",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "霞关医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.4668960571289,
	  //               "latitude": 27.17230224609375
	  //             },
	  //             {
	  //               "site_id": 1998,
	  //               "id": "1ba35ca4cc7cc587ca2915f0",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "泽雅医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.48504638671875,
	  //               "latitude": 28.033283233642578
	  //             },
	  //             {
	  //               "site_id": 4352,
	  //               "id": "eeef6edc0c55984ff8f0cb7d",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "平阳县人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.56208801269531,
	  //               "latitude": 27.670923233032227
	  //             },
	  //             {
	  //               "site_id": 6490,
	  //               "id": "d1469ee7f8049f1f84330460",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "乐清东南医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.96173095703125,
	  //               "latitude": 28.1192684173584
	  //             },
	  //             {
	  //               "site_id": 22388,
	  //               "id": "dc9d7e48218e3c632102201e",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "骨科医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 121.01790618896484,
	  //               "latitude": 28.187280654907227
	  //             },
	  //             {
	  //               "site_id": 5341,
	  //               "id": "fb92b222dfd7d1bad23ce353",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "仰义医院",
	  //               "type": "医疗",
	  //               "longitude": 120.58342742919922,
	  //               "latitude": 28.064035415649414
	  //             },
	  //             {
	  //               "site_id": 7603,
	  //               "id": "08fc93dfe7bbd2929b68b46a",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "朱雅医院",
	  //               "type": "医疗",
	  //               "longitude": 120.14665222167969,
	  //               "latitude": 27.946802139282227
	  //             },
	  //             {
	  //               "site_id": 24163,
	  //               "id": "e9ca152c4313ee646e3e1b46",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "仙降医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.61424255371094,
	  //               "latitude": 27.772777557373047
	  //             },
	  //             {
	  //               "site_id": 1858,
	  //               "id": "55a0bff690239465e31103f3",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "乐清市人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.96678924560547,
	  //               "latitude": 28.10765838623047
	  //             },
	  //             {
	  //               "site_id": 13076,
	  //               "id": "e889a7443782e6ac8705bfa4",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "光明医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.70172119140625,
	  //               "latitude": 28.00358772277832
	  //             },
	  //             {
	  //               "site_id": 11544,
	  //               "id": "998d81709c8695622fb90842",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "东方整形医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.67544555664062,
	  //               "latitude": 27.982206344604492
	  //             },
	  //             {
	  //               "site_id": 10861,
	  //               "id": "bba873888bb569a3bca8225a",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "岙底医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.49773406982422,
	  //               "latitude": 28.098119735717773
	  //             },
	  //             {
	  //               "site_id": 28661,
	  //               "id": "d5f93c0edb82645feddfca5b",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "同安医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.78152465820312,
	  //               "latitude": 28.03119468688965
	  //             },
	  //             {
	  //               "site_id": 28127,
	  //               "id": "e96b4420d9ec655882288a3a",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "景宁县人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 119.63217163085938,
	  //               "latitude": 27.974245071411133
	  //             },
	  //             {
	  //               "site_id": 21405,
	  //               "id": "01c612e44517474ac61b7577",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "浩瀚电脑医院",
	  //               "type": "医疗",
	  //               "longitude": 120.4017562866211,
	  //               "latitude": 27.50551986694336
	  //             },
	  //             {
	  //               "site_id": 8176,
	  //               "id": "fb92b222dfd7d1bad23ce353",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "仰义医院",
	  //               "type": "医疗",
	  //               "longitude": 120.58342742919922,
	  //               "latitude": 28.064035415649414
	  //             },
	  //             {
	  //               "site_id": 25236,
	  //               "id": "08fc93dfe7bbd2929b68b46a",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "朱雅医院",
	  //               "type": "医疗",
	  //               "longitude": 120.14665222167969,
	  //               "latitude": 27.946802139282227
	  //             },
	  //             {
	  //               "site_id": 6498,
	  //               "id": "46fab90b9d118fc66f3e1b32",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "城关医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.62641143798828,
	  //               "latitude": 27.783782958984375
	  //             },
	  //             {
	  //               "site_id": 6008,
	  //               "id": "5211d2199327325b7cc876a4",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "宜山医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.53980255126953,
	  //               "latitude": 27.514020919799805
	  //             },
	  //             {
	  //               "site_id": 8821,
	  //               "id": "91b6132d3ea565fb28d7ab32",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "温州市仁康医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.60142517089844,
	  //               "latitude": 28.021602630615234
	  //             },
	  //             {
	  //               "site_id": 5400,
	  //               "id": "93df0dc331166c28892fb5a7",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "曹村医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.457275390625,
	  //               "latitude": 27.71126937866211
	  //             },
	  //             {
	  //               "site_id": 25583,
	  //               "id": "d5f93c0edbf3645fecdfcaa8",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "慈爱康复医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.44426727294922,
	  //               "latitude": 27.23455047607422
	  //             },
	  //             {
	  //               "site_id": 16399,
	  //               "id": "5b25f446c86aee9710293569",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "南山医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 119.62688446044922,
	  //               "latitude": 27.982160568237305
	  //             },
	  //             {
	  //               "site_id": 19822,
	  //               "id": "b292b3aa0f85c82bbe4842c7",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "永兴医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.81578826904297,
	  //               "latitude": 27.899118423461914
	  //             },
	  //             {
	  //               "site_id": 25677,
	  //               "id": "d991e1167427c7561f38946f",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "陶山中心医院",
	  //               "type": "医疗",
	  //               "longitude": 120.48841857910156,
	  //               "latitude": 27.847728729248047
	  //             },
	  //             {
	  //               "site_id": 11876,
	  //               "id": "1eeed5466375d8f1b5147f11",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "藤桥医院",
	  //               "type": "医疗",
	  //               "longitude": 120.5014877319336,
	  //               "latitude": 28.072603225708008
	  //             },
	  //             {
	  //               "site_id": 25157,
	  //               "id": "378a6c062dbcab859a005040",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "肛泰肛肠医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.69086456298828,
	  //               "latitude": 28.01023292541504
	  //             },
	  //             {
	  //               "site_id": 10820,
	  //               "id": "d6ee87240cae98e3a9f0cdaa",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "南江妇产科医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.57198333740234,
	  //               "latitude": 27.488298416137695
	  //             },
	  //             {
	  //               "site_id": 16521,
	  //               "id": "ffb479ef9e3a12b576e5b610",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "爱尔五官科医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 120.72454071044922,
	  //               "latitude": 27.992204666137695
	  //             },
	  //             {
	  //               "site_id": 28127,
	  //               "id": "5b25f446c86aee9710293569",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "南山医院",
	  //               "type": "医疗/专科医院",
	  //               "longitude": 119.62688446044922,
	  //               "latitude": 27.982160568237305
	  //             },
	  //             {
	  //               "site_id": 6257,
	  //               "id": "a0351c28f4f71b907dc876e8",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "苍南惠民医院",
	  //               "type": "医疗",
	  //               "longitude": 120.40550231933594,
	  //               "latitude": 27.513778686523438
	  //             },
	  //             {
	  //               "site_id": 27277,
	  //               "id": "3a0fbba826c0d5429ee3215a",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "梅岙医院",
	  //               "type": "医疗",
	  //               "longitude": 120.57360076904297,
	  //               "latitude": 28.114025115966797
	  //             },
	  //             {
	  //               "site_id": 19081,
	  //               "id": "ab4927771b07aa7354bfff1c",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "瑞安九龙医院",
	  //               "type": "医疗",
	  //               "longitude": 120.67619323730469,
	  //               "latitude": 27.780969619750977
	  //             },
	  //             {
	  //               "site_id": 1425,
	  //               "id": "13d2ab1abdd4d4d6fa42b245",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "瓯海区人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.63128662109375,
	  //               "latitude": 28.012584686279297
	  //             },
	  //             {
	  //               "site_id": 19526,
	  //               "id": "4359d28600f34987cc06a42d",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "虹丰医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 121.02372741699219,
	  //               "latitude": 28.211566925048828
	  //             },
	  //             {
	  //               "site_id": 8771,
	  //               "id": "f41f77ce1950b83e0a5cb33a",
	  //               "val": 3.3364890258149864,
	  //               "relation_val": 3.3364890258149864,
	  //               "name": "鲤溪医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.7723159790039,
	  //               "latitude": 28.412080764770508
	  //             },
	  //             {
	  //               "site_id": 15757,
	  //               "id": "1eeed5466375d8f1b5147f11",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "藤桥医院",
	  //               "type": "医疗",
	  //               "longitude": 120.5014877319336,
	  //               "latitude": 28.072603225708008
	  //             },
	  //             {
	  //               "site_id": 15473,
	  //               "id": "4db647e6375654f416c905ec",
	  //               "val": 2.629116645587242,
	  //               "relation_val": 2.629116645587242,
	  //               "name": "平阳县红十字医院",
	  //               "type": "医疗",
	  //               "longitude": 120.53742980957031,
	  //               "latitude": 27.609750747680664
	  //             },
	  //             {
	  //               "site_id": 17414,
	  //               "id": "74c933fb661f565d9e82ea06",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "茗屿医院",
	  //               "type": "医疗",
	  //               "longitude": 120.82967376708984,
	  //               "latitude": 28.025911331176758
	  //             },
	  //             {
	  //               "site_id": 15870,
	  //               "id": "8e675f56de37ae132bb97c94",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "青田县人民医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.28314971923828,
	  //               "latitude": 28.13365936279297
	  //             },
	  //             {
	  //               "site_id": 13416,
	  //               "id": "8d96925c2a37df46c0f1cfaa",
	  //               "val": 2.626483780381821,
	  //               "relation_val": 2.626483780381821,
	  //               "name": "苍南县第三医院",
	  //               "type": "医疗/综合医院",
	  //               "longitude": 120.56900024414062,
	  //               "latitude": 27.484825134277344
	  //             },
	  //             {
	  //               "site_id": 17459,
	  //               "id": "d1469ee7cd1ccc7b84330454",
	  //               "val": 3.7273043938285904,
	  //               "relation_val": 3.7273043938285904,
	  //               "name": "海桐医院",
	  //               "type": "医疗",
	  //               "longitude": 120.21663665771484,
	  //               "latitude": 27.312992095947266
	  //             }
	  //           ]
	  //         },
	  //         {
	  //           "name": "门诊",
	  //           "val": 0.6342208385467529,
	  //           "data": [
	  //             {
	  //               "site_id": 18374,
	  //               "id": "34eb54c5aa92772288fc067e",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "瑞湖门诊",
	  //               "type": "医疗/其他",
	  //               "longitude": 120.62957763671875,
	  //               "latitude": 27.791311264038086
	  //             },
	  //             {
	  //               "site_id": 987,
	  //               "id": "19ef985864f307d6f8f0cbda",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "慈爱门诊",
	  //               "type": "医疗/其他",
	  //               "longitude": 120.55004119873047,
	  //               "latitude": 27.788373947143555
	  //             },
	  //             {
	  //               "site_id": 20287,
	  //               "id": "19ef985864f307d6f8f0cbda",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "慈爱门诊",
	  //               "type": "医疗/其他",
	  //               "longitude": 120.55004119873047,
	  //               "latitude": 27.788373947143555
	  //             },
	  //             {
	  //               "site_id": 15870,
	  //               "id": "53349ee3fecfb41322026264",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "牙专科门诊",
	  //               "type": "医疗/其他",
	  //               "longitude": 120.28351593017578,
	  //               "latitude": 28.140121459960938
	  //             },
	  //             {
	  //               "site_id": 19411,
	  //               "id": "85301a3a54f4815ec9c3dbfa",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "西医门诊",
	  //               "type": "医疗/其他",
	  //               "longitude": 120.66885375976562,
	  //               "latitude": 27.978084564208984
	  //             },
	  //             {
	  //               "site_id": 6163,
	  //               "id": "c8e8f633eefcba6fab0dd808",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "永嘉秀祥门诊",
	  //               "type": "医疗/其他",
	  //               "longitude": 120.62967681884766,
	  //               "latitude": 28.043678283691406
	  //             },
	  //             {
	  //               "site_id": 25804,
	  //               "id": "1ab9dbfe8134f2e7bea2403a",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "通策口腔门诊",
	  //               "type": "医疗",
	  //               "longitude": 120.6529541015625,
	  //               "latitude": 27.778738021850586
	  //             },
	  //             {
	  //               "site_id": 14247,
	  //               "id": "34eb54c5aa92772288fc067e",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "瑞湖门诊",
	  //               "type": "医疗/其他",
	  //               "longitude": 120.62957763671875,
	  //               "latitude": 27.791311264038086
	  //             },
	  //             {
	  //               "site_id": 14423,
	  //               "id": "12e424a2fa75e8449f9d681a",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "西医门诊",
	  //               "type": "医疗/其他",
	  //               "longitude": 120.65658569335938,
	  //               "latitude": 27.975139617919922
	  //             },
	  //             {
	  //               "site_id": 1618,
	  //               "id": "2f6f5c99a4c631e7077ce826",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "本正口腔门诊",
	  //               "type": "医疗",
	  //               "longitude": 120.63601684570312,
	  //               "latitude": 27.781465530395508
	  //             },
	  //             {
	  //               "site_id": 27212,
	  //               "id": "7b71472c3d92c7a1077ce88d",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "优抚门诊",
	  //               "type": "医疗/其他",
	  //               "longitude": 120.65359497070312,
	  //               "latitude": 27.788986206054688
	  //             },
	  //             {
	  //               "site_id": 15870,
	  //               "id": "a51f37579aabb0c86f4760ac",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "仁爱门诊",
	  //               "type": "医疗/其他",
	  //               "longitude": 120.28609466552734,
	  //               "latitude": 28.141029357910156
	  //             },
	  //             {
	  //               "site_id": 10395,
	  //               "id": "53349ee3fecfb41322026264",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "牙专科门诊",
	  //               "type": "医疗/其他",
	  //               "longitude": 120.28351593017578,
	  //               "latitude": 28.140121459960938
	  //             },
	  //             {
	  //               "site_id": 4249,
	  //               "id": "a35893f74d15464fa21de918",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "犬伤门诊",
	  //               "type": "医疗/其他",
	  //               "longitude": 120.63856506347656,
	  //               "latitude": 28.004993438720703
	  //             },
	  //             {
	  //               "site_id": 11223,
	  //               "id": "925c550fab9936b15a3ac842",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "骨伤科门诊",
	  //               "type": "医疗/其他",
	  //               "longitude": 120.5384750366211,
	  //               "latitude": 27.61187171936035
	  //             },
	  //             {
	  //               "site_id": 1151,
	  //               "id": "022e39f5aca5807b2efaa76c",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "发热门诊",
	  //               "type": "医疗/其他",
	  //               "longitude": 119.71106719970703,
	  //               "latitude": 27.560367584228516
	  //             },
	  //             {
	  //               "site_id": 10395,
	  //               "id": "a51f37579aabb0c86f4760ac",
	  //               "val": 2.2441148619213784,
	  //               "relation_val": 2.2441148619213784,
	  //               "name": "仁爱门诊",
	  //               "type": "医疗/其他",
	  //               "longitude": 120.28609466552734,
	  //               "latitude": 28.141029357910156
	  //             },
	  //             {
	  //               "site_id": 28127,
	  //               "id": "6954cb8bd3b3bc2a0869d9d8",
	  //               "val": 3.1814865267141057,
	  //               "relation_val": 3.1814865267141057,
	  //               "name": "口腔门诊",
	  //               "type": "医疗",
	  //               "longitude": 119.63387298583984,
	  //               "latitude": 27.97394371032715
	  //             },
	  //             {
	  //               "site_id": 16399,
	  //               "id": "6954cb8bd3b3bc2a0869d9d8",
	  //               "val": 3.1814865267141057,
	  //               "relation_val": 3.1814865267141057,
	  //               "name": "口腔门诊",
	  //               "type": "医疗",
	  //               "longitude": 119.63387298583984,
	  //               "latitude": 27.97394371032715
	  //             }
	  //           ]
	  //         },
	  //         {
	  //           "name": "医学院",
	  //           "val": 0.6303150057792664,
	  //           "data": [
	  //             {
	  //               "site_id": 3035,
	  //               "id": "bb303d89c8541669d2f49e53",
	  //               "val": 2.4092655580078732,
	  //               "relation_val": 2.4092655580078732,
	  //               "name": "医学院小区",
	  //               "type": "房地产/住宅区",
	  //               "longitude": 120.66744232177734,
	  //               "latitude": 28.01021957397461
	  //             },
	  //             {
	  //               "site_id": 1242,
	  //               "id": "161a0ed2ba2bcd535dd4fea0",
	  //               "val": 2.4068528573508132,
	  //               "relation_val": 2.4068528573508132,
	  //               "name": "温州医学院第三临床医学院",
	  //               "type": "医疗",
	  //               "longitude": 120.63208770751953,
	  //               "latitude": 27.78545570373535
	  //             },
	  //             {
	  //               "site_id": 1387,
	  //               "id": "c193ce234fe96c1a250bd023",
	  //               "val": 2.4092655580078732,
	  //               "relation_val": 2.4092655580078732,
	  //               "name": "集味园（医学院三食堂）",
	  //               "type": "美食",
	  //               "longitude": 120.70689392089844,
	  //               "latitude": 27.923694610595703
	  //             },
	  //             {
	  //               "site_id": 2682,
	  //               "id": "668f86e3e94163d78a36ee0d",
	  //               "val": 3.4156210282016013,
	  //               "relation_val": 3.4156210282016013,
	  //               "name": "医学院",
	  //               "type": "行政区划/商圈",
	  //               "longitude": 120.66777038574219,
	  //               "latitude": 28.010732650756836
	  //             }
	  //           ]
	  //         }
	  //       ]
	  //     }
	  //   ]
	  // }
	}];

	function mock() {

	  d3.select('#Specification_view').style('height', 'calc(82% - 60px)');

	  d3.select('#map-view').style('top', 'calc(82% + 10px)');

	  d3.select('#semantic-view').style('top', 'calc(82% + 10px)');

	  var nodelist = __webpack_require__(336);
	  nodelist.data = listData;

	  listData.map(function (poi) {
	    var name = poi.name,
	        data = poi.data;

	    nodelist.append_node({
	      name: name,
	      data: data
	    });
	  });
	}

/***/ }),
/* 363 */
/***/ (function(module, exports) {

	'use strict';

	Object.defineProperty(exports, "__esModule", {
			value: true
	});

	/*
		Case Two 
		
		1. 温州大学

	*/

	var mock_sites = exports.mock_sites = [];
	var mock_sites_origin = exports.mock_sites_origin = [21938];

	// 大学
	mock_sites[0] = [
	// 21938 , 24817 

	// 927,8301,17101,2267,27063  //21938 的 neighbor
	8301, 21938, 27063, 16711, 9969, 12617, 12495, 3750, 10353, 27063, 21938, 17101, 15435, 1856, 16938, 17101, 21938, 927, 12617, 21938, 8301, 16938, 15435, 2267, 21938, 2267, 10353, 11155, 17461, 14223, 22415, 16711, 927];

	// 医院 
	// mock_sites[1] = [25950, 8176, 2682, 770, 18644, 8194, 13076, 1764, 2217, 14207, 25157, 13076, 28068, 14775, 23547, 17103, 2682, 23665, 11544, 25157]

	mock_sites[1] = [];

	var mock_traj_ids = exports.mock_traj_ids = [[//  温州大学
	'460078578421760', '460002032335584', '460027570805270', '460023885619324'], [// 温州医科大学  医院
	'460006627063118', '460006876047930', '460027577227015', '460023889136178']];

/***/ })
/******/ ]);